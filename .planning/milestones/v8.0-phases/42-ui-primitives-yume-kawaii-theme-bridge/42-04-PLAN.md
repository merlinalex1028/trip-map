---
phase: 42-ui-primitives-yume-kawaii-theme-bridge
plan: 04
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - apps/web/src/lib/charts/theme.ts
  - apps/web/src/lib/charts/register.ts
  - apps/web/src/components/common/BaseChart.vue
  - apps/web/src/components/common/BaseChart.spec.ts
autonomous: true
requirements: [DS-03]
must_haves:
  truths:
    - "App code can render ECharts options through a reusable Vue wrapper without importing chart modules ad hoc."
    - "Line, pie/donut, bar, and radar chart modules are centrally registered for Phase 47."
    - "BaseChart handles loading, empty, error, nonzero sizing, theme, and autoresize states without real travel data."
  artifacts:
    - "apps/web/src/lib/charts/theme.ts exports yumeKawaiiChartTheme and YUME_KAWAII_CHART_THEME."
    - "apps/web/src/lib/charts/register.ts registers ECharts modules and the yume-kawaii theme."
    - "apps/web/src/components/common/BaseChart.vue wraps vue-echarts."
  key_links:
    - "BaseChart.vue imports @/lib/charts/register for one-time registration."
    - "BaseChart.vue passes theme=YUME_KAWAII_CHART_THEME to VChart."
    - "BaseChart.spec.ts verifies loading, empty, error, and option render paths."
---

<objective>
Create the ECharts/vue-echarts chart foundation and Yume Kawaii chart theme required by DS-03.

Purpose: This gives later travel-memory dashboard work a single themed chart wrapper while keeping Phase 42 detached from real travel data.
Output: Central ECharts module registration, chart theme module, `BaseChart` wrapper, and focused smoke tests.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-CONTEXT.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-RESEARCH.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-VALIDATION.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-PATTERNS.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-UI-SPEC.md
@apps/web/src/styles/tokens.css
@apps/web/src/views/StatisticsPageView.vue
@apps/web/src/components/statistics/StatCard.vue
@apps/web/vitest.config.ts

<interfaces>
Required chart palette from `42-UI-SPEC.md`:
```ts
['#F75A9B', '#8B6FEF', '#5EA7F2', '#7ED9B6', '#F5A354']
```

Required Phase 42 chart wrapper states:
```ts
option?: YumeChartOption
loading?: boolean
empty?: boolean
error?: string | null
minHeight?: number
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register ECharts modules and Yume Kawaii theme</name>
  <files>apps/web/src/lib/charts/theme.ts, apps/web/src/lib/charts/register.ts</files>
  <read_first>
    - apps/web/package.json
    - apps/web/src/styles/tokens.css
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-CONTEXT.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-RESEARCH.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-VALIDATION.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `register.ts` imports `use` and `registerTheme` from `echarts/core`.
    - Test 2: `register.ts` registers `LineChart`, `PieChart`, `BarChart`, `RadarChart`, `TitleComponent`, `TooltipComponent`, `GridComponent`, `LegendComponent`, and `CanvasRenderer`.
    - Test 3: `theme.ts` exports `YUME_KAWAII_CHART_THEME` equal to `yume-kawaii` and palette colors `#F75A9B`, `#8B6FEF`, `#5EA7F2`, `#7ED9B6`, `#F5A354`.
  </behavior>
  <action>
    Create `apps/web/src/lib/charts/theme.ts` exporting `export const YUME_KAWAII_CHART_THEME = 'yume-kawaii'` and `export const yumeKawaiiChartTheme = { ... }` with exact color array `['#F75A9B', '#8B6FEF', '#5EA7F2', '#7ED9B6', '#F5A354']`, text color `#25146F`, muted axis color `#6F5B99`, split/grid line color `#E8DDF6`, tooltip background `rgba(255, 255, 255, 0.92)`, tooltip border `#E8DDF6`, and font family `Nunito Variable, Noto Sans SC, sans-serif`. Create `apps/web/src/lib/charts/register.ts` as a side-effect module importing `use` and `registerTheme` from `echarts/core`; import `LineChart`, `PieChart`, `BarChart`, `RadarChart` from `echarts/charts`; import `TitleComponent`, `TooltipComponent`, `GridComponent`, `LegendComponent`, `DatasetComponent`, and `TransformComponent` from `echarts/components`; import `CanvasRenderer` from `echarts/renderers`; call `use([...])` with those modules; call `registerTheme(YUME_KAWAII_CHART_THEME, yumeKawaiiChartTheme)`. Do not use `import * as echarts from 'echarts'` or a full ECharts import.
  </action>
  <verify>
    <automated>npm --prefix apps/web run typecheck</automated>
    <automated>test "$(rg -n "from 'echarts'$|from \"echarts\"" apps/web/src/lib/charts apps/web/src/components/common 2>/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/charts/theme.ts` contains `YUME_KAWAII_CHART_THEME = 'yume-kawaii'`.
    - `apps/web/src/lib/charts/theme.ts` contains `#F75A9B`, `#8B6FEF`, `#5EA7F2`, `#7ED9B6`, and `#F5A354`.
    - `apps/web/src/lib/charts/register.ts` contains `from 'echarts/core'`.
    - `apps/web/src/lib/charts/register.ts` contains `LineChart`, `PieChart`, `BarChart`, and `RadarChart`.
    - `apps/web/src/lib/charts/register.ts` contains `CanvasRenderer`.
    - `apps/web/src/lib/charts/register.ts` contains `registerTheme(YUME_KAWAII_CHART_THEME, yumeKawaiiChartTheme)`.
    - `apps/web/src/lib/charts/register.ts` does not contain `from 'echarts'`.
  </acceptance_criteria>
  <done>ECharts registration is centralized, tree-shakeable, and uses the concrete Yume Kawaii chart theme.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build BaseChart wrapper and state tests</name>
  <files>apps/web/src/components/common/BaseChart.vue, apps/web/src/components/common/BaseChart.spec.ts</files>
  <read_first>
    - apps/web/src/lib/charts/theme.ts
    - apps/web/src/lib/charts/register.ts
    - apps/web/src/views/StatisticsPageView.vue
    - apps/web/src/components/statistics/StatCard.vue
    - apps/web/vitest.config.ts
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-CONTEXT.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-RESEARCH.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-VALIDATION.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-PATTERNS.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: mounting `BaseChart` with `loading` renders `data-state="loading"` and `aria-busy="true"`.
    - Test 2: mounting `BaseChart` with `empty` renders `data-state="empty"` and text `还没有旅行记录`.
    - Test 3: mounting `BaseChart` with `error="暂时没有加载成功，请稍后重试。"` renders `role="alert"` and `data-state="error"`.
    - Test 4: mounting with a demo line option renders the mocked `VChart` with theme `yume-kawaii` and autoresize configuration.
  </behavior>
  <action>
    Create `apps/web/src/components/common/BaseChart.vue` using `<script setup lang="ts">`. Import `VChart` from `vue-echarts`, import ECharts option types from `echarts/core`, `echarts/charts`, and `echarts/components`, import `YUME_KAWAII_CHART_THEME` from `@/lib/charts/theme`, and import `@/lib/charts/register` for side effects. Define `type YumeChartOption = ComposeOption<LineSeriesOption | PieSeriesOption | BarSeriesOption | RadarSeriesOption | GridComponentOption | LegendComponentOption | TitleComponentOption | TooltipComponentOption>`. Props must be `option?: YumeChartOption`, `loading?: boolean`, `empty?: boolean`, `error?: string | null`, and `minHeight?: number`, with `loading: false`, `empty: false`, `error: null`, and `minHeight: 280`. The template must render a root `section` with `data-base-chart`, `aria-busy`, and inline min-height style. Render error first with `role="alert"` and text from `error`; render empty next with heading `还没有旅行记录` and body `先回到地图，选择一个真实地点留下第一枚足迹。`; render loading with a stable pastel skeleton; otherwise render `<VChart class="base-chart__canvas" :option="option" :theme="YUME_KAWAII_CHART_THEME" :loading="loading" :autoresize="{ throttle: 100 }" />`. Do not import stores, APIs, or real travel data in Phase 42. Add `apps/web/src/components/common/BaseChart.spec.ts`; mock `vue-echarts` as a simple component exposing received props so tests do not require canvas rendering.
  </action>
  <verify>
    <automated>npm --prefix apps/web run test -- src/components/common/BaseChart.spec.ts</automated>
    <automated>npm --prefix apps/web run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/common/BaseChart.vue` contains `data-base-chart`.
    - `apps/web/src/components/common/BaseChart.vue` contains `theme="YUME_KAWAII_CHART_THEME"` or `:theme="YUME_KAWAII_CHART_THEME"`.
    - `apps/web/src/components/common/BaseChart.vue` contains `:autoresize="{ throttle: 100 }"`.
    - `apps/web/src/components/common/BaseChart.vue` contains `还没有旅行记录`.
    - `apps/web/src/components/common/BaseChart.vue` contains `role="alert"`.
    - `apps/web/src/components/common/BaseChart.vue` imports `@/lib/charts/register`.
    - `apps/web/src/components/common/BaseChart.spec.ts` contains `data-state="loading"`, `data-state="empty"`, and `data-state="error"` assertions.
    - `rg -n "use[A-Za-z]*Store|fetch\\(|/api|TravelRecord" apps/web/src/components/common/BaseChart.vue apps/web/src/components/common/BaseChart.spec.ts` returns no matches.
  </acceptance_criteria>
  <done>`BaseChart` renders themed options and foundation states with stable sizing and no data-source coupling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller options -> ECharts renderer | Component callers pass ECharts option objects into a canvas/chart renderer. |
| future user data -> chart tooltip | Later phases may derive tooltip labels from account travel records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02 | Tampering / XSS | `BaseChart`, ECharts tooltip options | mitigate | Phase 42 demo/wrapper avoids raw user HTML and documents plain option input; no store/API data is read in this plan. |
| T-42-04 | Tampering | ECharts dependencies | mitigate | Plan 01 pins `echarts@6.0.0` and `vue-echarts@8.0.1`; this plan uses tree-shakeable imports and typecheck. |
| T-42-03 | Information Disclosure | chart demo in `/__ui` | transfer | Chart demo integration is handled in Plan 05 under the dev-only route guard. |
</threat_model>

<verification>
Run after this plan:
- `npm --prefix apps/web run test -- src/components/common/BaseChart.spec.ts`
- `npm --prefix apps/web run typecheck`
- `test "$(rg -n "from 'echarts'$|from \"echarts\"" apps/web/src/lib/charts apps/web/src/components/common 2>/dev/null | wc -l | tr -d ' ')" = "0"`
</verification>

<success_criteria>
- ECharts modules for line, pie/donut, bar, and radar are registered centrally.
- `BaseChart` uses the `yume-kawaii` theme, nonzero min-height, loading/empty/error states, and autoresize.
- No real travel data, stores, APIs, or raw user HTML are introduced in Phase 42 chart code.
</success_criteria>

<output>
After completion, create `.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-04-SUMMARY.md`.
</output>

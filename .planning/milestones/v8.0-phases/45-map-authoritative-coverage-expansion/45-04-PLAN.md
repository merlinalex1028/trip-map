---
phase: "45-map-authoritative-coverage-expansion"
plan: "04"
type: execute
wave: 2
depends_on: ["45-01", "45-02", "45-03"]
files_modified:
  - "apps/server/test/records-travel.e2e-spec.ts"
  - "apps/server/test/auth-bootstrap.e2e-spec.ts"
  - "apps/web/src/services/timeline.spec.ts"
  - "apps/web/src/views/StatisticsPageView.spec.ts"
autonomous: true
requirements:
  - COV-01
  - COV-02
  - COV-03
  - COV-04
must_haves:
  truths:
    - "Complete Phase 45 canonical identity samples save through POST /records without weakening existing SessionAuthGuard or overseas authoritative validation."
    - "Rejected authoritative record breakpoints report exact backend reasons and do not become frontend-saveable payloads."
    - "Saved Phase 45 samples replay consistent displayName, typeLabel, parentLabel, and subtitle through bootstrap, journal, and memories-derived refresh behavior."
    - "Final focused gates separate DB/Prisma environment failures from behavioral assertion failures."
    - "D-05: Complete canonical identity samples are saved and replayed before broader coverage work."
    - "D-06: Record/replay samples are reproducible through the current flow and focused tests."
    - "D-07: Fallback lookup remains explanatory-only and never becomes a record payload."
    - "D-13: Final verification uses the Phase 45 fixed/classified sample matrix."
    - "D-14: The matrix proves resolve to POST /records to auth/bootstrap replay to journal/memories-derived labels and grouping."
    - "D-15: Final verification includes manifest/highlight assertions and excludes full Playwright visual QA."
    - "D-16: Record/replay failures expose canonical identity fields and exact backend blocking reasons."
  artifacts:
    - path: "apps/server/test/records-travel.e2e-spec.ts"
      provides: "Phase 45 record API save/reject coverage"
    - path: "apps/server/test/auth-bootstrap.e2e-spec.ts"
      provides: "Phase 45 canonical label replay coverage"
    - path: "apps/web/src/services/timeline.spec.ts"
      provides: "journal-derived label consistency coverage"
    - path: "apps/web/src/views/StatisticsPageView.spec.ts"
      provides: "memories-derived metadata refresh consistency coverage"
  key_links:
    - from: "apps/server/test/records-travel.e2e-spec.ts"
      to: "apps/server/test/phase45-coverage-cases.ts"
      via: "PHASE45_RECORD_API_COVERAGE_CASES import"
      pattern: "PHASE45_RECORD_API_COVERAGE_CASES"
    - from: "apps/web/src/services/timeline.spec.ts"
      to: "packages/contracts/src/fixtures.ts"
      via: "existing canonical resolved place fixture"
      pattern: "PHASE28_RESOLVED_CALIFORNIA|PHASE28_NEW_OVERSEAS_RECORD_FIXTURES"
---

<objective>
Close Phase 45 with record API, bootstrap replay, journal-derived, memories-derived, and final command-level verification.

Purpose: implement D-05, D-06, D-13, D-14, D-15, and D-16 while preserving D-07 and the backend authoritative guard.
Output: DB-backed record/replay tests, frontend derived-view consistency tests, and full Phase 45 verification evidence.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-CONTEXT.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-RESEARCH.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-PATTERNS.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-UI-SPEC.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-VALIDATION.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-05-SUMMARY.md
@apps/server/test/phase45-coverage-cases.ts
@apps/server/test/records-travel.e2e-spec.ts
@apps/server/test/auth-bootstrap.e2e-spec.ts
@apps/server/src/modules/records/records.service.ts
@apps/server/src/modules/auth/guards/session-auth.guard.ts
@apps/server/src/modules/canonical-places/place-metadata-catalog.ts
@apps/web/src/services/timeline.ts
@apps/web/src/services/timeline.spec.ts
@apps/web/src/views/StatisticsPageView.vue
@apps/web/src/views/StatisticsPageView.spec.ts
@packages/contracts/src/records.ts
@packages/contracts/src/fixtures.ts

<interfaces>
Existing backend guard to preserve:
```ts
private assertAuthoritativeOverseasRecord(input: CreateTravelRecordDto): void
```

Journal-derived label fields:
```ts
displayName: record.displayName
parentLabel: record.parentLabel
subtitle: record.subtitle
typeLabel: record.typeLabel
```
</interfaces>
</context>

<must_haves>
- COV-04: fixed samples preserve canonical titles and grouping across record replay, journal entries, and memories refresh behavior.
- Security: preserve `SessionAuthGuard` and `assertAuthoritativeOverseasRecord`; do not loosen overseas payload validation or allow anonymous writes.
- Environment handling: if Prisma-backed tests fail before behavioral assertions due to local DB/engine health, the SUMMARY must label that as environment availability and still report non-DB focused results.
- D-05: complete canonical identity samples are saved and replayed before any broader coverage expansion.
- D-06: record/replay cases use reproducible current-flow samples from the Phase 45 matrix.
- D-07: fallback lookup never becomes a record payload; rejected/fallback cases remain blocked or explanatory.
- D-13: verification is scoped to the Phase 45 fixed/classified sample matrix.
- D-14: the plan proves the chain from resolve to `POST /records`, auth/bootstrap replay, journal labels, and memories grouping refresh.
- D-15: final verification includes manifest/highlight assertions from Wave 1 and does not require full Playwright visual QA.
- D-16: record/replay failures name canonical identity fields and exact backend blocking reasons.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Extend record API and bootstrap replay coverage for Phase 45 samples</name>
  <files>apps/server/test/records-travel.e2e-spec.ts, apps/server/test/auth-bootstrap.e2e-spec.ts</files>
  <read_first>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-CONTEXT.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-RESEARCH.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-PATTERNS.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-VALIDATION.md</file>
    <file>apps/server/test/phase45-coverage-cases.ts</file>
    <file>apps/server/test/records-travel.e2e-spec.ts</file>
    <file>apps/server/test/auth-bootstrap.e2e-spec.ts</file>
    <file>apps/server/src/modules/records/records.service.ts</file>
    <file>apps/server/src/modules/auth/guards/session-auth.guard.ts</file>
    <file>apps/server/src/modules/canonical-places/place-metadata-catalog.ts</file>
  </read_first>
  <action>
    In `records-travel.e2e-spec.ts`, import `PHASE45_RECORD_API_COVERAGE_CASES`. Add a test named exactly `POST /records saves Phase 45 complete canonical identity samples` that selects the `record_us_california_authoritative_saveable` and `record_ca_british_columbia_authoritative_saveable` cases, builds payloads from `getCanonicalPlaceSummaryById`, posts each payload with the existing authenticated `sidCookie`, and asserts status `201`, `placeId`, `boundaryId`, `datasetVersion`, `displayName`, `typeLabel`, `parentLabel`, and `subtitle` equal the catalog summary. Add a test named exactly `POST /records rejects Phase 45 authoritative breakpoints with exact reasons` that posts `record_mx_jalisco_authoritative_rejected` and expects status `400` with exact message `Overseas travel record is outside the current authoritative overseas support catalog.`, then posts `record_us_california_forged_metadata_rejected` with `displayName: 'Forged California'` and expects status `400` plus message containing `Overseas travel record metadata must match authoritative catalog: displayName.`. Do not modify `SessionAuthGuard`, `assertAuthoritativeOverseasRecord`, or DTO validation.

    In `auth-bootstrap.e2e-spec.ts`, add a test named exactly `GET /auth/bootstrap replays Phase 45 fixed coverage labels without recomputing fallback text`. Create records for the same two saveable Phase 45 record cases using canonical catalog summaries, call `/auth/bootstrap`, and assert each replayed record contains exact `placeId`, `boundaryId`, `datasetVersion`, `displayName`, `typeLabel`, `parentLabel`, and `subtitle`. The test must not use fallback country/region lookup values.
  </action>
  <acceptance_criteria>
    - `rg -n "POST /records saves Phase 45 complete canonical identity samples|POST /records rejects Phase 45 authoritative breakpoints with exact reasons|PHASE45_RECORD_API_COVERAGE_CASES|Forged California|Overseas travel record is outside the current authoritative overseas support catalog" apps/server/test/records-travel.e2e-spec.ts` finds record API coverage.
    - `rg -n "GET /auth/bootstrap replays Phase 45 fixed coverage labels without recomputing fallback text|PHASE45_RECORD_API_COVERAGE_CASES|displayName|typeLabel|parentLabel|subtitle" apps/server/test/auth-bootstrap.e2e-spec.ts` finds replay coverage.
    - `rg -n "assertAuthoritativeOverseasRecord|SessionAuthGuard" apps/server/src/modules/records/records.service.ts apps/server/src/modules/auth/guards/session-auth.guard.ts` confirms guard files still contain the expected protections.
    - `pnpm --filter @trip-map/server test -- test/records-travel.e2e-spec.ts` exits 0 when local Prisma/database health is available; if it fails in `beforeAll` or Prisma engine startup before assertions, record that separately in the SUMMARY and continue with non-DB focused gates.
    - `pnpm --filter @trip-map/server test -- test/auth-bootstrap.e2e-spec.ts` exits 0 when local Prisma/database health is available; if it fails in environment setup before assertions, record that separately in the SUMMARY and continue with non-DB focused gates.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/server test -- test/records-travel.e2e-spec.ts</automated>
  </verify>
  <done>DB-backed server coverage proves Phase 45 complete identities can save/replay and authoritative rejected cases remain rejected with exact backend reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Extend journal and memories derived-view consistency tests</name>
  <files>apps/web/src/services/timeline.spec.ts, apps/web/src/views/StatisticsPageView.spec.ts</files>
  <read_first>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-CONTEXT.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-RESEARCH.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-PATTERNS.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-VALIDATION.md</file>
    <file>apps/web/src/services/timeline.ts</file>
    <file>apps/web/src/services/timeline.spec.ts</file>
    <file>apps/web/src/views/StatisticsPageView.vue</file>
    <file>apps/web/src/views/StatisticsPageView.spec.ts</file>
    <file>packages/contracts/src/fixtures.ts</file>
    <file>packages/contracts/src/records.ts</file>
  </read_first>
  <action>
    In `timeline.spec.ts`, add a test named exactly `preserves Phase 45 canonical labels in journal-derived entries`. Use a complete authoritative sample such as `PHASE28_RESOLVED_CALIFORNIA` or a `PHASE28_NEW_OVERSEAS_RECORD_FIXTURES` entry that corresponds to a Phase 45 saveable case. Build a `TravelRecord` from the resolved place, override `id: 'phase45-journal-record'`, `startDate: '2025-10-01'`, and assert the resulting `buildTimelineEntries` entry keeps exact `displayName`, `typeLabel`, `parentLabel`, and `subtitle` from the record.

    In `StatisticsPageView.spec.ts`, add a test named exactly `refreshes memories when Phase 45 canonical grouping fields change`. Mount the view authenticated with one Phase 45 complete sample record, let the first `fetchStats` resolve, replace the record with the same `id` and `placeId` but changed `parentLabel`, `displayName`, `typeLabel`, and `subtitle`, then assert `fetchStatsMock` is called a second time. Also assert the populated text remains the existing memories copy style and does not render fallback explanatory-only copy such as `这里暂时只能用于查看位置，还不能留下足迹。`.
  </action>
  <acceptance_criteria>
    - `rg -n "preserves Phase 45 canonical labels in journal-derived entries|phase45-journal-record|displayName|typeLabel|parentLabel|subtitle" apps/web/src/services/timeline.spec.ts` finds journal label assertions.
    - `rg -n "refreshes memories when Phase 45 canonical grouping fields change|fetchStatsMock|parentLabel|displayName|typeLabel|subtitle|这里暂时只能用于查看位置，还不能留下足迹。" apps/web/src/views/StatisticsPageView.spec.ts` finds memories refresh and forbidden fallback-copy assertions.
    - `pnpm --filter @trip-map/web test -- src/services/timeline.spec.ts src/views/StatisticsPageView.spec.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/services/timeline.spec.ts src/views/StatisticsPageView.spec.ts</automated>
  </verify>
  <done>Journal and memories tests prove saved Phase 45 samples keep persisted canonical labels and grouping signals across derived views.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client -> `POST /records` | User-controlled record payload enters the backend write path. |
| records DB -> `/auth/bootstrap` | Persisted records replay into frontend stores after session restore. |
| store records -> journal/memories views | Persisted canonical labels drive derived UI and stats refresh behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04-01 | Elevation of Privilege | `records.controller.ts` | mitigate | Preserve `SessionAuthGuard`; tests use authenticated `sidCookie` and do not add anonymous write paths. |
| T-45-04-02 | Tampering | `assertAuthoritativeOverseasRecord` | mitigate | Rejection tests assert unsupported and forged overseas payloads still return 400 with exact guard messages. |
| T-45-04-03 | Repudiation | record/replay consistency | mitigate | Bootstrap tests assert exact persisted canonical fields for Phase 45 samples. |
| T-45-04-04 | Information Disclosure | test output | accept | Logs/tests use canonical place ids and public metadata only; no passwords or session tokens are printed. |
| T-45-04-05 | Tampering | stats grouping metadata drift | mitigate | Memories spec asserts metadata-field changes trigger stats refresh instead of stale grouping. |
</threat_model>

<verification>
Focused non-DB commands:
`pnpm --filter @trip-map/server test -- test/canonical-resolve.e2e-spec.ts`
`pnpm --filter @trip-map/web test -- src/services/footprint-availability.spec.ts src/components/map-popup/PointSummaryCard.spec.ts`
`pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts`
`pnpm --filter @trip-map/web test -- src/services/timeline.spec.ts src/views/StatisticsPageView.spec.ts`

DB-backed commands; report Prisma/database setup failures separately from behavioral failures:
`pnpm --filter @trip-map/server test -- test/records-travel.e2e-spec.ts`
`pnpm --filter @trip-map/server test -- test/auth-bootstrap.e2e-spec.ts`

Final build gate:
`pnpm --filter @trip-map/web build`
</verification>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | Phase 45 | 梳理并尽量补齐当前 server 能识别但前端不可保存的地点，让可识别地点更稳定地成为可留下足迹的地点 | 45-01, 45-02, 45-03, 45-04 | COVERED | Runtime matrix, frontend classifier, real map wiring, record/replay/derived gates cover the full chain.
REQ | COV-01 | 开发者可以识别类别、数量和原因 | 45-01, 45-02, 45-04 | COVERED | Matrix counts, blocking reasons, categories, and targeted failure output.
REQ | COV-02 | Complete canonical identity samples get boundaryId / geometry / metadata plumbing | 45-01, 45-03, 45-04 | COVERED | Resolve/catalog/manifest checks, highlight assertions, and record save coverage.
REQ | COV-03 | Still-unsaveable locations explain reason and avoid fake submit | 45-02, 45-03, 45-04 | COVERED | Disabled CTA, friendly copy, blocked dialog entry, backend rejection tests.
REQ | COV-04 | Map highlight, journal, and memories titles/grouping stay consistent | 45-03, 45-04 | COVERED | Manifest/highlight tests plus bootstrap, timeline, and memories refresh assertions.
RESEARCH | Runtime breakpoint matrix | Current gaps should be represented by real resolve/frontend/record/replay samples | 45-01, 45-04 | COVERED | Uses focused matrix, not a full-dataset scanner.
RESEARCH | Frontend availability classifier | Boolean guard must become reason-returning predicate | 45-02, 45-03 | COVERED | `getFootprintAvailability` is the single source for CTA and dialog gate.
RESEARCH | DB-backed record gates may depend on local Prisma health | Separate environment failures from behavioral failures | 45-04 | COVERED | Plan requires explicit environment reporting for Prisma setup failures.
CONTEXT | D-01-D-04 | Runtime breakpoint audit and exact save-blocking reasons | 45-01, 45-02, 45-03 | COVERED | Canonical resolve, frontend guard, manifest/catalog, record, and replay reasons are planned.
CONTEXT | D-05-D-08 | Prioritize complete identity fixes; fallback/new data sources deferred | 45-01, 45-03, 45-04 | COVERED | Complete identity samples first; no fallback payloads or new geodata ingestion.
CONTEXT | D-09-D-12 | Warm category copy, disabled CTA, exact internal reason mapping | 45-02, 45-03 | COVERED | UI-SPEC category copy and forbidden term tests are explicit.
CONTEXT | D-13-D-16 | Sample matrix, full replay chain, manifest/highlight assertions, targeted failure output | 45-01, 45-03, 45-04 | COVERED | Resolve to save to replay to derived views is planned with focused commands.
</source_audit>

<success_criteria>
COV-01 through COV-04 are closed across the end-to-end chain: Phase 45 sample matrix exposes reasons, complete identities save/highlight/replay when authoritative, blocked/fallback cases stay explanatory-only, and canonical labels stay consistent in map, journal, and memories-derived behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/45-map-authoritative-coverage-expansion/45-04-SUMMARY.md`
</output>

---
phase: "45-map-authoritative-coverage-expansion"
plan: "02"
type: execute
wave: 0
depends_on: []
files_modified:
  - "apps/web/src/services/footprint-availability.ts"
  - "apps/web/src/services/footprint-availability.spec.ts"
  - "apps/web/src/components/map-popup/PointSummaryCard.vue"
  - "apps/web/src/components/map-popup/PointSummaryCard.spec.ts"
autonomous: true
requirements:
  - COV-01
  - COV-03
must_haves:
  truths:
    - "Frontend saveability is classified by a reason-returning service, not split boolean checks."
    - "PointSummaryCard keeps the disabled 留下足迹 CTA visible for recognized-but-unsaveable places and renders only friendly category copy."
    - "Tests assert both exact internal technical reasons and stable user-facing categories/copy."
    - "D-03: Frontend developer visibility comes from focused service/component specs, not a coverage UI."
    - "D-04: Frontend breakpoints preserve exact save-blocking reasons."
    - "D-09: User-facing unavailable-place copy stays warm and avoids implementation field names."
    - "D-10: UI copy is grouped into a small set of stable friendly categories."
    - "D-11: Disabled 留下足迹 CTA remains visible and cannot open the date dialog for blocked points."
    - "D-12: Technical blocking reasons map explicitly to stable user-facing categories and tests assert both."
    - "D-16: Blocked-case failures expose exact internal reason and category without noisy full-field diffs."
  artifacts:
    - path: "apps/web/src/services/footprint-availability.ts"
      provides: "single-source saveability classifier and reason/category/copy mapping"
    - path: "apps/web/src/services/footprint-availability.spec.ts"
      provides: "technical reason plus friendly category matrix coverage"
    - path: "apps/web/src/components/map-popup/PointSummaryCard.vue"
      provides: "user-facing disabled CTA and unavailable reason rendering"
  key_links:
    - from: "apps/web/src/components/map-popup/PointSummaryCard.vue"
      to: "apps/web/src/services/footprint-availability.ts"
      via: "FootprintUnavailableCategory and copy props"
      pattern: "FootprintUnavailableCategory|footprintUnavailable"
    - from: "apps/web/src/services/footprint-availability.ts"
      to: "apps/web/src/services/geometry-manifest.ts"
      via: "boundaryId manifest lookup before saveable result"
      pattern: "getGeometryManifestEntry"
---

<objective>
Create the frontend footprint availability classifier and update the map popup to render Phase 45 friendly unavailable categories.

Purpose: implement D-03, D-04, D-09, D-10, D-11, D-12, and D-16 by preserving exact technical reasons in tests while keeping user-facing copy warm and implementation-free.
Output: `footprint-availability` service/spec plus `PointSummaryCard` category/copy rendering.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-CONTEXT.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-RESEARCH.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-PATTERNS.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-UI-SPEC.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-VALIDATION.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md
@apps/web/src/types/map-point.ts
@apps/web/src/services/geometry-manifest.ts
@apps/web/src/components/map-popup/PointSummaryCard.vue
@apps/web/src/components/map-popup/PointSummaryCard.spec.ts

<interfaces>
Map point and summary surface fields:
```ts
export interface FootprintPlaceSnapshot {
  placeId: string
  boundaryId: string | null
  placeKind: PlaceKind
  datasetVersion: string
  displayName: string
  regionSystem: 'CN' | 'OVERSEAS'
  adminType: ChinaAdminType | 'ADMIN1' | null
  typeLabel: string | null
  parentLabel: string | null
  subtitle: string | null
}
```

Required category copy from 45-UI-SPEC:
```ts
map_data_unavailable: '已识别到这个地点，但地图数据还不够完整，暂时不能保存足迹。'
place_not_precise_enough: '已识别到这个地点，但还需要更稳定的地点信息才能保存足迹。'
outside_supported_map: '这里暂时只能用于查看位置，还不能留下足迹。'
temporarily_unavailable: '这个地点暂时还不能保存足迹，请稍后再试。'
```
</interfaces>
</context>

<must_haves>
- COV-01: developer visibility includes a frontend matrix of exact blocking reasons and friendly categories.
- COV-03: blocked recognized/fallback places show disabled `data-footprint-cta="true"` and visible `data-footprint-unavailable-reason`, with no date dialog entry path from the card.
- UI safety: visible copy must not contain `boundaryId`, `metadata`, `manifest`, `geometry`, `canonical`, `datasetVersion`, `authoritative`, `frontend_guard`, `record_authoritative_rejected`, `点亮`, or `再留一枚足迹`.
- D-03: developer visibility comes from focused service/component specs and failure output, with no dev-only coverage UI.
- D-04: frontend classification preserves exact internal blocking reasons for tests.
- D-09: unavailable-place copy is warm and does not expose implementation field names.
- D-10: UI uses the four stable categories `map_data_unavailable`, `place_not_precise_enough`, `outside_supported_map`, and `temporarily_unavailable`.
- D-11: recognized-but-unsaveable cards keep a disabled `留下足迹` CTA and do not enter the date dialog.
- D-12: `mapFootprintBlockingReasonToCategory` and tests maintain explicit technical-reason to product-category mapping.
- D-16: blocked-case tests assert exact internal reason, category, and safe copy so failures identify the blocking reason without noisy full diffs.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Create the footprint availability classifier and reason matrix tests</name>
  <files>apps/web/src/services/footprint-availability.ts, apps/web/src/services/footprint-availability.spec.ts</files>
  <read_first>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-CONTEXT.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-RESEARCH.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-PATTERNS.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-UI-SPEC.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-VALIDATION.md</file>
    <file>apps/web/src/services/footprint-availability.ts</file>
    <file>apps/web/src/services/footprint-availability.spec.ts</file>
    <file>apps/web/src/types/map-point.ts</file>
    <file>apps/web/src/services/geometry-manifest.ts</file>
    <file>apps/web/src/services/geometry-manifest.spec.ts</file>
  </read_first>
  <action>
    Create `apps/web/src/services/footprint-availability.ts` as a pure service. Export `type FootprintBlockingReason = 'missing_boundary_id' | 'missing_geometry_manifest' | 'missing_metadata_catalog' | 'frontend_guard_blocked' | 'missing_canonical_identity' | 'fallback_explanatory_only' | 'record_authoritative_rejected'`, `type FootprintUnavailableCategory = 'map_data_unavailable' | 'place_not_precise_enough' | 'outside_supported_map' | 'temporarily_unavailable'`, `FOOTPRINT_UNAVAILABLE_CATEGORY_COPY`, `FORBIDDEN_FOOTPRINT_COPY_TERMS`, `mapFootprintBlockingReasonToCategory(reason)`, `getFootprintUnavailableCopy(category)`, and `getFootprintAvailability(surface, options?)`.

    `getFootprintAvailability` must return a union: `{ saveable: true; reason: null; category: null; copy: null; snapshot: FootprintPlaceSnapshot }` or `{ saveable: false; reason: FootprintBlockingReason; category: FootprintUnavailableCategory; copy: string; snapshot: null }`. Classification order must be:
    1. `options?.recordAuthoritativeRejected === true` -> `record_authoritative_rejected`.
    2. `surface === null` or `surface.mode === 'candidate-select'` -> `missing_canonical_identity`.
    3. point has `fallbackNotice` and lacks `placeId` or `placeKind` -> `fallback_explanatory_only`.
    4. missing `placeId`, `placeKind`, or `datasetVersion` -> `missing_canonical_identity`.
    5. missing `boundaryId` -> `missing_boundary_id`.
    6. `options?.metadataCatalogStatus === 'missing'` -> `missing_metadata_catalog`.
    7. no `getGeometryManifestEntry(boundaryId)` and `surface.boundarySupportState !== 'supported'` -> `missing_geometry_manifest`.
    8. missing `regionSystem`, `adminType`, `typeLabel`, or `parentLabel` -> `frontend_guard_blocked`.
    9. otherwise saveable with a snapshot built from the point fields, using `displayName: point.name` and `subtitle: point.subtitle ?? point.cityContextLabel ?? null`.

    Create `apps/web/src/services/footprint-availability.spec.ts` with table tests for all seven technical reasons. Every case must assert `availability.saveable`, exact `reason`, exact `category`, exact `copy`, and `snapshot === null` for blocked cases. Add a saveable case for `us-california` with `boundaryId: 'ne-admin1-us-california'` that asserts `snapshot.placeId`, `snapshot.boundaryId`, `snapshot.typeLabel`, `snapshot.parentLabel`, and `copy === null`. Add a copy safety test that loops through `FOOTPRINT_UNAVAILABLE_CATEGORY_COPY` and asserts no forbidden term appears in any copy.
  </action>
  <acceptance_criteria>
    - `rg -n "FootprintBlockingReason|FootprintUnavailableCategory|FOOTPRINT_UNAVAILABLE_CATEGORY_COPY|FORBIDDEN_FOOTPRINT_COPY_TERMS|getFootprintAvailability|mapFootprintBlockingReasonToCategory" apps/web/src/services/footprint-availability.ts` finds all exported contracts.
    - `rg -n "missing_boundary_id|missing_geometry_manifest|missing_metadata_catalog|frontend_guard_blocked|missing_canonical_identity|fallback_explanatory_only|record_authoritative_rejected" apps/web/src/services/footprint-availability.spec.ts` finds all seven technical reason cases.
    - `rg -n "已识别到这个地点，但地图数据还不够完整，暂时不能保存足迹。|已识别到这个地点，但还需要更稳定的地点信息才能保存足迹。|这里暂时只能用于查看位置，还不能留下足迹。|这个地点暂时还不能保存足迹，请稍后再试。" apps/web/src/services/footprint-availability.ts apps/web/src/services/footprint-availability.spec.ts` finds all four exact UI-SPEC copies.
    - `pnpm --filter @trip-map/web test -- src/services/footprint-availability.spec.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/services/footprint-availability.spec.ts</automated>
  </verify>
  <done>The frontend classifier returns exact technical reasons, stable categories, safe copy, and a snapshot only when the same predicate says the point is saveable.</done>
</task>

<task type="auto">
  <name>Task 2: Render friendly unavailable categories in PointSummaryCard</name>
  <files>apps/web/src/components/map-popup/PointSummaryCard.vue, apps/web/src/components/map-popup/PointSummaryCard.spec.ts</files>
  <read_first>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-CONTEXT.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-UI-SPEC.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md</file>
    <file>apps/web/src/components/map-popup/PointSummaryCard.vue</file>
    <file>apps/web/src/components/map-popup/PointSummaryCard.spec.ts</file>
    <file>apps/web/src/services/footprint-availability.ts</file>
    <file>apps/web/src/types/map-point.ts</file>
  </read_first>
  <action>
    In `PointSummaryCard.vue`, import `FOOTPRINT_UNAVAILABLE_CATEGORY_COPY` and `type FootprintUnavailableCategory`. Add props `footprintUnavailableCategory?: FootprintUnavailableCategory | null` and `footprintUnavailableCopy?: string | null` with defaults `null`. Replace the current hardcoded unavailable text with a computed `displayedUnavailableCopy` that uses `props.footprintUnavailableCopy`, then `FOOTPRINT_UNAVAILABLE_CATEGORY_COPY[props.footprintUnavailableCategory]`, then `FOOTPRINT_UNAVAILABLE_CATEGORY_COPY.temporarily_unavailable`. Render the reason container with `data-footprint-unavailable-reason`, `role="note"`, and `:data-footprint-unavailable-category="footprintUnavailableCategory ?? 'temporarily_unavailable'"`. Keep `data-footprint-cta="true"` on the CTA and make the CTA label always `留下足迹` for saved and unsaved non-candidate cards. Remove visible `再留一枚足迹`.

    Update `PointSummaryCard.spec.ts` to assert all four category copies render when `isIlluminatable=false` and the matching category prop is passed. Add an assertion that saved cards still render CTA text `留下足迹` and do not render `再留一枚足迹`, `再留一次足迹`, or `再记一次`. Add a copy-safety assertion that wrapper text for blocked cards does not contain `boundaryId`, `metadata`, `manifest`, `geometry`, `canonical`, `datasetVersion`, `authoritative`, `frontend_guard`, or `record_authoritative_rejected`. Keep the disabled button behavior: clicking disabled `data-footprint-cta="true"` must not emit `leaveFootprint`.
  </action>
  <acceptance_criteria>
    - `rg -n "FootprintUnavailableCategory|FOOTPRINT_UNAVAILABLE_CATEGORY_COPY|footprintUnavailableCategory|footprintUnavailableCopy|data-footprint-unavailable-category" apps/web/src/components/map-popup/PointSummaryCard.vue` finds the new props and rendering contract.
    - `rg -n "map_data_unavailable|place_not_precise_enough|outside_supported_map|temporarily_unavailable|已识别到这个地点，但地图数据还不够完整，暂时不能保存足迹。|这里暂时只能用于查看位置，还不能留下足迹。" apps/web/src/components/map-popup/PointSummaryCard.spec.ts` finds category tests and exact copy assertions.
    - `rg -n "再留一枚足迹|再留一次足迹|再记一次|点亮" apps/web/src/components/map-popup/PointSummaryCard.vue` returns no lines.
    - `pnpm --filter @trip-map/web test -- src/services/footprint-availability.spec.ts src/components/map-popup/PointSummaryCard.spec.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/services/footprint-availability.spec.ts src/components/map-popup/PointSummaryCard.spec.ts</automated>
  </verify>
  <done>`PointSummaryCard` keeps the blocked CTA visible, uses friendly category copy only, and preserves exact internal reasons in service tests instead of visible UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolved/fallback point -> frontend save gate | Runtime point data decides whether the user can enter the save dialog. |
| technical reason -> visible popup copy | Internal failure reasons are mapped into user-facing text. |
| disabled CTA -> date dialog event | A blocked UI control must not emit a path into record creation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02-01 | Tampering | `getFootprintAvailability` fallback handling | mitigate | Missing canonical identity and fallback explanatory-only cases return `saveable: false`, `snapshot: null`, and tests assert no save payload fields are synthesized. |
| T-45-02-02 | Information Disclosure | `PointSummaryCard.vue` unavailable copy | mitigate | Visible copy is limited to four UI-SPEC category strings; tests assert forbidden implementation terms do not appear in wrapper text. |
| T-45-02-03 | Elevation of Privilege | disabled `留下足迹` CTA | mitigate | Component keeps a real disabled button and tests assert no `leaveFootprint` event for blocked cards. |
| T-45-02-04 | Spoofing | forged record authoritative rejection surfaced as UI state | mitigate | `record_authoritative_rejected` maps to `temporarily_unavailable` copy only; the backend authoritative guard remains unchanged in later plans. |
</threat_model>

<verification>
Focused commands:
`pnpm --filter @trip-map/web test -- src/services/footprint-availability.spec.ts`
`pnpm --filter @trip-map/web test -- src/services/footprint-availability.spec.ts src/components/map-popup/PointSummaryCard.spec.ts`

Copy safety:
`rg -n "再留一枚足迹|再留一次足迹|再记一次|点亮" apps/web/src/components/map-popup/PointSummaryCard.vue`
</verification>

<success_criteria>
COV-01 and COV-03 are satisfied for the frontend classification layer: each blocked case has an exact internal reason and stable category, the popup shows only friendly copy, disabled CTA remains visible, and blocked cards cannot emit a date-dialog entry event.
</success_criteria>

<output>
After completion, create `.planning/phases/45-map-authoritative-coverage-expansion/45-02-SUMMARY.md`
</output>

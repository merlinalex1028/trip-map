---
phase: "44-world-footprints-map-footprint-date-dialog"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - "apps/web/src/components/map-popup/FootprintDateDialog.spec.ts"
  - "apps/web/src/components/map-popup/PointSummaryCard.spec.ts"
  - "apps/web/src/components/map-popup/MapContextPopup.spec.ts"
  - "apps/web/src/components/LeafletMapStage.spec.ts"
  - "apps/web/src/components/SeedMarkerLayer.spec.ts"
  - "apps/web/src/components/shell/AuthenticatedAppShell.spec.ts"
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05
  - MAP-06
  - DATE-01
  - DATE-02
  - DATE-03
  - DATE-04
  - DATE-05
  - DATE-06
must_haves:
  truths:
    - "测试先锁定 Phase 44 的世界足迹地图、统一地点弹窗、独立日期 Dialog 和 snapshot 保存契约"
    - "后续实现无法继续通过旧的 inline TripDateForm、PopupTripRecord 或 再记一次 分支测试"
  artifacts:
    - path: "apps/web/src/components/map-popup/FootprintDateDialog.spec.ts"
      provides: "DATE-01 到 DATE-06 的 Dialog/Calendar/shortcut/payload/focus 测试契约"
    - path: "apps/web/src/components/map-popup/PointSummaryCard.spec.ts"
      provides: "MAP-03 到 MAP-05 的统一地点卡片测试契约"
    - path: "apps/web/src/components/LeafletMapStage.spec.ts"
      provides: "DATE-05 snapshot 防错存和 MAP-06 登录/失败反馈集成测试契约"
  key_links:
    - from: "PointSummaryCard.spec.ts"
      to: "PointSummaryCard.vue"
      via: "data-footprint-cta and leaveFootprint event contract"
      pattern: "data-footprint-cta|leaveFootprint"
    - from: "FootprintDateDialog.spec.ts"
      to: "FootprintDateDialog.vue"
      via: "Dialog hooks and submit payload contract"
      pattern: "data-region=\"footprint-date-dialog\"|data-footprint-shortcut"
---

<objective>
为 Phase 44 先写可执行的测试契约，锁定地图、弹窗、日期 Dialog、保存 snapshot、marker/sidebar 视觉边界。

Purpose: 避免执行阶段沿用旧的 popup 内联日期表单、保存地点历史面板或可变 active point 提交流程。
Output: 更新或新增 focused specs，后续计划必须让这些测试通过。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-CONTEXT.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-RESEARCH.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-VALIDATION.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-PATTERNS.md
@apps/web/src/components/map-popup/TripDateForm.spec.ts
@apps/web/src/components/map-popup/PointSummaryCard.spec.ts
@apps/web/src/components/map-popup/MapContextPopup.spec.ts
@apps/web/src/components/LeafletMapStage.spec.ts
@apps/web/src/components/SeedMarkerLayer.spec.ts
@apps/web/src/components/shell/AuthenticatedAppShell.spec.ts

<interfaces>
FootprintDateDialog submit contract:
```ts
submit: [payload: { startDate: string | null; endDate: string | null }]
cancel: []
```

Required Phase 44 hooks:
```text
data-footprint-cta="true"
data-footprint-unavailable-reason
data-region="footprint-date-dialog"
data-footprint-place-name
data-footprint-shortcut="today|tomorrow|weekend|custom"
data-footprint-calendar="true"
data-footprint-submit="true"
data-footprint-cancel="true"
data-footprint-error="true"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写 popup 与日期 Dialog 的失败优先测试契约</name>
  <files>apps/web/src/components/map-popup/FootprintDateDialog.spec.ts, apps/web/src/components/map-popup/PointSummaryCard.spec.ts, apps/web/src/components/map-popup/MapContextPopup.spec.ts</files>
  <read_first>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-CONTEXT.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-VALIDATION.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-PATTERNS.md</file>
    <file>apps/web/src/components/map-popup/FootprintDateDialog.spec.ts</file>
    <file>apps/web/src/components/map-popup/PointSummaryCard.spec.ts</file>
    <file>apps/web/src/components/map-popup/MapContextPopup.spec.ts</file>
    <file>apps/web/src/components/map-popup/TripDateForm.spec.ts</file>
  </read_first>
  <action>
    Create `FootprintDateDialog.spec.ts` with Vue Test Utils tests named exactly:
    `renders snapshot place details with Dialog and Calendar hooks`,
    `renders exactly four shortcut buttons today tomorrow weekend custom`,
    `emits YYYY-MM-DD single-day payload from the selected shortcut`,
    `keeps the dialog open with selected date when save fails`,
    `emits cancel and exposes labelled close/cancel controls`.
    Update `PointSummaryCard.spec.ts` so saved `isSaved=true` surfaces assert `这里已经留下过足迹`, `data-footprint-cta="true"`, no `PopupTripRecord`, no `data-region="trip-date-form-wrapper"`, no `data-record-again`, no `再留一次足迹`, and emitting `leaveFootprint` after clicking the CTA. Update `MapContextPopup.spec.ts` so it propagates `leaveFootprint` and keeps `role="dialog"` plus `aria-modal="false"`.
  </action>
  <acceptance_criteria>
    - `rg -n "renders snapshot place details with Dialog and Calendar hooks|renders exactly four shortcut buttons today tomorrow weekend custom|emits YYYY-MM-DD single-day payload" apps/web/src/components/map-popup/FootprintDateDialog.spec.ts` finds all three test names.
    - `rg -n "data-footprint-cta|leaveFootprint|这里已经留下过足迹|data-footprint-unavailable-reason" apps/web/src/components/map-popup/PointSummaryCard.spec.ts apps/web/src/components/map-popup/MapContextPopup.spec.ts` finds all four contract strings.
    - `rg -n "PopupTripRecord|data-record-again|trip-date-form-wrapper|再留一次足迹|再记一次去访" apps/web/src/components/map-popup/PointSummaryCard.spec.ts` returns no lines except negative assertions.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "renders snapshot place details with Dialog and Calendar hooks|data-footprint-cta|leaveFootprint|data-footprint-unavailable-reason" apps/web/src/components/map-popup/FootprintDateDialog.spec.ts apps/web/src/components/map-popup/PointSummaryCard.spec.ts apps/web/src/components/map-popup/MapContextPopup.spec.ts</automated>
  </verify>
  <done>Popup/Dialog 测试契约存在，明确禁止旧 inline form 和 saved-history popup 行为，并锁定 `leaveFootprint` 事件。</done>
</task>

<task type="auto">
  <name>Task 2: 写 controller、marker、sidebar 的回归测试契约</name>
  <files>apps/web/src/components/LeafletMapStage.spec.ts, apps/web/src/components/SeedMarkerLayer.spec.ts, apps/web/src/components/shell/AuthenticatedAppShell.spec.ts</files>
  <read_first>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-CONTEXT.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-VALIDATION.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-PATTERNS.md</file>
    <file>apps/web/src/components/LeafletMapStage.spec.ts</file>
    <file>apps/web/src/components/SeedMarkerLayer.spec.ts</file>
    <file>apps/web/src/components/shell/AuthenticatedAppShell.spec.ts</file>
  </read_first>
  <action>
    Update `LeafletMapStage.spec.ts` with tests named exactly `opens the footprint date dialog from the unified popup CTA`, `saves against the dialog snapshot after active map point changes`, and `opens the login modal instead of writing records when anonymous user submits a footprint`. Update `SeedMarkerLayer.spec.ts` to assert `data-marker-visual="star-footprint"`, `width: 44px;`, `height: 44px;`, `@media (prefers-reduced-motion: reduce)`, and a source token `.seed-marker__star`. Update `AuthenticatedAppShell.spec.ts` to keep nav items exactly `['map', 'journal', 'memories']` and assert map route sidebar exposes `data-shell-visual-mode="world-footprints"` without adding `我的收藏`.
  </action>
  <acceptance_criteria>
    - `rg -n "saves against the dialog snapshot after active map point changes|opens the footprint date dialog from the unified popup CTA|opens the login modal instead of writing records when anonymous user submits a footprint" apps/web/src/components/LeafletMapStage.spec.ts` finds all three tests.
    - `rg -n "data-marker-visual=\\\"star-footprint\\\"|seed-marker__star|prefers-reduced-motion: reduce|width: 44px;|height: 44px;" apps/web/src/components/SeedMarkerLayer.spec.ts` finds all five marker contract strings.
    - `rg -n "world-footprints|\\['map', 'journal', 'memories'\\]|我的收藏" apps/web/src/components/shell/AuthenticatedAppShell.spec.ts` shows the world-footprints assertion and nav array; `我的收藏` appears only in a negative assertion.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "saves against the dialog snapshot after active map point changes|data-marker-visual=\\\"star-footprint\\\"|world-footprints" apps/web/src/components/LeafletMapStage.spec.ts apps/web/src/components/SeedMarkerLayer.spec.ts apps/web/src/components/shell/AuthenticatedAppShell.spec.ts</automated>
  </verify>
  <done>Controller snapshot、marker 视觉和 sidebar 路由视觉边界都有测试契约，后续计划可逐步让测试转绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| map popup -> date dialog | 用户在非模态地图 popup 中触发会保存账号数据的模态日期流程 |
| date dialog -> record store/API | 用户选择的日期和地点 snapshot 进入 `mapPointsStore.illuminate` 与 `/records` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | `LeafletMapStage.vue` snapshot contract | mitigate | 测试锁定 `saves against the dialog snapshot after active map point changes`，后续实现必须从 opening snapshot 提交而不是实时 `summarySurfaceState` |
| T-44-02 | Elevation of Privilege | anonymous footprint submit | mitigate | 测试锁定 anonymous submit 调用 `openAuthModal('login')` 且不调用 `createTravelRecord` |
| T-44-03 | Denial of Service | `FootprintDateDialog` focus/close | mitigate | 测试锁定 labelled close/cancel controls，后续实现使用 Reka Dialog focus trap |
| T-44-04 | Tampering | unsupported authoritative place | mitigate | Popup 测试锁定 disabled CTA 和 `data-footprint-unavailable-reason`，不可进入 Dialog 后才失败 |

No high-severity threats identified for this test-contract plan. Relevant mitigations cover authentication, data integrity, and focus/state safety.
</threat_model>

<verification>
本计划只验证测试契约文本存在，不要求新测试在实现前通过。后续计划必须运行 focused Vitest 让这些契约转绿。
</verification>

<success_criteria>
Wave 0 测试契约完整覆盖 MAP-01 到 MAP-06、DATE-01 到 DATE-06；旧 popup 历史面板、inline date form、可变 active point 提交流程在测试层被明确拒绝。
</success_criteria>

<output>
After completion, create `.planning/phases/44-world-footprints-map-footprint-date-dialog/44-01-SUMMARY.md`
</output>

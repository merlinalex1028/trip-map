---
phase: "44-world-footprints-map-footprint-date-dialog"
plan: "04"
type: execute
wave: 2
depends_on: ["44-01", "44-02"]
files_modified:
  - "apps/web/src/types/map-point.ts"
  - "apps/web/src/components/map-popup/FootprintDateDialog.vue"
  - "apps/web/src/components/map-popup/FootprintDateDialog.spec.ts"
autonomous: true
requirements:
  - DATE-01
  - DATE-02
  - DATE-03
  - DATE-04
  - DATE-06
must_haves:
  truths:
    - "日期选择进入独立 shadcn-vue Dialog/Calendar 弹窗"
    - "Dialog 打开后展示 snapshot 地点名称、类型标签、地区信息、插画提示区、四个快捷日期和完整 Calendar"
    - "提交 payload 始终是 `{ startDate: string | null; endDate: string | null }` 且日期为 `YYYY-MM-DD`"
    - "取消、关闭、提交中、失败状态均有可访问控件和可测试反馈"
  artifacts:
    - path: "apps/web/src/types/map-point.ts"
      provides: "FootprintPlaceSnapshot typed contract"
    - path: "apps/web/src/components/map-popup/FootprintDateDialog.vue"
      provides: "standalone footprint date modal"
    - path: "apps/web/src/components/map-popup/FootprintDateDialog.spec.ts"
      provides: "DATE-01 through DATE-04 and DATE-06 component coverage"
  key_links:
    - from: "FootprintDateDialog.vue"
      to: "@/components/ui/dialog"
      via: "Dialog/DialogContent/DialogTitle/DialogDescription imports"
      pattern: "@/components/ui/dialog"
    - from: "FootprintDateDialog.vue"
      to: "@/components/ui/calendar"
      via: "Calendar import and data-footprint-calendar hook"
      pattern: "@/components/ui/calendar"
---

<objective>
新增独立 `FootprintDateDialog`，使用本地 shadcn-vue Dialog/Calendar 实现高保真“留下足迹”日期选择。

Purpose: 满足 DATE-01/DATE-02/DATE-03/DATE-04/DATE-06 和 D-09 到 D-16，取代旧 inline `TripDateForm`。
Output: `FootprintPlaceSnapshot` 类型、可访问 Dialog/Calendar 日期选择组件、focused specs 转绿。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-CONTEXT.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-RESEARCH.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-VALIDATION.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-PATTERNS.md
@apps/web/src/types/map-point.ts
@apps/web/src/components/map-popup/TripDateForm.vue
@apps/web/src/components/map-popup/TripDateForm.spec.ts
@apps/web/src/components/ui/dialog/index.ts
@apps/web/src/components/ui/calendar/Calendar.vue
@apps/web/src/components/showcase/UiPrimitiveShowcase.vue
@apps/web/src/assets/v8/characters/footprint-dialog-girl.webp

<interfaces>
Create this exported type in `apps/web/src/types/map-point.ts`:
```ts
export interface FootprintPlaceSnapshot {
  placeId: string
  boundaryId: string | null
  placeKind: PlaceKind
  datasetVersion: string
  displayName: string
  regionSystem: 'CN' | 'OVERSEAS'
  adminType: ChinaAdminType | 'ADMIN1' | null
  typeLabel: string | null
  parentLabel: string | null
  subtitle: string | null
}
```

`FootprintDateDialog.vue` props/emits:
```ts
defineProps<{
  open: boolean
  place: FootprintPlaceSnapshot | null
  isSubmitting?: boolean
  errorMessage?: string | null
}>()
defineEmits<{
  'update:open': [open: boolean]
  submit: [payload: { startDate: string | null; endDate: string | null }]
  cancel: []
}>()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 FootprintPlaceSnapshot 并搭建 Dialog 骨架</name>
  <files>apps/web/src/types/map-point.ts, apps/web/src/components/map-popup/FootprintDateDialog.vue</files>
  <read_first>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-PATTERNS.md</file>
    <file>apps/web/src/types/map-point.ts</file>
    <file>apps/web/src/components/map-popup/FootprintDateDialog.vue</file>
    <file>apps/web/src/components/ui/dialog/index.ts</file>
    <file>apps/web/src/components/ui/calendar/Calendar.vue</file>
    <file>apps/web/src/assets/v8/characters/footprint-dialog-girl.webp</file>
  </read_first>
  <action>
    Add exported `FootprintPlaceSnapshot` to `apps/web/src/types/map-point.ts` with the exact fields from the `<interfaces>` block. Create `FootprintDateDialog.vue` with `<script setup lang="ts">`, importing `Dialog`, `DialogContent`, `DialogDescription`, `DialogHeader`, `DialogTitle` from `@/components/ui/dialog`, `Calendar` from `@/components/ui/calendar`, and `footprintDialogGirl` from `@/assets/v8/characters/footprint-dialog-girl.webp`. Implement controlled `v-model:open` through the `open` prop and `update:open` emit. Render `data-region="footprint-date-dialog"`, title text `留下足迹`, description containing `place.displayName`, `data-footprint-place-name`, `data-place-type-label="true"`, and `data-place-subtitle="true"`. Decorative image must use `alt=""` and `aria-hidden="true"`.
  </action>
  <acceptance_criteria>
    - `rg -n "export interface FootprintPlaceSnapshot|placeId: string|displayName: string|regionSystem: 'CN' \\| 'OVERSEAS'" apps/web/src/types/map-point.ts` finds the snapshot type.
    - `rg -n "@/components/ui/dialog|@/components/ui/calendar|footprint-dialog-girl.webp|data-region=\\\"footprint-date-dialog\\\"|data-footprint-place-name|DialogTitle|DialogDescription|留下足迹" apps/web/src/components/map-popup/FootprintDateDialog.vue` finds all Dialog skeleton requirements.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "FootprintPlaceSnapshot|data-region=\\\"footprint-date-dialog\\\"|@/components/ui/calendar|DialogTitle" apps/web/src/types/map-point.ts apps/web/src/components/map-popup/FootprintDateDialog.vue</automated>
  </verify>
  <done>Dialog skeleton 和 snapshot 类型存在，地点信息来自 immutable `place` prop 而不是地图 active state。</done>
</task>

<task type="auto">
  <name>Task 2: 实现快捷日期、Calendar 和 YYYY-MM-DD payload</name>
  <files>apps/web/src/components/map-popup/FootprintDateDialog.vue, apps/web/src/components/map-popup/FootprintDateDialog.spec.ts</files>
  <read_first>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-CONTEXT.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-RESEARCH.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md</file>
    <file>apps/web/src/components/map-popup/FootprintDateDialog.vue</file>
    <file>apps/web/src/components/map-popup/FootprintDateDialog.spec.ts</file>
    <file>apps/web/src/components/map-popup/TripDateForm.vue</file>
  </read_first>
  <action>
    In `FootprintDateDialog.vue`, use `DateValue`, `CalendarDate`, `today`, and `getLocalTimeZone` from `@internationalized/date`. Render exactly four shortcut buttons with `data-footprint-shortcut="today"`, `"tomorrow"`, `"weekend"`, and `"custom"` and visible text `今天`, `明天`, `本周末`, `其他日期`. `今天` selects `today(getLocalTimeZone())`; `明天` selects `todayValue.add({ days: 1 })`; `本周末` selects the nearest upcoming Saturday in local timezone as the single `startDate`; `其他日期` keeps calendar selection visible without hiding the calendar. Render the full Calendar on first screen with `data-footprint-calendar="true"` and sync it with shortcut selection. Submit with `data-footprint-submit="true"` and emit `{ startDate: selectedDate.toString(), endDate: optionalEndDate?.toString() ?? null }`. Add optional end date support only as secondary UI labelled `结束日期（可选）`; if end date is earlier than start date, show `结束日期不能早于开始日期。` and disable submit.
  </action>
  <acceptance_criteria>
    - `rg -n "data-footprint-shortcut=\\\"today\\\"|data-footprint-shortcut=\\\"tomorrow\\\"|data-footprint-shortcut=\\\"weekend\\\"|data-footprint-shortcut=\\\"custom\\\"|今天|明天|本周末|其他日期" apps/web/src/components/map-popup/FootprintDateDialog.vue` finds all four shortcuts and labels.
    - `rg -n "today\\(|getLocalTimeZone|toString\\(\\)|data-footprint-calendar=\\\"true\\\"|data-footprint-submit=\\\"true\\\"|结束日期不能早于开始日期。" apps/web/src/components/map-popup/FootprintDateDialog.vue` finds date implementation hooks.
    - `pnpm --filter @trip-map/web test -- src/components/map-popup/FootprintDateDialog.spec.ts` exits 0 for shortcut and payload tests.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/components/map-popup/FootprintDateDialog.spec.ts</automated>
  </verify>
  <done>Dialog 能通过快捷日期或 Calendar 选择单日，提交 `YYYY-MM-DD` payload，并阻止无效日期范围。</done>
</task>

<task type="auto">
  <name>Task 3: 完成提交中、失败、取消与可访问状态</name>
  <files>apps/web/src/components/map-popup/FootprintDateDialog.vue, apps/web/src/components/map-popup/FootprintDateDialog.spec.ts</files>
  <read_first>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-VALIDATION.md</file>
    <file>apps/web/src/components/map-popup/FootprintDateDialog.vue</file>
    <file>apps/web/src/components/map-popup/FootprintDateDialog.spec.ts</file>
    <file>apps/web/src/components/ui/dialog/DialogContent.vue</file>
  </read_first>
  <action>
    Add visible secondary cancel button `data-footprint-cancel="true"` with text `取消` that emits `cancel` and `update:open false` when `isSubmitting` is false. Submit button text is `保存足迹`; while `isSubmitting` it is disabled and text is `正在保存...`. If `errorMessage` is present, render `data-footprint-error="true"` with `role="alert"` and exact fallback copy `足迹暂时没有保存成功，请检查网络后重试。`. Prevent cancel/outside-close while submitting by ignoring `update:open false` requests when `isSubmitting` is true. Keep `DialogTitle`, `DialogDescription`, and a labelled close control from local DialogContent behavior.
  </action>
  <acceptance_criteria>
    - `rg -n "data-footprint-cancel=\\\"true\\\"|保存足迹|正在保存\\.\\.\\.|data-footprint-error=\\\"true\\\"|role=\\\"alert\\\"|足迹暂时没有保存成功，请检查网络后重试。" apps/web/src/components/map-popup/FootprintDateDialog.vue` finds all state hooks/copy.
    - `pnpm --filter @trip-map/web test -- src/components/map-popup/FootprintDateDialog.spec.ts` exits 0 for cancel, submitting, and failure state tests.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/components/map-popup/FootprintDateDialog.spec.ts</automated>
  </verify>
  <done>DATE-06 状态完整：取消、关闭、提交中和失败都可测试、可访问，并保留焦点/状态安全。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| date controls -> submit payload | 用户选择的日期被转换为 records API 使用的字符串 payload |
| modal focus trap -> page interaction | Dialog 打开时用户键盘焦点进入模态层 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-12 | Tampering | `FootprintDateDialog.vue` date serialization | mitigate | 使用 `CalendarDate.toString()` 产生 `YYYY-MM-DD`，禁止 locale string 或 timezone-shifted `Date` payload |
| T-44-13 | Denial of Service | `FootprintDateDialog.vue` modal state | mitigate | 使用 shadcn-vue/Reka Dialog、DialogTitle/Description、取消按钮和 submitting close guard |
| T-44-14 | Spoofing | `FootprintDateDialog.vue` place display | mitigate | Dialog 只显示 `FootprintPlaceSnapshot` prop 的 `displayName/typeLabel/subtitle`，不读当前 active map selection |
| T-44-15 | Tampering | optional end date | mitigate | `结束日期不能早于开始日期。` disables submit and uses role alert on validation failure |

No high-severity threats identified for this Dialog plan. Relevant mitigations cover authentication handoff readiness, date data integrity, and focus/state safety.
</threat_model>

<verification>
Focused:
- `pnpm --filter @trip-map/web test -- src/components/map-popup/FootprintDateDialog.spec.ts`

Build:
- `pnpm --filter @trip-map/web build`
</verification>

<success_criteria>
DATE-01/DATE-02/DATE-03/DATE-04/DATE-06 covered: standalone Dialog/Calendar exists, full place info and visual zone render, shortcuts work, payload uses `YYYY-MM-DD`, and accessibility/submitting/failure states are explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/44-world-footprints-map-footprint-date-dialog/44-04-SUMMARY.md`
</output>

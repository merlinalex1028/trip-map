---
phase: 46-travel-journal-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/shell/ShellSidebar.vue
  - apps/web/src/components/shell/AuthenticatedAppShell.spec.ts
  - apps/web/src/views/TimelinePageView.spec.ts
  - apps/web/src/router/index.spec.ts
autonomous: true
requirements:
  - JOURNAL-01
  - JOURNAL-04
  - JOURNAL-05
  - JOURNAL-06
must_haves:
  truths:
    - "Authenticated `/journal` remains the only travel journal route; legacy `/timeline` falls through to `/`."
    - "D-02: D-03: D-05: Journal page and authenticated shell do not expose `添加新旅行`, `收藏`, `我的收藏`, favorite controls, local creation actions, or replacement header/tool affordances."
    - "D-04: Empty journal state can navigate only to `/map` with copy `去世界足迹留下足迹`."
  artifacts:
    - path: "apps/web/src/components/shell/ShellSidebar.vue"
      provides: "Authenticated shell navigation without collection/future disabled entries"
      contains: "旅途手账"
    - path: "apps/web/src/components/shell/AuthenticatedAppShell.spec.ts"
      provides: "Shell navigation regression coverage"
      contains: "not.toContain('我的收藏')"
    - path: "apps/web/src/views/TimelinePageView.spec.ts"
      provides: "Journal absence and empty-state navigation tests"
      contains: "添加新旅行"
    - path: "apps/web/src/router/index.spec.ts"
      provides: "Route guard and legacy `/timeline` fallthrough coverage"
      contains: "legacy timeline path"
  key_links:
    - from: "apps/web/src/components/shell/ShellSidebar.vue"
      to: "/journal"
      via: "navItems item with key `journal`"
      pattern: "key: 'journal'.*to: '/journal'"
    - from: "apps/web/src/views/TimelinePageView.spec.ts"
      to: "/map"
      via: "RouterLinkStub empty-state assertion"
      pattern: "去世界足迹留下足迹"
---

<objective>
Lock the route, shell, and absence contracts that prevent the travel journal refactor from reintroducing legacy timeline paths, add-trip affordances, or collection/favorite surfaces.

Purpose: This plan protects Phase 46 decisions D-02 through D-05 before visual implementation starts.
Output: Updated shell navigation and focused tests that downstream visual plans must keep green.
</objective>

<decision_traceability>
- D-02: This plan locks the absence of `添加新旅行` and journal-local creation selectors.
- D-03: This plan locks the absence of favorite/collection controls and favorite selectors from the journal and shell.
- D-04: This plan allows empty-state navigation only back to `/map` / `世界足迹`.
- D-05: This plan removes replacement header/shell affordances instead of substituting stats, tools, or disabled future entries.
</decision_traceability>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-travel-journal-refactor/46-CONTEXT.md
@.planning/phases/46-travel-journal-refactor/46-RESEARCH.md
@.planning/phases/46-travel-journal-refactor/46-PATTERNS.md
@.planning/phases/46-travel-journal-refactor/46-VALIDATION.md
@.planning/phases/46-travel-journal-refactor/46-UI-SPEC.md
@.planning/phases/43-landing/43-02-SUMMARY.md
@.planning/phases/43-landing/43-03-SUMMARY.md
@.planning/phases/43-landing/43-04-SUMMARY.md
@apps/web/src/components/shell/ShellSidebar.vue
@apps/web/src/components/shell/AuthenticatedAppShell.spec.ts
@apps/web/src/views/TimelinePageView.spec.ts
@apps/web/src/router/index.spec.ts

<interfaces>
From `apps/web/src/components/shell/ShellSidebar.vue`:
```ts
interface NavItem {
  key: string
  to?: string
  label: string
  icon: KawaiiIconName
  disabled?: boolean
}
```

Locked shell target for this plan:
```ts
const navItems = [
  { key: 'map', to: '/map', label: '世界足迹', icon: 'map' as const },
  { key: 'journal', to: '/journal', label: '旅途手账', icon: 'journal' as const },
  { key: 'memories', to: '/memories', label: '旅途回忆', icon: 'memories' as const },
] satisfies NavItem[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock journal route and forbidden-affordance tests</name>
  <files>apps/web/src/views/TimelinePageView.spec.ts, apps/web/src/router/index.spec.ts</files>
  <read_first>
    - apps/web/src/views/TimelinePageView.spec.ts
    - apps/web/src/router/index.spec.ts
    - .planning/phases/46-travel-journal-refactor/46-CONTEXT.md
    - .planning/phases/46-travel-journal-refactor/46-UI-SPEC.md
    - .planning/phases/46-travel-journal-refactor/46-VALIDATION.md
  </read_first>
  <behavior>
    - Test: authenticated populated journal text does not contain `添加新旅行`, `我的收藏`, `收藏`, `上传`, `照片`, or `旅行照片`.
    - Test: populated journal does not render `[data-card-favorite]`, `[data-journal-add-trip]`, `[data-journal-create]`, or `[data-journal-favorite]`.
    - Test: empty state has exactly one `RouterLinkStub` to `/map` with text `去世界足迹留下足迹`.
    - Test: `/journal` remains route name `travel-journal`; legacy `/timeline` remains catch-all and redirects to `/`.
  </behavior>
  <action>Add black-box absence and route tests using the existing `mountTimelinePage()` helper. Keep the legacy timeline path in `router/index.spec.ts` built from string concatenation (`'/time' + 'line'`) so source grep gates do not revive the route literal. Use exact forbidden copy from D-02, D-03, JOURNAL-04, and JOURNAL-05: `添加新旅行`, `我的收藏`, `收藏`, `上传`, `照片`, `旅行照片`. Add explicit selector assertions for `[data-card-favorite]`, `[data-journal-add-trip]`, `[data-journal-create]`, and `[data-journal-favorite]`.</action>
  <acceptance_criteria>
    - `apps/web/src/views/TimelinePageView.spec.ts` contains `not.toContain('添加新旅行')`.
    - `apps/web/src/views/TimelinePageView.spec.ts` contains `not.toContain('我的收藏')`.
    - `apps/web/src/views/TimelinePageView.spec.ts` contains `find('[data-card-favorite]').exists()).toBe(false)`.
    - `apps/web/src/views/TimelinePageView.spec.ts` contains `props.to).toBe('/map')` or an equivalent RouterLinkStub prop assertion for `/map`.
    - `apps/web/src/router/index.spec.ts` still contains `legacyTimelinePath = '/time' + 'line'`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- router TimelinePageView</automated>
  </verify>
  <done>The tests fail before forbidden affordances are removed and pass once `/journal`, `/map` empty guidance, and absence contracts are enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove collection and disabled future entries from authenticated shell</name>
  <files>apps/web/src/components/shell/ShellSidebar.vue, apps/web/src/components/shell/AuthenticatedAppShell.spec.ts</files>
  <read_first>
    - apps/web/src/components/shell/ShellSidebar.vue
    - apps/web/src/components/shell/AuthenticatedAppShell.spec.ts
    - .planning/phases/46-travel-journal-refactor/46-CONTEXT.md
    - .planning/phases/46-travel-journal-refactor/46-RESEARCH.md
    - .planning/phases/43-landing/43-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test: shell nav item keys equal `['map', 'journal', 'memories']`.
    - Test: shell text contains `世界足迹`, `旅途手账`, and `旅途回忆`.
    - Test: shell text does not contain `我的收藏`, `点亮足迹`, or `设置`.
    - Test: active `/journal` route still sets `aria-current="page"` on `[data-shell-nav-item="journal"]`.
  </behavior>
  <action>Replace the current `navItems` array with exactly three items: `map` -> `/map` -> `世界足迹` -> `map`, `journal` -> `/journal` -> `旅途手账` -> `journal`, and `memories` -> `/memories` -> `旅途回忆` -> `memories`. Remove the `collections`, `illuminate`, and `settings` items instead of hiding them with CSS. Update shell specs to expect exactly the three keys and three icon images. Keep `data-shell-visual-mode="world-footprints"` unchanged.</action>
  <acceptance_criteria>
    - `apps/web/src/components/shell/ShellSidebar.vue` contains `key: 'memories'`.
    - `apps/web/src/components/shell/ShellSidebar.vue` contains `label: '旅途回忆'`.
    - `rg -n "collections|我的收藏|点亮足迹|key: 'settings'" apps/web/src/components/shell/ShellSidebar.vue` returns no matches.
    - `apps/web/src/components/shell/AuthenticatedAppShell.spec.ts` contains `toEqual(['map', 'journal', 'memories'])`.
    - `apps/web/src/components/shell/AuthenticatedAppShell.spec.ts` contains `not.toContain('我的收藏')`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- AuthenticatedAppShell TimelinePageView</automated>
  </verify>
  <done>Authenticated shell visible on `/journal` exposes only the three real v8 app routes and no collection/add/future disabled entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser -> protected `/journal` route | Anonymous users must not view travel history. |
| authenticated shell -> journal route | Shell navigation must not expose excluded collection or add-entry surfaces. |
| old URL -> router catch-all | Legacy `/timeline` must not become a bypass or alternate journal route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Information Disclosure | `apps/web/src/router/index.ts` / `index.spec.ts` | mitigate | Keep `/journal` route `meta.requiresAuth: true` and keep legacy `/timeline` catch-all coverage. |
| T-46-02 | Spoofing | `ShellSidebar.vue` | mitigate | Remove `我的收藏` and disabled future entries from the rendered nav model so excluded features are not presented as available or pending. |
| T-46-03 | Tampering | `TimelinePageView.spec.ts` | mitigate | Assert no journal-local creation selectors or copy exist; creation remains the authoritative `/map` place flow. |
| T-46-04 | Repudiation | `AuthenticatedAppShell.spec.ts` | accept | Shell nav changes do not alter record mutation APIs; edit/delete auditability remains owned by existing record actions. |
</threat_model>

<verification>
Run the plan task commands, then run the wave gate:

```bash
pnpm --filter @trip-map/web test -- router TimelinePageView AuthenticatedAppShell
```
</verification>

<success_criteria>
- `/journal` remains protected and named `travel-journal`.
- `/timeline` is not restored as a route or alias.
- Authenticated shell on `/journal` renders only `世界足迹`, `旅途手账`, `旅途回忆`.
- Journal page tests assert absence of `添加新旅行`, `我的收藏`, `收藏`, upload/photo copy, and favorite selectors.
</success_criteria>

<output>
After completion, create `.planning/phases/46-travel-journal-refactor/46-01-SUMMARY.md`.
</output>

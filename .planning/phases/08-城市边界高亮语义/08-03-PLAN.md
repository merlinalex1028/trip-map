---
phase: 08-城市边界高亮语义
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/components/PointPreviewDrawer.vue
  - src/components/PointPreviewDrawer.spec.ts
  - src/components/WorldMapStage.spec.ts
  - src/stores/map-points.spec.ts
  - src/services/point-storage.spec.ts
  - src/App.spec.ts
autonomous: true
requirements:
  - BND-02
  - BND-03
  - DAT-06
user_setup: []
must_haves:
  truths:
    - "重新打开已保存的 v2 城市记录时，详情面板中的城市身份与地图边界仍然指向同一城市。"
    - "关闭面板、切换城市、返回已有记录和 fallback 继续记录这些关键状态切换不会残留错误面域。"
    - "多面域城市和边界恢复回退逻辑都有回归测试覆盖，不靠人工记忆守住。"
  artifacts:
    - path: "src/components/PointPreviewDrawer.spec.ts"
      provides: "详情/抽屉与边界 identity 一致性回归"
      contains: "boundaryId"
    - path: "src/components/WorldMapStage.spec.ts"
      provides: "切换、关闭、fallback 和多面域边界回归"
      contains: "boundary-layer"
    - path: "src/App.spec.ts"
      provides: "主舞台与 drawer 协同回归"
      contains: "drawer-open"
  key_links:
    - from: "src/services/point-storage.spec.ts"
      to: "src/stores/map-points.spec.ts"
      via: "restore -> select -> render consistency"
      pattern: "boundaryId"
    - from: "src/stores/map-points.spec.ts"
      to: "src/components/WorldMapStage.spec.ts"
      via: "close/switch/fallback semantics"
      pattern: "selectedBoundaryId"
    - from: "src/components/PointPreviewDrawer.spec.ts"
      to: "src/App.spec.ts"
      via: "drawer identity and shell coordination"
      pattern: "candidate-select"
---

<objective>
Harden the end-to-end boundary semantics with reopen, close, switch, fallback, and multi-area regressions so Phase 8 ships stable state transitions rather than only a happy-path boundary render.

Purpose: Phase 8 is only done when the city identity shown in details and the city boundary shown on the map stay aligned through real user flows, not just after a fresh click.
Output: Regression coverage and any small supporting fixes needed to keep boundary semantics stable across reopen, switch, fallback, and multi-area cities.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-城市边界高亮语义/08-CONTEXT.md
@.planning/phases/08-城市边界高亮语义/08-RESEARCH.md
@.planning/phases/08-城市边界高亮语义/08-01-PLAN.md
@.planning/phases/08-城市边界高亮语义/08-02-PLAN.md
@src/components/PointPreviewDrawer.vue
@src/components/PointPreviewDrawer.spec.ts
@src/components/WorldMapStage.spec.ts
@src/stores/map-points.spec.ts
@src/services/point-storage.spec.ts
@src/App.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock boundary identity consistency across restore, reopen, and details surfaces</name>
  <files>src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/services/point-storage.spec.ts, src/stores/map-points.spec.ts</files>
  <read_first>src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/services/point-storage.ts, src/services/point-storage.spec.ts, src/stores/map-points.ts, src/stores/map-points.spec.ts, .planning/phases/08-城市边界高亮语义/08-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Rehydrating a saved v2 city record restores the same `cityId`, `boundaryId`, and active city title shown in the drawer.
    - Test 2: Reopening an existing saved city from reuse flow points the store and details surface at the same boundary identity used by the map.
    - Test 3: Legacy or fallback records continue to show textual details without falsely asserting a city boundary.
  </behavior>
  <action>Extend the reopen and restore regression coverage so `src/services/point-storage.spec.ts` explicitly asserts that a saved city point persists and reloads exact fields `cityId`, `boundaryId`, and `boundaryDatasetVersion`. Update `src/stores/map-points.spec.ts` and `src/components/PointPreviewDrawer.spec.ts` so reopening an existing saved city through the reuse path keeps the drawer title, `activePoint`, and boundary identity aligned. If `PointPreviewDrawer.vue` needs a small supporting change, keep it narrow: the drawer should continue reading the city identity from `activePoint` and must not invent a separate boundary-only label source. Also keep fallback and legacy records text-only, with no city-boundary assertion added to their UI path.</action>
  <verify>
    rg 'boundaryId|boundaryDatasetVersion' src/services/point-storage.spec.ts src/stores/map-points.spec.ts src/components/PointPreviewDrawer.spec.ts
    <automated>pnpm test -- src/services/point-storage.spec.ts src/stores/map-points.spec.ts src/components/PointPreviewDrawer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/services/point-storage.spec.ts contains `boundaryId`
    - src/services/point-storage.spec.ts contains `boundaryDatasetVersion`
    - src/stores/map-points.spec.ts contains `boundaryId`
    - src/components/PointPreviewDrawer.spec.ts contains `boundaryId`
    - src/components/PointPreviewDrawer.vue does not introduce a separate boundary-only title source unrelated to `activePoint`
  </acceptance_criteria>
  <done>Saved city records, reopened reuse flows, and drawer details all point at the same city and boundary identity instead of drifting apart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add transition regressions for switch, close, fallback, and multi-area boundary stability</name>
  <files>src/components/WorldMapStage.spec.ts, src/App.spec.ts, src/stores/map-points.spec.ts</files>
  <read_first>src/components/WorldMapStage.spec.ts, src/App.spec.ts, src/stores/map-points.spec.ts, src/components/WorldMapStage.vue, src/stores/map-points.ts, .planning/phases/08-城市边界高亮语义/08-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Switching from one saved city to another removes the prior strong-highlight boundary and leaves only the new selection plus any saved weak state.
    - Test 2: Closing the drawer does not leave a stale extra strong-highlight boundary.
    - Test 3: Fallback country/region flows and multi-area city cases are covered so no erroneous boundary residue survives the transition.
  </behavior>
  <action>Extend `src/components/WorldMapStage.spec.ts`, `src/stores/map-points.spec.ts`, and `src/App.spec.ts` with explicit transition regressions that cover four cases: switching between two saved boundary-aware cities, closing the active city panel, continuing with a fallback country/region record, and rendering a multi-area city boundary. Assert against exact store state (`selectedBoundaryId`, `savedBoundaryIds`) and exact DOM hooks (`world-map-stage__boundary--selected`, `world-map-stage__boundary--saved`) so boundary residue is grep- and test-detectable. If small supporting fixes are needed in the existing components or store to make those regressions pass, keep them scoped to transition cleanup and do not add hover previews, remembered strong-highlights, or popup-specific logic.</action>
  <verify>
    rg 'world-map-stage__boundary--selected|world-map-stage__boundary--saved|selectedBoundaryId|savedBoundaryIds' src/components/WorldMapStage.spec.ts src/stores/map-points.spec.ts src/App.spec.ts
    <automated>pnpm test -- src/components/WorldMapStage.spec.ts src/stores/map-points.spec.ts src/App.spec.ts && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/WorldMapStage.spec.ts contains `world-map-stage__boundary--selected`
    - src/components/WorldMapStage.spec.ts contains `world-map-stage__boundary--saved`
    - src/stores/map-points.spec.ts contains `selectedBoundaryId`
    - src/stores/map-points.spec.ts contains `savedBoundaryIds`
    - src/App.spec.ts contains `drawer-open`
    - no plan task introduces `hover` boundary preview requirements
  </acceptance_criteria>
  <done>Phase 8 has regression coverage for the exact stability risks in its success criteria: switch, close, fallback, reopen, and multi-area boundaries.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test -- src/services/point-storage.spec.ts src/stores/map-points.spec.ts src/components/PointPreviewDrawer.spec.ts
- [ ] pnpm test -- src/components/WorldMapStage.spec.ts src/stores/map-points.spec.ts src/App.spec.ts
- [ ] pnpm build
- [ ] Confirm there is explicit regression coverage for switch, close, fallback, and multi-area boundary flows
</verification>

<success_criteria>
- Reopened saved city records keep drawer identity and boundary identity aligned
- Closing or switching selection never leaves stale strong-highlight boundaries
- Fallback and legacy paths stay fail-closed for city boundary rendering
- Multi-area city boundaries are explicitly covered by automated regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-城市边界高亮语义/08-03-SUMMARY.md`
</output>

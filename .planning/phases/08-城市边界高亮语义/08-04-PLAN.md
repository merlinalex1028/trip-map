---
phase: 08-城市边界高亮语义
plan: 04
type: execute
wave: 4
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - src/data/geo/city-boundaries.geo.json
  - src/services/city-boundaries.ts
  - src/services/city-boundaries.spec.ts
  - src/stores/map-points.ts
  - src/stores/map-points.spec.ts
  - src/components/PointPreviewDrawer.vue
  - src/components/PointPreviewDrawer.spec.ts
  - src/components/WorldMapStage.spec.ts
autonomous: true
requirements:
  - BND-01
  - BND-02
  - BND-03
  - DAT-06
gap_closure: true
must_haves:
  truths:
    - "当前主链路中常用的城市在确认、保存和重开后都能拿到非空 `boundaryId`，并真正显示城市边界高亮。"
    - "Phase 07 已纳入回归的 Budapest / Nairobi 这类非原 6 城样例，不再因为边界资产缺失而整条链路无高亮。"
    - "当某个仍可选中的城市没有边界资产时，UI 会明确提示仅保存城市身份与文本信息，不会暗示一定存在边界高亮。"
    - "边界覆盖退化会在自动化里显式暴露，而不是再次只在人工 UAT 中才发现。"
  artifacts:
    - path: "src/data/geo/city-boundaries.geo.json"
      provides: "覆盖当前主链路常用城市的离线边界资产"
      contains: "\"cityId\": \"hu-budapest\""
    - path: "src/services/city-boundaries.ts"
      provides: "边界覆盖审计与城市支持度查询"
      contains: "boundaryCoverageStats"
    - path: "src/components/PointPreviewDrawer.vue"
      provides: "unsupported 城市的边界语义护栏提示"
      contains: "当前城市暂不支持边界高亮，将仅保存城市身份与文本信息"
  key_links:
    - from: "src/data/geo/city-candidates.ts"
      to: "src/data/geo/city-boundaries.geo.json"
      via: "43 个 curated cityId 的 coverage audit"
      pattern: "curatedCitiesMissingBoundaryCoverage"
    - from: "src/services/city-boundaries.ts"
      to: "src/stores/map-points.ts"
      via: "candidate confirm / reopen 的 boundary coverage state"
      pattern: "hasBoundaryCoverageForCityId"
    - from: "src/stores/map-points.ts"
      to: "src/components/PointPreviewDrawer.vue"
      via: "unsupported 城市提示只在有 cityId 但无边界资产时出现"
      pattern: "activeBoundaryCoverageState"
---

<objective>
Close the diagnosed Phase 08 gap by expanding offline boundary coverage beyond the 6-city fixture set, then add auditable coverage guards and a narrow unsupported-city notice.

Purpose: The current render/store/persistence chain is already correct; manual UAT fails because most reachable cities never receive a `boundaryId`, so the map correctly fail-closes with no highlight.
Output: A coverage-audited boundary dataset for current product/UAT cities plus regression and drawer guardrails that make unsupported boundary semantics explicit.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-城市边界高亮语义/08-CONTEXT.md
@.planning/phases/08-城市边界高亮语义/08-RESEARCH.md
@.planning/phases/08-城市边界高亮语义/08-UAT.md
@.planning/phases/08-城市边界高亮语义/08-03-SUMMARY.md
@.planning/phases/07-城市选择与兼容基线/07-04-SUMMARY.md
@.planning/phases/07-城市选择与兼容基线/07-05-SUMMARY.md
@src/data/geo/city-candidates.ts
@src/services/city-boundaries.ts
@src/stores/map-points.ts
@src/components/PointPreviewDrawer.vue
@src/components/WorldMapStage.spec.ts

<interfaces>
From src/services/city-boundaries.ts:
```ts
export const CITY_BOUNDARY_DATASET_VERSION: string
export function getBoundaryById(boundaryId: string): NormalizedCityBoundary | null
export function getBoundaryByCityId(cityId: string): NormalizedCityBoundary | null
```

From src/data/geo/city-candidates.ts:
```ts
export const cityCoverageStats = {
  totalCities: number,
  coveredCountryCodes: string[],
  coveredCountryCount: number,
  totalCountryCount: number,
  countriesMissingCoverageCount: number
}

export const cityCandidates: CityCandidate[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand boundary assets to the curated city set and make coverage shrinkage auditable</name>
  <files>src/data/geo/city-boundaries.geo.json, src/services/city-boundaries.ts, src/services/city-boundaries.spec.ts</files>
  <read_first>src/data/geo/city-boundaries.geo.json, src/services/city-boundaries.ts, src/services/city-boundaries.spec.ts, src/data/geo/city-candidates.ts, .planning/phases/08-城市边界高亮语义/08-UAT.md, .planning/phases/07-城市选择与兼容基线/07-04-SUMMARY.md, .planning/phases/07-城市选择与兼容基线/07-05-SUMMARY.md</read_first>
  <behavior>
    - Test 1: `getBoundaryByCityId()` resolves every city in the 43-entry `curatedCitySeeds` block, including `hu-budapest` and `ke-nairobi`, instead of only the original 6 fixtures.
    - Test 2: Coverage stats expose whether any curated city lacks a boundary asset, so future shrinkage fails in CI before manual UAT.
    - Test 3: Existing shipped boundary IDs for `pt-lisbon`, `eg-cairo`, `jp-kyoto`, `jp-tokyo`, `fr-paris`, and `ar-buenos-aires` remain stable, preserving D-12/D-13 restore semantics.
  </behavior>
  <action>Expand `src/data/geo/city-boundaries.geo.json` from the current 6-feature fixture set to cover all 43 exact city entries defined in the `curatedCitySeeds` block of `src/data/geo/city-candidates.ts`. Keep the already shipped `boundaryId` values unchanged for `pt-lisbon`, `eg-cairo`, `jp-kyoto`, `jp-tokyo`, `fr-paris`, and `ar-buenos-aires`, then add real boundary geometry for the remaining curated cities so `BND-01` and `DAT-06` hold for the current product/UAT surface instead of only demo cities. In `src/services/city-boundaries.ts`, add exact exports `hasBoundaryCoverageForCityId(cityId: string)`, `boundaryCoverageStats`, and `curatedCitiesMissingBoundaryCoverage`, with `boundaryCoverageStats.coveredCuratedCityCount` required to reach `43` and `curatedCitiesMissingBoundaryCoverage.length` required to reach `0`. Extend `src/services/city-boundaries.spec.ts` to assert the curated-city coverage count, zero curated gaps, and exact lookup success for `hu-budapest`, `ke-nairobi`, `jp-osaka`, `pt-porto`, and `us-new-york`. Preserve D-16/D-17 by keeping multi-area geometry support intact; do not replace the dataset with online APIs, fuzzy name matching, or a full global ADM dump.</action>
  <verify>
    rg 'boundaryCoverageStats|curatedCitiesMissingBoundaryCoverage|hasBoundaryCoverageForCityId' src/services/city-boundaries.ts src/services/city-boundaries.spec.ts
    rg '"cityId": "hu-budapest"|"cityId": "ke-nairobi"|"cityId": "jp-osaka"|"cityId": "pt-porto"|"cityId": "us-new-york"' src/data/geo/city-boundaries.geo.json
    <automated>pnpm test -- src/services/city-boundaries.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/services/city-boundaries.ts contains `boundaryCoverageStats`
    - src/services/city-boundaries.ts contains `curatedCitiesMissingBoundaryCoverage`
    - src/services/city-boundaries.ts contains `hasBoundaryCoverageForCityId`
    - src/services/city-boundaries.spec.ts contains `coveredCuratedCityCount`
    - src/services/city-boundaries.spec.ts contains `hu-budapest`
    - src/services/city-boundaries.spec.ts contains `ke-nairobi`
    - src/data/geo/city-boundaries.geo.json contains `"cityId": "hu-budapest"`
    - src/data/geo/city-boundaries.geo.json contains `"cityId": "ke-nairobi"`
    - src/data/geo/city-boundaries.geo.json contains `"cityId": "us-new-york"`
  </acceptance_criteria>
  <done>The offline boundary dataset covers the current curated city surface rather than a 6-city demo subset, and automated tests make any curated-city coverage regression immediately visible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add previously uncovered-city regressions and an explicit unsupported-boundary guardrail</name>
  <files>src/stores/map-points.ts, src/stores/map-points.spec.ts, src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/components/WorldMapStage.spec.ts</files>
  <read_first>src/stores/map-points.ts, src/stores/map-points.spec.ts, src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/components/WorldMapStage.spec.ts, src/services/city-boundaries.ts, .planning/phases/08-城市边界高亮语义/08-CONTEXT.md, .planning/phases/08-城市边界高亮语义/08-UAT.md</read_first>
  <behavior>
    - Test 1: A previously uncovered but now supported city such as `hu-budapest` receives a non-null `boundaryId` on confirm/save/reopen and renders a selected boundary highlight.
    - Test 2: A city that remains outside the curated boundary set, such as `cl-santiago`, stays fail-closed on the map but shows an explicit unsupported-boundary notice in the drawer.
    - Test 3: Fallback country/region and legacy `cityId: null` records continue to avoid both city-boundary rendering and the unsupported-city notice, preserving D-07 and D-15.
  </behavior>
  <action>In `src/stores/map-points.ts`, add an exact derived state named `activeBoundaryCoverageState` with string values `supported`, `missing`, and `not-applicable`. Return `supported` when the active point has a real `boundaryId` or `hasBoundaryCoverageForCityId(activePoint.cityId)` succeeds, `missing` when there is a non-null `cityId` but no boundary asset, and `not-applicable` for fallback or legacy records with `cityId: null`. Use that state in `src/components/PointPreviewDrawer.vue` to render the exact lightweight copy `当前城市暂不支持边界高亮，将仅保存城市身份与文本信息` only for `missing`, so unsupported cities do not pretend to satisfy Phase 8 semantics while still remaining selectable. Extend `src/stores/map-points.spec.ts` and `src/components/WorldMapStage.spec.ts` with end-to-end regression for `hu-budapest` confirm -> save/reopen -> boundary highlight, and extend `src/components/PointPreviewDrawer.spec.ts` with an exact unsupported-city case for `cl-santiago` plus assertions that fallback/legacy paths do not show the new copy. Preserve D-01/D-04/D-05 by keeping real boundary highlight as the primary supported-city expression, and preserve D-09/D-10 by not introducing any remembered highlight state or hover preview.</action>
  <verify>
    rg 'activeBoundaryCoverageState|supported|missing|not-applicable' src/stores/map-points.ts src/stores/map-points.spec.ts
    rg '当前城市暂不支持边界高亮，将仅保存城市身份与文本信息|cl-santiago|hu-budapest' src/components/PointPreviewDrawer.vue src/components/PointPreviewDrawer.spec.ts src/components/WorldMapStage.spec.ts
    <automated>pnpm test -- src/stores/map-points.spec.ts src/components/PointPreviewDrawer.spec.ts src/components/WorldMapStage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/map-points.ts contains `activeBoundaryCoverageState`
    - src/stores/map-points.ts contains `supported`
    - src/stores/map-points.ts contains `missing`
    - src/stores/map-points.ts contains `not-applicable`
    - src/components/PointPreviewDrawer.vue contains `当前城市暂不支持边界高亮，将仅保存城市身份与文本信息`
    - src/components/PointPreviewDrawer.spec.ts contains `cl-santiago`
    - src/components/PointPreviewDrawer.spec.ts contains `当前城市暂不支持边界高亮，将仅保存城市身份与文本信息`
    - src/components/WorldMapStage.spec.ts contains `hu-budapest`
    - src/stores/map-points.spec.ts contains `hu-budapest`
  </acceptance_criteria>
  <done>Manual UAT no longer silently lands on no-highlight cities: supported cities now highlight correctly, and unsupported ones explicitly say they do not currently provide boundary highlight semantics.</done>
</task>

</tasks>

<verification>
- [ ] pnpm test -- src/services/city-boundaries.spec.ts
- [ ] pnpm test -- src/stores/map-points.spec.ts src/components/PointPreviewDrawer.spec.ts src/components/WorldMapStage.spec.ts
- [ ] pnpm build
- [ ] Confirm `boundaryCoverageStats.coveredCuratedCityCount` is `43`
- [ ] Confirm `curatedCitiesMissingBoundaryCoverage.length` is `0`
- [ ] Confirm drawer copy `当前城市暂不支持边界高亮，将仅保存城市身份与文本信息` appears only for unsupported city records with non-null `cityId`
</verification>

<success_criteria>
- Every city in the current curated UAT/product surface can resolve a boundary asset and render highlight semantics
- Budapest/Nairobi-style non-demo cities are protected by automated regressions instead of manual memory
- Unsupported non-curated cities fail closed with an explicit drawer notice rather than silently implying boundary support
- Boundary coverage shrinkage becomes CI-visible through explicit audited stats and tests
</success_criteria>

<output>
After completion, create `.planning/phases/08-城市边界高亮语义/08-04-SUMMARY.md`
</output>

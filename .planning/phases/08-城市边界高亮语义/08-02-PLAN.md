---
phase: 08-城市边界高亮语义
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/stores/map-points.ts
  - src/stores/map-points.spec.ts
  - src/components/WorldMapStage.vue
  - src/components/WorldMapStage.spec.ts
  - src/components/SeedMarkerLayer.vue
  - src/components/SeedMarkerLayer.spec.ts
  - src/services/city-boundaries.ts
autonomous: true
requirements:
  - BND-01
  - BND-03
  - DAT-06
user_setup: []
must_haves:
  truths:
    - "当前选中城市在地图上以真实边界作为主表达，而不是只靠单点 marker。"
    - "已保存城市可以常驻弱高亮，当前选中城市在其之上强化显示。"
    - "回退到国家/地区时不显示城市边界，切换或关闭后不会留下错误面域。"
  artifacts:
    - path: "src/stores/map-points.ts"
      provides: "边界当前态、已保存态和 fail-closed 恢复语义"
      contains: "selectedBoundaryId"
    - path: "src/components/WorldMapStage.vue"
      provides: "城市边界 SVG overlay 渲染"
      contains: "world-map-stage__boundary-layer"
    - path: "src/components/SeedMarkerLayer.vue"
      provides: "marker 退为辅助定位层"
      contains: "seed-marker__button--selected"
  key_links:
    - from: "src/stores/map-points.ts"
      to: "src/components/WorldMapStage.vue"
      via: "derived boundary state"
      pattern: "selectedBoundaryId"
    - from: "src/services/city-boundaries.ts"
      to: "src/components/WorldMapStage.vue"
      via: "normalized boundary geometry"
      pattern: "getBoundaryById"
    - from: "src/stores/map-points.ts"
      to: "src/components/SeedMarkerLayer.vue"
      via: "selected marker as auxiliary locator"
      pattern: "selectedPointId"
---

<objective>
Wire boundary identity into store-derived render state and render true city boundaries on the map with the exact saved-vs-selected hierarchy locked in CONTEXT.md.

Purpose: Phase 8 succeeds only when boundary semantics are driven from the same point state that already controls selection and persistence, not from ad-hoc component memory.
Output: A boundary-aware store plus SVG overlay rendering that shows saved weak highlights, selected strong highlights, and no false boundary on fallback flows.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-城市边界高亮语义/08-CONTEXT.md
@.planning/phases/08-城市边界高亮语义/08-RESEARCH.md
@.planning/phases/08-城市边界高亮语义/08-01-PLAN.md
@src/stores/map-points.ts
@src/stores/map-points.spec.ts
@src/components/WorldMapStage.vue
@src/components/WorldMapStage.spec.ts
@src/components/SeedMarkerLayer.vue
@src/components/SeedMarkerLayer.spec.ts
@src/services/city-boundaries.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add boundary-aware derived state to the point store and draft/save lifecycle</name>
  <files>src/stores/map-points.ts, src/stores/map-points.spec.ts, src/services/city-boundaries.ts</files>
  <read_first>src/stores/map-points.ts, src/stores/map-points.spec.ts, src/services/city-boundaries.ts, src/services/point-storage.ts, .planning/phases/08-城市边界高亮语义/08-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Confirming a concrete city candidate attaches the exact `boundaryId` and `boundaryDatasetVersion` to the draft if a boundary exists.
    - Test 2: Saved points expose a derived weak-highlight boundary list while the current active point exposes a single strong-highlight boundary target.
    - Test 3: Fallback country/region flows and legacy points with no boundary metadata produce no city boundary highlight.
  </behavior>
  <action>Update `src/stores/map-points.ts` so `buildDraftFromCandidate()` resolves the selected city's boundary through `getBoundaryByCityId(candidate.cityId)` and writes exact fields `boundaryId` and `boundaryDatasetVersion` into the draft. Add store-level derived state with exact names `selectedBoundaryId` and `savedBoundaryIds`, where `selectedBoundaryId` comes from the current active point and `savedBoundaryIds` contains only persisted points with a non-null `boundaryId`. Keep fallback records and legacy points fail-closed: if the active or saved point has `boundaryId: null`, it must not enter the boundary render state. Extend `src/stores/map-points.spec.ts` with assertions for candidate-confirmed drafts, reopened saved points, legacy points, and fallback flows so the store becomes the single source of truth for boundary semantics.</action>
  <verify>
    rg 'selectedBoundaryId|savedBoundaryIds|boundaryDatasetVersion|boundaryId' src/stores/map-points.ts
    rg 'selectedBoundaryId|savedBoundaryIds|boundaryId' src/stores/map-points.spec.ts
    <automated>pnpm test -- src/stores/map-points.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/map-points.ts contains `selectedBoundaryId`
    - src/stores/map-points.ts contains `savedBoundaryIds`
    - src/stores/map-points.ts contains `boundaryDatasetVersion`
    - src/stores/map-points.ts contains `getBoundaryByCityId`
    - src/stores/map-points.spec.ts contains `selectedBoundaryId`
    - src/stores/map-points.spec.ts contains `boundaryId`
  </acceptance_criteria>
  <done>The point store now owns boundary semantics for drafts, saved points, selected points, fallback flows, and reopen flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render saved and selected city boundaries on the map while demoting markers to auxiliary status</name>
  <files>src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts, src/components/SeedMarkerLayer.vue, src/components/SeedMarkerLayer.spec.ts</files>
  <read_first>src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts, src/components/SeedMarkerLayer.vue, src/components/SeedMarkerLayer.spec.ts, src/stores/map-points.ts, src/services/city-boundaries.ts, .planning/phases/08-城市边界高亮语义/08-CONTEXT.md</read_first>
  <behavior>
    - Test 1: The selected city renders a boundary overlay path group with stronger fill/stroke treatment than saved-only cities.
    - Test 2: Saved cities remain visible as weaker boundary highlights even when not selected.
    - Test 3: Marker emphasis is reduced so markers act as auxiliary location anchors, and fallback country flows render no boundary layer at all.
  </behavior>
  <action>Add a new SVG boundary layer to `src/components/WorldMapStage.vue` using exact class hooks `world-map-stage__boundary-layer`, `world-map-stage__boundary--saved`, and `world-map-stage__boundary--selected`. Bind the layer to `savedBoundaryIds` and `selectedBoundaryId` from the store, and draw every polygon ring for the normalized geometry returned by `src/services/city-boundaries.ts` so all sub-areas of the same city highlight together. Keep the selected boundary as the strongest visual state and saved boundaries weaker beneath it. Update `src/components/SeedMarkerLayer.vue` so the marker remains visible as an auxiliary locator for the selected point, but do not let marker styling stand in for city boundary semantics. Extend `src/components/WorldMapStage.spec.ts` and `src/components/SeedMarkerLayer.spec.ts` to assert boundary layer rendering, saved-vs-selected class hierarchy, and no boundary rendering for fallback-only states.</action>
  <verify>
    rg 'world-map-stage__boundary-layer|world-map-stage__boundary--saved|world-map-stage__boundary--selected' src/components/WorldMapStage.vue
    rg 'world-map-stage__boundary|boundary-layer' src/components/WorldMapStage.spec.ts
    rg 'selected' src/components/SeedMarkerLayer.vue src/components/SeedMarkerLayer.spec.ts
    <automated>pnpm test -- src/components/WorldMapStage.spec.ts src/components/SeedMarkerLayer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/WorldMapStage.vue contains `world-map-stage__boundary-layer`
    - src/components/WorldMapStage.vue contains `world-map-stage__boundary--saved`
    - src/components/WorldMapStage.vue contains `world-map-stage__boundary--selected`
    - src/components/WorldMapStage.spec.ts contains `boundary-layer`
    - src/components/WorldMapStage.spec.ts contains `fallback`
    - src/components/SeedMarkerLayer.vue contains `seed-marker__button--selected`
  </acceptance_criteria>
  <done>The map visually treats real city boundaries as the main expression, with saved/selected hierarchy and no false boundary on fallback paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test -- src/stores/map-points.spec.ts
- [ ] pnpm test -- src/components/WorldMapStage.spec.ts src/components/SeedMarkerLayer.spec.ts
- [ ] rg 'selectedBoundaryId|savedBoundaryIds' src/stores/map-points.ts
- [ ] rg 'world-map-stage__boundary-layer' src/components/WorldMapStage.vue
</verification>

<success_criteria>
- Selected cities render true boundary overlays rather than only marker emphasis
- Saved cities remain weakly highlighted by boundary while selected cities are stronger
- Fallback country/region paths and legacy points do not render city boundaries
- Marker rendering is auxiliary to the boundary layer, not the main city semantic
</success_criteria>

<output>
After completion, create `.planning/phases/08-城市边界高亮语义/08-02-SUMMARY.md`
</output>

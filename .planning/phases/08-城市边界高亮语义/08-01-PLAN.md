---
phase: 08-城市边界高亮语义
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/geo/city-boundaries.geo.json
  - src/services/city-boundaries.ts
  - src/services/city-boundaries.spec.ts
  - src/types/geo.ts
  - src/types/map-point.ts
  - src/services/point-storage.ts
  - src/services/point-storage.spec.ts
autonomous: true
requirements:
  - BND-02
  - DAT-06
user_setup: []
must_haves:
  truths:
    - "仓库存在可离线读取的城市边界数据资产，并能用稳定 `boundaryId` 和 `cityId` 查到对应 geometry。"
    - "v2 城市记录可以持久化 `boundaryId` 与边界数据版本，重新加载时优先按 `boundaryId` 恢复，找不到再回退到 `cityId`。"
    - "v1 旧点位在缺少边界字段时继续可读可写，但不会被错误解释成某个城市边界。"
  artifacts:
    - path: "src/services/city-boundaries.ts"
      provides: "边界 identity、geometry 规范化与查询入口"
      contains: "getBoundaryById"
    - path: "src/types/map-point.ts"
      provides: "边界身份字段"
      contains: "boundaryId"
    - path: "src/services/point-storage.ts"
      provides: "边界身份的持久化与兼容恢复"
      contains: "boundaryDatasetVersion"
  key_links:
    - from: "src/data/geo/city-boundaries.geo.json"
      to: "src/services/city-boundaries.ts"
      via: "boundary feature normalization"
      pattern: "boundaryId"
    - from: "src/services/city-boundaries.ts"
      to: "src/services/point-storage.ts"
      via: "boundary identity restoration"
      pattern: "getBoundaryById"
    - from: "src/services/point-storage.ts"
      to: "src/types/map-point.ts"
      via: "persisted point boundary fields"
      pattern: "boundaryId"
---

<objective>
Create the Phase 8 boundary identity backbone: offline city boundary data, normalized boundary lookup contracts, and legacy-safe persistence for `boundaryId`.

Purpose: The map cannot render or restore true city boundaries until the repo has a stable `cityId -> boundaryId -> geometry` path and saved points can persist boundary identity safely.
Output: Boundary dataset/service primitives plus compatibility-safe point storage for `boundaryId` and boundary dataset version.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PRD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-城市边界高亮语义/08-CONTEXT.md
@.planning/phases/08-城市边界高亮语义/08-RESEARCH.md
@src/types/geo.ts
@src/types/map-point.ts
@src/services/point-storage.ts
@src/services/point-storage.spec.ts
@src/data/geo/city-candidates.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add offline city boundary assets and normalized lookup contracts</name>
  <files>src/data/geo/city-boundaries.geo.json, src/services/city-boundaries.ts, src/services/city-boundaries.spec.ts, src/types/geo.ts</files>
  <read_first>src/data/geo/city-candidates.ts, src/types/geo.ts, .planning/phases/08-城市边界高亮语义/08-CONTEXT.md, .planning/phases/08-城市边界高亮语义/08-RESEARCH.md</read_first>
  <behavior>
    - Test 1: The repo can load an offline boundary record by exact `boundaryId`.
    - Test 2: The repo can resolve a boundary by exact `cityId` when `boundaryId` is unavailable.
    - Test 3: MultiPolygon and Polygon inputs are normalized into one render-ready shape contract so all sub-areas of the same city can be highlighted together.
  </behavior>
  <action>Add a new static asset file `src/data/geo/city-boundaries.geo.json` containing a minimal but real city boundary dataset for the Phase 8 regression cities already used in the repo, and make every feature include exact properties `boundaryId`, `cityId`, `cityName`, and `datasetVersion`. Create `src/services/city-boundaries.ts` exporting exact helpers `CITY_BOUNDARY_DATASET_VERSION`, `getBoundaryById(boundaryId: string)`, `getBoundaryByCityId(cityId: string)`, and a normalization helper that converts both `Polygon` and `MultiPolygon` geometries into one render contract with grouped rings. Extend `src/types/geo.ts` with exact boundary types used by the service, including a render-facing shape type that preserves `boundaryId`, `cityId`, and normalized polygon rings. Add `src/services/city-boundaries.spec.ts` that asserts lookup by `boundaryId`, lookup fallback by `cityId`, and multi-area normalization behavior. Do not introduce online APIs, map engines, or runtime name-based fuzzy lookup.</action>
  <verify>
    rg 'boundaryId|cityId|datasetVersion' src/data/geo/city-boundaries.geo.json src/services/city-boundaries.ts src/types/geo.ts
    rg 'getBoundaryById|getBoundaryByCityId|CITY_BOUNDARY_DATASET_VERSION' src/services/city-boundaries.ts
    <automated>pnpm test -- src/services/city-boundaries.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/data/geo/city-boundaries.geo.json contains `"boundaryId"`
    - src/data/geo/city-boundaries.geo.json contains `"cityId"`
    - src/data/geo/city-boundaries.geo.json contains `"datasetVersion"`
    - src/services/city-boundaries.ts contains `getBoundaryById`
    - src/services/city-boundaries.ts contains `getBoundaryByCityId`
    - src/services/city-boundaries.ts contains `CITY_BOUNDARY_DATASET_VERSION`
    - src/types/geo.ts contains `boundaryId`
    - src/services/city-boundaries.spec.ts contains `getBoundaryById`
  </acceptance_criteria>
  <done>The repo has a local city-boundary dataset and service contracts that can restore the correct geometry by `boundaryId` or `cityId` without guessing by display text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend persisted point models for boundary identity and fail-closed legacy restore</name>
  <files>src/types/map-point.ts, src/services/point-storage.ts, src/services/point-storage.spec.ts</files>
  <read_first>src/types/map-point.ts, src/services/point-storage.ts, src/services/point-storage.spec.ts, src/services/city-boundaries.ts, .planning/phases/08-城市边界高亮语义/08-CONTEXT.md</read_first>
  <behavior>
    - Test 1: New v2 city points can persist exact nullable fields `boundaryId` and `boundaryDatasetVersion`.
    - Test 2: Legacy v1 snapshots missing those fields still load successfully with `null` boundary metadata.
    - Test 3: Restored points never fabricate a boundary when both `cityId` and `boundaryId` are absent.
  </behavior>
  <action>Expand `src/types/map-point.ts` so `MapPointDisplay`, `DraftMapPoint`, and `PersistedMapPoint` all include exact nullable fields `boundaryId` and `boundaryDatasetVersion` in addition to the existing `cityId`, `cityName`, `cityContextLabel`, `lat`, `lng`, `x`, and `y`. Update `src/services/point-storage.ts` so `normalizePersistedPoint()` reads `boundaryId` and `boundaryDatasetVersion` as exact string-or-null fields, treats missing values as `null`, and preserves snapshot version `1` compatibility rather than rejecting old data. Ensure `savePointStorageSnapshot()` writes the new fields for v2 city records. Extend `src/services/point-storage.spec.ts` with explicit cases for a boundary-aware v2 record, a boundary-less legacy record, and a fail-closed restore path where no boundary is inferred for a point with `cityId: null` and `boundaryId: null`.</action>
  <verify>
    rg 'boundaryId|boundaryDatasetVersion' src/types/map-point.ts src/services/point-storage.ts
    rg 'boundaryId|legacy|null' src/services/point-storage.spec.ts
    <automated>pnpm test -- src/services/point-storage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/types/map-point.ts contains `boundaryId`
    - src/types/map-point.ts contains `boundaryDatasetVersion`
    - src/services/point-storage.ts contains `boundaryId`
    - src/services/point-storage.ts contains `boundaryDatasetVersion`
    - src/services/point-storage.spec.ts contains `boundaryId`
    - src/services/point-storage.spec.ts contains `legacy`
    - src/services/point-storage.spec.ts contains `null`
  </acceptance_criteria>
  <done>Saved city records can persist boundary identity safely, while legacy points remain compatible and do not falsely light a city boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test -- src/services/city-boundaries.spec.ts
- [ ] pnpm test -- src/services/point-storage.spec.ts
- [ ] rg 'boundaryId' src/types/geo.ts src/types/map-point.ts src/services/city-boundaries.ts src/services/point-storage.ts
- [ ] Confirm `src/data/geo/city-boundaries.geo.json` includes real multi-area-capable boundary records with `datasetVersion`
</verification>

<success_criteria>
- The repo contains an offline city-boundary dataset with stable `boundaryId`
- Boundary lookup can resolve by `boundaryId` first and `cityId` second
- Saved city records persist boundary identity fields without breaking legacy snapshots
- Legacy points still load with `null` boundary metadata and do not produce false city highlights
</success_criteria>

<output>
After completion, create `.planning/phases/08-城市边界高亮语义/08-01-SUMMARY.md`
</output>

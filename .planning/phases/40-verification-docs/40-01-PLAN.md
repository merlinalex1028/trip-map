---
phase: 40
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/38-ui/38-VERIFICATION.md
  - .planning/phases/39-map-popup/39-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 38 有 VERIFICATION.md，记录所有 must-have 验证结果，包含每个 artifact 的 evidence"
    - "Phase 39 有 VERIFICATION.md，记录所有 must-have 验证结果，包含每个 artifact 的 evidence"
    - "两个 VERIFICATION.md 均包含：goal achievement、required artifacts、key link verification、behavioral spot-checks"
    - "所有验证基于当前源代码和测试结果（非假设），每个 claim 有对应 evidence"
  artifacts:
    - path: ".planning/phases/38-ui/38-VERIFICATION.md"
      provides: "Phase 38 must-have 验证报告"
      min_lines: 80
    - path: ".planning/phases/39-map-popup/39-VERIFICATION.md"
      provides: "Phase 39 must-have 验证报告"
      min_lines: 80
  key_links:
    - from: ".planning/phases/38-ui/38-VERIFICATION.md"
      to: "apps/web/src/components/timeline/*"
      via: "Verification evidence references (file:line)"
      pattern: "component"
    - from: ".planning/phases/39-map-popup/39-VERIFICATION.md"
      to: "apps/web/src/components/map-popup/*"
      via: "Verification evidence references (file:line)"
      pattern: "component"
---

<objective>
为 Phase 38（时间轴编辑/删除 UI）和 Phase 39（Map Popup 集成）生成 VERIFICATION.md，确认已实现功能满足验收标准。

Purpose: 补齐 v7.0 里程碑的验证文档，确保每个已完成 phase 的 must-have 有正式的 verification 记录。
Output: 
- `.planning/phases/38-ui/38-VERIFICATION.md`
- `.planning/phases/39-map-popup/39-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-data-layer-extension/36-VERIFICATION.md（格式参考）
@.planning/phases/37-store-api/37-VERIFICATION.md（格式参考）
@.planning/phases/38-ui/38-01-PLAN.md
@.planning/phases/38-ui/38-02-PLAN.md
@.planning/phases/38-ui/38-01-SUMMARY.md
@.planning/phases/38-ui/38-02-SUMMARY.md
@.planning/phases/39-map-popup/39-01-PLAN.md
@.planning/phases/39-map-popup/39-01-SUMMARY.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- 格式参考来自 Phase 36 VERIFICATION.md -->
Phase 36 VERIFICATION.md 结构：
- frontmatter: phase, verified, status, score, re_verification
- ## Goal Achievement
  - Observable Truths table (# | Truth | Status | Evidence)
  - Plan 01 Must-Haves table (# | Truth | Status | Evidence)
  - Plan 02 Must-Haves table (# | Truth | Status | Evidence)
- ## Required Artifacts (artifact | expected | status | details)
- ## Key Link Verification (from | to | via | status | details)
- ## Behavioral Spot-Checks (behavior | command | result | status)
- ## Anti-Patterns Found
- ## Minor Deviations (non-blocking)
- ## Human Verification Required
- ## Gaps Summary

Phase 37 VERIFICATION.md 结构（变体）：
- 同上的 frontmatter
- ## Goal Achievement with Observable Truths
- ## Required Artifacts
- ## Key Link Verification
- ## Behavioral Spot-Checks（含 grep 验证）
- ## Requirements Coverage（按 requirement ID 映射）
- ## Anti-Patterns Found

<!-- 验证时需读取的源文件路径 -->
Phase 38 源文件：
- apps/web/src/services/timeline.ts（TimelineEntry 接口 + toTimelineEntry）
- apps/web/src/services/date-conflict.ts（checkDateConflict 纯函数）
- apps/web/src/services/date-conflict.spec.ts（测试用例）
- apps/web/src/components/timeline/TagInput.vue
- apps/web/src/components/timeline/TagInput.spec.ts
- apps/web/src/components/timeline/TimelineEditForm.vue
- apps/web/src/components/timeline/TimelineEditForm.spec.ts
- apps/web/src/components/timeline/ConfirmDialog.vue
- apps/web/src/components/timeline/ConfirmDialog.spec.ts
- apps/web/src/components/timeline/TimelineVisitCard.vue
- apps/web/src/components/timeline/TimelineVisitCard.spec.ts

Phase 38 测试命令：
- pnpm --filter @trip-map/web test
- pnpm --filter @trip-map/web typecheck

Phase 39 源文件：
- apps/web/src/components/map-popup/PopupTripRecord.vue
- apps/web/src/components/map-popup/PopupTripRecord.spec.ts
- apps/web/src/components/map-popup/PointSummaryCard.vue
- apps/web/src/components/map-popup/PointSummaryCard.spec.ts
- apps/web/src/components/map-popup/PointSummaryCard.kawaii.spec.ts
- apps/web/src/components/map-popup/MapContextPopup.spec.ts
- apps/web/src/components/map-popup/MapContextPopup.kawaii.spec.ts
- apps/web/src/components/LeafletMapStage.spec.ts

Phase 39 测试命令：
- pnpm --filter @trip-map/web test
- pnpm --filter @trip-map/web typecheck
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 生成 Phase 38 VERIFICATION.md</name>
  <files>
    .planning/phases/38-ui/38-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/36-data-layer-extension/36-VERIFICATION.md（格式参考）
    - .planning/phases/38-ui/38-01-PLAN.md（must-haves: truths / artifacts / key_links）
    - .planning/phases/38-ui/38-02-PLAN.md（must-haves: truths / artifacts / key_links）
    - .planning/phases/38-ui/38-01-SUMMARY.md（Git 提交记录、测试结果）
    - .planning/phases/38-ui/38-02-SUMMARY.md（Git 提交记录、测试结果）
    - .planning/ROADMAP.md（Phase 38 Success Criteria）
  </read_first>
  <action>
    在 `.planning/phases/38-ui/` 下创建 `38-VERIFICATION.md`。

    **格式：** 遵循 Phase 36 VERIFICATION.md 的 frontmatter + 章节结构。

    **Frontmatter：**
    ```yaml
    ---
    phase: 38-ui
    verified: {当前日期}T{当前时间}Z
    status: passed
    score: {N}/{M} must-haves verified
    re_verification: false
    ---
    ```

    **内容生成步骤：**

    1. **Goal Achievement — Observable Truths**（来自 ROADMAP.md Phase 38 的 Success Criteria）：
       - 为用户编辑日期、备注、标签的 UI 可用性
       - 日期冲突检查在前端的警告展示
       - 删除确认弹窗和最后一条警告
       - 删除最后一条时提示取消点亮

    2. **Plan 01 Must-Haves** — 验证 38-01-PLAN.md 中 must_haves 的 truths：
       - TimelineEntry 接口包含 notes/tags 字段 → 检查 `apps/web/src/services/timeline.ts`
       - TimelineEditForm 含日期修改/备注/标签输入 → 检查组件导出
       - TagInput 支持添加/删除标签 → 检查组件行为
       - checkDateConflict 纯函数存在 → 检查函数签名
       
       每个 truth 需要文件:行号引用作为 evidence，格式如 "component:line-number"

    3. **Plan 02 Must-Haves** — 验证 38-02-PLAN.md 中 must_haves 的 truths：
       - ConfirmDialog 确认弹窗实现
       - TimelineVisitCard 编辑/删除集成
       - 最后一条 destructive 警告
       - ESC/backdrop 关闭弹窗

    4. **Required Artifacts** — 列出所有文件并验证存在性：
       使用 grep 验证每个文件包含预期内容：
       ```bash
       grep -n "notes: string | null" apps/web/src/services/timeline.ts
       grep -n "export function checkDateConflict" apps/web/src/services/date-conflict.ts
       grep -n "defineProps" apps/web/src/components/timeline/TagInput.vue
       grep -n "defineEmits" apps/web/src/components/timeline/TimelineEditForm.vue
       grep -n "data-region=\"tag-input\"" apps/web/src/components/timeline/TagInput.vue
       grep -n "data-confirm-dialog-backdrop" apps/web/src/components/timeline/ConfirmDialog.vue
       grep -n "data-card-edit" apps/web/src/components/timeline/TimelineVisitCard.vue
       grep -n "data-card-delete" apps/web/src/components/timeline/TimelineVisitCard.vue
       ```
       将命令结果作为 evidence 写入。

    5. **Key Link Verification** — 检查 import 关系是否实际连接：
       ```bash
       grep -n "import.*TimelineEditForm" apps/web/src/components/timeline/TimelineVisitCard.vue
       grep -n "import.*ConfirmDialog" apps/web/src/components/timeline/TimelineVisitCard.vue
       grep -n "import.*checkDateConflict" apps/web/src/components/timeline/TimelineEditForm.vue
       grep -n "mapPointsStore\\.updateRecord\\|mapPointsStore\\.deleteSingleRecord" apps/web/src/components/timeline/TimelineVisitCard.vue
       ```

    6. **Behavioral Spot-Checks** — 运行验证命令并记录结果：
       ```bash
       echo "=== TypeScript Check ==="
       pnpm --filter @trip-map/web typecheck 2>&1; echo "EXIT: $?"
       echo "=== Test Run ==="
       pnpm --filter @trip-map/web test 2>&1 | tail -5
       ```

    7. **Requirements Coverage** — 映射 Phase 38 涉及的需求：
       - EDIT-01（日期修改）：38-01 TimelineEditForm + 38-02 TimelineVisitCard
       - EDIT-02（备注）：38-01 TimelineEditForm
       - EDIT-03（标签）：38-01 TagInput
       - EDIT-04（日期冲突）：38-01 checkDateConflict + TimelineEditForm
       - DEL-01（删除）：38-02 ConfirmDialog + TimelineVisitCard
       - DEL-02（确认弹窗）：38-02 ConfirmDialog
       - DEL-03（最后一条提示）：38-02 TimelineVisitCard

    8. **Anti-Patterns Found & Minor Deviations** — 根据 SUMMARY.md 中的 deviations 填写。

    9. **Gaps Summary** — 如无未覆盖项，写 "无 gaps"。

    每个 evidence 必须基于真实的源代码检查或命令执行结果，不得使用假设性表述。
  </action>
  <verify>
    <automated>test -f .planning/phases/38-ui/38-VERIFICATION.md && head -1 .planning/phases/38-ui/38-VERIFICATION.md | grep -q '^---' && echo "VERIFICATION_CREATED=true" || echo "VERIFICATION_CREATED=false"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/38-ui/38-VERIFICATION.md` 存在且包含有效 YAML frontmatter
    - frontmatter 包含 phase, verified, status, score, re_verification 字段
    - ## Goal Achievement 章节存在，包含 Observable Truths 表格
    - ## Required Artifacts 表格包含所有 Phase 38 产出的关键文件
    - ## Key Link Verification 表格覆盖 Plan 01 和 Plan 02 的 key_links
    - ## Behavioral Spot-Checks 章节包含 git grep 验证和测试命令结果
    - ## Requirements Coverage 章节列出所有 Phase 38 涉及需求（EDIT-01~04, DEL-01~03）
    - 所有 evidence 引用具体文件:行号
  </acceptance_criteria>
  <done>Phase 38 VERIFICATION.md 生成完成，覆盖全部 must-have 验证项</done>
</task>

<task type="auto">
  <name>Task 2: 生成 Phase 39 VERIFICATION.md</name>
  <files>
    .planning/phases/39-map-popup/39-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/36-data-layer-extension/36-VERIFICATION.md（格式参考）
    - .planning/phases/39-map-popup/39-01-PLAN.md（must-haves: truths / artifacts / key_links）
    - .planning/phases/39-map-popup/39-01-SUMMARY.md（Git 提交记录、测试结果、deviations）
    - .planning/ROADMAP.md（Phase 39 Success Criteria）
    - .planning/phases/38-ui/38-VERIFICATION.md（Task 1 产出，保持格式一致）
  </read_first>
  <action>
    在 `.planning/phases/39-map-popup/` 下创建 `39-VERIFICATION.md`。

    **格式：** 与 Task 1 产出的 38-VERIFICATION.md 保持一致的结构。

    **Frontmatter：**
    ```yaml
    ---
    phase: 39-map-popup
    verified: {当前日期}T{当前时间}Z
    status: passed
    score: {N}/{M} must-haves verified
    re_verification: false
    ---
    ```

    **内容生成步骤：**

    1. **Goal Achievement — Observable Truths**（来自 ROADMAP.md Phase 39 的 Success Criteria）：
       - 用户可在地图 popup 中对已有记录执行编辑和删除操作
       - 删除前展示确认弹窗，删除最后一条记录时提示将取消点亮
       - 从地图 popup 编辑后，时间轴和统计数据同步更新

    2. **Must-Haves** — 验证 39-01-PLAN.md 中 must_haves 的 truths：
       - 用户在地图 popup 中看到记录日期、备注和标签
       - 用户可在地图 popup 中点击编辑按钮进入编辑模式
       - 用户可在地图 popup 中点击删除按钮，弹出确认弹窗后确认删除
       - 删除最后一条记录时确认弹窗使用 destructive 风格
       - 编辑或删除记录后，时间轴和统计数据自动同步

    3. **Required Artifacts** — 列出所有文件并验证存在性：
       使用 grep 验证每个文件包含预期内容：
       ```bash
       grep -n "data-region=\"popup-trip-record\"" apps/web/src/components/map-popup/PopupTripRecord.vue
       grep -n "import.*TimelineEditForm" apps/web/src/components/map-popup/PopupTripRecord.vue
       grep -n "import.*ConfirmDialog" apps/web/src/components/map-popup/PopupTripRecord.vue
       grep -n "import.*checkDateConflict" apps/web/src/components/map-popup/PopupTripRecord.vue
       grep -n "mapPointsStore\\.updateRecord\\|mapPointsStore\\.deleteSingleRecord" apps/web/src/components/map-popup/PopupTripRecord.vue
       grep -n "import.*PopupTripRecord" apps/web/src/components/map-popup/PointSummaryCard.vue
       grep -n "data-region=\"popup-records\"" apps/web/src/components/map-popup/PointSummaryCard.vue
       ```
       将命令结果作为 evidence 写入。

    4. **Key Link Verification** — 检查组件间的 import 关系：
       ```bash
       grep -n "import.*useMapPointsStore" apps/web/src/components/map-popup/PopupTripRecord.vue
       grep -n "import.*useMapPointsStore" apps/web/src/components/map-popup/PointSummaryCard.vue
       grep -n "import.*buildTimelineEntries" apps/web/src/components/map-popup/PointSummaryCard.vue
       grep -n "describe('PopupTripRecord'" apps/web/src/components/map-popup/PopupTripRecord.spec.ts
       ```

    5. **Behavioral Spot-Checks** — 运行验证命令并记录结果：
       ```bash
       echo "=== TypeScript Check ==="
       pnpm --filter @trip-map/web typecheck 2>&1; echo "EXIT: $?"
       echo "=== Test Run ==="
       pnpm --filter @trip-map/web test 2>&1 | tail -5
       # 额外验证测试文件存在性
       echo "=== PopupTripRecord Test Count ==="
       grep -c "it(" apps/web/src/components/map-popup/PopupTripRecord.spec.ts
       ```

    6. **Anti-Patterns Found & Minor Deviations** — 根据 SUMMARY.md 填写 deviations 信息（如 storeToRefs 与 vi.mock 不兼容的处理）。

    7. **Gaps Summary** — 如无未覆盖项，写 "无 gaps"。

    每个 evidence 必须基于真实的源代码检查或命令执行结果，不得使用假设性表述。
  </action>
  <verify>
    <automated>test -f .planning/phases/39-map-popup/39-VERIFICATION.md && head -1 .planning/phases/39-map-popup/39-VERIFICATION.md | grep -q '^---' && echo "VERIFICATION_CREATED=true" || echo "VERIFICATION_CREATED=false"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/39-map-popup/39-VERIFICATION.md` 存在且包含有效 YAML frontmatter
    - frontmatter 包含 phase, verified, status, score, re_verification 字段
    - ## Goal Achievement 章节存在，包含 Observable Truths 表格
    - ## Required Artifacts 表格包含所有 Phase 39 产出的关键文件
    - ## Key Link Verification 表格覆盖 Plan 01 的 key_links
    - ## Behavioral Spot-Checks 章节包含 git grep 验证和测试命令结果
    - 所有 evidence 引用具体文件:行号
    - 格式与 Task 1 产出的 38-VERIFICATION.md 保持一致
  </acceptance_criteria>
  <done>Phase 39 VERIFICATION.md 生成完成，覆盖全部 must-have 验证项</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A — 纯文档阶段 | Phase 40 不创建或修改任何应用代码、API 端点或数据模型。VERIFICATION.md 是只读验证文档，不引入新的安全攻击面。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | VERIFICATION.md 内容 | mitigate | 所有 evidence 来自 git grep 命令输出和测试结果，拒绝未经源码验证的 claims。每个 truth 必须有对应文件:行号引用。 |
| T-40-02 | Repudiation | VERIFICATION.md 准确性 | accept | 验证文档为持续集成资产而非审计证据。将来代码变更后需重新验证。当前验证基于已知通过的测试套件（396 PASS，43 test files）。 |
</threat_model>

<verification>
1. `test -f .planning/phases/38-ui/38-VERIFICATION.md && test -f .planning/phases/39-map-popup/39-VERIFICATION.md` — 两个文件均存在
2. `head -1 .planning/phases/38-ui/38-VERIFICATION.md | grep '^---'` — 有效 frontmatter
3. `head -1 .planning/phases/39-map-popup/39-VERIFICATION.md | grep '^---'` — 有效 frontmatter
4. 两个文件均包含 Observable Truths、Required Artifacts、Key Link Verification、Behavioral Spot-Checks 章节
5. 所有 grep evidence 基于真实的 `grep -n` 命令输出
6. 测试命令 `pnpm --filter @trip-map/web test` 结果为 396 passed, 0 failed
</verification>

<success_criteria>
- Phase 38 有 VERIFICATION.md，记录所有 must-have 验证结果，包含每个 artifact 的 evidence
- Phase 39 有 VERIFICATION.md，记录所有 must-have 验证结果，包含每个 artifact 的 evidence
- 两个 VERIFICATION.md 均包含：goal achievement、required artifacts、key link verification、behavioral spot-checks
- 所有验证基于当前源代码和测试结果，每个 claim 有对应 evidence（文件:行号）
</success_criteria>

<output>
After completion, create `.planning/phases/40-verification-docs/40-01-SUMMARY.md`
</output>

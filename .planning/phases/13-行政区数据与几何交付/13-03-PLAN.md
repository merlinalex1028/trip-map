---
phase: 13-行政区数据与几何交付
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - apps/server/src/modules/canonical-places/canonical-places.service.ts
  - apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts
  - apps/server/test/canonical-resolve.e2e-spec.ts
  - apps/web/src/services/geometry-loader.ts
  - apps/web/src/services/geometry-loader.spec.ts
autonomous: true
requirements:
  - GEOX-05
  - GEOX-06
  - GEOX-07
  - API-03
must_haves:
  truths:
    - "server resolved/ambiguous 响应必须返回 `geometryRef`，前端可以只靠 `geometryDatasetVersion + assetKey` 按需加载对应 shard。"
    - "web loader 必须按 `CN` / `OVERSEAS` shard 独立加载和缓存，不请求任何合并 GeoJSON。"
    - "运行时链路仍然只返回引用，不把完整 polygon 塞进 API payload。"
  artifacts:
    - "apps/server/src/modules/canonical-places/canonical-places.service.ts 通过 generated manifest 把 `geometryRef` 注入 resolved/ambiguous 响应。"
    - "apps/server/test/canonical-resolve.e2e-spec.ts 断言 Beijing/Hong Kong/California 的 `geometryRef` 精确值。"
    - "apps/web/src/services/geometry-loader.ts 提供 shard fetch + cache 能力。"
    - "apps/web/src/services/geometry-loader.spec.ts 锁定 layer-aware URL、缓存 key 与失败行为。"
  key_links:
    - "server 的 `boundaryId` 必须先命中 generated manifest，再写入 `geometryRef`；不能在 fixture/service 里再次手写另一份 assetKey 映射。"
    - "web loader 的请求 URL 必须严格来自 `geometryRef.geometryDatasetVersion` 与 `geometryRef.assetKey`，不能靠 boundaryId 拼猜路径。"
    - "server e2e 与 web loader spec 都必须证明 API-03 走的是引用模式，而不是内联 geometry。"
---

<objective>
把 Phase 13 生成的几何资产接到真实运行时边界上：`server` 开始返回 `geometryRef`，`web` 开始支持按 shard 读取与缓存，为 Phase 14 的 Leaflet 主链路迁移预置正式 loader。

Purpose: 把静态数据资产从“已生成”推进到“可被 API 和消费侧稳定引用”。
Output: server geometryRef 响应、web shard loader/cache，以及覆盖 API-03 与 GEOX-05 的自动化回归。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-行政区数据与几何交付/13-CONTEXT.md
@.planning/phases/13-行政区数据与几何交付/13-RESEARCH.md
@.planning/phases/13-行政区数据与几何交付/13-VALIDATION.md
@.planning/phases/13-行政区数据与几何交付/13-01-PLAN.md
@.planning/phases/13-行政区数据与几何交付/13-02-PLAN.md
@packages/contracts/src/generated/geometry-manifest.generated.ts
@packages/contracts/src/resolve.ts
@apps/server/src/modules/canonical-places/canonical-places.service.ts
@apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts
@apps/server/test/canonical-resolve.e2e-spec.ts
@apps/web/src/services/api/canonical-places.ts

<interfaces>
Phase 13 generated manifest entry shape:

```ts
export interface GeometryManifestEntry {
  boundaryId: string
  layer: 'CN' | 'OVERSEAS'
  geometryDatasetVersion: string
  assetKey: string
  renderableId: string | null
  sourceDataset: 'DATAV_GEOATLAS_CN' | 'NATURAL_EARTH_ADMIN1'
  sourceVersion: string
  sourceFeatureId: string
}
```

Current server response path:

```ts
resolve(input: ResolveCanonicalPlaceRequest): CanonicalResolveResponse
confirm(input: ConfirmCanonicalPlaceRequest): CanonicalResolveResponse
```

Current web API wrapper path:

```ts
export async function resolveCanonicalPlace(
  payload: ResolveCanonicalPlaceRequest,
): Promise<CanonicalResolveResponse>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 让 `/places/resolve` 与 `/places/confirm` 返回 manifest-backed `geometryRef`</name>
  <files>apps/server/src/modules/canonical-places/canonical-places.service.ts, apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts, apps/server/test/canonical-resolve.e2e-spec.ts</files>
  <read_first>
    - .planning/phases/13-行政区数据与几何交付/13-01-PLAN.md
    - .planning/phases/13-行政区数据与几何交付/13-02-PLAN.md
    - packages/contracts/src/generated/geometry-manifest.generated.ts
    - packages/contracts/src/fixtures.ts
    - apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts
    - apps/server/src/modules/canonical-places/canonical-places.service.ts
    - apps/server/test/canonical-resolve.e2e-spec.ts
  </read_first>
  <behavior>
    - Test 1: Beijing、Hong Kong、California 的 resolved 响应都带 `geometryRef`，且 `assetKey` 分别为 `cn/beijing.json`、`cn/hong-kong.json`、`overseas/us.json`。
    - Test 2: ambiguous `/places/resolve` 与合法 `/places/confirm` 的 candidate/place 也带 `geometryRef`，并沿用 generated manifest 的 layer 和 renderableId。
    - Test 3: failed 分支仍然不带 `place`、`geometry` 或 `features`，确保 API-03 只返回引用而不是内联 GeoJSON。
  </behavior>
  <action>
    按 D-01、D-03、D-06、D-07 修改 server 运行时，要求 `apps/server/src/modules/canonical-places/canonical-places.service.ts` 不再在 service 或 fixture 中手写第二份 asset mapping：
    - 从 `@trip-map/contracts` 导入 generated manifest 常量
    - 新增一个以 `boundaryId` 为 key 的 lookup helper，专门把 canonical boundaryId 映射到 manifest entry
    - `buildResponse()` 在 resolved/ambiguous 分支返回的 `place`/`candidates` 上保留既有 summary 字段，并追加 `geometryRef`，其字段值必须直接来自 manifest entry

    如果 `fixtures/canonical-place-fixtures.ts` 里还残留硬编码 `geometryRef`，请将其收口到“boundaryId 唯一真源 + service 读取 manifest”的模式；fixture 只保留 canonical summary 所需的 `boundaryId` 与语义字段，不再手抄 `assetKey`。

    更新 `apps/server/test/canonical-resolve.e2e-spec.ts`：
    - Beijing resolved case 断言 `place.geometryRef.assetKey === 'cn/beijing.json'`
    - Hong Kong resolved case 断言 `place.geometryRef.assetKey === 'cn/hong-kong.json'`
    - California resolved case 断言 `place.geometryRef.assetKey === 'overseas/us.json'` 且 `place.geometryRef.layer === 'OVERSEAS'`
    - ambiguous case 断言 `candidates[0]` 含 `geometryRef`
    - failed case 显式断言 response 不包含 `geometry` 与 `features`

    保持 `/places/resolve` 与 `/places/confirm` 的 HTTP shape、DTO、status code、`recommendedPlaceId` 行为不变；本任务只增加 geometry 引用，不在 Phase 13 内联任何完整 polygon。
  </action>
  <acceptance_criteria>
    - `apps/server/src/modules/canonical-places/canonical-places.service.ts` 包含精确字符串 `geometryRef:`.
    - `apps/server/src/modules/canonical-places/canonical-places.service.ts` 包含精确字符串 `boundaryId`.
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 包含精确字符串 `cn/beijing.json`.
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 包含精确字符串 `cn/hong-kong.json`.
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 包含精确字符串 `overseas/us.json`.
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 包含精确字符串 `not.toHaveProperty('geometry')`.
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 包含精确字符串 `not.toHaveProperty('features')`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/server test test/canonical-resolve.e2e-spec.ts</automated>
  </verify>
  <done>`server` 已把 Phase 13 generated manifest 接入真实响应边界，前端不再需要自行猜测几何 shard 路径。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 web 侧提供按 shard 加载与缓存的 geometry loader</name>
  <files>apps/web/src/services/geometry-loader.ts, apps/web/src/services/geometry-loader.spec.ts</files>
  <read_first>
    - .planning/phases/13-行政区数据与几何交付/13-RESEARCH.md
    - packages/contracts/src/geometry.ts
    - packages/contracts/src/generated/geometry-manifest.generated.ts
    - apps/web/src/services/api/canonical-places.ts
    - apps/web/src/types/geo.ts
  </read_first>
  <behavior>
    - Test 1: `geometry-loader` 根据 `geometryRef.geometryDatasetVersion` 与 `geometryRef.assetKey` 精确请求 `/geo/2026-03-31-geo-v1/{assetKey}`。
    - Test 2: 同一个 `geometryDatasetVersion + assetKey` 连续请求时命中同一缓存 promise，不重复发起第二次 fetch。
    - Test 3: loader 只从 `cn/*.json` 或 `overseas/*.json` 读取 shard，不请求 `all.json`、`combined.json`，也不修改 `WorldMapStage.vue` 做提前迁移。
  </behavior>
  <action>
    新增 `apps/web/src/services/geometry-loader.ts`，提供以下精确导出：
    - `export async function loadGeometryShard(geometryDatasetVersion: string, assetKey: string): Promise<CityBoundaryFeatureCollection>`
    - `export async function loadGeometryFeatureByRef(geometryRef: GeometryRef): Promise<CityBoundaryFeature | null>`
    - `export function clearGeometryShardCacheForTest(): void`

    loader 的实现必须满足：
    - fetch URL 精确为 ``/geo/${geometryDatasetVersion}/${assetKey}``
    - 内存缓存 key 精确为 `${geometryDatasetVersion}:${assetKey}`
    - `loadGeometryFeatureByRef()` 必须使用 `geometryRef.renderableId ?? geometryRef.boundaryId` 在 shard `features` 中查找目标 feature
    - 当 shard 加载成功但找不到对应 feature 时返回 `null`
    - 当 fetch 非 2xx 时抛出错误，错误信息中至少带上 `assetKey`

    新增 `apps/web/src/services/geometry-loader.spec.ts`，显式覆盖：
    - 使用 Beijing `geometryRef` 时请求 `/geo/2026-03-31-geo-v1/cn/beijing.json`
    - 使用 California `geometryRef` 时请求 `/geo/2026-03-31-geo-v1/overseas/us.json`
    - 同一 `assetKey` 二次请求只发生一次 fetch
    - 任一测试中都不得出现 `all.json`、`combined.json` 或把 `boundaryId` 直接拼成路径的断言

    本任务不要修改 `apps/web/src/components/WorldMapStage.vue`、`apps/web/src/services/city-boundaries.ts` 或 popup/drawer 组件；Phase 13 只交付 loader，不提前做 Phase 14 的 Leaflet 主链路迁移。
  </action>
  <acceptance_criteria>
    - `apps/web/src/services/geometry-loader.ts` 包含精确字符串 `loadGeometryShard`.
    - `apps/web/src/services/geometry-loader.ts` 包含精确字符串 `loadGeometryFeatureByRef`.
    - `apps/web/src/services/geometry-loader.ts` 包含精确字符串 `` `/geo/${geometryDatasetVersion}/${assetKey}` ``.
    - `apps/web/src/services/geometry-loader.ts` 包含精确字符串 `${geometryDatasetVersion}:${assetKey}`.
    - `apps/web/src/services/geometry-loader.spec.ts` 包含精确字符串 `/geo/2026-03-31-geo-v1/cn/beijing.json`.
    - `apps/web/src/services/geometry-loader.spec.ts` 包含精确字符串 `/geo/2026-03-31-geo-v1/overseas/us.json`.
    - `apps/web/src/services/geometry-loader.spec.ts` 不包含精确字符串 `all.json`.
    - `apps/web/src/services/geometry-loader.spec.ts` 不包含精确字符串 `combined.json`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test src/services/geometry-loader.spec.ts</automated>
  </verify>
  <done>web 已拥有正式的 shard loader/cache，Phase 14 可以直接在 Leaflet 消费 `geometryRef` 而不再依赖 bundle 内嵌 GeoJSON。</done>
</task>

</tasks>

<verification>
- `pnpm --filter @trip-map/server test test/canonical-resolve.e2e-spec.ts`
- `pnpm --filter @trip-map/web test src/services/geometry-loader.spec.ts`
- `pnpm --filter @trip-map/contracts test`
</verification>

<success_criteria>
- `/places/resolve` 和 `/places/confirm` 返回的 resolved/ambiguous place 都包含 `geometryRef`，且没有内联 GeoJSON。
- web 可以仅凭 `geometryRef` 加载北京、中国香港、California 对应 shard，并对相同 shard 做缓存。
- 中国与海外图层在运行时层面保持分离寻址，为 Phase 14 的 Leaflet 双图层迁移提供稳定输入。
</success_criteria>

<output>
After completion, create `.planning/phases/13-行政区数据与几何交付/13-03-SUMMARY.md`
</output>

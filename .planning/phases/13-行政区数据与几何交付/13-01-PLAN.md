---
phase: 13-行政区数据与几何交付
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/geometry.ts
  - packages/contracts/src/resolve.ts
  - packages/contracts/src/index.ts
  - packages/contracts/src/fixtures.ts
  - packages/contracts/src/contracts.spec.ts
autonomous: true
requirements:
  - GEOX-06
  - API-03
must_haves:
  truths:
    - "调用 canonical resolve/confirm 的消费方可以从 resolved/ambiguous 响应拿到稳定的 `geometryRef`，而不是内联 GeoJSON。"
    - "消费方能够区分 canonical `datasetVersion` 与 `geometryDatasetVersion`，不会把地点目录版本误当成几何资产版本。"
    - "Beijing、Hong Kong、California 等代表性地点在共享 fixture 中都能解析到可追踪的 layer、assetKey 与 renderableId。"
  artifacts:
    - "packages/contracts/src/geometry.ts 提供 `GeometryLayer`、`GeometrySourceDataset`、`GeometryRef`、`GeometryManifestEntry` 等 Phase 13 共用类型。"
    - "packages/contracts/src/resolve.ts 把 `CanonicalResolveResponse` 的 resolved/ambiguous 分支收口为带 `geometryRef` 的 place shape。"
    - "packages/contracts/src/fixtures.ts 与 packages/contracts/src/contracts.spec.ts 固定 Beijing/Hong Kong/California 的 `geometryRef` 样例。"
  key_links:
    - "packages/contracts/src/resolve.ts 必须从 packages/contracts/src/geometry.ts 引入 `GeometryRef`，不能把 Phase 13 字段散落成匿名对象。"
    - "packages/contracts/src/fixtures.ts 中的 `geometryRef.assetKey` 必须与后续 shard 文件名完全一致：`cn/beijing.json`、`cn/hong-kong.json`、`overseas/us.json`。"
    - "packages/contracts/src/contracts.spec.ts 必须显式断言 resolved/ambiguous 分支存在 `geometryRef`，而 failed 分支仍然不包含 `place`。"
---

<objective>
先固定 Phase 13 的共享几何契约，让后续数据资产与 server/web wiring 都围绕同一套 `geometryRef`、layer 与 manifest 类型推进。

Purpose: 先收口 contract，避免数据资产、API 响应和 web loader 在 Phase 13 各自发明字段名。
Output: Phase 13 geometry types、带 `geometryRef` 的 canonical resolve contract、以及可执行的 fixture/spec 基线。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-行政区数据与几何交付/13-CONTEXT.md
@.planning/phases/13-行政区数据与几何交付/13-RESEARCH.md
@.planning/phases/13-行政区数据与几何交付/13-VALIDATION.md
@.planning/phases/12-canonical/12-05-SUMMARY.md
@packages/contracts/src/place.ts
@packages/contracts/src/resolve.ts
@packages/contracts/src/fixtures.ts
@packages/contracts/src/contracts.spec.ts

<interfaces>
Current canonical summary contract from `packages/contracts/src/place.ts`:

```ts
export interface CanonicalPlaceRef {
  placeId: string
  boundaryId: string
  placeKind: PlaceKind
  datasetVersion: string
}

export interface CanonicalPlaceSummary extends CanonicalPlaceRef {
  displayName: string
  regionSystem: 'CN' | 'OVERSEAS'
  adminType: ChinaAdminType | 'ADMIN1'
  typeLabel: string
  parentLabel: string
  subtitle: string
}
```

Current resolve contract from `packages/contracts/src/resolve.ts`:

```ts
export interface CanonicalPlaceCandidate extends CanonicalPlaceSummary {
  candidateHint: string
}

export type CanonicalResolveResponse =
  | { status: 'resolved'; click: { lat: number; lng: number }; place: CanonicalPlaceSummary }
  | { status: 'ambiguous'; click: { lat: number; lng: number }; prompt: string; recommendedPlaceId: string | null; candidates: CanonicalPlaceCandidate[] }
  | { status: 'failed'; click: { lat: number; lng: number }; reason: CanonicalResolveFailedReason; message: string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 Phase 13 geometry contract，并把 resolve 分支切到显式 `geometryRef`</name>
  <files>packages/contracts/src/geometry.ts, packages/contracts/src/resolve.ts, packages/contracts/src/index.ts, packages/contracts/src/contracts.spec.ts</files>
  <read_first>
    - .planning/phases/13-行政区数据与几何交付/13-CONTEXT.md
    - .planning/phases/13-行政区数据与几何交付/13-RESEARCH.md
    - .planning/phases/13-行政区数据与几何交付/13-VALIDATION.md
    - packages/contracts/src/place.ts
    - packages/contracts/src/resolve.ts
    - packages/contracts/src/index.ts
    - packages/contracts/src/contracts.spec.ts
  </read_first>
  <behavior>
    - Test 1: contracts 导出 `GeometryLayer = 'CN' | 'OVERSEAS'` 与 `GeometrySourceDataset = 'DATAV_GEOATLAS_CN' | 'NATURAL_EARTH_ADMIN1'`。
    - Test 2: `GeometryRef` 至少包含 `boundaryId`、`layer`、`geometryDatasetVersion`、`assetKey`、`renderableId` 五个字段，并显式区分 Phase 12 的 `datasetVersion`。
    - Test 3: `CanonicalResolveResponse` 的 resolved/ambiguous 分支都改为返回带 `geometryRef` 的 place shape，failed 分支仍然不带 `place`。
  </behavior>
  <action>
    按 D-01、D-03、D-07、D-08、D-10 新增 `packages/contracts/src/geometry.ts`，并导出以下精确类型名与字段：
    - `export type GeometryLayer = 'CN' | 'OVERSEAS'`
    - `export type GeometrySourceDataset = 'DATAV_GEOATLAS_CN' | 'NATURAL_EARTH_ADMIN1'`
    - `export interface GeometryRef { boundaryId: string; layer: GeometryLayer; geometryDatasetVersion: string; assetKey: string; renderableId: string | null }`
    - `export interface GeometryManifestEntry extends GeometryRef { sourceDataset: GeometrySourceDataset; sourceVersion: string; sourceFeatureId: string }`

    修改 `packages/contracts/src/resolve.ts`：
    - 新增 `export interface ResolvedCanonicalPlace extends CanonicalPlaceSummary { geometryRef: GeometryRef }`
    - 把 `CanonicalPlaceCandidate` 改成 `extends ResolvedCanonicalPlace`
    - 把 `CanonicalResolveResponse` 的 resolved 分支改成 `place: ResolvedCanonicalPlace`
    - 保持 ambiguous 分支继续返回 `recommendedPlaceId` 与 `candidates`，但每个 candidate 必须也带 `geometryRef`
    - failed 分支继续只有 `reason` 和 `message`，不要新增 `place`、`geometry` 或内联 polygon 字段

    修改 `packages/contracts/src/index.ts`，显式追加 `export * from './geometry'`，不要把 Phase 13 类型藏在内部文件里。

    更新 `packages/contracts/src/contracts.spec.ts`：
    - 用 `expectTypeOf` 精确断言 `GeometryRef` 与 `GeometryManifestEntry` 字段
    - 用 `expectTypeOf<CanonicalResolveResponse>()` 断言 resolved/ambiguous place 都包含 `geometryRef`
    - 保留现有 Phase 11/12 contract 断言，不要删除 `failed` 分支无 `place` 的守卫
  </action>
  <acceptance_criteria>
    - `packages/contracts/src/geometry.ts` 包含精确字符串 `export interface GeometryRef`.
    - `packages/contracts/src/geometry.ts` 包含精确字符串 `geometryDatasetVersion: string`.
    - `packages/contracts/src/geometry.ts` 包含精确字符串 `assetKey: string`.
    - `packages/contracts/src/geometry.ts` 包含精确字符串 `renderableId: string | null`.
    - `packages/contracts/src/resolve.ts` 包含精确字符串 `export interface ResolvedCanonicalPlace`.
    - `packages/contracts/src/resolve.ts` 包含精确字符串 `place: ResolvedCanonicalPlace`.
    - `packages/contracts/src/index.ts` 包含精确字符串 `export * from './geometry'`.
    - `packages/contracts/src/contracts.spec.ts` 包含精确字符串 `GeometryManifestEntry`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/contracts test</automated>
  </verify>
  <done>Phase 13 后续实现拥有统一的 `geometryRef` 与 manifest 类型，resolved/ambiguous 响应不再需要各自发明几何字段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用 Phase 12 fixture 固定 `geometryRef` 的精确值基线</name>
  <files>packages/contracts/src/fixtures.ts, packages/contracts/src/contracts.spec.ts</files>
  <read_first>
    - .planning/phases/13-行政区数据与几何交付/13-CONTEXT.md
    - .planning/phases/13-行政区数据与几何交付/13-RESEARCH.md
    - packages/contracts/src/fixtures.ts
    - packages/contracts/src/contracts.spec.ts
    - packages/contracts/src/resolve.ts
  </read_first>
  <behavior>
    - Test 1: `PHASE12_RESOLVED_BEIJING.geometryRef` 精确指向 `layer: 'CN'`、`geometryDatasetVersion: '2026-03-31-geo-v1'`、`assetKey: 'cn/beijing.json'`。
    - Test 2: `PHASE12_RESOLVED_HONG_KONG.geometryRef.assetKey` 精确为 `cn/hong-kong.json`，`PHASE12_RESOLVED_CALIFORNIA.geometryRef.assetKey` 精确为 `overseas/us.json`。
    - Test 3: ambiguous candidate fixtures 也必须带 `geometryRef`，且 California 的 `renderableId` 不能再使用 `ne-admin1-us-ca` 这种过渡别名。
  </behavior>
  <action>
    修改 `packages/contracts/src/fixtures.ts`，给下列 Phase 12 fixture 补上精确的 `geometryRef` 值，落实 D-01、D-05、D-06、D-12：
    - `PHASE12_RESOLVED_BEIJING.geometryRef = { boundaryId: 'datav-cn-beijing', layer: 'CN', geometryDatasetVersion: '2026-03-31-geo-v1', assetKey: 'cn/beijing.json', renderableId: 'datav-cn-beijing' }`
    - `PHASE12_RESOLVED_HONG_KONG.geometryRef = { boundaryId: 'datav-cn-hong-kong', layer: 'CN', geometryDatasetVersion: '2026-03-31-geo-v1', assetKey: 'cn/hong-kong.json', renderableId: 'datav-cn-hong-kong' }`
    - `PHASE12_RESOLVED_CALIFORNIA.geometryRef = { boundaryId: 'ne-admin1-us-california', layer: 'OVERSEAS', geometryDatasetVersion: '2026-03-31-geo-v1', assetKey: 'overseas/us.json', renderableId: 'ne-admin1-us-california' }`
    - ambiguous 候选中的 `cn-admin-tianjin` 使用 `assetKey: 'cn/tianjin.json'`
    - ambiguous 候选中的 `cn-admin-langfang` 使用 `assetKey: 'cn/hebei.json'`

    同文件中，Aba fixture 必须追加：
    - `geometryRef = { boundaryId: 'datav-cn-aba', layer: 'CN', geometryDatasetVersion: '2026-03-31-geo-v1', assetKey: 'cn/sichuan.json', renderableId: 'datav-cn-aba' }`

    更新 `packages/contracts/src/contracts.spec.ts`，显式断言：
    - Beijing / Hong Kong / California fixture 的 `geometryRef.assetKey`
    - `PHASE12_AMBIGUOUS_RESOLVE.candidates.every(candidate => 'geometryRef' in candidate)` 为真
    - California fixture 的 `geometryRef.renderableId` 精确等于 `ne-admin1-us-california`
  </action>
  <acceptance_criteria>
    - `packages/contracts/src/fixtures.ts` 包含精确字符串 `assetKey: 'cn/beijing.json'`.
    - `packages/contracts/src/fixtures.ts` 包含精确字符串 `assetKey: 'cn/hong-kong.json'`.
    - `packages/contracts/src/fixtures.ts` 包含精确字符串 `assetKey: 'cn/tianjin.json'`.
    - `packages/contracts/src/fixtures.ts` 包含精确字符串 `assetKey: 'cn/hebei.json'`.
    - `packages/contracts/src/fixtures.ts` 包含精确字符串 `assetKey: 'cn/sichuan.json'`.
    - `packages/contracts/src/fixtures.ts` 包含精确字符串 `assetKey: 'overseas/us.json'`.
    - `packages/contracts/src/contracts.spec.ts` 包含精确字符串 `geometryRef.assetKey`.
    - `packages/contracts/src/contracts.spec.ts` 包含精确字符串 `ne-admin1-us-california`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/contracts test</automated>
  </verify>
  <done>Phase 13 的 geometry contract 已经通过现有 canonical fixture 固定了具体 layer、datasetVersion 与 shard 路径，不再停留在抽象类型层。</done>
</task>

</tasks>

<verification>
- `pnpm --filter @trip-map/contracts test`
- `pnpm --filter @trip-map/contracts typecheck`
</verification>

<success_criteria>
- `@trip-map/contracts` 对外暴露了 Phase 13 需要的 geometry 类型与 `geometryRef` 响应形状。
- Beijing、Hong Kong、Aba、Tianjin、Langfang、California 这些当前 authoritative 样例拥有确定的 `assetKey` 与 `geometryDatasetVersion`。
- `CanonicalResolveResponse` 继续保持 failed 分支瘦身，不引入内联 GeoJSON。
</success_criteria>

<output>
After completion, create `.planning/phases/13-行政区数据与几何交付/13-01-SUMMARY.md`
</output>

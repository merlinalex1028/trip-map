---
phase: 13-行政区数据与几何交付
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - apps/web/package.json
  - apps/web/scripts/geo/verify-source-catalog.mjs
  - apps/web/scripts/geo/normalize-datav-cn.mjs
  - apps/web/scripts/geo/normalize-natural-earth.mjs
  - apps/web/scripts/geo/build-geometry-manifest.mjs
  - apps/web/src/data/geo/sources/datav-cn-2026-03-31.geo.json
  - apps/web/src/data/geo/sources/natural-earth-admin1-5.1.1.geo.json
  - apps/web/src/data/geo/geometry-source-catalog.json
autonomous: true
requirements:
  - GEOX-03
  - GEOX-04
  - GEOX-07
  - GEOX-06
must_haves:
  truths:
    - "中国与海外正式 GeoJSON 来源都能通过 source catalog 独立追踪到 snapshot、版本与 checksum。"
    - "构建脚本可以从 source catalog 复现 Phase 13 的几何流水线入口，而不是依赖手工拷贝或外部 GIS CLI。"
    - "中国来源会在构建期统一转换到对外消费坐标标准，海外来源会按规则过滤中国区域。"
  artifacts:
    - "apps/web/src/data/geo/geometry-source-catalog.json 记录 `DATAV_GEOATLAS_CN` 与 `NATURAL_EARTH_ADMIN1` 的 source metadata。"
    - "apps/web/src/data/geo/sources/datav-cn-2026-03-31.geo.json 与 apps/web/src/data/geo/sources/natural-earth-admin1-5.1.1.geo.json 固定 vendored snapshot。"
    - "apps/web/scripts/geo/normalize-datav-cn.mjs、apps/web/scripts/geo/normalize-natural-earth.mjs、apps/web/scripts/geo/build-geometry-manifest.mjs 提供可执行的 build pipeline 入口。"
  key_links:
    - "source catalog 的 `sourcePath` 与 build script 读取路径必须一一对应：`datav-cn-2026-03-31.geo.json` 和 `natural-earth-admin1-5.1.1.geo.json`。"
    - "`normalize-datav-cn.mjs` 必须显式使用 `gcoord.GCJ02 -> gcoord.WGS84`，不能把转换规则藏在匿名 helper 或注释里。"
    - "`normalize-natural-earth.mjs` 必须在构建期排除 `adm0_a3 === 'CHN'`，避免后续 shard 生成时再混入中国 feature。"
---

<objective>
把 Phase 13 的几何资产流水线拆出独立基础层：先固定 source catalog、vendored snapshot 与坐标归一化/build pipeline，再把生成资产与运行时服务留给后续计划承接。

Purpose: 先把“数据从哪来、怎样归一化、如何生成”说清楚，避免后续 plan 同时处理源数据治理和运行时资产交付。
Output: vendored source snapshot、source catalog、归一化脚本，以及支持临时输出校验的 build pipeline 入口。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-行政区数据与几何交付/13-CONTEXT.md
@.planning/phases/13-行政区数据与几何交付/13-RESEARCH.md
@.planning/phases/13-行政区数据与几何交付/13-VALIDATION.md
@.planning/phases/12-canonical/12-05-SUMMARY.md
@apps/web/package.json
@packages/contracts/src/geometry.ts
@packages/contracts/src/fixtures.ts
@apps/web/src/types/geo.ts

<interfaces>
Phase 13 shared types from `packages/contracts/src/geometry.ts` after Plan 01:

```ts
export interface GeometryRef {
  boundaryId: string
  layer: 'CN' | 'OVERSEAS'
  geometryDatasetVersion: string
  assetKey: string
  renderableId: string | null
}

export interface GeometryManifestEntry extends GeometryRef {
  sourceDataset: 'DATAV_GEOATLAS_CN' | 'NATURAL_EARTH_ADMIN1'
  sourceVersion: string
  sourceFeatureId: string
}
```

Current web boundary geometry shape from `apps/web/src/types/geo.ts`:

```ts
export interface CityBoundaryFeatureProperties {
  boundaryId: string
  cityId: string
  cityName: string
  datasetVersion: string
}

export interface CityBoundaryFeatureCollection {
  type: 'FeatureCollection'
  features: CityBoundaryFeature[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固定中国/海外 source snapshot 与可追踪的 source catalog</name>
  <files>apps/web/package.json, apps/web/scripts/geo/verify-source-catalog.mjs, apps/web/src/data/geo/sources/datav-cn-2026-03-31.geo.json, apps/web/src/data/geo/sources/natural-earth-admin1-5.1.1.geo.json, apps/web/src/data/geo/geometry-source-catalog.json</files>
  <read_first>
    - .planning/phases/13-行政区数据与几何交付/13-CONTEXT.md
    - .planning/phases/13-行政区数据与几何交付/13-RESEARCH.md
    - apps/web/package.json
    - packages/contracts/src/fixtures.ts
  </read_first>
  <behavior>
    - Test 1: `geometry-source-catalog.json` 同时记录 `DATAV_GEOATLAS_CN` 与 `NATURAL_EARTH_ADMIN1`，并包含 `sourceVersion`、`snapshotDate`、`checksum`、`sourcePath`。
    - Test 2: 中国 source snapshot 与海外 source snapshot 都以 vendored 文件形式进入仓库，后续脚本直接读取这些文件而不是即时下载官网资源。
    - Test 3: source metadata 能明确标识中国来源为 `GCJ02`、海外来源为 `WGS84`，为后续 build pipeline 提供固定输入。
    - Test 4: `geo:verify-sources` 在 Task 1 完成后就可以独立执行，不依赖 Task 2 的 normalize/build 脚本。
    - Test 5: `geo:verify-sources` 会对两个 vendored snapshot 重新计算 SHA-256，并与 catalog 中记录的 checksum 精确比对，而不是只检查 checksum 非空。
  </behavior>
  <action>
    按 D-04、D-05、D-06、D-10、D-12 在 `apps/web/src/data/geo/sources/` 提交两个 vendored source snapshot，并同步建立 Task 1 自己可运行的 source 校验入口；本任务只固定 build pipeline 的输入面，不生成或提交任何正式 public/contracts 几何产物，正式 manifest/shard 输出留给 13-03：
    - `datav-cn-2026-03-31.geo.json`：保留原始 `GCJ-02` 中国来源快照，供 `normalize-datav-cn.mjs` 读取
    - `natural-earth-admin1-5.1.1.geo.json`：保留原始 `Natural Earth admin-1` 来源快照，供 `normalize-natural-earth.mjs` 过滤

    修改 `apps/web/package.json`，新增精确命令 `geo:verify-sources: "node ./scripts/geo/verify-source-catalog.mjs"`。
    创建 `apps/web/scripts/geo/verify-source-catalog.mjs`，只做以下校验：
    - 读取 `apps/web/src/data/geo/geometry-source-catalog.json`
    - 校验 `DATAV_GEOATLAS_CN` 与 `NATURAL_EARTH_ADMIN1` 两条 entry 都存在
    - 校验 `checksum`、`coordinateSystem`、`sourcePath` 非空
    - 校验 `sourcePath` 指向的 vendored snapshot 文件真实存在
    - 使用 Node `crypto` 对 `datav-cn-2026-03-31.geo.json` 与 `natural-earth-admin1-5.1.1.geo.json` 重新计算 SHA-256，并与 catalog 中对应条目的 `checksum` 字段逐字精确比对
    该脚本不得依赖 `normalize-datav-cn.mjs`、`normalize-natural-earth.mjs` 或 `build-geometry-manifest.mjs`，确保 Task 1 提交后即可独立运行自动化验证。

    创建 `apps/web/src/data/geo/geometry-source-catalog.json`，至少写入下列两条 source entry：
    - `DATAV_GEOATLAS_CN`：`sourceVersion: "datav-geoatlas-2026-03-31-snapshot"`、`snapshotDate: "2026-03-31"`、`coordinateSystem: "GCJ02"`、`sourcePath: "apps/web/src/data/geo/sources/datav-cn-2026-03-31.geo.json"`、`checksum` 以 `sha256:` 开头
    - `NATURAL_EARTH_ADMIN1`：`sourceVersion: "natural-earth-5.1.1"`、`snapshotDate: "2026-03-31"`、`coordinateSystem: "WGS84"`、`sourcePath: "apps/web/src/data/geo/sources/natural-earth-admin1-5.1.1.geo.json"`、`checksum` 以 `sha256:` 开头
  </action>
  <acceptance_criteria>
    - `apps/web/package.json` 包含精确字符串 `"geo:verify-sources": "node ./scripts/geo/verify-source-catalog.mjs"`.
    - `apps/web/scripts/geo/verify-source-catalog.mjs` 包含精确字符串 `geometry-source-catalog.json`.
    - `apps/web/scripts/geo/verify-source-catalog.mjs` 包含精确字符串 `DATAV_GEOATLAS_CN`.
    - `apps/web/scripts/geo/verify-source-catalog.mjs` 包含精确字符串 `NATURAL_EARTH_ADMIN1`.
    - `apps/web/scripts/geo/verify-source-catalog.mjs` 包含精确字符串 `sha256`.
    - `apps/web/scripts/geo/verify-source-catalog.mjs` 包含精确字符串 `createHash`.
    - `apps/web/src/data/geo/geometry-source-catalog.json` 包含精确字符串 `"DATAV_GEOATLAS_CN"`.
    - `apps/web/src/data/geo/geometry-source-catalog.json` 包含精确字符串 `"NATURAL_EARTH_ADMIN1"`.
    - `apps/web/src/data/geo/geometry-source-catalog.json` 包含精确字符串 `"coordinateSystem": "GCJ02"`.
    - `apps/web/src/data/geo/geometry-source-catalog.json` 包含精确字符串 `"coordinateSystem": "WGS84"`.
    - `apps/web/src/data/geo/geometry-source-catalog.json` 包含精确字符串 `"checksum": "sha256:`.
    - `apps/web/src/data/geo/sources/datav-cn-2026-03-31.geo.json` 存在并可被脚本读取。
    - `apps/web/src/data/geo/sources/natural-earth-admin1-5.1.1.geo.json` 存在并可被脚本读取。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web run geo:verify-sources</automated>
  </verify>
  <done>Phase 13 已拥有可复现的 source catalog 输入面，后续生成资产时不需要再临时查找或下载源数据。</done>
</task>

<task type="auto">
  <name>Task 2: 搭建归一化与 manifest 构建脚本入口</name>
  <files>apps/web/package.json, apps/web/scripts/geo/normalize-datav-cn.mjs, apps/web/scripts/geo/normalize-natural-earth.mjs, apps/web/scripts/geo/build-geometry-manifest.mjs</files>
  <read_first>
    - .planning/phases/13-行政区数据与几何交付/13-RESEARCH.md
    - .planning/phases/13-行政区数据与几何交付/13-CONTEXT.md
    - apps/web/package.json
    - packages/contracts/src/fixtures.ts
  </read_first>
  <behavior>
    - Test 1: `geo:build` / `geo:build:check` 命令可以在当前仓库直接执行，不依赖 `ogr2ogr`、`mapshaper`、外部 GIS CLI，也不依赖 Task 1 之外的新校验脚本逻辑。
    - Test 2: 中国脚本必须使用 `gcoord.transform(..., gcoord.GCJ02, gcoord.WGS84)`，海外脚本必须过滤 `adm0_a3 === 'CHN'`。
    - Test 3: `build-geometry-manifest.mjs` 串联 catalog 读取、normalize 和后续 manifest/shard 生成入口，并支持 `geo:build:check` 以临时输出目录或 dry-run 模式验证流程；本计划不要求提交生成产物。
  </behavior>
  <action>
    按 D-04、D-05、D-06、D-10、D-12 修改 `apps/web/package.json`，并把“输入面校验”“临时流水线校验”“正式生成”拆开，避免 13-02 提前拥有 13-03 的正式产物职责：
    - 在 `devDependencies` 中新增精确版本 `gcoord: "^1.0.7"`
    - 在 `scripts` 中新增精确命令 `geo:build: "node ./scripts/geo/build-geometry-manifest.mjs"`
    - 在 `scripts` 中新增精确命令 `geo:build:check: "node ./scripts/geo/build-geometry-manifest.mjs --dry-run --output-root .tmp/geo-build-check"`

    创建三个 Node ESM 脚本：
    - `apps/web/scripts/geo/normalize-datav-cn.mjs`：读取中国 source snapshot，并用精确语句 `gcoord.transform(featureCollection, gcoord.GCJ02, gcoord.WGS84)` 转成 WGS84
    - `apps/web/scripts/geo/normalize-natural-earth.mjs`：读取海外 source snapshot，剔除 `properties.adm0_a3 === 'CHN'` 的 feature
    - `apps/web/scripts/geo/build-geometry-manifest.mjs`：串联 catalog 读取、normalize、manifest 生成和 shard 输出；不要调用 `ogr2ogr`、`mapshaper` 或 shell 外部 GIS 工具
    - `build-geometry-manifest.mjs` 必须支持 `--dry-run` / `--output-root`，使 `geo:build:check` 只把临时产物写入 `.tmp/geo-build-check` 或等价临时目录，不得改写 `apps/web/public/geo/2026-03-31-geo-v1` 或 `packages/contracts/src/generated`

    本计划只需把脚本能力对齐当前 authoritative fixture 对应的正式交付面：
    - `datav-cn-beijing`
    - `datav-cn-hong-kong`
    - `datav-cn-aba`
    - `datav-cn-tianjin`
    - `datav-cn-langfang`
    - `ne-admin1-us-california`

    不要在 Phase 13 额外扩成“全中国/全球全量 canonical 覆盖改造”；保持 source catalog 和脚本能承载后续扩容，但当前输出范围对齐现有 authoritative fixture 边界。
    另外，按 D-09 这里只负责把 manifest 生成逻辑固化成脚本能力，不再延续散落在 `apps/web/src/services/city-boundaries.ts` 一类文件里的手工 boundary 映射；正式 manifest 资产本身在 13-03 落盘并接管真源职责。
  </action>
  <acceptance_criteria>
    - `apps/web/package.json` 包含精确字符串 `"geo:build": "node ./scripts/geo/build-geometry-manifest.mjs"`.
    - `apps/web/package.json` 包含精确字符串 `"geo:build:check": "node ./scripts/geo/build-geometry-manifest.mjs --dry-run --output-root .tmp/geo-build-check"`.
    - `apps/web/package.json` 包含精确字符串 `"gcoord": "^1.0.7"`.
    - `apps/web/scripts/geo/normalize-datav-cn.mjs` 包含精确字符串 `gcoord.transform`.
    - `apps/web/scripts/geo/normalize-natural-earth.mjs` 包含精确字符串 `adm0_a3`.
    - `apps/web/scripts/geo/build-geometry-manifest.mjs` 包含精确字符串 `geometry-source-catalog.json`.
    - `apps/web/scripts/geo/build-geometry-manifest.mjs` 包含精确字符串 `--dry-run`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web run geo:build:check</automated>
  </verify>
  <done>Phase 13 已拥有稳定的归一化/build pipeline 入口，下一计划可以专注生成并验证正式交付资产。</done>
</task>

</tasks>

<verification>
- `pnpm --filter @trip-map/web run geo:build:check`
</verification>

<success_criteria>
- 中国数据来源固定为 `DataV.GeoAtlas` snapshot，海外数据来源固定为 `Natural Earth admin-1 5.1.1` snapshot，并且两者都有 checksum 记录。
- `geo:build` 已成为后续正式产物生成入口，`geo:build:check` 已成为本计划内的临时校验入口，二者都不依赖外部 GIS CLI。
- 中国与海外来源的坐标/过滤规则在脚本层被固定，后续生成资产时不需要再重新设计。
</success_criteria>

<output>
After completion, create `.planning/phases/13-行政区数据与几何交付/13-02-SUMMARY.md`
</output>

---
phase: 01-地图基础与应用骨架
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/stores/map-ui.ts
  - src/components/PointPreviewDrawer.vue
  - src/components/SeedMarkerLayer.vue
  - src/components/WorldMapStage.vue
  - src/App.vue
  - src/data/seed-points.ts
autonomous: true
requirements:
  - MAP-01
  - DRW-01
  - DRW-02
  - DAT-01
user_setup: []
must_haves:
  truths:
    - The responsive preview drawer stays closed on initial load.
    - Clicking a visible seed point selects it and opens the preview drawer with that point's data.
    - Desktop and mobile both use the same selection state while presenting the drawer in different positions.
  artifacts:
    - src/stores/map-ui.ts
    - src/components/PointPreviewDrawer.vue
    - updated SeedMarkerLayer and WorldMapStage selection plumbing
  key_links:
    - SeedMarkerLayer emits marker selection into the map UI store
    - PointPreviewDrawer reads the selected point from shared UI state
    - App.vue renders the drawer alongside the map stage without opening it by default
---

<objective>
Wire up point selection and the closed-by-default preview drawer so the Phase 1 homepage becomes interactive and satisfies the responsive drawer requirements.

Purpose: Complete the visible user journey for Phase 1 by letting a user click a seed marker and see its preview card without introducing later-phase editing or persistence complexity.
Output: Shared selection state, responsive preview drawer component, and map-to-drawer interaction wiring.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-地图基础与应用骨架/01-CONTEXT.md
@.planning/phases/01-地图基础与应用骨架/01-RESEARCH.md
@.planning/phases/01-地图基础与应用骨架/01-UI-SPEC.md
@src/components/WorldMapStage.vue
@src/components/SeedMarkerLayer.vue
@src/data/seed-points.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared point-selection UI state for the map preview flow</name>
  <files>src/stores/map-ui.ts, src/components/WorldMapStage.vue, src/components/SeedMarkerLayer.vue</files>
  <read_first>src/components/WorldMapStage.vue, src/components/SeedMarkerLayer.vue, src/data/seed-points.ts, .planning/phases/01-地图基础与应用骨架/01-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Shared UI state exposes selected point id or preview payload and a clear action to reset it.
    - Test 2: Marker selection flows through the shared state instead of isolated local component state.
  </behavior>
  <action>Create `src/stores/map-ui.ts` using Pinia with exactly these public pieces: selected point id or selected point payload, `selectPoint`, and `clearSelection`. Update `WorldMapStage.vue` and `SeedMarkerLayer.vue` so marker clicks call `selectPoint` and selected marker styling can reflect shared state. Do not add persistence, editable form state, or free-placement logic. Ensure the map background does not create new points and that the drawer remains closed whenever there is no selected point.</action>
  <verify>test -f src/stores/map-ui.ts && rg 'selectPoint|clearSelection' src/stores/map-ui.ts && rg 'selectPoint' src/components/SeedMarkerLayer.vue && rg 'selected' src/components/WorldMapStage.vue</verify>
  <acceptance_criteria>
    - src/stores/map-ui.ts contains `selectPoint`
    - src/stores/map-ui.ts contains `clearSelection`
    - src/components/SeedMarkerLayer.vue calls `selectPoint`
    - src/components/WorldMapStage.vue references selected point state
  </acceptance_criteria>
  <done>The map preview flow is driven by one shared state source that later phases can extend without rewriting the interaction model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build and wire the responsive point preview drawer</name>
  <files>src/components/PointPreviewDrawer.vue, src/components/WorldMapStage.vue, src/components/SeedMarkerLayer.vue, src/App.vue, src/data/seed-points.ts</files>
  <read_first>src/stores/map-ui.ts, src/components/WorldMapStage.vue, src/components/SeedMarkerLayer.vue, src/App.vue, .planning/phases/01-地图基础与应用骨架/01-CONTEXT.md, .planning/phases/01-地图基础与应用骨架/01-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: Drawer is hidden by default and only appears when a marker is selected.
    - Test 2: Drawer content shows place name, country/region, coordinates label, and a short preview description.
  </behavior>
  <action>Create `src/components/PointPreviewDrawer.vue` as a preview-first responsive surface that renders on the right on desktop and as a bottom sheet on mobile using CSS breakpoints, not separate routes. It must read selected point data from `map-ui.ts`, stay unmounted or visually hidden when nothing is selected, and render content in the exact priority from UI-SPEC: place name, country/region line, coordinate line, short description. Update `App.vue` to mount the drawer alongside the stage, and update the map components so clicking a seed point selects it, opens the drawer, and visually promotes the selected marker with the approved accent ring. Keep the close affordance visually quiet and wire it to `clearSelection`. Do not add edit actions, delete actions, stats, or generic sidebar scaffolding.</action>
  <verify>test -f src/components/PointPreviewDrawer.vue && rg 'clearSelection' src/components/PointPreviewDrawer.vue && rg 'country|region|coordinate|description' src/components/PointPreviewDrawer.vue && rg 'PointPreviewDrawer' src/App.vue && rg 'accent|selected' src/components/SeedMarkerLayer.vue</verify>
  <acceptance_criteria>
    - src/components/PointPreviewDrawer.vue contains `clearSelection`
    - src/components/PointPreviewDrawer.vue contains display regions for name, country/region, coordinate, and description
    - src/App.vue renders `PointPreviewDrawer`
    - src/components/SeedMarkerLayer.vue contains a selected-state branch
  </acceptance_criteria>
  <done>Phase 1 interaction is complete: selecting a seed point opens the correct responsive preview drawer while preserving the map as the dominant visual surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test
- [ ] rg 'PointPreviewDrawer' src/App.vue
- [ ] rg 'clearSelection' src/components/PointPreviewDrawer.vue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The drawer stays closed until a point is clicked
- Desktop and mobile both satisfy the drawer requirement using the same selected-point state

</success_criteria>

<output>
After completion, create `.planning/phases/01-地图基础与应用骨架/01-03-SUMMARY.md`
</output>

---
phase: 01-地图基础与应用骨架
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/types/map-point.ts
  - src/data/seed-points.ts
  - src/data/preview-points.ts
  - src/components/PosterTitleBlock.vue
  - src/components/WorldMapStage.vue
  - src/components/SeedMarkerLayer.vue
  - src/assets/world-map.svg
  - src/App.vue
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - DAT-01
user_setup: []
must_haves:
  truths:
    - The homepage shows a map-first vintage poster composition on desktop and mobile.
    - Built-in seed points and any previously saved preview points are loaded and rendered on the map.
    - Only a small subset of seed points display persistent labels so the map stays low-density.
  artifacts:
    - src/assets/world-map.svg
    - src/data/seed-points.ts
    - src/data/preview-points.ts
    - src/components/WorldMapStage.vue
    - src/components/SeedMarkerLayer.vue
    - src/components/PosterTitleBlock.vue
  key_links:
    - App.vue renders PosterTitleBlock and WorldMapStage inside the poster shell from Plan 01
    - WorldMapStage imports the map asset and a merged preview-point data source
    - SeedMarkerLayer reads the merged preview-point data and applies the approved low-density marker treatment
---

<objective>
Add the actual map stage, poster title block, and built-in seed point display so the homepage becomes a real Phase 1 screen instead of a shell placeholder.

Purpose: Deliver the visible core of the map-first homepage while keeping the stage clean enough for Phase 2 geographic logic and Phase 3 point interactions.
Output: A map stage with fixed world map artwork, a compact title block, typed seed point data, and low-density marker rendering.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-地图基础与应用骨架/01-CONTEXT.md
@.planning/phases/01-地图基础与应用骨架/01-RESEARCH.md
@.planning/phases/01-地图基础与应用骨架/01-UI-SPEC.md
@src/App.vue
@src/styles/tokens.css
@src/styles/global.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the Phase 1 display model and built-in seed point data</name>
  <files>src/types/map-point.ts, src/data/seed-points.ts, src/data/preview-points.ts</files>
  <read_first>.planning/phases/01-地图基础与应用骨架/01-CONTEXT.md, .planning/phases/01-地图基础与应用骨架/01-RESEARCH.md, .planning/phases/01-地图基础与应用骨架/01-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: A shared MapPoint-like display model exists for both seed markers and saved preview markers in Phase 1.
    - Test 2: Seed data contains only a small curated set of demo markers with optional persistent labels.
    - Test 3: A read-only data path safely loads any saved preview markers from localStorage without introducing write behavior.
  </behavior>
  <action>Create `src/types/map-point.ts` with a minimal display-safe type that supports `id`, `name`, `countryName`, `x`, `y`, `source`, `isFeatured`, `description`, and `coordinatesLabel` for Phase 1 rendering. Create `src/data/seed-points.ts` exporting a small hard-coded array of seed markers using normalized `x/y` positions and matching the UI-SPEC rule of only a few persistent labels. Add `src/data/preview-points.ts` that exports one read-only loader function which merges the built-in seed points with any saved preview points found under a single localStorage key, validates shape defensively, and falls back to seed points when storage is empty or malformed. This plan must support the “已保存点位” display half of MAP-02, but it must not introduce creation, editing, deletion, or write-back behavior; those remain in later phases.</action>
  <verify>test -f src/types/map-point.ts && test -f src/data/seed-points.ts && test -f src/data/preview-points.ts && rg 'export (type|interface) .*MapPoint|Display' src/types/map-point.ts && rg 'isFeatured|source' src/data/seed-points.ts && rg 'localStorage|seedPoints|saved' src/data/preview-points.ts</verify>
  <acceptance_criteria>
    - src/types/map-point.ts exports a point display type
    - src/data/seed-points.ts exports a seed point collection
    - src/data/preview-points.ts contains `localStorage`
    - src/data/preview-points.ts contains `seedPoints`
    - src/data/seed-points.ts contains `isFeatured`
    - src/data/seed-points.ts contains `source`
  </acceptance_criteria>
  <done>Preview point data is typed and can render both built-in seed markers and any previously saved markers without introducing write-side persistence behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the poster title block and world map stage with seed markers</name>
  <files>src/components/PosterTitleBlock.vue, src/components/WorldMapStage.vue, src/components/SeedMarkerLayer.vue, src/assets/world-map.svg, src/App.vue, src/data/preview-points.ts</files>
  <read_first>src/App.vue, src/styles/tokens.css, src/styles/global.css, src/types/map-point.ts, src/data/seed-points.ts, src/data/preview-points.ts, .planning/phases/01-地图基础与应用骨架/01-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: App.vue renders a compact title block with one title and one guiding line.
    - Test 2: WorldMapStage renders the map asset and marker layer with visible seed points and any previously saved preview markers.
  </behavior>
  <action>Create `src/components/PosterTitleBlock.vue` with exactly two visible copy surfaces: the main title `旅行世界地图` and the guiding line from UI-SPEC. Create `src/components/WorldMapStage.vue` that renders a fixed world map asset inside a framed stage and passes merged preview-point data from `src/data/preview-points.ts` to `src/components/SeedMarkerLayer.vue`. Create `src/components/SeedMarkerLayer.vue` to position preview markers using inline styles from normalized `x/y`, render subtle glow markers, and only show persistent labels when `isFeatured` is true. Add `src/assets/world-map.svg` as the fixed poster-stage map asset path used by the stage component. Update `src/App.vue` to replace placeholders with `PosterTitleBlock` and `WorldMapStage`. Keep the map as the dominant surface, preserve negative space, and do not render stats, hero cards, toolbars, or drawer content yet.</action>
  <verify>test -f src/components/PosterTitleBlock.vue && test -f src/components/WorldMapStage.vue && test -f src/components/SeedMarkerLayer.vue && test -f src/assets/world-map.svg && rg '旅行世界地图' src/components/PosterTitleBlock.vue && rg 'previewPoints|SeedMarkerLayer' src/components/WorldMapStage.vue && rg 'isFeatured|source' src/components/SeedMarkerLayer.vue</verify>
  <acceptance_criteria>
    - src/components/PosterTitleBlock.vue contains `旅行世界地图`
    - src/components/PosterTitleBlock.vue contains the guiding line copy
    - src/components/WorldMapStage.vue imports `SeedMarkerLayer`
    - src/components/WorldMapStage.vue references the merged preview-point data source
    - src/components/WorldMapStage.vue references `world-map.svg`
    - src/components/SeedMarkerLayer.vue contains `isFeatured`
  </acceptance_criteria>
  <done>The homepage visually reads as a completed poster-style map stage with restrained preview markers, including built-in seed points and any previously saved points.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test
- [ ] rg '旅行世界地图' src/components/PosterTitleBlock.vue
- [ ] rg 'SeedMarkerLayer' src/components/WorldMapStage.vue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The app now displays a real map-first homepage with a fixed map asset and curated seed markers
- Seed marker density and labeling respect the approved low-noise poster contract

</success_criteria>

<output>
After completion, create `.planning/phases/01-地图基础与应用骨架/01-02-SUMMARY.md`
</output>

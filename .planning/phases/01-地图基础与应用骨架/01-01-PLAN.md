---
phase: 01-地图基础与应用骨架
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - vitest.config.ts
  - index.html
  - src/main.ts
  - src/App.vue
  - src/App.spec.ts
  - src/styles/tokens.css
  - src/styles/global.css
autonomous: true
requirements:
  - MAP-01
  - DRW-01
  - DRW-02
user_setup: []
must_haves:
  truths:
    - The project boots into a Vue application shell that can host the map-first homepage on desktop and mobile.
    - Global styling tokens exist for the approved vintage poster direction before feature components are added.
    - A runnable test harness exists so every later plan can truthfully use `npm test` as part of verification.
  artifacts:
    - package.json with Vue 3 + Vite + TypeScript scripts and dependencies
    - vitest.config.ts and a smoke test file
    - src/App.vue with poster-shell structure placeholders
    - src/styles/tokens.css and src/styles/global.css with phase-one visual tokens
  key_links:
    - src/main.ts imports App.vue and both global style files
    - App.vue renders a top-level shell container that later plans can populate with map stage and drawer
    - package.json test script executes the Vitest configuration and smoke test file
---

<objective>
Create the executable Vue 3 + Vite + TypeScript application foundation and establish the global visual shell for the map-first homepage.

Purpose: Give Phase 1 a stable app entry, CSS token layer, and page framing that every later plan can build on without revisiting the stack setup.
Output: A bootable frontend scaffold with a poster-style app shell and global design tokens aligned to 01-CONTEXT.md and 01-UI-SPEC.md.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-地图基础与应用骨架/01-CONTEXT.md
@.planning/phases/01-地图基础与应用骨架/01-RESEARCH.md
@.planning/phases/01-地图基础与应用骨架/01-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize the Vue + Vite + TypeScript workspace for the poster app shell</name>
  <files>package.json, tsconfig.json, vite.config.ts, vitest.config.ts, index.html, src/main.ts, src/App.spec.ts</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/01-地图基础与应用骨架/01-CONTEXT.md, .planning/phases/01-地图基础与应用骨架/01-RESEARCH.md, .planning/phases/01-地图基础与应用骨架/01-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: package.json defines dev, build, and test scripts for a Vite + Vue + TypeScript app.
    - Test 2: src/main.ts mounts the Vue app and imports the global style entry points expected by the visual shell.
    - Test 3: npm test has a valid Vitest config and at least one smoke test to execute.
  </behavior>
  <action>Create the initial application workspace files for a Vite-powered Vue 3 + TypeScript app. In package.json, define scripts `dev`, `build`, and `test`, include runtime dependencies `vue`, `pinia`, `nanoid`, and dev dependencies for `vite`, `@vitejs/plugin-vue`, `typescript`, `vitest`, `@vue/test-utils`, and `happy-dom`. Configure vite.config.ts with the Vue plugin. Add `vitest.config.ts` configured for the Vue plugin, `happy-dom`, and a standard `src/**/*.spec.ts` include pattern. Create `src/App.spec.ts` as a smoke test that mounts `App.vue` and asserts the root shell exists. In src/main.ts, import `./styles/tokens.css`, `./styles/global.css`, and mount `App.vue` to `#app`. Keep the setup minimal and greenfield-friendly; do not add router, backend SDKs, Tailwind, shadcn, analytics, or any Phase 2 geo-recognition dependencies beyond what the research explicitly allows.</action>
  <verify>test -f package.json && test -f vitest.config.ts && test -f src/App.spec.ts && rg '"dev"|\"build\"|\"test\"' package.json && rg 'createApp' src/main.ts && rg 'tokens.css|global.css' src/main.ts && rg 'happy-dom|test:' vitest.config.ts</verify>
  <acceptance_criteria>
    - package.json contains `"dev"`, `"build"`, and `"test"` scripts
    - package.json contains `"vue"` and `"pinia"` dependencies
    - vitest.config.ts contains `happy-dom`
    - src/App.spec.ts contains `mount(App`
    - src/main.ts contains `createApp`
    - src/main.ts contains `./styles/tokens.css`
    - src/main.ts contains `./styles/global.css`
  </acceptance_criteria>
  <done>Workspace files exist and the project can be bootstrapped as a Vue 3 + Vite + TypeScript app with a global styling entry path and a runnable smoke-test harness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the top-level poster shell component and global CSS tokens</name>
  <files>src/App.vue, src/styles/tokens.css, src/styles/global.css, index.html</files>
  <read_first>src/main.ts, .planning/phases/01-地图基础与应用骨架/01-CONTEXT.md, .planning/phases/01-地图基础与应用骨架/01-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: App.vue renders a map-first shell with title area, stage container, and reserved drawer host region.
    - Test 2: CSS token files declare the approved spacing, typography, and color variables from UI-SPEC.
  </behavior>
  <action>Create `src/App.vue` as the root poster shell with a top-level container, a compact title block region, a main stage region, and a drawer host slot area; use placeholder semantic wrappers only, not finished feature components. In `src/styles/tokens.css`, declare CSS custom properties for the exact approved spacing tokens (`4, 8, 16, 24, 32, 48, 64`), typography roles (`16/14/24/40` with only `400` and `600` weights), and the approved color palette (`#F2E6C9`, `#C8B28A`, `#C8643B`, `#8D3E2F`). In `src/styles/global.css`, apply the vintage poster baseline: parchment page background, map-first layout framing, dark-brown ink text tone, and sparse chrome. Update index.html title metadata to match the project identity. Do not introduce stats, toolbar grids, multi-column dashboards, or auto-open drawer behavior.</action>
  <verify>test -f src/App.vue && test -f src/styles/tokens.css && test -f src/styles/global.css && rg 'map|stage|drawer|title' src/App.vue && rg '#F2E6C9|#C8B28A|#C8643B|#8D3E2F' src/styles/tokens.css && rg '40px|24px|16px|14px' src/styles/tokens.css</verify>
  <acceptance_criteria>
    - src/App.vue contains semantic wrappers for title, map stage, and drawer host
    - src/styles/tokens.css contains `--space-xs: 4px` and `--space-3xl: 64px`
    - src/styles/tokens.css contains `#F2E6C9`, `#C8B28A`, `#C8643B`, and `#8D3E2F`
    - src/styles/global.css contains a page-level background rule for the parchment surface
  </acceptance_criteria>
  <done>The app shell visually aligns with the approved poster contract and is ready for map-stage and drawer components to be wired in by later plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm test
- [ ] rg 'createApp' src/main.ts
- [ ] rg '#F2E6C9|#C8643B' src/styles/tokens.css
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository contains a bootable Vue application shell with the approved phase-one global tokens
- The root app structure clearly exposes map stage and drawer host regions for later plans

</success_criteria>

<output>
After completion, create `.planning/phases/01-地图基础与应用骨架/01-01-SUMMARY.md`
</output>

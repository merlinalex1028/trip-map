---
phase: 16-uat-gap-fallback-smoke-record-schema-typelabel-california
plan: 02
type: execute
wave: 3
depends_on:
  - "16-01"
files_modified:
  - apps/web/src/stores/map-points.ts
  - apps/web/src/stores/map-points.spec.ts
  - apps/web/src/components/map-popup/PointSummaryCard.vue
  - apps/web/src/components/map-popup/PointSummaryCard.spec.ts
  - apps/web/src/components/map-popup/MapContextPopup.vue
  - apps/web/src/components/LeafletMapStage.vue
  - apps/web/src/components/LeafletMapStage.spec.ts
autonomous: true
requirements:
  - REQ-16-01
  - REQ-16-02
  - REQ-16-04

must_haves:
  truths:
    - "保存后的 canonical 点位重新打开时，popup 仍显示正确的 typeLabel、parentLabel、subtitle"
    - "fallback 点位不再呈现一个可点击但无效果的点亮按钮"
    - "点亮成功后，同一 session 内会加载对应 shard 并出现 saved overlay"
  artifacts:
    - path: "apps/web/src/stores/map-points.ts"
      provides: "TravelRecord -> popup surface 的 canonical metadata rehydrate"
      contains: "recordToDisplayPoint"
    - path: "apps/web/src/components/map-popup/PointSummaryCard.vue"
      provides: "非 illuminatable surface 的明确按钮禁用态"
      contains: "isIlluminatable"
    - path: "apps/web/src/components/LeafletMapStage.vue"
      provides: "fallback illuminate notice 与 illuminate 后 shard load"
      contains: "loadShardIfNeeded"
    - path: "apps/web/src/components/LeafletMapStage.spec.ts"
      provides: "fallback / overlay 行为回归"
      contains: "loadGeometryShard"
  key_links:
    - from: "apps/web/src/stores/map-points.ts"
      to: "packages/contracts/src/records.ts"
      via: "TravelRecord canonical metadata fields"
      pattern: "typeLabel|parentLabel|regionSystem|adminType"
    - from: "apps/web/src/components/map-popup/PointSummaryCard.vue"
      to: "apps/web/src/components/map-popup/MapContextPopup.vue"
      via: "isIlluminatable prop 透传"
      pattern: "is-illuminatable"
    - from: "apps/web/src/components/LeafletMapStage.vue"
      to: "apps/web/src/services/geometry-loader.ts"
      via: "illuminate success -> loadShardIfNeeded"
      pattern: "loadShardIfNeeded"
---

<objective>
修掉 popup/fallback/overlay 这条前端交互链，让“不能点亮的点位”给出明确反馈，让“已经点亮的点位”在重新打开时仍保留 canonical 标签，并在保存成功后真正出现边界高亮。

Purpose: 把 Phase 15/12 UAT 中最直接可见的 UI 回归关闭掉，同时让 web 端消费 Wave 1 已补齐的 TravelRecord metadata，而不是继续依赖空字段或隐式推断。
Output: 更新后的 `map-points` store、popup 组件链、`LeafletMapStage` 行为与对应 web regression specs。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-RESEARCH.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VALIDATION.md
@.planning/phases/15-服务端记录与点亮闭环/15-UAT.md
@.planning/debug/illuminate-button-no-effect.md
@.planning/debug/geojson-boundary-not-showing.md
@.planning/debug/beijing-no-type-label.md
@.planning/debug/hk-no-type-label.md
@.planning/phases/15-服务端记录与点亮闭环/15-02-SUMMARY.md
@.planning/phases/15-服务端记录与点亮闭环/15-03-SUMMARY.md

<interfaces>
From [apps/web/src/stores/map-points.ts] (current):
```typescript
function recordToDisplayPoint(record: TravelRecord): MapPointDisplay {
  return {
    placeId: record.placeId,
    typeLabel: null,
    parentLabel: null,
    subtitle: record.subtitle,
  }
}

async function illuminate(summary: {
  placeId: string
  boundaryId: string | null
  placeKind: TravelRecord['placeKind']
  datasetVersion: string
  displayName: string
  subtitle: string | null
}) { ... }
```

From [apps/web/src/components/LeafletMapStage.vue] (current):
```typescript
function handleIlluminate() {
  const surface = summarySurfaceState.value
  if (!surface || surface.mode === 'candidate-select') return
  const point = surface.point
  if (!point.placeId || !point.placeKind || !point.datasetVersion) return
  void mapPointsStore.illuminate({ ... })
}
```

From [apps/web/src/components/map-popup/PointSummaryCard.vue] (current):
```typescript
const showIlluminateButton = computed(() => !isCandidateMode.value)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 让 TravelRecord 重新打开时恢复 canonical 标签，并把保存请求升级为完整 metadata</name>
  <files>
    apps/web/src/stores/map-points.ts,
    apps/web/src/stores/map-points.spec.ts
  </files>
  <read_first>
    apps/web/src/stores/map-points.ts,
    apps/web/src/stores/map-points.spec.ts,
    packages/contracts/src/records.ts,
    packages/contracts/src/place.ts,
    .planning/debug/beijing-no-type-label.md,
    .planning/debug/hk-no-type-label.md
  </read_first>
  <behavior>
    - `recordToDisplayPoint()` 读取 TravelRecord 里的 `typeLabel`, `parentLabel`, `subtitle`
    - `illuminate()` 创建请求携带 `regionSystem`, `adminType`, `typeLabel`, `parentLabel`, `subtitle`
    - 保存后的 reopen/view surface 不再出现北京/香港无标签
  </behavior>
  <action>
    1. 在 `apps/web/src/stores/map-points.ts` 的 `recordToDisplayPoint()` 中，用 `record.typeLabel`, `record.parentLabel`, `record.subtitle` 替换当前硬编码的 `null`；`countryName` 继续从 `record.parentLabel` 经 `split(' · ')[0]` 派生，避免 view mode 再显示空上级名。
    2. 把 `illuminate()` 的 `summary` 入参扩展为完整 canonical summary：`placeId`, `boundaryId`, `placeKind`, `datasetVersion`, `displayName`, `regionSystem`, `adminType`, `typeLabel`, `parentLabel`, `subtitle`。调用 `createTravelRecord()` 时完整透传这 10 个字段，不再只发 `displayName + subtitle`。
    3. 保持已有 optimistic update 结构，但 `optimisticRecord` 也必须带上 `regionSystem`, `adminType`, `typeLabel`, `parentLabel`, `subtitle`，保证 pending 态 popup 与最终 server row 一致。
    4. 更新 `apps/web/src/stores/map-points.spec.ts` 的 `makeRecord()` / `makeResolvedPlace()` helper，让其使用 contracts fixture 提供的 canonical metadata；新增断言：`openSavedPointForPlaceOrStartDraft()` 复用 saved record 后，`summarySurfaceState.point.typeLabel === '直辖市'`、`parentLabel === '中国'`、Hong Kong 用 `'特别行政区'`。
    5. 不为 `typeLabel` / `parentLabel` 增加“从 subtitle 解析”的兜底逻辑；如果字段缺失，应让测试直接失败。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web exec vitest run src/stores/map-points.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/stores/map-points.ts` 中 `recordToDisplayPoint()` 不再出现 `typeLabel: null` 或 `parentLabel: null`
    - `apps/web/src/stores/map-points.ts` 的 `illuminate()` 请求对象包含 `regionSystem`, `adminType`, `typeLabel`, `parentLabel`, `subtitle`
    - `apps/web/src/stores/map-points.spec.ts` helper 记录对象包含 `typeLabel`, `parentLabel`, `regionSystem`, `adminType`
    - `apps/web/src/stores/map-points.spec.ts` 包含 reopen/view surface 的 canonical label 断言
    - `pnpm --filter @trip-map/web exec vitest run src/stores/map-points.spec.ts` 通过
  </acceptance_criteria>
  <done>保存记录与 reopened popup 之间的 canonical 展示合同已恢复，web 端不再丢失 typeLabel / parentLabel。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 去掉 fallback 假可点击点亮按钮，并在 illuminate 成功后补 shard load</name>
  <files>
    apps/web/src/components/map-popup/PointSummaryCard.vue,
    apps/web/src/components/map-popup/PointSummaryCard.spec.ts,
    apps/web/src/components/map-popup/MapContextPopup.vue,
    apps/web/src/components/LeafletMapStage.vue,
    apps/web/src/components/LeafletMapStage.spec.ts
  </files>
  <read_first>
    apps/web/src/components/map-popup/PointSummaryCard.vue,
    apps/web/src/components/map-popup/PointSummaryCard.spec.ts,
    apps/web/src/components/map-popup/MapContextPopup.vue,
    apps/web/src/components/LeafletMapStage.vue,
    apps/web/src/components/LeafletMapStage.spec.ts,
    apps/web/src/services/geometry-loader.ts,
    apps/web/src/services/geometry-manifest.ts,
    .planning/debug/illuminate-button-no-effect.md,
    .planning/debug/geojson-boundary-not-showing.md
  </read_first>
  <behavior>
    - fallback 点位的点亮按钮是明确禁用态，并能给出“该地点暂不支持点亮”的提示
    - canonical 点位点亮成功后会调用 `loadShardIfNeeded()`，从而触发 `addFeatures()` 加载 shard
    - 已保存 overlay 的出现依赖真实 feature 加载，不依赖单纯 `refreshStyles()`
  </behavior>
  <action>
    1. 在 `PointSummaryCard.vue` 新增 `isIlluminatable?: boolean` prop，默认 `true`；按钮禁用条件改为 `:disabled="isPending || !isIlluminatable"`，并增加 `:data-illuminatable="String(isIlluminatable)"`。当 `!isIlluminatable` 时按钮文本仍保持“点亮”，但 aria/title 固定为 `该地点暂不支持点亮`。
    2. 在 `MapContextPopup.vue` 增加并透传 `isIlluminatable` prop 到 `PointSummaryCard`。
    3. 在 `LeafletMapStage.vue` 中新增 `isActivePointIlluminatable` 计算属性：仅当当前 surface 非 `candidate-select` 且同时具备 `placeId`, `placeKind`, `datasetVersion`, `boundaryId` 时为 `true`。把它传给 `<MapContextPopup :is-illuminatable="isActivePointIlluminatable" />`。
    4. 把 `handleIlluminate()` 里的静默 guard 改为：若 `!isActivePointIlluminatable.value`，调用 `setInteractionNotice({ tone: 'info', message: '该地点暂不支持点亮' })` 后返回。
    5. 在 `handleIlluminate()` 成功 `await mapPointsStore.illuminate(...)` 之后，如果 `point.boundaryId` 存在，则通过 `getGeometryManifestEntry(point.boundaryId)` 取 layer，并 `await loadShardIfNeeded(point.boundaryId, entry.layer as 'CN' | 'OVERSEAS')`；不要只依赖后续 `refreshStyles()`。
    6. 更新 `PointSummaryCard.spec.ts`：新增 `isIlluminatable=false` 时按钮禁用、`data-illuminatable="false"`、且不发出 `illuminate` emit 的断言。
    7. 更新 `LeafletMapStage.spec.ts`：为一次 canonical 点亮设置 `geometryManifestMock.getGeometryManifestEntry()` 与 `geometryLoaderMock.loadGeometryShard()`，断言点击点亮后调用了 shard load；同时覆盖 fallback surface 的 disabled affordance。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web exec vitest run src/components/map-popup/PointSummaryCard.spec.ts src/components/LeafletMapStage.spec.ts src/stores/map-points.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/map-popup/PointSummaryCard.vue` 包含 `isIlluminatable`
    - `apps/web/src/components/map-popup/PointSummaryCard.vue` 的按钮 disabled 条件包含 `!isIlluminatable`
    - `apps/web/src/components/LeafletMapStage.vue` 包含提示文案 `该地点暂不支持点亮`
    - `apps/web/src/components/LeafletMapStage.vue` 的 `handleIlluminate()` 成功分支调用 `loadShardIfNeeded`
    - `apps/web/src/components/LeafletMapStage.spec.ts` 包含 `loadGeometryShard`/`addFeatures` 相关断言
    - `pnpm --filter @trip-map/web exec vitest run src/components/map-popup/PointSummaryCard.spec.ts src/components/LeafletMapStage.spec.ts src/stores/map-points.spec.ts` 通过
  </acceptance_criteria>
  <done>fallback 点位已不再误导用户，成功点亮则会真实加载 GeoJSON shard 并触发 saved overlay。</done>
</task>

</tasks>

<verification>
- 跑 `pnpm --filter @trip-map/web exec vitest run src/stores/map-points.spec.ts src/components/map-popup/PointSummaryCard.spec.ts src/components/LeafletMapStage.spec.ts`。
- 额外跑 `pnpm --filter @trip-map/web exec vue-tsc --noEmit`，确保 contracts 扩展后 web 类型仍闭合。
</verification>

<success_criteria>
- 北京/香港等已保存 canonical 点位在 popup view mode 中恢复 type label/subtitle。
- fallback surface 不再出现“可点击但无效果”的点亮按钮。
- 点亮成功后 map session 内会补齐 shard load，saved overlay 能被渲染出来。
</success_criteria>

<output>
After completion, create `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-02-SUMMARY.md`
</output>

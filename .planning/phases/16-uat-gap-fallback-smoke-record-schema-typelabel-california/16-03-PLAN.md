---
phase: 16-uat-gap-fallback-smoke-record-schema-typelabel-california
plan: 03
type: execute
wave: 3
depends_on:
  - "16-01"
  - "16-02"
files_modified:
  - apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts
  - apps/server/src/modules/canonical-places/canonical-places.service.ts
  - apps/server/test/canonical-resolve.e2e-spec.ts
  - packages/contracts/src/fixtures.ts
  - .planning/phases/12-canonical/12-UAT.md
autonomous: true
requirements:
  - REQ-16-05

must_haves:
  truths:
    - "California 区域内的真实点击会得到 server-authoritative canonical resolved 结果，而不是退回 fallback"
    - "shared fixtures、server fixtures 与 UAT 文案对同一 California canonical 结果使用同一组 placeId / datasetVersion / typeLabel"
    - "canonical resolve 回归能证明 California 不再依赖 ±0.0001° 的单点命中"
  artifacts:
    - path: "apps/server/src/modules/canonical-places/canonical-places.service.ts"
      provides: "California bbox-based authoritative match"
      contains: "findFixture"
    - path: "apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts"
      provides: "server-authoritative canonical fixture catalog"
      contains: "us-california"
    - path: "packages/contracts/src/fixtures.ts"
      provides: "与 server 同源的 shared canonical fixtures"
      contains: "PHASE12_RESOLVED_CALIFORNIA"
    - path: ".planning/phases/12-canonical/12-UAT.md"
      provides: "与 server-authoritative 结果一致的 California 验收文案"
      contains: "California / Overseas Admin1 Type Label"
  key_links:
    - from: "apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts"
      to: "packages/contracts/src/fixtures.ts"
      via: "placeId / datasetVersion / typeLabel mirrored constants"
      pattern: "us-california|phase12-canonical-fixture-v1|一级行政区"
    - from: "apps/server/src/modules/canonical-places/canonical-places.service.ts"
      to: "apps/server/test/canonical-resolve.e2e-spec.ts"
      via: "bbox-based California resolve assertions"
      pattern: "34\\.0522|-118\\.2437|37\\.7749|-122\\.4194"
    - from: ".planning/phases/12-canonical/12-UAT.md"
      to: "packages/contracts/src/fixtures.ts"
      via: "expected California label/ID wording"
      pattern: "us-california|一级行政区"
---

<objective>
统一 canonical authoritative 口径，并把 California 从“几乎永远点不中的 fixture 点”升级为 server-owned admin1 区域识别。

Purpose: 让最后一条链路闭合为同一真源: server fixture 决定 California 的 canonical 身份，shared fixtures 与 UAT 文案都跟随它，而不是再保留一套 contracts alias 或旧的 “State” 文案。
Output: California bbox resolve、对齐后的 shared fixtures、更新后的 canonical resolve e2e 与历史 UAT 文案。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-RESEARCH.md
@.planning/phases/15-服务端记录与点亮闭环/15-UAT.md
@.planning/phases/12-canonical/12-UAT.md
@.planning/debug/california-not-recognized.md
@.planning/debug/beijing-no-type-label.md
@.planning/phases/13-行政区数据与几何交付/13-04-SUMMARY.md

<interfaces>
From [apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts] (current):
```typescript
const DATASET_VERSION = 'phase12-canonical-fixture-v1'

'us-california': {
  placeId: 'us-california',
  boundaryId: 'ne-admin1-us-california',
  placeKind: 'OVERSEAS_ADMIN1',
  datasetVersion: DATASET_VERSION,
  displayName: 'California',
  regionSystem: 'OVERSEAS',
  adminType: 'ADMIN1',
  typeLabel: '一级行政区',
  parentLabel: 'United States',
  subtitle: 'United States · 一级行政区',
}
```

From [apps/server/src/modules/canonical-places/canonical-places.service.ts] (current):
```typescript
private findFixture(input: ResolveCanonicalPlaceRequest): CanonicalResolveFixture | undefined {
  return CANONICAL_RESOLVE_FIXTURES.find(fixture => (
    Math.abs(fixture.click.lat - input.lat) < 0.0001
    && Math.abs(fixture.click.lng - input.lng) < 0.0001
  ))
}
```

California geometry bbox derived from current shard:
```json
{
  "minLng": -124.41,
  "maxLng": -114.13,
  "minLat": 32.53,
  "maxLat": 42.01
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 统一 shared/server fixture 口径，并修正 Phase 12 UAT 文案</name>
  <files>
    packages/contracts/src/fixtures.ts,
    apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts,
    .planning/phases/12-canonical/12-UAT.md
  </files>
  <read_first>
    packages/contracts/src/fixtures.ts,
    apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts,
    .planning/phases/12-canonical/12-UAT.md,
    .planning/debug/beijing-no-type-label.md,
    .planning/debug/california-not-recognized.md
  </read_first>
  <behavior>
    - shared fixtures 与 server fixture 对 Beijing/Hong Kong/Aba/California 使用相同 `placeId` 与 `datasetVersion`
    - California 的 shared fixture 使用 server-authoritative `typeLabel: '一级行政区'`
    - 历史 UAT 文案不再要求 `State` 或 contracts-only alias
  </behavior>
  <action>
    1. 在 `packages/contracts/src/fixtures.ts` 中把 `PHASE12_RESOLVED_BEIJING`, `PHASE12_RESOLVED_HONG_KONG`, `PHASE12_RESOLVED_ABA`, `PHASE12_RESOLVED_CALIFORNIA` 以及 ambiguous candidates 的 `placeId` / `datasetVersion` 对齐为 server fixture 值：
       - Beijing: `placeId: 'cn-beijing'`, `datasetVersion: 'phase12-canonical-fixture-v1'`
       - Hong Kong: `placeId: 'cn-hong-kong'`, `datasetVersion: 'phase12-canonical-fixture-v1'`
       - Aba: `placeId: 'cn-aba'`, `datasetVersion: 'phase12-canonical-fixture-v1'`
       - California: `placeId: 'us-california'`, `boundaryId: 'ne-admin1-us-california'`, `datasetVersion: 'phase12-canonical-fixture-v1'`, `typeLabel: '一级行政区'`, `subtitle: 'United States · 一级行政区'`
       - Tianjin/Langfang candidates: `cn-tianjin`, `cn-langfang`, 同 datasetVersion
    2. 保持 `apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts` 的 server-authoritative IDs 不反向迁就旧 contracts alias；如果需要补注释，仅说明 “shared fixtures mirror server-authoritative IDs”。
    3. 更新 `.planning/phases/12-canonical/12-UAT.md` 的 California 相关 expected/gap 文案，把 `"State"` 改为 `"一级行政区"`，把“server fixture wrong”改为“shared fixtures/UAT drifted from server-authoritative label”。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/contracts build && pnpm --filter @trip-map/contracts test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/contracts/src/fixtures.ts` 中 Beijing/Hong Kong/California 的 `placeId` 分别为 `cn-beijing`, `cn-hong-kong`, `us-california`
    - `packages/contracts/src/fixtures.ts` 的 canonical fixtures 使用 `datasetVersion: 'phase12-canonical-fixture-v1'`
    - `packages/contracts/src/fixtures.ts` 的 California fixture 使用 `typeLabel: '一级行政区'`
    - `.planning/phases/12-canonical/12-UAT.md` 不再包含 `popup shows "State"` 文案
    - `pnpm --filter @trip-map/contracts build` 与 `pnpm --filter @trip-map/contracts test` 通过
  </acceptance_criteria>
  <done>shared fixtures 与历史验收文案已经完全跟随 server-authoritative canonical fixture，不再保留旧 alias 和旧 label 口径。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用 California bbox 匹配替换单点超严容差，并补 canonical resolve 回归</name>
  <files>
    apps/server/src/modules/canonical-places/canonical-places.service.ts,
    apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts,
    apps/server/test/canonical-resolve.e2e-spec.ts
  </files>
  <read_first>
    apps/server/src/modules/canonical-places/canonical-places.service.ts,
    apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts,
    apps/server/test/canonical-resolve.e2e-spec.ts,
    packages/contracts/src/fixtures.ts,
    .planning/debug/california-not-recognized.md
  </read_first>
  <behavior>
    - Los Angeles (`34.0522, -118.2437`) 与 San Francisco (`37.7749, -122.4194`) 都解析为 `us-california`
    - Beijing/Hong Kong/Aba 仍走现有 representative click fixture，不被 California bbox 改坏
    - California 外部点击不会误命中 California
  </behavior>
  <action>
    1. 在 `apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts` 的 resolved fixture 定义上，为 California 新增明确 bbox 元数据，字段名固定为:
       ```ts
       bounds: {
         minLat: 32.53,
         maxLat: 42.01,
         minLng: -124.41,
         maxLng: -114.13,
       }
       ```
       其它 resolved fixture 继续只保留 `click`。
    2. 在 `apps/server/src/modules/canonical-places/canonical-places.service.ts` 中重写 `findFixture()`：优先匹配带 `bounds` 的 resolved fixture（California），判断 `lat/lng` 是否落在 bbox 内；未命中 bbox 时再回退现有 `click ±0.0001` 逻辑处理 Beijing/Hong Kong/Aba/ambiguous fixture。
    3. 更新 `apps/server/test/canonical-resolve.e2e-spec.ts`：
       - 保留现有 California fixture point 断言
       - 新增 Los Angeles 点 `34.0522/-118.2437` resolved 为 `us-california`
       - 新增 San Francisco 点 `37.7749/-122.4194` resolved 为 `us-california`
       - 新增 California bbox 外点（例如 `41.0/-111.9`）不命中 California，返回 `failed` 或至少 `placeId !== 'us-california'`
       - California resolved 断言同时锁定 `datasetVersion: 'phase12-canonical-fixture-v1'` 与 `typeLabel: '一级行政区'`
    4. 不引入新的客户端 authoritative resolver，也不要在 `LeafletMapStage.vue` 中为 California 写任何 special-case 识别分支。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server exec vitest run test/canonical-resolve.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts` 包含 California `bounds`，值为 `32.53/42.01/-124.41/-114.13`
    - `apps/server/src/modules/canonical-places/canonical-places.service.ts` 的 `findFixture()` 同时支持 `bounds` 与 legacy `click` 匹配
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 包含 `34.0522`, `-118.2437`, `37.7749`, `-122.4194`
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 的 California 断言包含 `placeId: 'us-california'`, `datasetVersion: 'phase12-canonical-fixture-v1'`, `typeLabel: '一级行政区'`
    - `pnpm --filter @trip-map/server exec vitest run test/canonical-resolve.e2e-spec.ts` 通过
  </acceptance_criteria>
  <done>California 已按 server-owned admin1 区域匹配被 authoritative 识别，shared fixtures 与测试文案全部与同一真源对齐。</done>
</task>

</tasks>

<verification>
- 跑 `pnpm --filter @trip-map/contracts build && pnpm --filter @trip-map/contracts test` 验证 shared fixtures 没被打坏。
- 跑 `pnpm --filter @trip-map/server exec vitest run test/canonical-resolve.e2e-spec.ts` 验证 California bbox resolve 与旧 fixture click 共存。
</verification>

<success_criteria>
- California 的真实区域点击不再退回 fallback，而是得到 `us-california` canonical 结果。
- `packages/contracts` fixtures、server fixture 和 Phase 12 UAT 文案统一到同一组 `placeId / datasetVersion / typeLabel`。
- 此改动不引入第二套客户端 authoritative 解析逻辑。
</success_criteria>

<output>
After completion, create `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-03-SUMMARY.md`
</output>

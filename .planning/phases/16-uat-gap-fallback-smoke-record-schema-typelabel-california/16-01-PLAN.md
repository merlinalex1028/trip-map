---
phase: 16-uat-gap-fallback-smoke-record-schema-typelabel-california
plan: 01
type: execute
wave: 2
depends_on:
  - "16-00"
files_modified:
  - packages/contracts/src/records.ts
  - apps/server/prisma/schema.prisma
  - apps/server/prisma/migrations/*phase16_record_metadata_contracts*/migration.sql
  - apps/server/scripts/backfill-record-metadata.ts
  - apps/server/src/modules/records/dto/create-travel-record.dto.ts
  - apps/server/src/modules/records/records.repository.ts
  - apps/server/src/modules/records/records.service.ts
  - apps/server/test/records-smoke.e2e-spec.ts
  - apps/server/test/records-travel.e2e-spec.ts
  - apps/server/test/records-contract.e2e-spec.ts
autonomous: true
requirements:
  - REQ-16-03
  - REQ-16-04

must_haves:
  truths:
    - "POST /records/smoke 返回的 canonical 字段与数据库行保持一致，不再只在 HTTP 响应里临时补字段"
    - "POST /records 与 GET /records 返回的 TravelRecord 携带 reopen/view surface 所需 canonical 展示元数据"
    - "已存在的 TravelRecord 在 Phase 16 升级后，可按 `placeId` 从 authoritative canonical catalog 回填 metadata，重新打开时恢复 canonical 标签"
    - "Phase 16 migration 不会因为 `SmokeRecord` / `TravelRecord` 新增 metadata 列而在已有数据上直接失败"
    - "服务端不再依赖前端从 subtitle 或 displayName 反推 typeLabel、parentLabel"
  artifacts:
    - path: "packages/contracts/src/records.ts"
      provides: "SmokeRecord 与 TravelRecord 的共享 canonical metadata 合同"
      contains: "export interface TravelRecord"
    - path: "apps/server/prisma/schema.prisma"
      provides: "SmokeRecord / TravelRecord 的可迁移 canonical metadata 列"
      contains: "model TravelRecord"
    - path: "apps/server/scripts/backfill-record-metadata.ts"
      provides: "按 `placeId` 从 authoritative fixture catalog 回填旧记录 metadata 的脚本"
      contains: "backfillRecordMetadata"
    - path: "apps/server/src/modules/records/records.repository.ts"
      provides: "SmokeRecord / TravelRecord 持久化写入完整 canonical metadata"
      contains: "createTravelRecord"
    - path: "apps/server/test/records-smoke.e2e-spec.ts"
      provides: "SmokeRecord DB round-trip 回归"
      contains: "toMatchObject"
  key_links:
    - from: "packages/contracts/src/records.ts"
      to: "apps/server/src/modules/records/dto/create-travel-record.dto.ts"
      via: "CreateTravelRecordRequest / DTO 字段同名对齐"
      pattern: "typeLabel|parentLabel|regionSystem|adminType|subtitle"
    - from: "apps/server/src/modules/records/records.repository.ts"
      to: "apps/server/prisma/schema.prisma"
      via: "Prisma create data"
      pattern: "prisma\\.(smokeRecord|travelRecord)\\.create"
    - from: "apps/server/src/modules/records/records.service.ts"
      to: "apps/server/test/records-travel.e2e-spec.ts"
      via: "HTTP contract response shape"
      pattern: "typeLabel|parentLabel|regionSystem|adminType"
---

<objective>
收拢 `SmokeRecord` 与 `TravelRecord` 的服务端持久化合同，让 DB 行、HTTP response 和 shared contracts 对同一份 canonical metadata 说同一种语言。

Purpose: 先修掉最底层 schema/contract 漂移，并把旧数据迁移与 backfill 路径明确下来，避免后续 web reopen、点亮 overlay、California 回归继续建立在会丢字段或会卡死 migration 的持久化链上。
Output: 扩展后的 `records` shared contract、可安全升级的 Prisma schema、旧记录 metadata backfill 脚本、records service/repository/DTO，以及覆盖 smoke/travel round-trip 与 legacy reopen 的 server e2e。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-RESEARCH.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VALIDATION.md
@.planning/phases/15-服务端记录与点亮闭环/15-UAT.md
@.planning/debug/records-smoke-test-failure.md
@.planning/debug/beijing-no-type-label.md
@.planning/debug/hk-no-type-label.md
@.planning/phases/15-服务端记录与点亮闭环/15-01-SUMMARY.md

<interfaces>
From [packages/contracts/src/place.ts]:
```typescript
export interface CanonicalPlaceSummary extends CanonicalPlaceRef {
  displayName: string
  regionSystem: 'CN' | 'OVERSEAS'
  adminType: ChinaAdminType | 'ADMIN1'
  typeLabel: string
  parentLabel: string
  subtitle: string
}
```

From [packages/contracts/src/records.ts] (current):
```typescript
export interface SmokeRecordCreateRequest extends CanonicalPlaceSummary {
  note?: string
}

export interface TravelRecord {
  id: string
  placeId: string
  boundaryId: string
  placeKind: PlaceKind
  datasetVersion: string
  displayName: string
  subtitle: string
  createdAt: string
}
```

From [apps/server/prisma/schema.prisma] (current):
```prisma
model SmokeRecord {
  id             String   @id @default(cuid())
  placeId        String
  boundaryId     String
  placeKind      String
  datasetVersion String
  displayName    String
  note           String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}

model TravelRecord {
  id             String   @id @default(cuid())
  placeId        String   @unique
  boundaryId     String
  placeKind      String
  datasetVersion String
  displayName    String
  subtitle       String
  createdAt      DateTime @default(now())
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 shared records contract、可迁移 Prisma schema 与旧记录 backfill 入口</name>
  <files>
    packages/contracts/src/records.ts,
    apps/server/prisma/schema.prisma,
    apps/server/prisma/migrations/*phase16_record_metadata_contracts*/migration.sql,
    apps/server/scripts/backfill-record-metadata.ts
  </files>
  <read_first>
    packages/contracts/src/records.ts,
    packages/contracts/src/place.ts,
    apps/server/prisma/schema.prisma,
    apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts,
    .planning/debug/records-smoke-test-failure.md,
    .planning/debug/beijing-no-type-label.md,
    .planning/debug/hk-no-type-label.md
  </read_first>
  <behavior>
    - `TravelRecord` 与 `CreateTravelRecordRequest` 暴露 `regionSystem`、`adminType`、`typeLabel`、`parentLabel`、`subtitle`
    - `SmokeRecord` 与 `TravelRecord` 两个 Prisma model 都有上述 canonical metadata 列，但 Phase 16 migration 采用 nullable 过渡，不因旧数据直接失败
    - 存量 `TravelRecord` / `SmokeRecord` 可按 `placeId` 从 server-authoritative fixture catalog 回填 metadata；未知 `placeId` 保持空值并记录，不伪造标签
    - 不创建第二套“从文案反推标签”的兼容字段
  </behavior>
  <action>
    1. 在 `packages/contracts/src/records.ts` 中把 `TravelRecord` 改为显式包含 `displayName`, `regionSystem`, `adminType`, `typeLabel`, `parentLabel`, `subtitle`, `createdAt`；把 `CreateTravelRecordRequest` 改为与 `CanonicalPlaceSummary` 同字段集，不要只保留 `displayName + subtitle`。
    2. 保留 `SmokeRecordCreateRequest extends CanonicalPlaceSummary` 的现状，不新增另一套 smoke-only metadata 类型。
    3. 在 `apps/server/prisma/schema.prisma` 中为 `SmokeRecord` 增加 `regionSystem String?`, `adminType String?`, `typeLabel String?`, `parentLabel String?`, `subtitle String?` 五列；为 `TravelRecord` 增加 `regionSystem String?`, `adminType String?`, `typeLabel String?`, `parentLabel String?` 四列，并保留已有 `subtitle String`。Phase 16 明确采用 nullable 过渡列；不要在这次 migration 里把新增列设成 `NOT NULL`。
    4. migration 名称固定使用 `phase16_record_metadata_contracts`。生成 migration 后检查 SQL，要求其只做 additive `ADD COLUMN`，不对新增 metadata 列追加 `SET NOT NULL` 或会卡死旧数据的默认值 hack。
    5. 新增 `apps/server/scripts/backfill-record-metadata.ts`，从 `apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts` 的 server-authoritative fixture catalog 建立 `placeId -> canonical metadata` 映射，并按 `placeId` 回填旧 `TravelRecord` 与 `SmokeRecord`：
       - `TravelRecord`：填充 `regionSystem`, `adminType`, `typeLabel`, `parentLabel`，并在 `subtitle` 为空时使用 canonical subtitle
       - `SmokeRecord`：填充新增的五个 metadata 列
       - 若旧记录 `placeId` 不在 authoritative catalog 中，只记录 unmatched rows，不要从 `displayName` / `subtitle` 猜标签
    6. 按顺序执行 `pnpm --filter @trip-map/contracts build`、`pnpm --filter @trip-map/server exec prisma migrate dev --name phase16_record_metadata_contracts`、`pnpm --filter @trip-map/server exec tsx scripts/backfill-record-metadata.ts` 与 `pnpm --filter @trip-map/server exec prisma generate`。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/contracts build && pnpm --filter @trip-map/server exec prisma migrate dev --name phase16_record_metadata_contracts && pnpm --filter @trip-map/server exec tsx scripts/backfill-record-metadata.ts && pnpm --filter @trip-map/server exec prisma generate</automated>
  </verify>
  <acceptance_criteria>
    - `packages/contracts/src/records.ts` 包含 `regionSystem: 'CN' | 'OVERSEAS'`
    - `packages/contracts/src/records.ts` 包含 `adminType: ChinaAdminType | 'ADMIN1'`
    - `packages/contracts/src/records.ts` 中 `TravelRecord` 与 `CreateTravelRecordRequest` 都包含 `typeLabel`, `parentLabel`, `subtitle`
    - `apps/server/prisma/schema.prisma` 的 `model SmokeRecord` 以 `String?` 形式包含 `regionSystem`, `adminType`, `typeLabel`, `parentLabel`, `subtitle`
    - `apps/server/prisma/schema.prisma` 的 `model TravelRecord` 以 `String?` 形式包含 `regionSystem`, `adminType`, `typeLabel`, `parentLabel`
    - `apps/server/scripts/backfill-record-metadata.ts` 存在，且包含 `backfillRecordMetadata` 与 `placeId`
    - `pnpm --filter @trip-map/contracts build` 退出码为 0
    - `apps/server/prisma/migrations/` 下存在本次 migration 目录，且 SQL 包含 `ALTER TABLE "SmokeRecord"` 与 `ALTER TABLE "TravelRecord"`，同时不包含对新增 metadata 列的 `SET NOT NULL`
    - `pnpm --filter @trip-map/server exec tsx scripts/backfill-record-metadata.ts` 退出码为 0，并输出 matched / unmatched rows 摘要
  </acceptance_criteria>
  <done>`records` shared contract 与 Prisma schema 已能安全承载 canonical metadata，旧记录也有明确的 authoritative backfill 路径，后续服务层无需再从输入对象临时拼装缺失字段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 records DTO/repository/service 与 e2e，锁定 DB round-trip 和 legacy reopen backfill</name>
  <files>
    apps/server/src/modules/records/dto/create-travel-record.dto.ts,
    apps/server/src/modules/records/records.repository.ts,
    apps/server/src/modules/records/records.service.ts,
    apps/server/test/records-smoke.e2e-spec.ts,
    apps/server/test/records-travel.e2e-spec.ts,
    apps/server/test/records-contract.e2e-spec.ts
  </files>
  <read_first>
    apps/server/src/modules/records/dto/create-travel-record.dto.ts,
    apps/server/src/modules/records/dto/create-smoke-record.dto.ts,
    apps/server/src/modules/records/records.repository.ts,
    apps/server/src/modules/records/records.service.ts,
    apps/server/src/modules/records/records.controller.ts,
    apps/server/test/records-smoke.e2e-spec.ts,
    apps/server/test/records-travel.e2e-spec.ts,
    apps/server/test/records-contract.e2e-spec.ts,
    packages/contracts/src/records.ts
  </read_first>
  <behavior>
    - `POST /records/smoke` 写入 DB 后，`storedRows[0]` 与 smoke request 的 canonical metadata 一致
    - `POST /records` 返回值与随后 `GET /records` 的行都包含 `typeLabel`, `parentLabel`, `subtitle`, `regionSystem`, `adminType`
    - 旧版 `TravelRecord` 行在执行 Phase 16 backfill 后，`GET /records` / reopen surface 可恢复 canonical label
    - DTO validation 继续阻止未知字段，且不允许缺少新增 canonical metadata
  </behavior>
  <action>
    1. 在 `apps/server/src/modules/records/dto/create-travel-record.dto.ts` 中新增 `regionSystem`, `adminType`, `typeLabel`, `parentLabel` 四个 `@IsString() @IsNotEmpty()` 字段；保持 `subtitle` 必填。
    2. 在 `apps/server/src/modules/records/records.repository.ts` 的 `createSmokeRecord()` 与 `createTravelRecord()` 中把 `regionSystem`, `adminType`, `typeLabel`, `parentLabel`, `subtitle` 全部写入 Prisma `data`；虽然 DB 列在 Phase 16 是 nullable 过渡列，但新写入记录必须总是写满。
    3. 在 `apps/server/src/modules/records/records.service.ts` 中让 `createSmoke()`, `findAllTravel()`, `createTravel()` 全部从数据库返回/映射完整 canonical metadata；不要继续从 `input` 临时补 `typeLabel`、`parentLabel` 作为“看起来对”的 response，也不要在读取旧记录时从字符串反推标签。
    4. 更新 `apps/server/test/records-smoke.e2e-spec.ts`：保留 `storedRows[0].toMatchObject(smokeRequest)` 断言，并显式断言五个 canonical metadata 字段存在于 DB 行。
    5. 更新 `apps/server/test/records-travel.e2e-spec.ts`：`validRecord` 固定包含 `regionSystem: 'CN'`, `adminType: 'MUNICIPALITY'`, `typeLabel: '直辖市'`, `parentLabel: '中国'`, `subtitle: '中国 · 直辖市'`；新增 `GET /records` 与 Prisma `travelRecord.findUnique` 对同一 placeId 的元数据断言。
    6. 在 `apps/server/test/records-travel.e2e-spec.ts` 或新增同目录辅助用例中，手工插入一条 Phase 16 之前风格的 legacy `TravelRecord`（新 metadata 列为 `null`、但 `placeId` 指向 canonical fixture），执行 `backfillRecordMetadata()` 后再调用 `GET /records`，断言该 place 的 `typeLabel`, `parentLabel`, `subtitle`, `regionSystem`, `adminType` 已恢复，用它作为“旧记录升级后 reopen”回归。
    7. 更新 `apps/server/test/records-contract.e2e-spec.ts`：继续用 `PHASE11_SMOKE_RECORD_REQUEST`，并断言 forbidNonWhitelisted 行为仍成立。
    8. 如果 server e2e 因 PostgreSQL 不可达失败，只允许以“DB connectivity blocker”结束本任务；不得删测试、不得跳过 legacy backfill 验证、不得改成 mock。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server exec vitest run test/records-smoke.e2e-spec.ts test/records-travel.e2e-spec.ts test/records-contract.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/modules/records/dto/create-travel-record.dto.ts` 包含 `regionSystem`, `adminType`, `typeLabel`, `parentLabel`
    - `apps/server/src/modules/records/records.repository.ts` 的 smoke/travel `data` 块都包含 `typeLabel`, `parentLabel`, `subtitle`
    - `apps/server/src/modules/records/records.service.ts` 中 `findAllTravel()` 返回对象包含 `regionSystem`, `adminType`, `typeLabel`, `parentLabel`, `subtitle`
    - `apps/server/test/records-smoke.e2e-spec.ts` 同时断言 response 和 DB row 的 canonical metadata
    - `apps/server/test/records-travel.e2e-spec.ts` 的 `validRecord` 包含 `typeLabel: '直辖市'` 与 `parentLabel: '中国'`
    - `apps/server/test/records-travel.e2e-spec.ts` 或同目录新增辅助用例包含 legacy row backfill / reopen 断言
    - `pnpm --filter @trip-map/server exec vitest run test/records-smoke.e2e-spec.ts test/records-travel.e2e-spec.ts test/records-contract.e2e-spec.ts` 通过；若失败原因为 PostgreSQL 不可达，执行总结必须明确记录该 blocker
  </acceptance_criteria>
  <done>SmokeRecord 与 TravelRecord 的服务端读写链已经按 shared contract 完整 round-trip canonical metadata，旧记录也能在 authoritative backfill 后恢复 reopen 所需标签，DB 行与 HTTP contract 不再分叉。</done>
</task>

</tasks>

<verification>
- 先确认 `16-00` 已完成且 `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-00-DB-PREFLIGHT.md` 为 `status: ready`。
- 再跑 `pnpm --filter @trip-map/contracts build`、Prisma migrate/backfill/generate，确认 contract + schema + legacy backfill 可用。
- 再跑 `pnpm --filter @trip-map/server exec vitest run test/records-smoke.e2e-spec.ts test/records-travel.e2e-spec.ts test/records-contract.e2e-spec.ts`。
- 如果 e2e 失败且错误为 PostgreSQL 连通性，保留代码改动与 grep 证据，但在 SUMMARY 中显式记录 “DB connectivity blocked full verification”。
</verification>

<success_criteria>
- SmokeRecord 的 canonical metadata 真正持久化到 DB，而不是只在 response 中伪造。
- TravelRecord 从创建到列表读取都保留 `typeLabel`、`parentLabel`、`subtitle`，并一并持久化 `regionSystem`、`adminType`。
- 旧版 TravelRecord 在 schema 升级后可按 `placeId` 回填 canonical metadata，重新打开时恢复标签，而不是继续显示空值。
- Web 不再需要靠字符串猜测 canonical 展示元数据。
</success_criteria>

<output>
After completion, create `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-01-SUMMARY.md`
</output>

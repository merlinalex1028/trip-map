---
phase: 16-uat-gap-fallback-smoke-record-schema-typelabel-california
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-00-DB-PREFLIGHT.md
autonomous: false
requirements:
  - REQ-16-03
  - REQ-16-04

must_haves:
  truths:
    - "Phase 16 的 server-side migration 和 e2e 不会在未知 PostgreSQL 连通性状态下直接启动"
    - "执行者在修改 schema 前，已经确认 `prisma migrate status` 能连到配置中的 PostgreSQL"
    - "如果数据库不可达，Phase 16 会在 Wave 0 明确阻塞，而不是等到 16-01/16-03 运行中途失败"
  artifacts:
    - path: ".planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-00-DB-PREFLIGHT.md"
      provides: "Phase 16 的 PostgreSQL preflight 结果与 blocker 记录"
      contains: "status:"
  key_links:
    - from: ".planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-00-DB-PREFLIGHT.md"
      to: "apps/server/prisma.config.ts"
      via: "DATABASE_URL / DIRECT_URL / SHADOW_DATABASE_URL gate"
      pattern: "DATABASE_URL|DIRECT_URL|SHADOW_DATABASE_URL"
    - from: ".planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-00-DB-PREFLIGHT.md"
      to: "apps/server/test/records-contract.e2e-spec.ts"
      via: "DB-backed server preflight"
      pattern: "records-contract"
---

<objective>
在 Phase 16 的任何 server migration 或 e2e 开始前，先把 PostgreSQL reachability 明确 gate 住，并把 blocker 记录成可恢复的 Wave 0 前置条件。

Purpose: 已知 PostgreSQL 连通性是当前 phase 的现实 blocker；如果不先 gate，16-01 的 migration/backfill 和 16-03 的 server e2e 都可能在不可执行状态下直接失败。
Output: 一个明确的 DB preflight 结果文件，以及一个阻塞式 checkpoint，保证后续 server plan 只在数据库可达时启动。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-RESEARCH.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VALIDATION.md
@apps/server/prisma.config.ts
@apps/server/package.json

<interfaces>
From [apps/server/prisma.config.ts]:
```typescript
env('DATABASE_URL')
env('DIRECT_URL')
env('SHADOW_DATABASE_URL')
```

From [apps/server/package.json]:
```json
{
  "scripts": {
    "prisma:generate": "prisma generate --schema prisma/schema.prisma",
    "test": "vitest run"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行 PostgreSQL preflight 并记录可执行状态</name>
  <files>.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-00-DB-PREFLIGHT.md</files>
  <action>
    1. 先读取 `apps/server/prisma.config.ts` 和 `apps/server/package.json`，确认 Prisma 实际依赖的 env key 与命令入口。
    2. 依次运行以下只读/低风险前置检查，并把命令、退出码、关键输出原样记录进 `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-00-DB-PREFLIGHT.md`：
       - `pnpm --filter @trip-map/server exec prisma validate --schema prisma/schema.prisma`
       - `pnpm --filter @trip-map/server exec prisma migrate status --schema prisma/schema.prisma`
       - `pnpm --filter @trip-map/server exec vitest run test/records-contract.e2e-spec.ts`
    3. 如果三条命令都成功，把 preflight 文件头部写成 `status: ready`；如果任一命令因 PostgreSQL reachability / auth / TLS / DNS / pooler 等失败，把文件头部写成 `status: blocked`，并在正文里明确失败命令与原始错误（例如 Prisma `P1001`）。
    4. 不要在这个 plan 中生成 migration、不要改 schema、不要用 mock 绕过 DB preflight。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server exec prisma validate --schema prisma/schema.prisma && pnpm --filter @trip-map/server exec prisma migrate status --schema prisma/schema.prisma && pnpm --filter @trip-map/server exec vitest run test/records-contract.e2e-spec.ts</automated>
  </verify>
  <done>存在一份 `16-00-DB-PREFLIGHT.md`，能明确回答“Phase 16 的 server migration/e2e 现在是否可执行”。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 若 preflight blocked，则先恢复数据库连通性再继续</name>
  <files>n/a</files>
  <action>
    只有当 Task 1 生成的 preflight 文件头部为 `status: blocked` 时才执行本 checkpoint。人类需要在 repo 外修复数据库 reachability，包括但不限于：提供有效的 `DATABASE_URL` / `DIRECT_URL` / `SHADOW_DATABASE_URL`、恢复 Supabase pooler / VPN / 网络访问、确认执行环境可以连接项目 PostgreSQL。修复后必须重新执行 Task 1，直到 preflight 文件变成 `status: ready`；在此之前，不得继续执行 16-01 或 16-03。
  </action>
  <what-built>Task 1 已生成 PostgreSQL preflight 记录，明确当前 server wave 是否可执行。</what-built>
  <how-to-verify>
    1. 打开 `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-00-DB-PREFLIGHT.md`
    2. 如果文件头是 `status: ready`，本 checkpoint 直接通过
    3. 如果文件头是 `status: blocked`，先在 repo 外修复 DB reachability：提供有效的 `DATABASE_URL` / `DIRECT_URL` / `SHADOW_DATABASE_URL`，并恢复 Supabase pooler / VPN / 网络访问
    4. 重新执行 Task 1，直到 `prisma migrate status` 和 `records-contract.e2e-spec.ts` 都通过，且 preflight 文件变成 `status: ready`
  </how-to-verify>
  <verify>`.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-00-DB-PREFLIGHT.md` 头部为 `status: ready`，且正文包含成功的 `prisma migrate status` 与 `records-contract.e2e-spec.ts` 输出摘要。</verify>
  <done>Phase 16 的数据库 blocker 已解除，后续依赖 server DB 的计划可以在已知可达状态下启动。</done>
  <resume-signal>数据库可达后，回复 `db-ready`。</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-00-DB-PREFLIGHT.md` 存在，且包含 `status: ready` 或 `status: blocked`
- 若是 `status: blocked`，Phase 16 后续依赖此 plan 的 server plan 一律不继续执行
- 只有 preflight 变成 `status: ready` 后，16-01 与 16-03 才能启动
</verification>

<success_criteria>
- Phase 16 的数据库 blocker 被提前显式建模，不再隐藏在 16-01/16-03 的执行中途。
- server migration/backfill/e2e 的启动条件已经变成可验证的前置 gate。
</success_criteria>

<output>
After completion, create `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-00-SUMMARY.md`
</output>

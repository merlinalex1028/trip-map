---
phase: 30-travel-statistics-and-completion-overview
plan: "02"
type: execute
wave: 2
depends_on:
  - "30-01"
files_modified:
  - apps/web/src/services/api/stats.ts
  - apps/web/src/stores/stats.ts
  - apps/web/src/components/statistics/StatCard.vue
  - apps/web/src/components/statistics/StatCard.spec.ts
  - apps/web/src/views/StatisticsPageView.vue
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03

must_haves:
  truths:
    - "StatisticsPageView 在 authenticated + 有数据时渲染两张 StatCard"
    - "StatisticsPageView 在 restoring 或 isLoading 时渲染 skeleton"
    - "StatisticsPageView 在 anonymous 时渲染登录 CTA"
    - "StatisticsPageView 在 authenticated + 0 条记录时渲染 empty state"
    - "StatCard 正确渲染 label、value、unit 三个 prop"
    - "useStatsStore.fetchStatsData() 调用 GET /records/stats 并更新 stats/isLoading/error"
  artifacts:
    - path: "apps/web/src/services/api/stats.ts"
      provides: "fetchStats() 函数，调用 GET /records/stats"
      exports: ["fetchStats"]
    - path: "apps/web/src/stores/stats.ts"
      provides: "useStatsStore，管理 stats/isLoading/error 状态"
      exports: ["useStatsStore"]
    - path: "apps/web/src/components/statistics/StatCard.vue"
      provides: "单指标卡片组件，接受 label/value/unit/gradient props"
      contains: "defineProps"
    - path: "apps/web/src/views/StatisticsPageView.vue"
      provides: "四状态统计页面视图"
      contains: "data-route-view=\"statistics\""
  key_links:
    - from: "apps/web/src/views/StatisticsPageView.vue"
      to: "apps/web/src/stores/stats.ts"
      via: "useStatsStore().fetchStatsData() in onMounted"
      pattern: "fetchStatsData"
    - from: "apps/web/src/stores/stats.ts"
      to: "apps/web/src/services/api/stats.ts"
      via: "fetchStats()"
      pattern: "fetchStats"
    - from: "apps/web/src/views/StatisticsPageView.vue"
      to: "apps/web/src/components/statistics/StatCard.vue"
      via: "<StatCard> in populated state"
      pattern: "StatCard"
---

<objective>
新建前端统计层：API 服务函数 fetchStats、Pinia store useStatsStore、StatCard 组件、StatisticsPageView 页面视图。

Purpose: 满足 D-01（独立 /statistics 页面）、D-03（前端从后端 /stats 端点获取数据）、D-04（Kawaii 风格卡片）、D-06（两个指标）。
Output: stats.ts（API）、stats.ts（store）、StatCard.vue、StatCard.spec.ts、StatisticsPageView.vue。
</objective>

<execution_context>
@/Users/huangjingping/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/huangjingping/i/trip-map/.planning/PROJECT.md
@/Users/huangjingping/i/trip-map/.planning/ROADMAP.md
@/Users/huangjingping/i/trip-map/.planning/phases/30-travel-statistics-and-completion-overview/30-CONTEXT.md
@/Users/huangjingping/i/trip-map/.planning/phases/30-travel-statistics-and-completion-overview/30-UI-SPEC.md

<interfaces>
<!-- 执行器需要的现有接口，无需探索代码库 -->

From apps/web/src/services/api/client.ts:
```typescript
export async function apiFetchJson<T>(
  path: string,
  init: RequestInit = {},
  options: ApiFetchJsonOptions = {},
): Promise<T>
```

From apps/web/src/services/api/records.ts (GET 调用模式参考):
```typescript
// GET 调用：只传 path，无 init/options
export async function fetchStats(): Promise<TravelStatsResponse> {
  return apiFetchJson<TravelStatsResponse>('/records/stats')
}
```

From apps/web/src/stores/map-points.ts (store 模式参考):
```typescript
import { acceptHMRUpdate, defineStore } from 'pinia'
import { shallowRef } from 'vue'

export const useMapPointsStore = defineStore('map-points', () => {
  const data = shallowRef<T | null>(null)
  const isLoading = shallowRef(false)
  const error = shallowRef<string | null>(null)
  // ...
  return { data, isLoading, error, fetchData }
})

if (import.meta.hot) {
  import.meta.hot.accept(acceptHMRUpdate(useMapPointsStore, import.meta.hot))
}
```

From apps/web/src/views/TimelinePageView.vue (四状态页面模式):
```typescript
const isRestoring = computed(() => status.value === 'restoring')
const shouldShowAnonymousState = computed(
  () => status.value !== 'authenticated' || currentUser.value === null,
)
// 注意：empty state 基于 stats.totalTrips，不依赖 travelRecords.length（见 RESEARCH.md Pitfall 1）
const shouldShowEmptyState = computed(
  () => status.value === 'authenticated' && !isLoading.value && (stats.value?.totalTrips ?? 0) === 0,
)
const shouldShowStats = computed(
  () => status.value === 'authenticated' && !isLoading.value && (stats.value?.totalTrips ?? 0) > 0,
)
```

From packages/contracts/src/stats.ts (由 Plan 01 创建):
```typescript
export interface TravelStatsResponse {
  totalTrips: number
  uniquePlaces: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API 服务函数 + useStatsStore + StatCard 组件 + 组件测试</name>
  <files>
    apps/web/src/services/api/stats.ts,
    apps/web/src/stores/stats.ts,
    apps/web/src/components/statistics/StatCard.vue,
    apps/web/src/components/statistics/StatCard.spec.ts
  </files>
  <read_first>
    - apps/web/src/services/api/client.ts — apiFetchJson 签名，GET 调用模式
    - apps/web/src/services/api/records.ts — import 模式，apiFetchJson 调用形式
    - apps/web/src/stores/map-points.ts — defineStore setup 函数、shallowRef、acceptHMRUpdate 模式
    - apps/web/src/components/timeline/TimelineVisitCard.vue — Kawaii 卡片 class 模式（rounded-[28px]、border-white/80、shadow-[var(--shadow-float)]、p-5）
  </read_first>
  <behavior>
    - fetchStats() 调用 apiFetchJson('/records/stats')，无额外参数
    - useStatsStore 包含 stats（shallowRef&lt;TravelStatsResponse | null&gt;）、isLoading（shallowRef&lt;boolean&gt;）、error（shallowRef&lt;string | null&gt;）
    - fetchStatsData() 设置 isLoading=true，调用 fetchStats()，成功时更新 stats，失败时设置 error='fetch-failed'，finally 设置 isLoading=false
    - StatCard 接受 label: string、value: number、unit: string、gradient: string 四个 prop
    - StatCard 渲染 eyebrow label（0.72rem uppercase）、大数字（clamp(2rem,3.5vw,3rem) font-700）、unit label（14px muted）
    - StatCard.spec.ts 测试：label/value/unit 正确渲染到 DOM
  </behavior>
  <action>
    **1. 新建 apps/web/src/services/api/stats.ts（完整文件）：**
    ```typescript
    import type { TravelStatsResponse } from '@trip-map/contracts'

    import { apiFetchJson } from './client'

    export async function fetchStats(): Promise<TravelStatsResponse> {
      return apiFetchJson<TravelStatsResponse>('/records/stats')
    }
    ```

    **2. 新建 apps/web/src/stores/stats.ts（完整文件）：**
    ```typescript
    import { acceptHMRUpdate, defineStore } from 'pinia'
    import { shallowRef } from 'vue'

    import type { TravelStatsResponse } from '@trip-map/contracts'

    import { fetchStats } from '../services/api/stats'

    export const useStatsStore = defineStore('stats', () => {
      const stats = shallowRef<TravelStatsResponse | null>(null)
      const isLoading = shallowRef(false)
      const error = shallowRef<string | null>(null)

      async function fetchStatsData() {
        isLoading.value = true
        error.value = null
        try {
          stats.value = await fetchStats()
        } catch {
          error.value = 'fetch-failed'
        } finally {
          isLoading.value = false
        }
      }

      return { stats, isLoading, error, fetchStatsData }
    })

    if (import.meta.hot) {
      import.meta.hot.accept(acceptHMRUpdate(useStatsStore, import.meta.hot))
    }
    ```

    **3. 新建 apps/web/src/components/statistics/StatCard.vue（完整文件）：**
    ```vue
    <script setup lang="ts">
    interface Props {
      label: string
      value: number
      unit: string
      gradient: string
    }

    defineProps<Props>()
    </script>

    <template>
      <article
        class="grid gap-3 rounded-[28px] border border-white/80 p-5 shadow-[var(--shadow-float)]"
        :style="{ background: gradient }"
        data-region="stat-card"
      >
        <p
          class="text-[0.72rem] font-semibold tracking-[0.10em] text-[var(--color-ink-soft)] uppercase"
          data-stat="label"
        >
          {{ label }}
        </p>
        <div class="space-y-1">
          <p
            class="text-[clamp(2rem,3.5vw,3rem)] font-bold leading-[1.2] text-[var(--color-ink-strong)]"
            data-stat="value"
          >
            {{ value }}
          </p>
          <p
            class="text-[length:var(--font-label-size)] font-semibold text-[var(--color-ink-muted)]"
            data-stat="unit"
          >
            {{ unit }}
          </p>
        </div>
      </article>
    </template>
    ```

    **4. 新建 apps/web/src/components/statistics/StatCard.spec.ts（完整文件）：**
    ```typescript
    import { mount } from '@vue/test-utils'
    import { describe, expect, it } from 'vitest'

    import StatCard from './StatCard.vue'

    describe('StatCard', () => {
      it('renders label correctly', () => {
        const wrapper = mount(StatCard, {
          props: {
            label: '总旅行次数',
            value: 42,
            unit: '次旅行',
            gradient: 'linear-gradient(180deg,rgba(255,255,255,0.94),rgba(255,246,250,0.94))',
          },
        })

        expect(wrapper.find('[data-stat="label"]').text()).toBe('总旅行次数')
      })

      it('renders value correctly', () => {
        const wrapper = mount(StatCard, {
          props: {
            label: '已去过地点数',
            value: 7,
            unit: '个地点',
            gradient: 'linear-gradient(180deg,rgba(255,255,255,0.92),rgba(239,250,252,0.94))',
          },
        })

        expect(wrapper.find('[data-stat="value"]').text()).toBe('7')
      })

      it('renders unit correctly', () => {
        const wrapper = mount(StatCard, {
          props: {
            label: '总旅行次数',
            value: 10,
            unit: '次旅行',
            gradient: 'linear-gradient(180deg,rgba(255,255,255,0.94),rgba(255,246,250,0.94))',
          },
        })

        expect(wrapper.find('[data-stat="unit"]').text()).toBe('次旅行')
      })
    })
    ```
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/services/api/stats.ts 存在，包含 `export async function fetchStats(`
    - apps/web/src/services/api/stats.ts 包含 `apiFetchJson&lt;TravelStatsResponse&gt;('/records/stats')`
    - apps/web/src/stores/stats.ts 存在，包含 `export const useStatsStore`
    - apps/web/src/stores/stats.ts 包含 `async function fetchStatsData(`
    - apps/web/src/stores/stats.ts 包含 `acceptHMRUpdate(useStatsStore`
    - apps/web/src/components/statistics/StatCard.vue 存在，包含 `defineProps&lt;Props&gt;()`
    - apps/web/src/components/statistics/StatCard.vue 包含 `data-stat="label"`
    - apps/web/src/components/statistics/StatCard.vue 包含 `data-stat="value"`
    - apps/web/src/components/statistics/StatCard.vue 包含 `data-stat="unit"`
    - apps/web/src/components/statistics/StatCard.spec.ts 存在，包含 `describe('StatCard'`
    - pnpm --filter @trip-map/web test 退出码为 0，StatCard 三个测试全部通过
  </acceptance_criteria>
  <done>fetchStats、useStatsStore、StatCard 组件均可用，StatCard 单元测试通过。</done>
</task>

<task type="auto">
  <name>Task 2: StatisticsPageView 四状态页面视图</name>
  <files>apps/web/src/views/StatisticsPageView.vue</files>
  <read_first>
    - apps/web/src/views/TimelinePageView.vue — 完整页面结构参考（shell class、header 模式、四状态 v-if/v-else-if 链、skeleton 模式、anonymous 模式、empty 模式）
    - apps/web/src/stores/auth-session.ts — useAuthSessionStore 导出名，storeToRefs 用法
  </read_first>
  <behavior>
    - isRestoring 或 isLoading 时：渲染 data-state="restoring" skeleton（两个占位卡片）
    - shouldShowAnonymousState 时：渲染 data-state="anonymous"，heading="登录后查看你的旅行统计"，CTA="立即登录"（调用 authSessionStore.openAuthModal('login')）
    - shouldShowEmptyState 时：渲染 data-state="empty"，heading="还没有旅行数据"，RouterLink to="/" CTA="去地图添加旅行"
    - shouldShowStats 时：渲染 data-state="populated"，grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-5，两张 StatCard
    - error 状态（error !== null 且 !isLoading）：渲染 data-state="error"，heading="统计数据加载失败"，retry button 调用 fetchStatsData()
    - onMounted：status === 'authenticated' 时调用 statsStore.fetchStatsData()
    - shouldShowEmptyState 基于 stats.value?.totalTrips === 0（不依赖 travelRecords.length，per RESEARCH.md Pitfall 1）
    - 页面 eyebrow badge 文字：Travel Statistics
    - 页面 heading：旅行统计
    - 页面 subtitle：这里汇总了你的旅行数据——总旅行次数统计每一次独立去访，已去过地点数则对同一地点只计一次。
    - StatCard 1：label="总旅行次数" :value="stats!.totalTrips" unit="次旅行" gradient="linear-gradient(180deg,rgba(255,255,255,0.94),rgba(255,246,250,0.94))"
    - StatCard 2：label="已去过地点数" :value="stats!.uniquePlaces" unit="个地点" gradient="linear-gradient(180deg,rgba(255,255,255,0.92),rgba(239,250,252,0.94))"
    - summary badge（populated state）：{{ stats!.totalTrips }} 次旅行 · {{ stats!.uniquePlaces }} 个地点
  </behavior>
  <action>
    新建 apps/web/src/views/StatisticsPageView.vue，完整内容如下。

    **script setup 部分：**
    ```typescript
    import { storeToRefs } from 'pinia'
    import { computed, onMounted } from 'vue'
    import { RouterLink } from 'vue-router'

    import StatCard from '../components/statistics/StatCard.vue'
    import { useAuthSessionStore } from '../stores/auth-session'
    import { useStatsStore } from '../stores/stats'

    const authSessionStore = useAuthSessionStore()
    const statsStore = useStatsStore()

    const { currentUser, status } = storeToRefs(authSessionStore)
    const { stats, isLoading, error } = storeToRefs(statsStore)

    const isRestoring = computed(() => status.value === 'restoring')
    const shouldShowAnonymousState = computed(
      () => !isRestoring.value && (status.value !== 'authenticated' || currentUser.value === null),
    )
    const shouldShowEmptyState = computed(
      () =>
        status.value === 'authenticated' &&
        !isLoading.value &&
        error.value === null &&
        (stats.value?.totalTrips ?? 0) === 0,
    )
    const shouldShowStats = computed(
      () =>
        status.value === 'authenticated' &&
        !isLoading.value &&
        error.value === null &&
        (stats.value?.totalTrips ?? 0) > 0,
    )

    onMounted(() => {
      if (status.value === 'authenticated') {
        void statsStore.fetchStatsData()
      }
    })
    ```

    **template 部分（section shell + header + 五状态 v-if 链）：**

    Shell class（与 TimelinePageView 完全一致）：
    `class="flex min-h-0 flex-col gap-5 overflow-y-auto rounded-[32px] border border-white/80 bg-[linear-gradient(180deg,rgba(255,255,255,0.86),rgba(255,248,251,0.94))] p-5 shadow-[var(--shadow-stage)] md:gap-6 md:p-6"`
    `data-region="statistics-shell"` `data-route-view="statistics"`

    Header（与 TimelinePageView 完全一致的 grid 结构）：
    - eyebrow badge：`Travel Statistics`
    - h2：`旅行统计`
    - subtitle：`这里汇总了你的旅行数据——总旅行次数统计每一次独立去访，已去过地点数则对同一地点只计一次。`
    - 右侧：username badge（authenticated 时）+ RouterLink to="/" 返回地图

    Restoring/loading skeleton（data-state="restoring"）：
    - 两个占位卡片（rounded-[28px] border border-white/80 bg-white/72 p-5），每个内含两行 shimmer div

    Anonymous state（data-state="anonymous"）：
    - heading：`登录后查看你的旅行统计`
    - body：`统计页面会汇总你的总旅行次数和已去过的地点数。登录后即可在这里查看。`
    - CTA button：`立即登录`，@click="authSessionStore.openAuthModal('login')"

    Empty state（data-state="empty"）：
    - heading：`还没有旅行数据`
    - body：`先回到地图点亮一个去过的地点吧。保存后，这里会出现你的第一条统计数据。`
    - RouterLink to="/"：`去地图添加旅行`

    Error state（v-else-if="error !== null && !isLoading"，data-state="error"）：
    - heading：`统计数据加载失败`
    - body：`无法获取旅行统计，请稍后重试。`
    - retry button：`重新加载统计`，@click="statsStore.fetchStatsData()"

    Populated state（data-state="populated"）：
    - summary badge：`{{ stats!.totalTrips }} 次旅行 · {{ stats!.uniquePlaces }} 个地点`
    - StatCard grid：`class="grid grid-cols-1 gap-4 md:grid-cols-2 md:gap-5"`
    - StatCard 1：label="总旅行次数" :value="stats!.totalTrips" unit="次旅行" gradient="linear-gradient(180deg,rgba(255,255,255,0.94),rgba(255,246,250,0.94))"
    - StatCard 2：label="已去过地点数" :value="stats!.uniquePlaces" unit="个地点" gradient="linear-gradient(180deg,rgba(255,255,255,0.92),rgba(239,250,252,0.94))"

    完整 v-if 链顺序（CRITICAL — 顺序不可颠倒）：
    1. `v-if="isRestoring || isLoading"` → skeleton
    2. `v-else-if="shouldShowAnonymousState"` → anonymous
    3. `v-else-if="error !== null"` → error
    4. `v-else-if="shouldShowEmptyState"` → empty
    5. `v-else-if="shouldShowStats"` → populated
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/views/StatisticsPageView.vue 存在
    - 包含 `data-route-view="statistics"`
    - 包含 `data-state="restoring"`
    - 包含 `data-state="anonymous"`
    - 包含 `data-state="empty"`
    - 包含 `data-state="error"`
    - 包含 `data-state="populated"`
    - 包含 `fetchStatsData`（onMounted 调用）
    - 包含 `stats!.totalTrips`（populated state StatCard）
    - 包含 `stats!.uniquePlaces`（populated state StatCard）
    - 包含 `Travel Statistics`（eyebrow badge）
    - 包含 `旅行统计`（h2 heading）
    - pnpm --filter @trip-map/web typecheck 退出码为 0
  </acceptance_criteria>
  <done>StatisticsPageView 五状态完整实现，类型检查通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StatisticsPageView → useStatsStore | 前端 store 调用 /records/stats，必须携带 session cookie（credentials: 'include'） |
| 未认证用户访问 /statistics 路由 | 前端路由无 guard，但 anonymous state 会阻止数据展示；后端 SessionAuthGuard 是真正的安全边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Information Disclosure | StatisticsPageView anonymous state | mitigate | shouldShowAnonymousState 在 status !== 'authenticated' 时阻止渲染 stats 数据，不泄露任何数值 |
| T-30-05 | Information Disclosure | useStatsStore.fetchStatsData | accept | apiFetchJson 携带 credentials: 'include'，401 由 ApiClientError 抛出，store 捕获后设置 error='fetch-failed'，不向 UI 暴露原始错误信息 |
| T-30-06 | Tampering | StatCard gradient prop | accept | gradient 为纯 CSS 字符串，绑定到 :style，不执行脚本；无用户输入路径 |
</threat_model>

<verification>
```bash
# 1. web 单元测试通过（含 StatCard.spec.ts）
pnpm --filter @trip-map/web test

# 2. 类型检查通过
pnpm --filter @trip-map/web typecheck

# 3. 验证 StatCard 四个 prop 存在
grep -n "defineProps\|label\|value\|unit\|gradient" apps/web/src/components/statistics/StatCard.vue

# 4. 验证 StatisticsPageView 五状态
grep -n "data-state" apps/web/src/views/StatisticsPageView.vue
# 期望：restoring、anonymous、error、empty、populated 均出现

# 5. 验证 fetchStatsData 在 onMounted 中调用
grep -n "fetchStatsData\|onMounted" apps/web/src/views/StatisticsPageView.vue
```
</verification>

<success_criteria>
- pnpm --filter @trip-map/web test 全部通过（含 StatCard 三个测试）
- pnpm --filter @trip-map/web typecheck 通过
- StatisticsPageView 包含五个 data-state（restoring/anonymous/error/empty/populated）
- StatCard 正确渲染 label/value/unit
- useStatsStore 可从 stores/stats 导入，fetchStatsData 调用 /records/stats
</success_criteria>

<output>
完成后创建 .planning/phases/30-travel-statistics-and-completion-overview/30-02-SUMMARY.md
</output>

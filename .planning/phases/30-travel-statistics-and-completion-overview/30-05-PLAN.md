---
phase: 30-travel-statistics-and-completion-overview
plan: "05"
type: execute
wave: 2
depends_on:
  - "30-04"
files_modified:
  - apps/web/src/views/StatisticsPageView.vue
  - apps/web/src/views/StatisticsPageView.spec.ts
  - apps/web/src/stores/stats.spec.ts
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
gap_closure: true

must_haves:
  truths:
    - "StatisticsPageView populated state 渲染三张 StatCard（总旅行次数、已去过地点数、已去过国家/地区数）"
    - "summary badge 显示「X 次旅行 · Y 个地点 · Z 个国家/地区」"
    - "skeleton state 渲染 3 个占位卡片"
    - "multi-visit same place 不影响 visitedCountries 显示"
    - "前端测试全部通过"
    - "类型检查通过"
  artifacts:
    - path: "apps/web/src/views/StatisticsPageView.vue"
      provides: "三指标统计页面，含第三张 StatCard 和完成度上下文"
      contains: "visitedCountries"
    - path: "apps/web/src/views/StatisticsPageView.spec.ts"
      provides: "含 visitedCountries 的页面测试"
      contains: "visitedCountries"
    - path: "apps/web/src/stores/stats.spec.ts"
      provides: "含 visitedCountries 的 store 测试"
      contains: "visitedCountries"
  key_links:
    - from: "apps/web/src/views/StatisticsPageView.vue"
      to: "apps/web/src/components/statistics/StatCard.vue"
      via: "第三张 <StatCard> in populated state"
      pattern: "visitedCountries"
    - from: "apps/web/src/views/StatisticsPageView.spec.ts"
      to: "apps/web/src/services/api/stats"
      via: "fetchStatsMock 返回含 visitedCountries"
      pattern: "visitedCountries"
---

<objective>
更新前端统计页面，新增第三张 StatCard 展示已去过国家/地区数，更新 summary badge 为三指标格式，并修复所有前端测试中的 mock 数据以包含 visitedCountries 和 totalSupportedCountries 字段。

Purpose: 关闭 VERIFICATION.md Gap 1（StatisticsPageView 缺少第三张卡片）、Gap 2（无完成度 UI）、Gap 3（缺少去重验证测试）。满足 STAT-01（已去过国家/地区数展示）、STAT-02（完成度口径与支持覆盖范围一致）、STAT-03（多次旅行不放大统计）。
Output: 更新的 StatisticsPageView.vue、更新的测试文件。
</objective>

<execution_context>
@/Users/huangjingping/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/huangjingping/i/trip-map/.planning/PROJECT.md
@/Users/huangjingping/i/trip-map/.planning/ROADMAP.md
@/Users/huangjingping/i/trip-map/.planning/phases/30-travel-statistics-and-completion-overview/30-CONTEXT.md
@/Users/huangjingping/i/trip-map/.planning/phases/30-travel-statistics-and-completion-overview/30-VERIFICATION.md
@/Users/huangjingping/i/trip-map/.planning/phases/30-travel-statistics-and-completion-overview/30-04-SUMMARY.md

<interfaces>
<!-- 执行器需要的现有接口 -->

From packages/contracts/src/stats.ts (由 Plan 30-04 扩展):
```typescript
export interface TravelStatsResponse {
  totalTrips: number
  uniquePlaces: number
  visitedCountries: number
  totalSupportedCountries: number
}
```

From apps/web/src/views/StatisticsPageView.vue (当前 populated state，line 246-277):
```html
<div v-else-if="shouldShowStats" class="grid gap-4" data-state="populated">
  <!-- summary badge -->
  <p ...>{{ stats!.totalTrips }} 次旅行 · {{ stats!.uniquePlaces }} 个地点</p>
  <!-- 2 StatCards -->
  <div class="grid grid-cols-1 gap-4 md:grid-cols-2 md:gap-5">
    <StatCard label="总旅行次数" :value="stats!.totalTrips" unit="次旅行" gradient="..." />
    <StatCard label="已去过地点数" :value="stats!.uniquePlaces" unit="个地点" gradient="..." />
  </div>
</div>
```

From apps/web/src/views/StatisticsPageView.vue (skeleton state，line 155-165):
```html
<div class="grid grid-cols-1 gap-4 md:grid-cols-2 md:gap-5">
  <div v-for="index in 2" :key="index" ...>
    <!-- shimmer divs -->
  </div>
</div>
```

From apps/web/src/views/StatisticsPageView.spec.ts (mock 返回值):
```typescript
resolveFirst({ totalTrips: 1, uniquePlaces: 1 })
resolveSecond({ totalTrips: 2, uniquePlaces: 2 })
// 断言: '2 次旅行 · 2 个地点'
```

From apps/web/src/stores/stats.spec.ts (mock 返回值):
```typescript
fetchStatsMock.mockResolvedValueOnce({ totalTrips: 4, uniquePlaces: 3 })
// 断言: expect(statsStore.stats).toEqual({ totalTrips: 4, uniquePlaces: 3 })
```

StatCard component (现有 props):
```typescript
interface Props {
  label: string
  value: number
  unit: string
  gradient: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 StatisticsPageView 为三指标布局</name>
  <files>apps/web/src/views/StatisticsPageView.vue</files>
  <read_first>
    - apps/web/src/views/StatisticsPageView.vue — 完整文件（当前 populated state 只有 2 张卡片，skeleton 只有 2 个占位）
    - apps/web/src/components/statistics/StatCard.vue — 确认 props 接口不变
  </read_first>
  <behavior>
    - populated state summary badge 改为「{{ stats!.totalTrips }} 次旅行 · {{ stats!.uniquePlaces }} 个地点 · {{ stats!.visitedCountries }} 个国家/地区」
    - populated state StatCard grid 改为 md:grid-cols-3（响应式：1 列 → 3 列）
    - 新增第三张 StatCard：label="已去过国家/地区数" :value="stats!.visitedCountries" unit="个国家/地区" gradient="linear-gradient(180deg,rgba(255,255,255,0.93),rgba(241,248,255,0.94))"
    - skeleton state 占位卡片数量从 2 改为 3（v-for="index in 3"）
    - header subtitle 更新，加入国家/地区统计的说明
  </behavior>
  <action>
    修改 apps/web/src/views/StatisticsPageView.vue，以下 3 处变更：

    **变更 1: skeleton 占位卡片数量** — 找到 `v-for="index in 2"`（约 line 157），改为：
    ```html
    v-for="index in 3"
    ```

    **变更 2: header subtitle** — 找到 subtitle 文案（约 line 118-119），将：
    ```
    这里汇总了你的旅行数据——总旅行次数统计每一次独立去访，已去过地点数则对同一地点只计一次。
    ```
    改为：
    ```
    这里汇总了你的旅行数据——总旅行次数统计每一次独立去访，已去过地点数和国家/地区数则分别按地点和国家去重。
    ```

    **变更 3: populated state** — 找到 `data-state="populated"` 区块（约 line 246-277），做以下修改：

    3a. summary badge 文案改为：
    ```html
    {{ stats!.totalTrips }} 次旅行 · {{ stats!.uniquePlaces }} 个地点 · {{ stats!.visitedCountries }} 个国家/地区
    ```

    3b. summary badge 下方说明文案改为：
    ```html
    总旅行次数统计每一次独立去访，已去过地点数和国家/地区数则分别按地点和国家去重。当前支持覆盖 {{ stats!.totalSupportedCountries }} 个国家/地区。
    ```

    3c. StatCard grid 的 class 从 `md:grid-cols-2` 改为 `md:grid-cols-3`：
    ```html
    <div class="grid grid-cols-1 gap-4 md:grid-cols-3 md:gap-5">
    ```

    3d. 在第二张 StatCard 之后，`</div>` 之前，插入第三张 StatCard：
    ```html
    <StatCard
      label="已去过国家/地区数"
      :value="stats!.visitedCountries"
      unit="个国家/地区"
      gradient="linear-gradient(180deg,rgba(255,255,255,0.93),rgba(241,248,255,0.94))"
    />
    ```
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/views/StatisticsPageView.vue 包含 `stats!.visitedCountries`
    - apps/web/src/views/StatisticsPageView.vue 包含 `stats!.totalSupportedCountries`
    - apps/web/src/views/StatisticsPageView.vue 包含 `已去过国家/地区数`
    - apps/web/src/views/StatisticsPageView.vue 包含 `md:grid-cols-3`
    - apps/web/src/views/StatisticsPageView.vue 包含 `v-for="index in 3"`
    - apps/web/src/views/StatisticsPageView.vue 包含 `个国家/地区`（summary badge）
    - pnpm --filter @trip-map/web typecheck 退出码为 0
  </acceptance_criteria>
  <done>StatisticsPageView 渲染三张 StatCard，summary badge 包含三指标，skeleton 有 3 个占位卡片，类型检查通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新前端测试 mock 数据和断言</name>
  <files>
    apps/web/src/views/StatisticsPageView.spec.ts,
    apps/web/src/stores/stats.spec.ts
  </files>
  <read_first>
    - apps/web/src/views/StatisticsPageView.spec.ts — 完整文件（当前 mock 只返回 totalTrips + uniquePlaces）
    - apps/web/src/stores/stats.spec.ts — 完整文件（当前 mock 和断言只包含 2 个字段）
    - apps/web/src/views/StatisticsPageView.vue — 变更后的 populated state（确认新的 summary badge 文案）
  </read_first>
  <behavior>
    - 所有 fetchStatsMock 返回值新增 visitedCountries 和 totalSupportedCountries
    - 断言更新为三指标格式
    - 新增测试：multi-visit same place 时 visitedCountries 不变
    - 新增测试：多国多地点时 visitedCountries 正确
  </behavior>
  <action>
    **StatisticsPageView.spec.ts** — 做以下修改：

    1. 在 re-fetches 测试中，更新两处 mock 返回值（约 line 141 和 line 147）：
    ```typescript
    resolveFirst({ totalTrips: 1, uniquePlaces: 1, visitedCountries: 1, totalSupportedCountries: 22 })
    ```
    ```typescript
    resolveSecond({ totalTrips: 2, uniquePlaces: 2, visitedCountries: 2, totalSupportedCountries: 22 })
    ```

    2. 更新断言文案（约 line 151）：
    ```typescript
    expect(wrapper.get('[data-state="populated"]').text()).toContain('2 次旅行 · 2 个地点 · 2 个国家/地区')
    ```

    3. 在 describe 块末尾追加 2 个新测试：

    ```typescript
    it('shows visitedCountries in populated state without inflating for multi-visit same place', async () => {
      let resolveStats!: (value: { totalTrips: number; uniquePlaces: number; visitedCountries: number; totalSupportedCountries: number }) => void

      fetchStatsMock.mockImplementationOnce(
        () =>
          new Promise((resolve) => {
            resolveStats = resolve
          }),
      )

      const { wrapper } = mountStatisticsPage(({ authSessionStore }) => {
        authSessionStore.status = 'authenticated'
        authSessionStore.currentUser = makeUser()
      })

      // 3 trips to Beijing: totalTrips=3, uniquePlaces=1, visitedCountries=1
      resolveStats({ totalTrips: 3, uniquePlaces: 1, visitedCountries: 1, totalSupportedCountries: 22 })
      await flushPromises()
      await nextTick()

      const populated = wrapper.get('[data-state="populated"]')
      expect(populated.text()).toContain('3 次旅行')
      expect(populated.text()).toContain('1 个地点')
      expect(populated.text()).toContain('1 个国家/地区')
    })

    it('shows totalSupportedCountries in the description text', async () => {
      fetchStatsMock.mockResolvedValueOnce({
        totalTrips: 5,
        uniquePlaces: 4,
        visitedCountries: 3,
        totalSupportedCountries: 22,
      })

      const { wrapper } = mountStatisticsPage(({ authSessionStore }) => {
        authSessionStore.status = 'authenticated'
        authSessionStore.currentUser = makeUser()
      })

      await flushPromises()
      await nextTick()

      expect(wrapper.get('[data-state="populated"]').text()).toContain('当前支持覆盖 22 个国家/地区')
    })
    ```

    **stats.spec.ts** — 做以下修改：

    1. 更新所有 fetchStatsMock 返回值，新增 visitedCountries 和 totalSupportedCountries：
    - line 36: `{ totalTrips: 4, uniquePlaces: 3, visitedCountries: 2, totalSupportedCountries: 22 }`
    - line 40: `expect(statsStore.stats).toEqual({ totalTrips: 4, uniquePlaces: 3, visitedCountries: 2, totalSupportedCountries: 22 })`
    - line 75: `{ totalTrips: 8, uniquePlaces: 5, visitedCountries: 4, totalSupportedCountries: 22 }`
    - line 88: `statsStore.stats = { totalTrips: 2, uniquePlaces: 1, visitedCountries: 1, totalSupportedCountries: 22 }`
    - line 127: `{ totalTrips: 3, uniquePlaces: 2, visitedCountries: 2, totalSupportedCountries: 22 }`
    - line 133: `{ totalTrips: 5, uniquePlaces: 4, visitedCountries: 3, totalSupportedCountries: 22 }`
    - line 136: `expect(statsStore.stats).toEqual({ totalTrips: 5, uniquePlaces: 4, visitedCountries: 3, totalSupportedCountries: 22 })`

    2. 在 describe 块末尾追加 1 个新测试：

    ```typescript
    it('stores visitedCountries and totalSupportedCountries from the response', async () => {
      const authSessionStore = useAuthSessionStore()
      const statsStore = useStatsStore()

      authSessionStore.status = 'authenticated'
      authSessionStore.currentUser = makeUser()
      fetchStatsMock.mockResolvedValueOnce({
        totalTrips: 6,
        uniquePlaces: 4,
        visitedCountries: 3,
        totalSupportedCountries: 22,
      })

      await statsStore.fetchStatsData()

      expect(statsStore.stats?.visitedCountries).toBe(3)
      expect(statsStore.stats?.totalSupportedCountries).toBe(22)
    })
    ```
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/views/StatisticsPageView.spec.ts 包含 `visitedCountries`
    - apps/web/src/views/StatisticsPageView.spec.ts 包含 `totalSupportedCountries`
    - apps/web/src/views/StatisticsPageView.spec.ts 包含 `1 个国家/地区`
    - apps/web/src/views/StatisticsPageView.spec.ts 包含 `当前支持覆盖 22 个国家/地区`
    - apps/web/src/views/StatisticsPageView.spec.ts 包含 `does not inflate`
    - apps/web/src/stores/stats.spec.ts 包含 `visitedCountries`
    - apps/web/src/stores/stats.spec.ts 包含 `totalSupportedCountries`
    - pnpm --filter @trip-map/web test 退出码为 0，所有测试通过
    - pnpm --filter @trip-map/web typecheck 退出码为 0
  </acceptance_criteria>
  <done>前端测试全部更新，mock 数据含 4 个字段，去重语义测试通过，类型检查通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetchStatsMock → StatisticsPageView | mock 数据结构变更需与 contract 一致 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-G3 | Information Disclosure | StatisticsPageView visitedCountries display | accept | 仅展示聚合数值，不暴露具体国家列表或地点信息 |
</threat_model>

<verification>
```bash
# 1. 前端测试全部通过
pnpm --filter @trip-map/web test

# 2. 类型检查通过
pnpm --filter @trip-map/web typecheck

# 3. 验证三张 StatCard
grep -n "visitedCountries\|已去过国家" apps/web/src/views/StatisticsPageView.vue

# 4. 验证 summary badge 三指标
grep "个国家/地区" apps/web/src/views/StatisticsPageView.vue

# 5. 验证 skeleton 3 个占位
grep "index in 3" apps/web/src/views/StatisticsPageView.vue

# 6. 全量构建
pnpm build
```
</verification>

<success_criteria>
- StatisticsPageView populated state 渲染三张 StatCard
- summary badge 包含「X 次旅行 · Y 个地点 · Z 个国家/地区」
- skeleton 有 3 个占位卡片
- 所有前端 mock 数据包含 visitedCountries 和 totalSupportedCountries
- pnpm --filter @trip-map/web test 全部通过
- pnpm --filter @trip-map/web typecheck 通过
</success_criteria>

<output>
完成后创建 .planning/phases/30-travel-statistics-and-completion-overview/30-05-SUMMARY.md
</output>

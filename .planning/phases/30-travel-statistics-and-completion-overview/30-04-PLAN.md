---
phase: 30-travel-statistics-and-completion-overview
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/stats.ts
  - apps/server/src/modules/records/records.repository.ts
  - apps/server/src/modules/records/records.service.spec.ts
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
gap_closure: true

must_haves:
  truths:
    - "TravelStatsResponse 包含 visitedCountries 和 totalSupportedCountries 字段"
    - "getTravelStats 返回的 visitedCountries 基于 parentLabel 第一段去重计数"
    - "同一国家多个 admin1 只计一次 visitedCountries"
    - "同一地点多次旅行不放大 visitedCountries"
    - "contracts build 通过"
    - "后端测试全部通过"
  artifacts:
    - path: "packages/contracts/src/stats.ts"
      provides: "TravelStatsResponse 接口含 visitedCountries + totalSupportedCountries"
      contains: "visitedCountries"
    - path: "apps/server/src/modules/records/records.repository.ts"
      provides: "getTravelStats 返回含 visitedCountries + totalSupportedCountries"
      contains: "visitedCountries"
    - path: "apps/server/src/modules/records/records.service.spec.ts"
      provides: "国家去重和多地点同国聚合测试"
      contains: "visitedCountries"
  key_links:
    - from: "apps/server/src/modules/records/records.repository.ts"
      to: "packages/contracts/src/stats.ts"
      via: "import TravelStatsResponse"
      pattern: "TravelStatsResponse"
    - from: "apps/server/src/modules/records/records.service.spec.ts"
      to: "apps/server/src/modules/records/records.repository.ts"
      via: "getTravelStats mock"
      pattern: "getTravelStats"
---

<objective>
扩展 TravelStatsResponse 合约，新增 visitedCountries（已去过国家/地区数）和 totalSupportedCountries（当前支持的国家/地区总数），并在后端 repository 层实现基于 parentLabel 的国家去重聚合。

Purpose: 关闭 VERIFICATION.md Gap 1（缺少已去过国家/地区数）和 Gap 2（缺少完成度上下文），满足 STAT-01（已去过国家/地区数）和 STAT-02（完成度口径与支持覆盖范围一致）。
Output: 扩展的 contracts/stats.ts、更新的 repository 查询、新增的后端测试用例。
</objective>

<execution_context>
@/Users/huangjingping/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/huangjingping/i/trip-map/.planning/PROJECT.md
@/Users/huangjingping/i/trip-map/.planning/ROADMAP.md
@/Users/huangjingping/i/trip-map/.planning/phases/30-travel-statistics-and-completion-overview/30-CONTEXT.md
@/Users/huangjingping/i/trip-map/.planning/phases/30-travel-statistics-and-completion-overview/30-VERIFICATION.md

<interfaces>
<!-- 执行器需要的现有接口 -->

From packages/contracts/src/stats.ts (当前状态):
```typescript
export interface TravelStatsResponse {
  totalTrips: number
  uniquePlaces: number
}
```

From packages/contracts/src/generated/geometry-manifest.generated.ts:
```typescript
// 21 个海外国家，每个有 iso2, labelEn, labelZh, regionKey, defaultTypeLabel
export const SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES = [
  { iso2: "JP", labelZh: "日本", ... },
  { iso2: "KR", labelZh: "韩国", ... },
  // ... 共 21 个
] as const
```

From apps/server/src/modules/records/records.repository.ts (当前 getTravelStats):
```typescript
async getTravelStats(userId: string): Promise<TravelStatsResponse> {
  const [totalTrips, uniquePlaceRecords] = await Promise.all([
    this.prisma.userTravelRecord.count({ where: { userId } }),
    this.prisma.userTravelRecord.findMany({
      where: { userId },
      select: { placeId: true },
      distinct: ['placeId'],
    }),
  ])
  return { totalTrips, uniquePlaces: uniquePlaceRecords.length }
}
```

From apps/server/src/modules/records/records.service.spec.ts (mock 工厂):
```typescript
function createRepositoryMock() {
  return {
    createSmokeRecord: vi.fn(),
    findAllTravelRecords: vi.fn(),
    createTravelRecord: vi.fn(),
    importTravelRecords: vi.fn(),
    deleteTravelRecordByPlaceId: vi.fn(),
    getTravelStats: vi.fn(),
  }
}
```

UserTravelRecord schema 关键字段:
```prisma
model UserTravelRecord {
  parentLabel    String?   // "中国" 或 "日本" 或 "中国 · 四川" 等
  placeId        String
  // ...
}
```

parentLabel 语义:
- 国内记录: "中国" 或 "中国 · {省份}"
- 海外记录: "{国家中文名}" 如 "日本"、"美国"
- 提取国家: split(" · ")[0] 即可得到国家名
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 TravelStatsResponse 合约 + build</name>
  <files>packages/contracts/src/stats.ts</files>
  <read_first>
    - packages/contracts/src/stats.ts — 当前接口定义（2 个字段）
    - packages/contracts/src/index.ts — re-export 已存在，无需修改
    - packages/contracts/src/generated/geometry-manifest.generated.ts:8579-8727 — SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES 结构和数量（21 个海外国家）
  </read_first>
  <behavior>
    - TravelStatsResponse 新增 visitedCountries: number 和 totalSupportedCountries: number
    - visitedCountries 表示用户去过的不同国家/地区数
    - totalSupportedCountries 表示当前系统支持的国家/地区总数（21 海外 + 1 中国 = 22）
    - contracts build 通过
  </behavior>
  <action>
    修改 packages/contracts/src/stats.ts，将接口扩展为：
    ```typescript
    export interface TravelStatsResponse {
      totalTrips: number
      uniquePlaces: number
      visitedCountries: number
      totalSupportedCountries: number
    }
    ```

    执行 contracts build 确认通过：
    ```bash
    pnpm --filter @trip-map/contracts build
    ```
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/contracts build</automated>
  </verify>
  <acceptance_criteria>
    - packages/contracts/src/stats.ts 包含 `visitedCountries: number`
    - packages/contracts/src/stats.ts 包含 `totalSupportedCountries: number`
    - packages/contracts/src/stats.ts 包含 `totalTrips: number`
    - packages/contracts/src/stats.ts 包含 `uniquePlaces: number`
    - pnpm --filter @trip-map/contracts build 退出码为 0
  </acceptance_criteria>
  <done>TravelStatsResponse 包含 4 个字段，contracts build 通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 后端 repository 国家聚合 + 测试</name>
  <files>
    apps/server/src/modules/records/records.repository.ts,
    apps/server/src/modules/records/records.service.spec.ts
  </files>
  <read_first>
    - apps/server/src/modules/records/records.repository.ts — 当前 getTravelStats 方法，import 列表，Prisma 查询模式
    - apps/server/src/modules/records/records.service.spec.ts — createRepositoryMock 工厂，现有 getStats 测试
    - packages/contracts/src/generated/geometry-manifest.generated.ts:8579-8727 — SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES 的 labelZh 值
  </read_first>
  <behavior>
    - getTravelStats 新增第三条查询：findMany distinct parentLabel，提取第一段（split(" · ")[0]）去重计数
    - totalSupportedCountries = SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES.length + 1（中国）
    - parentLabel 为 null 时归入 "未知" 桶
    - 同一国家（如 "日本"）多个 admin1（如 "东京都"、"大阪府"）只计 1 次 visitedCountries
    - 同一地点多次旅行不放大 visitedCountries
    - 测试 1: 基本委托返回 4 个字段
    - 测试 2: 3 条北京记录 → visitedCountries=1（"中国"）
    - 测试 3: 北京 + 东京 + 加州 → visitedCountries=3（"中国"、"日本"、"美国"）
    - 测试 4: 北京 + 上海 + 东京 → visitedCountries=2（"中国"、"日本"），验证同国多地点不重复
  </behavior>
  <action>
    **records.repository.ts** — 在 import 列表中追加：
    ```typescript
    import { SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES } from '@trip-map/contracts'
    ```

    将 getTravelStats 方法替换为（使用三条并发查询）：
    ```typescript
    async getTravelStats(userId: string): Promise<TravelStatsResponse> {
      const totalSupportedCountries = SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES.length + 1

      const [totalTrips, uniquePlaceRecords, parentLabelRecords] = await Promise.all([
        this.prisma.userTravelRecord.count({ where: { userId } }),
        this.prisma.userTravelRecord.findMany({
          where: { userId },
          select: { placeId: true },
          distinct: ['placeId'],
        }),
        this.prisma.userTravelRecord.findMany({
          where: { userId },
          select: { parentLabel: true },
          distinct: ['parentLabel'],
        }),
      ])

      const visitedCountries = new Set(
        parentLabelRecords.map((r) => {
          const label = r.parentLabel ?? '未知'
          const separatorIndex = label.indexOf(' · ')
          return separatorIndex === -1 ? label : label.slice(0, separatorIndex)
        }),
      ).size

      return {
        totalTrips,
        uniquePlaces: uniquePlaceRecords.length,
        visitedCountries,
        totalSupportedCountries,
      }
    }
    ```

    **records.service.spec.ts** — 在现有 `RecordsService.getStats` describe 块内，追加 3 个新测试用例（在现有 2 个 it 块之后）：

    ```typescript
    it('returns visitedCountries based on distinct parentLabel country extraction', async () => {
      const repository = createRepositoryMock()
      const service = new RecordsService(repository as never)

      repository.getTravelStats.mockResolvedValueOnce({
        totalTrips: 3,
        uniquePlaces: 3,
        visitedCountries: 3,
        totalSupportedCountries: 22,
      })

      const result = await service.getStats('user-1')

      expect(result.visitedCountries).toBe(3)
      expect(result.totalSupportedCountries).toBe(22)
    })

    it('does not inflate visitedCountries when same country has multiple admin1 places', async () => {
      const repository = createRepositoryMock()
      const service = new RecordsService(repository as never)

      // 北京 + 上海 + 东京 → visitedCountries=2（中国、日本）
      repository.getTravelStats.mockResolvedValueOnce({
        totalTrips: 3,
        uniquePlaces: 3,
        visitedCountries: 2,
        totalSupportedCountries: 22,
      })

      const result = await service.getStats('user-1')

      expect(result.visitedCountries).toBe(2)
      expect(result.uniquePlaces).toBe(3)
    })

    it('does not inflate visitedCountries for multi-visit same place', async () => {
      const repository = createRepositoryMock()
      const service = new RecordsService(repository as never)

      // 3 次去北京 → totalTrips=3, uniquePlaces=1, visitedCountries=1
      repository.getTravelStats.mockResolvedValueOnce({
        totalTrips: 3,
        uniquePlaces: 1,
        visitedCountries: 1,
        totalSupportedCountries: 22,
      })

      const result = await service.getStats('user-1')

      expect(result.totalTrips).toBe(3)
      expect(result.uniquePlaces).toBe(1)
      expect(result.visitedCountries).toBe(1)
    })
    ```
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server test</automated>
  </verify>
  <acceptance_criteria>
    - apps/server/src/modules/records/records.repository.ts 包含 `SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES`
    - apps/server/src/modules/records/records.repository.ts 包含 `visitedCountries`
    - apps/server/src/modules/records/records.repository.ts 包含 `totalSupportedCountries`
    - apps/server/src/modules/records/records.repository.ts 包含 `distinct: ['parentLabel']`
    - apps/server/src/modules/records/records.repository.ts 包含 `indexOf(' · ')`
    - apps/server/src/modules/records/records.service.spec.ts 包含 `visitedCountries`
    - apps/server/src/modules/records/records.service.spec.ts 包含 `totalSupportedCountries`
    - apps/server/src/modules/records/records.service.spec.ts 包含 `does not inflate visitedCountries`
    - pnpm --filter @trip-map/server test 退出码为 0，所有测试通过
  </acceptance_criteria>
  <done>getTravelStats 返回 4 个字段，国家去重聚合正确，后端测试全部通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma query → parentLabel extraction | parentLabel 来自权威 catalog，split(" · ") 逻辑简单确定 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-G1 | Information Disclosure | getTravelStats parentLabel query | accept | 仅返回聚合数值（count），不返回任何 PII 或具体地点信息 |
| T-30-G2 | Tampering | SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES import | accept | 来自编译时生成的常量，不可被运行时修改 |
</threat_model>

<verification>
```bash
# 1. contracts build 通过
pnpm --filter @trip-map/contracts build

# 2. 后端测试全部通过
pnpm --filter @trip-map/server test

# 3. 验证 visitedCountries 在 repository 中
grep -n "visitedCountries\|totalSupportedCountries\|parentLabel" apps/server/src/modules/records/records.repository.ts

# 4. 验证测试覆盖国家去重
grep -n "visitedCountries\|inflate" apps/server/src/modules/records/records.service.spec.ts
```
</verification>

<success_criteria>
- TravelStatsResponse 包含 visitedCountries 和 totalSupportedCountries
- getTravelStats 基于 parentLabel 第一段去重计算 visitedCountries
- totalSupportedCountries = 22（21 海外 + 1 中国）
- pnpm --filter @trip-map/contracts build 通过
- pnpm --filter @trip-map/server test 全部通过
</success_criteria>

<output>
完成后创建 .planning/phases/30-travel-statistics-and-completion-overview/30-04-SUMMARY.md
</output>

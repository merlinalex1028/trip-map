---
phase: 19-tailwind-token
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/vite.config.ts
  - apps/web/src/tailwind-token.spec.ts
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "apps/web 独立声明 Tailwind v4 与 @tailwindcss/vite，前端包本身就能提供 Tailwind 工具类编译能力。"
    - "Vite 在 web 包内按 `tailwindcss(), vue()` 顺序加载插件，且现有 alias/proxy 配置不被改坏。"
    - "静态契约测试能直接证明依赖范围与插件接入已经落地。"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Tailwind v4 and @tailwindcss/vite scoped to @trip-map/web"
    - path: "pnpm-lock.yaml"
      provides: "Workspace lockfile entries for Tailwind dependencies"
    - path: "apps/web/vite.config.ts"
      provides: "Vite plugin chain with tailwindcss() before vue()"
    - path: "apps/web/src/tailwind-token.spec.ts"
      provides: "Static contract coverage for Tailwind dependency scope and Vite plugin wiring"
  key_links:
    - from: "apps/web/package.json"
      to: "pnpm-lock.yaml"
      via: "pnpm workspace install"
      pattern: "tailwindcss|@tailwindcss/vite"
    - from: "apps/web/vite.config.ts"
      to: "apps/web/package.json"
      via: "import '@tailwindcss/vite' and plugins array"
      pattern: "import tailwindcss from '@tailwindcss/vite'|plugins: \\[tailwindcss\\(\\), vue\\(\\)\\]"
---

<objective>
锁定 `apps/web` 的 Tailwind v4 依赖范围、Vite 插件接入方式和静态契约测试，先把工具链入口做成可验证的基础设施。

Purpose: 满足 `INFRA-01`，并为后续 `style.css`、字体基线和 App shell Tailwind 样板提供稳定前提。
Output: `apps/web/package.json` / `pnpm-lock.yaml` 的依赖更新、`vite.config.ts` 插件接入、`tailwind-token.spec.ts` 的静态契约测试。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-tailwind-token/19-CONTEXT.md
@.planning/phases/19-tailwind-token/19-RESEARCH.md
@.planning/phases/19-tailwind-token/19-VALIDATION.md
@apps/web/package.json
@apps/web/vite.config.ts
@apps/web/vitest.config.ts

<interfaces>
Current `apps/web/vite.config.ts` must keep everything except the plugin line intact:

```ts
export default defineConfig({
  plugins: [vue()],
  resolve: { alias: { /* existing aliases */ } },
  server: { proxy: { /* existing proxy */ }, fs: { allow: [repoRoot] } }
})
```

Current `apps/web/package.json` already defines the package-local scripts the contract test should keep using:

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "test": "vitest run",
    "typecheck": "vue-tsc --noEmit"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 锁定 Tailwind v4 依赖范围与 Vite 插件接入</name>
  <read_first>
.planning/phases/19-tailwind-token/19-CONTEXT.md
.planning/phases/19-tailwind-token/19-RESEARCH.md
.planning/phases/19-tailwind-token/19-VALIDATION.md
apps/web/package.json
apps/web/vite.config.ts
apps/web/vitest.config.ts
  </read_first>
  <files>apps/web/src/tailwind-token.spec.ts, apps/web/package.json, pnpm-lock.yaml, apps/web/vite.config.ts</files>
  <behavior>
    - Test 1: `apps/web/package.json` 只在 `devDependencies` 中新增 `"tailwindcss": "^4.2.2"` 和 `"@tailwindcss/vite": "^4.2.2"`。
    - Test 2: `apps/web/vite.config.ts` 包含 `import tailwindcss from '@tailwindcss/vite'`，并且 `plugins` 精确为 `plugins: [tailwindcss(), vue()]`。
    - Test 3: 静态契约测试运行 `pnpm --filter @trip-map/web exec vitest run src/tailwind-token.spec.ts` 时退出码为 0。
  </behavior>
  <action>先创建 `apps/web/src/tailwind-token.spec.ts`，直接读取 `apps/web/package.json` 与 `apps/web/vite.config.ts` 源码字符串，断言 `tailwindcss`、`@tailwindcss/vite` 和 `plugins: [tailwindcss(), vue()]` 这三组精确模式。然后执行 `pnpm --filter @trip-map/web add -D tailwindcss@^4.2.2 @tailwindcss/vite@^4.2.2`，同步更新 `pnpm-lock.yaml`，在 `apps/web/vite.config.ts` 增加 `import tailwindcss from '@tailwindcss/vite'` 并把插件数组改成 `plugins: [tailwindcss(), vue()]`。保留 `resolve.alias`、`server.proxy`、`server.fs.allow` 的现有实现不动，避免把本计划扩展成 Vite 重构。此任务实现的是 D-03 与 `INFRA-01` 的工具链入口，不要创建 `tailwind.config.*`、`postcss.config.*` 或任何 Phase 20 组件迁移文件。</action>
  <acceptance_criteria>
    - `apps/web/package.json` 包含精确字符串 `"tailwindcss": "^4.2.2"`。
    - `apps/web/package.json` 包含精确字符串 `"@tailwindcss/vite": "^4.2.2"`。
    - `apps/web/vite.config.ts` 包含精确字符串 `import tailwindcss from '@tailwindcss/vite'`。
    - `apps/web/vite.config.ts` 包含精确字符串 `plugins: [tailwindcss(), vue()]`。
    - `apps/web/src/tailwind-token.spec.ts` 包含 `describe('tailwind token contracts'`，并断言 `tailwindcss`、`@tailwindcss/vite`、`plugins: [tailwindcss(), vue()]`。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web exec vitest run src/tailwind-token.spec.ts</automated>
  </verify>
  <done>`INFRA-01` 达成：Tailwind v4 与 `@tailwindcss/vite` 已在 `apps/web` 范围接入，且有可重复运行的静态契约测试锁住依赖范围和 Vite 插件顺序。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace manifest → web build toolchain | 依赖清单和锁文件决定前端构建链实际加载的插件与版本 |
| vite config → compiled frontend assets | Vite 插件顺序错误会直接改变 CSS 处理结果 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | T | apps/web/package.json / pnpm-lock.yaml | mitigate | 依赖只安装到 `@trip-map/web`，并用 `tailwind-token.spec.ts` 断言 `tailwindcss` 与 `@tailwindcss/vite` 的包级范围和版本字符串。 |
| T-19-02 | D | apps/web/vite.config.ts | mitigate | 只新增 `tailwindcss()` 插件并保持 `plugins: [tailwindcss(), vue()]`，其余 alias/proxy/fs 配置不动；用 targeted Vitest 静态契约测试锁定插件顺序。 |
</threat_model>

<verification>
运行 `pnpm --filter @trip-map/web exec vitest run src/tailwind-token.spec.ts`，确认 Tailwind 依赖范围与 Vite 插件接入断言全部通过。
</verification>

<success_criteria>
- `apps/web/package.json`、`pnpm-lock.yaml`、`apps/web/vite.config.ts`、`apps/web/src/tailwind-token.spec.ts` 已更新。
- `tailwind-token.spec.ts` 能证明 Tailwind v4 依赖与 Vite 插件只落在 `apps/web`。
- 本计划未引入 `tailwind.config.*`、`postcss.config.*` 或 Phase 20 组件迁移改动。
</success_criteria>

<output>
After completion, create `.planning/phases/19-tailwind-token/19-01-SUMMARY.md`
</output>

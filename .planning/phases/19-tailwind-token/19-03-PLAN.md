---
phase: 19-tailwind-token
plan: 03
type: execute
wave: 3
depends_on:
  - 19-02
files_modified:
  - .planning/phases/19-tailwind-token/19-03-SUMMARY.md
autonomous: false
requirements:
  - INFRA-04
must_haves:
  truths:
    - "Tailwind preflight 接入后，Leaflet 缩放按钮、归因链接、图层控件与 popup 在真实浏览器里没有样式崩坏。"
    - "Phase 19 的自动化质量门已经覆盖静态契约、App shell smoke、typecheck 和生产构建。"
  artifacts:
    - path: "apps/web/src/tailwind-token.spec.ts"
      provides: "Static verification for style/main/Leaflet import order"
    - path: "apps/web/src/App.spec.ts"
      provides: "App shell smoke verification"
    - path: ".planning/phases/19-tailwind-token/19-03-SUMMARY.md"
      provides: "Recorded browser smoke verdict and any failure checklist for INFRA-04 evidence"
  key_links:
    - from: "apps/web/src/style.css"
      to: "leaflet/dist/leaflet.css"
      via: "preflight override order"
      pattern: "@import 'leaflet/dist/leaflet.css';"
    - from: "apps/web/src/App.vue"
      to: "apps/web/src/components/LeafletMapStage.vue"
      via: "browser-rendered map shell"
      pattern: "data-region=\"map-shell\"|data-region=\"map-stage\""
    - from: "checkpoint:human-verify"
      to: ".planning/phases/19-tailwind-token/19-03-SUMMARY.md"
      via: "recorded approved/failure evidence"
      pattern: "approved|failure|Leaflet"
---

<objective>
执行 Phase 19 的最终质量门，并通过真实浏览器冒烟确认 Leaflet 没有受到 Tailwind preflight 破坏。

Purpose: 独立完成 `INFRA-04`，把 automated gate 和人工浏览器 gate 分开，避免把“Leaflet 不坏”停留在静态推断。
Output: 绿色自动化门禁结果，以及写入 `19-03-SUMMARY.md` 的明确人工浏览器验收结论。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-tailwind-token/19-CONTEXT.md
@.planning/phases/19-tailwind-token/19-VALIDATION.md
@apps/web/package.json
@apps/web/src/style.css
@apps/web/src/App.vue
@apps/web/src/App.spec.ts
@apps/web/src/tailwind-token.spec.ts
@apps/web/src/components/LeafletMapStage.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 跑完 Phase 19 自动化质量门</name>
  <read_first>
.planning/phases/19-tailwind-token/19-VALIDATION.md
apps/web/package.json
apps/web/src/App.spec.ts
apps/web/src/tailwind-token.spec.ts
  </read_first>
  <files></files>
  <action>严格按 `19-VALIDATION.md` 的快路径执行自动化门禁：先跑 `pnpm --filter @trip-map/web exec vitest run src/App.spec.ts src/tailwind-token.spec.ts`，再跑 `pnpm --filter @trip-map/web exec vue-tsc --noEmit`，最后跑 `pnpm --filter @trip-map/web build`。任何一步失败都必须先修复再继续，不要跳过 build，也不要把 watch 模式带入 phase 执行。</action>
  <acceptance_criteria>
    - `pnpm --filter @trip-map/web exec vitest run src/App.spec.ts src/tailwind-token.spec.ts` 退出码为 0。
    - `pnpm --filter @trip-map/web exec vue-tsc --noEmit` 退出码为 0。
    - `pnpm --filter @trip-map/web build` 退出码为 0。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web exec vitest run src/App.spec.ts src/tailwind-token.spec.ts && pnpm --filter @trip-map/web exec vue-tsc --noEmit && pnpm --filter @trip-map/web build</automated>
  </verify>
  <done>Phase 19 的自动化门禁已绿：静态契约、App shell smoke、typecheck 和构建都通过。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 浏览器冒烟确认 Leaflet preflight 兼容</name>
  <read_first>
.planning/phases/19-tailwind-token/19-CONTEXT.md
.planning/phases/19-tailwind-token/19-VALIDATION.md
apps/web/src/style.css
apps/web/src/styles/global.css
apps/web/src/App.vue
apps/web/src/components/LeafletMapStage.vue
  </read_first>
  <files>.planning/phases/19-tailwind-token/19-03-SUMMARY.md</files>
  <action>在 Task 1 全绿后，启动 `pnpm --filter @trip-map/web dev --host 127.0.0.1 --port 4173` 作为人工冒烟环境，引导用户只检查 Phase 19 范围内的三类结果：cream + Nunito 壳层基线、Leaflet 控件稳定性、popup 是否保持正常排版与锚定。用户给出结论后，必须把结果写入 `.planning/phases/19-tailwind-token/19-03-SUMMARY.md`：通过时记录明确的 `approved` 与检查日期；失败时记录逐条失败现象、涉及控件和复现步骤，不要用“看起来还行”之类模糊表述。</action>
  <what-built>Tailwind v4 已通过单一 CSS 入口接入 `apps/web`，App shell 已切到 cream + Nunito 基线，并保留 Leaflet SVG 兼容补丁。</what-built>
  <how-to-verify>
1. 启动 `pnpm --filter @trip-map/web dev --host 127.0.0.1 --port 4173`。
2. 打开 `http://127.0.0.1:4173/`。
3. 先看页面壳层：顶部栏与页面底色应位于 `#FAFAFA` / `#FFF5F5` 奶油白范围，`Travel Diary` / `旅记` 的英文字形应明显比旧 sans-serif 更圆润。
4. 再看 Leaflet 控件：缩放按钮保持正常边框与对齐，归因链接不被 reset 成杂乱纯文本，图层控件如果出现可正常展开且 label/checkbox 不错位。
5. 点击地图打开 popup，确认 popup 锚定、外框、内容排版没有因 Tailwind preflight 被挤压、裁切或错位。
6. 如果任何一项失败，记录具体现象后返回；全部通过则回复 `approved`。
  </how-to-verify>
  <acceptance_criteria>
    - 页面根壳层呈现奶油白背景，不是纯白硬底。
    - 顶部栏文案显示为 `Travel Diary` 与 `旅记`，且字形表现为 Nunito Variable 风格的圆润基线。
    - `.leaflet-control-zoom a` 保持正常控件外观，`+` / `-` 图形可见且未塌陷。
    - `.leaflet-control-attribution` 文本和链接保持可读、未错位、未被裁切。
    - `.leaflet-control-layers` 若可见，可展开且 checkbox / label 排版正常。
    - 打开的 popup 没有被 preflight 压扁、错位或出现异常边距。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web exec vitest run src/App.spec.ts src/tailwind-token.spec.ts && pnpm --filter @trip-map/web exec vue-tsc --noEmit && pnpm --filter @trip-map/web build</automated>
  </verify>
  <done>用户明确回复 `approved`，或提供具体失败项供后续 gap closure 处理；执行者已把结论写入 `.planning/phases/19-tailwind-token/19-03-SUMMARY.md`，只有收到通过结论且摘要落盘时，`INFRA-04` 才算满足。</done>
  <resume-signal>回复 `approved`，或逐条描述失败现象。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated checks → shipped frontend bundle | 静态测试/类型检查/构建只能覆盖文件与编译，不等于真实浏览器下第三方 CSS 完全稳定 |
| browser rendering → Leaflet controls/popup | Leaflet 控件与 popup 依赖第三方 CSS，在真实浏览器中才会暴露 preflight 副作用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | D | Leaflet controls / popup rendering | mitigate | 先跑完 Vitest + vue-tsc + build，再用真实浏览器检查 zoom controls、attribution、layers、popup；未通过不允许把 `INFRA-04` 标记完成。 |
| T-19-06 | R | Manual smoke evidence | mitigate | 把人工验收结果显式记录为 `approved` 或失败清单，避免“看起来没问题”但没有证据的状态。 |
</threat_model>

<verification>
自动化门禁全部为 0 退出码，随后完成一次真实浏览器人工冒烟，并把明确结论写入 `.planning/phases/19-tailwind-token/19-03-SUMMARY.md`。
</verification>

<success_criteria>
- `pnpm --filter @trip-map/web exec vitest run src/App.spec.ts src/tailwind-token.spec.ts`、`vue-tsc --noEmit`、`build` 全部通过。
- 用户在真实浏览器里确认 Leaflet 控件、归因链接、图层控件和 popup 没有因为 Tailwind preflight 退化。
- 若人工检查失败，问题被明确记录到 `.planning/phases/19-tailwind-token/19-03-SUMMARY.md`，而不是含糊跳过。
</success_criteria>

<output>
After completion, create `.planning/phases/19-tailwind-token/19-03-SUMMARY.md`
</output>

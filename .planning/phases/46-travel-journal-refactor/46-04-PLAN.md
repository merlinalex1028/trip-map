---
phase: 46-travel-journal-refactor
plan: 04
type: execute
wave: 3
depends_on:
  - 46-01
  - 46-03
files_modified:
  - apps/web/src/views/TimelinePageView.vue
  - apps/web/src/views/TimelinePageView.spec.ts
autonomous: true
requirements:
  - JOURNAL-01
  - JOURNAL-02
  - JOURNAL-04
  - JOURNAL-05
  - JOURNAL-06
must_haves:
  truths:
    - "The populated `/journal` page is a glowing vertical travel journal stream with star nodes and card flow."
    - "Restoring, empty, warning/error, anonymous, and populated states use the same Yume Kawaii journal surface language."
    - "Populated page header leaves upper-right breathing room and does not add stats pills, add buttons, favorite controls, return-map tools, or management actions."
    - "Mobile layout reserves a timeline node column so text and thumbnails do not overlap."
  artifacts:
    - path: "apps/web/src/views/TimelinePageView.vue"
      provides: "Route-level journal stream and state surfaces"
      contains: "data-journal-stream"
    - path: "apps/web/src/views/TimelinePageView.spec.ts"
      provides: "Page-level stream, state, and forbidden-affordance coverage"
      contains: "data-journal-line"
  key_links:
    - from: "TimelinePageView.vue"
      to: "TimelineVisitCard.vue"
      via: "`v-for` over `timelineEntries`"
      pattern: "TimelineVisitCard"
    - from: "TimelinePageView.vue"
      to: "/map"
      via: "empty/error state RouterLink only"
      pattern: "去世界足迹留下足迹"
    - from: "TimelinePageView.vue"
      to: "useMapUiStore"
      via: "warning/error panel from existing interaction notice"
      pattern: "interactionNotice"
---

<objective>
Complete the route-level travel journal surface with glowing vertical line, star nodes, responsive state panels, and final Phase 46 verification gates.

Purpose: This plan turns the existing page grid into the user-visible “旅途手账” experience while enforcing all excluded affordances.
Output: Updated `TimelinePageView.vue`, page tests, and final phase gates.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-travel-journal-refactor/46-CONTEXT.md
@.planning/phases/46-travel-journal-refactor/46-RESEARCH.md
@.planning/phases/46-travel-journal-refactor/46-PATTERNS.md
@.planning/phases/46-travel-journal-refactor/46-VALIDATION.md
@.planning/phases/46-travel-journal-refactor/46-UI-SPEC.md
@.planning/phases/43-landing/43-04-SUMMARY.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-05-SUMMARY.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-04-SUMMARY.md
@apps/web/src/views/TimelinePageView.vue
@apps/web/src/views/TimelinePageView.spec.ts
@apps/web/src/components/timeline/TimelineVisitCard.vue
@apps/web/src/stores/auth-session.ts
@apps/web/src/stores/map-points.ts
@apps/web/src/stores/map-ui.ts

<interfaces>
Existing view state inputs:
```ts
const { currentUser, status } = storeToRefs(authSessionStore)
const { timelineEntries } = storeToRefs(mapPointsStore)
```

Existing notice store available for warning/error state:
```ts
const mapUiStore = useMapUiStore()
const { interactionNotice } = storeToRefs(mapUiStore)
```

Required page data hooks:
```html
<div data-state="restoring" aria-live="polite">...</div>
<div data-state="anonymous">...</div>
<div data-state="empty">...</div>
<div data-state="error" role="status">...</div>
<div data-state="populated" data-journal-stream>...</div>
<span data-journal-line aria-hidden="true"></span>
<span data-journal-node aria-hidden="true"></span>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the glowing journal stream and responsive state panels</name>
  <files>apps/web/src/views/TimelinePageView.vue, apps/web/src/views/TimelinePageView.spec.ts</files>
  <read_first>
    - apps/web/src/views/TimelinePageView.vue
    - apps/web/src/views/TimelinePageView.spec.ts
    - apps/web/src/components/timeline/TimelineVisitCard.vue
    - apps/web/src/stores/map-ui.ts
    - .planning/phases/46-travel-journal-refactor/46-CONTEXT.md
    - .planning/phases/46-travel-journal-refactor/46-UI-SPEC.md
    - .planning/phases/46-travel-journal-refactor/46-VALIDATION.md
  </read_first>
  <behavior>
    - Test: populated state contains `[data-journal-stream]`, one `[data-journal-line]`, and one `[data-journal-node]` per record.
    - Test: restoring state contains skeleton elements aligned to the journal stream and has `aria-live="polite"`.
    - Test: warning/error panel appears when `useMapUiStore().interactionNotice = { tone: 'warning', message: '云端记录刷新失败，当前仍显示上次同步结果，请稍后重试。' }`, and contains exact UI-SPEC copy `旅途手账暂时加载失败，请稍后重试，或返回世界足迹确认旅行记录是否已同步。`.
    - Test: populated header text contains `旅途手账` and does not contain `返回世界足迹`, `添加新旅行`, `收藏`, `我的收藏`, or `一次旅行一张卡片`.
  </behavior>
  <action>Keep `TimelinePageView.vue` as the route composition surface and use direct import for `TimelineVisitCard` if the current `import.meta.glob` indirection is no longer needed. Treat `prd/v8.0/UI/旅途手帐.png` as visual authority for atmosphere, spacing, glowing vertical line, star nodes, card rhythm, and postcard placement per D-01 while excluding its add and favorite controls per D-02 and D-03. Remove the populated header right-side `返回世界足迹` link and the `一次旅行一张卡片` pill per D-05. Keep the authenticated user pill only if it already exists in the header; otherwise leave the upper-right area empty. Empty state may keep only the `/map` guidance path `去世界足迹留下足迹` per D-04. Replace the populated grid wrapper with a `data-journal-stream` layout using CSS grid tracks `grid-cols-[48px_minmax(0,1fr)]` on mobile and a wider desktop gutter. Render a single vertical `data-journal-line` decorative span and per-entry `data-journal-node` decorative spans with `aria-hidden="true"`. Use transform/opacity-only hover/entrance effects and add a scoped `@media (prefers-reduced-motion: reduce)` block that disables shimmer/float/entrance and hover transform. Add a non-blocking warning/error panel driven by `interactionNotice?.tone === 'warning'` with exact copy `旅途手账暂时加载失败，请稍后重试，或返回世界足迹确认旅行记录是否已同步。` and a `/map` RouterLink labeled `返回世界足迹` inside that panel only.</action>
  <acceptance_criteria>
    - `apps/web/src/views/TimelinePageView.vue` contains `data-journal-stream`.
    - `apps/web/src/views/TimelinePageView.vue` contains `data-journal-line`.
    - `apps/web/src/views/TimelinePageView.vue` contains `data-journal-node`.
    - `apps/web/src/views/TimelinePageView.vue` contains `prefers-reduced-motion: reduce`.
    - `apps/web/src/views/TimelinePageView.vue` contains `旅途手账暂时加载失败，请稍后重试，或返回世界足迹确认旅行记录是否已同步。`.
    - `rg -n "一次旅行一张卡片|添加新旅行|我的收藏|data-journal-add-trip|data-journal-favorite" apps/web/src/views/TimelinePageView.vue` returns no matches.
    - `apps/web/src/views/TimelinePageView.spec.ts` contains `data-journal-stream`.
    - `apps/web/src/views/TimelinePageView.spec.ts` contains `data-journal-node`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- TimelinePageView TimelineVisitCard</automated>
  </verify>
  <done>The page renders the glowing stream and all journal states without header action leakage or mobile-overlap-prone absolute nodes over text.</done>
</task>

<task type="auto">
  <name>Task 2: Run final Phase 46 focused gates and grep audits</name>
  <files>apps/web/src/views/TimelinePageView.spec.ts</files>
  <read_first>
    - apps/web/src/views/TimelinePageView.spec.ts
    - apps/web/src/components/timeline/TimelineVisitCard.spec.ts
    - apps/web/src/components/shell/AuthenticatedAppShell.spec.ts
    - .planning/phases/46-travel-journal-refactor/46-VALIDATION.md
  </read_first>
  <action>Run the final focused test and build gates. If a gate fails due to Phase 46 source changes, fix only the Phase 46-owned files listed in this plan set. Add a final page spec assertion that `wrapper.findAll('[data-journal-node]').length` equals the number of `[data-region="timeline-entry"]` cards. Add a final absence assertion in `TimelinePageView.spec.ts` for `wrapper.text()` not containing `旅行照片`, `上传`, `添加新旅行`, `我的收藏`, and `收藏`.</action>
  <acceptance_criteria>
    - `apps/web/src/views/TimelinePageView.spec.ts` contains `findAll('[data-journal-node]')`.
    - `apps/web/src/views/TimelinePageView.spec.ts` contains `旅行照片`.
    - `apps/web/src/views/TimelinePageView.spec.ts` contains `上传`.
    - `pnpm --filter @trip-map/web test -- router TimelinePageView TimelineVisitCard timeline ShellSidebar AuthenticatedAppShell` exits 0.
    - `pnpm --filter @trip-map/web build` exits 0.
    - `rg -n "添加新旅行|我的收藏|data-card-favorite|data-journal-add-trip|data-journal-favorite|旅行照片|上传" apps/web/src/views/TimelinePageView.vue apps/web/src/components/timeline apps/web/src/components/shell/ShellSidebar.vue` returns no matches outside test files.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- router TimelinePageView TimelineVisitCard timeline ShellSidebar AuthenticatedAppShell</automated>
    <automated>pnpm --filter @trip-map/web build</automated>
  </verify>
  <done>Focused Phase 46 tests, build, and forbidden-affordance grep audit all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated store -> `/journal` route view | Private travel records render in page state and card stream. |
| warning/error notice -> journal view | Existing app warning notices become visible journal error state copy. |
| responsive CSS -> mobile browser | Decorative nodes/line must not cover private text or controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-14 | Information Disclosure | `TimelinePageView.vue` | mitigate | Keep route auth from Plan 46-01 and consume only `mapPointsStore.timelineEntries` for authenticated users. |
| T-46-15 | Denial of Service | journal motion CSS | mitigate | Use transform/opacity only and disable nonessential motion under `prefers-reduced-motion: reduce`. |
| T-46-16 | Spoofing | page header/actions | mitigate | Remove populated header return-map/add/favorite/tool actions; keep `/map` navigation only in empty/error recovery states. |
| T-46-17 | Information Disclosure | warning/error state | accept | The error copy is generic and does not expose record details, account IDs, or backend error payloads. |
| T-46-18 | Tampering | final grep/test gates | mitigate | Run focused tests and grep audits proving excluded add/favorite/upload surfaces are absent from Phase 46 source surfaces. |
</threat_model>

<verification>
Final phase gate:

```bash
pnpm --filter @trip-map/web test -- router TimelinePageView TimelineVisitCard timeline ShellSidebar AuthenticatedAppShell
pnpm --filter @trip-map/web build
```

Forbidden-affordance audit:

```bash
rg -n "添加新旅行|我的收藏|data-card-favorite|data-journal-add-trip|data-journal-favorite|旅行照片|上传" apps/web/src/views/TimelinePageView.vue apps/web/src/components/timeline apps/web/src/components/shell/ShellSidebar.vue
```
</verification>

<success_criteria>
- `/journal` populated state renders `data-journal-stream`, `data-journal-line`, and matching `data-journal-node` count.
- Restoring, empty, anonymous, and warning/error states use Yume Kawaii panels and exact contracted copy.
- Header upper-right has no new action or stats controls; `/map` navigation appears only in empty/error recovery states.
- Focused test suite and build pass.
- Forbidden add/favorite/collection/upload/photo surfaces are absent from production journal and shell files.
</success_criteria>

<output>
After completion, create `.planning/phases/46-travel-journal-refactor/46-04-SUMMARY.md`.
</output>

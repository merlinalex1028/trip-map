---
phase: 38
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - apps/web/src/components/timeline/ConfirmDialog.vue
  - apps/web/src/components/timeline/ConfirmDialog.spec.ts
  - apps/web/src/components/timeline/TimelineVisitCard.vue
  - apps/web/src/components/timeline/TimelineVisitCard.spec.ts
autonomous: true
requirements: [DEL-01, DEL-02, DEL-03, EDIT-01, EDIT-02, EDIT-03]
must_haves:
  truths:
    - "用户可以在时间轴卡片上点击编辑按钮进入编辑模式，编辑完成后自动退出"
    - "用户可以在时间轴卡片上点击删除按钮，系统展示确认弹窗"
    - "确认弹窗需用户确认后才执行删除操作"
    - "删除该地点最后一条记录时，确认弹窗显示额外警告（取消点亮状态）"
    - "确认弹窗支持 ESC 键关闭、点击 backdrop 关闭"
    - "编辑成功后卡片自动排序到新日期位置"
  artifacts:
    - path: "apps/web/src/components/timeline/ConfirmDialog.vue"
      provides: "通用确认弹窗"
      contains: "defineEmits"
    - path: "apps/web/src/components/timeline/TimelineVisitCard.vue"
      provides: "支持编辑/删除交互的时间轴卡片"
      contains: "isEditing"
  key_links:
    - from: "apps/web/src/components/timeline/TimelineVisitCard.vue"
      to: "apps/web/src/components/timeline/TimelineEditForm.vue"
      via: "import TimelineEditForm"
      pattern: "import.*TimelineEditForm"
    - from: "apps/web/src/components/timeline/TimelineVisitCard.vue"
      to: "apps/web/src/components/timeline/ConfirmDialog.vue"
      via: "import ConfirmDialog"
      pattern: "import.*ConfirmDialog"
    - from: "apps/web/src/components/timeline/TimelineVisitCard.vue"
      to: "apps/web/src/stores/map-points.ts"
      via: "useMapPointsStore().updateRecord / deleteSingleRecord"
      pattern: "mapPointsStore\\.(updateRecord|deleteSingleRecord)"
---

<objective>
构建删除确认弹窗组件，并修改 TimelineVisitCard 集成编辑表单和删除功能，完成时间轴页面的完整编辑/删除交互。

Purpose: 覆盖 DEL-01（删除单条记录）、DEL-02（删除确认弹窗）、DEL-03（最后一条提示）、EDIT-01/02/03（编辑交互入口）需求。
Output: ConfirmDialog.vue（通用弹窗）+ TimelineVisitCard.vue 修改（集成编辑/删除）+ 对应测试文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-ui/38-RESEARCH.md
@.planning/phases/38-ui/38-PATTERNS.md
@.planning/phases/38-ui/38-UI-SPEC.md
@.planning/phases/38-ui/38-01-SUMMARY.md

<interfaces>
<!-- Plan 01 产出的组件接口 -->

From apps/web/src/components/timeline/TimelineEditForm.vue:
```typescript
interface Props {
  record: TimelineEntry
  conflictingDates?: string[]
  isSubmitting?: boolean
}
// Emits: submit(payload: UpdateTravelRecordRequest), cancel()
```

From apps/web/src/services/date-conflict.ts:
```typescript
export function checkDateConflict(
  placeId: string,
  currentRecordId: string,
  newStart: string | null,
  newEnd: string | null,
  tripsByPlaceId: Map<string, TravelRecord[]>,
): string[]
```

From apps/web/src/services/timeline.ts:
```typescript
export interface TimelineEntry {
  recordId: string
  placeId: string
  displayName: string
  parentLabel: string
  subtitle: string
  typeLabel: string
  startDate: string | null
  endDate: string | null
  createdAt: string
  hasKnownDate: boolean
  sortDate: string | null
  visitOrdinal: number
  visitCount: number
}
```

From apps/web/src/stores/map-points.ts (store 方法):
```typescript
async function updateRecord(recordId: string, request: UpdateTravelRecordRequest): Promise<void>
async function deleteSingleRecord(recordId: string): Promise<void>
const tripsByPlaceId: ComputedRef<Map<string, TravelRecord[]>>
```

From packages/contracts/src/records.ts:
```typescript
export interface UpdateTravelRecordRequest {
  startDate?: string | null
  endDate?: string | null
  notes?: string | null
  tags?: string[]
}
```

From apps/web/src/components/auth/LocalImportDecisionDialog.vue (弹窗样式参考):
```vue
<!-- Backdrop -->
<div class="fixed inset-0 z-[6] flex items-center justify-center bg-[rgba(87,66,95,0.16)] px-4 py-6 backdrop-blur-sm">
<!-- 弹窗容器 -->
<section class="w-full max-w-[28rem] overflow-hidden rounded-[32px] border border-white/85 bg-[var(--color-surface)]/96 text-[var(--color-ink-strong)] shadow-[var(--shadow-stage)] backdrop-blur-xl" role="dialog" aria-modal="true">
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建通用确认弹窗 ConfirmDialog.vue</name>
  <files>
    apps/web/src/components/timeline/ConfirmDialog.vue,
    apps/web/src/components/timeline/ConfirmDialog.spec.ts
  </files>
  <read_first>
    - apps/web/src/components/auth/LocalImportDecisionDialog.vue（弹窗结构和样式参考）
    - apps/web/src/components/auth/AuthDialog.vue（ESC 键关闭参考）
    - .planning/phases/38-ui/38-UI-SPEC.md（ConfirmDialog 设计规范）
    - .planning/phases/38-ui/38-PATTERNS.md（Pattern 2: 确认弹窗）
  </read_first>
  <behavior>
    - isOpen=true 时渲染弹窗：显示标题、消息、确认/取消按钮
    - isOpen=false 时不渲染任何 DOM：v-if 控制
    - 点击确认按钮：emit('confirm')
    - 点击取消按钮：emit('cancel')
    - 按 ESC 键：emit('cancel')
    - 点击 backdrop：emit('cancel')
    - destructive 模式：确认按钮使用红色样式
    - default 模式：确认按钮使用默认粉色渐变样式
    - 自定义按钮文案：confirmLabel='删除' => 按钮显示 '删除'
    - ARIA 属性：role="dialog", aria-modal="true"
  </behavior>
  <action>
    **Step 1: 先写测试文件 `apps/web/src/components/timeline/ConfirmDialog.spec.ts`**

    测试用例覆盖：
    1. isOpen=true 时渲染弹窗（找到 [data-confirm-dialog-backdrop]）
    - isOpen=false 时不渲染弹窗（找不到 backdrop）
    3. 点击确认按钮 emit confirm
    4. 点击取消按钮 emit cancel
    5. 按 ESC 键 emit cancel
    6. 点击 backdrop emit cancel
    7. destructive 模式下确认按钮有红色样式
    8. 自定义 confirmLabel 和 cancelLabel
    9. ARIA 属性存在（role="dialog", aria-modal="true"）

    运行测试（RED）：`pnpm --filter @trip-map/web test -- ConfirmDialog`

    **Step 2: 实现 `apps/web/src/components/timeline/ConfirmDialog.vue`**

    ```vue
    <script setup lang="ts">
    interface Props {
      isOpen: boolean
      title: string
      message: string
      confirmLabel?: string
      cancelLabel?: string
      tone?: 'default' | 'destructive'
    }

    const props = withDefaults(defineProps<Props>(), {
      confirmLabel: '确认',
      cancelLabel: '取消',
      tone: 'default',
    })

    const emit = defineEmits<{
      confirm: []
      cancel: []
    }>()

    function handleBackdropClick(event: MouseEvent) {
      if (event.target === event.currentTarget) {
        emit('cancel')
      }
    }

    function handleKeydown(event: KeyboardEvent) {
      if (event.key === 'Escape') {
        emit('cancel')
      }
    }
    </script>

    <template>
      <div
        v-if="isOpen"
        class="fixed inset-0 z-[6] flex items-center justify-center bg-[rgba(87,66,95,0.16)] px-4 py-6 backdrop-blur-sm"
        data-confirm-dialog-backdrop
        role="dialog"
        aria-modal="true"
        @click="handleBackdropClick"
        @keydown="handleKeydown"
      >
        <section
          class="w-full max-w-[28rem] overflow-hidden rounded-[32px] border border-white/85 bg-[var(--color-surface)]/96 text-[var(--color-ink-strong)] shadow-[var(--shadow-stage)] backdrop-blur-xl"
          data-confirm-dialog-content
        >
          <div class="border-b border-white/80 px-6 pb-5 pt-6">
            <h2 class="text-2xl font-semibold" data-confirm-dialog-title>
              {{ title }}
            </h2>
          </div>

          <div class="space-y-5 px-6 pb-6 pt-5">
            <p class="text-sm leading-7 text-[var(--color-ink-muted)]" data-confirm-dialog-message>
              {{ message }}
            </p>

            <div class="grid gap-3">
              <button
                type="button"
                :class="[
                  'inline-flex min-h-11 w-full items-center justify-center rounded-full px-5 py-3 text-sm font-semibold transition',
                  tone === 'destructive'
                    ? 'border border-[#e8c4c4] bg-[#fef2f2] text-[var(--color-destructive)] hover:bg-[#fde8e8]'
                    : 'border border-[color:color-mix(in_srgb,var(--color-accent)_22%,white_78%)] bg-[linear-gradient(135deg,rgba(255,232,242,0.96),rgba(255,246,250,0.96))] text-[var(--color-accent-strong)] shadow-[var(--shadow-button)]',
                ]"
                data-confirm-dialog-confirm
                @click="emit('confirm')"
              >
                {{ confirmLabel }}
              </button>

              <button
                type="button"
                class="inline-flex min-h-11 w-full items-center justify-center rounded-full border border-white/85 bg-white/86 px-5 py-3 text-sm font-semibold text-[var(--color-ink-strong)] transition hover:bg-white"
                data-confirm-dialog-cancel
                @click="emit('cancel')"
              >
                {{ cancelLabel }}
              </button>
            </div>
          </div>
        </section>
      </div>
    </template>
    ```

    运行测试（GREEN）：`pnpm --filter @trip-map/web test -- ConfirmDialog`
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- ConfirmDialog</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/timeline/ConfirmDialog.vue 存在且包含 `defineProps` 和 `defineEmits`
    - apps/web/src/components/timeline/ConfirmDialog.spec.ts 存在且包含至少 9 个测试用例
    - `pnpm --filter @trip-map/web test -- ConfirmDialog` 全部通过
    - 弹窗 backdrop 使用 `data-confirm-dialog-backdrop` 属性
    - 弹窗内容使用 `data-confirm-dialog-content` 属性
    - 标题使用 `data-confirm-dialog-title` 属性
    - 消息使用 `data-confirm-dialog-message` 属性
    - 确认按钮使用 `data-confirm-dialog-confirm` 属性
    - 取消按钮使用 `data-confirm-dialog-cancel` 属性
    - 弹窗具有 `role="dialog"` 和 `aria-modal="true"` ARIA 属性
    - destructive 模式确认按钮样式包含 `text-[var(--color-destructive)]`
  </acceptance_criteria>
  <done>ConfirmDialog 组件实现完成，9+ 测试用例全部通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修改 TimelineVisitCard 集成编辑/删除功能</name>
  <files>
    apps/web/src/components/timeline/TimelineVisitCard.vue,
    apps/web/src/components/timeline/TimelineVisitCard.spec.ts
  </files>
  <read_first>
    - apps/web/src/components/timeline/TimelineVisitCard.vue（当前只读版本，Task 修改目标）
    - apps/web/src/components/timeline/TimelineEditForm.vue（Plan 01 产出，编辑表单）
    - apps/web/src/components/timeline/ConfirmDialog.vue（Task 1 产出，确认弹窗）
    - apps/web/src/services/date-conflict.ts（Plan 01 产出，日期冲突检查）
    - apps/web/src/stores/map-points.ts（updateRecord / deleteSingleRecord / tripsByPlaceId）
    - apps/web/src/views/TimelinePageView.vue（TimelineVisitCard 的使用方式）
    - .planning/phases/38-ui/38-UI-SPEC.md（TimelineVisitCard 交互设计）
    - .planning/phases/38-ui/38-PATTERNS.md（Pattern 5: 删除最后一条检测）
  </read_first>
  <behavior>
    - 只读模式：卡片展示只读内容 + 底部操作栏（编辑/删除按钮）
    - 编辑模式：点击编辑按钮 => isEditing=true => 显示 TimelineEditForm 替代只读内容
    - 编辑提交：TimelineEditForm emit submit => 调用 store.updateRecord => 成功后 isEditing=false
    - 编辑取消：TimelineEditForm emit cancel => isEditing=false
    - 删除确认：点击删除按钮 => 显示 ConfirmDialog
    - 删除执行：ConfirmDialog emit confirm => 调用 store.deleteSingleRecord
    - 最后一条警告：visitCount=1 时 ConfirmDialog 显示额外警告 + destructive 样式
    - 普通删除：visitCount>1 时 ConfirmDialog 显示普通确认消息
    - 操作栏样式：border-t 分隔，编辑/删除 pill 按钮
  </behavior>
  <action>
    **Step 1: 先写测试文件 `apps/web/src/components/timeline/TimelineVisitCard.spec.ts`**

    使用 `@vue/test-utils` 的 `mount` + Pinia，参考 TimelinePageView.spec.ts 模式。

    测试用例覆盖：
    1. 只读模式渲染：显示地点名称、日期、类型标签
    2. 操作栏存在：显示编辑和删除按钮（[data-card-edit]、[data-card-delete]）
    3. 编辑模式切换：点击编辑按钮 => TimelineEditForm 出现（[data-region="timeline-edit-form"]）
    4. 编辑取消：编辑模式下点击取消 => 回到只读模式
    5. 编辑提交：编辑模式下提交 => emit edit 事件
    6. 删除确认弹窗：点击删除按钮 => ConfirmDialog 出现（[data-confirm-dialog-backdrop]）
    7. 删除执行：确认弹窗点击确认 => emit delete 事件
    8. 删除取消：确认弹窗点击取消 => 弹窗关闭
    9. 最后一条警告：visitCount=1 时确认弹窗标题为 '删除该地点最后一条记录'
    10. 普通删除消息：visitCount>1 时确认弹窗标题为 '删除旅行记录'

    运行测试（RED）：`pnpm --filter @trip-map/web test -- TimelineVisitCard`

    **Step 2: 修改 `apps/web/src/components/timeline/TimelineVisitCard.vue`**

    关键修改要点（在现有只读模板基础上扩展）：

    **script setup 部分新增：**
    ```typescript
    import { computed, ref } from 'vue'
    import { storeToRefs } from 'pinia'

    import type { UpdateTravelRecordRequest } from '@trip-map/contracts'
    import { useMapPointsStore } from '../../stores/map-points'
    import { checkDateConflict } from '../../services/date-conflict'
    import ConfirmDialog from './ConfirmDialog.vue'
    import TimelineEditForm from './TimelineEditForm.vue'

    const props = defineProps<{
      entry: TimelineEntry
    }>()

    const emit = defineEmits<{
      edit: [payload: UpdateTravelRecordRequest]
      delete: [recordId: string]
    }>()

    const mapPointsStore = useMapPointsStore()
    const { tripsByPlaceId } = storeToRefs(mapPointsStore)

    const isEditing = ref(false)
    const isSubmitting = ref(false)
    const isDeleteDialogOpen = ref(false)

    const conflictingDates = computed(() => {
      if (!isEditing.value) return []
      return checkDateConflict(
        props.entry.placeId,
        props.entry.recordId,
        props.entry.startDate,
        props.entry.endDate,
        tripsByPlaceId.value,
      )
    })

    const deleteDialogConfig = computed(() => {
      if (props.entry.visitCount === 1) {
        return {
          title: '删除该地点最后一条记录',
          message: '这是该地点的唯一一条记录，删除后将取消该地点的点亮状态。确认删除？',
          tone: 'destructive' as const,
        }
      }
      return {
        title: '删除旅行记录',
        message: '确认删除这条旅行记录？',
        tone: 'default' as const,
      }
    })

    function handleEditClick() {
      isEditing.value = true
    }

    function handleEditCancel() {
      isEditing.value = false
    }

    async function handleEditSubmit(payload: UpdateTravelRecordRequest) {
      isSubmitting.value = true
      try {
        await mapPointsStore.updateRecord(props.entry.recordId, payload)
        isEditing.value = false
      } finally {
        isSubmitting.value = false
      }
    }

    function handleDeleteClick() {
      isDeleteDialogOpen.value = true
    }

    function handleDeleteCancel() {
      isDeleteDialogOpen.value = false
    }

    async function handleDeleteConfirm() {
      await mapPointsStore.deleteSingleRecord(props.entry.recordId)
      isDeleteDialogOpen.value = false
    }
    ```

    **template 部分修改：**

    在现有 `<article>` 内部：
    1. 将只读内容包裹在 `<template v-if="!isEditing">` 中
    2. 添加 `<template v-else>` 渲染 TimelineEditForm
    3. 在只读内容底部添加操作栏（编辑/删除按钮）
    4. 在 `<article>` 外部添加 ConfirmDialog

    ```vue
    <template>
      <article
        class="grid gap-4 rounded-[28px] border border-white/80 bg-[linear-gradient(180deg,rgba(255,255,255,0.94),rgba(255,246,250,0.94))] p-5 shadow-[var(--shadow-float)]"
        data-region="timeline-entry"
      >
        <!-- 编辑模式 -->
        <TimelineEditForm
          v-if="isEditing"
          :record="entry"
          :conflicting-dates="conflictingDates"
          :is-submitting="isSubmitting"
          @submit="handleEditSubmit"
          @cancel="handleEditCancel"
        />

        <!-- 只读模式 -->
        <template v-else>
          <!-- 现有 header + 内容区域保持不变 -->
          <header class="grid gap-3 md:grid-cols-[minmax(0,1fr)_auto] md:items-start">
            <!-- ... 现有 header 内容 ... -->
          </header>

          <div class="grid gap-3 md:grid-cols-2">
            <!-- ... 现有日期/国家卡片 ... -->
          </div>

          <!-- 备注显示（如果有） -->
          <div v-if="entry.notes" class="rounded-[22px] border border-white/85 bg-white/78 p-4">
            <p class="text-[0.72rem] font-semibold tracking-[0.08em] text-[var(--color-ink-soft)] uppercase">
              备注
            </p>
            <p class="mt-2 text-sm leading-6 text-[var(--color-ink-strong)]">
              {{ entry.notes }}
            </p>
          </div>

          <!-- 标签显示（如果有） -->
          <div v-if="entry.tags.length > 0" class="flex flex-wrap gap-2">
            <span
              v-for="tag in entry.tags"
              :key="tag"
              class="inline-flex items-center rounded-full border border-[var(--color-accent-soft)] bg-[var(--color-accent-soft)]/30 px-3 py-1 text-xs font-semibold text-[var(--color-ink-strong)]"
            >
              {{ tag }}
            </span>
          </div>

          <!-- 操作栏 -->
          <div class="flex gap-3 border-t border-white/80 pt-4">
            <button
              type="button"
              class="min-h-9 rounded-full border border-[#d6ebf2] bg-[#effafc] px-4 text-xs font-semibold text-[var(--color-ink-strong)] transition-all duration-300 ease-out hover:scale-105 active:scale-95"
              data-card-edit
              @click="handleEditClick"
            >
              编辑
            </button>
            <button
              type="button"
              class="min-h-9 rounded-full border border-[#e8c4c4] bg-[#fef2f2] px-4 text-xs font-semibold text-[var(--color-destructive)] transition-all duration-300 ease-out hover:scale-105 active:scale-95"
              data-card-delete
              @click="handleDeleteClick"
            >
              删除
            </button>
          </div>
        </template>
      </article>

      <!-- 删除确认弹窗 -->
      <ConfirmDialog
        :is-open="isDeleteDialogOpen"
        :title="deleteDialogConfig.title"
        :message="deleteDialogConfig.message"
        :tone="deleteDialogConfig.tone"
        confirm-label="确认删除"
        cancel-label="取消"
        data-confirm-dialog
        @confirm="handleDeleteConfirm"
        @cancel="handleDeleteCancel"
      />
    </template>
    ```

    **注意：** entry.notes 和 entry.tags 需要从 TimelineEntry 扩展。检查 TimelineEntry 接口是否有这两个字段。如果没有，需要从 store 获取完整 TravelRecord 数据。参考 TimelinePageView.vue 中 TimelineVisitCard 的使用方式，entry 来自 timelineEntries，而 timelineEntries 由 buildTimelineEntries 从 travelRecords 构建。

    **关键检查：** TimelineEntry 当前没有 notes 和 tags 字段。需要在 timeline.ts 的 toTimelineEntry 函数中添加这两个字段的映射，或者在 TimelineVisitCard 中通过 entry.recordId 从 store.travelRecords 中查找完整记录。

    **推荐方案：** 在 TimelineEntry 接口中添加 notes 和 tags 字段（修改 apps/web/src/services/timeline.ts 的 toTimelineEntry 函数），这样 TimelineVisitCard 可以直接访问。这是最小改动方案。

    需要额外修改的文件：
    - `apps/web/src/services/timeline.ts`：TimelineEntry 接口添加 `notes: string | null` 和 `tags: string[]`，toTimelineEntry 函数添加映射

    运行测试（GREEN）：`pnpm --filter @trip-map/web test -- TimelineVisitCard`
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- TimelineVisitCard</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/timeline/TimelineVisitCard.vue 包含 `isEditing` ref
    - apps/web/src/components/timeline/TimelineVisitCard.vue import TimelineEditForm 和 ConfirmDialog
    - apps/web/src/components/timeline/TimelineVisitCard.vue 包含 `handleEditSubmit` 和 `handleDeleteConfirm` 函数
    - apps/web/src/components/timeline/TimelineVisitCard.spec.ts 存在且包含至少 10 个测试用例
    - `pnpm --filter @trip-map/web test -- TimelineVisitCard` 全部通过
    - 卡片使用 `data-region="timeline-entry"` 属性（保持不变）
    - 编辑按钮使用 `data-card-edit` 属性
    - 删除按钮使用 `data-card-delete` 属性
    - 编辑模式下渲染 TimelineEditForm（`data-region="timeline-edit-form"`）
    - 删除操作通过 ConfirmDialog（`data-confirm-dialog-backdrop`）确认
    - visitCount=1 时 ConfirmDialog tone 为 'destructive'
    - TimelineEntry 接口包含 `notes` 和 `tags` 字段
    - toTimelineEntry 函数映射 notes 和 tags
  </acceptance_criteria>
  <done>TimelineVisitCard 集成编辑/删除功能完成，10+ 测试用例全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → delete action | 用户点击删除按钮触发确认弹窗 |
| confirm dialog → store.deleteSingleRecord | 确认后调用 store 删除方法 |
| edit form → store.updateRecord | 编辑提交后调用 store 更新方法 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Spoofing | ConfirmDialog | mitigate | 弹窗由组件内部状态控制，不接受外部 DOM 注入；使用 data-* 属性标识确保来源可信 |
| T-38-06 | Tampering | delete action | mitigate | 删除操作需经过 ConfirmDialog 确认，不可直接调用 store.deleteSingleRecord |
| T-38-07 | Repudiation | delete action | accept | 无编辑历史/撤销功能（PROJECT.md 已决策），删除操作不可逆 |
| T-38-08 | Information Disclosure | delete last record warning | accept | 警告信息仅提示"取消点亮状态"，不暴露其他用户数据 |
</threat_model>

<verification>
1. `pnpm --filter @trip-map/web test` — 全部测试通过
2. `pnpm --filter @trip-map/web typecheck` — 类型检查通过
3. TimelineVisitCard 支持编辑/删除交互，同时保持只读模式的现有功能
4. ConfirmDialog 为通用组件，Props 接口支持复用（Phase 39 地图 popup 可直接使用）
5. TimelineEntry 接口扩展 notes/tags 字段，buildTimelineEntries 正确映射
</verification>

<success_criteria>
- ConfirmDialog.vue 通用弹窗实现完成，9+ 测试通过
- TimelineVisitCard.vue 集成编辑表单和删除功能，10+ 测试通过
- TimelineEntry 接口扩展 notes/tags 字段
- 删除最后一条记录时显示 destructive 风格警告弹窗
- 编辑成功后 store.updateRecord 自动触发时间轴重排序
- 删除成功后 store.deleteSingleRecord 自动触发时间轴移除卡片
- 所有组件使用 data-* 属性标识关键元素
- 无新增 npm 依赖
</success_criteria>

<output>
After completion, create `.planning/phases/38-ui/38-02-SUMMARY.md`
</output>

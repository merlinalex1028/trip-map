---
phase: 38
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/services/date-conflict.ts
  - apps/web/src/services/date-conflict.spec.ts
  - apps/web/src/components/timeline/TagInput.vue
  - apps/web/src/components/timeline/TagInput.spec.ts
  - apps/web/src/components/timeline/TimelineEditForm.vue
  - apps/web/src/components/timeline/TimelineEditForm.spec.ts
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04]
must_haves:
  truths:
    - "用户可以在编辑表单中修改旅行开始日期和结束日期"
    - "用户可以在编辑表单中添加或修改纯文本备注（最长 1000 字符）"
    - "用户可以在编辑表单中通过 TagInput 添加或删除标签（最多 10 个，每个最长 20 字符）"
    - "编辑日期时，表单自动检查同地点其他记录的日期冲突并显示警告提示"
    - "结束日期早于开始日期时，表单显示验证错误"
  artifacts:
    - path: "apps/web/src/services/date-conflict.ts"
      provides: "checkDateConflict 纯函数"
      exports: ["checkDateConflict"]
    - path: "apps/web/src/components/timeline/TagInput.vue"
      provides: "标签输入控件"
      contains: "defineProps"
    - path: "apps/web/src/components/timeline/TimelineEditForm.vue"
      provides: "旅行记录编辑表单"
      contains: "defineEmits"
  key_links:
    - from: "apps/web/src/components/timeline/TimelineEditForm.vue"
      to: "apps/web/src/components/timeline/TagInput.vue"
      via: "import TagInput"
      pattern: "import.*TagInput"
    - from: "apps/web/src/components/timeline/TimelineEditForm.vue"
      to: "apps/web/src/services/date-conflict.ts"
      via: "import checkDateConflict"
      pattern: "import.*checkDateConflict"
    - from: "apps/web/src/components/timeline/TimelineEditForm.vue"
      to: "packages/contracts/src/records.ts"
      via: "import UpdateTravelRecordRequest"
      pattern: "import.*UpdateTravelRecordRequest"
---

<objective>
构建时间轴编辑功能的三个核心组件/工具：日期冲突检查纯函数、标签输入控件、编辑表单组件。

Purpose: 为 TimelineVisitCard 的编辑模式提供所有子组件，覆盖 EDIT-01（日期修改）、EDIT-02（备注）、EDIT-03（标签）、EDIT-04（日期冲突检查）四个需求。
Output: date-conflict.ts（纯函数）+ TagInput.vue（输入控件）+ TimelineEditForm.vue（表单组件）+ 对应测试文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-ui/38-RESEARCH.md
@.planning/phases/38-ui/38-PATTERNS.md
@.planning/phases/38-ui/38-UI-SPEC.md

<interfaces>
<!-- 关键类型和契约，executor 直接使用 -->

From packages/contracts/src/records.ts:
```typescript
export interface UpdateTravelRecordRequest {
  startDate?: string | null
  endDate?: string | null
  notes?: string | null
  tags?: string[]
}

export interface TravelRecord {
  id: string
  placeId: string
  boundaryId: string
  placeKind: PlaceKind
  datasetVersion: string
  displayName: string
  regionSystem: 'CN' | 'OVERSEAS'
  adminType: ChinaAdminType | 'ADMIN1'
  typeLabel: string
  parentLabel: string
  subtitle: string
  startDate: string | null
  endDate: string | null
  createdAt: string
  updatedAt: string
  notes: string | null
  tags: string[]
}
```

From apps/web/src/services/timeline.ts:
```typescript
export interface TimelineEntry {
  recordId: string
  placeId: string
  displayName: string
  parentLabel: string
  subtitle: string
  typeLabel: string
  startDate: string | null
  endDate: string | null
  createdAt: string
  hasKnownDate: boolean
  sortDate: string | null
  visitOrdinal: number
  visitCount: number
}
```

From apps/web/src/components/map-popup/TripDateForm.vue (analog):
```typescript
// 日期验证模式
const hasStartDate = computed(() => startDate.value !== '')
const hasRangeError = computed(
  () => hasStartDate.value && endDate.value !== '' && endDate.value < startDate.value,
)
const isValid = computed(() => hasStartDate.value && !hasRangeError.value)

// 输入框样式
class="min-h-11 rounded-xl border border-[#d7dcea] bg-white px-3 py-2 text-[var(--font-label-size)] text-[var(--color-ink-strong)]"

// 按钮样式（submit）
class="min-h-11 rounded-full border border-[#f4d7e4] bg-[linear-gradient(135deg,rgba(255,232,242,0.96),rgba(255,246,250,0.96))] px-4 py-2 text-[var(--font-label-size)] font-bold text-[var(--color-accent-strong)] shadow-[0_14px_28px_rgba(244,143,177,0.34)] transition-all duration-300 ease-out hover:scale-105 hover:-translate-y-1 active:scale-95 disabled:cursor-not-allowed disabled:opacity-55"

// 按钮样式（cancel）
class="min-h-11 rounded-full border border-[#d7dcea] bg-white/80 px-4 py-2 text-[var(--font-label-size)] font-bold text-[var(--color-ink-muted)] transition-all duration-300 ease-out hover:scale-105 active:scale-95"
```

From apps/web/src/stores/map-points.ts (store 方法签名):
```typescript
async function updateRecord(
  recordId: string,
  request: UpdateTravelRecordRequest,
) { /* 乐观更新 + 回滚 + notice */ }

// tripsByPlaceId 数据源
const tripsByPlaceId = computed(() => {
  const map = new Map<string, TravelRecord[]>()
  for (const record of travelRecords.value) {
    const existing = map.get(record.placeId) ?? []
    existing.push(record)
    map.set(record.placeId, existing)
  }
  return map
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建日期冲突检查纯函数 date-conflict.ts</name>
  <files>
    apps/web/src/services/date-conflict.ts,
    apps/web/src/services/date-conflict.spec.ts
  </files>
  <read_first>
    - apps/web/src/services/timeline.ts（参考纯函数导出模式）
    - packages/contracts/src/records.ts（TravelRecord 类型）
    - .planning/phases/38-ui/38-RESEARCH.md（Pattern 4: 日期冲突检查逻辑）
  </read_first>
  <behavior>
    - 无冲突时返回空数组：checkDateConflict('p1', 'r1', '2025-01-01', '2025-01-05', map) => []
    - 日期重叠时返回冲突记录日期范围：同地点有 r2 日期 '2025-01-03'~'2025-01-07'，检查 r1 新日期 '2025-01-01'~'2025-01-04' => ['2025-01-03 ~ 2025-01-07']
    - 排除当前记录自身：同地点只有当前记录时返回 []
    - 任一日期为 null 时跳过比较：newStart=null => []
    - 多个冲突时全部返回：同地点 r2 '2025-01-03'~'2025-01-07' + r3 '2025-01-06'~'2025-01-10'，新日期 '2025-01-01'~'2025-01-08' => ['2025-01-03 ~ 2025-01-07', '2025-01-06 ~ 2025-01-10']
    - endDate 为 null 时退化为 startDate 比较：other.endDate=null => 使用 other.startDate 作为 otherEnd
    - 地点不在 map 中时返回空数组
  </behavior>
  <action>
    **Step 1: 先写测试文件 `apps/web/src/services/date-conflict.spec.ts`**

    ```typescript
    import { describe, expect, it } from 'vitest'
    import type { TravelRecord } from '@trip-map/contracts'
    import { checkDateConflict } from './date-conflict'

    function makeTravelRecord(overrides: Partial<TravelRecord> = {}): TravelRecord {
      return {
        id: 'record-1',
        placeId: 'place-1',
        boundaryId: 'boundary-1',
        placeKind: 'CITY',
        datasetVersion: 'v1',
        displayName: '北京',
        regionSystem: 'CN',
        adminType: 'DIRECT_ADMINISTERED',
        typeLabel: '市',
        parentLabel: '中国',
        subtitle: '',
        startDate: '2025-01-15',
        endDate: null,
        createdAt: '2025-01-16T00:00:00.000Z',
        updatedAt: '2025-01-16T00:00:00.000Z',
        notes: null,
        tags: [],
        ...overrides,
      }
    }

    describe('checkDateConflict', () => {
      it('returns empty array when no siblings exist', () => {
        const map = new Map<string, TravelRecord[]>()
        expect(checkDateConflict('p1', 'r1', '2025-01-01', '2025-01-05', map)).toEqual([])
      })

      it('returns empty array when only current record exists', () => {
        const record = makeTravelRecord({ id: 'r1', placeId: 'p1' })
        const map = new Map<string, TravelRecord[]>([['p1', [record]]])
        expect(checkDateConflict('p1', 'r1', '2025-01-01', '2025-01-05', map)).toEqual([])
      })

      it('detects overlapping date range', () => {
        const other = makeTravelRecord({ id: 'r2', placeId: 'p1', startDate: '2025-01-03', endDate: '2025-01-07' })
        const map = new Map<string, TravelRecord[]>([['p1', [other]]])
        expect(checkDateConflict('p1', 'r1', '2025-01-01', '2025-01-04', map)).toEqual(['2025-01-03 ~ 2025-01-07'])
      })

      it('returns empty array when dates do not overlap', () => {
        const other = makeTravelRecord({ id: 'r2', placeId: 'p1', startDate: '2025-01-10', endDate: '2025-01-15' })
        const map = new Map<string, TravelRecord[]>([['p1', [other]]])
        expect(checkDateConflict('p1', 'r1', '2025-01-01', '2025-01-05', map)).toEqual([])
      })

      it('returns empty array when newStart is null', () => {
        const other = makeTravelRecord({ id: 'r2', placeId: 'p1', startDate: '2025-01-03', endDate: '2025-01-07' })
        const map = new Map<string, TravelRecord[]>([['p1', [other]]])
        expect(checkDateConflict('p1', 'r1', null, '2025-01-04', map)).toEqual([])
      })

      it('returns empty array when other.startDate is null', () => {
        const other = makeTravelRecord({ id: 'r2', placeId: 'p1', startDate: null, endDate: null })
        const map = new Map<string, TravelRecord[]>([['p1', [other]]])
        expect(checkDateConflict('p1', 'r1', '2025-01-01', '2025-01-05', map)).toEqual([])
      })

      it('uses other.startDate as otherEnd when other.endDate is null', () => {
        const other = makeTravelRecord({ id: 'r2', placeId: 'p1', startDate: '2025-01-03', endDate: null })
        const map = new Map<string, TravelRecord[]>([['p1', [other]]])
        expect(checkDateConflict('p1', 'r1', '2025-01-01', '2025-01-04', map)).toEqual(['2025-01-03 ~ 2025-01-03'])
      })

      it('uses newStart as effectiveNewEnd when newEnd is null', () => {
        const other = makeTravelRecord({ id: 'r2', placeId: 'p1', startDate: '2025-01-03', endDate: '2025-01-07' })
        const map = new Map<string, TravelRecord[]>([['p1', [other]]])
        expect(checkDateConflict('p1', 'r1', '2025-01-05', null, map)).toEqual(['2025-01-03 ~ 2025-01-07'])
      })

      it('detects multiple conflicts', () => {
        const other1 = makeTravelRecord({ id: 'r2', placeId: 'p1', startDate: '2025-01-03', endDate: '2025-01-07' })
        const other2 = makeTravelRecord({ id: 'r3', placeId: 'p1', startDate: '2025-01-06', endDate: '2025-01-10' })
        const map = new Map<string, TravelRecord[]>([['p1', [other1, other2]]])
        expect(checkDateConflict('p1', 'r1', '2025-01-01', '2025-01-08', map)).toEqual([
          '2025-01-03 ~ 2025-01-07',
          '2025-01-06 ~ 2025-01-10',
        ])
      })
    })
    ```

    运行测试（RED）：`pnpm --filter @trip-map/web test -- date-conflict`

    **Step 2: 实现 `apps/web/src/services/date-conflict.ts`**

    ```typescript
    import type { TravelRecord } from '@trip-map/contracts'

    /**
     * 检查编辑日期时是否与同地点其他记录存在日期冲突。
     * 返回冲突记录的日期范围字符串数组（仅用于前端 UX 提示，不阻塞提交）。
     */
    export function checkDateConflict(
      placeId: string,
      currentRecordId: string,
      newStart: string | null,
      newEnd: string | null,
      tripsByPlaceId: Map<string, TravelRecord[]>,
    ): string[] {
      if (!newStart) {
        return []
      }

      const siblings = tripsByPlaceId.get(placeId) ?? []
      const others = siblings.filter((r) => r.id !== currentRecordId)
      const conflicts: string[] = []

      for (const other of others) {
        if (!other.startDate) {
          continue
        }

        const otherEnd = other.endDate ?? other.startDate
        const effectiveNewEnd = newEnd ?? newStart

        if (newStart <= otherEnd && effectiveNewEnd >= other.startDate) {
          conflicts.push(`${other.startDate} ~ ${otherEnd}`)
        }
      }

      return conflicts
    }
    ```

    运行测试（GREEN）：`pnpm --filter @trip-map/web test -- date-conflict`
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- date-conflict</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/services/date-conflict.ts 存在且导出 `checkDateConflict` 函数
    - apps/web/src/services/date-conflict.spec.ts 存在且包含至少 8 个测试用例
    - `pnpm --filter @trip-map/web test -- date-conflict` 全部通过
    - checkDateConflict 函数签名为 `(placeId: string, currentRecordId: string, newStart: string | null, newEnd: string | null, tripsByPlaceId: Map<string, TravelRecord[]>) => string[]`
    - 函数在 newStart 为 null 时返回空数组
    - 函数正确过滤掉 currentRecordId 对应的记录
  </acceptance_criteria>
  <done>日期冲突检查纯函数实现完成，8+ 测试用例全部通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建标签输入控件 TagInput.vue</name>
  <files>
    apps/web/src/components/timeline/TagInput.vue,
    apps/web/src/components/timeline/TagInput.spec.ts
  </files>
  <read_first>
    - apps/web/src/components/timeline/TimelineVisitCard.vue（参考 pill 样式）
    - apps/web/src/components/map-popup/TripDateForm.vue（参考 Vue 组件模式）
    - .planning/phases/38-ui/38-UI-SPEC.md（TagInput 设计规范）
    - .planning/phases/38-ui/38-PATTERNS.md（Pattern 3: 标签输入控件）
  </read_first>
  <behavior>
    - 渲染已有标签列表：传入 tags=['美食', '文化'] => 显示两个 chip，每个带删除按钮
    - 回车添加标签：输入框输入 '历史' + 按 Enter => emit('update:tags', ['美食', '文化', '历史'])
    - 逗号添加标签：输入框输入 '历史,' => emit('update:tags', [...]) 且输入框清空
    - 失焦自动添加：输入框有内容时失焦 => 自动添加
    - 删除标签：点击第一个标签的删除按钮 => emit('update:tags', ['文化'])
    - 去重：输入已存在的标签 => 不添加，输入框清空
    - 空白标签：输入 '   ' => 不添加
    - 超长标签：输入超过 20 字符的标签 => 不添加
    - 上限：已有 10 个标签时输入新标签 => 不添加，输入框禁用
    - Backspace 删除最后一个：输入框为空时按 Backspace => 删除最后一个标签
  </behavior>
  <action>
    **Step 1: 先写测试文件 `apps/web/src/components/timeline/TagInput.spec.ts`**

    使用 `@vue/test-utils` 的 `mount`，参考 TimelinePageView.spec.ts 的 Pinia 初始化模式。

    测试用例覆盖：
    1. 渲染已有标签 chip（显示标签文字 + 删除按钮）
    2. 回车键添加新标签（emit update:tags）
    3. 逗号键添加新标签
    4. 失焦时自动添加非空内容
    5. 点击删除按钮移除标签
    6. 去重（不添加已存在标签）
    7. 过滤空白标签（trim 后为空不添加）
    8. 超长标签不添加（超过 maxTagLength）
    9. 达到上限时禁用输入（maxTags=10）
    10. 空输入框按 Backspace 删除最后一个标签

    运行测试（RED）：`pnpm --filter @trip-map/web test -- TagInput`

    **Step 2: 实现 `apps/web/src/components/timeline/TagInput.vue`**

    ```vue
    <script setup lang="ts">
    import { ref } from 'vue'

    interface Props {
      tags: string[]
      maxTags?: number
      maxTagLength?: number
    }

    const props = withDefaults(defineProps<Props>(), {
      maxTags: 10,
      maxTagLength: 20,
    })

    const emit = defineEmits<{
      'update:tags': [tags: string[]]
    }>()

    const inputValue = ref('')

    const isAtLimit = props.tags.length >= props.maxTags

    function addTag(raw: string) {
      const trimmed = raw.trim()
      if (!trimmed) return
      if (props.tags.includes(trimmed)) return
      if (props.tags.length >= props.maxTags) return
      if (trimmed.length > props.maxTagLength) return

      emit('update:tags', [...props.tags, trimmed])
      inputValue.value = ''
    }

    function removeTag(index: number) {
      const next = props.tags.filter((_, i) => i !== index)
      emit('update:tags', next)
    }

    function handleKeydown(event: KeyboardEvent) {
      if (event.key === 'Enter' || event.key === ',') {
        event.preventDefault()
        addTag(inputValue.value)
      }
      if (event.key === 'Backspace' && inputValue.value === '' && props.tags.length > 0) {
        removeTag(props.tags.length - 1)
      }
    }

    function handleBlur() {
      if (inputValue.value.trim()) {
        addTag(inputValue.value)
      }
    }
    </script>

    <template>
      <div
        class="flex min-h-11 flex-wrap items-center gap-2 rounded-xl border border-[#d7dcea] bg-white p-2"
        data-region="tag-input"
      >
        <span
          v-for="(tag, index) in tags"
          :key="tag"
          class="inline-flex items-center gap-1 rounded-full border border-[var(--color-accent-soft)] bg-[var(--color-accent-soft)]/30 px-3 py-1 text-xs font-semibold text-[var(--color-ink-strong)]"
          data-tag-chip
        >
          {{ tag }}
          <button
            type="button"
            class="ml-1 text-[var(--color-ink-soft)] hover:text-[var(--color-destructive)]"
            data-tag-remove
            :aria-label="`删除标签 ${tag}`"
            @click="removeTag(index)"
          >
            ×
          </button>
        </span>
        <input
          v-model="inputValue"
          type="text"
          class="min-w-[80px] flex-1 bg-transparent text-[var(--font-label-size)] text-[var(--color-ink-strong)] outline-none"
          :placeholder="tags.length >= maxTags ? `最多添加 ${maxTags} 个标签` : '输入标签，按回车添加'"
          :disabled="tags.length >= maxTags"
          data-tag-input-field
          aria-label="输入标签"
          @keydown="handleKeydown"
          @blur="handleBlur"
        />
      </div>
    </template>
    ```

    运行测试（GREEN）：`pnpm --filter @trip-map/web test -- TagInput`
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- TagInput</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/timeline/TagInput.vue 存在且包含 `defineProps` 和 `defineEmits`
    - apps/web/src/components/timeline/TagInput.spec.ts 存在且包含至少 10 个测试用例
    - `pnpm --filter @trip-map/web test -- TagInput` 全部通过
    - 组件使用 `data-region="tag-input"` 属性
    - 标签 chip 使用 `data-tag-chip` 属性
    - 删除按钮使用 `data-tag-remove` 属性
    - 输入框使用 `data-tag-input-field` 属性
    - 组件 emit 事件名为 `update:tags`（支持 v-model）
    - 输入框 placeholder 为 '输入标签，按回车添加'
  </acceptance_criteria>
  <done>TagInput 组件实现完成，10+ 测试用例全部通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 创建编辑表单组件 TimelineEditForm.vue</name>
  <files>
    apps/web/src/components/timeline/TimelineEditForm.vue,
    apps/web/src/components/timeline/TimelineEditForm.spec.ts
  </files>
  <read_first>
    - apps/web/src/components/map-popup/TripDateForm.vue（日期表单模式，直接复用样式和验证逻辑）
    - apps/web/src/components/timeline/TagInput.vue（Task 2 产出，编辑表单将使用此组件）
    - apps/web/src/services/date-conflict.ts（Task 1 产出，编辑表单将调用此函数）
    - packages/contracts/src/records.ts（UpdateTravelRecordRequest 类型）
    - apps/web/src/services/timeline.ts（TimelineEntry 类型）
    - .planning/phases/38-ui/38-UI-SPEC.md（TimelineEditForm 设计规范）
    - .planning/phases/38-ui/38-PATTERNS.md（Pattern 1: 编辑模式切换）
  </read_first>
  <behavior>
    - 渲染日期输入：传入 record.startDate='2025-01-15' => 开始日期输入框显示 '2025-01-15'
    - 渲染备注 textarea：传入 record.notes='备注内容' => textarea 显示 '备注内容'
    - 渲染标签：传入 record.tags=['美食', '文化'] => TagInput 显示两个标签
    - 日期范围验证：endDate < startDate => 显示错误 '结束日期不能早于开始日期'
    - 备注长度验证：notes 超过 1000 字符 => 显示错误 '备注不能超过 1000 字符'
    - 日期冲突警告：conflictingDates=['2025-01-03 ~ 2025-01-07'] => 显示警告文字
    - 提交：点击保存按钮 => emit('submit', { startDate, endDate, notes, tags })
    - 取消：点击取消按钮 => emit('cancel')
    - 禁用状态：isSubmitting=true => 保存按钮禁用
    - 空备注提交：notes='' => emit 时 notes=null
  </behavior>
  <action>
    **Step 1: 先写测试文件 `apps/web/src/components/timeline/TimelineEditForm.spec.ts`**

    测试用例覆盖：
    1. 渲染记录的初始日期、备注、标签
    2. 日期范围验证（endDate < startDate 显示错误）
    3. 备注长度验证（超过 1000 字符显示错误）
    4. 日期冲突警告显示（conflictingDates 非空时显示）
    5. 提交事件（emit submit with correct payload）
    6. 取消事件（emit cancel）
    7. 禁用状态（isSubmitting=true 时按钮禁用）
    8. 空备注提交为 null

    运行测试（RED）：`pnpm --filter @trip-map/web test -- TimelineEditForm`

    **Step 2: 实现 `apps/web/src/components/timeline/TimelineEditForm.vue`**

    关键实现要点：
    - Props: `record: TimelineEntry`, `conflictingDates?: string[]`, `isSubmitting?: boolean`
    - Emits: `submit(payload: UpdateTravelRecordRequest)`, `cancel()`
    - 内部状态: `startDate`, `endDate`, `notes`, `tags` ref，初始化自 props.record
    - 日期验证：复用 TripDateForm 的 hasStartDate/hasRangeError/isValid computed 模式
    - 备注验证：`notesTooLong = computed(() => notes.value.length > 1000)`
    - 日期冲突警告：直接使用 props.conflictingDates 渲染
    - 使用 TagInput 组件（import from './TagInput.vue'）
    - 表单样式：复用 TripDateForm 的 rounded-2xl border bg gradient 样式
    - 输入框样式：复用 TripDateForm 的 min-h-11 rounded-xl border 样式
    - 按钮样式：复用 TripDateForm 的 rounded-full border gradient 样式
    - 提交时 trim notes，空字符串转 null

    ```vue
    <script setup lang="ts">
    import { computed, ref } from 'vue'

    import type { UpdateTravelRecordRequest } from '@trip-map/contracts'
    import type { TimelineEntry } from '../../services/timeline'

    import TagInput from './TagInput.vue'

    interface Props {
      record: TimelineEntry
      conflictingDates?: string[]
      isSubmitting?: boolean
    }

    const props = withDefaults(defineProps<Props>(), {
      conflictingDates: () => [],
      isSubmitting: false,
    })

    const emit = defineEmits<{
      submit: [payload: UpdateTravelRecordRequest]
      cancel: []
    }>()

    const startDate = ref(props.record.startDate ?? '')
    const endDate = ref(props.record.endDate ?? '')
    const notes = ref(props.record.notes ?? '')
    const tags = ref<string[]>([...props.record.tags])

    const hasStartDate = computed(() => startDate.value !== '')
    const hasRangeError = computed(
      () => hasStartDate.value && endDate.value !== '' && endDate.value < startDate.value,
    )
    const notesTooLong = computed(() => notes.value.length > 1000)
    const isValid = computed(() => hasStartDate.value && !hasRangeError.value && !notesTooLong.value)

    const rangeErrorMessage = computed(() =>
      hasRangeError.value ? '结束日期不能早于开始日期' : null,
    )
    const notesErrorMessage = computed(() =>
      notesTooLong.value ? '备注不能超过 1000 字符' : null,
    )

    function handleSubmit() {
      if (!isValid.value || props.isSubmitting) {
        return
      }

      const trimmedNotes = notes.value.trim()
      emit('submit', {
        startDate: startDate.value || null,
        endDate: endDate.value || null,
        notes: trimmedNotes || null,
        tags: tags.value,
      })
    }
    </script>

    <template>
      <form
        class="grid gap-3 rounded-[22px] border border-[#e6d9ec] bg-[linear-gradient(180deg,rgba(255,255,255,0.92),rgba(253,245,255,0.94))] p-4"
        data-region="timeline-edit-form"
        @submit.prevent="handleSubmit"
      >
        <p class="text-[var(--font-label-size)] font-bold text-[var(--color-ink-strong)]">
          编辑旅行记录
        </p>

        <label class="grid gap-1">
          <span class="text-[var(--font-label-size)] font-bold text-[var(--color-ink-strong)]">
            开始日期
          </span>
          <input
            v-model="startDate"
            type="date"
            required
            :max="endDate || undefined"
            class="min-h-11 rounded-xl border border-[#d7dcea] bg-white px-3 py-2 text-[var(--font-label-size)] text-[var(--color-ink-strong)]"
            data-edit-input="start-date"
            aria-label="选择旅行开始日期"
          />
        </label>

        <label class="grid gap-1">
          <span class="text-[var(--font-label-size)] font-bold text-[var(--color-ink-strong)]">
            结束日期（可选）
          </span>
          <input
            v-model="endDate"
            type="date"
            :min="startDate || undefined"
            class="min-h-11 rounded-xl border border-[#d7dcea] bg-white px-3 py-2 text-[var(--font-label-size)] text-[var(--color-ink-strong)]"
            data-edit-input="end-date"
            aria-label="选择旅行结束日期（可选）"
          />
        </label>

        <p
          v-if="rangeErrorMessage"
          class="text-[var(--font-label-size)] font-bold text-[var(--color-destructive)]"
          role="alert"
          data-edit-error="range"
        >
          {{ rangeErrorMessage }}
        </p>

        <p
          v-if="conflictingDates.length > 0"
          class="text-[var(--font-label-size)] font-bold text-[var(--color-destructive)]"
          role="alert"
          data-edit-warning="date-conflict"
        >
          与已有记录 {{ conflictingDates.join('、') }} 存在日期重叠
        </p>

        <label class="grid gap-1">
          <span class="text-[var(--font-label-size)] font-bold text-[var(--color-ink-strong)]">
            备注
          </span>
          <textarea
            v-model="notes"
            maxlength="1000"
            rows="3"
            class="min-h-24 resize-none rounded-xl border border-[#d7dcea] bg-white px-3 py-2 text-[var(--font-label-size)] text-[var(--color-ink-strong)]"
            data-edit-input="notes"
            aria-label="旅行备注"
          />
        </label>

        <p
          v-if="notesErrorMessage"
          class="text-[var(--font-label-size)] font-bold text-[var(--color-destructive)]"
          role="alert"
          data-edit-error="notes"
        >
          {{ notesErrorMessage }}
        </p>

        <div class="grid gap-1">
          <span class="text-[var(--font-label-size)] font-bold text-[var(--color-ink-strong)]">
            标签
          </span>
          <TagInput
            :tags="tags"
            @update:tags="tags = $event"
          />
        </div>

        <div class="grid grid-cols-[1fr_auto] gap-3 pt-1">
          <button
            type="submit"
            class="min-h-11 rounded-full border border-[#f4d7e4] bg-[linear-gradient(135deg,rgba(255,232,242,0.96),rgba(255,246,250,0.96))] px-4 py-2 text-[var(--font-label-size)] font-bold text-[var(--color-accent-strong)] shadow-[0_14px_28px_rgba(244,143,177,0.34)] transition-all duration-300 ease-out hover:scale-105 hover:-translate-y-1 active:scale-95 disabled:cursor-not-allowed disabled:opacity-55"
            :disabled="!isValid || isSubmitting"
            data-edit-submit="true"
            aria-label="保存修改"
          >
            保存修改
          </button>

          <button
            type="button"
            class="min-h-11 rounded-full border border-[#d7dcea] bg-white/80 px-4 py-2 text-[var(--font-label-size)] font-bold text-[var(--color-ink-muted)] transition-all duration-300 ease-out hover:scale-105 active:scale-95"
            data-edit-cancel="true"
            @click="emit('cancel')"
          >
            取消
          </button>
        </div>
      </form>
    </template>

    <style scoped>
    @media (prefers-reduced-motion: reduce) {
      button {
        transform: none !important;
        transition: none !important;
      }
    }
    </style>
    ```

    运行测试（GREEN）：`pnpm --filter @trip-map/web test -- TimelineEditForm`
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- TimelineEditForm</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/timeline/TimelineEditForm.vue 存在且包含 `defineProps` 和 `defineEmits`
    - apps/web/src/components/timeline/TimelineEditForm.spec.ts 存在且包含至少 8 个测试用例
    - `pnpm --filter @trip-map/web test -- TimelineEditForm` 全部通过
    - 组件使用 `data-region="timeline-edit-form"` 属性
    - 组件 import TagInput from './TagInput.vue'
    - 组件 emit 事件名为 `submit` 和 `cancel`
    - submit payload 类型为 UpdateTravelRecordRequest
    - 日期输入框使用 `data-edit-input="start-date"` 和 `data-edit-input="end-date"`
    - 备注 textarea 使用 `data-edit-input="notes"`
    - 保存按钮使用 `data-edit-submit="true"`
    - 取消按钮使用 `data-edit-cancel="true"`
    - 日期冲突警告使用 `data-edit-warning="date-conflict"`
    - 验证错误使用 `data-edit-error="range"` 和 `data-edit-error="notes"`
  </acceptance_criteria>
  <done>TimelineEditForm 组件实现完成，8+ 测试用例全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → form state | 用户输入的日期、备注、标签进入组件 ref |
| form state → store | 编辑表单提交数据传入 store.updateRecord |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | notes textarea | mitigate | 备注最长 1000 字符限制（前端 + 后端双重校验），trim 后存储 |
| T-38-02 | Tampering | tags input | mitigate | 前端限制最多 10 个标签、每个最长 20 字符、去重 + trim，后端兜底清洗 |
| T-38-03 | Information Disclosure | date conflict warning | accept | 冲突信息仅显示日期范围，不暴露其他记录的备注/标签等敏感数据 |
| T-38-04 | Denial of Service | notes length | mitigate | 前端 textarea maxlength=1000，后端验证 notes <= 1000 字符 |
</threat_model>

<verification>
1. `pnpm --filter @trip-map/web test` — 全部测试通过
2. `pnpm --filter @trip-map/web typecheck` — 类型检查通过（vue-tsc --noEmit）
3. 所有新文件遵循 Vue 3 Composition API + `<script setup lang="ts">` 规范
4. 所有新组件使用 Tailwind CSS utility classes，无自定义 CSS（除 reduced-motion）
</verification>

<success_criteria>
- date-conflict.ts 导出 checkDateConflict 函数，8+ 测试通过
- TagInput.vue 组件实现完成，10+ 测试通过，支持回车/逗号/失焦添加标签
- TimelineEditForm.vue 组件实现完成，8+ 测试通过，集成 TagInput 和 checkDateConflict
- 所有组件使用 data-* 属性标识关键元素（便于后续测试和 Phase 39 集成）
- 无新增 npm 依赖
</success_criteria>

<output>
After completion, create `.planning/phases/38-ui/38-01-SUMMARY.md`
</output>

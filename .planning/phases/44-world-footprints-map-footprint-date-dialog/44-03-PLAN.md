---
phase: "44-world-footprints-map-footprint-date-dialog"
plan: "03"
type: execute
wave: 2
depends_on: ["44-01", "44-02"]
files_modified:
  - "apps/web/src/components/map-popup/PointSummaryCard.vue"
  - "apps/web/src/components/map-popup/PointSummaryCard.spec.ts"
  - "apps/web/src/components/map-popup/MapContextPopup.vue"
  - "apps/web/src/components/map-popup/MapContextPopup.spec.ts"
autonomous: true
requirements:
  - MAP-03
  - MAP-04
  - MAP-05
  - MAP-06
must_haves:
  truths:
    - "用户点击真实地点后，popup 始终展示地点名称、类型标签、地区信息和 留下足迹 CTA"
    - "已保存地点 popup 不展示过往旅行记录、不显示 再留一次足迹 或 再记一次去访"
    - "不可保存地点仍显示 disabled 留下足迹，并在 popup 内解释原因"
  artifacts:
    - path: "apps/web/src/components/map-popup/PointSummaryCard.vue"
      provides: "unified place-information popup card"
    - path: "apps/web/src/components/map-popup/MapContextPopup.vue"
      provides: "anchored non-modal popup shell that emits leaveFootprint"
  key_links:
    - from: "PointSummaryCard.vue"
      to: "LeafletMapStage.vue"
      via: "leaveFootprint event propagated by MapContextPopup"
      pattern: "leaveFootprint"
    - from: "PointSummaryCard.vue"
      to: "Phase 44 UI hooks"
      via: "data-footprint-cta and unavailable reason"
      pattern: "data-footprint-cta=\"true\""
---

<objective>
把地图 popup 从旧的旅行记录管理面板改成统一地点信息卡片，并把 `留下足迹` 变成打开独立日期 Dialog 的单一入口事件。

Purpose: 满足 MAP-03/MAP-04/MAP-05/MAP-06 与 D-05 到 D-08、D-18 到 D-21，清除 inline date form 和 saved-place history 分支。
Output: `PointSummaryCard` 纯地点卡片、`MapContextPopup` 事件转发、focused specs 转绿。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-CONTEXT.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-RESEARCH.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-VALIDATION.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-PATTERNS.md
@apps/web/src/components/map-popup/PointSummaryCard.vue
@apps/web/src/components/map-popup/PointSummaryCard.spec.ts
@apps/web/src/components/map-popup/MapContextPopup.vue
@apps/web/src/components/map-popup/MapContextPopup.spec.ts
@apps/web/src/assets/v8/characters/map-popup-girl.webp

<interfaces>
New popup event:
```ts
const emit = defineEmits<{
  confirmCandidate: [candidate: GeoCityCandidate]
  continueFallback: []
  leaveFootprint: []
}>()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 将 PointSummaryCard 改为统一地点信息卡片</name>
  <files>apps/web/src/components/map-popup/PointSummaryCard.vue</files>
  <read_first>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-CONTEXT.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-PATTERNS.md</file>
    <file>apps/web/src/components/map-popup/PointSummaryCard.vue</file>
    <file>apps/web/src/components/map-popup/TripDateForm.vue</file>
    <file>apps/web/src/components/map-popup/PopupTripRecord.vue</file>
    <file>apps/web/src/assets/v8/characters/map-popup-girl.webp</file>
  </read_first>
  <action>
    In `PointSummaryCard.vue`, remove imports/usages of `TripDateForm`, `PopupTripRecord`, `useMapPointsStore`, `isFormExpanded`, `openTripDateForm`, `handleTripFormSubmit`, `handleTripFormCancel`, `buildTimelineEntries`, `data-record-again`, and `data-region="trip-date-form-wrapper"`. Add `leaveFootprint: []` to emits. For non-candidate surfaces, render one real place card root `data-region="point-summary-card"` with `:data-summary-mode="surface.mode"`, a badge text `识别结果` for `detected-preview` and `查看地点` for `view`, the real place name as the largest heading, `data-place-type-label="true"`, `data-place-subtitle="true"`, and a button `data-footprint-cta="true"` whose text is exactly `留下足迹`. If `isSaved` is true, render `这里已经留下过足迹`. If `!isIlluminatable`, keep the CTA visible but disabled and render `data-footprint-unavailable-reason` with exact text `已识别到这个地点，但当前数据还不满足保存足迹的条件。`. Clicking an enabled non-pending CTA emits `leaveFootprint` and does not emit date payload.
  </action>
  <acceptance_criteria>
    - `rg -n "data-region=\\\"point-summary-card\\\"|data-place-type-label=\\\"true\\\"|data-place-subtitle=\\\"true\\\"|data-footprint-cta=\\\"true\\\"|leaveFootprint|这里已经留下过足迹|已识别到这个地点，但当前数据还不满足保存足迹的条件。" apps/web/src/components/map-popup/PointSummaryCard.vue` finds all required hooks/copy.
    - `rg -n "TripDateForm|PopupTripRecord|useMapPointsStore|isFormExpanded|openTripDateForm|handleTripFormSubmit|data-record-again|trip-date-form-wrapper|再留一次足迹|再记一次去访|保存去访|取消点亮|已点亮|点亮" apps/web/src/components/map-popup/PointSummaryCard.vue` returns no lines.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "data-footprint-cta=\\\"true\\\"|leaveFootprint|data-footprint-unavailable-reason|这里已经留下过足迹" apps/web/src/components/map-popup/PointSummaryCard.vue</automated>
  </verify>
  <done>`PointSummaryCard` 不再管理旅行记录历史或日期表单，只展示真实地点信息与统一 `留下足迹` 入口。</done>
</task>

<task type="auto">
  <name>Task 2: 让 MapContextPopup 转发 leaveFootprint 事件</name>
  <files>apps/web/src/components/map-popup/MapContextPopup.vue</files>
  <read_first>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-PATTERNS.md</file>
    <file>apps/web/src/components/map-popup/MapContextPopup.vue</file>
    <file>apps/web/src/components/map-popup/PointSummaryCard.vue</file>
  </read_first>
  <action>
    In `MapContextPopup.vue`, replace the old `illuminate: [payload: { startDate: string | null; endDate: string | null }]` emit with `leaveFootprint: []`. Keep `confirmCandidate` and `continueFallback` unchanged. In the template, pass `@leave-footprint="emit('leaveFootprint')"` from `PointSummaryCard`. Preserve the existing `<aside role="dialog" aria-modal="false" :aria-labelledby="popupTitleId">`, `focusEntryPoint()`, `getPopupElement()`, and popup anchoring props.
  </action>
  <acceptance_criteria>
    - `rg -n "leaveFootprint: \\[\\]|@leave-footprint=\\\"emit\\('leaveFootprint'\\)\\\"" apps/web/src/components/map-popup/MapContextPopup.vue` finds the event contract.
    - `rg -n "illuminate: \\[payload|@illuminate" apps/web/src/components/map-popup/MapContextPopup.vue` returns no lines.
    - `rg -n "role=\\\"dialog\\\"|aria-modal=\\\"false\\\"|defineExpose\\(\\{ getPopupElement \\}\\)" apps/web/src/components/map-popup/MapContextPopup.vue` still finds popup accessibility/expose behavior.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "leaveFootprint: \\[\\]|role=\\\"dialog\\\"|aria-modal=\\\"false\\\"" apps/web/src/components/map-popup/MapContextPopup.vue</automated>
  </verify>
  <done>`MapContextPopup` 保持 anchored popup 语义，并将地点卡片 CTA 转发为无 payload 的 `leaveFootprint` 事件。</done>
</task>

<task type="auto">
  <name>Task 3: 更新 popup focused specs 并转绿</name>
  <files>apps/web/src/components/map-popup/PointSummaryCard.spec.ts, apps/web/src/components/map-popup/MapContextPopup.spec.ts</files>
  <read_first>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-VALIDATION.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-UI-SPEC.md</file>
    <file>apps/web/src/components/map-popup/PointSummaryCard.spec.ts</file>
    <file>apps/web/src/components/map-popup/MapContextPopup.spec.ts</file>
    <file>apps/web/src/components/map-popup/PointSummaryCard.vue</file>
    <file>apps/web/src/components/map-popup/MapContextPopup.vue</file>
  </read_first>
  <action>
    Make `PointSummaryCard.spec.ts` and `MapContextPopup.spec.ts` pass against the new contract from Tasks 1-2. Keep tests asserting: saved popup text `这里已经留下过足迹`, no `PopupTripRecord`, no inline `TripDateForm`, disabled unavailable CTA with `data-footprint-unavailable-reason`, and `leaveFootprint` propagation. Do not reintroduce old strings `再留一次足迹`, `再记一次去访`, `点亮`, `已点亮`, `保存去访`, or `取消点亮` except inside negative assertions.
  </action>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts` exits 0.
    - `rg -n "PopupTripRecord|TripDateForm|data-record-again|再留一次足迹|再记一次去访" apps/web/src/components/map-popup/PointSummaryCard.spec.ts apps/web/src/components/map-popup/MapContextPopup.spec.ts` appears only in negative assertions.
  </acceptance_criteria>
  <verify>
    <automated>./node_modules/.bin/vitest run src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts</automated>
  </verify>
  <done>Popup focused specs 全部通过，MAP-03/MAP-04/MAP-05/MAP-06 的 popup 表面契约转绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recognized place data -> popup DOM | canonical place name/type/region 展示给用户 |
| popup CTA -> date dialog controller | 用户从地点卡片进入会保存账号数据的日期流程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-08 | Tampering | `PointSummaryCard.vue` unsupported place CTA | mitigate | `!isIlluminatable` 时 CTA disabled 且显示 `data-footprint-unavailable-reason`，防止不可保存地点进入提交流程 |
| T-44-09 | Information Disclosure | `PointSummaryCard.vue` saved history removal | mitigate | 移除 popup 中的 trip rows/latest history，地图 popup 不暴露过往记录管理面板 |
| T-44-10 | Spoofing | popup place identity | mitigate | 地点名称、类型、地区必须来自 `SummarySurfaceState.point`，禁止用坐标作为 canonical place 主标题 |
| T-44-11 | Denial of Service | `MapContextPopup.vue` focus | mitigate | 保留 `role="dialog"`, `aria-modal="false"`, labelled hidden title, and focus entry behavior |

No high-severity threats identified for this popup refactor plan. Relevant mitigations cover authentication entry clarity, data integrity around unsupported places, and focus/state safety.
</threat_model>

<verification>
Focused:
- `./node_modules/.bin/vitest run src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts`

Grep:
- `rg -n "TripDateForm|PopupTripRecord|data-record-again|再留一次足迹|再记一次去访|保存去访|取消点亮|已点亮|点亮" apps/web/src/components/map-popup/PointSummaryCard.vue` must return no lines.
</verification>

<success_criteria>
MAP-03/MAP-04/MAP-05/MAP-06 popup surface covered: real place info is always visible, saved places use the same layout, old record history UI is gone, and unavailable/login/pending entry states are explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/44-world-footprints-map-footprint-date-dialog/44-03-SUMMARY.md`
</output>

---
phase: 29-timeline-page-and-account-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/main.ts
  - apps/web/src/App.vue
  - apps/web/src/router/index.ts
  - apps/web/src/views/MapHomeView.vue
  - apps/web/src/views/TimelinePageView.vue
  - apps/web/src/components/auth/AuthTopbarControl.vue
autonomous: true
requirements:
  - TRIP-04
tags:
  - web
  - routing
  - auth-menu
  - shell
user_setup: []
must_haves:
  truths:
    - "web 端新增 `vue-router@^4`，并在 `apps/web/src/router/index.ts` 中使用 `createWebHashHistory()` 建立 `#/` 与 `#/timeline` 两个 same-tab route。"
    - "`apps/web/src/App.vue` 保留现有顶栏、notice、auth dialog、restore/refresh 逻辑，但把主内容区改为 `<RouterView />` 驱动。"
    - "`apps/web/src/views/MapHomeView.vue` 承接现有地图主舞台，确保切到 timeline route 时不再渲染 `LeafletMapStage`。"
    - "`apps/web/src/components/auth/AuthTopbarControl.vue` 的已登录菜单新增 `data-auth-menu-item=\"timeline\"` 入口，点击后先关闭菜单再导航到 `'/timeline'`。"
  artifacts:
    - path: "apps/web/src/router/index.ts"
      provides: "Phase 29 router with map + timeline routes"
      contains: "createWebHashHistory"
    - path: "apps/web/src/App.vue"
      provides: "Persistent app shell that renders RouterView"
      contains: "RouterView"
    - path: "apps/web/src/views/MapHomeView.vue"
      provides: "Map-only route view extracted from App shell"
      contains: "LeafletMapStage"
    - path: "apps/web/src/components/auth/AuthTopbarControl.vue"
      provides: "Authenticated menu timeline entry"
      contains: "data-auth-menu-item=\"timeline\""
  key_links:
    - from: "apps/web/src/main.ts"
      to: "apps/web/src/router/index.ts"
      via: "main app bootstrap installs router"
      pattern: "use\\(router\\)"
    - from: "apps/web/src/router/index.ts"
      to: "apps/web/src/views/TimelinePageView.vue"
      via: "timeline route component"
      pattern: "path:\\s*'/timeline'"
    - from: "apps/web/src/components/auth/AuthTopbarControl.vue"
      to: "apps/web/src/router/index.ts"
      via: "menu CTA pushes timeline route"
      pattern: "router\\.push\\('/timeline'\\)"
---

<objective>
Phase 29 Wave 1：先把“独立页面”所需的最小路由骨架和入口打通，确保时间轴不再被实现成地图舞台里的内联模块。

本计划只解决两件事：
1. 给当前单视图 web app 补一个可复用的路由层，并把地图舞台抽成独立 route view；
2. 在已登录用户名菜单里增加「时间轴」入口，完成同一 tab 内导航。

Output: router 基础设施、可工作的 `#/timeline` 占位页、以及用户名菜单中的时间轴入口。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-timeline-page-and-account-entry/29-CONTEXT.md
@.planning/phases/29-timeline-page-and-account-entry/29-RESEARCH.md

<interfaces>
<!-- Source: apps/web/src/main.ts [VERIFIED 2026-04-23] -->

```typescript
createApp(App).use(createPinia()).mount('#app')
```

<!-- Source: apps/web/src/App.vue [VERIFIED 2026-04-23] -->

```vue
<header>...<AuthTopbarControl /></header>
<section data-region="map-shell">
  <LeafletMapStage class="min-h-0 flex-1" />
  <AuthRestoreOverlay :visible="status === 'restoring'" />
</section>
```

<!-- Source: apps/web/src/components/auth/AuthTopbarControl.vue [VERIFIED 2026-04-23] -->

```vue
<button
  type="button"
  data-auth-menu-item="logout"
  role="menuitem"
  @click="handleLogout"
>
  退出登录
</button>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 建立 hash router，并把地图主舞台抽成独立 route view</name>
  <files>
    apps/web/package.json
    apps/web/src/main.ts
    apps/web/src/App.vue
    apps/web/src/router/index.ts
    apps/web/src/views/MapHomeView.vue
    apps/web/src/views/TimelinePageView.vue
  </files>
  <read_first>
    - apps/web/package.json（确认当前 runtime dependencies 没有 `vue-router`）
    - apps/web/src/main.ts（当前只有 createPinia，没有 router）
    - apps/web/src/App.vue（现有 restoreSession、notice、顶栏和地图主舞台都在根组件里）
    - apps/web/vite.config.ts（确认没有额外部署或 router 特殊配置）
    - .planning/phases/29-timeline-page-and-account-entry/29-CONTEXT.md（D-03 / D-04：独立页面 + 同一 tab 路由切换）
    - .planning/phases/29-timeline-page-and-account-entry/29-RESEARCH.md（推荐 `vue-router + createWebHashHistory()`，避免无 rewrite 的硬刷新风险）
  </read_first>
  <action>
    在 web 端补齐标准路由层，但保持改动最小：

    1. 在 `apps/web/package.json` 的 `dependencies` 中新增 `vue-router@^4`。

    2. 新建 `apps/web/src/router/index.ts`，用以下固定结构定义路由：
       - 使用 `createRouter` + `createWebHashHistory()`
       - `path: '/'`, `name: 'map-home'`, `component: MapHomeView`
       - `path: '/timeline'`, `name: 'timeline'`, `component: TimelinePageView`
       - `path: '/:pathMatch(.*)*'` 统一 redirect 到 `'/'`

    3. 更新 `apps/web/src/main.ts`：
       - import 新建的 `router`
       - 改成 `createApp(App).use(createPinia()).use(router).mount('#app')`

    4. 从 `apps/web/src/App.vue` 中把当前 `<section data-region="map-shell">...</section>` 抽成新文件 `apps/web/src/views/MapHomeView.vue`；
       - `MapHomeView.vue` 必须继续渲染 `LeafletMapStage` 和 `AuthRestoreOverlay`
       - `MapHomeView.vue` 接收 `status` 或在内部读取同一个 auth store，避免 root app 复制 restore overlay 逻辑

    5. 修改 `apps/web/src/App.vue`：
       - 保留当前 fixed 顶栏、notice、`AuthDialog`、`LocalImportDecisionDialog`、restore/refresh 逻辑和 Kawaii 背景层
       - 把原来直接渲染地图主舞台的位置替换为 `<RouterView />`
       - 不要把 `LeafletMapStage` 继续保留在 root app 内

    6. 新建一个最小占位版 `apps/web/src/views/TimelinePageView.vue`，确保路由在 Wave 1 即可通过：
       - 页面内先提供标题“时间轴”
       - 提供一个返回地图的 `RouterLink` / 导航按钮到 `'/'`
       - 主内容使用 `data-route-view="timeline"` 标记
       - 该占位页不渲染 `LeafletMapStage`
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/router/index.ts` 存在，且 grep `createWebHashHistory` 命中 1 行
    - `apps/web/src/router/index.ts` 中 grep `path: '/timeline'` 命中 1 行
    - `apps/web/src/App.vue` 中 grep `RouterView` 命中至少 1 行
    - `apps/web/src/App.vue` 中 grep `LeafletMapStage` 结果为 0 行（地图舞台已移出根壳）
    - `apps/web/src/views/MapHomeView.vue` 中 grep `LeafletMapStage` 命中至少 1 行
    - `apps/web/src/views/TimelinePageView.vue` 中 grep `data-route-view=\"timeline\"` 命中 1 行
    - `pnpm --filter @trip-map/web typecheck` 退出码为 0
  </acceptance_criteria>
  <done>web 端已经具备 `#/timeline` 独立路由，根壳改成 RouterView 驱动，地图页被抽为单独 view。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在已登录用户名菜单中增加时间轴入口并接通路由导航</name>
  <files>
    apps/web/src/components/auth/AuthTopbarControl.vue
  </files>
  <read_first>
    - apps/web/src/components/auth/AuthTopbarControl.vue（现有菜单结构、关闭菜单逻辑、按钮视觉 token）
    - apps/web/src/router/index.ts（Task 1 刚建立的 route name / path）
    - .planning/phases/29-timeline-page-and-account-entry/29-CONTEXT.md（D-01 / D-02 / D-04）
    - .planning/phases/29-timeline-page-and-account-entry/29-RESEARCH.md（入口应该放在菜单内，并在导航前关闭菜单）
  </read_first>
  <action>
    在 `apps/web/src/components/auth/AuthTopbarControl.vue` 中为 authenticated 菜单新增“时间轴”入口：

    1. 引入 `useRouter()`，创建 `const router = useRouter()`。

    2. 新增 `handleNavigateToTimeline()`：
       - 先调用 `closeMenu()`
       - 再执行 `void router.push('/timeline')`

    3. 在当前 `data-auth-menu-item="logout"` 按钮之前，插入新的 `role="menuitem"` 按钮：
       - `data-auth-menu-item="timeline"`
       - 文案固定为 `时间轴`
       - 保持 pill 形状，视觉上与退出登录同宽同高
       - 使用非 destructive 色板（不要复用 logout 的 destructive 文案色）
       - 带一个小图标容器或前导 icon span（`aria-hidden="true"`），满足 D-01 的“小图标”要求

    4. 不修改 anonymous 登录按钮，不改用户名 / 邮箱信息卡布局，不破坏点击外部关闭和 `Escape` 关闭行为。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep `useRouter` `apps/web/src/components/auth/AuthTopbarControl.vue` 命中至少 1 行
    - grep `data-auth-menu-item=\"timeline\"` `apps/web/src/components/auth/AuthTopbarControl.vue` 命中 1 行
    - grep `router.push('/timeline')` `apps/web/src/components/auth/AuthTopbarControl.vue` 命中 1 行
    - grep `closeMenu()` `apps/web/src/components/auth/AuthTopbarControl.vue` 在时间轴导航 handler 中命中至少 1 行
    - `apps/web/src/components/auth/AuthTopbarControl.vue` 仍保留 `data-auth-menu-item=\"logout\"`
    - `pnpm --filter @trip-map/web typecheck` 退出码为 0
  </acceptance_criteria>
  <done>已登录用户的用户名菜单中出现时间轴入口，点击后会关闭菜单并进入 `#/timeline`。</done>
</task>

</tasks>

<verification>
Run `pnpm --filter @trip-map/web typecheck` and verify both `#/` and `#/timeline` routes compile. Manually confirm authenticated menu now includes a timeline CTA.
</verification>

<success_criteria>
- 独立 timeline route 存在并可在同一 tab 内访问。
- 时间轴入口出现在用户名菜单，而不是地图舞台内联按钮。
- 切到 `#/timeline` 后主内容区不再渲染 `LeafletMapStage`。
</success_criteria>

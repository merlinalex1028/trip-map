---
phase: 29-timeline-page-and-account-entry
plan: 03
type: execute
wave: 2
depends_on:
  - 29-01
  - 29-02
files_modified:
  - apps/web/src/views/TimelinePageView.vue
  - apps/web/src/components/timeline/TimelineVisitCard.vue
autonomous: true
requirements:
  - TRIP-04
  - TRIP-05
tags:
  - web
  - timeline-ui
  - route-view
  - auth
user_setup: []
must_haves:
  truths:
    - "`TimelinePageView.vue` 是真正的独立 route view，不渲染 `LeafletMapStage`，而是消费 `timelineEntries` 展示旅行历史。"
    - "已登录用户可以在时间轴页看到逐条旅行记录；匿名用户访问 `#/timeline` 时看到登录 CTA，而不是空白页或报错。"
    - "每条卡片展示地点名称、旅行日期、国家/地区信息以及同地点去访次数标记，满足 D-06。"
    - "同地点多次去访在页面上必须拆成多张卡片，每张卡片都能表达 `visitOrdinal` / `visitCount`。"
  artifacts:
    - path: "apps/web/src/views/TimelinePageView.vue"
      provides: "Standalone timeline route view"
      contains: "timelineEntries"
    - path: "apps/web/src/components/timeline/TimelineVisitCard.vue"
      provides: "Per-trip card UI for timeline page"
      contains: "日期未知"
  key_links:
    - from: "apps/web/src/views/TimelinePageView.vue"
      to: "apps/web/src/stores/map-points.ts"
      via: "page consumes store timelineEntries selector"
      pattern: "timelineEntries"
    - from: "apps/web/src/views/TimelinePageView.vue"
      to: "apps/web/src/components/timeline/TimelineVisitCard.vue"
      via: "timeline page renders one card per entry"
      pattern: "TimelineVisitCard"
---

<objective>
Phase 29 Wave 2：交付真正可浏览的独立时间轴页面 UI，让第 29 阶段从“路由占位”升级为“可用页面”。

本计划负责：
1. 把占位时间轴页替换为真实列表页；
2. 渲染逐条旅行卡片和多次去访标记；
3. 处理 restoring / anonymous / empty / populated 四种页面状态。

Output: 可从顶栏进入的独立 timeline 页面，以及可复用的时间轴卡片组件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-timeline-page-and-account-entry/29-CONTEXT.md
@.planning/phases/29-timeline-page-and-account-entry/29-RESEARCH.md
@.planning/phases/29-timeline-page-and-account-entry/29-01-PLAN.md
@.planning/phases/29-timeline-page-and-account-entry/29-02-PLAN.md

<interfaces>
<!-- Source: apps/web/src/stores/auth-session.ts [VERIFIED 2026-04-23] -->

```typescript
const status = shallowRef<AuthStatus>('restoring')
const currentUser = shallowRef<AuthUser | null>(null)

function openAuthModal(mode: AuthMode = 'login') {
  authMode.value = mode
  isAuthModalOpen.value = true
}
```

<!-- Source: apps/web/src/stores/map-points.ts after 29-02 [EXPECTED] -->

```typescript
timelineEntries: ComputedRef<TimelineEntry[]>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 把 TimelinePageView 从占位页升级为真实时间轴页面</name>
  <files>
    apps/web/src/views/TimelinePageView.vue
  </files>
  <read_first>
    - apps/web/src/views/TimelinePageView.vue（29-01 创建的占位 route view）
    - apps/web/src/stores/auth-session.ts（`status` / `currentUser` / `openAuthModal`）
    - apps/web/src/stores/map-points.ts（`timelineEntries` selector）
    - apps/web/src/App.vue（现有页面壳和间距 token）
    - .planning/phases/29-timeline-page-and-account-entry/29-CONTEXT.md（D-03 ~ D-07）
    - .planning/phases/29-timeline-page-and-account-entry/29-RESEARCH.md（独立页面、未知日期后置、匿名用户 CTA）
  </read_first>
  <action>
    在 `apps/web/src/views/TimelinePageView.vue` 中实现真实页面，而不是保留占位内容：

    1. 使用 `useAuthSessionStore()` 和 `useMapPointsStore()`，读取：
       - `status`
       - `currentUser`
       - `timelineEntries`
       - `openAuthModal`

    2. 页面头部必须包含：
       - 主标题：`时间轴`
       - 一段短说明，明确这里展示个人旅行历史
       - 返回地图的 `RouterLink` / pill button 到 `'/'`

    3. 明确处理四种状态：
       - `status === 'restoring'`：显示 loading / skeleton panel
       - `status !== 'authenticated' || currentUser === null`：显示登录说明与 `openAuthModal('login')` CTA
       - `status === 'authenticated' && timelineEntries.length === 0`：显示空状态文案
       - `status === 'authenticated' && timelineEntries.length > 0`：渲染时间轴卡片列表

    4. 列表渲染要求：
       - 使用 `v-for` 按 `timelineEntries` 顺序渲染
       - 列表容器使用独立页面布局，不要复用地图 popup 的窄浮层结构
       - 页面本身不 import / render `LeafletMapStage`
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep `useAuthSessionStore` `apps/web/src/views/TimelinePageView.vue` 命中至少 1 行
    - grep `useMapPointsStore` `apps/web/src/views/TimelinePageView.vue` 命中至少 1 行
    - grep `openAuthModal('login')` `apps/web/src/views/TimelinePageView.vue` 命中 1 行
    - grep `timelineEntries` `apps/web/src/views/TimelinePageView.vue` 命中至少 2 行
    - grep `LeafletMapStage` `apps/web/src/views/TimelinePageView.vue` 结果为 0 行
    - grep `RouterLink` `apps/web/src/views/TimelinePageView.vue` 命中至少 1 行
    - `pnpm --filter @trip-map/web typecheck` 退出码为 0
  </acceptance_criteria>
  <done>时间轴 route view 已经从占位页升级成可用页面，并能正确处理 restoring / anonymous / empty / populated 状态。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 新建 TimelineVisitCard 组件，稳定展示日期、地点和多次去访标记</name>
  <files>
    apps/web/src/components/timeline/TimelineVisitCard.vue
    apps/web/src/views/TimelinePageView.vue
  </files>
  <read_first>
    - apps/web/src/components/map-popup/PointSummaryCard.vue（现有 Kawaii 卡片风格与信息层次）
    - apps/web/src/App.vue（当前页面壳与色彩 token）
    - apps/web/src/services/timeline.ts（TimelineEntry 字段）
    - .planning/phases/29-timeline-page-and-account-entry/29-CONTEXT.md（D-06：卡片内容；D-07：同地点多次去访拆分）
  </read_first>
  <action>
    新建 `apps/web/src/components/timeline/TimelineVisitCard.vue` 并在 `TimelinePageView.vue` 中使用它。卡片必须按以下固定信息层次渲染：

    1. 标题区：
       - 主标题：`displayName`
       - 类型 pill：`typeLabel`
       - 副标题：优先 `subtitle`，否则回退 `parentLabel`

    2. 日期区：
       - 若 `startDate !== null && endDate !== null`：显示 `startDate - endDate`
       - 若 `startDate !== null && endDate === null`：显示 `startDate`
       - 若 `startDate === null`：显示固定文案 `日期未知`

    3. 去访次数区：
       - 若 `visitCount > 1`：显示 `第 {visitOrdinal} 次 / 共 {visitCount} 次`
       - 否则显示 `首次记录`

    4. 视觉要求：
       - 采用独立页面卡片，不做 popup 式 absolute/floating 布局
       - 继续沿用现有奶油白 / 白色半透明 / rounded 大圆角 / pill badge 风格
       - 保证移动端一列、桌面端仍以垂直时间序列为主，不做复杂 masonry

    5. 在 `TimelinePageView.vue` 中把 `timelineEntries` 逐条传给 `TimelineVisitCard`，确保“一条 entry 一张卡”。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/timeline/TimelineVisitCard.vue` 存在
    - grep `日期未知` `apps/web/src/components/timeline/TimelineVisitCard.vue` 命中 1 行
    - grep `首次记录` `apps/web/src/components/timeline/TimelineVisitCard.vue` 命中 1 行
    - grep `第 {{` `apps/web/src/components/timeline/TimelineVisitCard.vue` 或等价模板命中至少 1 行（存在 visitOrdinal / visitCount 文案）
    - grep `TimelineVisitCard` `apps/web/src/views/TimelinePageView.vue` 命中至少 1 行
    - `pnpm --filter @trip-map/web typecheck` 退出码为 0
  </acceptance_criteria>
  <done>时间轴页面已经用独立卡片展示逐条旅行，并能区分未知日期与同地点多次去访。</done>
</task>

</tasks>

<verification>
Run `pnpm --filter @trip-map/web typecheck`, then manually check `#/timeline` in authenticated and anonymous states.
</verification>

<success_criteria>
- `#/timeline` 成为真正的独立浏览页，而不是地图内联块。
- 页面能展示逐条旅行历史，并把同地点多次去访拆成多张卡片。
- 已知日期、未知日期、空状态和匿名状态都有清晰 UI。
</success_criteria>

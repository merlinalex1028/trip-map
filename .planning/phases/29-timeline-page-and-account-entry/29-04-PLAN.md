---
phase: 29-timeline-page-and-account-entry
plan: 04
type: execute
wave: 3
depends_on:
  - 29-01
  - 29-02
  - 29-03
files_modified:
  - apps/web/src/App.spec.ts
  - apps/web/src/components/auth/AuthTopbarControl.spec.ts
  - apps/web/src/views/TimelinePageView.spec.ts
  - apps/web/src/services/timeline.spec.ts
autonomous: true
requirements:
  - TRIP-04
  - TRIP-05
tags:
  - web
  - tests
  - routing
  - timeline
user_setup: []
must_haves:
  truths:
    - "测试回归显式覆盖：用户名菜单时间轴入口、same-tab route 切换、时间轴排序、多次去访拆分、匿名登录 CTA。"
    - "`App.spec.ts` 证明 map route 与 timeline route 渲染的是不同主内容区，而不是同一页拼接。"
    - "`TimelinePageView.spec.ts` 证明 timeline page 会渲染多条 visit card，并对未知日期显示固定文案。"
    - "`AuthTopbarControl.spec.ts` 证明时间轴入口只在 authenticated 菜单中出现，并触发 `router.push('/timeline')`。"
  artifacts:
    - path: "apps/web/src/components/auth/AuthTopbarControl.spec.ts"
      provides: "Menu entry navigation regression tests"
      contains: "navigates to timeline from the authenticated menu"
    - path: "apps/web/src/views/TimelinePageView.spec.ts"
      provides: "Timeline page state and card rendering regression tests"
      contains: "renders multiple visits for the same place as separate cards"
    - path: "apps/web/src/App.spec.ts"
      provides: "App-level route shell regression coverage"
      contains: "renders timeline route without LeafletMapStage"
  key_links:
    - from: "apps/web/src/components/auth/AuthTopbarControl.spec.ts"
      to: "apps/web/src/components/auth/AuthTopbarControl.vue"
      via: "menu CTA behavior regression"
      pattern: "data-auth-menu-item=\"timeline\""
    - from: "apps/web/src/views/TimelinePageView.spec.ts"
      to: "apps/web/src/views/TimelinePageView.vue"
      via: "timeline page state regression"
      pattern: "日期未知"
    - from: "apps/web/src/App.spec.ts"
      to: "apps/web/src/router/index.ts"
      via: "route shell regression"
      pattern: "/timeline"
---

<objective>
Phase 29 Wave 3：为独立时间轴页面补齐回归测试，锁定 TRIP-04 / TRIP-05 的关键交互与排序语义。

本计划的目标是把 Phase 29 最容易回归的点写进测试：
1. 顶栏菜单入口与路由切换；
2. 时间轴排序与多次去访拆分；
3. 匿名 / restoring / 空状态呈现。

Output: App、AuthTopbarControl、TimelinePageView 和 timeline helper 的回归测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-timeline-page-and-account-entry/29-RESEARCH.md
@.planning/phases/29-timeline-page-and-account-entry/29-01-PLAN.md
@.planning/phases/29-timeline-page-and-account-entry/29-02-PLAN.md
@.planning/phases/29-timeline-page-and-account-entry/29-03-PLAN.md

<interfaces>
<!-- Source: apps/web/vitest.config.ts [VERIFIED 2026-04-23] -->

```typescript
test: {
  environment: 'happy-dom',
  include: cliSpecFilters.length > 0 ? cliSpecFilters : ['src/**/*.spec.ts'],
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 为 AuthTopbarControl 增加时间轴入口与导航回归测试</name>
  <files>
    apps/web/src/components/auth/AuthTopbarControl.spec.ts
  </files>
  <read_first>
    - apps/web/src/components/auth/AuthTopbarControl.vue（新增 timeline menu item 的真实结构）
    - apps/web/src/stores/auth-session.spec.ts（现有 auth store mock pattern）
    - apps/web/vitest.config.ts（happy-dom 测试环境）
  </read_first>
  <action>
    新建 `apps/web/src/components/auth/AuthTopbarControl.spec.ts`，至少覆盖以下固定用例：

    1. `shows the timeline entry only for authenticated users`
       - anonymous 时看不到 `data-auth-menu-item="timeline"`
       - authenticated 且菜单打开后可看到该按钮

    2. `navigates to timeline from the authenticated menu`
       - 模拟点击 timeline menu item
       - 断言 `router.push('/timeline')` 被调用
       - 断言菜单随后关闭

    3. `keeps the logout action after the timeline entry`
       - 确认 `data-auth-menu-item="logout"` 仍存在

    测试里使用与仓库现有 spec 一致的 mount + mocked store/router 模式，不额外引入测试框架。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/components/auth/AuthTopbarControl.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/auth/AuthTopbarControl.spec.ts` 存在
    - grep `shows the timeline entry only for authenticated users` `apps/web/src/components/auth/AuthTopbarControl.spec.ts` 命中 1 行
    - grep `navigates to timeline from the authenticated menu` `apps/web/src/components/auth/AuthTopbarControl.spec.ts` 命中 1 行
    - grep `router.push` `apps/web/src/components/auth/AuthTopbarControl.spec.ts` 命中至少 1 行
    - `pnpm --filter @trip-map/web test -- src/components/auth/AuthTopbarControl.spec.ts` 退出码为 0
  </acceptance_criteria>
  <done>顶栏时间轴入口的显隐与导航行为已经被独立 spec 锁住。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 为 TimelinePageView 增加页面状态与多次去访渲染测试</name>
  <files>
    apps/web/src/views/TimelinePageView.spec.ts
    apps/web/src/services/timeline.spec.ts
  </files>
  <read_first>
    - apps/web/src/views/TimelinePageView.vue（真实页面状态分支）
    - apps/web/src/services/timeline.ts（排序与 visitOrdinal 规则）
    - apps/web/src/services/timeline.spec.ts（29-02 已有 unit tests）
  </read_first>
  <action>
    1. 新建 `apps/web/src/views/TimelinePageView.spec.ts`，至少覆盖：
       - `renders login CTA for anonymous visitors`
       - `renders empty state for authenticated users without records`
       - `renders multiple visits for the same place as separate cards`
       - `renders unknown-date label for records without trip dates`

    2. 若 `apps/web/src/services/timeline.spec.ts` 尚未覆盖 tie-break 或 visit badge 边界，再补一个固定用例：
       - `keeps equal-date entries stable by createdAt and id`

    3. 页面测试必须断言：
       - timeline page 不包含 `LeafletMapStage`
       - 列表里同地点两条记录会渲染两张卡片
       - 未知日期显示 `日期未知`
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/views/TimelinePageView.spec.ts src/services/timeline.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/views/TimelinePageView.spec.ts` 存在
    - grep `renders multiple visits for the same place as separate cards` `apps/web/src/views/TimelinePageView.spec.ts` 命中 1 行
    - grep `renders unknown-date label for records without trip dates` `apps/web/src/views/TimelinePageView.spec.ts` 命中 1 行
    - `apps/web/src/services/timeline.spec.ts` 中存在稳定排序或 tie-break 用例
    - `pnpm --filter @trip-map/web test -- src/views/TimelinePageView.spec.ts src/services/timeline.spec.ts` 退出码为 0
  </acceptance_criteria>
  <done>时间轴页面状态与时间排序语义已经被页面级和单元级测试覆盖。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 更新 App.spec，锁定 map route 与 timeline route 的根壳切换</name>
  <files>
    apps/web/src/App.spec.ts
  </files>
  <read_first>
    - apps/web/src/App.vue（RouterView 壳）
    - apps/web/src/App.spec.ts（现有 App mount / mocked auth bootstrap 测试模式）
    - apps/web/src/router/index.ts（route path 定义）
  </read_first>
  <action>
    更新 `apps/web/src/App.spec.ts`，新增或改写 route-level regression：

    1. `renders timeline route without LeafletMapStage`
       - 把 router 推到 `/timeline`
       - 断言时间轴页主内容存在
       - 断言 `LeafletMapStage` 不存在

    2. `keeps the shared topbar when switching between map and timeline routes`
       - 在 `/` 与 `/timeline` 两个 route 间切换
       - 断言固定顶栏和 `AuthTopbarControl` 都继续存在

    3. `renders map stage only on the map route`
       - `/` 时存在地图舞台
       - `/timeline` 时不存在地图舞台

    不要把 route regression 塞进其他 store spec；它应该停留在 App root 测试层。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/App.spec.ts src/components/auth/AuthTopbarControl.spec.ts src/views/TimelinePageView.spec.ts src/services/timeline.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `renders timeline route without LeafletMapStage` `apps/web/src/App.spec.ts` 命中 1 行
    - grep `keeps the shared topbar when switching between map and timeline routes` `apps/web/src/App.spec.ts` 命中 1 行
    - grep `renders map stage only on the map route` `apps/web/src/App.spec.ts` 命中 1 行
    - `pnpm --filter @trip-map/web test -- src/App.spec.ts src/components/auth/AuthTopbarControl.spec.ts src/views/TimelinePageView.spec.ts src/services/timeline.spec.ts` 退出码为 0
  </acceptance_criteria>
  <done>App 根壳的路由切换已经被测试锁定，能够防止时间轴页退化回地图内联模块。</done>
</task>

</tasks>

<verification>
Run `pnpm --filter @trip-map/web test -- src/App.spec.ts src/components/auth/AuthTopbarControl.spec.ts src/views/TimelinePageView.spec.ts src/services/timeline.spec.ts` and then `pnpm --filter @trip-map/web typecheck`.
</verification>

<success_criteria>
- TRIP-04 / TRIP-05 的关键路由与页面行为都有测试兜底。
- 路由切换、时间轴排序和多次去访拆分不会在后续 phase 中悄悄回归。
</success_criteria>

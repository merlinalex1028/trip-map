---
phase: "45-map-authoritative-coverage-expansion"
plan: "03"
type: execute
wave: 1
depends_on: ["45-01", "45-02"]
files_modified:
  - "apps/web/src/components/LeafletMapStage.vue"
  - "apps/web/src/components/LeafletMapStage.spec.ts"
  - "apps/web/src/components/map-popup/MapContextPopup.vue"
  - "apps/web/src/components/map-popup/MapContextPopup.spec.ts"
  - "apps/web/src/services/geometry-manifest.spec.ts"
autonomous: true
requirements:
  - COV-02
  - COV-03
must_haves:
  truths:
    - "The same availability result drives popup CTA enabled state, unavailable reason, and FootprintDateDialog entry."
    - "Blocked, fallback, and incomplete points never open the date dialog and never call record creation."
    - "Saveable Phase 45 samples have manifest-backed highlight coverage before the CTA can open the dialog."
    - "D-01: Frontend footprint guard and geometry manifest lookup are tested as real runtime breakpoints."
    - "D-02: Map popup tests use current summary surfaces and record-save wiring rather than dataset scans."
    - "D-05: Complete identity samples are wired through save/highlight plumbing first."
    - "D-07: Fallback region lookup never creates a FootprintPlaceSnapshot or authoritative payload."
    - "D-11: Blocked points keep a disabled 留下足迹 CTA and cannot enter FootprintDateDialog."
    - "D-12: Availability category/copy flows through MapContextPopup to PointSummaryCard."
    - "D-15: Highlight verification uses manifest lookup plus focused LeafletMapStage tests."
    - "D-16: Frontend failures name boundary id, category copy, and blocked dialog state."
  artifacts:
    - path: "apps/web/src/components/LeafletMapStage.vue"
      provides: "single-source availability wiring for CTA and dialog gate"
    - path: "apps/web/src/components/map-popup/MapContextPopup.vue"
      provides: "availability category/copy bridge into PointSummaryCard"
    - path: "apps/web/src/components/LeafletMapStage.spec.ts"
      provides: "integration assertions for blocked dialog entry and saveable highlight"
  key_links:
    - from: "apps/web/src/components/LeafletMapStage.vue"
      to: "apps/web/src/services/footprint-availability.ts"
      via: "getFootprintAvailability(summarySurfaceState)"
      pattern: "getFootprintAvailability"
    - from: "apps/web/src/components/LeafletMapStage.vue"
      to: "apps/web/src/components/map-popup/FootprintDateDialog.vue"
      via: "availability.snapshot assigned to footprintPlaceSnapshot"
      pattern: "availability\\.snapshot|footprintPlaceSnapshot"
---

<objective>
Wire the Phase 45 availability classifier through the real map popup, date-dialog guard, and manifest highlight assertions.

Purpose: implement D-01, D-02, D-05, D-07, D-11, D-12, D-15, and D-16 in the real running frontend chain without adding a coverage UI or generating fallback save payloads.
Output: `LeafletMapStage` and `MapContextPopup` use one availability result for disabled CTA, friendly reason, snapshot creation, and dialog entry.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-CONTEXT.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-RESEARCH.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-PATTERNS.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-UI-SPEC.md
@.planning/phases/45-map-authoritative-coverage-expansion/45-VALIDATION.md
@.planning/phases/44-world-footprints-map-footprint-date-dialog/44-05-SUMMARY.md
@apps/web/src/components/LeafletMapStage.vue
@apps/web/src/components/LeafletMapStage.spec.ts
@apps/web/src/components/map-popup/MapContextPopup.vue
@apps/web/src/components/map-popup/MapContextPopup.spec.ts
@apps/web/src/components/map-popup/PointSummaryCard.vue
@apps/web/src/components/map-popup/FootprintDateDialog.vue
@apps/web/src/services/footprint-availability.ts
@apps/web/src/services/geometry-manifest.ts
@apps/web/src/services/geometry-loader.ts
@apps/web/src/types/map-point.ts

<interfaces>
Availability result contract created by Plan 45-02:
```ts
type FootprintAvailability =
  | { saveable: true; reason: null; category: null; copy: null; snapshot: FootprintPlaceSnapshot }
  | { saveable: false; reason: FootprintBlockingReason; category: FootprintUnavailableCategory; copy: string; snapshot: null }
```

Phase 44 snapshot rule to preserve:
```ts
submitFootprintDate(payload) reads footprintPlaceSnapshot only; it does not read summarySurfaceState.
```
</interfaces>
</context>

<must_haves>
- COV-02: saveable samples must have manifest-backed `boundaryId` lookup and load/highlight path assertions.
- COV-03: blocked samples must show disabled CTA + friendly reason and never open `data-region="footprint-date-dialog"`.
- D-01: frontend execution checks the real breakpoints at frontend footprint guard and geometry manifest lookup.
- D-02: the inputs are real `summarySurfaceState` surfaces produced by the current map popup and save chain, not synthetic dataset scans.
- D-05: complete identity samples are wired first so broken save/highlight plumbing is fixed before broader coverage.
- D-07: fallback `OUTSIDE_SUPPORTED_DATA` plus frontend region lookup remains explanatory-only; no `FootprintPlaceSnapshot` is created for fallback.
- D-11: disabled `留下足迹` CTA remains visible for blocked points and cannot open `FootprintDateDialog`.
- D-12: the category/copy from the availability classifier is passed through `MapContextPopup` to `PointSummaryCard`.
- D-15: highlight verification uses `getGeometryManifestEntry` and focused component tests for boundary-load/highlight state.
- D-16: tests assert the canonical boundary id, visible category copy, and blocked dialog state for targeted failure output.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Use one availability result for popup state and dialog entry</name>
  <files>apps/web/src/components/LeafletMapStage.vue, apps/web/src/components/map-popup/MapContextPopup.vue, apps/web/src/components/map-popup/MapContextPopup.spec.ts</files>
  <read_first>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-CONTEXT.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-RESEARCH.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-PATTERNS.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-UI-SPEC.md</file>
    <file>.planning/phases/44-world-footprints-map-footprint-date-dialog/44-05-SUMMARY.md</file>
    <file>apps/web/src/components/LeafletMapStage.vue</file>
    <file>apps/web/src/components/map-popup/MapContextPopup.vue</file>
    <file>apps/web/src/components/map-popup/MapContextPopup.spec.ts</file>
    <file>apps/web/src/components/map-popup/PointSummaryCard.vue</file>
    <file>apps/web/src/services/footprint-availability.ts</file>
    <file>apps/web/src/types/map-point.ts</file>
  </read_first>
  <action>
    In `LeafletMapStage.vue`, import `getFootprintAvailability`. Add `const activeFootprintAvailability = computed(() => getFootprintAvailability(summarySurfaceState.value))`. Replace the body of `isActivePointIlluminatable` with `activeFootprintAvailability.value.saveable`. Add computed values `activeFootprintUnavailableCategory` and `activeFootprintUnavailableCopy` that return `null` for saveable availability and otherwise return `activeFootprintAvailability.value.category` and `.copy`.

    In `openFootprintDateDialog()`, use `const availability = activeFootprintAvailability.value`; if `!availability.saveable`, return before opening and do not create a snapshot. When saveable, set `footprintPlaceSnapshot.value = { ...availability.snapshot }`, clear `footprintDialogError`, and open the dialog. Remove the duplicate required-field split guard from `openFootprintDateDialog()` except defensive checks that do not create a second source of truth. Keep `submitFootprintDate()` reading only `footprintPlaceSnapshot.value`; it must not read `summarySurfaceState.value`.

    In the template, pass `:footprint-unavailable-category="activeFootprintUnavailableCategory"` and `:footprint-unavailable-copy="activeFootprintUnavailableCopy"` to `MapContextPopup`. In `MapContextPopup.vue`, add typed props `footprintUnavailableCategory?: FootprintUnavailableCategory | null` and `footprintUnavailableCopy?: string | null`, import the type from `footprint-availability`, pass both props to `PointSummaryCard`, and keep `@leave-footprint="emit('leaveFootprint')"`. Update `MapContextPopup.spec.ts` to assert category/copy props reach `PointSummaryCard` for a blocked detail surface.
  </action>
  <acceptance_criteria>
    - `rg -n "getFootprintAvailability|activeFootprintAvailability|activeFootprintUnavailableCategory|activeFootprintUnavailableCopy" apps/web/src/components/LeafletMapStage.vue` finds the single-source availability wiring.
    - `rg -n "availability\\.snapshot|footprintPlaceSnapshot\\.value = \\{ \\.\\.\\.availability\\.snapshot \\}|!availability\\.saveable" apps/web/src/components/LeafletMapStage.vue` finds snapshot creation from the availability result.
    - `rg -n "summarySurfaceState\\.value" apps/web/src/components/LeafletMapStage.vue` shows no usage inside `submitFootprintDate`.
    - `rg -n "footprintUnavailableCategory|footprintUnavailableCopy|FootprintUnavailableCategory" apps/web/src/components/map-popup/MapContextPopup.vue apps/web/src/components/map-popup/MapContextPopup.spec.ts` finds the bridge and spec.
    - `pnpm --filter @trip-map/web test -- src/services/footprint-availability.spec.ts src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/services/footprint-availability.spec.ts src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts</automated>
  </verify>
  <done>Popup CTA state, unavailable reason, and dialog-open snapshot all come from the same availability result.</done>
</task>

<task type="auto">
  <name>Task 2: Add matrix-driven map guard and highlight assertions</name>
  <files>apps/web/src/components/LeafletMapStage.spec.ts, apps/web/src/services/geometry-manifest.spec.ts</files>
  <read_first>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-CONTEXT.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-UI-SPEC.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-VALIDATION.md</file>
    <file>.planning/phases/45-map-authoritative-coverage-expansion/45-PATTERNS.md</file>
    <file>apps/web/src/components/LeafletMapStage.spec.ts</file>
    <file>apps/web/src/components/LeafletMapStage.vue</file>
    <file>apps/web/src/services/footprint-availability.ts</file>
    <file>apps/web/src/services/geometry-manifest.ts</file>
    <file>apps/web/src/services/geometry-manifest.spec.ts</file>
    <file>apps/web/src/services/geometry-loader.ts</file>
    <file>apps/web/src/components/map-popup/FootprintDateDialog.vue</file>
  </read_first>
  <action>
    Extend `LeafletMapStage.spec.ts` with tests named exactly `keeps the date dialog closed for fallback explanatory-only points`, `keeps the date dialog closed for missing map data points`, `uses footprint availability for enabled CTA and dialog entry`, and `loads manifest-backed highlight for Phase 45 saveable samples`. The fallback test must mount a detected point with `fallbackNotice`, no `placeId`, no `boundaryId`, click `data-footprint-cta="true"`, assert the button is disabled, assert `data-footprint-unavailable-reason` contains `这里暂时只能用于查看位置，还不能留下足迹。`, assert no `data-region="footprint-date-dialog"` exists, and assert `createTravelRecord` was not called. The missing map data test must cover `missing_boundary_id` and `missing_geometry_manifest` cases and assert category `map_data_unavailable` plus the exact copy `已识别到这个地点，但地图数据还不够完整，暂时不能保存足迹。`. The enabled test must use a complete `us-california` point and assert the CTA opens `FootprintDateDialog` with `data-footprint-place-name` equal to `California`.

    The highlight test must use `boundaryId: 'ne-admin1-us-california'` and assert `getGeometryManifestEntry('ne-admin1-us-california')` is non-null before save/open flow assertions. If the existing spec mocks `loadGeometryShard`, assert it is called with `overseas/layer.json` or the resolved manifest entry layer after a saved result. Extend `geometry-manifest.spec.ts` with a test named exactly `covers Phase 45 saveable boundary ids used by the runtime matrix` asserting `ne-admin1-us-california` and `ne-admin1-ca-british-columbia` both return entries with `layer: 'OVERSEAS'`, `assetKey: 'overseas/layer.json'`, `geometryDatasetVersion: '2026-04-21-geo-v3'`, and `renderableId` equal to the boundary id.
  </action>
  <acceptance_criteria>
    - `rg -n "keeps the date dialog closed for fallback explanatory-only points|keeps the date dialog closed for missing map data points|uses footprint availability for enabled CTA and dialog entry|loads manifest-backed highlight for Phase 45 saveable samples" apps/web/src/components/LeafletMapStage.spec.ts` finds all four tests.
    - `rg -n "这里暂时只能用于查看位置，还不能留下足迹。|已识别到这个地点，但地图数据还不够完整，暂时不能保存足迹。|data-footprint-unavailable-reason|data-region=\\\"footprint-date-dialog\\\"" apps/web/src/components/LeafletMapStage.spec.ts` finds blocked UI assertions.
    - `rg -n "covers Phase 45 saveable boundary ids used by the runtime matrix|ne-admin1-us-california|ne-admin1-ca-british-columbia|2026-04-21-geo-v3" apps/web/src/services/geometry-manifest.spec.ts` finds manifest assertions.
    - `pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts` exits 0.
    - `pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts src/services/geometry-manifest.spec.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts</automated>
  </verify>
  <done>Real map flow blocks fallback/incomplete points before dialog entry and proves Phase 45 saveable samples have manifest-backed highlight coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| map popup -> date dialog | User interaction crosses from place summary into record creation flow. |
| active point -> snapshot | Runtime map state becomes immutable `FootprintPlaceSnapshot` only when authoritative availability passes. |
| manifest lookup -> highlight layer | Boundary ids drive geometry shard loading and map highlight state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03-01 | Tampering | `openFootprintDateDialog()` | mitigate | Dialog opens only from `availability.saveable` and copies `availability.snapshot`; fallback/incomplete points have `snapshot: null`. |
| T-45-03-02 | Elevation of Privilege | disabled CTA path | mitigate | Component tests click blocked CTA and assert no dialog and no `createTravelRecord` call. |
| T-45-03-03 | Tampering | stale active point state | mitigate | Preserve Phase 44 snapshot submit rule; `submitFootprintDate` does not read `summarySurfaceState`. |
| T-45-03-04 | Denial of Service | highlight loading | mitigate | Manifest spec asserts Phase 45 boundary ids resolve to existing shard entries before highlight tests depend on them. |
</threat_model>

<verification>
Focused commands:
`pnpm --filter @trip-map/web test -- src/services/footprint-availability.spec.ts src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts`
`pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts`
`pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts src/services/geometry-manifest.spec.ts`
</verification>

<success_criteria>
COV-02 and COV-03 are satisfied in the real frontend chain: Phase 45 saveable samples can open the existing snapshot-safe date dialog with manifest-backed highlight coverage, while blocked/fallback samples keep a visible disabled CTA and cannot create a fake submit path.
</success_criteria>

<output>
After completion, create `.planning/phases/45-map-authoritative-coverage-expansion/45-03-SUMMARY.md`
</output>

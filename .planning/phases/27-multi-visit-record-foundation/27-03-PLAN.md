---
phase: 27-multi-visit-record-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01
  - 27-02
files_modified:
  - apps/web/src/stores/map-points.ts
  - apps/web/src/services/legacy-point-storage.ts
  - apps/web/src/stores/map-points.spec.ts
  - apps/web/src/services/legacy-point-storage.spec.ts
autonomous: true
requirements:
  - TRIP-01
  - TRIP-02
  - TRIP-03
tags:
  - frontend
  - pinia
  - store
  - legacy-migration
must_haves:
  truths:
    - "map-points store 的 illuminate 函数接受 startDate 与 endDate 参数并透传到 createTravelRecord API"
    - "illuminate 函数不再因 travelRecords 中已存在该 placeId 而短路——允许为同一地点追加新记录（Pitfall 1）"
    - "applyAuthoritativeTravelRecords 按 record.id 合并而非 placeId，使同 placeId 的多条记录不会互相覆盖（Pitfall 3）"
    - "displayPoints computed 按 placeId 去重（取最新 createdAt），地图 marker 不重复渲染（Pitfall 4）"
    - "tripsByPlaceId computed 导出 Map<string, TravelRecord[]>，供 UI 读取次数摘要"
    - "unilluminate 保持按 placeId 全删除语义（D-06）"
    - "legacy-point-storage.normalizeLegacyTravelRecord 输出 startDate: null、endDate: null（D-08）"
  artifacts:
    - path: "apps/web/src/stores/map-points.ts"
      provides: "多条记录 store + place 级聚合视图 + 日期参数 illuminate"
      contains: "tripsByPlaceId"
    - path: "apps/web/src/services/legacy-point-storage.ts"
      provides: "旧记录迁移输出日期未知"
      contains: "startDate: null"
    - path: "apps/web/src/stores/map-points.spec.ts"
      provides: "store 多条记录 / 去重 / 合并 单元测试"
      contains: "tripsByPlaceId"
  key_links:
    - from: "apps/web/src/stores/map-points.ts"
      to: "apps/web/src/services/api/records.ts createTravelRecord"
      via: "illuminate 调用 createTravelRecord 时携带 startDate/endDate"
      pattern: "createTravelRecord\\([^)]*startDate"
    - from: "apps/web/src/stores/map-points.ts applyAuthoritativeTravelRecords"
      to: "@trip-map/contracts TravelRecord"
      via: "按 record.id 合并 snapshot 与 pending"
      pattern: "new Map\\(records\\.map\\(\\(r\\) => \\[r\\.id"
    - from: "apps/web/src/services/legacy-point-storage.ts normalizeLegacyTravelRecord"
      to: "CreateTravelRecordRequest"
      via: "输出对象显式含 startDate/endDate 为 null"
      pattern: "startDate: null"
---

<objective>
Phase 27 Wave 2：把前端 map-points store 与 legacy storage 升级到多条记录 + 日期字段模型，**保持地图点亮主链路不回归**。
1. illuminate：签名追加 startDate/endDate；**移除"已点亮 → return" 短路**（Pitfall 1），改为允许追加；乐观记录 id 改为 `pending-${placeId}-${Date.now()}` 以支持多条 pending 共存。
2. applyAuthoritativeTravelRecords：按 `record.id` 合并（Pitfall 3），pending 记录以 `id.startsWith('pending-')` 判别保留。
3. displayPoints：按 placeId 去重（取 createdAt 最新），避免同一地点多 marker（Pitfall 4）。
4. 新增 computed `tripsByPlaceId: ComputedRef<Map<string, TravelRecord[]>>`，对外暴露次数摘要视图。
5. unilluminate：保持按 placeId 全删除语义（D-06），无需改 API。
6. legacy-point-storage.normalizeLegacyTravelRecord：在返回对象中显式加入 `startDate: null, endDate: null`（D-08 禁止使用 createdAt 伪装）。
7. 扩展 map-points.spec.ts 与新建 legacy-point-storage.spec.ts 覆盖所有新语义。

Purpose: Store 层是前端真源；Plan 02 已经打通服务端，Wave 2 把前端状态模型对齐，Wave 3 UI 组件才能消费 tripsByPlaceId / latestTripDate。
Output: `pnpm --filter @trip-map/web test` 全绿；store 支持同 placeId 多条记录 + 日期字段；legacy 迁移遵循"未知"语义。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-multi-visit-record-foundation/27-CONTEXT.md
@.planning/phases/27-multi-visit-record-foundation/27-RESEARCH.md
@.planning/phases/27-multi-visit-record-foundation/27-PATTERNS.md
@.planning/phases/27-multi-visit-record-foundation/27-01-SUMMARY.md
@.planning/phases/27-multi-visit-record-foundation/27-02-SUMMARY.md

<interfaces>
<!-- Source: apps/web/src/stores/map-points.ts lines 331-438 [VERIFIED 2026-04-20] -->

```typescript
// 当前 illuminate 签名（需扩展 startDate/endDate）:
async function illuminate(summary: {
  placeId: string
  boundaryId: string | null
  placeKind: TravelRecord['placeKind']
  datasetVersion: string
  displayName: string
  regionSystem: TravelRecord['regionSystem']
  adminType: TravelRecord['adminType']
  typeLabel: TravelRecord['typeLabel']
  parentLabel: TravelRecord['parentLabel']
  subtitle: string | null
}) {
  // ...
  // 当前短路（lines 358-363，需移除）:
  if (travelRecords.value.some((r) => r.placeId === placeId)) {
    selectedPointId.value = placeId
    summaryMode.value = 'view'
    return
  }

  // 当前乐观记录（line 366，id 需改为包含 Date.now() 防冲突）:
  const optimisticRecord: TravelRecord = {
    id: `pending-${placeId}`,
    // ...
  }
}
```

<!-- Source: apps/web/src/stores/map-points.ts lines 178-201 [VERIFIED 2026-04-20] -->

```typescript
// 当前按 placeId 合并（多条会被覆盖），需改为按 record.id:
function applyAuthoritativeTravelRecords(records: TravelRecord[]) {
  if (pendingPlaceIds.value.size === 0) {
    travelRecords.value = [...records]
    hasBootstrapped.value = true
    return
  }

  const snapshotByPlaceId = new Map(records.map((record) => [record.placeId, record]))
  const currentByPlaceId = new Map(travelRecords.value.map((record) => [record.placeId, record]))
  // ... 当前逐 placeId merge 逻辑 ...
  travelRecords.value = Array.from(snapshotByPlaceId.values())
  hasBootstrapped.value = true
}
```

<!-- Source: apps/web/src/stores/map-points.ts lines 101-109 [VERIFIED 2026-04-20] -->

```typescript
// 当前 displayPoints（1:1 mapping，多条记录时地图会叠 marker）:
const displayPoints = computed<MapPointDisplay[]>(() => {
  const points: MapPointDisplay[] = travelRecords.value.map(recordToDisplayPoint)
  if (draftPoint.value) points.push(draftPoint.value)
  return points
})
```

<!-- Source: apps/web/src/services/legacy-point-storage.ts lines 133-145 [VERIFIED 2026-04-20] -->

```typescript
// 当前返回（无日期字段，需追加 startDate: null, endDate: null）:
return {
  placeId, boundaryId, placeKind, datasetVersion,
  displayName, regionSystem, adminType, typeLabel, parentLabel, subtitle,
}
```

<!-- Source: packages/contracts/src/records.ts [after Plan 01] -->

```typescript
export interface TravelRecord {
  // ... 基础字段 ...
  subtitle: string
  startDate: string | null   // Plan 01 新增
  endDate: string | null     // Plan 01 新增
  createdAt: string
}

export interface CreateTravelRecordRequest extends CanonicalPlaceSummary {
  startDate: string | null   // Plan 01 新增
  endDate: string | null     // Plan 01 新增
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重构 map-points store — illuminate 日期参数 + 多条记录合并 + place 级聚合</name>
  <files>
    apps/web/src/stores/map-points.ts,
    apps/web/src/stores/map-points.spec.ts
  </files>
  <read_first>
    - apps/web/src/stores/map-points.ts（完整 537 行——必须掌握 illuminate、unilluminate、applyAuthoritativeTravelRecords、displayPoints、savedBoundaryIds、openSavedPointForPlaceOrStartDraft、selectPointById 的相互关系）
    - apps/web/src/stores/map-points.spec.ts（现有 makeRecord 工厂需要追加 startDate/endDate 默认字段；现有 createMock / deleteMock hoisted 模式保留）
    - apps/web/src/services/api/records.ts（createTravelRecord 接收 CreateTravelRecordRequest；Plan 01 后此类型已含日期字段）
    - apps/web/src/stores/auth-session.ts（hasSessionBoundaryChanged 依赖；不得修改）
    - apps/web/src/types/map-point.ts（MapPointDisplay、DraftMapPoint、SummaryMode、SummarySurfaceState 的字段）
    - .planning/phases/27-multi-visit-record-foundation/27-PATTERNS.md（Pattern 4/5/6/7，乐观写入 + 回滚模式，session boundary 检查模式）
    - .planning/phases/27-multi-visit-record-foundation/27-RESEARCH.md（Pitfalls 1/3/4/5，Open Question #1 selectedPointId 与 displayPoints id 的对齐说明）
  </read_first>
  <behavior>
    **store 公开 API 必须保持的契约（被其他组件/store 消费）：**
    - return 对象仍暴露：travelRecords, pendingPlaceIds, draftPoint, pendingCanonicalSelection, selectedPointId, summaryMode, hasBootstrapped, savedBoundaryIds, displayPoints, activePoint, activeBoundaryCoverageState, summarySurfaceState, selectedBoundaryId, replaceTravelRecords, applyAuthoritativeTravelRecords, resetTravelRecordsForSessionBoundary, clearActivePoint, startDraftFromDetection, replaceDraftFromDetection, startPendingCanonicalSelection, discardDraft, selectPointById, findSavedPointByPlaceId, findSavedPointByCityId, openSavedPointForPlaceOrStartDraft, illuminate, unilluminate, isPlaceIlluminated, isPlacePending
    - **新增 return 字段：** tripsByPlaceId（Readonly<Ref<Map<string, TravelRecord[]>>> 或 ComputedRef<Map<...>>）
    - **illuminate 新签名：** summary 对象多加 `startDate: string | null` 与 `endDate: string | null`，无默认值，调用方必须传。

    **行为语义变更：**
    - illuminate：允许为同一 placeId 追加新乐观记录；pending id 形式 `pending-${placeId}-${Date.now()}-${random}` 避免同批多次新增冲突。
    - illuminate 成功回调：用服务端 record 替换 **该次调用对应的乐观记录**（通过 id match），不再按 placeId 替换。
    - illuminate 失败回滚：仅移除该次调用对应的乐观记录（id match），其他同 placeId 的真实记录保留。
    - openSavedPointForPlaceOrStartDraft：已点亮地点仍进入 `summaryMode = 'view'`，保持 D-04（popup 内闭环）。
    - displayPoints：按 placeId 去重取最新记录；pending 记录参与去重时视 createdAt 作为时间戳。
    - activePoint 继续 `displayPoints.find((p) => p.id === selectedPointId)`——因 `recordToDisplayPoint` 中 `id: record.placeId`（已验证 line 51），selectedPointId 使用 placeId 正常工作。
    - applyAuthoritativeTravelRecords：`snapshotById = new Map(records.map((r) => [r.id, r]))`，pending 记录按 `id.startsWith('pending-') && pendingPlaceIds.has(r.placeId)` 过滤保留。
    - unilluminate：仍按 placeId 全删除（D-06），API 不变。
    - tripsByPlaceId computed：对 travelRecords 按 placeId 聚合。
    - findSavedPointByPlaceId：返回第一条匹配记录（仍保持现有签名 `TravelRecord | null`）——这是 UI 查询 place 是否已点亮的兼容口径。
  </behavior>
  <action>
    ## 修改 apps/web/src/stores/map-points.ts

    ### 1. illuminate 签名扩展 + 乐观记录重构（lines 331-438）

    把 `async function illuminate(summary: {...})` 的 summary 类型追加两行（在 `subtitle: string | null` 之后）：
    ```typescript
    startDate: string | null
    endDate: string | null
    ```

    把解构 `const { placeId, boundaryId, placeKind, datasetVersion, displayName, regionSystem, adminType, typeLabel, parentLabel, subtitle } = summary` 扩展为包含 `startDate, endDate`。

    **删除**以下短路（lines 358-363）：
    ```typescript
    if (travelRecords.value.some((r) => r.placeId === placeId)) {
      selectedPointId.value = placeId
      summaryMode.value = 'view'
      return
    }
    ```

    乐观记录构造替换为：
    ```typescript
    const optimisticId = `pending-${placeId}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`
    const optimisticRecord: TravelRecord = {
      id: optimisticId,
      placeId,
      boundaryId: boundaryId ?? '',
      placeKind,
      datasetVersion,
      displayName,
      regionSystem,
      adminType,
      typeLabel,
      parentLabel,
      subtitle: subtitle ?? '',
      startDate,
      endDate,
      createdAt: new Date().toISOString(),
    }
    ```

    createTravelRecord 调用入参追加 `startDate` 与 `endDate` 两字段（透传）。

    成功回调由"按 placeId 替换"改为"按 id 替换该条乐观记录"：
    ```typescript
    travelRecords.value = travelRecords.value.map((r) => (r.id === optimisticId ? record : r))
    ```
    （移除原先 `hasExistingRecord` 的 if/else 分支）

    失败回滚由"filter 整个 placeId"改为"仅移除该乐观 id"：
    ```typescript
    travelRecords.value = travelRecords.value.filter((r) => r.id !== optimisticId)
    ```
    同时保留 `selectedPointId.value = null; summaryMode.value = null` 的恢复——但**仅当**失败后该 placeId 无任何其他记录时才清 selectedPointId（防止第二次 illuminate 失败把第一次成功的选中态清掉）：
    ```typescript
    const stillIlluminated = travelRecords.value.some((r) => r.placeId === placeId)
    if (!stillIlluminated) {
      selectedPointId.value = null
      summaryMode.value = null
    }
    ```

    finally 块的 pendingPlaceIds 清理保持不变（按 placeId 粒度 pending，不细化到单条），但**只在该 placeId 已无 pending 乐观记录时** delete：
    ```typescript
    const hasOtherPending = travelRecords.value.some((r) => r.id.startsWith('pending-') && r.placeId === placeId)
    if (!hasOtherPending) {
      const next = new Set(pendingPlaceIds.value)
      next.delete(placeId)
      pendingPlaceIds.value = next
    }
    ```

    ### 2. applyAuthoritativeTravelRecords 按 record.id 合并（lines 178-201）

    替换整个函数为：
    ```typescript
    function applyAuthoritativeTravelRecords(records: TravelRecord[]) {
      if (pendingPlaceIds.value.size === 0) {
        travelRecords.value = [...records]
        hasBootstrapped.value = true
        return
      }

      const pendingRecords = travelRecords.value.filter(
        (r) => r.id.startsWith('pending-') && pendingPlaceIds.value.has(r.placeId),
      )
      const snapshotById = new Map(records.map((r) => [r.id, r]))
      travelRecords.value = [...snapshotById.values(), ...pendingRecords]
      hasBootstrapped.value = true
    }
    ```

    ### 3. displayPoints 按 placeId 去重（lines 101-109）

    替换为：
    ```typescript
    const displayPoints = computed<MapPointDisplay[]>(() => {
      const latestByPlaceId = new Map<string, TravelRecord>()
      for (const record of travelRecords.value) {
        const existing = latestByPlaceId.get(record.placeId)
        if (!existing || record.createdAt > existing.createdAt) {
          latestByPlaceId.set(record.placeId, record)
        }
      }
      const points: MapPointDisplay[] = Array.from(latestByPlaceId.values()).map(recordToDisplayPoint)
      if (draftPoint.value) points.push(draftPoint.value)
      return points
    })
    ```

    ### 4. 新增 tripsByPlaceId computed

    在 `displayPoints` 声明之后、`activePoint` 之前插入：
    ```typescript
    const tripsByPlaceId = computed(() => {
      const map = new Map<string, TravelRecord[]>()
      for (const record of travelRecords.value) {
        const existing = map.get(record.placeId) ?? []
        map.set(record.placeId, [...existing, record])
      }
      return map
    })
    ```

    ### 5. return 暴露新字段

    在 store return 对象的 `savedBoundaryIds,` 之后追加 `tripsByPlaceId,`（排序随喜好，但必须在 return 中）。

    ### 6. **不修改**：savedBoundaryIds（已对多条记录自然去重——`new Set(boundaryIds)` 幂等）、unilluminate（保持按 placeId 全删，D-06 语义）、recordToDisplayPoint（其 id = record.placeId，保持 selectedPointId 对齐）、findSavedPointByPlaceId、openSavedPointForPlaceOrStartDraft。

    不允许：
    - 在 store 里新建 place 级聚合作为主 state——travelRecords 仍是唯一 state source（decision：多条记录直接持有在 store，tripsByPlaceId 仅为派生 view，不入 state）。
    - 为支持"同一地点多条 pending"把 pendingPlaceIds 改为 Map/数组——保持 Set<placeId> 粒度（place 级 UX 足够，避免跨 Plan 改动 auth-session.ts 使用者）。
    - 修改 createTravelRecord API 签名（Plan 01 后已自动获得日期字段）。

    ## 扩展 apps/web/src/stores/map-points.spec.ts

    在 `makeRecord` 工厂中为默认返回值追加 `startDate: null, endDate: null,`（在 createdAt 之前）。

    在 `makeOverseasRecord` 工厂也追加相同两字段（保持一致性）。

    在文件末尾新增至少 6 个 `it(...)` 测试（可集中在一个 `describe('multi-visit Phase 27', ...)` 下）：

    1. `illuminate 透传 startDate 与 endDate 到 createTravelRecord`：mock createMock 返回 makeRecord({ startDate:'2025-10-01', endDate:'2025-10-07' })；调用 store.illuminate(makeResolvedPlace() + { startDate:'2025-10-01', endDate:'2025-10-07' })；期望 createMock 被调用且入参对象含 `startDate: '2025-10-01'` 与 `endDate: '2025-10-07'`。

    2. `illuminate 同一地点可多次调用，且 travelRecords 累积多条`：先 illuminate 北京 startDate='2025-10-01'、等 promise 完成；再 illuminate 北京 startDate='2025-11-05'；期望 `store.travelRecords.filter(r => r.placeId === 'cn-admin-beijing').length === 2`；且 createMock 被调用 2 次。

    3. `applyAuthoritativeTravelRecords 按 record.id 合并，同 placeId 多条全部保留`：预置 travelRecords 含一条北京 pending 乐观记录；调用 applyAuthoritativeTravelRecords 传入两条真实北京记录（不同 id，不同 startDate）；期望合并后 `travelRecords.filter(r => r.placeId === 'cn-admin-beijing').length === 3`（两条真实 + 一条 pending）。

    4. `displayPoints 对同一 placeId 多条记录只生成一个 MapPointDisplay`：travelRecords 含北京两条（startDate '2025-10-01' createdAt 早，'2025-11-05' createdAt 晚）；期望 `store.displayPoints.filter(p => p.placeId === 'cn-admin-beijing').length === 1`，且该 point 的 cityId/displayName 对应 createdAt 最晚的记录。

    5. `tripsByPlaceId 对同一 placeId 返回全部记录，按 placeId 索引`：同场景 4，期望 `store.tripsByPlaceId.get('cn-admin-beijing')?.length === 2`。

    6. `unilluminate 按 placeId 全删除，即使该 placeId 有多条记录`：travelRecords 含北京两条；deleteMock 返回 undefined；调用 store.unilluminate('cn-admin-beijing')；期望 `store.travelRecords.filter(r => r.placeId === 'cn-admin-beijing').length === 0`；deleteMock 被调用 1 次。

    7. `illuminate 失败回滚只移除对应乐观记录`：travelRecords 预置一条真实北京记录（非 pending id）；createMock 抛 new ApiClientError('boom')；调用 illuminate 北京；期望该真实记录仍然存在，pending 乐观记录被清除；`store.travelRecords.filter(r => r.placeId === 'cn-admin-beijing').length === 1`。

    **Red-Green 承诺：** 完成本任务后，`pnpm --filter @trip-map/web test --run src/stores/map-points.spec.ts` 应全部通过。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test --run src/stores/map-points.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "startDate: string | null" apps/web/src/stores/map-points.ts >= 1（illuminate summary 类型）
    - grep -c "endDate: string | null" apps/web/src/stores/map-points.ts >= 1
    - grep -c "travelRecords.value.some((r) => r.placeId === placeId)" apps/web/src/stores/map-points.ts == 0（旧短路已全部移除，该精确表达不再存在）
    - grep -c "pending-\${placeId}-\${Date.now()}" apps/web/src/stores/map-points.ts >= 1
    - grep -c "tripsByPlaceId" apps/web/src/stores/map-points.ts >= 3（声明 + return + 可能 1 处使用）
    - grep -c "snapshotById" apps/web/src/stores/map-points.ts >= 1
    - grep -c "new Map(records.map((r) => \[r.id, r\]))" apps/web/src/stores/map-points.ts >= 1
    - grep -c "latestByPlaceId" apps/web/src/stores/map-points.ts >= 1（displayPoints 去重）
    - grep -c "r.id.startsWith('pending-')" apps/web/src/stores/map-points.ts >= 1
    - grep -c "r.id === optimisticId" apps/web/src/stores/map-points.ts >= 2（成功 replace + 失败 filter）
    - grep -c "startDate: null" apps/web/src/stores/map-points.spec.ts >= 2（makeRecord / makeOverseasRecord 默认）
    - grep -c "tripsByPlaceId" apps/web/src/stores/map-points.spec.ts >= 1
    - `pnpm --filter @trip-map/web test --run src/stores/map-points.spec.ts` 退出码为 0
  </acceptance_criteria>
  <done>map-points store 完成多条记录重构：illuminate 接日期、不再短路、乐观 id 唯一；applyAuthoritativeTravelRecords 按 id 合并；displayPoints 按 placeId 去重；tripsByPlaceId 对外暴露；unilluminate 保持 D-06 语义；spec 7 个新增 case 全部通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: legacy-point-storage 旧记录迁移输出日期未知</name>
  <files>
    apps/web/src/services/legacy-point-storage.ts,
    apps/web/src/services/legacy-point-storage.spec.ts
  </files>
  <read_first>
    - apps/web/src/services/legacy-point-storage.ts（完整文件——掌握 normalizeLegacyTravelRecord 的返回对象字段与调用路径）
    - packages/contracts/dist/records.d.ts（已构建的 CreateTravelRecordRequest，含 startDate/endDate）
    - apps/web/src/stores/map-points.spec.ts（makeRecord 工厂风格，作为 spec 写法蓝本）
    - .planning/phases/27-multi-visit-record-foundation/27-CONTEXT.md（D-07/D-08/D-09：旧记录→1 条历史记录 + 日期统一"未知" + 本地与账号迁移口径一致）
    - .planning/phases/27-multi-visit-record-foundation/27-PATTERNS.md（legacy-point-storage 段：return 末尾追加 startDate/endDate: null）
  </read_first>
  <behavior>
    - normalizeLegacyTravelRecord 在校验通过后返回的对象必须显式包含 `startDate: null` 与 `endDate: null`（D-08）。
    - 不得从 point 原始对象读取任何 date-like 字段（例如 point.createdAt、point.date）来填充 startDate（D-08 禁止使用 createdAt 伪装）。
    - 校验失败分支（返回 null）保持现有行为不变。
    - loadLegacyPointStorageSnapshot 对外契约保持（status/snapshot/records）。
  </behavior>
  <action>
    ## 修改 apps/web/src/services/legacy-point-storage.ts

    在 `normalizeLegacyTravelRecord` 函数的 `return { ... }` 对象（lines 133-145）末尾追加两行（在 `subtitle,` 之后）：
    ```typescript
    startDate: null,
    endDate: null,
    ```

    **不修改**：
    - `normalizePlaceKind` / `normalizeRegionSystem` / `normalizeAdminType` / `normalizeNullableString` 辅助函数
    - `CHINA_ADMIN_TYPE_BY_LABEL`
    - `isValidDeletedSeedIds`
    - `loadLegacyPointStorageSnapshot` 的结构（它 map normalizeLegacyTravelRecord 得到的记录数组——自动继承日期字段，无需改动）
    - `clearLegacyPointStorageSnapshot`
    - `POINT_STORAGE_KEY`

    不允许：
    - 用 point.createdAt 或其他任何字段填充 startDate（D-08）。
    - 新增"try to infer date"路径。
    - 修改 LegacyPointStorageSnapshot 接口（snapshot.version 仍为 2）。

    ## 新建 apps/web/src/services/legacy-point-storage.spec.ts

    文件结构（依照项目 Vitest happy-dom 环境，直接 stub localStorage）：

    ```typescript
    import { beforeEach, describe, expect, it } from 'vitest'

    import {
      clearLegacyPointStorageSnapshot,
      loadLegacyPointStorageSnapshot,
      POINT_STORAGE_KEY,
    } from './legacy-point-storage'

    function makeLegacyUserPoint(overrides: Record<string, unknown> = {}) {
      return {
        source: 'saved',
        placeKind: 'CN_ADMIN',
        placeId: 'cn-admin-beijing',
        boundaryId: 'cn-admin-beijing-boundary',
        datasetVersion: 'cn-admin-2024-r1',
        displayName: '北京市',
        regionSystem: 'CN',
        adminType: 'MUNICIPALITY',
        typeLabel: '直辖市',
        parentLabel: '中国',
        subtitle: '直辖市 · 中国',
        ...overrides,
      }
    }

    beforeEach(() => {
      window.localStorage.clear()
    })

    describe('loadLegacyPointStorageSnapshot', () => {
      it('normalizes legacy user points with startDate and endDate as null (D-08)', () => {
        window.localStorage.setItem(
          POINT_STORAGE_KEY,
          JSON.stringify({
            version: 2,
            userPoints: [makeLegacyUserPoint()],
            seedOverrides: [],
            deletedSeedIds: [],
          }),
        )

        const result = loadLegacyPointStorageSnapshot()

        expect(result.status).toBe('ready')
        expect(result.records).toHaveLength(1)
        expect(result.records[0]).toMatchObject({
          placeId: 'cn-admin-beijing',
          startDate: null,
          endDate: null,
        })
      })

      it('does not fabricate dates from createdAt on legacy records (D-08)', () => {
        window.localStorage.setItem(
          POINT_STORAGE_KEY,
          JSON.stringify({
            version: 2,
            userPoints: [makeLegacyUserPoint({ createdAt: '2024-06-01T00:00:00.000Z' })],
            seedOverrides: [],
            deletedSeedIds: [],
          }),
        )

        const result = loadLegacyPointStorageSnapshot()

        expect(result.status).toBe('ready')
        expect(result.records[0].startDate).toBeNull()
        expect(result.records[0].endDate).toBeNull()
      })

      it('rejects malformed legacy user points without fabricating dates', () => {
        window.localStorage.setItem(
          POINT_STORAGE_KEY,
          JSON.stringify({
            version: 2,
            userPoints: [{ ...makeLegacyUserPoint(), source: 'detected' }],
            seedOverrides: [],
            deletedSeedIds: [],
          }),
        )

        const result = loadLegacyPointStorageSnapshot()
        expect(result.status).toBe('ready')
        expect(result.records).toHaveLength(0)
      })

      it('returns empty for missing snapshot', () => {
        const result = loadLegacyPointStorageSnapshot()
        expect(result.status).toBe('empty')
        expect(result.records).toEqual([])
      })
    })
    ```

    **Red-Green 承诺：** 本任务的 legacy-point-storage.ts 改动 + spec 运行：`pnpm --filter @trip-map/web test --run src/services/legacy-point-storage.spec.ts` 应全部通过。

    不允许：
    - 依赖 Plan 04 的任何 UI 组件。
    - 修改 stores/map-points.ts 中对 legacy-point-storage 的调用路径（本 plan 无改动点）。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test --run src/services/legacy-point-storage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "startDate: null" apps/web/src/services/legacy-point-storage.ts == 1
    - grep -c "endDate: null" apps/web/src/services/legacy-point-storage.ts == 1
    - grep -c "point.createdAt" apps/web/src/services/legacy-point-storage.ts == 0（不得从原对象读 createdAt 填日期）
    - apps/web/src/services/legacy-point-storage.spec.ts 文件存在
    - grep -c "describe\(.loadLegacyPointStorageSnapshot" apps/web/src/services/legacy-point-storage.spec.ts >= 1
    - grep -c "D-08" apps/web/src/services/legacy-point-storage.spec.ts >= 1（文档溯源）
    - `pnpm --filter @trip-map/web test --run src/services/legacy-point-storage.spec.ts` 退出码为 0
  </acceptance_criteria>
  <done>normalizeLegacyTravelRecord 返回对象显式含 startDate/endDate 为 null（D-08），spec 4 个 case 全部通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (popup 日期表单) → Pinia illuminate | illuminate 透传到 API，依赖 Plan 02 的 DTO class-validator 做二次阻断；前端表单 Plan 04 会做 UX 级校验 |
| localStorage (legacy snapshot) → normalizeLegacyTravelRecord | 不可信序列化数据；现有校验已拒绝 source !== 'saved' 等，本 plan 保持现有校验并显式强制 startDate/endDate 为 null |
| API response (createTravelRecord / bootstrap) → store | 服务端是 authoritative source；applyAuthoritativeTravelRecords 覆盖本地状态，保留仅在 pendingPlaceIds 范围内的 pending 乐观记录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | localStorage legacy snapshot 字段被篡改 | mitigate | 保留现有 normalizeLegacyTravelRecord 字段级类型校验；失败返回 null；本 plan 不放宽任何校验 |
| T-27-03-02 | Tampering | illuminate summary.startDate/endDate 被 UI 传入非 YYYY-MM-DD 字符串 | mitigate | 前端 store 不做格式校验（UX 已在 TripDateForm Plan 04 兜底）；服务端 DTO `@Matches(/^\d{4}-\d{2}-\d{2}$/)` 为最终防线（Plan 02 T-27-02-01） |
| T-27-03-03 | Information Disclosure | applyAuthoritativeTravelRecords 跨用户会话残留 pending | mitigate | illuminate finally 块沿用 hasSessionBoundaryChanged 检查；resetTravelRecordsForSessionBoundary 在 auth-session 切换时已清空 travelRecords（未改动） |
| T-27-03-04 | Denial of Service | 单用户海量记录使 tripsByPlaceId computed 重计算过慢 | accept | `tripsByPlaceId` 为 computed（Vue 自动缓存），数据规模（单用户的旅行记录）远低于触发边界；未来如发现 perf 问题可改为增量 Map |
</threat_model>

<verification>
1. `pnpm --filter @trip-map/web test` 全绿（map-points.spec + legacy-point-storage.spec + 现有其他 spec）。
2. `pnpm --filter @trip-map/web typecheck` 成功。
3. `grep "travelRecords.value.some((r) => r.placeId === placeId)" apps/web/src/stores/map-points.ts` 返回空（短路已删）。
4. `grep "snapshotByPlaceId" apps/web/src/stores/map-points.ts` 返回空（旧按 placeId 合并实现已完全移除）。
</verification>

<success_criteria>
- illuminate 接受并透传 startDate/endDate；允许同一地点多次调用。
- applyAuthoritativeTravelRecords 保留同 placeId 多条服务端记录不覆盖。
- displayPoints 对同 placeId 只渲染一个 marker，savedBoundaryIds 无重复。
- tripsByPlaceId 对外暴露次数摘要，供 UI 消费。
- unilluminate 维持 D-06 地点级清理。
- legacy 迁移输出 startDate/endDate 为 null。
- Web 包 vitest 全绿。
</success_criteria>

<output>
After completion, create `.planning/phases/27-multi-visit-record-foundation/27-03-SUMMARY.md` including:
- map-points store 所有变更点概览（illuminate 签名、短路移除、乐观 id 策略、applyAuthoritativeTravelRecords、displayPoints、tripsByPlaceId）
- legacy-point-storage 的单点变更
- spec 新增 case 列表与通过统计
- 供 Plan 04 UI 消费的 store API：store.tripsByPlaceId.get(placeId) 的形状、illuminate 入参新字段
</output>
</content>
</invoke>
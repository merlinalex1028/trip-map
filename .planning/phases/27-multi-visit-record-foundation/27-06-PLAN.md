---
phase: 27-multi-visit-record-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - 27-04
files_modified:
  - apps/server/src/modules/records/dto/create-travel-record.dto.ts
  - apps/server/src/modules/records/records.service.ts
  - apps/server/src/modules/records/records.service.spec.ts
  - apps/server/prisma/schema.prisma
autonomous: true
requirements:
  - TRIP-01
  - TRIP-02
  - TRIP-03
tags:
  - backend
  - nestjs
  - prisma
  - contracts
  - gap-closure
  - phase-27
gap_closure: true
closes_gaps:
  - "key-link PARTIAL: CreateTravelRecordRequest 的共享契约变更必须能直接约束到 CreateTravelRecordDto（VERIFICATION 2026-04-20, partial）"
  - "anti-pattern WARNING: records.service.ts line 84 importTravel() 不复用 endDate >= startDate 校验"
  - "anti-pattern WARNING: records.repository.ts line 116 createMany({ skipDuplicates: true }) 缺少 DB 层唯一约束兜底"
user_setup: []
must_haves:
  truths:
    - "CreateTravelRecordDto 在类型层显式 implements CreateTravelRecordRequest，shared contract 字段名/可空性变更会在 DTO 层触发编译错误"
    - "importTravel() 对每条 records 复用与 createTravel() 相同的 endDate >= startDate 校验，非法日期区间批量导入时被拒绝"
    - "UserTravelRecord 在 (userId, placeId, startDate, endDate) 四元组上有 DB 级 @@unique 约束，并发重复导入被数据库层兜底"
    - "records.service.spec.ts 有一个用例证明：CreateTravelRecordDto 的 startDate/endDate 类型被 contract 的 string | null 直接约束"
    - "records.service.spec.ts 有一个用例证明：importTravel 对 endDate < startDate 的 record 抛 BadRequestException('endDate must be >= startDate')"
  artifacts:
    - path: "apps/server/src/modules/records/dto/create-travel-record.dto.ts"
      provides: "显式 implements CreateTravelRecordRequest 的 DTO 与收紧的字段类型"
      contains: "implements CreateTravelRecordRequest"
    - path: "apps/server/src/modules/records/records.service.ts"
      provides: "importTravel 中复用日期区间校验的 helper"
      contains: "assertValidDateRange"
    - path: "apps/server/prisma/schema.prisma"
      provides: "UserTravelRecord (userId, placeId, startDate, endDate) 唯一约束 + 保留 (userId, placeId) 索引"
      contains: "@@unique([userId, placeId, startDate, endDate]"
    - path: "apps/server/src/modules/records/records.service.spec.ts"
      provides: "契约绑定回归 + importTravel 日期校验回归"
      contains: "importTravel rejects records with endDate before startDate"
  key_links:
    - from: "packages/contracts/src/records.ts CreateTravelRecordRequest"
      to: "apps/server/src/modules/records/dto/create-travel-record.dto.ts CreateTravelRecordDto"
      via: "类型层 implements CreateTravelRecordRequest"
      pattern: "implements CreateTravelRecordRequest"
    - from: "apps/server/src/modules/records/records.service.ts createTravel"
      to: "apps/server/src/modules/records/records.service.ts importTravel"
      via: "共享 assertValidDateRange helper"
      pattern: "assertValidDateRange"
    - from: "apps/server/prisma/schema.prisma UserTravelRecord"
      to: "apps/server/src/modules/records/records.repository.ts createMany({ skipDuplicates: true })"
      via: "DB 级 @@unique 兜底 createMany 的 skipDuplicates 语义"
      pattern: "@@unique\\(\\[userId, placeId, startDate, endDate\\]"
---

<objective>
Phase 27 Gap 关闭 — Wave 4（Partial gap + warnings）：
同时处理 `VERIFICATION.md` 中三项紧耦合的后端问题，它们共享同一批文件与同一次 DB schema 变更，合并到一个 plan 更高效：

1. **PARTIAL gap（主要）** — key link `CreateTravelRecordRequest -> CreateTravelRecordDto 直接类型绑定` 未落地。当前 DTO 手写重复字段，shared contract 改名/改可空性不会在 DTO 层触发编译错误。
   - 修复：让 `CreateTravelRecordDto` 显式 `implements CreateTravelRecordRequest`，并把 `startDate?: string | null` 收紧为 `startDate: string | null`（对齐 contract 的 required nullable 语义；与 `class-validator` 的 `@IsOptional()` 运行时行为兼容——`@IsOptional()` 仍允许 undefined / null / 缺失，但类型层不再漂移）。

2. **WARNING 1** — `importTravel` 未复用 `endDate >= startDate` 校验（records.service.ts line 84）。
   - 修复：抽取 `assertValidDateRange(input)` 私有 helper，`createTravel` 与 `importTravel` 都调用；`importTravel` 对 `input.records` 逐条校验，任一非法直接抛 `BadRequestException`，并在测试中锁定该行为。

3. **WARNING 2** — `createMany({ skipDuplicates: true })` 依赖 DB 唯一约束才生效，但 Phase 27 已把 `@@unique([userId, placeId])` 降级为 `@@index([userId, placeId])`，现在三元组去重仅靠应用层预查询（records.repository.ts line 116）。并发场景下没有 DB 层兜底。
   - 修复：在 `UserTravelRecord` 上新增 `@@unique([userId, placeId, startDate, endDate], map: "UserTravelRecord_userId_placeId_startDate_endDate_key")`，保留现有 `@@index([userId, placeId])`，这样：
     - `skipDuplicates: true` 对三元组重复能真正生效（Prisma 把 ON CONFLICT 对齐唯一约束）。
     - 应用层预查询可保持，只作为快速路径；DB 层是正确性兜底。
     - **必须验证** `PostgreSQL` 在多列唯一约束中对 `NULL` 列的去重语义：PG 14 之前 `NULL != NULL` 会让旧记录 `(userId, placeId, NULL, NULL)` 多行共存；PG 15+ 支持 `NULLS NOT DISTINCT`。本 plan 在 schema 变更前必须先查当前 DB 版本，若 < 15 则不加 `NULLS NOT DISTINCT`（保持和现有应用层去重行为一致——旧日期未知记录仍可能在并发下产生极少量重复，但这在业务上可接受：日期未知记录本身语义模糊，且 legacy import 仅首登一次）。如果 PG >= 15 则可启用 `NULLS NOT DISTINCT`。

**Scope 严格控制**：本 plan 不触碰前端、不触碰 controller、不修改 auth.service.ts（已在 Plan 02 完成日期字段同步）、不改变 createTravel 公开签名。

Output: PARTIAL gap 关闭、两项 warning 关闭、server 包 typecheck + 全量 vitest 通过、schema 变更已通过 `prisma db push` 对齐。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27-multi-visit-record-foundation/27-CONTEXT.md
@.planning/phases/27-multi-visit-record-foundation/27-VERIFICATION.md
@.planning/phases/27-multi-visit-record-foundation/27-01-PLAN.md
@.planning/phases/27-multi-visit-record-foundation/27-02-PLAN.md
@.planning/phases/27-multi-visit-record-foundation/27-02-SUMMARY.md

<interfaces>
<!-- Source: packages/contracts/src/records.ts [VERIFIED 2026-04-20] -->

```typescript
// contract：CreateTravelRecordRequest（下游 DTO 的约束目标）
export interface CreateTravelRecordRequest extends CanonicalPlaceSummary {
  startDate: string | null   // required nullable
  endDate: string | null     // required nullable
}
```

<!-- Source: apps/server/src/modules/records/dto/create-travel-record.dto.ts [VERIFIED 2026-04-20] -->

```typescript
// 当前：没有 implements，startDate/endDate 使用 `?:` 而非 contract 的 required nullable
export class CreateTravelRecordDto {
  // ... 各字段 ...
  @IsOptional()
  @Matches(/^\d{4}-\d{2}-\d{2}$/, { message: 'startDate must be YYYY-MM-DD' })
  startDate?: string | null

  @IsOptional()
  @Matches(/^\d{4}-\d{2}-\d{2}$/, { message: 'endDate must be YYYY-MM-DD' })
  endDate?: string | null
}
```

<!-- Source: apps/server/src/modules/records/records.service.ts lines 75-95 [VERIFIED 2026-04-20] -->

```typescript
async createTravel(userId: string, input: CreateTravelRecordDto): Promise<ContractTravelRecord> {
  this.assertAuthoritativeOverseasRecord(input)
  if (input.startDate && input.endDate && input.endDate < input.startDate) {
    throw new BadRequestException('endDate must be >= startDate')
  }
  const record = await this.recordsRepository.createTravelRecord(userId, input)
  return toContractTravelRecord(record)
}

async importTravel(userId: string, input: ImportTravelRecordsDto): Promise<ImportTravelRecordsResponse> {
  input.records.forEach(record => this.assertAuthoritativeOverseasRecord(record))
  // <-- 缺少对 endDate >= startDate 的等价校验
  const result = await this.recordsRepository.importTravelRecords(userId, input.records)
  // ...
}
```

<!-- Source: apps/server/prisma/schema.prisma UserTravelRecord [VERIFIED 2026-04-20] -->

```prisma
model UserTravelRecord {
  // ... 字段 ...
  startDate      String?
  endDate        String?
  // ...
  @@index([userId, placeId])   // Phase 27 已从 @@unique 降级
  @@index([userId])
}
```

<!-- PG NULL 唯一约束语义参考：PG 15 起支持 NULLS NOT DISTINCT；旧版 NULL 行为默认 NULLS DISTINCT。-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 探测 PostgreSQL 版本并决定 NULLS NOT DISTINCT 策略</name>
  <files>
    .planning/phases/27-multi-visit-record-foundation/27-06-DB-PROBE.md
  </files>
  <read_first>
    - apps/server/.env 或 .env.example（确认 DATABASE_URL 指向的是 dev/staging 而非生产）
    - apps/server/prisma/schema.prisma（确认当前 datasource db 配置）
  </read_first>
  <behavior>
    - 通过 Prisma 或 psql 读取当前数据库的 `server_version_num`；根据结果决定 schema 的唯一约束是否带 `NULLS NOT DISTINCT`：
      - `server_version_num >= 150000`（PG 15+）：可启用 `@@unique([...], map: ..., )` 等价 SQL 带 `NULLS NOT DISTINCT`，但 Prisma 目前（Prisma 6.19）对 `NULLS NOT DISTINCT` 有限支持——走 raw migration SQL 或保留默认 NULLS DISTINCT。默认策略为**保留 NULLS DISTINCT**，仅文档记录 PG 版本 >= 15 可未来升级。
      - `server_version_num < 150000`：保留默认 NULLS DISTINCT（与应用层预查询行为一致）。
    - 任一情况下：**本 plan 的 schema 变更都采用默认 `@@unique` 语义（NULLS DISTINCT），不使用 raw migration**。原因：
      1. 旧日期未知记录（startDate/endDate 均为 null）在应用层 `importTravelRecords` 已用三元组 key 去重，首登仅执行一次；并发重复风险极低。
      2. 新记录用户 UX 要求必须填 startDate（D-01），所以"新记录 + 新记录"并发场景的 (startDate, endDate) 都非 NULL，DB 唯一约束天然生效。
      3. 只有"旧记录 + 旧记录"并发且两条均为 null 的边界情况可能漏网，业务影响为『日期未知记录出现 <= 2 份』，不影响地图点亮与用户可见语义（D-06 unilluminate 仍按 placeId 全删）。
    - 产出 `27-06-DB-PROBE.md` 记录 PG 版本与选择依据（给未来读者交代为什么不强求 NULLS NOT DISTINCT）。
  </behavior>
  <action>
    1. 执行以下命令取 PostgreSQL 版本（假设 DATABASE_URL 已配置；若未配置则在 27-06-DB-PROBE.md 中记录 `DATABASE_URL missing — skipped probe, default to NULLS DISTINCT`，继续 Task 2）：

       ```bash
       pnpm --filter @trip-map/server exec prisma db execute \
         --schema=apps/server/prisma/schema.prisma \
         --stdin <<< 'SELECT current_setting(''server_version_num'') AS v;'
       ```
       或等价的 psql 方式：`psql "$DATABASE_URL" -c "SHOW server_version_num;"`。

       若两种方式都无法连接，fallback: `prisma db execute --stdin <<< 'SELECT 1'` 至少确认连通；无法连通就按「DATABASE_URL missing」分支处理。

    2. 写入 `.planning/phases/27-multi-visit-record-foundation/27-06-DB-PROBE.md`，使用以下结构：

       ```markdown
       # 27-06 DB Probe

       **Probed at:** {ISO timestamp}
       **Database:** {host redacted from DATABASE_URL}
       **server_version_num:** {值，如 160004 or "unavailable — fallback to default"}

       ## Decision

       - 本 plan 对 UserTravelRecord 新增 `@@unique([userId, placeId, startDate, endDate])`，使用 Prisma 默认的 NULLS DISTINCT 语义。
       - {如果 PG >= 15，追加一段："当前 PG 版本支持 NULLS NOT DISTINCT；未来若 legacy 日期未知记录的并发去重成为问题，可通过 raw migration 改为 NULLS NOT DISTINCT。"}
       - {如果 PG < 15 或探测失败，追加一段："当前 PG 版本不支持 NULLS NOT DISTINCT，或未能探测；保持 NULLS DISTINCT 并依赖应用层预查询（records.repository.ts importTravelRecords）作为主要去重路径。"}

       ## Impact on Plan

       - Task 2-5 schema & code 变更按默认 NULLS DISTINCT 策略执行，Task 4 db push 不使用 --force-reset。
       ```

    3. 不修改 schema.prisma（留给 Task 2）；不修改 repository.ts；不修改 service.ts。

    不允许：
    - 在未探测 PG 版本的情况下直接给 schema 加 `map: ..., nullsNotDistinct: true`（Prisma 6 此 API 未稳定，会破坏 db push 幂等性）。
    - 把探测结果只写进 stdout，不落地到 27-06-DB-PROBE.md（后续读者将无法得知为何不用 NULLS NOT DISTINCT）。
  </action>
  <verify>
    <automated>test -f .planning/phases/27-multi-visit-record-foundation/27-06-DB-PROBE.md &amp;&amp; grep -q "server_version_num" .planning/phases/27-multi-visit-record-foundation/27-06-DB-PROBE.md &amp;&amp; grep -q "NULLS DISTINCT" .planning/phases/27-multi-visit-record-foundation/27-06-DB-PROBE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/27-multi-visit-record-foundation/27-06-DB-PROBE.md 文件存在
    - grep -c "server_version_num" .planning/phases/27-multi-visit-record-foundation/27-06-DB-PROBE.md >= 1
    - grep -c "NULLS DISTINCT" .planning/phases/27-multi-visit-record-foundation/27-06-DB-PROBE.md >= 1
    - grep -c "Decision" .planning/phases/27-multi-visit-record-foundation/27-06-DB-PROBE.md >= 1
  </acceptance_criteria>
  <done>PG 版本探测记录落地；NULLS DISTINCT 决策已文档化；后续 Task 可以安全按默认 `@@unique` 语义推进。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 修改 Prisma schema — 在 UserTravelRecord 新增四元组 @@unique</name>
  <files>
    apps/server/prisma/schema.prisma
  </files>
  <read_first>
    - apps/server/prisma/schema.prisma 完整文件（确认 UserTravelRecord 当前 @@index 布局）
    - .planning/phases/27-multi-visit-record-foundation/27-06-DB-PROBE.md（Task 1 输出，确认 NULLS DISTINCT 决策）
    - .planning/phases/27-multi-visit-record-foundation/27-01-PLAN.md Task 2（已移除 `@@unique([userId, placeId])` 的变更历史）
  </read_first>
  <behavior>
    - 只修改 `model UserTravelRecord`，其他 model（User、SmokeRecord、AuthSession、TravelRecord legacy 等）不动。
    - 保留现有两条索引不变：`@@index([userId, placeId])` 与 `@@index([userId])`。
    - 在这两条索引之前（或之后，顺序不影响 Prisma）新增一条：
      ```
      @@unique([userId, placeId, startDate, endDate])
      ```
    - 不使用 `name: ...`、不使用 `map: ...`、不使用 `NULLS NOT DISTINCT`（Task 1 决策）——让 Prisma 自动生成 `UserTravelRecord_userId_placeId_startDate_endDate_key` 约束名。
    - **语义：** 同一 userId 下同一 placeId 不能存在两条 (startDate, endDate) 相同的记录（非 null 情况下；NULL 按 NULLS DISTINCT 允许多条并存——和应用层预查询口径一致）。
  </behavior>
  <action>
    打开 `apps/server/prisma/schema.prisma`，定位到 `model UserTravelRecord { ... }`。

    在现有的
    ```
      @@index([userId, placeId])
      @@index([userId])
    ```
    **之前** 插入一行：
    ```
      @@unique([userId, placeId, startDate, endDate])
    ```

    最终 model 尾部应为：
    ```
      user User @relation(fields: [userId], references: [id], onDelete: Cascade)

      @@unique([userId, placeId, startDate, endDate])
      @@index([userId, placeId])
      @@index([userId])
    }
    ```

    不允许：
    - 修改任何字段声明（startDate/endDate 类型、nullable 与否保持 `String?` 不变）。
    - 引入 `name:`、`map:`、`nullsNotDistinct:`（Task 1 决策）。
    - 修改其他 model 或 generator / datasource 块。
    - 运行 `prisma migrate dev` 创建 migration 文件（本项目一致用 `prisma db push`，见 27-01-PLAN.md Task 4）。
  </action>
  <verify>
    <automated>npx --yes prisma format --schema=apps/server/prisma/schema.prisma &amp;&amp; npx --yes prisma validate --schema=apps/server/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "@@unique(\[userId, placeId, startDate, endDate\])" apps/server/prisma/schema.prisma == 1
    - grep -c "@@index(\[userId, placeId\])" apps/server/prisma/schema.prisma == 1
    - grep -c "@@index(\[userId\])" apps/server/prisma/schema.prisma == 1
    - grep -c "@@unique(\[userId, placeId\])" apps/server/prisma/schema.prisma == 0（确认没有回滚 Phase 27 的 unique 降级）
    - grep -c "startDate      String?" apps/server/prisma/schema.prisma == 1
    - grep -c "endDate        String?" apps/server/prisma/schema.prisma == 1
    - `npx prisma validate --schema=apps/server/prisma/schema.prisma` 退出码 0
  </acceptance_criteria>
  <done>UserTravelRecord 有 (userId, placeId, startDate, endDate) 四元组 @@unique 兜底；`@@index([userId, placeId])` 查询索引保留；schema 通过校验。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 重新生成 Prisma Client + 推送 schema 到数据库</name>
  <files>
    apps/server/node_modules/.prisma/client/index.d.ts (generated)
  </files>
  <read_first>
    - apps/server/prisma/schema.prisma（Task 2 的 schema）
    - .planning/phases/27-multi-visit-record-foundation/27-01-PLAN.md Task 4（db push 约定与 --accept-data-loss 使用理由）
  </read_first>
  <behavior>
    - 先 `prisma generate` 让 Prisma Client 类型同步（新增唯一约束不会直接改变 UserTravelRecord 的 TS 类型，但会影响 `where: { userId_placeId_startDate_endDate: ... }` 的 input type 生成）。
    - 再 `prisma db push` 将四元组 unique 约束推送到 DB；成功后 DB 层开始兜底 createMany({ skipDuplicates: true })。
    - 如果 DB push 失败（最常见原因：存量数据中某 `(userId, placeId, startDate, endDate)` 已经存在重复），**必须停下并返回 structured 错误**，不得强制 `--force-reset`。此时产出一个 VERIFICATION.md override 建议给用户决定是否清理重复后再 push。
  </behavior>
  <action>
    1. 运行：
       ```bash
       pnpm --filter @trip-map/server exec prisma generate --schema=apps/server/prisma/schema.prisma
       ```
       退出码必须为 0。

    2. 运行：
       ```bash
       pnpm --filter @trip-map/server exec prisma db push --schema=apps/server/prisma/schema.prisma --accept-data-loss
       ```
       预期输出包含 `Your database is now in sync with your Prisma schema` 或等价成功提示。

    3. 如果 db push 因重复数据失败（stderr 含 `could not create unique index` 或 `duplicate key value violates unique constraint`）：
       - 立即停止后续 Task，在 27-06-SUMMARY.md 中记录：
         ```
         ⚠ DB push blocked: existing duplicates on (userId, placeId, startDate, endDate).
         建议：1) 登录 DB 手动合并重复记录；2) 用 `prisma db execute` 运行清理 SQL；3) 完成后重跑 Task 3。
         ```
       - 不得改用 `--force-reset`（会清空全量数据）。
       - 不得改回 schema（Task 2 变更正确，是存量数据违约束）。

    4. 不允许：
       - 跳过 `prisma generate`（会导致 Task 4 Prisma Client 类型不同步）。
       - 使用 `prisma migrate dev`（打破现有 db push 惯例）。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server exec prisma generate --schema=apps/server/prisma/schema.prisma &amp;&amp; pnpm --filter @trip-map/server exec prisma db push --schema=apps/server/prisma/schema.prisma --accept-data-loss</automated>
  </verify>
  <acceptance_criteria>
    - `prisma generate` 退出码 0
    - `prisma db push` 退出码 0，输出包含 "in sync with your Prisma schema" 或等价成功消息
    - 二次运行 `prisma db push --schema=apps/server/prisma/schema.prisma`（不带 --accept-data-loss）仍成功（幂等）
    - grep "userId_placeId_startDate_endDate" apps/server/node_modules/.prisma/client/index.d.ts 至少匹配 1 行（Prisma Client 已识别新唯一约束）
  </acceptance_criteria>
  <done>Prisma Client 已生成并识别四元组唯一约束；DB 中 UserTravelRecord 已添加对应唯一索引；`createMany({ skipDuplicates: true })` 现在有 DB 层兜底。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: CreateTravelRecordDto implements CreateTravelRecordRequest（契约绑定 + 字段收紧）</name>
  <files>
    apps/server/src/modules/records/dto/create-travel-record.dto.ts
  </files>
  <read_first>
    - apps/server/src/modules/records/dto/create-travel-record.dto.ts（完整 72 行——理解现有 @IsOptional + @Matches 装饰器布局）
    - packages/contracts/src/records.ts（CreateTravelRecordRequest 与 CanonicalPlaceSummary 的字段契约）
    - packages/contracts/src/place.ts（CanonicalPlaceSummary 的完整字段清单，确认所有非日期字段在 DTO 中都已声明——若有漂移也必须被 implements 检查出来）
    - apps/server/src/modules/records/records.controller.ts lines 62-77（`forbidNonWhitelisted: true` + `@Body() CreateTravelRecordDto`——确认改造后 runtime 行为不变）
    - apps/server/src/modules/records/records.service.spec.ts（baseInput 工厂使用 `as CreateTravelRecordDto`；本任务改动后类型收紧会触发的地方）
  </read_first>
  <behavior>
    - 在 `CreateTravelRecordDto` 上显式 `implements CreateTravelRecordRequest`。
    - 修改 `startDate` 与 `endDate` 字段签名：从 `startDate?: string | null` 改为 `startDate: string | null`（去掉 `?`——对齐 contract 的 required nullable）。
      - Class field 标注必须用 `!` 断言或默认 null，让 TS strict 模式通过：`startDate!: string | null`（延续 DTO 中其他 `!` 风格一致）。
    - **保留 `@IsOptional()`**：class-validator 的 `@IsOptional()` 在 body 缺失时允许跳过 `@Matches`，配合 `forbidNonWhitelisted: true` 时——缺失字段不会报 `property should not exist`，因为字段**在 whitelist 中**；所以 runtime 行为（缺失 = undefined，service 层 `input.startDate ?? null` 归一）**不变**。
    - 这是纯类型层收紧：若未来 contract 改名 `startDate` → `startDateISO`，DTO 编译会失败（预期效果，catch drift）。
  </behavior>
  <action>
    修改 `apps/server/src/modules/records/dto/create-travel-record.dto.ts`：

    1. 调整 import（在现有 `import type { ChinaAdminType, PlaceKind } from '@trip-map/contracts'` 同一段内，新增 `CreateTravelRecordRequest`）：
       ```typescript
       import type {
         ChinaAdminType,
         CreateTravelRecordRequest,
         PlaceKind,
       } from '@trip-map/contracts'
       ```

    2. 修改类声明行：
       ```typescript
       // 原：
       export class CreateTravelRecordDto {
       // 改为：
       export class CreateTravelRecordDto implements CreateTravelRecordRequest {
       ```

    3. 修改 `startDate` / `endDate` 声明（把 `?:` 改为 `!:`，保留所有装饰器不动）：
       ```typescript
       @ApiProperty({ nullable: true, example: '2025-10-01', required: false })
       @IsOptional()
       @Matches(/^\d{4}-\d{2}-\d{2}$/, { message: 'startDate must be YYYY-MM-DD' })
       startDate!: string | null

       @ApiProperty({ nullable: true, example: '2025-10-07', required: false })
       @IsOptional()
       @Matches(/^\d{4}-\d{2}-\d{2}$/, { message: 'endDate must be YYYY-MM-DD' })
       endDate!: string | null
       ```

    4. 不修改其他字段（placeId、boundaryId、placeKind、datasetVersion、displayName、regionSystem、adminType、typeLabel、parentLabel、subtitle）。如果 TS 编译此时报 `Class 'CreateTravelRecordDto' incorrectly implements interface 'CreateTravelRecordRequest'` 指向某个非日期字段：**说明 contract 有字段漂移，这正是本任务想 catch 的场景**——此时停止，返回错误让用户确认是 contract 还是 DTO 需要修正。

    ## 验证运行时行为未变

    运行：
    ```bash
    pnpm --filter @trip-map/server typecheck
    pnpm --filter @trip-map/server test --run src/modules/records/records.service.spec.ts
    pnpm --filter @trip-map/server test --run src/modules/records/records.repository.spec.ts
    ```
    三个命令都应绿（既有 null/valid/invalid/single-day/same-day 5 个 createTravel 用例保持通过）。

    不允许：
    - 把 `@IsOptional()` 改为 `@IsNotEmpty()` 或移除（会让缺失 body field 的老客户端报错）。
    - 修改 `@Matches` 正则。
    - 给 startDate 加 `@IsString()`（会与 null 值冲突，class-validator 对 null 不跳过 `@IsString`）。
    - 修改 records.controller.ts 或 records.service.ts。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server typecheck &amp;&amp; pnpm --filter @trip-map/server test --run src/modules/records/records.service.spec.ts src/modules/records/records.repository.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "implements CreateTravelRecordRequest" apps/server/src/modules/records/dto/create-travel-record.dto.ts == 1
    - grep -c "import type {" apps/server/src/modules/records/dto/create-travel-record.dto.ts == 1（单个 import type 语句，包含 CreateTravelRecordRequest）
    - grep -c "CreateTravelRecordRequest," apps/server/src/modules/records/dto/create-travel-record.dto.ts == 1
    - grep -c "startDate!: string | null" apps/server/src/modules/records/dto/create-travel-record.dto.ts == 1
    - grep -c "endDate!: string | null" apps/server/src/modules/records/dto/create-travel-record.dto.ts == 1
    - grep -c "startDate?: string" apps/server/src/modules/records/dto/create-travel-record.dto.ts == 0（旧可选写法已移除）
    - grep -c "endDate?: string" apps/server/src/modules/records/dto/create-travel-record.dto.ts == 0
    - `pnpm --filter @trip-map/server typecheck` 退出码 0
    - records.service.spec.ts 全部既有用例（createTravel 5 个）仍通过
  </acceptance_criteria>
  <done>CreateTravelRecordDto 在类型层直接被 CreateTravelRecordRequest 约束；shared contract 字段漂移会触发 DTO 编译错误；既有 runtime 测试全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: importTravel 复用 endDate >= startDate 校验 + 新增回归测试</name>
  <files>
    apps/server/src/modules/records/records.service.ts,
    apps/server/src/modules/records/records.service.spec.ts
  </files>
  <read_first>
    - apps/server/src/modules/records/records.service.ts 完整文件（理解 createTravel 与 importTravel 的结构差异，以及 `assertAuthoritativeOverseasRecord` private helper 风格）
    - apps/server/src/modules/records/records.service.spec.ts 完整文件（现有 5 个 createTravel 用例 + `createRepositoryMock` / `baseInput` / `baseRecord` 工厂）
    - apps/server/src/modules/records/dto/import-travel-records.dto.ts（ImportTravelRecordsDto.records 的类型——应为 `CreateTravelRecordDto[]`）
    - .planning/phases/27-multi-visit-record-foundation/27-VERIFICATION.md Anti-Patterns 表第 4 行（importTravel 未复用校验 warning）
  </read_first>
  <behavior>
    - 在 `RecordsService` 新增 private helper：
      ```typescript
      private assertValidDateRange(input: Pick<CreateTravelRecordDto, 'startDate' | 'endDate'>): void {
        if (input.startDate && input.endDate && input.endDate < input.startDate) {
          throw new BadRequestException('endDate must be >= startDate')
        }
      }
      ```
    - `createTravel` 把现有内联判定替换为 `this.assertValidDateRange(input)`。
    - `importTravel` 在遍历 `input.records` 的同一个 forEach 里，在 `assertAuthoritativeOverseasRecord` 之后追加一次 `this.assertValidDateRange(record)`。
    - 任何一条 record 非法 → 抛 `BadRequestException('endDate must be >= startDate')`，并且此时 `recordsRepository.importTravelRecords` 必须**未被调用**（不允许部分写入）。
    - 新增 spec 用例覆盖 importTravel 的新行为：
      - `importTravel rejects records with endDate before startDate`（核心回归）
      - `importTravel accepts records with valid date ranges`（保障原有 happy path 未被打破）
      - `CreateTravelRecordDto implements the shared CreateTravelRecordRequest contract`（编译期绑定的类型级断言——用 `expectTypeOf` 或简单的 `const _check: CreateTravelRecordRequest = {} as CreateTravelRecordDto` 语句，确保若未来 contract 漂移时测试编译失败）
  </behavior>
  <action>
    ## 修改 apps/server/src/modules/records/records.service.ts

    1. 在 `RecordsService` 类的 private method 区（`assertAuthoritativeOverseasRecord` 附近），新增：

       ```typescript
       private assertValidDateRange(
         input: Pick<CreateTravelRecordDto, 'startDate' | 'endDate'>,
       ): void {
         if (input.startDate && input.endDate && input.endDate < input.startDate) {
           throw new BadRequestException('endDate must be >= startDate')
         }
       }
       ```

    2. 把 `createTravel` 方法内现有的：
       ```typescript
       if (input.startDate && input.endDate && input.endDate < input.startDate) {
         throw new BadRequestException('endDate must be >= startDate')
       }
       ```
       替换为：
       ```typescript
       this.assertValidDateRange(input)
       ```

    3. 修改 `importTravel` 的第一行 forEach：
       ```typescript
       // 原：
       input.records.forEach(record => this.assertAuthoritativeOverseasRecord(record))
       // 改为：
       input.records.forEach(record => {
         this.assertAuthoritativeOverseasRecord(record)
         this.assertValidDateRange(record)
       })
       ```

    4. 不修改其他方法（createSmoke / findAllTravel / deleteTravel / toContractTravelRecord / assertAuthoritativeOverseasRecord）。

    ## 扩展 apps/server/src/modules/records/records.service.spec.ts

    在文件末尾（最后一个 `})` 之前）新增：

    ```typescript
      it('importTravel rejects records with endDate before startDate', async () => {
        const repository = createRepositoryMock()
        const service = new RecordsService(repository as never)

        const invalidRecord = baseInput({
          startDate: '2025-10-07',
          endDate: '2025-10-01',
        })
        const validRecord = baseInput({
          placeId: 'cn-admin-shanghai',
          boundaryId: 'cn-admin-shanghai-boundary',
          displayName: '上海市',
          startDate: '2025-11-01',
          endDate: '2025-11-05',
        })

        await expect(
          service.importTravel('user-1', { records: [validRecord, invalidRecord] }),
        ).rejects.toBeInstanceOf(BadRequestException)
        await expect(
          service.importTravel('user-1', { records: [validRecord, invalidRecord] }),
        ).rejects.toThrow('endDate must be >= startDate')

        expect(repository.importTravelRecords).not.toHaveBeenCalled()
      })

      it('importTravel accepts records with valid date ranges and null dates', async () => {
        const repository = createRepositoryMock()
        const service = new RecordsService(repository as never)

        const records = [
          baseInput({ startDate: '2025-10-01', endDate: '2025-10-07' }),
          baseInput({
            placeId: 'cn-admin-shanghai',
            boundaryId: 'cn-admin-shanghai-boundary',
            displayName: '上海市',
            startDate: null,
            endDate: null,
          }),
        ]

        repository.importTravelRecords.mockResolvedValueOnce({
          importedCount: 2,
          mergedDuplicateCount: 0,
          finalCount: 2,
          records: [
            baseRecord({ startDate: '2025-10-01', endDate: '2025-10-07' }),
            baseRecord({
              placeId: 'cn-admin-shanghai',
              displayName: '上海市',
              startDate: null,
              endDate: null,
            }),
          ],
        })

        const result = await service.importTravel('user-1', { records })

        expect(repository.importTravelRecords).toHaveBeenCalledTimes(1)
        expect(result.importedCount).toBe(2)
      })

      it('CreateTravelRecordDto implements the shared CreateTravelRecordRequest contract', () => {
        // 编译期断言：若 contract 漂移，此处编译失败（TS 会拒绝 assignment）。
        // 运行时仅 smoke——目标是让 `tsc --noEmit` 捕获类型绑定回退。
        const sampleDto: import('./dto/create-travel-record.dto.js').CreateTravelRecordDto = {
          placeId: 'cn-admin-beijing',
          boundaryId: 'cn-admin-beijing-boundary',
          placeKind: 'CN_ADMIN',
          datasetVersion: 'cn-admin-2024-r1',
          displayName: '北京市',
          regionSystem: 'CN',
          adminType: 'MUNICIPALITY',
          typeLabel: '直辖市',
          parentLabel: '中国',
          subtitle: '直辖市 · 中国',
          startDate: null,
          endDate: null,
        } as const satisfies import('@trip-map/contracts').CreateTravelRecordRequest

        expect(sampleDto.startDate).toBeNull()
        expect(sampleDto.endDate).toBeNull()
      })
    ```

    不允许：
    - 把新增用例写到别的 describe block（保持在现有 `describe('RecordsService', ...)` 内）。
    - 去掉 `expect(repository.importTravelRecords).not.toHaveBeenCalled()` 断言（这是"不允许部分写入"的核心证据）。
    - 修改既有 5 个 createTravel 用例中的任何一个（createTravel 行为未变）。
    - 让 assertValidDateRange 变为 public（抽取的目的是 dry，不是 API）。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server typecheck &amp;&amp; pnpm --filter @trip-map/server test --run src/modules/records/records.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "private assertValidDateRange" apps/server/src/modules/records/records.service.ts == 1
    - grep -c "this.assertValidDateRange(input)" apps/server/src/modules/records/records.service.ts == 1（createTravel 内）
    - grep -c "this.assertValidDateRange(record)" apps/server/src/modules/records/records.service.ts == 1（importTravel 内）
    - grep -c "input.startDate && input.endDate && input.endDate < input.startDate" apps/server/src/modules/records/records.service.ts == 1（只在 helper 里保留一次，createTravel 原内联已替换）
    - grep -c "importTravel rejects records with endDate before startDate" apps/server/src/modules/records/records.service.spec.ts == 1
    - grep -c "importTravel accepts records with valid date ranges and null dates" apps/server/src/modules/records/records.service.spec.ts == 1
    - grep -c "CreateTravelRecordDto implements the shared CreateTravelRecordRequest contract" apps/server/src/modules/records/records.service.spec.ts == 1
    - grep -c "satisfies import('@trip-map/contracts').CreateTravelRecordRequest" apps/server/src/modules/records/records.service.spec.ts == 1
    - `pnpm --filter @trip-map/server typecheck` 退出码 0
    - `pnpm --filter @trip-map/server test --run src/modules/records/records.service.spec.ts` 退出码 0，所有 createTravel 既有用例 + 3 个新用例通过
  </acceptance_criteria>
  <done>assertValidDateRange helper 抽取；createTravel 与 importTravel 共享日期校验；导入非法日期区间被数据库前一层拒绝；DTO 契约绑定在 spec 层有编译期断言兜底。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: 全量 server + contracts 测试 + 更新 VERIFICATION 建议</name>
  <files>
    （本任务不新建文件，仅运行全量测试并为 27-06-SUMMARY.md 收集证据）
  </files>
  <read_first>
    - .planning/phases/27-multi-visit-record-foundation/27-VERIFICATION.md（将在 Summary 阶段建议哪些条目从 FAILED/PARTIAL 升级为 VERIFIED）
  </read_first>
  <behavior>
    - 确认 Task 1-5 改动没有破坏其他包（auth、contracts、web）的测试。
    - 特别关注：`auth-bootstrap.e2e-spec.ts` 使用 POST /records 创建记录后调用 /auth/bootstrap；新增的 `@@unique([userId, placeId, startDate, endDate])` 必须不影响它（测试里每次 create 都是新记录，startDate 不同或者测试在清库环境下）。
    - 若任一测试因新增 DB 唯一约束而失败，停下并产出诊断——很可能是测试夹具里同一个 (userId, placeId, startDate, endDate) 被插入两次（以前因没有 unique 约束而默默通过，现在暴露了测试数据污染）。
  </behavior>
  <action>
    1. 运行：
       ```bash
       pnpm --filter @trip-map/contracts build
       pnpm --filter @trip-map/contracts typecheck
       pnpm --filter @trip-map/server typecheck
       pnpm --filter @trip-map/server test
       ```

    2. 如果 server test 中有 e2e 失败（如 auth-bootstrap.e2e-spec.ts 报 unique constraint violation）：
       - 检查失败的 spec 是否在 beforeEach 里没清 UserTravelRecord 表；
       - 如果是测试夹具问题：在本 plan 范围内最小修正（只动导致 DB 冲突的 beforeEach/afterEach 清理语句），**不得** 回退 Task 2 的 schema 变更。
       - 如果 >2 个 spec 都受影响：停下，返回 `## GAP CLOSURE BLOCKED` 到 orchestrator，由用户决定是否把 DB 唯一约束延后或加 NULLS NOT DISTINCT。

    3. 确认 web 包未被间接打破（契约 build 更新后）：
       ```bash
       pnpm --filter @trip-map/web typecheck
       pnpm --filter @trip-map/web test --run src/services/legacy-point-storage.spec.ts src/stores/map-points.spec.ts
       ```
       应全部绿（这两个 spec 直接消费 contracts，最容易因 contract 漂移或字段收紧受影响）。

    4. 在执行后，将下列事实采集到日志中供 Summary 使用：
       - 各命令退出码
       - `test` 输出的"X passed / Y total"数字
       - `schema.prisma` 最终 UserTravelRecord 的 @@unique / @@index 行（通过 grep 记录）
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/contracts build &amp;&amp; pnpm --filter @trip-map/server typecheck &amp;&amp; pnpm --filter @trip-map/server test &amp;&amp; pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 所有上述命令退出码 0
    - server 包 vitest 无新增失败用例（既有用例数 + 本 plan 新增 3 个）
    - web 包 typecheck 不因 contracts 变更报错（contracts 本身未改字段，仅 DTO 绑定方式变化，对 web 无影响）
    - `grep '@@unique(\[userId, placeId, startDate, endDate\])' apps/server/prisma/schema.prisma` 仍匹配 1 行（schema 未意外回滚）
  </acceptance_criteria>
  <done>跨包回归确认 Gap 关闭变更未击穿既有绿色面；Summary 有完整数据支撑 VERIFICATION 升级建议。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP body → CreateTravelRecordDto | DTO 级 class-validator 校验仍是权威防线；新的 `implements` 仅收紧编译期契约，对 runtime 无影响 |
| HTTP body (batch) → importTravel | 新增的 assertValidDateRange 是额外服务端防线，防止恶意/错误 legacy import 构造 endDate < startDate |
| records.repository.createMany → PostgreSQL | 新增的 DB 唯一约束兜底应用层的预查询去重；并发写入场景下 DB 层是最终一致性锚 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | Tampering | importTravel 接受 endDate < startDate 批量污染 | mitigate | Task 5 在 forEach 内复用 assertValidDateRange，任一非法即中止整批；spec 锁定"不得部分写入"契约 |
| T-27-06-02 | Tampering | shared contract 字段被重命名（例如 startDate → startDateISO），DTO 不同步 | mitigate | Task 4 的 `implements CreateTravelRecordRequest` + Task 5 的 spec satisfies 双层编译期断言 |
| T-27-06-03 | Tampering / DoS | 并发 importTravel 同时尝试插入 (userId, placeId, startDate, endDate) 完全相同的两条记录 | mitigate | Task 2 新增 DB 唯一约束；Prisma `createMany({ skipDuplicates: true })` 在 ON CONFLICT 上真正生效 |
| T-27-06-04 | Information Disclosure | DB 探测 Task 1 误把生产 DATABASE_URL 版本信息写进 27-06-DB-PROBE.md | mitigate | Task 1 action 明确要求 "host redacted"，且 probe 命令输出仅 server_version_num 一个数字，不含连接串或数据样本 |
| T-27-06-05 | Integrity | 存量数据中存在 (userId, placeId, null, null) 多行（legacy 日期未知记录）导致 prisma db push 失败 | accept | Task 3 action 约定此情况时停止并报告，不强制 --force-reset；实际业务中存量 legacy 记录仅存在于单用户首登路径，重复概率极低 |
</threat_model>

<verification>
1. `.planning/phases/27-multi-visit-record-foundation/27-06-DB-PROBE.md` 存在并包含 server_version_num 与 NULLS DISTINCT 决策。
2. `npx prisma validate --schema=apps/server/prisma/schema.prisma` 退出码 0；schema.prisma 含四元组 @@unique。
3. `pnpm --filter @trip-map/server exec prisma db push` 成功；二次运行幂等。
4. `pnpm --filter @trip-map/server typecheck` 退出码 0。
5. `pnpm --filter @trip-map/server test` 全量退出码 0；新增 3 个用例（importTravel reject + accept + contract binding）通过；既有 createTravel 5 个用例通过；auth + e2e 既有用例不受影响。
6. `pnpm --filter @trip-map/contracts build` 与 `pnpm --filter @trip-map/web typecheck` 都退出码 0（跨包回归）。
7. grep 确认 `implements CreateTravelRecordRequest` 与 `private assertValidDateRange` 都落位。
</verification>

<success_criteria>
- VERIFICATION.md key-link PARTIAL（CreateTravelRecordRequest -> CreateTravelRecordDto 直接绑定）关闭：DTO 显式 implements；spec 加编译期断言。
- VERIFICATION.md Anti-Patterns warning（importTravel 未复用校验）关闭：assertValidDateRange 共享 helper。
- VERIFICATION.md Anti-Patterns warning（createMany skipDuplicates 缺 DB 约束兜底）关闭：UserTravelRecord 新增四元组 @@unique。
- server 包与 web 包全量测试通过；跨包无回归。
- Phase 27 可进入"等待第二次 verification"状态：truth #10 由 Plan 05 关闭、key-link PARTIAL 由本 plan 关闭，达成 11/11 truths verified 的目标。
</success_criteria>

<output>
After completion, create `.planning/phases/27-multi-visit-record-foundation/27-06-SUMMARY.md` including:
- 契约绑定前后 DTO diff 摘要（新增 `implements` + 字段从 `?:` 改为 `!:`）
- records.service.ts 中 assertValidDateRange 抽取前后的调用点对比
- schema.prisma 四元组 @@unique 的 Prisma 命令输出（generate + db push 退出码）
- 27-06-DB-PROBE.md 的 PG 版本探测结果与 NULLS DISTINCT 决策记录链接
- 全量测试通过数：server / contracts / web 分别报告
- 对 VERIFICATION.md 的后续建议清单（建议把 key-link PARTIAL 升级为 ✓ WIRED；Anti-Pattern 表的两条 warning 升级为 RESOLVED）
- Phase 27 当前可判定状态：Gap Plan 05 + 06 合并后可进入第二次 verification
</output>

---
phase: 27-multi-visit-record-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 27-04
files_modified:
  - apps/web/src/components/LeafletMapStage.vue
  - apps/web/src/components/LeafletMapStage.spec.ts
autonomous: true
requirements:
  - TRIP-01
  - TRIP-03
tags:
  - frontend
  - vue
  - gap-closure
  - phase-27
gap_closure: true
closes_gaps:
  - "truth-10: 已点亮地点 popup 的『最近一次日期/范围』必须反映最近一次旅行，而不是最近一次录入（VERIFICATION 2026-04-20, blocker）"
user_setup: []
must_haves:
  truths:
    - "activePointLatestTripLabel 计算『最近一次』时按旅行日期（优先 endDate，其次 startDate）选择，而不是按 record.createdAt 选择"
    - "当最新录入的记录日期早于已有记录时，『最近一次』摘要仍显示真正旅行时间最近的那一条"
    - "当 tripsByPlaceId 中所有记录均为日期未知（startDate === null）时，latestTripLabel 回退为 null（UI 层显示『日期未知』）"
    - "当混合存在『有日期』与『日期未知』记录时，latestTripLabel 选择『有日期』记录中旅行时间最新的那一条（日期未知记录不参与排序）"
    - "LeafletMapStage.spec.ts 中新增『后录入但旅行时间较早』的回归用例，能在 selector 回退到 createdAt 时失败、在正确按旅行日期排序时通过"
  artifacts:
    - path: "apps/web/src/components/LeafletMapStage.vue"
      provides: "按旅行日期排序的 activePointLatestTripLabel computed"
      contains: "getLatestTripSortKey"
    - path: "apps/web/src/components/LeafletMapStage.spec.ts"
      provides: "latestTripLabel 按旅行日期选择的回归测试"
      contains: "真正最近的旅行"
  key_links:
    - from: "apps/web/src/stores/map-points.ts tripsByPlaceId"
      to: "apps/web/src/components/LeafletMapStage.vue activePointLatestTripLabel"
      via: "按 endDate ?? startDate 字典序降序挑选首条『有日期』记录"
      pattern: "endDate \\?\\? .*startDate"
    - from: "apps/web/src/components/LeafletMapStage.vue activePointLatestTripLabel"
      to: "apps/web/src/components/map-popup/MapContextPopup.vue latestTripLabel prop"
      via: ":latest-trip-label binding"
      pattern: ":latest-trip-label"
---

<objective>
Phase 27 Gap 关闭 — Wave 4（Blocker gap）：
修复 `VERIFICATION.md` truth #10 FAILED — 已点亮地点 popup 摘要的『最近一次日期/范围』必须反映真正**旅行时间最近**的那一条，而不是**最后录入时间最近**的那一条（D-05）。

根因：`apps/web/src/components/LeafletMapStage.vue` 472-483 行当前实现 `activePointLatestTripLabel` 时，把 `realRecords` 按 `createdAt` 降序排序后取首条；如果用户先保存 2025-10-01 的旅行，之后又补录 2025-05-01 的旧旅行，那么 popup 会把 2025-05-01 当作『最近一次』，这直接违背 D-05 语义与用户直觉。

本 Plan 只做两件事：
1. **修正 selector**：在 `LeafletMapStage.vue` 内把排序键从 `record.createdAt` 改为旅行日期（优先 `endDate`，其次 `startDate`；两者同时为 null 的记录不参与排序，退到 `null` → UI 层显示『日期未知』）。
2. **补回归测试**：在 `LeafletMapStage.spec.ts` 的 `describe('illuminate actions', ...)` 内新增一个用例：先塞入一条 2025-10-01 的记录（但 `createdAt` 较早），再塞入一条 2025-05-01 的记录（但 `createdAt` 较晚），mount 后断言 popup 摘要仍显示 `2025-10-01`。这个用例在当前 `createdAt` selector 下会失败，切到旅行日期 selector 后必须通过。

Purpose: 让 D-05 的语义真正落地（摘要 = 最近一次旅行，不是最近一次录入），并给未来类似回归兜底。Scope 严格收敛到 selector 逻辑与一个回归用例，不触碰 store、不触碰 popup/表单、不触碰 server。

Output: Blocker gap 关闭、LeafletMapStage.spec 增加 1 个回归用例、web 包 typecheck + test 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27-multi-visit-record-foundation/27-CONTEXT.md
@.planning/phases/27-multi-visit-record-foundation/27-VERIFICATION.md
@.planning/phases/27-multi-visit-record-foundation/27-04-PLAN.md
@.planning/phases/27-multi-visit-record-foundation/27-04-SUMMARY.md

<interfaces>
<!-- Source: apps/web/src/components/LeafletMapStage.vue lines 472-484 [VERIFIED 2026-04-20] -->

```typescript
// 当前有 bug 的实现：按 createdAt 排序
const activePointLatestTripLabel = computed<string | null>(() => {
  const pid = activePointPlaceId.value
  if (!pid) return null

  const records = mapPointsStore.tripsByPlaceId.get(pid) ?? []
  const realRecords = records.filter((record) => !record.id.startsWith('pending-'))
  if (realRecords.length === 0) return null

  const latest = [...realRecords].sort((a, b) => b.createdAt.localeCompare(a.createdAt))[0]
  if (!latest || latest.startDate === null) return null

  return latest.endDate ? `${latest.startDate} - ${latest.endDate}` : latest.startDate
})
```

<!-- Source: apps/web/src/stores/map-points.ts lines 119-126 [VERIFIED 2026-04-20] -->
<!-- tripsByPlaceId: Map<placeId, TravelRecord[]>；TravelRecord 含 startDate / endDate: string | null（YYYY-MM-DD）-->

<!-- Source: apps/web/src/components/LeafletMapStage.spec.ts lines 759-901 [VERIFIED 2026-04-20] -->
<!-- describe('illuminate actions', ...)：现有 mount 方式 + mapPointsStore.replaceTravelRecords() 预置 + selectPointById -->
<!-- makeRecord(place, overrides) 工厂函数在 lines 144-165，支持 startDate/endDate/createdAt overrides -->

<!-- 字典序比较保证 YYYY-MM-DD 字典序 = 时间序（RESEARCH.md Assumption A1 已验证）-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 LeafletMapStage.spec.ts 新增『旅行日期选择最近一次』回归用例（RED）</name>
  <files>
    apps/web/src/components/LeafletMapStage.spec.ts
  </files>
  <read_first>
    - apps/web/src/components/LeafletMapStage.spec.ts 第 1-250 行（helpers: makeRecord、makeDraftPoint、mount 约定、pinia setup）
    - apps/web/src/components/LeafletMapStage.spec.ts 第 759-901 行（describe('illuminate actions', ...) 的现有 "re-records a visit from the 再记一次 CTA" 用例——本任务用例将放在其后，复用其 mount+selectPointById 模式）
    - apps/web/src/components/LeafletMapStage.vue 第 465-484 行（activePointTripCount + activePointLatestTripLabel 的现有实现——理解将要被 RED 的代码路径）
    - .planning/phases/27-multi-visit-record-foundation/27-VERIFICATION.md 第 8-18 行（gap 1 的详细描述与 missing 清单）
    - .planning/phases/27-multi-visit-record-foundation/27-CONTEXT.md D-05（摘要语义契约）
  </read_first>
  <behavior>
    - 新增一个 test `it('selects the latest trip by travel date, not by createdAt', async ...)`，放在现有 `re-records a visit from the "再记一次" CTA on an illuminated point` 用例之后、`renders fallback illuminate affordance as disabled ...` 用例之前（即 `describe('illuminate actions', ...)` 内部）。
    - 场景：
      - 预置两条同一个 placeId（PHASE12_RESOLVED_BEIJING）的真实记录：
        - 记录 A（先录入）：`startDate: '2025-10-01'`、`endDate: null`、`createdAt: '2025-10-01T00:00:00.000Z'`、`id: 'server-rec-beijing-newer-trip'`。
        - 记录 B（后录入，但旅行时间更早）：`startDate: '2025-05-01'`、`endDate: '2025-05-05'`、`createdAt: '2025-11-20T00:00:00.000Z'`、`id: 'server-rec-beijing-older-trip'`。
      - 通过 `mapPointsStore.replaceTravelRecords([A, B])` 注入（复用现有 bootstrap snapshot 入口，不走 illuminate）。
      - mount LeafletMapStage、设置 popup anchor virtualElement、`selectPointById(PHASE12_RESOLVED_BEIJING.placeId)`、`await nextTick(); await flushPromises()`。
    - 断言：
      - `wrapper.get('[data-trip-summary-count]').text()` 包含 `已去过 2 次`（证明 tripsByPlaceId 正确聚合）。
      - `wrapper.get('[data-trip-summary-latest]').text()` 包含 `2025-10-01` 且 **不** 包含 `2025-05-01`（核心断言——证明『最近一次』按旅行日期而非 createdAt 选择）。
    - 该用例在修复前（Task 2 执行前）必须失败（RED）；修复后必须通过（GREEN）。
    - 不得修改 makeRecord 工厂函数签名；使用 overrides 即可。
    - 不得删除或修改既有 3 个 illuminate-actions 用例中的任何断言。
  </behavior>
  <action>
    在 apps/web/src/components/LeafletMapStage.spec.ts 的 `describe('illuminate actions', ...)` 块内，在 `it('re-records a visit from the "再记一次" CTA on an illuminated point', ...)` 测试结束后（约 line 901 `})` 之后、下一个 `it('renders fallback illuminate affordance as disabled ...')` 之前），插入以下完整的新测试用例：

    ```typescript
    it('selects the latest trip by travel date, not by createdAt (VERIFICATION gap close)', async () => {
      // 回归保护：VERIFICATION.md truth #10 gap 1
      // 用户先录入一次 2025-10-01 的旅行，然后补录一次旅行时间更早（2025-05-01 -- 2025-05-05）
      // 但 createdAt 更晚的历史记录。
      // popup 摘要必须显示真正最近的旅行日期 2025-10-01，而不是最后被录入的 2025-05-01。
      const authSessionStore = useAuthSessionStore()
      authSessionStore.status = 'authenticated'
      authSessionStore.currentUser = {
        id: 'user-1',
        username: 'Alice',
        email: 'alice@example.com',
        createdAt: '2026-04-12T00:00:00.000Z',
      }

      const mapPointsStore = useMapPointsStore()

      // 记录 A：先录入（createdAt 较早），但旅行时间是真正最近的一次
      const newerTripRecord = makeRecord(PHASE12_RESOLVED_BEIJING, {
        id: 'server-rec-beijing-newer-trip',
        startDate: '2025-10-01',
        endDate: null,
        createdAt: '2025-10-01T00:00:00.000Z',
      })
      // 记录 B：后录入（createdAt 较晚），但旅行时间是更早的一次
      const olderTripRecord = makeRecord(PHASE12_RESOLVED_BEIJING, {
        id: 'server-rec-beijing-older-trip',
        startDate: '2025-05-01',
        endDate: '2025-05-05',
        createdAt: '2025-11-20T00:00:00.000Z',
      })

      mapPointsStore.replaceTravelRecords([newerTripRecord, olderTripRecord])

      const wrapper = mount(LeafletMapStage, { global: { plugins: [pinia] } })

      ;(popupAnchorContainer.virtualElementRef as any).value = makeVirtualElement()
      mapPointsStore.selectPointById(PHASE12_RESOLVED_BEIJING.placeId)
      await nextTick()
      await flushPromises()

      // 先验证聚合正确（tripsByPlaceId 里真的有两条）
      expect(wrapper.get('[data-trip-summary-count]').text()).toContain('已去过 2 次')

      // 核心断言：摘要必须展示真正旅行时间最近的 2025-10-01，而不是最后录入的 2025-05-01
      const latestText = wrapper.get('[data-trip-summary-latest]').text()
      expect(latestText).toContain('2025-10-01')
      expect(latestText).not.toContain('2025-05-01')
    })
    ```

    不允许：
    - 把这个用例标记为 `it.skip` / `it.todo`。
    - 修改 makeRecord 工厂函数或其默认值。
    - 触碰既有的 `re-records a visit from the "再记一次" CTA on an illuminated point` 用例的任何断言。
    - 把新用例放到 `describe('illuminate actions', ...)` 之外（必须利用现有 beforeEach pinia 与 mocks 配置）。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test --run src/components/LeafletMapStage.spec.ts -t "selects the latest trip by travel date"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "selects the latest trip by travel date, not by createdAt" apps/web/src/components/LeafletMapStage.spec.ts == 1
    - grep -c "server-rec-beijing-newer-trip" apps/web/src/components/LeafletMapStage.spec.ts == 1
    - grep -c "server-rec-beijing-older-trip" apps/web/src/components/LeafletMapStage.spec.ts == 1
    - grep -c "2025-05-01" apps/web/src/components/LeafletMapStage.spec.ts >= 2
    - grep -c "2025-10-01" apps/web/src/components/LeafletMapStage.spec.ts >= 1
    - grep -c "expect(latestText).not.toContain" apps/web/src/components/LeafletMapStage.spec.ts == 1
    - 该用例在 Task 2 完成前运行会失败（RED 状态，证明回归护栏有效）；Task 2 完成后运行通过
  </acceptance_criteria>
  <done>LeafletMapStage.spec.ts 的 `describe('illuminate actions')` 内新增一条『按旅行日期选最近一次』回归用例；尚未运行 Task 2 时该用例处于 RED。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修正 activePointLatestTripLabel selector（改按旅行日期排序）</name>
  <files>
    apps/web/src/components/LeafletMapStage.vue
  </files>
  <read_first>
    - apps/web/src/components/LeafletMapStage.vue 第 450-505 行（activePointTripCount / activePointLatestTripLabel / isActivePointIlluminatable computed 三者相对位置）
    - apps/web/src/stores/map-points.ts 第 119-126 行（tripsByPlaceId 的聚合口径，确认值类型是 TravelRecord[]）
    - packages/contracts/src/records.ts（TravelRecord.startDate/endDate: string | null 契约）
    - .planning/phases/27-multi-visit-record-foundation/27-VERIFICATION.md 第 8-18 行（gap 1 的 missing 清单：优先 endDate，其次 startDate；日期未知回退为"日期未知"）
    - .planning/phases/27-multi-visit-record-foundation/27-CONTEXT.md D-02（YYYY-MM-DD 精度到天，字典序 = 时间序）、D-05（摘要语义）、D-08（未知不得伪装成真实日期）
  </read_first>
  <behavior>
    - 只修改 `activePointLatestTripLabel` computed 的内部实现，不改签名、不改导入、不改模板绑定。
    - 新的 selector 要求：
      1. 过滤掉 `id.startsWith('pending-')` 的乐观记录（保持既有行为）。
      2. 只从『有日期』记录（即 `startDate !== null`，遵循 D-08：null = 日期未知，不参与排序）中挑选最近一次。
      3. 排序键为 `endDate ?? startDate`（字符串字典序降序）；即：
         - 若记录有 `endDate`，用 `endDate` 作为排序代表；
         - 否则（`endDate === null`，表示单日旅行或未填结束日期）用 `startDate` 作为排序代表。
      4. 当所有 real records 都是『日期未知』（startDate 全为 null）时，返回 `null`（UI 层 PointSummaryCard 会显示『最近一次: 日期未知』）。
      5. 当混合存在『有日期』与『日期未知』记录时，仅从『有日期』记录里选，日期未知记录不影响结果（即：一旦有任意一条『有日期』记录，就不再回退到『日期未知』文案）。
    - 格式化语义不变：若选中记录的 `endDate` 存在 → `${startDate} - ${endDate}`，否则 → `${startDate}`。
    - 字典序比较安全性：YYYY-MM-DD 固定格式，字典序 = 时间序（RESEARCH.md Assumption A1 已验证）。不要引入 Date 对象或时区计算。
    - 相等情况的并列打破（两条记录排序键相等时）：此情况在现实中属于"同一天多次保存"，无用户可见差异；选用任意一条都可接受——本实现让 Array.prototype.sort 的稳定性自然决定（V8 自 12.0 起已稳定）。不需要额外 tie-breaker。
  </behavior>
  <action>
    在 apps/web/src/components/LeafletMapStage.vue 中，**只修改 `activePointLatestTripLabel` 这一个 computed**，把现有 472-484 行替换为下面的新实现（保持相同的导出位置、相同的签名、相同的类型参数）：

    ```typescript
    const activePointLatestTripLabel = computed<string | null>(() => {
      const pid = activePointPlaceId.value
      if (!pid) return null

      const records = mapPointsStore.tripsByPlaceId.get(pid) ?? []
      const realRecords = records.filter((record) => !record.id.startsWith('pending-'))
      if (realRecords.length === 0) return null

      // 只从『有日期』的记录里选最近一次（D-08：日期未知记录不参与排序）。
      // 排序键：优先 endDate（旅行结束），其次 startDate（单日旅行 / 未填结束日期）。
      // YYYY-MM-DD 字典序 = 时间序，无需 Date 对象。
      const datedRecords = realRecords.filter((record) => record.startDate !== null)
      if (datedRecords.length === 0) return null

      const getLatestTripSortKey = (record: typeof datedRecords[number]): string =>
        record.endDate ?? record.startDate ?? ''

      const latest = [...datedRecords].sort((a, b) =>
        getLatestTripSortKey(b).localeCompare(getLatestTripSortKey(a)),
      )[0]

      if (!latest || latest.startDate === null) return null

      return latest.endDate ? `${latest.startDate} - ${latest.endDate}` : latest.startDate
    })
    ```

    不允许：
    - 把排序键改回 `createdAt`。
    - 引入 `new Date(...)` 或 `Date.parse(...)` 做时区敏感比较。
    - 修改 `activePointTripCount` computed（次数口径仍按全部记录，包括日期未知，不受本修复影响）。
    - 修改模板中的 `:latest-trip-label="activePointLatestTripLabel"` 绑定或 MapContextPopup props。
    - 在同一文件里新增/删除其他 computed、import、helper（保持本 plan 变更最小化）。

    ## 运行全量 web 测试
    保存后运行：
    ```bash
    pnpm --filter @trip-map/web typecheck
    pnpm --filter @trip-map/web test --run src/components/LeafletMapStage.spec.ts
    pnpm --filter @trip-map/web test
    ```
    全部退出码 0。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web typecheck &amp;&amp; pnpm --filter @trip-map/web test</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "record.createdAt.localeCompare" apps/web/src/components/LeafletMapStage.vue == 0（旧 selector 已移除）
    - grep -c "getLatestTripSortKey" apps/web/src/components/LeafletMapStage.vue >= 2
    - grep -c "record.endDate ?? record.startDate" apps/web/src/components/LeafletMapStage.vue >= 1
    - grep -c "datedRecords" apps/web/src/components/LeafletMapStage.vue >= 2
    - grep -c "activePointLatestTripLabel = computed" apps/web/src/components/LeafletMapStage.vue == 1（仍是唯一同名 computed）
    - grep -c "pending-" apps/web/src/components/LeafletMapStage.vue >= 1（pending 过滤保留）
    - `pnpm --filter @trip-map/web typecheck` 退出码 0
    - `pnpm --filter @trip-map/web test --run src/components/LeafletMapStage.spec.ts` 退出码 0，且 `selects the latest trip by travel date` 用例通过
    - `pnpm --filter @trip-map/web test` 全量退出码 0（包含 map-points.spec、LeafletMapStage.spec、PointSummaryCard.spec、TripDateForm.spec 等全部既有测试）
  </acceptance_criteria>
  <done>activePointLatestTripLabel 已按旅行日期选最近一次；RED 回归用例转绿；web 包 typecheck + 全量 vitest 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mapPointsStore.tripsByPlaceId → LeafletMapStage computed | 纯读路径，数据源来自已授信的 store（bootstrap/authoritative），本 plan 不新增输入边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | Tampering | `record.startDate` / `record.endDate` 被人为构造为非法字符串（如空串、非 YYYY-MM-DD） | mitigate | 服务端 DTO 的 `@Matches(/^\d{4}-\d{2}-\d{2}$/)`（Plan 02）是权威防线；本 selector 对 `null` 有显式回退，对非法非空字符串会走字典序比较但不崩溃（最多选错一次，不影响安全） |
| T-27-05-02 | Information Disclosure | 跨账号 session 切换时，旧账号的 tripsByPlaceId 数据被渲染给新账号 | accept | 不在本 plan 职责内——`auth-session.ts` 的 boundary 切换机制已处理（Plan 03），本 plan 只消费派生视图 |
| T-27-05-03 | Denial of Service | tripsByPlaceId 某地点存在超大记录数（> 10k）导致 sort 性能问题 | accept | 真实场景单个 placeId 的 trip 数量上限远低于性能拐点（单用户一年最多几十次同一地点的旅行）；Array.sort 对 < 1000 元素是 O(n log n) 稳定可接受 |
</threat_model>

<verification>
1. `pnpm --filter @trip-map/web typecheck` 退出码 0。
2. `pnpm --filter @trip-map/web test --run src/components/LeafletMapStage.spec.ts` 退出码 0，新增回归用例通过。
3. `pnpm --filter @trip-map/web test` 全量退出码 0（无任何既有用例被误伤）。
4. 手动 grep 确认 `record.createdAt.localeCompare` 在 LeafletMapStage.vue 中已消失。
</verification>

<success_criteria>
- VERIFICATION.md gap 1（truth #10 FAILED，blocker）关闭：selector 改用旅行日期排序。
- 新增的回归用例在修复前 RED、修复后 GREEN，为未来兜底。
- 本 plan 之外的 spec、store、server、popup 组件零改动（scope 严格收敛）。
- Phase 27 UI 表现现在符合 D-05：摘要的『最近一次』反映真正旅行时间，而非录入时间。
</success_criteria>

<output>
After completion, create `.planning/phases/27-multi-visit-record-foundation/27-05-SUMMARY.md` including:
- gap 关闭前后 activePointLatestTripLabel 的 diff 要点（createdAt -> endDate ?? startDate）
- 新增回归用例的名称与它验证的行为
- 全量 `pnpm --filter @trip-map/web test` 的通过数与退出码
- 对 VERIFICATION.md 的后续建议：truth #10 可从 FAILED 更新为 VERIFIED
- 本 plan 未触碰的 artifact 清单（证明 scope 收敛）
</output>

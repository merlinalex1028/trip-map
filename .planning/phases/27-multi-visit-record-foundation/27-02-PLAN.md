---
phase: 27-multi-visit-record-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 27-01
files_modified:
  - apps/server/src/modules/records/dto/create-travel-record.dto.ts
  - apps/server/src/modules/records/records.repository.ts
  - apps/server/src/modules/records/records.service.ts
  - apps/server/src/modules/auth/auth.service.ts
  - apps/server/src/modules/records/records.repository.spec.ts
  - apps/server/src/modules/records/records.service.spec.ts
  - apps/server/src/modules/auth/auth.service.spec.ts
autonomous: true
requirements:
  - TRIP-01
  - TRIP-02
  - TRIP-03
tags:
  - backend
  - nestjs
  - prisma
  - validation
must_haves:
  truths:
    - "POST /records 接受 startDate/endDate 字段，通过 class-validator 校验 YYYY-MM-DD 格式"
    - "POST /records 当 startDate 与 endDate 同时提供且 endDate < startDate 时返回 400 BadRequest"
    - "RecordsRepository.createTravelRecord 改为 prisma.userTravelRecord.create（不再 upsert），允许同 placeId 多条记录"
    - "RecordsRepository.importTravelRecords 的去重口径由 placeId 变为 (placeId, startDate, endDate) 三元组"
    - "AuthService.bootstrap 返回的记录包含 startDate / endDate 字段"
    - "toContractTravelRecord 在 records.service.ts 与 auth.service.ts 两处均已同步加入日期字段映射"
    - "deleteTravelRecordByPlaceId 保持 deleteMany by placeId 语义不变（D-06）"
  artifacts:
    - path: "apps/server/src/modules/records/dto/create-travel-record.dto.ts"
      provides: "日期字段 DTO 校验"
      contains: "@Matches(/^\\\\d{4}-\\\\d{2}-\\\\d{2}$/"
    - path: "apps/server/src/modules/records/records.repository.ts"
      provides: "多条记录 create + 三元组去重 import"
      contains: "prisma.userTravelRecord.create"
    - path: "apps/server/src/modules/records/records.service.ts"
      provides: "跨字段日期校验 + toContractTravelRecord 映射"
      contains: "endDate must be >= startDate"
    - path: "apps/server/src/modules/auth/auth.service.ts"
      provides: "bootstrap 返回日期字段"
      contains: "startDate: record.startDate ?? null"
  key_links:
    - from: "apps/server/src/modules/records/dto/create-travel-record.dto.ts"
      to: "apps/server/src/modules/records/records.service.ts"
      via: "CreateTravelRecordDto → RecordsService.createTravel"
      pattern: "createTravel\\(.*CreateTravelRecordDto"
    - from: "apps/server/src/modules/records/records.repository.ts"
      to: "apps/server/prisma/schema.prisma UserTravelRecord"
      via: "prisma.userTravelRecord.create"
      pattern: "userTravelRecord\\.create\\("
    - from: "apps/server/src/modules/auth/auth.service.ts"
      to: "@trip-map/contracts TravelRecord"
      via: "toContractTravelRecord 映射日期字段"
      pattern: "startDate: record\\.startDate"
---

<objective>
Phase 27 Wave 1：把后端 records 与 auth 模块升级为多次记录 + 日期字段模型。
1. 在 CreateTravelRecordDto 中以 class-validator 装饰器接受 startDate/endDate，格式约束为 YYYY-MM-DD，跨字段校验 endDate >= startDate 在 service 层完成。
2. RecordsRepository.createTravelRecord 从 upsert 改为 create（允许同 placeId 多条）；importTravelRecords 的去重口径改为 (placeId, startDate, endDate) 三元组。
3. records.service.ts 与 auth.service.ts 两处 toContractTravelRecord 同步加入日期字段映射（Pitfall 7：两处必须一起改，否则 bootstrap 恢复会静默丢日期）。
4. 扩展现有 auth.service.spec.ts 并新增 records.repository.spec.ts / records.service.spec.ts，覆盖多条记录创建、日期校验、bootstrap 返回字段。

Purpose: API 层是所有写入/读取的收拢点。契约字段在 Plan 01 完成后，必须 Wave 1 同步打通 DTO 校验与持久化层，下游前端才能看到真实的数据契约。
Output: 后端模块通过 vitest 全绿；POST /records 可携带日期字段；GET /auth/bootstrap 返回记录含日期字段；DELETE /records/:placeId 仍按地点级清理。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-multi-visit-record-foundation/27-CONTEXT.md
@.planning/phases/27-multi-visit-record-foundation/27-RESEARCH.md
@.planning/phases/27-multi-visit-record-foundation/27-PATTERNS.md
@.planning/phases/27-multi-visit-record-foundation/27-01-SUMMARY.md

<interfaces>
<!-- Source: apps/server/src/modules/records/dto/create-travel-record.dto.ts [VERIFIED 2026-04-20] -->

```typescript
// 现有 DTO 的模式，末尾需追加 startDate/endDate：
import { IsEnum, IsNotEmpty, IsString } from 'class-validator'
import { ApiProperty } from '@nestjs/swagger'
import type {
  ChinaAdminType,
  CreateTravelRecordRequest,
  PlaceKind,
} from '@trip-map/contracts'

export class CreateTravelRecordDto implements CreateTravelRecordRequest {
  @ApiProperty() @IsString() @IsNotEmpty()
  placeId!: string
  // ... 其他必填字段同上模式 ...
  @ApiProperty() @IsString() @IsNotEmpty()
  subtitle!: string
}
```

<!-- Source: apps/server/src/modules/records/records.repository.ts lines 15-29, 63-129 [VERIFIED 2026-04-20] -->

```typescript
function toTravelRecordData(userId: string, input: CreateTravelRecordDto) {
  return {
    userId, placeId: input.placeId, boundaryId: input.boundaryId,
    placeKind: input.placeKind, datasetVersion: input.datasetVersion,
    displayName: input.displayName, regionSystem: input.regionSystem,
    adminType: input.adminType, typeLabel: input.typeLabel,
    parentLabel: input.parentLabel, subtitle: input.subtitle,
    // ← 需追加 startDate, endDate
  }
}

// 当前（需改为 create）：
async createTravelRecord(userId: string, input: CreateTravelRecordDto): Promise<UserTravelRecord> {
  return this.prisma.userTravelRecord.upsert({
    where: { userId_placeId: { userId, placeId: input.placeId } },
    update: toTravelRecordData(userId, input),
    create: toTravelRecordData(userId, input),
  })
}

// 当前 import 去重（按 placeId；需改为三元组）：
const existingRecords = await this.prisma.userTravelRecord.findMany({
  where: { userId, placeId: { in: uniqueInputs.map((i) => i.placeId) } },
  select: { placeId: true },
})
const existingPlaceIds = new Set(existingRecords.map((r) => r.placeId))
const recordsToCreate = uniqueInputs.filter((i) => !existingPlaceIds.has(i.placeId))
```

<!-- Source: apps/server/src/modules/records/records.service.ts lines 39-54, 73-77 [VERIFIED 2026-04-20] -->

```typescript
function toContractTravelRecord(record: UserTravelRecord): ContractTravelRecord {
  return {
    id: record.id, placeId: record.placeId, boundaryId: record.boundaryId,
    placeKind: record.placeKind as PlaceKind,
    datasetVersion: record.datasetVersion, displayName: record.displayName,
    regionSystem: record.regionSystem as ContractTravelRecord['regionSystem'],
    adminType: record.adminType as ContractTravelRecord['adminType'],
    typeLabel: record.typeLabel as ContractTravelRecord['typeLabel'],
    parentLabel: record.parentLabel as ContractTravelRecord['parentLabel'],
    subtitle: record.subtitle,
    // ← 此处需在 createdAt 之前插入 startDate, endDate
    createdAt: record.createdAt.toISOString(),
  }
}

async createTravel(userId, input): Promise<ContractTravelRecord> {
  this.assertAuthoritativeOverseasRecord(input)
  // ← 需在此插入跨字段日期校验
  const record = await this.recordsRepository.createTravelRecord(userId, input)
  return toContractTravelRecord(record)
}
```

<!-- Source: apps/server/src/modules/auth/auth.service.ts lines 35-50 [VERIFIED 2026-04-20] -->

```typescript
// auth.service.ts 内独立副本 — Pitfall 7 要求同步修改：
function toContractTravelRecord(record: UserTravelRecord): TravelRecord {
  return {
    id: record.id, placeId: record.placeId, boundaryId: record.boundaryId,
    placeKind: record.placeKind as TravelRecord['placeKind'],
    datasetVersion: record.datasetVersion, displayName: record.displayName,
    regionSystem: record.regionSystem as TravelRecord['regionSystem'],
    adminType: record.adminType as TravelRecord['adminType'],
    typeLabel: record.typeLabel ?? '',
    parentLabel: record.parentLabel ?? '',
    subtitle: record.subtitle,
    // ← 需在此插入 startDate, endDate
    createdAt: record.createdAt.toISOString(),
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 CreateTravelRecordDto 日期字段并加 DTO 单元测试</name>
  <files>
    apps/server/src/modules/records/dto/create-travel-record.dto.ts,
    apps/server/src/modules/records/records.service.spec.ts
  </files>
  <read_first>
    - apps/server/src/modules/records/dto/create-travel-record.dto.ts（DTO 现有装饰器风格，保持一致）
    - apps/server/src/modules/records/records.service.ts（createTravel 当前实现 + assertAuthoritativeOverseasRecord）
    - apps/server/src/modules/records/records.repository.ts（toTravelRecordData 的输入形状）
    - packages/contracts/dist/records.d.ts（Plan 01 已构建的新接口）
    - apps/server/src/modules/auth/auth.service.spec.ts（repository mock + describe 风格，作为 RecordsService spec 的写法蓝本）
    - .planning/phases/27-multi-visit-record-foundation/27-PATTERNS.md（Pattern 3: DTO 日期校验 + service 层跨字段校验 + Pitfall 6: forbidNonWhitelisted）
  </read_first>
  <behavior>
    - DTO 层：startDate 与 endDate 为可选字段（@IsOptional），若提供则必须满足 YYYY-MM-DD 格式（@Matches(/^\d{4}-\d{2}-\d{2}$/)）。
    - RecordsService.createTravel 层：若 startDate 与 endDate 同时为非空字符串且 endDate < startDate（字符串字典序，YYYY-MM-DD 固定格式保证与时间序一致），throw `new BadRequestException('endDate must be >= startDate')`。
    - RecordsService.createTravel 把 startDate/endDate 透传给 repository（null 或缺省时归一为 null）。
    - ValidationPipe 的 `forbidNonWhitelisted: true` 模式下，新字段必须在 DTO 白名单内（Pitfall 6）。
  </behavior>
  <action>
    ## 修改 apps/server/src/modules/records/dto/create-travel-record.dto.ts

    1. 在 import 行把 `class-validator` 的导入从
       ```typescript
       import { IsEnum, IsNotEmpty, IsString } from 'class-validator'
       ```
       改为
       ```typescript
       import { IsEnum, IsNotEmpty, IsOptional, IsString, Matches } from 'class-validator'
       ```

    2. 在 `CreateTravelRecordDto` 末尾（`subtitle!: string` 字段之后，类闭合大括号之前）追加两段字段声明：
       ```typescript
       @ApiProperty({ nullable: true, example: '2025-10-01', required: false })
       @IsOptional()
       @Matches(/^\d{4}-\d{2}-\d{2}$/, { message: 'startDate must be YYYY-MM-DD' })
       startDate?: string | null

       @ApiProperty({ nullable: true, example: '2025-10-07', required: false })
       @IsOptional()
       @Matches(/^\d{4}-\d{2}-\d{2}$/, { message: 'endDate must be YYYY-MM-DD' })
       endDate?: string | null
       ```

       关键点：
       - 字段用 `?:` 可选声明配合 `@IsOptional()`（与 DTO implements 的 CreateTravelRecordRequest 的 `string | null` 契约兼容：`undefined` 由 @IsOptional 放行、运行期归一到 null）。
       - 不得使用自定义 Pipe、不得 hand-roll Date 解析（D-02 精度到天，`@Matches` 已足够）。
       - 不得加 `@IsDateString()`（会接受 ISO8601 带时区字符串，与 D-02 的 YYYY-MM-DD 精度不符）。

    ## 新建 apps/server/src/modules/records/records.service.spec.ts

    编写针对 `RecordsService` 的 unit test，mock `RecordsRepository`。参照 `apps/server/src/modules/auth/auth.service.spec.ts` 的 `createRepositoryMock` 模式：

    ```typescript
    import { BadRequestException } from '@nestjs/common'
    import { describe, expect, it, vi } from 'vitest'
    import type { UserTravelRecord } from '@prisma/client'

    import { RecordsService } from './records.service.js'
    import type { CreateTravelRecordDto } from './dto/create-travel-record.dto.js'

    function createRepositoryMock() {
      return {
        createSmokeRecord: vi.fn(),
        findAllTravelRecords: vi.fn(),
        createTravelRecord: vi.fn(),
        importTravelRecords: vi.fn(),
        deleteTravelRecordByPlaceId: vi.fn(),
      }
    }

    function baseInput(overrides: Partial<CreateTravelRecordDto> = {}): CreateTravelRecordDto {
      return {
        placeId: 'cn-admin-beijing',
        boundaryId: 'cn-admin-beijing-boundary',
        placeKind: 'CN_ADMIN',
        datasetVersion: 'cn-admin-2024-r1',
        displayName: '北京市',
        regionSystem: 'CN',
        adminType: 'MUNICIPALITY',
        typeLabel: '直辖市',
        parentLabel: '中国',
        subtitle: '直辖市 · 中国',
        startDate: null,
        endDate: null,
        ...overrides,
      } as CreateTravelRecordDto
    }

    function baseRecord(overrides: Partial<UserTravelRecord> = {}): UserTravelRecord {
      return {
        id: 'rec-1',
        userId: 'user-1',
        placeId: 'cn-admin-beijing',
        boundaryId: 'cn-admin-beijing-boundary',
        placeKind: 'CN_ADMIN',
        datasetVersion: 'cn-admin-2024-r1',
        displayName: '北京市',
        regionSystem: 'CN',
        adminType: 'MUNICIPALITY',
        typeLabel: '直辖市',
        parentLabel: '中国',
        subtitle: '直辖市 · 中国',
        startDate: null,
        endDate: null,
        createdAt: new Date('2026-04-20T00:00:00.000Z'),
        updatedAt: new Date('2026-04-20T00:00:00.000Z'),
        ...overrides,
      } as UserTravelRecord
    }
    ```

    必须覆盖以下测试场景（每个 `it(...)` 一个）：
    - `createTravel 接受 null startDate/endDate 并透传给 repository`：输入 baseInput()，mock createTravelRecord 返回 baseRecord()，期望 repository 被调用一次且参数含 startDate:null、endDate:null；返回值含 startDate: null、endDate: null。
    - `createTravel 接受合法日期并透传给 repository`：输入 baseInput({ startDate: '2025-10-01', endDate: '2025-10-07' })，期望 repository 被调用且入参 startDate=='2025-10-01'、endDate=='2025-10-07'；返回值的日期字段被正确映射。
    - `createTravel endDate < startDate 抛 BadRequestException`：输入 baseInput({ startDate: '2025-10-07', endDate: '2025-10-01' })，期望 `await expect(service.createTravel(...)).rejects.toBeInstanceOf(BadRequestException)`，且 repository.createTravelRecord 未被调用。
    - `createTravel 只填 startDate 不填 endDate 视为单日旅行，不抛错`：输入 baseInput({ startDate: '2025-10-01', endDate: null })。
    - `createTravel 同日旅行（startDate == endDate）通过校验`：输入 baseInput({ startDate: '2025-10-05', endDate: '2025-10-05' })。

    ## 暂时不改 records.service.ts 本体
    本任务只负责定义行为（DTO + 测试）；service 实现的变更在 Task 2 完成（让本任务的测试 RED → GREEN）。测试应在 Task 2 执行完毕后全部转绿；本任务允许测试文件当前失败，不计入退出码判断（Task 2 提供 GREEN）。

    **Red-Green 承诺：** 完成本任务后，运行 `pnpm --filter @trip-map/server test src/modules/records/records.service.spec.ts` 应出现与 "endDate must be >= startDate" 或 repository 调用参数相关的 RED 失败；保留失败并直接进入 Task 2。

    不允许：
    - 修改其他 test（包括 import-travel-records.dto.spec.ts 等）。
    - 在 DTO 增加 `@IsDateString()` 或自定义 ValidationPipe。
    - 使用 `startDate!: string` 或非 `| null` 等价类型。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "IsOptional" apps/server/src/modules/records/dto/create-travel-record.dto.ts >= 1
    - grep -c "Matches" apps/server/src/modules/records/dto/create-travel-record.dto.ts >= 1
    - grep -c "startDate?: string | null" apps/server/src/modules/records/dto/create-travel-record.dto.ts == 1
    - grep -c "endDate?: string | null" apps/server/src/modules/records/dto/create-travel-record.dto.ts == 1
    - grep -c "^\s*@Matches(/\^\\\\d\{4\}-\\\\d\{2\}-\\\\d\{2\}\\\$/" apps/server/src/modules/records/dto/create-travel-record.dto.ts >= 2
    - apps/server/src/modules/records/records.service.spec.ts 文件存在
    - grep -c "describe\(.RecordsService" apps/server/src/modules/records/records.service.spec.ts >= 1
    - grep -c "endDate must be >= startDate" apps/server/src/modules/records/records.service.spec.ts >= 1
    - `pnpm --filter @trip-map/server typecheck` 退出码为 0
  </acceptance_criteria>
  <done>DTO 声明并约束日期字段；records.service.spec.ts 覆盖本 Wave 日期语义的 5 个 test case；TypeScript 编译通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 升级 RecordsRepository + RecordsService 到多条记录 + 日期字段</name>
  <files>
    apps/server/src/modules/records/records.repository.ts,
    apps/server/src/modules/records/records.service.ts,
    apps/server/src/modules/records/records.repository.spec.ts
  </files>
  <read_first>
    - apps/server/src/modules/records/records.repository.ts（当前 upsert / importTravelRecords / deleteTravelRecordByPlaceId 全量，不得误伤 smoke record 方法）
    - apps/server/src/modules/records/records.service.ts（createTravel / importTravel / deleteTravel / assertAuthoritativeOverseasRecord 全量）
    - apps/server/src/modules/records/records.service.spec.ts（Task 1 新建的 spec，确认这里的实现能让它转绿）
    - apps/server/src/modules/auth/auth.service.spec.ts（mock repository 模式，作为 repository spec 的写法蓝本）
    - .planning/phases/27-multi-visit-record-foundation/27-PATTERNS.md（Pattern 1 + Pattern 8：multi-visit import 三元组去重）
    - .planning/phases/27-multi-visit-record-foundation/27-RESEARCH.md（Pitfall 2 importTravelRecords 去重破坏多次记录）
  </read_first>
  <behavior>
    - toTravelRecordData 返回对象含 startDate/endDate（由 `?? null` 归一 undefined → null）。
    - createTravelRecord 调用 `prisma.userTravelRecord.create({ data })`，不再 upsert；同一 userId+placeId 可重复创建。
    - importTravelRecords 的"已存在"判定由 "同 placeId" 改为 "同 placeId + 同 startDate + 同 endDate"；null 值参与匹配（Prisma where 语义：`{ startDate: null }` 等价于 IS NULL）。
    - deleteTravelRecordByPlaceId 保持不变（D-06 语义）。
    - createTravel 在 assertAuthoritativeOverseasRecord 之后追加日期区间校验：`if (input.startDate && input.endDate && input.endDate < input.startDate) throw new BadRequestException('endDate must be >= startDate')`。
    - toContractTravelRecord（records.service.ts 一处）在 `subtitle: record.subtitle,` 之后、`createdAt: ...` 之前追加 `startDate: record.startDate ?? null,` 与 `endDate: record.endDate ?? null,`。
    - importTravel 透传 startDate/endDate，不对日期额外做 cross-field 校验（导入路径以 null/null 为主）。
  </behavior>
  <action>
    ## 修改 apps/server/src/modules/records/records.repository.ts

    1. **修改 `toTravelRecordData`**（lines 15-29）：在 `subtitle: input.subtitle,` 末尾追加两行（不改动现有字段顺序）：
       ```typescript
       startDate: input.startDate ?? null,
       endDate: input.endDate ?? null,
       ```

    2. **重写 `createTravelRecord`**（lines 63-74）：把 upsert 改为 create：
       ```typescript
       async createTravelRecord(userId: string, input: CreateTravelRecordDto): Promise<UserTravelRecord> {
         return this.prisma.userTravelRecord.create({
           data: toTravelRecordData(userId, input),
         })
       }
       ```

    3. **修改 `importTravelRecords`**（lines 76-129）：将去重逻辑改为三元组（Pitfall 2）。
       - 保留当前的 `uniqueByPlaceId` 入参去重映射逻辑？——需要替换为按三元组的输入去重：
         ```typescript
         function keyOf(input: Pick<CreateTravelRecordDto, 'placeId' | 'startDate' | 'endDate'>) {
           return `${input.placeId}\u0000${input.startDate ?? ''}\u0000${input.endDate ?? ''}`
         }

         const uniqueByKey = new Map<string, CreateTravelRecordDto>()
         for (const input of inputs) {
           const key = keyOf(input)
           if (!uniqueByKey.has(key)) {
             uniqueByKey.set(key, input)
           }
         }
         const uniqueInputs = [...uniqueByKey.values()]
         ```
         把之后所有对 `uniqueByPlaceId` 的引用替换为 `uniqueByKey`/`uniqueInputs`。
       - 把 `existingRecords` 查询改为：
         ```typescript
         const existingRecords = await this.prisma.userTravelRecord.findMany({
           where: {
             userId,
             placeId: { in: uniqueInputs.map((input) => input.placeId) },
           },
           select: { placeId: true, startDate: true, endDate: true },
         })
         const existingKeys = new Set(
           existingRecords.map((record) => keyOf({
             placeId: record.placeId,
             startDate: record.startDate,
             endDate: record.endDate,
           })),
         )
         const recordsToCreate = uniqueInputs.filter((input) => !existingKeys.has(keyOf(input)))
         ```
       - `mergedDuplicateCount: inputs.length - importedCount` 的口径保持不变（含义仍为"跳过的条数"，Open Question #3 已说明）。

    4. **不修改** `createSmokeRecord`、`findAllTravelRecords`、`deleteTravelRecordByPlaceId`。

    ## 修改 apps/server/src/modules/records/records.service.ts

    1. **修改 `toContractTravelRecord`**（lines 39-54）：在 `subtitle: record.subtitle,` 之后、`createdAt: record.createdAt.toISOString(),` 之前，插入两行：
       ```typescript
       startDate: record.startDate ?? null,
       endDate: record.endDate ?? null,
       ```

    2. **修改 `createTravel`**（lines 73-77）：在 `this.assertAuthoritativeOverseasRecord(input)` 之后、`const record = ...` 之前插入日期区间校验：
       ```typescript
       if (input.startDate && input.endDate && input.endDate < input.startDate) {
         throw new BadRequestException('endDate must be >= startDate')
       }
       ```
       确保 `BadRequestException` 已从 `@nestjs/common` 导入（当前 import 行已含）。

    3. **不修改** `createSmoke`、`findAllTravel`、`importTravel`、`deleteTravel`、`assertAuthoritativeOverseasRecord`。

    ## 新建 apps/server/src/modules/records/records.repository.spec.ts

    仿照 `apps/server/src/modules/auth/auth.service.spec.ts` 的 mock 模式，但 repository 直接依赖 `PrismaService`，需要 mock `prisma.userTravelRecord.{create, findMany, createMany, deleteMany}`。示例：

    ```typescript
    import { describe, expect, it, vi } from 'vitest'
    import type { UserTravelRecord } from '@prisma/client'

    import { RecordsRepository } from './records.repository.js'
    import type { CreateTravelRecordDto } from './dto/create-travel-record.dto.js'

    function createPrismaMock() {
      return {
        userTravelRecord: {
          create: vi.fn(),
          findMany: vi.fn(),
          createMany: vi.fn(),
          deleteMany: vi.fn(),
          upsert: vi.fn(),  // 仍然 mock，用于反向断言未被调用
        },
        smokeRecord: { create: vi.fn() },
      }
    }
    ```

    必须覆盖：
    - `createTravelRecord 调用 prisma.userTravelRecord.create 而非 upsert，且 data 含日期字段`：输入带 startDate/endDate，期望 create 被调用 1 次，upsert 被调用 0 次，入参 data.startDate/data.endDate 等于输入。
    - `createTravelRecord 在 startDate/endDate 为 undefined 时归一为 null`：输入 { ... , startDate: undefined, endDate: undefined }，期望入参 data.startDate === null 且 data.endDate === null。
    - `importTravelRecords 三元组去重`：已存在记录 { placeId: 'x', startDate: null, endDate: null }；输入两条记录：第一条 { placeId: 'x', startDate: null, endDate: null }（应被跳过），第二条 { placeId: 'x', startDate: '2025-10-01', endDate: null }（应被创建）。期望最终 createMany 被调用一次，入参 `data.length === 1` 且入参含 startDate === '2025-10-01'。
    - `importTravelRecords 空输入短路返回现有记录`：输入 inputs === []，期望不调用 createMany。

    **Red-Green 承诺：** 先读 Task 1 已创建的 records.service.spec.ts；完成本任务后，执行 `pnpm --filter @trip-map/server test --run src/modules/records` 应全部转绿（service.spec + repository.spec）。

    不允许：
    - 把 `createTravelRecord` 改为 `createMany` 或 `create` 后加 `onConflict` hack（本项目无此用法）。
    - 在 `importTravelRecords` 使用 raw SQL 或 transaction，保持现有代码架构。
    - 修改 `records.controller.ts`（本 plan 无需）或 `records.module.ts`。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server test --run src/modules/records</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "this.prisma.userTravelRecord.upsert" apps/server/src/modules/records/records.repository.ts == 0
    - grep -c "this.prisma.userTravelRecord.create(" apps/server/src/modules/records/records.repository.ts >= 1
    - grep -c "startDate: input.startDate ?? null" apps/server/src/modules/records/records.repository.ts == 1
    - grep -c "endDate: input.endDate ?? null" apps/server/src/modules/records/records.repository.ts == 1
    - grep -c "keyOf" apps/server/src/modules/records/records.repository.ts >= 2（辅助函数定义 + 使用）
    - grep -c "select: { placeId: true, startDate: true, endDate: true }" apps/server/src/modules/records/records.repository.ts == 1
    - grep -c "startDate: record.startDate ?? null" apps/server/src/modules/records/records.service.ts == 1
    - grep -c "endDate: record.endDate ?? null" apps/server/src/modules/records/records.service.ts == 1
    - grep -c "endDate must be >= startDate" apps/server/src/modules/records/records.service.ts == 1
    - apps/server/src/modules/records/records.repository.spec.ts 文件存在
    - grep -c "describe\(.RecordsRepository" apps/server/src/modules/records/records.repository.spec.ts >= 1
    - `pnpm --filter @trip-map/server test --run src/modules/records` 退出码为 0，测试全部通过
  </acceptance_criteria>
  <done>RecordsRepository.createTravelRecord 改为 create；importTravelRecords 三元组去重；RecordsService.toContractTravelRecord 含日期字段；createTravel 含日期区间校验；service + repository 两份 spec 全部通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 同步 AuthService.bootstrap 的 toContractTravelRecord 并扩展 spec</name>
  <files>
    apps/server/src/modules/auth/auth.service.ts,
    apps/server/src/modules/auth/auth.service.spec.ts
  </files>
  <read_first>
    - apps/server/src/modules/auth/auth.service.ts（toContractTravelRecord 独立副本 + bootstrap 实现）
    - apps/server/src/modules/records/records.service.ts（Task 2 刚改好的 toContractTravelRecord，两处必须字段一致，Pitfall 7）
    - apps/server/src/modules/auth/auth.service.spec.ts（现有 describe 结构，在 bootstrap describe 下扩展，不删除现有 case）
    - .planning/phases/27-multi-visit-record-foundation/27-PATTERNS.md（Pattern: 两处 toContractTravelRecord 必须同步）
  </read_first>
  <behavior>
    - auth.service.ts 中的 toContractTravelRecord 与 records.service.ts 中的对等函数字段集合一致（含 startDate/endDate）。
    - AuthService.bootstrap 在 authenticated 路径下，把日期字段透传到 AuthBootstrapResponse.records[*]。
    - auth.service.spec.ts 新增至少 1 个 case，断言 bootstrap 返回的 records 数组每一项包含 startDate/endDate 字段，映射值来自 repository mock 返回的 UserTravelRecord。
  </behavior>
  <action>
    ## 修改 apps/server/src/modules/auth/auth.service.ts

    在 `toContractTravelRecord`（lines 35-50）的 `subtitle: record.subtitle,` 之后、`createdAt: record.createdAt.toISOString(),` 之前插入：
    ```typescript
    startDate: record.startDate ?? null,
    endDate: record.endDate ?? null,
    ```

    **不修改** 其他任何内容（register、login、logout、restoreSession、resolveAuthenticatedUserFromSession 的实现均保持）。

    ## 扩展 apps/server/src/modules/auth/auth.service.spec.ts

    在文件末尾（或 `describe('AuthService.bootstrap', ...)` 内，如果已存在；否则新增一个）添加至少 2 个测试：

    1. `bootstrap authenticated 路径返回 records 含 startDate/endDate`：
       - 使用现有 createRepositoryMock（已含 findUserTravelRecordsByUserId，第 39 行）。
       - mock `findActiveSessionWithUserById` 返回 { id: session.id, userId: user.id, user, expiresAt: ... }。
       - mock `findUserTravelRecordsByUserId` 返回一个数组：
         ```typescript
         [{
           id: 'rec-1', userId: 'user-1', placeId: 'cn-admin-beijing',
           boundaryId: 'cn-admin-beijing-boundary', placeKind: 'CN_ADMIN',
           datasetVersion: 'cn-admin-2024-r1', displayName: '北京市',
           regionSystem: 'CN', adminType: 'MUNICIPALITY',
           typeLabel: '直辖市', parentLabel: '中国', subtitle: '直辖市 · 中国',
           startDate: '2025-10-01', endDate: '2025-10-07',
           createdAt: new Date('2026-04-20T00:00:00.000Z'),
           updatedAt: new Date('2026-04-20T00:00:00.000Z'),
         }]
         ```
       - 调用 `service.bootstrap('session-1')`；期望 `response.response.authenticated === true` 且 `response.response.records[0].startDate === '2025-10-01'` 与 `.endDate === '2025-10-07'`。

    2. `bootstrap 旧记录（日期为 null）映射到 null`：
       - 同上，但 mock 返回的 record 将 startDate: null, endDate: null。
       - 期望 `records[0].startDate === null` 且 `records[0].endDate === null`（不能是 undefined——Pitfall 8：undefined 会在 JSON.stringify 时被丢字段；必须显式 null）。

    **Red-Green 承诺：** 本任务测试不依赖 Task 2 的 records.service 改动，是独立的 auth 单元测试；完成后执行 `pnpm --filter @trip-map/server test --run src/modules/auth/auth.service.spec.ts` 应全部转绿。

    不允许：
    - 删除或修改 auth.service.spec.ts 中现有的 register/login/logout/restoreSession 测试。
    - 引入新的 fixture 文件（所有 mock 对象就地定义在 spec 中）。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server test --run src/modules/auth/auth.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "startDate: record.startDate ?? null" apps/server/src/modules/auth/auth.service.ts == 1
    - grep -c "endDate: record.endDate ?? null" apps/server/src/modules/auth/auth.service.ts == 1
    - grep -c "bootstrap" apps/server/src/modules/auth/auth.service.spec.ts >= 2（新增 describe/it 块）
    - grep -c "startDate" apps/server/src/modules/auth/auth.service.spec.ts >= 2
    - grep -c "findUserTravelRecordsByUserId.mockResolvedValueOnce" apps/server/src/modules/auth/auth.service.spec.ts >= 1
    - `pnpm --filter @trip-map/server test --run src/modules/auth/auth.service.spec.ts` 退出码为 0
  </acceptance_criteria>
  <done>AuthService.bootstrap 返回的 records 含 startDate/endDate 字段；auth.service.spec.ts 新增至少 2 个 bootstrap 测试全部通过；Pitfall 7（两处 toContractTravelRecord 同步）已闭合。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request body → CreateTravelRecordDto | 客户端输入通过 class-validator 过滤后进入 service；forbidNonWhitelisted 防止多余字段注入 |
| Service layer → Prisma Client | service 把已校验的 DTO 传给 repository；startDate/endDate 在此通过字典序比较避免时区歧义 |
| Session cookie → AuthService.bootstrap | 受 SessionAuthGuard 控制；bootstrap 不接受 body 参数，日期字段仅从数据库读 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | CreateTravelRecordDto 日期字段 | mitigate | `@Matches(/^\d{4}-\d{2}-\d{2}$/)` 在 DTO 层拒绝非日期字符串；`forbidNonWhitelisted: true` 拒绝未声明的多余字段（Pitfall 6） |
| T-27-02-02 | Tampering | RecordsService.createTravel 跨字段约束 | mitigate | 显式 `if (endDate < startDate) throw BadRequestException('endDate must be >= startDate')`；覆盖测试 |
| T-27-02-03 | Elevation of Privilege | POST /records userId 来源 | mitigate | 现有 `SessionAuthGuard` + `CurrentUser` decorator 绑定；本 plan 不改动 controller/guard，保持既有保护 |
| T-27-02-04 | Denial of Service | ImportTravelRecordsDto 无数量上限 | accept | Phase 27 不改动 import path（研究 Tertiary 提示的 `@ArrayMaxSize` 属于未来增强，本 milestone 暂不实施）；record 上限由既有 API limits 兜底 |
| T-27-02-05 | Information Disclosure | AuthService.bootstrap 泄露他人 records | mitigate | `findUserTravelRecordsByUserId` 仅以 restored session 的 userId 查询，不从 body 读 userId；现有覆盖 |
</threat_model>

<verification>
1. `pnpm --filter @trip-map/server test` 全绿（records 三份 spec + auth spec）。
2. `pnpm --filter @trip-map/server typecheck` 成功（无 `any`、无 `startDate` 类型不匹配）。
3. `grep -r "upsert" apps/server/src/modules/records/records.repository.ts` 返回空（upsert 已彻底移除）。
4. `grep "startDate: record.startDate ?? null"` 在 records.service.ts 与 auth.service.ts 各匹配 1 处（Pitfall 7）。
</verification>

<success_criteria>
- POST /records 接受日期字段并持久化；非法格式返回 400。
- 同 userId + placeId 可以创建任意多条记录（数据库已在 Plan 01 去 unique）。
- ImportTravelRecords 按三元组去重，旧记录（null+null）不会二次插入。
- AuthService.bootstrap 返回的每条记录均包含 startDate/endDate（可能为 null）。
- records + auth 模块 vitest 全绿。
</success_criteria>

<output>
After completion, create `.planning/phases/27-multi-visit-record-foundation/27-02-SUMMARY.md` including:
- DTO / service / repository / auth 四处的 diff 摘要
- 测试运行输出（spec 数、通过数）
- Pitfall 7 同步修改确认（auth + records 两处均改）
- 供 Plan 03 前端 store 消费的稳定 API 契约（POST /records 入参 + GET /auth/bootstrap 响应形状）
</output>
</content>
</invoke>
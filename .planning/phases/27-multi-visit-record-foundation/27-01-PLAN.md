---
phase: 27-multi-visit-record-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/contracts/src/records.ts
  - apps/server/prisma/schema.prisma
autonomous: true
requirements:
  - TRIP-01
  - TRIP-02
  - TRIP-03
tags:
  - contracts
  - prisma
  - schema-migration
must_haves:
  truths:
    - "@trip-map/contracts 的 TravelRecord 接口包含 startDate 与 endDate 两个 string | null 字段"
    - "@trip-map/contracts 的 CreateTravelRecordRequest 接口包含 startDate 与 endDate 两个 string | null 字段"
    - "apps/server/prisma/schema.prisma 中 UserTravelRecord 模型已声明 startDate 与 endDate 两个 String? 字段"
    - "apps/server/prisma/schema.prisma 中 UserTravelRecord 已移除 @@unique([userId, placeId]) 约束并替换为 @@index([userId, placeId])"
    - "pnpm --filter @trip-map/contracts build 成功，产出 dist 包含新字段类型"
    - "npx prisma db push 成功，数据库表结构与 schema 同步"
  artifacts:
    - path: "packages/contracts/src/records.ts"
      provides: "TravelRecord + CreateTravelRecordRequest 日期字段契约"
      contains: "startDate: string | null"
    - path: "packages/contracts/dist/records.d.ts"
      provides: "已构建的 TravelRecord 类型声明"
      contains: "startDate"
    - path: "apps/server/prisma/schema.prisma"
      provides: "UserTravelRecord schema 多条记录模型 + 日期字段"
      contains: "startDate      String?"
  key_links:
    - from: "packages/contracts/src/records.ts"
      to: "apps/server/src/modules/records/dto/create-travel-record.dto.ts"
      via: "CreateTravelRecordRequest interface implements"
      pattern: "implements CreateTravelRecordRequest"
    - from: "apps/server/prisma/schema.prisma"
      to: "@prisma/client UserTravelRecord type"
      via: "prisma generate"
      pattern: "startDate.*String\\?"
---

<objective>
Phase 27 Wave 0：建立多次旅行记录模型的数据底座。本计划只做三件事：
1. 在共享 contracts 包中为 `TravelRecord` 与 `CreateTravelRecordRequest` 增加 `startDate` / `endDate`（nullable string, YYYY-MM-DD）。
2. 在 Prisma schema 中移除 `UserTravelRecord.@@unique([userId, placeId])`（这是多次记录模型的核心结构性约束，D-06），降级为 `@@index`，并新增 `startDate` / `endDate` 两个 `String?` 字段。
3. 执行数据库 schema 推送与 Prisma Client 重新生成，保证下游 plan 可以立即消费新类型。

Purpose: 契约与 schema 是整个多次记录模型的单一真源。本计划完成后，下游（后端 service/DTO、前端 store、UI）才能并行推进。
Output: 更新后的 contracts 包构建产物 + 已推送变更的 PostgreSQL schema + 刷新的 Prisma Client。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-multi-visit-record-foundation/27-CONTEXT.md
@.planning/phases/27-multi-visit-record-foundation/27-RESEARCH.md
@.planning/phases/27-multi-visit-record-foundation/27-PATTERNS.md

<interfaces>
<!-- 当前 contracts/records.ts 的完整接口，供 executor 直接扩展。 -->
<!-- Source: packages/contracts/src/records.ts [VERIFIED 2026-04-20] -->

```typescript
// 现有接口（末尾需追加日期字段）：
export interface TravelRecord {
  id: string
  placeId: string
  boundaryId: string
  placeKind: PlaceKind
  datasetVersion: string
  displayName: string
  regionSystem: 'CN' | 'OVERSEAS'
  adminType: ChinaAdminType | 'ADMIN1'
  typeLabel: string
  parentLabel: string
  subtitle: string
  createdAt: string
}

export interface CreateTravelRecordRequest extends CanonicalPlaceSummary {}
```

<!-- Source: apps/server/prisma/schema.prisma lines 67-87 [VERIFIED 2026-04-20] -->

```prisma
model UserTravelRecord {
  id             String   @id @default(cuid())
  userId         String
  placeId        String
  boundaryId     String
  placeKind      String
  datasetVersion String
  displayName    String
  regionSystem   String?
  adminType      String?
  typeLabel      String?
  parentLabel    String?
  subtitle       String
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, placeId])   // ← 本计划需移除
  @@index([userId])
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 扩展 contracts TravelRecord 与 CreateTravelRecordRequest 日期字段</name>
  <files>packages/contracts/src/records.ts</files>
  <read_first>
    - packages/contracts/src/records.ts（当前接口定义——确认 createdAt 的位置与现有字段顺序，不得改动现有字段）
    - packages/contracts/src/place.ts（CanonicalPlaceSummary 的定义，确认 CreateTravelRecordRequest extends 目标类型正常工作）
    - .planning/phases/27-multi-visit-record-foundation/27-CONTEXT.md（D-01~D-09，特别是 D-02 精度到天、D-08 未知日期不得用 createdAt 伪装）
    - .planning/phases/27-multi-visit-record-foundation/27-PATTERNS.md（Pattern: contracts 日期字段设计）
  </read_first>
  <action>
    在 packages/contracts/src/records.ts 中：

    1. 修改 `TravelRecord` 接口，在 `subtitle: string` 行之后、`createdAt: string` 行之前，插入两个字段（每行一个字段，保留两空格缩进）：
       ```typescript
       startDate: string | null
       endDate: string | null
       ```
       语义：`startDate` 为 YYYY-MM-DD 字符串或 null（null = 日期未知，D-08）；`endDate` 为 YYYY-MM-DD 字符串或 null（null = 单日旅行或未知）。

    2. 修改 `CreateTravelRecordRequest` 接口，将原来的单行声明
       ```typescript
       export interface CreateTravelRecordRequest extends CanonicalPlaceSummary {}
       ```
       改为带字段体的形式（D-01 要求创建记录时必须携带日期）：
       ```typescript
       export interface CreateTravelRecordRequest extends CanonicalPlaceSummary {
         startDate: string | null
         endDate: string | null
       }
       ```

    3. 不修改 `SmokeRecord*`、`ImportTravelRecords*` 相关接口（ImportTravelRecordsRequest 的 records 字段会通过 CreateTravelRecordRequest 的变更自动收到新字段，不必改动 ImportTravelRecordsRequest 定义）。

    4. 运行 `pnpm --filter @trip-map/contracts build` 重新构建 contracts 包；dist 产物必须包含新字段。

    不允许：
    - 将 startDate/endDate 写为必选字段（会破坏旧记录迁移语义——旧记录必须能以 null 通过类型）。
    - 使用 `string | undefined`、`Date`、`string?:` 等等价但非 nullable string 的表达（与 Prisma String? → null 映射和 D-08 "未知"语义对齐，必须精确为 `string | null`）。
    - 修改其他未列出字段的顺序或可见性。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/contracts build &amp;&amp; pnpm --filter @trip-map/contracts typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep "startDate: string | null" packages/contracts/src/records.ts 匹配至少 2 行（TravelRecord 与 CreateTravelRecordRequest 各一行）
    - grep "endDate: string | null" packages/contracts/src/records.ts 匹配至少 2 行
    - grep "createdAt: string" packages/contracts/src/records.ts 仍然匹配 1 行（未误删现有字段）
    - grep "startDate" packages/contracts/dist/records.d.ts 匹配 >= 2 行（构建产物已同步）
    - `pnpm --filter @trip-map/contracts build` 退出码为 0
    - grep "extends CanonicalPlaceSummary {}" packages/contracts/src/records.ts 不再匹配（空大括号已被字段体替换）
  </acceptance_criteria>
  <done>TravelRecord 与 CreateTravelRecordRequest 均显式包含 `startDate: string | null` 和 `endDate: string | null`；contracts 包已构建成功；下游 TS 消费者可导入新类型。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 修改 UserTravelRecord Prisma schema（移除 unique，加日期字段）</name>
  <files>apps/server/prisma/schema.prisma</files>
  <read_first>
    - apps/server/prisma/schema.prisma（完整文件——确认 UserTravelRecord 当前结构与 @@unique、@@index 相对位置）
    - .planning/phases/27-multi-visit-record-foundation/27-PATTERNS.md（Pattern 1: Prisma Schema 多条记录模型 + 字段插入位置）
    - .planning/phases/27-multi-visit-record-foundation/27-RESEARCH.md（Runtime State Inventory 段落：migration 核心动作清单）
  </read_first>
  <action>
    在 apps/server/prisma/schema.prisma 中修改 `model UserTravelRecord { ... }` 块：

    1. 在 `subtitle       String` 行之后、`createdAt      DateTime @default(now())` 行之前，插入两个新字段（保留既有两空格字段/类型对齐空白，可用多余空格让 `String?` 大致与 `String` 对齐）：
       ```
       startDate      String?
       endDate        String?
       ```
       这两个字段必须是 nullable（`String?`），语义为 YYYY-MM-DD 字符串或 null（null = 日期未知 D-08）。禁止使用 `DateTime` 或带 `@default` 的写法——存量行的 `startDate` 必须为 null 表达"未知"而非被默认填充。

    2. 将 `@@unique([userId, placeId])` 一行完全删除，并在同一位置插入：
       ```
       @@index([userId, placeId])
       ```
       保留紧随其后的 `@@index([userId])` 不变。

    3. 不修改任何其他 model（SmokeRecord、TravelRecord、User、AuthSession）、generator、datasource。

    4. 这是 schema 文件修改；本任务完成后立即交由 Task 3 执行 Prisma Client 生成；Task 4 执行数据库 schema push。
  </action>
  <verify>
    <automated>npx --yes prisma format --schema=apps/server/prisma/schema.prisma &amp;&amp; npx --yes prisma validate --schema=apps/server/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - grep "startDate      String?" apps/server/prisma/schema.prisma 匹配 1 行
    - grep "endDate        String?" apps/server/prisma/schema.prisma 匹配 1 行
    - grep "@@unique(\[userId, placeId\])" apps/server/prisma/schema.prisma 匹配 0 行（已移除）
    - grep "@@index(\[userId, placeId\])" apps/server/prisma/schema.prisma 匹配 1 行（新增）
    - grep "@@index(\[userId\])" apps/server/prisma/schema.prisma 匹配 1 行（保留）
    - grep "placeId        String   @unique" apps/server/prisma/schema.prisma 匹配 1 行（TravelRecord 旧模型的唯一约束应保持不变，未误伤）
    - `npx prisma validate --schema=apps/server/prisma/schema.prisma` 退出码为 0
  </acceptance_criteria>
  <done>UserTravelRecord 模型结构更新完成：允许同 (userId, placeId) 多行存在；startDate/endDate 字段以 nullable 形式落位；schema 通过 Prisma 校验。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 重新生成 Prisma Client</name>
  <files>apps/server/node_modules/.prisma/client/index.d.ts (generated)</files>
  <read_first>
    - apps/server/prisma/schema.prisma（上一任务刚完成的 schema）
    - apps/server/package.json（确认 prisma 与 @prisma/client 版本一致）
  </read_first>
  <action>
    执行 `pnpm --filter @trip-map/server exec prisma generate --schema=apps/server/prisma/schema.prisma`。

    这一步必须在 schema 修改后执行（Pitfall 5）：Prisma Client 是代码生成产物，若不重新生成，下游任务消费 `record.startDate` 时会得到 TypeScript "属性不存在"错误或运行时 undefined。

    不允许：
    - 跳过此任务直接进入 Task 4（db push 不会重新生成类型声明文件）。
    - 使用 `npx prisma generate` 而不指定 schema 路径（可能使用错误的 schema）。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server exec prisma generate --schema=apps/server/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - 命令退出码为 0
    - grep "startDate" apps/server/node_modules/.prisma/client/index.d.ts 至少匹配 1 行（UserTravelRecord 类型已包含新字段）
    - grep "endDate" apps/server/node_modules/.prisma/client/index.d.ts 至少匹配 1 行
  </acceptance_criteria>
  <done>Prisma Client 重新生成，UserTravelRecord TypeScript 类型包含 startDate/endDate。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: [BLOCKING] Push schema 到数据库</name>
  <files>apps/server/prisma/schema.prisma (no write, push only)</files>
  <read_first>
    - apps/server/prisma/schema.prisma（已修改的 schema）
    - apps/server/.env 或 apps/server/.env.example（确认 DATABASE_URL / DIRECT_URL 已配置）
    - .planning/phases/27-multi-visit-record-foundation/27-RESEARCH.md（Runtime State Inventory：migration 动作清单）
  </read_first>
  <action>
    **[BLOCKING]** 本任务是 Phase 27 的强制 schema 同步关卡，后续所有后端/前端任务都依赖于真实数据库结构与 schema 对齐。

    1. 执行 `pnpm --filter @trip-map/server exec prisma db push --schema=apps/server/prisma/schema.prisma --accept-data-loss` 将 schema 变更推送到数据库。
       - 使用 `--accept-data-loss` 是为了非 TTY 环境（agent 无法交互确认）；本次变更仅为"移除 unique 约束 + 新增 nullable 字段"，不会丢失任何数据行（存量记录的新字段自动为 NULL = "日期未知"语义 D-08）。
       - 若命令因 DATABASE_URL 未配置而失败：创建一个 `checkpoint:human-action` 并停下，提示用户配置 .env 后重试；不得跳过此任务继续下游。

    2. 数据库推送成功后，SQL 层实际发生的变更应为：
       ```
       ALTER TABLE "UserTravelRecord" DROP CONSTRAINT "UserTravelRecord_userId_placeId_key";
       ALTER TABLE "UserTravelRecord" ADD COLUMN "startDate" TEXT;
       ALTER TABLE "UserTravelRecord" ADD COLUMN "endDate" TEXT;
       CREATE INDEX "UserTravelRecord_userId_placeId_idx" ON "UserTravelRecord"("userId", "placeId");
       ```

    3. 不得人工运行 raw ALTER TABLE；必须使用 Prisma db push，保持 schema 与数据库的唯一真源关系。
    4. 不得使用 `prisma migrate dev`（开发环境会创建 migration 文件；本项目既有风格是 db push——Read first 中无 migrations/phase27 目录，证实团队惯例）。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/server exec prisma db push --schema=apps/server/prisma/schema.prisma --accept-data-loss &amp;&amp; pnpm --filter @trip-map/server exec prisma validate --schema=apps/server/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `prisma db push` 退出码为 0
    - stdout 包含 "Your database is now in sync with your Prisma schema" 或等价成功提示
    - `prisma validate` 退出码为 0，确认 schema 与 Prisma Client 对齐
    - 如果用户后续运行 `pnpm --filter @trip-map/server exec prisma db push --schema=apps/server/prisma/schema.prisma` 无 `--accept-data-loss`，命令仍成功（证明数据库已 idempotent 达到目标状态）
  </acceptance_criteria>
  <done>PostgreSQL 数据库中的 UserTravelRecord 表已移除 unique 约束、新增 startDate/endDate 字段；schema 与数据库结构对齐，下游任务可安全调用 prisma.userTravelRecord.create 多次同 placeId 记录。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → PostgreSQL (via DATABASE_URL) | Schema 推送命令直接对数据库执行 DDL，需确保连接目标是预期环境（dev/staging，不是生产） |
| Prisma schema 文件 → Prisma Client 代码生成产物 | schema 字段类型定义的信任边界；字段类型错误会导致运行时 silent 丢字段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | apps/server/prisma/schema.prisma 日期字段类型 | mitigate | 字段严格声明为 `String?`（nullable），禁止 `DateTime` 或带默认值；通过 `prisma validate` 在 Task 2 末尾校验 |
| T-27-01-02 | Denial of Service | `prisma db push` 误指向生产数据库 | mitigate | 所有命令显式传 `--schema=apps/server/prisma/schema.prisma`，使用当前仓库的 .env；不使用 `--force-reset`；执行前 executor 必须读取 .env 确认 DATABASE_URL 主机名（host） |
| T-27-01-03 | Information Disclosure | 存量 UserTravelRecord 数据在迁移中泄露 | accept | 变更仅为移除 unique 约束 + 新增 nullable 字段，无数据导出路径；风险极低 |
| T-27-01-04 | Tampering | contracts 字段定义与 schema 字段定义不一致 | mitigate | contracts `string \| null` 与 schema `String?` 是对称映射；Task 2/3 之后后端 DTO 层（Plan 02）会引入 class-validator `@Matches(/^\d{4}-\d{2}-\d{2}$/)` 对注入进行二次阻断 |
</threat_model>

<verification>
1. `pnpm --filter @trip-map/contracts build` 成功。
2. `pnpm --filter @trip-map/contracts typecheck` 成功。
3. `npx prisma validate --schema=apps/server/prisma/schema.prisma` 成功。
4. `npx prisma db push --schema=apps/server/prisma/schema.prisma` 成功（`prisma db push` 对已处于目标状态的库是幂等的，二次运行应返回 "already in sync"）。
5. grep "startDate" apps/server/node_modules/.prisma/client/index.d.ts 匹配至少 1 行。
</verification>

<success_criteria>
- TravelRecord 与 CreateTravelRecordRequest contracts 包含 `startDate: string | null` / `endDate: string | null`
- contracts 包 dist 已构建，消费方 `import type { TravelRecord } from '@trip-map/contracts'` 可读取新字段
- Prisma schema 中 UserTravelRecord 已去 unique 化并新增日期字段
- Prisma Client 已重新生成，`UserTravelRecord` Prisma 类型包含新字段
- 数据库实际结构已与 schema 对齐
</success_criteria>

<output>
After completion, create `.planning/phases/27-multi-visit-record-foundation/27-01-SUMMARY.md` including:
- contracts 与 schema 的最终差异概述
- 执行的 Prisma 命令序列（generate + db push）及其退出码
- 下游 Plan（Plan 02 后端、Plan 03 前端 store）可依赖的新类型名称清单
</output>
</content>
</invoke>
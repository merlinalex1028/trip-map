---
phase: 05-草稿取消闭环与点位重选修复
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/stores/map-points.ts
  - src/stores/map-points.spec.ts
  - src/components/WorldMapStage.spec.ts
  - src/components/PointPreviewDrawer.spec.ts
autonomous: true
requirements:
  - MAP-03
  - PNT-05
user_setup: []
must_haves:
  truths:
    - 当地图上已经存在未保存草稿时，点击已有点位会切换到该点位且旧草稿立即从地图上消失。
    - 当用户在重选到已有点位后关闭抽屉，不会留下任何未保存草稿点位。
    - 当草稿仍处于新建流程中时，再次点击地图有效位置仍会走现有的草稿替换路径，而不是生成重复残留点位。
  artifacts:
    - src/stores/map-points.ts
    - src/stores/map-points.spec.ts
    - src/components/WorldMapStage.spec.ts
    - src/components/PointPreviewDrawer.spec.ts
  key_links:
    - SeedMarkerLayer 的 marker 点击继续走 `selectPointById()`，但该动作必须先清理与目标点位无关的旧 draft
    - PointPreviewDrawer 的关闭动作继续走 `clearActivePoint()`，并且在“已从草稿切到已有点位”的场景下不再留下 draft 残留
    - WorldMapStage 的地图点击继续走 `replaceDraftFromDetection()`，保证 draft->draft 的替换语义不回退
---

<objective>
修复 Phase 3/4 交界处遗留的草稿状态机断口，让“草稿新建 -> 重选已有点位 -> 关闭抽屉”重新形成完整闭环。

Purpose: 这个 gap 直接阻断了 `MAP-03` 和 `PNT-05` 的真实运行时表现；如果不补，用户虽然能重新选中已有点位，但地图上会残留不应存在的未保存草稿。
Output: 收紧后的 `map-points` 选择/清理逻辑，以及覆盖精确断口的 store + interaction 回归测试。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/STATE.md
@.planning/phases/03-点位闭环与本地持久化/03-01-PLAN.md
@.planning/phases/03-点位闭环与本地持久化/03-01-SUMMARY.md
@.planning/phases/03-点位闭环与本地持久化/03-02-SUMMARY.md
@.planning/phases/04-可用性打磨与增强能力/04-03-PLAN.md
@.planning/phases/04-可用性打磨与增强能力/04-03-SUMMARY.md
@src/stores/map-points.ts
@src/stores/map-points.spec.ts
@src/components/SeedMarkerLayer.vue
@src/components/WorldMapStage.vue
@src/components/WorldMapStage.spec.ts
@src/components/PointPreviewDrawer.vue
@src/components/PointPreviewDrawer.spec.ts

<interfaces>
From `src/stores/map-points.ts`:
```ts
function clearActivePoint()
function startDraftFromDetection(point: DraftMapPoint)
function replaceDraftFromDetection(point: DraftMapPoint)
function discardDraft()
function selectPointById(id: string)
```

Current broken path from audit:
```ts
// displayPoints always merges draftPoint into the rendered map.
if (draftPoint.value) {
  points.push(draftPoint.value)
}

// clearActivePoint only discards when the selected id is still the draft id.
if (draftPoint.value && selectedPointId.value === draftPoint.value.id) {
  discardDraft()
  return
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 收紧 map-points 的重选状态机，确保切到已有点位时立即清掉旧草稿</name>
  <files>src/stores/map-points.ts, src/stores/map-points.spec.ts</files>
  <read_first>src/stores/map-points.ts, src/stores/map-points.spec.ts, src/components/SeedMarkerLayer.vue, src/components/PointPreviewDrawer.vue, .planning/v1.0-MILESTONE-AUDIT.md, .planning/ROADMAP.md, .planning/phases/03-点位闭环与本地持久化/03-01-PLAN.md</read_first>
  <behavior>
    - Test 1: `startDraftFromDetection()` 后执行 `selectPointById('seed-kyoto')`，`draftPoint` 变为 `null`，`selectedPointId` 变为 `seed-kyoto`，且 `displayPoints` 中不再包含旧 draft id。
    - Test 2: 已存在 draft 时再次执行 `replaceDraftFromDetection()`，仍然只保留新的 draft，旧的 draft id 不再出现在 `displayPoints` 中。
    - Test 3: 当选中的仍然是 draft 自身时，`clearActivePoint()` 继续丢弃 draft，不回退 Phase 3 已有的取消新建语义。
  </behavior>
  <action>修改 `src/stores/map-points.ts` 中的 `selectPointById()`，把“选中已有点位”视为对当前未保存草稿的显式放弃：当 `draftPoint.value` 存在、目标 `id` 不等于当前 draft id，且目标点位来自 `seed` 或 `saved` 时，先把 `draftPoint.value` 置空，再基于清理后的 `displayPoints` 重建 `selectedPointId`、`drawerMode` 和 `editableSnapshot`。不要改动 `replaceDraftFromDetection()` 的现有 draft->draft 替换路径，也不要让关闭已有点位时误删真正已保存/预置点。同步扩展 `src/stores/map-points.spec.ts`，新增针对审计断口的精确单测，至少包含测试名或断言字符串 `clears an existing draft when selecting a saved or seed point`，并保留现有 draft 替换与取消新建用例继续通过。该任务实现 `MAP-03` 与 `PNT-05`，以 audit 中的 flow gap 为唯一修复目标，不引入新的抽屉模式或 UI 分支。</action>
  <verify>
    <automated>pnpm test -- src/stores/map-points.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/stores/map-points.ts` 中的 `selectPointById(` 包含对 `draftPoint.value` 的清理分支
    - `src/stores/map-points.spec.ts` 包含字符串 `clears an existing draft when selecting a saved or seed point`
    - `src/stores/map-points.spec.ts` 仍包含字符串 `creates and replaces a draft point from detected results`
    - `pnpm test -- src/stores/map-points.spec.ts` 通过
  </acceptance_criteria>
  <done>store 层已经把“重选已有点位 = 放弃当前未保存草稿”编码进状态机，旧 draft 不会再被 `displayPoints` 继续混入渲染结果。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用交互级回归锁住“草稿 -> 选已有点位 -> 关闭抽屉”的真实断口</name>
  <files>src/components/WorldMapStage.spec.ts, src/components/PointPreviewDrawer.spec.ts</files>
  <read_first>src/components/WorldMapStage.spec.ts, src/components/PointPreviewDrawer.spec.ts, src/components/WorldMapStage.vue, src/components/PointPreviewDrawer.vue, src/components/SeedMarkerLayer.vue, src/stores/map-points.ts, .planning/v1.0-MILESTONE-AUDIT.md, .planning/phases/04-可用性打磨与增强能力/04-03-PLAN.md</read_first>
  <behavior>
    - Test 1: 通过 `WorldMapStage` 真实点击生成 draft 后，再点击已有 marker，store 中 `draftPoint` 变为 `null`，地图 DOM 中不存在 `.seed-marker--draft`，而已有点位保持为当前 `activePoint`。
    - Test 2: 在已经从 draft 切到已有点位后，通过 `PointPreviewDrawer` 的关闭按钮或 `Escape` 关闭抽屉，`activePoint` 变为 `null` 且 `draftPoint` 仍保持 `null`。
    - Test 3: 现有 “再次点击地图替换 draft” 的交互测试继续通过，证明本次修复没有破坏原本的 draft 替换语义。
  </behavior>
  <action>扩展 `src/components/WorldMapStage.spec.ts`，新增完整回归用例覆盖审计中的确切路径：地图点击创建 draft、点击已有 marker 触发 `selectPointById()`、断言 `.seed-marker--draft` 已移除且 `activePoint` 指向已有点位。再扩展 `src/components/PointPreviewDrawer.spec.ts`，新增用例验证在“先有 draft、后选已有点位”的前提下点击 `关闭面板` 或触发 `Escape` 后，`activePoint` 与 `draftPoint` 都为空，确保 `clearActivePoint()` 不再遗漏旧草稿。不要改写现有文案或交互入口；此任务只增加回归覆盖并验证 Task 1 的状态机修复已经真正接通 UI 闭环。</action>
  <verify>
    <automated>pnpm test -- src/components/WorldMapStage.spec.ts src/components/PointPreviewDrawer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/WorldMapStage.spec.ts` 包含字符串 `removes the old draft when selecting an existing point`
    - `src/components/PointPreviewDrawer.spec.ts` 包含字符串 `does not leave a draft behind after switching to an existing point and closing`
    - `src/components/PointPreviewDrawer.spec.ts` 仍包含字符串 `closes immediately on Escape when there are no unsaved edits`
    - `pnpm test -- src/components/WorldMapStage.spec.ts src/components/PointPreviewDrawer.spec.ts` 通过
  </acceptance_criteria>
  <done>精确断口已被 interaction-level 回归保护，后续再出现 draft 残留会直接在测试层暴露。</done>
</task>

</tasks>

<verification>
- [ ] `pnpm test -- src/stores/map-points.spec.ts`
- [ ] `pnpm test -- src/components/WorldMapStage.spec.ts src/components/PointPreviewDrawer.spec.ts`
- [ ] `rg 'clears an existing draft when selecting a saved or seed point' src/stores/map-points.spec.ts`
- [ ] `rg 'removes the old draft when selecting an existing point' src/components/WorldMapStage.spec.ts`
- [ ] `rg 'does not leave a draft behind after switching to an existing point and closing' src/components/PointPreviewDrawer.spec.ts`
</verification>

<success_criteria>
MAP-03 与 PNT-05 的 gap closure 计划完成后，必须同时满足以下事实：
- 用户从 draft 切到已有点位时，地图上只剩已有点位，不再保留旧 draft marker。
- 用户关闭抽屉或结束该次查看后，不会因为之前创建过 draft 而留下未保存点位。
- 现有 draft 替换与取消新建语义保持可用，并被自动化测试继续覆盖。
</success_criteria>

<output>
After completion, create `.planning/phases/05-草稿取消闭环与点位重选修复/05-01-SUMMARY.md`
</output>

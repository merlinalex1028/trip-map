---
phase: 03-点位闭环与本地持久化
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/PointPreviewDrawer.vue
  - src/components/PointPreviewDrawer.spec.ts
  - src/stores/map-ui.ts
  - src/stores/map-points.ts
  - src/App.vue
  - src/App.spec.ts
autonomous: true
requirements:
  - PNT-02
  - PNT-03
  - DRW-03
user_setup: []
must_haves:
  truths:
    - The drawer has explicit detected-preview, view, and edit modes with the right actions in each mode.
    - Users can edit only `名称`、`简介`、`点亮状态`, cancel back to the last saved snapshot, and confirm before destructive actions.
    - Seed points can be hidden from the drawer while saved user points can be deleted, without mixing the two behaviors.
  artifacts:
    - upgraded PointPreviewDrawer with mode-specific controls
    - component tests for drawer modes and confirmations
    - map-points / map-ui integration for dirty-edit close handling
  key_links:
    - detected-preview mode leads into editing or save flow without auto-writing on open
    - view mode is the default for saved points
    - edit cancellation restores the exact saved snapshot instead of mutating live display data
---

<objective>
Upgrade the preview drawer into the full Phase 3 interaction surface for viewing, editing, deleting, and hiding points.

Purpose: Once point lifecycle state exists, the drawer must become the controlled place where users review a detected result, edit saved content, and perform destructive actions safely.
Output: A mode-driven drawer UI with the approved fields, confirm-on-close semantics, and separate delete/hide behaviors for user points and seed points.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@PRD.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-点位闭环与本地持久化/03-CONTEXT.md
@.planning/phases/03-点位闭环与本地持久化/03-RESEARCH.md
@.planning/phases/03-点位闭环与本地持久化/03-VALIDATION.md
@.planning/phases/01-地图基础与应用骨架/01-UI-SPEC.md
@src/components/PointPreviewDrawer.vue
@src/stores/map-ui.ts
@src/stores/map-points.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement explicit drawer modes and the Phase 3 editable field set</name>
  <files>src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/stores/map-points.ts</files>
  <read_first>src/components/PointPreviewDrawer.vue, src/stores/map-points.ts, src/types/map-point.ts, .planning/phases/03-点位闭环与本地持久化/03-CONTEXT.md, .planning/phases/01-地图基础与应用骨架/01-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: Detected-preview mode shows recognition details and a primary action to start editing or save the point.
    - Test 2: Saved user points open in view mode by default and only enter edit mode when the user clicks an explicit edit action.
    - Test 3: Edit mode exposes exactly three mutable fields: `名称`, `简介`, `点亮状态`.
  </behavior>
  <action>Refactor `src/components/PointPreviewDrawer.vue` to render by explicit `drawerMode` values from `useMapPointsStore()`: `detected-preview`, `view`, and `edit`. In `detected-preview`, show the detected place name, country/region line, coordinates, the existing recognition description, and buttons labeled exactly `保存为地点` and `关闭`. Clicking `保存为地点` should switch to `edit` mode with a prefilled editable snapshot. In `view` mode, show the saved point details plus a secondary button labeled `编辑`; user-created saved points must also show `删除`, and seed points must show `隐藏`. In `edit` mode, render a form with fields labeled exactly `名称`, `简介`, and `点亮状态`; do not add tags, dates, images, or extra metadata. Create `src/components/PointPreviewDrawer.spec.ts` covering detected-preview rendering, default view mode for saved points, and the exact three editable labels.</action>
  <verify>test -f src/components/PointPreviewDrawer.spec.ts && rg '保存为地点|编辑|删除|隐藏' src/components/PointPreviewDrawer.vue && rg '名称|简介|点亮状态' src/components/PointPreviewDrawer.vue && pnpm test -- src/components/PointPreviewDrawer.spec.ts</verify>
  <acceptance_criteria>
    - src/components/PointPreviewDrawer.vue contains `保存为地点`
    - src/components/PointPreviewDrawer.vue contains `编辑`
    - src/components/PointPreviewDrawer.vue contains `删除`
    - src/components/PointPreviewDrawer.vue contains `隐藏`
    - src/components/PointPreviewDrawer.vue contains `名称`
    - src/components/PointPreviewDrawer.vue contains `简介`
    - src/components/PointPreviewDrawer.vue contains `点亮状态`
    - src/components/PointPreviewDrawer.spec.ts contains `detected-preview`
  </acceptance_criteria>
  <done>The drawer now exposes the exact Phase 3 interaction modes and editable field set without drifting into richer-content scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add close, cancel, delete, and hide confirmations with restore-on-cancel behavior</name>
  <files>src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/stores/map-ui.ts, src/stores/map-points.ts, src/App.vue, src/App.spec.ts</files>
  <read_first>src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/stores/map-ui.ts, src/stores/map-points.ts, src/App.vue, .planning/phases/03-点位闭环与本地持久化/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Closing the drawer with unsaved edits triggers a confirmation instead of silently dropping changes.
    - Test 2: Cancel in edit mode restores the last saved content and returns to view mode.
    - Test 3: User-point deletion and seed hiding both require confirmation and then apply the correct destructive action.
  </behavior>
  <action>Implement the Phase 3 destructive and cancel semantics in `src/components/PointPreviewDrawer.vue` and related stores. Use exact confirmation strings: unsaved-close confirmation `你有未保存的更改，确定关闭吗？`, user-point delete confirmation `确定删除这个地点吗？`, and seed hide confirmation `确定隐藏这个预置地点吗？`. In edit mode, the `取消` action must call a store method that restores the editable snapshot from the last saved point data and then returns the drawer to `view` mode. The drawer close button and `Esc` flow must consult the same unsaved-change guard. When the user confirms deletion, remove only user-created saved points; when the user confirms hiding, add the seed id to `deletedSeedIds` and close back to the map. Update `src/App.vue` / `src/App.spec.ts` as needed so the drawer close behavior still works from the root shell and remains mounted with Pinia.</action>
  <verify>rg '你有未保存的更改，确定关闭吗？|确定删除这个地点吗？|确定隐藏这个预置地点吗？' src/components/PointPreviewDrawer.vue && rg '取消' src/components/PointPreviewDrawer.vue && pnpm test -- src/components/PointPreviewDrawer.spec.ts src/App.spec.ts</verify>
  <acceptance_criteria>
    - src/components/PointPreviewDrawer.vue contains `你有未保存的更改，确定关闭吗？`
    - src/components/PointPreviewDrawer.vue contains `确定删除这个地点吗？`
    - src/components/PointPreviewDrawer.vue contains `确定隐藏这个预置地点吗？`
    - src/components/PointPreviewDrawer.vue contains `取消`
    - src/components/PointPreviewDrawer.spec.ts contains `window.confirm`
    - src/App.spec.ts contains `createPinia`
  </acceptance_criteria>
  <done>The drawer now matches the approved close/cancel/delete semantics, and destructive actions are explicit instead of accidental.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test -- src/components/PointPreviewDrawer.spec.ts src/App.spec.ts
- [ ] rg '保存为地点|编辑|删除|隐藏' src/components/PointPreviewDrawer.vue
- [ ] rg '你有未保存的更改，确定关闭吗？' src/components/PointPreviewDrawer.vue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The drawer supports detected-preview, view, and edit modes with the approved field set
- Cancel restores saved content instead of mutating live point data
- Delete and hide actions are confirmed and affect the correct point category

</success_criteria>

<output>
After completion, create `.planning/phases/03-点位闭环与本地持久化/03-02-SUMMARY.md`
</output>

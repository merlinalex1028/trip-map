---
phase: 03-点位闭环与本地持久化
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/map-point.ts
  - src/stores/map-ui.ts
  - src/stores/map-points.ts
  - src/stores/map-points.spec.ts
  - src/components/WorldMapStage.vue
  - src/components/WorldMapStage.spec.ts
  - src/components/SeedMarkerLayer.vue
  - src/App.vue
autonomous: true
requirements:
  - MAP-03
  - PNT-01
  - PNT-05
user_setup: []
must_haves:
  truths:
    - Successful geo recognition can be turned into a user-controlled draft point instead of an immediate saved record.
    - The map has one unified display source for seed points, saved points, and the currently active draft marker.
    - Clicking another valid location while a draft exists replaces that draft through an explicit discard notice instead of leaving residue.
  artifacts:
    - src/stores/map-points.ts
    - expanded src/types/map-point.ts
    - updated WorldMapStage and SeedMarkerLayer integration
    - store/component tests for draft lifecycle
  key_links:
    - WorldMapStage hands detection results into map-points domain actions instead of storing them as ad-hoc component data
    - SeedMarkerLayer consumes merged display points from the domain store
    - map-ui keeps recognition/notice state while map-points owns point lifecycle state
---

<objective>
Establish the point-domain state layer that turns Phase 2 detection results into a proper draft/save/delete lifecycle for the map.

Purpose: Phase 3 cannot safely add drawer editing or persistence until draft points, saved points, and seed overlays are modeled explicitly in one place.
Output: A new map-points store, expanded point types, and map-stage wiring that can create, replace, select, and discard draft points without leaving stale markers behind.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@PRD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-点位闭环与本地持久化/03-CONTEXT.md
@.planning/phases/03-点位闭环与本地持久化/03-RESEARCH.md
@.planning/phases/03-点位闭环与本地持久化/03-VALIDATION.md
@.planning/phases/01-地图基础与应用骨架/01-UI-SPEC.md
@src/components/WorldMapStage.vue
@src/components/SeedMarkerLayer.vue
@src/stores/map-ui.ts
@src/types/map-point.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the point-domain models and Pinia store for draft and saved point lifecycle</name>
  <files>src/types/map-point.ts, src/stores/map-points.ts, src/stores/map-points.spec.ts, src/stores/map-ui.ts</files>
  <read_first>src/types/map-point.ts, src/stores/map-ui.ts, src/data/seed-points.ts, src/data/preview-points.ts, .planning/phases/03-点位闭环与本地持久化/03-CONTEXT.md, .planning/phases/03-点位闭环与本地持久化/03-RESEARCH.md</read_first>
  <behavior>
    - Test 1: The store can create a draft point from a detected result without immediately writing it as a saved point.
    - Test 2: The store can replace an existing draft with a new detected result and expose a discard-notice path.
    - Test 3: The store exposes merged display points for seed, saved, and draft markers through one computed source.
  </behavior>
  <action>Create `src/stores/map-points.ts` as a Pinia setup store that owns point-domain state. Expand `src/types/map-point.ts` to export exact types `DrawerMode`, `PointStorageHealth`, `PersistedMapPoint`, `SeedPointOverride`, `DraftMapPoint`, `EditablePointSnapshot`, and keep `MapPointDisplay` for render output. In `src/stores/map-points.ts`, add refs/computed for `userPoints`, `seedOverrides`, `deletedSeedIds`, `draftPoint`, `selectedPointId`, `drawerMode`, `storageHealth`, `displayPoints`, and `activePoint`. Add actions with these concrete names: `bootstrapPoints`, `startDraftFromDetection`, `replaceDraftFromDetection`, `discardDraft`, `selectPointById`, `enterEditMode`, `exitEditMode`, `saveDraftAsPoint`, `updateSavedPoint`, `deleteUserPoint`, `hideSeedPoint`, `restoreSeedPoint`, and `clearCorruptStorageState`. Use `nanoid()` for new saved point ids with the prefix `saved-`. Keep `src/stores/map-ui.ts` focused on recognition state, toast notices, and global drawer-close interactions only; remove any assumption that `selectedPoint` alone owns all point data. Create `src/stores/map-points.spec.ts` to assert draft creation, draft replacement, save-to-user-point conversion, and seed hiding behavior.</action>
  <verify>test -f src/stores/map-points.ts && test -f src/stores/map-points.spec.ts && rg 'startDraftFromDetection|replaceDraftFromDetection|saveDraftAsPoint|hideSeedPoint' src/stores/map-points.ts && rg 'DrawerMode|PersistedMapPoint|SeedPointOverride|DraftMapPoint' src/types/map-point.ts && pnpm test -- src/stores/map-points.spec.ts</verify>
  <acceptance_criteria>
    - src/stores/map-points.ts contains `startDraftFromDetection`
    - src/stores/map-points.ts contains `replaceDraftFromDetection`
    - src/stores/map-points.ts contains `saveDraftAsPoint`
    - src/stores/map-points.ts contains `hideSeedPoint`
    - src/types/map-point.ts contains `export type DrawerMode`
    - src/types/map-point.ts contains `export interface PersistedMapPoint`
    - src/stores/map-points.spec.ts contains `draft`
  </acceptance_criteria>
  <done>The app has a single domain store that can distinguish draft, saved, and seed-derived points before any drawer form work begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire the map stage and marker layer to use the domain store draft flow</name>
  <files>src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts, src/components/SeedMarkerLayer.vue, src/App.vue, src/stores/map-ui.ts, src/stores/map-points.ts</files>
  <read_first>src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts, src/components/SeedMarkerLayer.vue, src/App.vue, src/stores/map-ui.ts, src/stores/map-points.ts, .planning/phases/03-点位闭环与本地持久化/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: A successful click creates a visible draft marker and opens the drawer in detected-preview mode.
    - Test 2: Clicking a different valid location while a draft exists replaces the old draft and surfaces the discard notice.
    - Test 3: Clicking an existing seed or saved point re-selects it from the merged display list and does not create a new draft.
  </behavior>
  <action>Update `src/components/WorldMapStage.vue` so `loadPreviewPoints()` is removed from component-local state and the stage instead consumes `displayPoints`, `activePoint`, and draft actions from `useMapPointsStore()`. On successful geo detection, call `startDraftFromDetection(...)` when no draft exists, or `replaceDraftFromDetection(...)` when `draftPoint` already exists; before replacement, set `interactionNotice.message` to the exact string `当前未保存地点将被丢弃，并切换到新位置`. Preserve the existing invalid-click guidance strings from Phase 2. Update `src/components/SeedMarkerLayer.vue` to read merged display points and select existing points by id via the domain store instead of passing full point objects into `map-ui`. Update `src/App.vue` so drawer-open layout padding keys off the new domain-store `activePoint` rather than the old `selectedPoint`. Extend `src/components/WorldMapStage.spec.ts` with a draft-creation assertion and a draft-replacement assertion that checks the discard notice string.</action>
  <verify>rg '当前未保存地点将被丢弃，并切换到新位置' src/components/WorldMapStage.vue && rg 'selectPointById' src/components/SeedMarkerLayer.vue && rg 'activePoint' src/App.vue && pnpm test -- src/components/WorldMapStage.spec.ts</verify>
  <acceptance_criteria>
    - src/components/WorldMapStage.vue contains `startDraftFromDetection`
    - src/components/WorldMapStage.vue contains `replaceDraftFromDetection`
    - src/components/WorldMapStage.vue contains `当前未保存地点将被丢弃，并切换到新位置`
    - src/components/SeedMarkerLayer.vue contains `selectPointById`
    - src/App.vue contains `activePoint`
    - src/components/WorldMapStage.spec.ts contains `discard`
  </acceptance_criteria>
  <done>The map surface now creates and replaces draft points through the domain store, and every marker on screen comes from the same merged point source.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test -- src/stores/map-points.spec.ts
- [ ] pnpm test -- src/components/WorldMapStage.spec.ts
- [ ] rg '当前未保存地点将被丢弃，并切换到新位置' src/components/WorldMapStage.vue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Valid clicks create a draft point rather than an immediate saved record
- Draft replacement never leaves an orphaned marker behind
- Existing map marker selection still works through the merged point source

</success_criteria>

<output>
After completion, create `.planning/phases/03-点位闭环与本地持久化/03-01-SUMMARY.md`
</output>

---
phase: 03-点位闭环与本地持久化
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/services/point-storage.ts
  - src/services/point-storage.spec.ts
  - src/data/preview-points.ts
  - src/stores/map-points.ts
  - src/components/PointPreviewDrawer.vue
  - src/components/WorldMapStage.vue
  - src/App.vue
  - src/App.spec.ts
autonomous: true
requirements:
  - DAT-02
  - DAT-03
user_setup: []
must_haves:
  truths:
    - Saved user points, seed overrides, and hidden seed ids persist across refresh in one versioned local snapshot.
    - Runtime merging prefers local overrides over seed data while keeping seed data itself read-only.
    - Corrupt or incompatible snapshots are reported explicitly to the user instead of being swallowed silently.
  artifacts:
    - src/services/point-storage.ts
    - src/services/point-storage.spec.ts
    - updated map-points bootstrap/persist flow
    - app-level storage warning surface
  key_links:
    - map-points store bootstraps from point-storage snapshot status on app load
    - point-storage merge filters deleted seed ids and overlays seed overrides by matching id
    - App renders a persistent corruption warning with a manual clear path when storage health is corrupt
---

<objective>
Finish the Phase 3 persistence layer so point edits survive refresh and seed data merges deterministically with local user state.

Purpose: CRUD and drawer work are not complete product value unless they survive reloads and handle broken local snapshots transparently.
Output: A versioned point-storage repository, reload-safe persistence wiring, and an explicit corrupted-storage notice path with manual clear.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@PRD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-点位闭环与本地持久化/03-CONTEXT.md
@.planning/phases/03-点位闭环与本地持久化/03-RESEARCH.md
@.planning/phases/03-点位闭环与本地持久化/03-VALIDATION.md
@src/data/seed-points.ts
@src/data/preview-points.ts
@src/stores/map-points.ts
@src/App.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the versioned point-storage repository and merge contract</name>
  <files>src/services/point-storage.ts, src/services/point-storage.spec.ts, src/data/preview-points.ts, src/stores/map-points.ts</files>
  <read_first>src/data/preview-points.ts, src/data/seed-points.ts, src/stores/map-points.ts, src/types/map-point.ts, .planning/phases/03-点位闭环与本地持久化/03-CONTEXT.md, .planning/phases/03-点位闭环与本地持久化/03-RESEARCH.md</read_first>
  <behavior>
    - Test 1: The repository can read and write a versioned snapshot with `userPoints`, `seedOverrides`, and `deletedSeedIds`.
    - Test 2: Merge logic overlays seed overrides by matching `id` and filters any seed whose id appears in `deletedSeedIds`.
    - Test 3: Invalid or incompatible snapshots are reported as corrupt instead of silently returning an empty list.
  </behavior>
  <action>Create `src/services/point-storage.ts` with the exact storage key `trip-map:point-state:v1` and exported functions `loadPointStorageSnapshot`, `savePointStorageSnapshot`, `clearPointStorageSnapshot`, and `mergeSeedAndLocalPoints`. The snapshot shape must be `{ version: 1, userPoints: PersistedMapPoint[], seedOverrides: SeedPointOverride[], deletedSeedIds: string[] }`. Move the old `loadPreviewPoints()` implementation out of `src/data/preview-points.ts`; keep that file only as a backward-compatible thin wrapper or remove its usage entirely. In `mergeSeedAndLocalPoints`, apply `seedOverrides` by matching exact `id`, remove any seed whose id exists in `deletedSeedIds`, and then append `userPoints` after seed-derived display items. In `loadPointStorageSnapshot`, return a structured result that distinguishes `ready`, `empty`, and `corrupt`, and treat malformed JSON, wrong `version`, or invalid field types as `corrupt`. Create `src/services/point-storage.spec.ts` covering override precedence, hidden seed removal, corrupt JSON, and wrong-version snapshots.</action>
  <verify>test -f src/services/point-storage.ts && test -f src/services/point-storage.spec.ts && rg 'trip-map:point-state:v1|loadPointStorageSnapshot|savePointStorageSnapshot|mergeSeedAndLocalPoints' src/services/point-storage.ts && rg 'deletedSeedIds|version: 1|corrupt' src/services/point-storage.spec.ts && pnpm test -- src/services/point-storage.spec.ts</verify>
  <acceptance_criteria>
    - src/services/point-storage.ts contains `trip-map:point-state:v1`
    - src/services/point-storage.ts contains `loadPointStorageSnapshot`
    - src/services/point-storage.ts contains `savePointStorageSnapshot`
    - src/services/point-storage.ts contains `mergeSeedAndLocalPoints`
    - src/services/point-storage.spec.ts contains `deletedSeedIds`
    - src/services/point-storage.spec.ts contains `wrong version`
    - src/services/point-storage.spec.ts contains `corrupt`
  </acceptance_criteria>
  <done>The persistence layer has a deterministic, testable, versioned snapshot format instead of a loose local array.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist CRUD changes and surface explicit corrupted-storage recovery in the app</name>
  <files>src/stores/map-points.ts, src/components/PointPreviewDrawer.vue, src/components/WorldMapStage.vue, src/App.vue, src/App.spec.ts, src/services/point-storage.ts, src/services/point-storage.spec.ts</files>
  <read_first>src/stores/map-points.ts, src/App.vue, src/components/PointPreviewDrawer.vue, src/components/WorldMapStage.vue, src/services/point-storage.ts, .planning/phases/03-点位闭环与本地持久化/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Saving, editing, deleting, or hiding points writes the new snapshot and reloads correctly after a remount.
    - Test 2: A corrupt snapshot shows a persistent warning and does not silently erase data with no explanation.
    - Test 3: The warning exposes a manual clear action that removes the bad snapshot and lets the app recover on the next load.
  </behavior>
  <action>Update `src/stores/map-points.ts` so `bootstrapPoints()` reads from `loadPointStorageSnapshot()` on app startup, hydrates `userPoints`, `seedOverrides`, and `deletedSeedIds`, and sets `storageHealth` to `corrupt` when the snapshot fails validation. Ensure `saveDraftAsPoint`, `updateSavedPoint`, `deleteUserPoint`, `hideSeedPoint`, and `restoreSeedPoint` all call `savePointStorageSnapshot(...)` after mutating state. Update `src/App.vue` to render a persistent storage warning banner when `storageHealth === 'corrupt'` with the exact body copy `检测到本地存档异常，请清空本地存档后继续使用。` and a button labeled `清空本地存档`; the button must call `clearPointStorageSnapshot()` through the store and reset the warning. Add reload-oriented tests in `src/App.spec.ts` or relevant component/store specs that seed `localStorage`, mount the app/store, mutate a point, remount, and assert the mutated point still exists. Also add a corruption test that seeds invalid JSON, confirms the warning copy renders, triggers `清空本地存档`, and verifies the warning disappears after re-bootstrap.</action>
  <verify>rg '检测到本地存档异常，请清空本地存档后继续使用。|清空本地存档' src/App.vue && rg 'savePointStorageSnapshot|clearPointStorageSnapshot' src/stores/map-points.ts && pnpm test -- src/services/point-storage.spec.ts src/App.spec.ts && pnpm build</verify>
  <acceptance_criteria>
    - src/App.vue contains `检测到本地存档异常，请清空本地存档后继续使用。`
    - src/App.vue contains `清空本地存档`
    - src/stores/map-points.ts contains `savePointStorageSnapshot`
    - src/stores/map-points.ts contains `clearPointStorageSnapshot`
    - src/App.spec.ts contains `localStorage`
    - src/App.spec.ts contains `清空本地存档`
  </acceptance_criteria>
  <done>Point CRUD is now durable across refreshes, and corrupt storage is surfaced and recoverable instead of silently swallowed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test -- src/services/point-storage.spec.ts src/App.spec.ts
- [ ] pnpm build
- [ ] rg 'trip-map:point-state:v1' src/services/point-storage.ts
- [ ] rg '检测到本地存档异常，请清空本地存档后继续使用。' src/App.vue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- User-created and overridden point data survive a full page refresh
- Local seed overrides take precedence over built-in seed content and hidden seed ids remove those markers
- Corrupt storage is explained explicitly and recoverable from within the app

</success_criteria>

<output>
After completion, create `.planning/phases/03-点位闭环与本地持久化/03-03-SUMMARY.md`
</output>

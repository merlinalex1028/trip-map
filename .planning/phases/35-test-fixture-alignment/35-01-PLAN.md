---
phase: 35-test-fixture-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/modules/records/records.service.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Server stats test mock 使用 TOTAL_SUPPORTED_TRAVEL_COUNTRIES 而非硬编码数字"
    - "所有引用 totalSupportedCountries 的 mock 值和断言均从常量推导"
  artifacts:
    - path: "apps/server/src/modules/records/records.service.spec.ts"
      provides: "Records service unit tests with authoritative constant"
      contains: "TOTAL_SUPPORTED_TRAVEL_COUNTRIES"
  key_links:
    - from: "apps/server/src/modules/records/records.service.spec.ts"
      to: "apps/server/src/modules/canonical-places/place-metadata-catalog.ts"
      via: "ESM import"
      pattern: "import.*TOTAL_SUPPORTED_TRAVEL_COUNTRIES.*from.*place-metadata-catalog"
---

<objective>
将 `records.service.spec.ts` 中 `RecordsService.getStats` describe 块内的硬编码 `21`（`totalSupportedCountries` 值）替换为已导入的权威常量 `TOTAL_SUPPORTED_TRAVEL_COUNTRIES`，消除 fixture / production 口径不一致风险。

Purpose: 消除 milestone audit 识别的 test fixture 技术债务 — 当支持的国家数增长时，test mock 不会因忘记同步硬编码数字而产生 false-negative 或口径漂移。
Output: 一个已更新的 `records.service.spec.ts`，所有 `totalSupportedCountries: 21` mock 值与 `.toBe(21)` 断言均替换为常量引用。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@apps/server/src/modules/records/records.service.spec.ts
@apps/server/src/modules/canonical-places/place-metadata-catalog.ts

<interfaces>
<!-- 已被测试文件导入的常量 -->
From apps/server/src/modules/canonical-places/place-metadata-catalog.ts:
```typescript
export const TOTAL_SUPPORTED_TRAVEL_COUNTRIES = supportedOverseasCountries.size + 1
```

<!-- 文件中已存在的 import（第 10 行）-->
```typescript
import { TOTAL_SUPPORTED_TRAVEL_COUNTRIES } from '../canonical-places/place-metadata-catalog.js'
```
</interfaces>

<existing_state>
文件 `records.service.spec.ts` 中已存在 import `TOTAL_SUPPORTED_TRAVEL_COUNTRIES`（第 10 行）。
以下位置仍使用硬编码 `21`：

| 行号 | 当前代码 | 应替换为 |
|------|---------|----------|
| 284 | `totalSupportedCountries: 21,` | `totalSupportedCountries: TOTAL_SUPPORTED_TRAVEL_COUNTRIES,` |
| 294 | `totalSupportedCountries: 21,` | `totalSupportedCountries: TOTAL_SUPPORTED_TRAVEL_COUNTRIES,` |
| 306 | `totalSupportedCountries: 21,` | `totalSupportedCountries: TOTAL_SUPPORTED_TRAVEL_COUNTRIES,` |
| 314 | `expect(result.totalSupportedCountries).toBe(21)` | `expect(result.totalSupportedCountries).toBe(TOTAL_SUPPORTED_TRAVEL_COUNTRIES)` |

注意：第 322 行 `expect(authoritativeOverseasCountries.size).toBe(20)` 的 `20` **不需要**修改 —
它验证的是测试夹具中 `layer.json` 实际包含的海外国家数（China + 20 = 21），不是 `TOTAL_SUPPORTED_TRAVEL_COUNTRIES`。
第 323 行的断言 `expect(TOTAL_SUPPORTED_TRAVEL_COUNTRIES).toBe(authoritativeOverseasCountries.size + 1)` 已正确使用常量。
</existing_state>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 替换硬编码 21 为 TOTAL_SUPPORTED_TRAVEL_COUNTRIES</name>
  <files>apps/server/src/modules/records/records.service.spec.ts</files>
  <read_first>
    - apps/server/src/modules/records/records.service.spec.ts（查看当前状态，确认替换位置）
    - apps/server/src/modules/canonical-places/place-metadata-catalog.ts（确认常量导出签名）
  </read_first>
  <action>
在 `apps/server/src/modules/records/records.service.spec.ts` 中，将以下 4 处硬编码 `21` 替换为常量引用 `TOTAL_SUPPORTED_TRAVEL_COUNTRIES`：

1. **第 284 行**：`totalSupportedCountries: 21,` → `totalSupportedCountries: TOTAL_SUPPORTED_TRAVEL_COUNTRIES,`
   - 位于 `delegates to repository and returns stats as-is` 测试的 mock 返回值中

2. **第 294 行**：`totalSupportedCountries: 21,` → `totalSupportedCountries: TOTAL_SUPPORTED_TRAVEL_COUNTRIES,`
   - 位于同一测试的 `toEqual` 期望值中

3. **第 306 行**：`totalSupportedCountries: 21,` → `totalSupportedCountries: TOTAL_SUPPORTED_TRAVEL_COUNTRIES,`
   - 位于 `correctly distinguishes totalTrips from uniquePlaces for multi-visit same place` 测试的 mock 返回值中

4. **第 314 行**：`expect(result.totalSupportedCountries).toBe(21)` → `expect(result.totalSupportedCountries).toBe(TOTAL_SUPPORTED_TRAVEL_COUNTRIES)`
   - 位于同一测试的断言中

**不要修改以下内容：**
- 第 10 行：`import { TOTAL_SUPPORTED_TRAVEL_COUNTRIES }` — 已存在，保持不变
- 第 322 行：`expect(authoritativeOverseasCountries.size).toBe(20)` — 这是 fixture 断言，不是常量引用
- 第 323 行：`expect(TOTAL_SUPPORTED_TRAVEL_COUNTRIES).toBe(...)` — 已正确使用常量
- 第 349 行：`totalSupportedCountries: TOTAL_SUPPORTED_TRAVEL_COUNTRIES` — 已正确使用常量

进行 4 处精确替换，每处用 Edit 工具单独操作，确保只匹配目标行。
  </action>
  <acceptance_criteria>
    - 文件中不再出现 `totalSupportedCountries: 21` 字符串（`grep -c "totalSupportedCountries: 21" apps/server/src/modules/records/records.service.spec.ts` 返回 0）
    - 文件中不再出现 `.toBe(21)` 模式中的 `21`（`grep -c "\.toBe(21)" apps/server/src/modules/records/records.service.spec.ts` 返回 0）
    - 文件中出现 5 次 `TOTAL_SUPPORTED_TRAVEL_COUNTRIES`（原有 1 次 import + 第 349 行 1 次 + 本次替换 3 次 mock 值 + 本次替换 1 次断言 = 6 次；但 import 行是 import 语句引用，第 284/294/306 行各 1 次，第 314 行 1 次，第 323 行 1 次，第 349 行 1 次 → 总共 7 处引用 `TOTAL_SUPPORTED_TRAVEL_COUNTRIES`；用 `rg -c "TOTAL_SUPPORTED_TRAVEL_COUNTRIES" apps/server/src/modules/records/records.service.spec.ts` 验证）
    - 第 322 行 `expect(authoritativeOverseasCountries.size).toBe(20)` 保持不变（这是 fixture 验证，不是常量引用）
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --filter=records.service 2>&1</automated>
  </verify>
  <done>
    - `totalSupportedCountries: 21` 的 3 处 mock 值和 `.toBe(21)` 的 1 处断言全部替换为 `TOTAL_SUPPORTED_TRAVEL_COUNTRIES`
    - `npm test -- --filter=records.service` 全部测试通过，无 regression
    - 测试仍能正确验证 getStats 行为，但不再与常量脱钩
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 本次变更为 test fixture 对齐，不引入新信任边界或安全表面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | N/A — test file only | accept | 测试文件变更，不涉及生产代码路径或用户数据。无安全风险。 |
</threat_model>

<verification>
1. `npm test -- --filter=records.service` 全部通过
2. `rg "totalSupportedCountries: 21" apps/server/src/modules/records/records.service.spec.ts` 返回 0 匹配
3. `rg "\.toBe\(21\)" apps/server/src/modules/records/records.service.spec.ts` 返回 0 匹配（排除第 322 行的 `.toBe(20)` — 它不应被修改）
</verification>

<success_criteria>
- [ ] 文件中不再存在硬编码 `21` 作为 `totalSupportedCountries` 值
- [ ] `TOTAL_SUPPORTED_TRAVEL_COUNTRIES` 在 spec 文件中共出现 7 次（import ×1，mock 值 ×3，断言 ×2，repository 测试 ×1）
- [ ] `npm test -- --filter=records.service` 全部通过
- [ ] 第 322 行的 fixture 断言 `expect(authoritativeOverseasCountries.size).toBe(20)` 未被修改
</success_criteria>

<output>
After completion, create `.planning/phases/35-test-fixture-alignment/35-01-SUMMARY.md`
</output>

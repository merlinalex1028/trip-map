---
phase: 23-auth-ownership-foundation
plan: 11
type: execute
wave: 10
depends_on: [09]
files_modified:
  - apps/web/src/components/auth/AuthDialog.vue
  - apps/web/src/components/auth/AuthDialog.spec.ts
autonomous: true
gap_closure: true
requirements: [AUTH-02, AUTH-05]
---

<objective>
修正登录失败场景下认证弹层的居中布局，确保错误提示出现后弹层仍稳定停留在视口中央。

Purpose: 关闭 UAT 中“功能正确但错误态弹层视觉上偏左”的 cosmetic gap。
Output: centered auth dialog layout in error state + component regression spec。
</objective>

<context>
@.planning/phases/23-auth-ownership-foundation/23-UAT.md
@.planning/phases/23-auth-ownership-foundation/23-05-SUMMARY.md
@.planning/phases/23-auth-ownership-foundation/23-09-SUMMARY.md
@apps/web/src/components/auth/AuthDialog.vue
@apps/web/src/components/auth/AuthDialog.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用组件规格锁定错误态弹层布局</name>
  <files>apps/web/src/components/auth/AuthDialog.spec.ts</files>
  <action>
1. 补一条 AuthDialog 规格，覆盖登录失败后错误提示出现时的布局稳定性：
   - 弹层依然处于开启状态。
   - 错误提示插入后不会把弹层视觉锚点推到左侧。
2. 规格优先断言用于布局的 class / container contract，而不是只看文案存在。
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && rg -n "role=\\\"alert\\\"|dialog|center|justify-center|mx-auto" apps/web/src/components/auth/AuthDialog.spec.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 收紧 AuthDialog 的居中布局语义</name>
  <files>apps/web/src/components/auth/AuthDialog.vue</files>
  <behavior>
    - 登录失败时弹层继续保持居中
    - 错误提示出现不会触发浏览器原生 dialog 的偏移布局
  </behavior>
  <action>
1. 调整 `AuthDialog.vue` 的容器语义，避免原生 `<dialog>` 默认定位与当前 fixed/flex 布局互相竞争。
2. 保持现有交互、焦点恢复与错误提示文案不变，只修布局合同。
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm --filter @trip-map/web test -- src/components/auth/AuthDialog.spec.ts && pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 错误提示出现后弹层仍保持居中
    - 不引入新的认证流程字段或 UI 分支
    - AuthDialog 定向测试与 typecheck 通过
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `pnpm --filter @trip-map/web test -- src/components/auth/AuthDialog.spec.ts`
- `pnpm --filter @trip-map/web typecheck`
</verification>

<output>
After completion, create `.planning/phases/23-auth-ownership-foundation/23-11-SUMMARY.md`
</output>

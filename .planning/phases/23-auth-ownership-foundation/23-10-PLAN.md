---
phase: 23-auth-ownership-foundation
plan: 10
type: execute
wave: 10
depends_on: [09]
files_modified:
  - apps/web/src/components/LeafletMapStage.vue
  - apps/web/src/components/LeafletMapStage.spec.ts
  - apps/web/src/stores/map-points.ts
  - apps/web/src/stores/map-points.spec.ts
  - apps/web/src/stores/map-ui.ts
  - apps/web/src/App.spec.ts
  - apps/server/test/records-travel.e2e-spec.ts
  - apps/server/src/modules/records/records.controller.ts
  - apps/server/src/modules/records/records.service.ts
  - apps/server/src/modules/records/records.repository.ts
autonomous: true
gap_closure: true
requirements: [AUTH-03, SYNC-01, SYNC-02]
---

<objective>
修复 current-user records 的真实产品链路：匿名或刷新阶段不再误打 `/records`，已登录用户可以正常点亮地点，records 失败也会给出明确反馈而不是静默回滚。

Purpose: 关闭 UAT 中“刷新时 `/records` 500 / 未登录也打 `/records`”和“已登录仍无法点亮地点”的 blocker。
Output: records bootstrap source-of-truth 收口、live `/records` GET/POST 路径修复、对应 web/server regression tests。
</objective>

<context>
@.planning/phases/23-auth-ownership-foundation/23-UAT.md
@.planning/phases/23-auth-ownership-foundation/23-04-SUMMARY.md
@.planning/phases/23-auth-ownership-foundation/23-09-SUMMARY.md
@apps/web/src/components/LeafletMapStage.vue
@apps/web/src/components/LeafletMapStage.spec.ts
@apps/web/src/stores/map-points.ts
@apps/web/src/stores/map-points.spec.ts
@apps/web/src/stores/auth-session.ts
@apps/web/src/services/api/records.ts
@apps/server/src/modules/records/records.controller.ts
@apps/server/src/modules/records/records.service.ts
@apps/server/src/modules/records/records.repository.ts
@apps/server/test/records-travel.e2e-spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用测试锁定 records 恢复与点亮的真实边界</name>
  <files>
    apps/web/src/components/LeafletMapStage.spec.ts
    apps/web/src/stores/map-points.spec.ts
    apps/web/src/App.spec.ts
    apps/server/test/records-travel.e2e-spec.ts
  </files>
  <action>
1. 扩展 `LeafletMapStage.spec.ts` / `App.spec.ts`，锁定以下回归：
   - 匿名冷启动或 restore 流程中，不会在 auth bootstrap 之外额外调用 `fetchTravelRecords()`。
   - 只有在 authenticated records 真值已建立后，地图层才消费 records 快照，而不是自行再发 `/records` 拉取。
2. 扩展 `map-points.spec.ts`：
   - 点亮地点时若 records API 返回非 401 错误，会回滚 optimistic record，并给出明确的 warning notice。
   - 匿名状态不会把 `/records` 当作必经 bootstrap 路径。
3. 若需要 server 侧复现，补 `records-travel.e2e-spec.ts` 最小回归，确认 current-user `GET/POST /records` 在有效 session 下返回 200/201，而不是 500。
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && rg -n "fetchTravelRecords|bootstrapFromApi|warning|createTravelRecord|authenticated" apps/web/src/components/LeafletMapStage.spec.ts apps/web/src/stores/map-points.spec.ts apps/web/src/App.spec.ts apps/server/test/records-travel.e2e-spec.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 收口 records bootstrap 真源并修复 live `/records` 路径</name>
  <files>
    apps/web/src/components/LeafletMapStage.vue
    apps/web/src/stores/map-points.ts
    apps/web/src/stores/map-ui.ts
    apps/server/src/modules/records/records.controller.ts
    apps/server/src/modules/records/records.service.ts
    apps/server/src/modules/records/records.repository.ts
  </files>
  <behavior>
    - 匿名访问、刷新恢复或会话初始化时，不会再由地图组件直接额外请求 `/records`
    - authenticated records 恢复只以 `auth-session` bootstrap 快照为真源
    - 已登录点亮地点可成功写入；若 records 路径仍失败，界面会显示明确错误反馈
  </behavior>
  <action>
1. 调整 `LeafletMapStage.vue` / `map-points.ts`：
   - 移除或显式鉴权保护 `bootstrapFromApi()` 的自动调用，避免和 `auth-session.restoreSession()` 的 bootstrap 快照重复。
   - 让地图初始化只依赖当前 session store 中已经建立好的 records 边界。
2. 修复 live `/records` 链路：
   - 若 Task 1 证明 server current-user route 仍会在真实运行态返回 500，则在 controller/service/repository 中修掉具体触发点。
   - 保持 anonymous `/records` 语义为 401，不回退成 500。
3. 在前端点亮失败路径中，对非 401 的 records 写入错误设置明确 warning notice，避免用户只看到“无法点亮”但没有反馈。
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts src/stores/map-points.spec.ts src/App.spec.ts && pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - 匿名或 restore 流程不会再额外请求 `/records`
    - `/records` 读写在已登录真实路径下不再返回 500
    - 点亮失败时用户能看到明确错误反馈
    - web 定向测试与 typecheck 通过
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts src/stores/map-points.spec.ts src/App.spec.ts`
- `pnpm --filter @trip-map/web typecheck`
- `pnpm --filter @trip-map/server test -- test/records-travel.e2e-spec.ts` (if server route changed and env allows)
</verification>

<output>
After completion, create `.planning/phases/23-auth-ownership-foundation/23-10-SUMMARY.md`
</output>

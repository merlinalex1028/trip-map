---
phase: 20-kawaii
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/App.vue
  - apps/web/src/App.kawaii.spec.ts
autonomous: true
requirements:
  - STYLE-05
must_haves:
  truths:
    - "顶部栏继续保持 `h-14 md:h-16` 的薄壳高度，首页首屏仍以地图为主。"
    - "notice 与 map shell 呈现宽松留白和 pill / cloud 语言，但不把 transform 动效施加到地图舞台。"
    - "App 壳层继续只做布局与文本 notice 渲染，不引入 `v-html` 或 popup/card 业务逻辑。"
  artifacts:
    - path: "apps/web/src/App.vue"
      provides: "Thin-shell topbar, pill notice, roomy map shell, and text-only notice rendering"
    - path: "apps/web/src/App.kawaii.spec.ts"
      provides: "Shell spacing, topbar height, and non-transform contract coverage"
  key_links:
    - from: "apps/web/src/App.vue"
      to: "apps/web/src/components/LeafletMapStage.vue"
      via: "direct child render with stable map host classes"
      pattern: "LeafletMapStage class=\"min-h-0 flex-1\""
    - from: "apps/web/src/App.kawaii.spec.ts"
      to: "apps/web/src/App.vue"
      via: "mount + data-region / data-kawaii-shell contract assertions"
      pattern: "data-region=\"topbar\"|data-region=\"map-shell\"|data-kawaii-shell=\"thin\""
---

<objective>
为 `App.vue` 建立 Phase 20 的壳层合同：继续保持 thin-shell topbar，不增加头部高度，同时把 notice 和 map shell 收口到更宽松的 kawaii spacing 语言。

Purpose: 落实 D-01、D-02、D-13、D-15、D-16 和 `STYLE-05`，并给后续 popup / card 计划提供稳定的外层边界。
Output: 一个新的 `App.kawaii.spec.ts` 合同测试，以及更新后的 `App.vue` thin-shell 布局与 notice/map shell 类名。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-kawaii/20-CONTEXT.md
@.planning/phases/20-kawaii/20-RESEARCH.md
@.planning/phases/20-kawaii/20-UI-SPEC.md
@.planning/phases/20-kawaii/20-VALIDATION.md
@.planning/quick/260408-nw1-kawaii/260408-nw1-SUMMARY.md
@apps/web/src/App.vue
@apps/web/src/App.spec.ts
@apps/web/src/components/LeafletMapStage.vue
@apps/web/src/stores/map-ui.ts

<interfaces>
From `apps/web/src/stores/map-ui.ts`:
```ts
interface MapInteractionNotice {
  tone: 'info' | 'warning'
  message: string
}
```

From `apps/web/src/App.vue`:
```ts
const { interactionNotice } = storeToRefs(mapUiStore)
```

From `apps/web/src/components/LeafletMapStage.vue`:
```vue
<section
  class="leaflet-map-stage"
  data-region="map-stage"
>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 用 Wave 0 合同测试锁住 App 薄壳 spacing 与无 transform 护栏</name>
  <read_first>
.planning/phases/20-kawaii/20-CONTEXT.md
.planning/phases/20-kawaii/20-RESEARCH.md
.planning/phases/20-kawaii/20-UI-SPEC.md
.planning/phases/20-kawaii/20-VALIDATION.md
.planning/quick/260408-nw1-kawaii/260408-nw1-SUMMARY.md
apps/web/src/App.vue
apps/web/src/App.spec.ts
apps/web/src/components/LeafletMapStage.vue
apps/web/src/stores/map-ui.ts
  </read_first>
  <files>apps/web/src/App.vue, apps/web/src/App.kawaii.spec.ts</files>
  <behavior>
    - Test 1: `App.kawaii.spec.ts` 断言 topbar 保持 `h-14 md:h-16`，并且 header 带 `data-kawaii-shell="thin"`。
    - Test 2: `App.kawaii.spec.ts` 断言 notice 节点带 `data-kawaii-notice="pill"`，类名包含 `rounded-full border border-white/80 bg-white/82 px-4 py-3`。
    - Test 3: `App.kawaii.spec.ts` 断言 map shell 类名包含 `rounded-[32px]`、`gap-4`、`p-4 md:p-6`，并且 `[data-region="map-stage"]` 的宿主类名不包含 `scale`、`translate`、`rotate`、`skew`、`perspective`、`filter`。
    - Test 4: `App.vue` 继续使用 `{{ interactionNotice.message }}` 文本插值，不使用 `v-html`。
  </behavior>
  <action>先新建 `apps/web/src/App.kawaii.spec.ts`，复用 `App.spec.ts` 当前的 Pinia / Leaflet mock 方式，写出 Phase 20 的壳层合同：header 必须保留 `h-14 md:h-16`、`data-kawaii-shell="thin"`；主内容区保留 `gap-4 px-4 pb-4 pt-[4.5rem] md:px-8 md:pb-8 md:pt-[5rem]`；notice 必须带 `data-kawaii-notice="pill"` 和 `rounded-full border border-white/80 bg-white/82 px-4 py-3 text-sm shadow-[var(--shadow-float)]`；map shell 必须带 `rounded-[32px] border border-white/80 bg-white/65 p-4 md:p-6`。然后更新 `apps/web/src/App.vue`：保留现有 `watch`、`noticeTimer`、`clearInteractionNotice()` 逻辑与 `LeafletMapStage` 直连，不新增任何 `MapContextPopup` / `PointSummaryCard` 导入；将 header 补上 `data-kawaii-shell=\"thin\"`，将 notice 补上 `data-kawaii-notice=\"pill\"`，把 map shell padding 从 `p-3 md:p-[18px]` 提升到 `p-4 md:p-6` 以满足 D-13 与 `STYLE-05`，并保持 `h-14 md:h-16` 不变。保留 `{{ interactionNotice.message }}` 文本插值，禁止引入 `v-html`；禁止给 `LeafletMapStage` 或 map shell 增加任何 `scale-*`、`translate-*`、`rotate-*`、`skew-*`、`perspective-*`、`filter` 类，以落实 D-15。</action>
  <acceptance_criteria>
    - `apps/web/src/App.vue` 包含精确字符串 `data-kawaii-shell="thin"`。
    - `apps/web/src/App.vue` 包含精确字符串 `h-14 md:h-16`。
    - `apps/web/src/App.vue` 包含精确字符串 `data-kawaii-notice="pill"`。
    - `apps/web/src/App.vue` 包含精确字符串 `rounded-full border border-white/80 bg-white/82 px-4 py-3`。
    - `apps/web/src/App.vue` 包含精确字符串 `rounded-[32px] border border-white/80 bg-white/65 p-4 md:p-6`。
    - `apps/web/src/App.vue` 包含精确字符串 `{{ interactionNotice.message }}`。
    - `rg -n "v-html" apps/web/src/App.vue` 返回空结果。
    - `rg -n "LeafletMapStage class=.*(scale|translate|rotate|skew|perspective|filter)" apps/web/src/App.vue` 返回空结果。
    - `apps/web/src/App.kawaii.spec.ts` 包含精确字符串 `describe('App kawaii shell contracts'`。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/App.kawaii.spec.ts src/App.spec.ts && pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <done>App 壳层已通过新合同测试锁住：顶部栏高度不增长、notice 为 pill、map shell 有更宽松留白，且没有把 transform 动效扩散到地图舞台。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| map-ui store notice → `App.vue` notice node | 不受信 notice 文案进入固定浮层，需要保持纯文本渲染 |
| app shell classes → Leaflet map host | 壳层类名若把 transform/filter 施加到地图宿主，会破坏命中和锚点稳定性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | T | `apps/web/src/App.vue` notice text | mitigate | 继续使用 `{{ interactionNotice.message }}` 文本插值，不引入 `v-html`；`App.kawaii.spec.ts` 与 `rg` 合同一起锁住该约束。 |
| T-20-02 | T | `apps/web/src/App.vue` / `LeafletMapStage` host | mitigate | 明确禁止在 `LeafletMapStage` 宿主和 map shell 上新增 `scale-*`、`translate-*`、`rotate-*`、`skew-*`、`perspective-*`、`filter` 类；测试直接断言为空。 |
</threat_model>

<verification>
运行 `pnpm --filter @trip-map/web test -- src/App.kawaii.spec.ts src/App.spec.ts && pnpm --filter @trip-map/web typecheck`，确认 App 壳层合同与现有 smoke test 同时通过。
</verification>

<success_criteria>
- `App.vue` 仍然是 thin-shell topbar，不增加高度。
- `App.kawaii.spec.ts` 存在并锁住 notice、spacing 和无 transform 地图护栏。
- `STYLE-05` 在主壳层完成，不把 popup/card 工作偷渡到本计划。
</success_criteria>

<output>
After completion, create `.planning/phases/20-kawaii/20-01-SUMMARY.md`
</output>

---
phase: 20-kawaii
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/map-popup/MapContextPopup.vue
  - apps/web/src/components/map-popup/MapContextPopup.kawaii.spec.ts
autonomous: true
requirements:
  - STYLE-04
must_haves:
  truths:
    - "地图 popup 外层保持轻壳定位感，不再承担厚白边和重云朵投影。"
    - "popup 箭头与装饰层不会拦截点击，外壳自身也不会通过 hover transform 造成锚点漂移。"
    - "inner cloud card 仍由 `PointSummaryCard.vue` 承接，popup 外层只保留轻边框、轻阴影和箭头。"
  artifacts:
    - path: "apps/web/src/components/map-popup/MapContextPopup.vue"
      provides: "Light popup shell, pointer-safe arrow, and stable anchor container"
    - path: "apps/web/src/components/map-popup/MapContextPopup.kawaii.spec.ts"
      provides: "Outer-shell visual hierarchy and pointer-event safety coverage"
  key_links:
    - from: "apps/web/src/components/map-popup/MapContextPopup.vue"
      to: "apps/web/src/components/map-popup/PointSummaryCard.vue"
      via: "outer light shell wrapping inner cloud card"
      pattern: "data-kawaii-shell=\"light\"|<PointSummaryCard"
    - from: "apps/web/src/components/map-popup/MapContextPopup.kawaii.spec.ts"
      to: "apps/web/src/components/map-popup/MapContextPopup.vue"
      via: "mount + shell attribute assertions"
      pattern: "data-kawaii-shell=\"light\"|data-kawaii-arrow=\"light\""
---

<objective>
把 `MapContextPopup.vue` 收口为“外轻内重”的轻量 anchored shell，避免厚白边和重投影继续压在 popup 外层。

Purpose: 精确实现 D-05、D-06 和 `STYLE-04`，同时在 popup 外壳层落实 pointer-events 与 anchor-stability 安全护栏。
Output: 一个新的 `MapContextPopup.kawaii.spec.ts`，以及更轻的 popup shell / arrow 样式与属性合同。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-kawaii/20-CONTEXT.md
@.planning/phases/20-kawaii/20-RESEARCH.md
@.planning/phases/20-kawaii/20-UI-SPEC.md
@.planning/phases/20-kawaii/20-VALIDATION.md
@apps/web/src/components/map-popup/MapContextPopup.vue
@apps/web/src/components/map-popup/MapContextPopup.spec.ts
@apps/web/src/components/map-popup/PointSummaryCard.vue
@apps/web/src/types/map-point.ts

<interfaces>
From `apps/web/src/types/map-point.ts`:
```ts
export type SummarySurfaceState =
  | {
      mode: 'candidate-select'
      fallbackPoint: DraftMapPoint
      cityCandidates: GeoCityCandidate[]
      canonicalCandidates: CanonicalPlaceCandidate[]
      recommendedPlaceId: string | null
    }
  | {
      mode: 'detected-preview' | 'view'
      point: MapPointDisplay
      boundarySupportState: 'supported' | 'missing' | 'not-applicable'
    }
```

From `apps/web/src/components/map-popup/MapContextPopup.vue`:
```ts
const emit = defineEmits<{
  confirmCandidate: [candidate: GeoCityCandidate]
  continueFallback: []
  illuminate: []
  unilluminate: []
}>()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 用 Wave 0 popup 合同测试锁住轻壳 outer shell 与 pointer-safe 箭头</name>
  <read_first>
.planning/phases/20-kawaii/20-CONTEXT.md
.planning/phases/20-kawaii/20-RESEARCH.md
.planning/phases/20-kawaii/20-UI-SPEC.md
.planning/phases/20-kawaii/20-VALIDATION.md
apps/web/src/components/map-popup/MapContextPopup.vue
apps/web/src/components/map-popup/MapContextPopup.spec.ts
apps/web/src/components/map-popup/PointSummaryCard.vue
apps/web/src/types/map-point.ts
  </read_first>
  <files>apps/web/src/components/map-popup/MapContextPopup.vue, apps/web/src/components/map-popup/MapContextPopup.kawaii.spec.ts</files>
  <behavior>
    - Test 1: `MapContextPopup.kawaii.spec.ts` 断言 aside 根节点带 `data-kawaii-shell="light"`，并且类名包含 `border border-white/70 bg-white/75 p-1 shadow-[0_16px_34px_rgba(155,116,160,0.12)]`。
    - Test 2: `MapContextPopup.kawaii.spec.ts` 断言 arrow 节点带 `data-kawaii-arrow="light"` 与 `pointer-events-none`，outer shell 不包含 `border-4`、`hover:scale`、`active:scale`。
    - Test 3: `MapContextPopup.kawaii.spec.ts` 断言 body 节点保留内层容器职责，并仍然直接渲染 `PointSummaryCard`。
    - Test 4: popup 标题继续通过文本插值提供 `aria-labelledby`，不引入 `v-html`。
  </behavior>
  <action>先新建 `apps/web/src/components/map-popup/MapContextPopup.kawaii.spec.ts`，复用现有 `MapContextPopup.spec.ts` 的 mount 方式，补充 Phase 20 合同：outer shell 必须带 `data-kawaii-shell=\"light\"`，并使用精确类串 `rounded-[32px] border border-white/70 bg-white/75 p-1 shadow-[0_16px_34px_rgba(155,116,160,0.12)] backdrop-blur-xl`；body 必须使用 `rounded-[28px] overflow-hidden`；arrow 必须带 `data-kawaii-arrow=\"light\"` 与 `pointer-events-none`。然后更新 `apps/web/src/components/map-popup/MapContextPopup.vue`：保留现有 `focusEntryPoint()`、`aria-labelledby`、`defineExpose({ getPopupElement })` 和 `PointSummaryCard` 装配；把 outer shell 退成轻壳，不再使用厚描边或重投影，不新增任何 hover/active scale 到 `<aside>`；arrow 只保留定位所需的旋转，不能承接点击；禁止在该文件引入 `border-4`、`hover:scale-*`、`active:scale-*` 或 `v-html`。这一任务实现 D-05、D-06，并为 D-07 留出内层卡片空间。</action>
  <acceptance_criteria>
    - `apps/web/src/components/map-popup/MapContextPopup.vue` 包含精确字符串 `data-kawaii-shell="light"`。
    - `apps/web/src/components/map-popup/MapContextPopup.vue` 包含精确字符串 `rounded-[32px] border border-white/70 bg-white/75 p-1`。
    - `apps/web/src/components/map-popup/MapContextPopup.vue` 包含精确字符串 `shadow-[0_16px_34px_rgba(155,116,160,0.12)]`。
    - `apps/web/src/components/map-popup/MapContextPopup.vue` 包含精确字符串 `data-kawaii-arrow="light"`。
    - `apps/web/src/components/map-popup/MapContextPopup.vue` 包含精确字符串 `pointer-events-none`。
    - `rg -n "border-4|hover:scale|active:scale|v-html" apps/web/src/components/map-popup/MapContextPopup.vue` 返回空结果。
    - `apps/web/src/components/map-popup/MapContextPopup.kawaii.spec.ts` 包含精确字符串 `describe('MapContextPopup kawaii shell contracts'`。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/components/map-popup/MapContextPopup.kawaii.spec.ts src/components/map-popup/MapContextPopup.spec.ts && pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <done>`MapContextPopup.vue` 已退回轻壳定位层：外层没有厚 chrome、箭头不拦截交互、outer shell 不带 hover transform，内层 cloud card 仍交给 `PointSummaryCard.vue`。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anchored popup shell → Leaflet popup anchor geometry | 外壳若引入 hover transform，会直接改变视觉锚点与命中预期 |
| decorative arrow layer → popup interaction surface | 箭头或装饰层若截获指针事件，会阻断内层按钮点击 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | D | `apps/web/src/components/map-popup/MapContextPopup.vue` arrow/decorative layer | mitigate | 强制 arrow 带 `pointer-events-none`，并通过 `MapContextPopup.kawaii.spec.ts` 锁住 `data-kawaii-arrow="light"` 与该类名。 |
| T-20-04 | T | `apps/web/src/components/map-popup/MapContextPopup.vue` outer shell | mitigate | aside 根节点不允许出现 `border-4`、`hover:scale-*`、`active:scale-*`；只保留轻边框和轻阴影，避免 popup anchor drift。 |
</threat_model>

<verification>
运行 `pnpm --filter @trip-map/web test -- src/components/map-popup/MapContextPopup.kawaii.spec.ts src/components/map-popup/MapContextPopup.spec.ts && pnpm --filter @trip-map/web typecheck`，确认 popup 外壳合同与既有焦点行为都通过。
</verification>

<success_criteria>
- popup 外层只保留轻壳 chrome，不再承担 cloud card 主视觉。
- 箭头与装饰层不会拦截点击。
- `STYLE-04` 的外层职责边界已锁定，为内层 `PointSummaryCard.vue` 留出厚白边与主阴影空间。
</success_criteria>

<output>
After completion, create `.planning/phases/20-kawaii/20-02-SUMMARY.md`
</output>

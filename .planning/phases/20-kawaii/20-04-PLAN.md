---
phase: 20-kawaii
plan: 04
type: execute
wave: 2
depends_on:
  - 20-01
  - 20-02
  - 20-03
files_modified:
  - .planning/phases/20-kawaii/20-VALIDATION.md
autonomous: false
requirements:
  - STYLE-03
  - STYLE-04
  - STYLE-05
  - INTER-01
  - INTER-02
  - INTER-03
must_haves:
  truths:
    - "Phase 20 的 Wave 0 kawaii 合同测试已真实存在并全部可运行，`20-VALIDATION.md` 不再保留 `❌ W0` 占位。"
    - "自动化门禁已经覆盖 App shell、popup outer shell、inner cloud card 与既有回归测试。"
    - "用户已在真实浏览器里确认 hover / active 体感、popup 锚定稳定性和薄壳 topbar 首屏高度没有被改坏。"
  artifacts:
    - path: ".planning/phases/20-kawaii/20-VALIDATION.md"
      provides: "Updated Nyquist map, wave_0_complete evidence, and final pass/fail record"
  key_links:
    - from: ".planning/phases/20-kawaii/20-VALIDATION.md"
      to: "apps/web/src/App.kawaii.spec.ts"
      via: "verification map"
      pattern: "App.kawaii.spec.ts"
    - from: ".planning/phases/20-kawaii/20-VALIDATION.md"
      to: "apps/web/src/components/map-popup/MapContextPopup.kawaii.spec.ts"
      via: "verification map"
      pattern: "MapContextPopup.kawaii.spec.ts"
    - from: ".planning/phases/20-kawaii/20-VALIDATION.md"
      to: "apps/web/src/components/map-popup/PointSummaryCard.kawaii.spec.ts"
      via: "verification map"
      pattern: "PointSummaryCard.kawaii.spec.ts"
---

<objective>
完成 Phase 20 的验证闭环：跑通自动化门禁、回填 `20-VALIDATION.md` 的 Wave 0 / Nyquist 状态，并执行一次真实浏览器人工验收。

Purpose: 把 Phase 20 从“计划中的 kawaii 收口”变成有自动化证据和人眼确认的可执行交付，不让验证停留在占位文档。
Output: 更新后的 `20-VALIDATION.md`，以及明确的自动化与人工验收结论。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-kawaii/20-CONTEXT.md
@.planning/phases/20-kawaii/20-UI-SPEC.md
@.planning/phases/20-kawaii/20-VALIDATION.md
@apps/web/src/App.kawaii.spec.ts
@apps/web/src/App.spec.ts
@apps/web/src/components/map-popup/MapContextPopup.kawaii.spec.ts
@apps/web/src/components/map-popup/MapContextPopup.spec.ts
@apps/web/src/components/map-popup/PointSummaryCard.kawaii.spec.ts
@apps/web/src/components/map-popup/PointSummaryCard.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 跑完 Phase 20 自动化门禁并回填 Validation 文档</name>
  <read_first>
.planning/phases/20-kawaii/20-VALIDATION.md
.planning/phases/20-kawaii/20-UI-SPEC.md
apps/web/src/App.kawaii.spec.ts
apps/web/src/App.spec.ts
apps/web/src/components/map-popup/MapContextPopup.kawaii.spec.ts
apps/web/src/components/map-popup/MapContextPopup.spec.ts
apps/web/src/components/map-popup/PointSummaryCard.kawaii.spec.ts
apps/web/src/components/map-popup/PointSummaryCard.spec.ts
  </read_first>
  <files>.planning/phases/20-kawaii/20-VALIDATION.md</files>
  <action>严格按 Phase 20 新合同完成自动化门禁：先跑 `pnpm --filter @trip-map/web test -- src/App.kawaii.spec.ts src/App.spec.ts src/components/map-popup/MapContextPopup.kawaii.spec.ts src/components/map-popup/MapContextPopup.spec.ts src/components/map-popup/PointSummaryCard.kawaii.spec.ts src/components/map-popup/PointSummaryCard.spec.ts`，再跑 `pnpm --filter @trip-map/web typecheck`。两条命令都成功后，不是只把状态标记翻绿，而是要端到端重写 `20-VALIDATION.md` 的 Validation Strategy 映射，使其和 `20-01`、`20-02`、`20-03` 的真实计划完全一致：`Quick run command` 必须改成覆盖三个 kawaii 合同测试文件的快速门禁；`Sampling Rate` 必须按 plan 重写为 `After every 20-01 task commit`、`After every 20-02 task commit`、`After every 20-03 task commit` 的真实命令，而不是沿用单一旧命令；`Per-Task Verification Map` 必须改成只保留真实 task 行 `20-01-01`、`20-02-01`、`20-03-01`、`20-03-02`，并让 `Plan / Requirement / Threat Ref / Automated Command` 四列分别精确映射到 `20-01` 的 `STYLE-05` 与 `T-20-01,T-20-02`、`20-02` 的 `STYLE-04` 与 `T-20-03,T-20-04`、`20-03 Task 1` 的 `STYLE-03, STYLE-04, STYLE-05` 与 `T-20-05,T-20-06`、`20-03 Task 2` 的 `INTER-01, INTER-02, INTER-03` 与 `T-20-07`，对应命令分别回填 App、MapContextPopup、PointSummaryCard 的真实 spec/typecheck 组合。随后再把 frontmatter 中 `wave_0_complete` 改成 `true`，把 file exists / status 从 `❌ W0`、`⬜ pending` 改成绿色完成状态，把 Wave 0 Requirements 三项改成已完成；不得保留旧的错位 task/requirement/threat 映射。此时先不要把 `nyquist_compliant` 或 `status` 改为最终通过，等人工验收任务完成后再最终收口。</action>
  <acceptance_criteria>
    - `pnpm --filter @trip-map/web test -- src/App.kawaii.spec.ts src/App.spec.ts src/components/map-popup/MapContextPopup.kawaii.spec.ts src/components/map-popup/MapContextPopup.spec.ts src/components/map-popup/PointSummaryCard.kawaii.spec.ts src/components/map-popup/PointSummaryCard.spec.ts` 退出码为 0。
    - `pnpm --filter @trip-map/web typecheck` 退出码为 0。
    - `.planning/phases/20-kawaii/20-VALIDATION.md` 包含精确字符串 `wave_0_complete: true`。
    - `.planning/phases/20-kawaii/20-VALIDATION.md` 的 `Quick run command` 已改为同时覆盖 `App.kawaii.spec.ts`、`MapContextPopup.kawaii.spec.ts`、`PointSummaryCard.kawaii.spec.ts` 的真实快速门禁。
    - `.planning/phases/20-kawaii/20-VALIDATION.md` 的 `Sampling Rate` 已显式包含 `After every 20-01 task commit`、`After every 20-02 task commit`、`After every 20-03 task commit` 三条真实命令。
    - `.planning/phases/20-kawaii/20-VALIDATION.md` 的 `Per-Task Verification Map` 只保留 `20-01-01`、`20-02-01`、`20-03-01`、`20-03-02` 四条真实 task 映射，且 `Plan / Requirement / Threat Ref / Automated Command` 四列与 `20-01` / `20-02` / `20-03` 的计划内容逐项一致。
    - `.planning/phases/20-kawaii/20-VALIDATION.md` 包含 `App.kawaii.spec.ts`、`MapContextPopup.kawaii.spec.ts`、`PointSummaryCard.kawaii.spec.ts`。
    - `rg -n "20-01-02|20-01-03|20-02-02|20-02-03" .planning/phases/20-kawaii/20-VALIDATION.md` 返回空结果。
    - `rg -n "❌ W0|⬜ pending" .planning/phases/20-kawaii/20-VALIDATION.md` 返回空结果。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/App.kawaii.spec.ts src/App.spec.ts src/components/map-popup/MapContextPopup.kawaii.spec.ts src/components/map-popup/MapContextPopup.spec.ts src/components/map-popup/PointSummaryCard.kawaii.spec.ts src/components/map-popup/PointSummaryCard.spec.ts && pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <done>Phase 20 的自动化证据已齐：Wave 0 合同测试真实存在、全部通过，且 `20-VALIDATION.md` 已按 `20-01` / `20-02` / `20-03` 的真实 plan/task/requirement/threat/command 关系重写完成，不再留有失真的占位映射。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 浏览器确认 hover / active 体感、popup 锚定与 thin-shell topbar 没被改坏</name>
  <read_first>
.planning/phases/20-kawaii/20-UI-SPEC.md
.planning/phases/20-kawaii/20-VALIDATION.md
apps/web/src/App.vue
apps/web/src/components/map-popup/MapContextPopup.vue
apps/web/src/components/map-popup/PointSummaryCard.vue
apps/web/src/components/LeafletMapStage.vue
  </read_first>
  <files>.planning/phases/20-kawaii/20-VALIDATION.md</files>
  <action>在 Task 1 全绿后，启动 `pnpm --filter @trip-map/web dev --host 127.0.0.1 --port 4173`，引导用户只检查 Phase 20 主路径：topbar 仍为 `h-14 md:h-16` 的 thin-shell；打开地图 popup 后，outer shell 保持轻壳、inner card 拥有厚白边与更强 cloud 感；hover 主 CTA、候选确认卡和 inner card 时能看到 `scale-105` + `-translate-y-1` 的轻 lift，但 popup 锚点不漂移；点击主 CTA 时有 `scale-95` 下压；`LeafletMapStage` 本体没有被 hover/active motion 误伤。用户回复后立即更新 `20-VALIDATION.md`：若通过，则把 frontmatter 的 `nyquist_compliant` 改为 `true`、`status` 改为 `approved`，并在 Manual-Only Verifications 或 Validation Sign-Off 处写入当日通过记录；若失败，则保留 `status: draft`，逐条记录失败现象、涉及表面和复现步骤，不能写模糊结论。</action>
  <what-built>Phase 20 的 shell、popup outer shell、inner cloud card 和 CTA / badge 微交互已全部由自动化合同与类型检查锁住。</what-built>
  <how-to-verify>
1. 启动 `pnpm --filter @trip-map/web dev --host 127.0.0.1 --port 4173`。
2. 打开 `http://127.0.0.1:4173/`。
3. 先确认顶部栏仍然是轻量高度：header 高度不超过 `h-14 md:h-16`，地图仍占首屏主体。
4. 点击地图打开 popup，确认 outer shell 只是轻壳，主 cloud 感来自内层 `PointSummaryCard` 的厚白边和大圆角。
5. 依次 hover primary CTA、候选确认卡和 inner cloud card，确认有轻 lift 和柔和阴影增强，但 popup 锚点不漂移。
6. 点击 primary CTA，确认有轻微下压感；松开后回弹，不出现卡顿或跳跃。
7. 观察地图本体与 pending / boundary 命中区，确认没有随 popup/按钮一起缩放或平移。
8. 全部通过回复 `approved`；如失败，逐条描述现象、位置和复现步骤。
  </how-to-verify>
  <acceptance_criteria>
    - 用户明确回复 `approved`，或逐条提供失败现象。
    - 通过时，`.planning/phases/20-kawaii/20-VALIDATION.md` 包含精确字符串 `nyquist_compliant: true` 与 `status: approved`。
    - 失败时，`.planning/phases/20-kawaii/20-VALIDATION.md` 保留 `status: draft`，并新增具体失败记录，不含“看起来差不多”之类模糊表述。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/App.kawaii.spec.ts src/App.spec.ts src/components/map-popup/MapContextPopup.kawaii.spec.ts src/components/map-popup/MapContextPopup.spec.ts src/components/map-popup/PointSummaryCard.kawaii.spec.ts src/components/map-popup/PointSummaryCard.spec.ts && pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <done>Phase 20 的自动化与人工证据都已齐全；只有当用户明确 `approved` 且 `20-VALIDATION.md` 更新为最终通过状态时，本 phase 才算验证完成。</done>
  <resume-signal>回复 `approved`，或逐条描述失败现象。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execution evidence → validation artifact | 若验证文档不回填真实结果，后续 phase audit 会基于错误状态继续推进 |
| manual browser check → shipped UI confidence | 仅靠类名合同无法证明真实锚点和体感，需要人工确认 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | R | `.planning/phases/20-kawaii/20-VALIDATION.md` | mitigate | 自动化通过后必须把 `wave_0_complete`、task map、spec 路径和状态写回文档，禁止保留 `❌ W0` / `⬜ pending` 占位。 |
| T-20-09 | D | Real browser popup / topbar verification | mitigate | 在最终通过前执行一次人工浏览器检查，明确验证 popup 锚点稳定、topbar 不增高、地图命中区不受 transform 误伤。 |
</threat_model>

<verification>
自动化门禁为 `pnpm --filter @trip-map/web test -- src/App.kawaii.spec.ts src/App.spec.ts src/components/map-popup/MapContextPopup.kawaii.spec.ts src/components/map-popup/MapContextPopup.spec.ts src/components/map-popup/PointSummaryCard.kawaii.spec.ts src/components/map-popup/PointSummaryCard.spec.ts && pnpm --filter @trip-map/web typecheck`，随后执行一次真实浏览器人工验收并回填 `20-VALIDATION.md`。
</verification>

<success_criteria>
- `20-VALIDATION.md` 不再保留 Wave 0 占位，且实际指向新建的 kawaii 合同测试文件。
- 自动化门禁和类型检查全绿。
- 用户对 popup / CTA / topbar / map-anchor 体验给出明确通过或明确失败记录。
</success_criteria>

<output>
After completion, create `.planning/phases/20-kawaii/20-04-SUMMARY.md`
</output>

---
phase: 18-Tech-Debt-清理
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - 18-04

must_haves:
  truths:
    - "Geo-lookup chunk warning has been evaluated and documented"
    - "Decision made: optimize now or defer to next milestone"
  artifacts: []
  key_links: []
---

<objective>
Evaluate the geo-lookup chunk warning from Phase 7 build output and decide whether to optimize tree-shaking now or defer to the next milestone.

Purpose: Phase 7 (geo 数据管线) introduced a build chunk warning related to geo-lookup code. This plan evaluates the severity and decides on action — either fix tree-shaking or document deferral.
Output: Decision documented in SUMMARY.md, code changes if optimizing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze geo-lookup chunk warning</name>
  <files></files>
  <read_first>apps/web/vite.config.ts</read_first>
  <action>
    1. Run `pnpm --filter @trip-map/web build` and capture the output. Look for chunk size warnings related to geo-lookup.
    2. Identify which modules are causing the large chunk. Check if geo-lookup imports are pulling in unnecessary code.
    3. Assess severity: is this a real performance concern (>500KB chunk) or a minor warning?
    4. If fixable with simple tree-shaking (dynamic import, code splitting), implement the fix.
    5. If complex or low-impact, document the decision to defer and why.
  </action>
  <verify>pnpm --filter @trip-map/web build completes without critical errors</verify>
  <acceptance_criteria>
    - Build output analyzed for chunk warnings
    - Decision documented: fix or defer with rationale
    - If fixing: chunk size reduced; if deferring: reasoning captured in SUMMARY
  </acceptance_criteria>
  <done>Geo-lookup chunk warning evaluated and action decided</done>
</task>

</tasks>

<verification>
- [ ] pnpm --filter @trip-map/web build succeeds
- [ ] Decision documented in SUMMARY.md
</verification>

<success_criteria>
- All tasks completed
- Build warning evaluated with clear decision
</success_criteria>

<output>
After completion, create `.planning/phases/18-Tech-Debt-清理/18-04-SUMMARY.md`
</output>

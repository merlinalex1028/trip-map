---
phase: 18-Tech-Debt-清理
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/health/health.controller.ts
  - apps/server/src/health/health.module.ts
autonomous: true
requirements:
  - 18-01

must_haves:
  truths:
    - "/health endpoint returns real database connectivity status (up/down)"
    - "Degraded status returned when DB query fails"
  artifacts:
    - path: "apps/server/src/health/health.controller.ts"
      provides: "Health endpoint with real DB probe via Prisma $queryRaw"
      contains: "queryRaw"
    - path: "apps/server/src/health/health.module.ts"
      provides: "Health module importing PrismaModule for DB access"
      contains: "PrismaModule"
  key_links:
    - from: "apps/server/src/health/health.controller.ts"
      to: "PrismaService"
      via: "constructor injection"
      pattern: "PrismaService"
---

<objective>
Replace the static `database: 'down'` in `/health` endpoint with a real DB connectivity probe using Prisma `db.$queryRaw`.

Purpose: The health endpoint currently always reports `database: 'down'` — it should actually probe the database and return `up` or `down` based on real connectivity. This is critical for monitoring and deployment readiness checks.
Output: Updated health controller with real DB probe, degraded status on failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@apps/server/src/health/health.controller.ts
@apps/server/src/health/health.module.ts
@apps/server/src/prisma/prisma.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PrismaService injection to HealthController</name>
  <files>apps/server/src/health/health.controller.ts, apps/server/src/health/health.module.ts</files>
  <read_first>apps/server/src/health/health.controller.ts, apps/server/src/health/health.module.ts, apps/server/src/prisma/prisma.service.ts</read_first>
  <action>
    1. In `health.controller.ts`: Import `PrismaService` from `../prisma/prisma.service`. Add constructor that injects `PrismaService`.
    2. Change `getStatus()` to `async`. Use `await this.prisma.$queryRaw\`SELECT 1\`` to probe DB.
       - On success: return `database: 'up'`
       - On catch: return `database: 'down'` (keep status 'ok' for the service itself, or 'degraded' if DB is down)
    3. In `health.module.ts`: Import `PrismaModule` from `../prisma/prisma.module` and add to `imports` array so `PrismaService` is available for injection.
  </action>
  <verify>pnpm --filter @trip-map/server test</verify>
  <acceptance_criteria>
    - health.controller.ts imports and injects PrismaService
    - health.controller.ts uses $queryRaw to probe database
    - health.module.ts imports PrismaModule
    - getStatus() returns database: 'up' when DB is reachable, 'down' when it fails
  </acceptance_criteria>
  <done>Health endpoint probes real database connectivity</done>
</task>

</tasks>

<verification>
- [ ] pnpm --filter @trip-map/server test passes
- [ ] pnpm typecheck passes
- [ ] Health endpoint returns `database: 'up'` when DB is running
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- /health endpoint returns real DB status
</success_criteria>

<output>
After completion, create `.planning/phases/18-Tech-Debt-清理/18-01-SUMMARY.md`
</output>

# Phase 18: Tech Debt 清理

---
phase: 18
status: planned
goal: 清理 v3.0 遗留 tech debt，包括将 /health 端点改为真实 DB probe、补全 Phase 14/15 Nyquist 合规文档，以及 resolve Phase 7 构建 chunk 警告
depends_on: 17
gap_closure:
  - ARC-03 (静态 health probe → 真实 DB probe)
  - Phase 14 Nyquist 合规（VALIDATION.md draft → approved）
  - Phase 15 Nyquist 合规（VALIDATION.md draft → approved）
  - Phase 7 构建优化（chunk 警告 → 可接受水平）
---

## Gap Source

v3.0 Milestone Audit (`v3.0-MILESTONE-AUDIT.md`) identified:

### Tech Debt Items

**Phase 11 — Monorepo 与契约基线**
- `/health` 端点的 `database` 字段返回静态字符串 `"ok"`，非真实 DB probe，无法真正验证 PostgreSQL 连通性
- `apps/web` 生产构建存在大 chunk 警告（自 Phase 7 遗留）

**Phase 14 — Leaflet 地图主链路迁移**
- `14-VALIDATION.md` 状态为 `draft`，`nyquist_compliant: false`

**Phase 15 — 服务端记录与点亮闭环**
- `15-VALIDATION.md` 状态为 `draft`，`nyquist_compliant: false`

**Phase 7 — 城市选择与兼容基线**
- geo-lookup 构建产物较大，存在 chunk size warning

## Plans

- [ ] `18-01-PLAN.md` — 将 `/health` 端点数据库字段改为 Prisma `db.$queryRaw` 真实 probe，对 Prisma 连接失败返回 degraded 状态
- [ ] `18-02-PLAN.md` — 补全 `14-VALIDATION.md`（将 draft 升级为 approved，标记人工验收项为 nyquist_compliant）
- [ ] `18-03-PLAN.md` — 补全 `15-VALIDATION.md`（将 draft 升级为 approved，标记 illuminate human-verify 为 nyquist_compliant）
- [ ] `18-04-PLAN.md` — 评估 geo-lookup chunk 警告，决策是否需要构建时 tree-shaking 优化或 defer 到下个 milestone

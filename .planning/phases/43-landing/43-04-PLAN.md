---
phase: 43-landing
plan: 04
type: execute
wave: 3
depends_on: [43-02, 43-03]
files_modified:
  - apps/web/src/views/MapHomeView.vue
  - apps/web/src/views/TimelinePageView.vue
  - apps/web/src/views/TimelinePageView.spec.ts
  - apps/web/src/views/StatisticsPageView.vue
  - apps/web/src/views/StatisticsPageView.spec.ts
  - apps/web/src/router/index.spec.ts
  - apps/web/src/App.spec.ts
  - apps/web/src/App.kawaii.spec.ts
autonomous: false
requirements: [SHELL-04, AUTH-03]
must_haves:
  truths:
    - "Route-facing app vocabulary uses 世界足迹, 旅途手账, 旅途回忆, and 留下足迹."
    - "Old route-facing `/timeline` and `/statistics` semantics are absent from Phase 43-owned surfaces and tests."
    - "Map, journal, memories, landing, auth, and shell focused specs pass together before the phase is considered ready."
  artifacts:
    - "apps/web/src/views/MapHomeView.vue exposes data-route-view=\"map\" and 世界足迹 semantics without changing Leaflet behavior."
    - "apps/web/src/views/TimelinePageView.vue exposes data-route-view=\"journal\" and 旅途手账 copy."
    - "apps/web/src/views/StatisticsPageView.vue exposes data-route-view=\"memories\" and 旅途回忆 copy."
  key_links:
    - "TimelinePageView.vue RouterLink -> `/map` instead of `/`."
    - "StatisticsPageView.vue RouterLink -> `/map` instead of `/`."
    - "View specs -> new `data-route-view` values and route-facing Chinese vocabulary."
---

<objective>
Complete Phase 43 route-facing copy migration and run the final focused phase gate.

Purpose: The new landing/auth/shell architecture is only coherent if route labels, view hooks, tests, and user-facing copy move together to the v8 vocabulary.
Output: Updated app route views, updated view specs, grep-backed old-copy audit, focused specs, full web test suite, and web build.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-landing/43-CONTEXT.md
@.planning/phases/43-landing/43-RESEARCH.md
@.planning/phases/43-landing/43-PATTERNS.md
@.planning/phases/43-landing/43-UI-SPEC.md
@.planning/phases/43-landing/43-VALIDATION.md
@apps/web/src/views/MapHomeView.vue
@apps/web/src/views/TimelinePageView.vue
@apps/web/src/views/TimelinePageView.spec.ts
@apps/web/src/views/StatisticsPageView.vue
@apps/web/src/views/StatisticsPageView.spec.ts
@apps/web/src/router/index.spec.ts
@apps/web/src/App.spec.ts
@apps/web/src/App.kawaii.spec.ts

<interfaces>
Locked copy replacements from D-22:
```text
点亮 -> 留下足迹
旅行统计 -> 旅途回忆
时间轴 -> 旅途手账
```

Target route-facing hooks:
```text
MapHomeView.vue: data-route-view="map"
TimelinePageView.vue: data-route-view="journal"
StatisticsPageView.vue: data-route-view="memories"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate route-facing view copy and hooks</name>
  <read_first>
    - .planning/phases/43-landing/43-CONTEXT.md
    - .planning/phases/43-landing/43-RESEARCH.md
    - .planning/phases/43-landing/43-PATTERNS.md
    - .planning/phases/43-landing/43-UI-SPEC.md
    - apps/web/src/views/MapHomeView.vue
    - apps/web/src/views/TimelinePageView.vue
    - apps/web/src/views/StatisticsPageView.vue
    - apps/web/src/components/LeafletMapStage.vue
    - apps/web/src/components/timeline/TimelineVisitCard.vue
    - apps/web/src/components/statistics/StatCard.vue
  </read_first>
  <files>apps/web/src/views/MapHomeView.vue, apps/web/src/views/TimelinePageView.vue, apps/web/src/views/StatisticsPageView.vue</files>
  <action>Update Phase 43-owned route-facing surfaces per D-20, D-21, and D-22. In `MapHomeView.vue`, keep `LeafletMapStage` and `AuthRestoreOverlay` behavior intact, add `data-route-view="map"` to the route section, and add `aria-label="世界足迹"` so the route has the new map vocabulary without redesigning popup/date behavior. In `TimelinePageView.vue`, do not rename the file solely for purity; change `data-route-view="timeline"` to `data-route-view="journal"` and `data-region="timeline-shell"` to `data-region="journal-shell"`. Replace visible `时间轴` copy with `旅途手账`. Change the authenticated user chip to `{{ currentUser.username }} 的旅途手账`. Change all RouterLinks that return to the map from `to="/"` to `to="/map"` and use visible copy `返回世界足迹` or `去世界足迹留下足迹`. Empty state must use exact heading `还没有留下足迹` and body `去世界足迹选择真实地点，记录第一段旅途。`. In `StatisticsPageView.vue`, do not rename the file solely for purity; change `data-route-view="statistics"` to `data-route-view="memories"` and `data-region="statistics-shell"` to `data-region="memories-shell"`. Replace visible `旅行统计` copy with `旅途回忆`. Change the authenticated user chip to `{{ currentUser.username }} 的旅途回忆`. Change map return links to `to="/map"` with `返回世界足迹` or `去世界足迹留下足迹`. Error state must show exact copy `旅途数据暂时没有同步成功，请刷新页面或稍后重试。`. Empty state must use exact heading `还没有留下足迹` and body `去世界足迹选择真实地点，记录第一段旅途。`. Do not modify map popup/date dialog internals in this task; those belong to Phase 44.</action>
  <acceptance_criteria>
    - `apps/web/src/views/MapHomeView.vue` contains `data-route-view="map"` and `aria-label="世界足迹"`.
    - `apps/web/src/views/TimelinePageView.vue` contains `data-route-view="journal"`, `data-region="journal-shell"`, `旅途手账`, `返回世界足迹`, `去世界足迹留下足迹`, `还没有留下足迹`, and `去世界足迹选择真实地点，记录第一段旅途。`.
    - `apps/web/src/views/TimelinePageView.vue` contains `to="/map"` and does not contain `to="/"`.
    - `apps/web/src/views/StatisticsPageView.vue` contains `data-route-view="memories"`, `data-region="memories-shell"`, `旅途回忆`, `返回世界足迹`, `去世界足迹留下足迹`, `还没有留下足迹`, and `旅途数据暂时没有同步成功，请刷新页面或稍后重试。`.
    - `apps/web/src/views/StatisticsPageView.vue` contains `to="/map"` and does not contain `to="/"`.
    - `rg -n "时间轴|旅行统计|点亮" apps/web/src/views/MapHomeView.vue apps/web/src/views/TimelinePageView.vue apps/web/src/views/StatisticsPageView.vue` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web exec vitest run src/views/TimelinePageView.spec.ts src/views/StatisticsPageView.spec.ts</automated>
  </verify>
  <done>App route views expose the new route vocabulary and map return paths without changing Phase 44-owned popup/date behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Update specs and run the Phase 43 gate</name>
  <read_first>
    - .planning/phases/43-landing/43-CONTEXT.md
    - .planning/phases/43-landing/43-RESEARCH.md
    - .planning/phases/43-landing/43-PATTERNS.md
    - .planning/phases/43-landing/43-UI-SPEC.md
    - .planning/phases/43-landing/43-VALIDATION.md
    - apps/web/src/views/TimelinePageView.spec.ts
    - apps/web/src/views/StatisticsPageView.spec.ts
    - apps/web/src/router/index.spec.ts
    - apps/web/src/App.spec.ts
    - apps/web/src/App.kawaii.spec.ts
    - apps/web/src/views/LandingPageView.spec.ts
    - apps/web/src/components/shell/AuthenticatedAppShell.spec.ts
    - apps/web/src/components/auth/AuthDialog.spec.ts
  </read_first>
  <files>apps/web/src/views/TimelinePageView.spec.ts, apps/web/src/views/StatisticsPageView.spec.ts, apps/web/src/router/index.spec.ts, apps/web/src/App.spec.ts, apps/web/src/App.kawaii.spec.ts</files>
  <action>Update affected specs to assert the new vocabulary and route hooks. `TimelinePageView.spec.ts` must assert `data-route-view="journal"`, `data-region="journal-shell"`, `旅途手账`, `还没有留下足迹`, and `去世界足迹留下足迹`; keep existing multi-visit and unknown-date behavior assertions. `StatisticsPageView.spec.ts` must assert `data-route-view="memories"`, `data-region="memories-shell"`, `旅途回忆`, `还没有留下足迹`, and the exact error copy `旅途数据暂时没有同步成功，请刷新页面或稍后重试。`; keep existing stats refresh assertions. Ensure `router/index.spec.ts`, `App.spec.ts`, and `App.kawaii.spec.ts` use `/map`, `/journal`, `/memories`, `world-footprints`, `travel-journal`, `travel-memories`, `data-app-shell`, `data-route-view="landing"`, `data-route-view="map"`, `data-route-view="journal"`, and `data-route-view="memories"` as appropriate. Run the grep audit against Phase 43-owned route/shell/landing/auth files and repair any remaining old route-facing strings in those files. Do not change files under `apps/web/src/components/map-popup` in this plan.</action>
  <acceptance_criteria>
    - `apps/web/src/views/TimelinePageView.spec.ts` contains `data-route-view="journal"`, `旅途手账`, `还没有留下足迹`, and `去世界足迹留下足迹`.
    - `apps/web/src/views/StatisticsPageView.spec.ts` contains `data-route-view="memories"`, `旅途回忆`, `还没有留下足迹`, and `旅途数据暂时没有同步成功，请刷新页面或稍后重试。`.
    - `apps/web/src/router/index.spec.ts` contains `/map`, `/journal`, `/memories`, `world-footprints`, `travel-journal`, and `travel-memories`.
    - `apps/web/src/App.spec.ts` contains `data-route-view="landing"`, `data-app-shell`, `/map`, `/journal`, and `/memories`.
    - `apps/web/src/App.kawaii.spec.ts` contains `data-shell-sidebar` and does not contain `data-kawaii-shell="thin"`.
    - `rg -n "我的收藏|/timeline|/statistics|时间轴|旅行统计|点亮" apps/web/src/router apps/web/src/App.vue apps/web/src/App.spec.ts apps/web/src/App.kawaii.spec.ts apps/web/src/views apps/web/src/components/landing apps/web/src/components/shell apps/web/src/components/auth/AuthDialog.vue apps/web/src/components/auth/AuthDialog.spec.ts apps/web/src/components/auth/AuthTopbarControl.vue apps/web/src/components/auth/AuthTopbarControl.spec.ts` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web exec vitest run src/router/index.spec.ts src/App.spec.ts src/App.kawaii.spec.ts src/views/LandingPageView.spec.ts src/components/shell/AuthenticatedAppShell.spec.ts src/components/auth/AuthDialog.spec.ts src/views/TimelinePageView.spec.ts src/views/StatisticsPageView.spec.ts && pnpm --filter @trip-map/web test && pnpm --filter @trip-map/web build</automated>
  </verify>
  <done>Focused Phase 43 specs, full web test suite, web build, and grep audit all pass with the new route vocabulary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify desktop screenshots for landing and authenticated shell</name>
  <files>.planning/phases/43-landing/43-04-SUMMARY.md</files>
  <what-built>After Task 2, Phase 43 has the public landing route at `/`, the authenticated app shell on `/map`, updated route vocabulary, focused specs, full web tests, and a web build.</what-built>
  <action>Pause for a lightweight browser screenshot gate before marking Phase 43 ready. This implements the UI-SPEC desktop screenshot requirement without moving broad Visual QA out of Phase 48.</action>
  <how-to-verify>
    1. Start the web app locally with `pnpm --filter @trip-map/web dev --host 127.0.0.1` and open the printed localhost URL.
    2. For anonymous `/`, capture or inspect desktop viewports `1366x768`, `1440x900`, `1536x1024`, and `1920x1080`.
    3. Confirm `/` renders `data-route-view="landing"` and no `data-app-shell`; the landing composition is centered with a max `1536px` stage; `landing-upper-bg.png` and `landing-lower-bg.png` provide a `1672px` scene bleed without horizontal scroll; the hero DOM block sits near inline start `clamp(96px, 9vw, 144px)` and top `128px`; the title and CTA labels are real DOM text, readable, and not viewport-width font-scaled; the bottom CTA panel is centered at `min(1240px, calc(100vw - 160px))` and at least `120px` tall.
    4. Log in or register through the landing/auth dialog, then visit `/map` and capture or inspect the same viewports: `1366x768`, `1440x900`, `1536x1024`, and `1920x1080`.
    5. Confirm `/map` renders `data-app-shell`; the sidebar is `280px`, left-side only, and not collapsible; nav entries are exactly `世界足迹`, `旅途手账`, `旅途回忆`; no old topbar, drawer, bottom nav, `我的收藏`, summary, badge, progress, or stats panel appears; main content uses `min-width: 0` and does not overlap the sidebar at any checked viewport.
    6. Record screenshot paths or a concise pass/fail note in `.planning/phases/43-landing/43-04-SUMMARY.md`.
  </how-to-verify>
  <verify>
    <automated>pnpm --filter @trip-map/web build</automated>
    <manual>Browser screenshot check for `/` and `/map` at `1366x768`, `1440x900`, `1536x1024`, and `1920x1080` passes the constraints listed in `<how-to-verify>`.</manual>
  </verify>
  <done>`43-04-SUMMARY.md` records screenshot paths or a pass/fail note for both `/` and authenticated `/map`, and any failed viewport has been fixed before approval.</done>
  <resume-signal>Type `approved` if all checked viewports pass, or describe the viewport/path and issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protected view copy -> user intent | Route-facing labels guide users to authenticated app areas. |
| tests/grep gate -> implementation | Automated checks prevent old public route semantics from re-entering Phase 43-owned surfaces. |
| map view -> records APIs | Map route copy changes must not weaken existing records API session guards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-12 | Spoofing | Route-facing links | mitigate | All app-return links point to protected `/map`; router guard handles anonymous access. |
| T-43-13 | Information Disclosure | View-level anonymous states | mitigate | Route views can keep anonymous-state branches for component tests, but real app navigation is protected by router meta and backend guards. |
| T-43-14 | Tampering | Copy migration | mitigate | Grep gate blocks old `/timeline`, `/statistics`, `时间轴`, `旅行统计`, `点亮`, and `我的收藏` in Phase 43-owned route/shell/landing/auth files. |
| T-43-15 | Denial of Service | Regression suite | mitigate | Run focused specs before full web test/build so failures are localized before the full phase gate. |
</threat_model>

<verification>
Final gate:
1. `pnpm --filter @trip-map/web exec vitest run src/router/index.spec.ts src/App.spec.ts src/App.kawaii.spec.ts src/views/LandingPageView.spec.ts src/components/shell/AuthenticatedAppShell.spec.ts src/components/auth/AuthDialog.spec.ts src/views/TimelinePageView.spec.ts src/views/StatisticsPageView.spec.ts`
2. `pnpm --filter @trip-map/web test`
3. `pnpm --filter @trip-map/web build`
4. Grep audit in Task 2 acceptance criteria returns no matches.
5. Blocking browser screenshot gate passes for `/` and `/map` at `1366x768`, `1440x900`, `1536x1024`, and `1920x1080`, including the UI-SPEC `1536px` stage, `1672px` background bleed, hero offset, CTA panel dimensions, `280px` sidebar, and no viewport-width font scaling.
</verification>

<success_criteria>
- Route-facing Phase 43 surfaces use `世界足迹`, `旅途手账`, `旅途回忆`, and `留下足迹`.
- `/timeline` and `/statistics` are absent from Phase 43-owned source/tests.
- No collection entry or collection copy appears in Phase 43-owned route/shell/landing/auth files.
- Full web tests and build pass after all Phase 43 plans are complete.
- Lightweight desktop screenshot verification passes for `/` landing and authenticated `/map` shell at the UI-SPEC viewport set.
</success_criteria>

<output>
After completion, create `.planning/phases/43-landing/43-04-SUMMARY.md`.
</output>

---
phase: 14-leaflet
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - apps/web/src/components/LeafletMapStage.spec.ts
  - apps/web/src/components/WorldMapStage.vue
  - apps/web/src/components/WorldMapStage.spec.ts
autonomous: false
requirements:
  - MAP-04
  - MAP-05
  - MAP-06
  - MAP-08
  - UIX-01
  - GEOX-05

must_haves:
  truths:
    - "LeafletMapStage has unit tests covering map click, boundary click, popup display, and highlight state transitions"
    - "Old WorldMapStage.spec.ts is either removed or superseded by LeafletMapStage.spec.ts"
    - "Full test suite passes including the new LeafletMapStage specs"
    - "User can visually confirm the Leaflet map works with popup and highlights"
  artifacts:
    - path: "apps/web/src/components/LeafletMapStage.spec.ts"
      provides: "Unit tests for LeafletMapStage covering MAP-04, MAP-05, MAP-06, MAP-08, UIX-01, GEOX-05"
      min_lines: 100
  key_links:
    - from: "apps/web/src/components/LeafletMapStage.spec.ts"
      to: "apps/web/src/components/LeafletMapStage.vue"
      via: "component mount"
      pattern: "mount.*LeafletMapStage"
---

<objective>
Write comprehensive unit tests for LeafletMapStage, clean up old WorldMapStage files, and visually verify the Leaflet map works end-to-end.

Purpose: Ensure the Leaflet migration is regression-safe with test coverage matching the old WorldMapStage.spec.ts, and get user sign-off on the visual result.

Output: LeafletMapStage.spec.ts with passing tests, old SVG files cleaned up, visual checkpoint approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-leaflet/14-CONTEXT.md
@.planning/phases/14-leaflet/14-RESEARCH.md
@.planning/phases/14-leaflet/14-UI-SPEC.md
@.planning/phases/14-leaflet/14-01-SUMMARY.md
@.planning/phases/14-leaflet/14-02-SUMMARY.md

@apps/web/src/components/LeafletMapStage.vue
@apps/web/src/components/WorldMapStage.spec.ts
@apps/web/src/composables/useLeafletMap.ts
@apps/web/src/composables/useGeoJsonLayers.ts
@apps/web/src/composables/useLeafletPopupAnchor.ts

<interfaces>
<!-- Test mock pattern from RESEARCH.md -->
```typescript
vi.mock('../composables/useLeafletMap', () => ({
  useLeafletMap: () => ({
    map: shallowRef(null),
    isReady: shallowRef(false),
  }),
}))
vi.mock('../composables/useGeoJsonLayers', () => ({
  useGeoJsonLayers: () => ({
    addFeatures: vi.fn(),
    refreshStyles: vi.fn(),
    cnLayer: {},
    overseasLayer: {},
  }),
}))
vi.mock('../composables/useLeafletPopupAnchor', () => ({
  useLeafletPopupAnchor: () => ({
    virtualElement: computed(() => null),
  }),
}))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LeafletMapStage.spec.ts with full regression coverage</name>
  <files>apps/web/src/components/LeafletMapStage.spec.ts</files>
  <read_first>
    - apps/web/src/components/WorldMapStage.spec.ts (reference for test structure and behaviors covered)
    - apps/web/src/components/LeafletMapStage.vue (component under test)
    - apps/web/src/composables/useLeafletMap.ts (mock target)
    - apps/web/src/composables/useGeoJsonLayers.ts (mock target)
    - apps/web/src/composables/useLeafletPopupAnchor.ts (mock target)
    - apps/web/src/stores/map-points.ts (store mocks)
    - apps/web/src/stores/map-ui.ts (store mocks)
    - apps/web/src/types/map-point.ts (SummarySurfaceState type)
  </read_first>
  <behavior>
    - MAP-04: Component mounts and calls useLeafletMap with container ref; map click triggers resolveCanonicalPlace API call
    - MAP-05: Component calls useGeoJsonLayers which creates CN and OVERSEAS layers (mock verification that addFeatures called for both 'CN' and 'OVERSEAS')
    - MAP-06: After resolve returns a place with boundaryId, selectedBoundaryId updates in store (triggering GeoJSON layer style refresh)
    - MAP-08: When selectedPointId changes (switching selection), selectedBoundaryId changes accordingly; refreshStyles called; no double-highlight
    - UIX-01: When summarySurfaceState is non-null and drawerMode is null, MapContextPopup renders; when drawerMode is non-null, PointPreviewDrawer renders
    - GEOX-05: useGeoJsonLayers receives two separate layer calls (CN and OVERSEAS), not a merged single call
    - D-12: When onBoundaryClick fires for a boundaryId that matches a saved point, the component opens that point's popup without calling resolveCanonicalPlace
  </behavior>
  <action>
Create `apps/web/src/components/LeafletMapStage.spec.ts`. Follow the existing WorldMapStage.spec.ts structure as reference for test organization, but adapt all assertions for Leaflet.

**Mock strategy:**
- `vi.mock('../composables/useLeafletMap')` — return controllable `map` and `isReady` shallowRefs
- `vi.mock('../composables/useGeoJsonLayers')` — return mock `addFeatures`, `refreshStyles` functions
- `vi.mock('../composables/useLeafletPopupAnchor')` — return mock `virtualElement` computed
- `vi.mock('../composables/usePopupAnchoring')` — return mock `floatingStyles`, `updatePosition`
- `vi.mock('../services/api/canonical-places')` — mock `resolveCanonicalPlace`, `confirmCanonicalPlace`
- `vi.mock('../services/geometry-loader')` — mock `loadGeometryShard`
- `vi.mock('../services/geometry-manifest')` — mock `listGeometryManifestEntriesByLayer`, `getGeometryManifestEntry`
- Create Pinia test stores with `createTestingPinia` or manual store setup

**Test cases (minimum):**

1. **"mounts and initializes Leaflet map"** — verify useLeafletMap called, container ref connected
2. **"loads CN and OVERSEAS shard data on map ready"** — set isReady to true, verify addFeatures called twice (once for 'CN', once for 'OVERSEAS') — covers GEOX-05
3. **"calls resolveCanonicalPlace on map click"** — simulate the map click callback, verify API called with lat/lng — covers MAP-04
4. **"shows pending marker during recognition"** — verify circleMarker creation (or mock pattern)
5. **"creates draft point on resolved response"** — mock resolveCanonicalPlace response as 'resolved', verify store interaction
6. **"enters candidate selection on ambiguous response"** — mock response as 'ambiguous', verify startPendingCanonicalSelection called
7. **"shows MapContextPopup when summarySurfaceState is set and no drawer"** — set store state, verify popup renders — covers UIX-01
8. **"shows PointPreviewDrawer when drawerMode is set"** — set store state, verify drawer renders — covers UIX-01
9. **"boundary click on saved point opens popup without resolve API"** — invoke onBoundaryClick callback for a saved boundaryId, verify resolveCanonicalPlace NOT called, verify openSavedPointForPlaceOrStartDraft called — covers D-12
10. **"refreshes styles when selectedBoundaryId changes"** — change store selectedBoundaryId, verify refreshStyles called — covers MAP-06
11. **"no double highlight on selection switch"** — change selectedPointId, verify refreshStyles called (which internally resets old + applies new) — covers MAP-08

**Test file structure:**
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { mount } from '@vue/test-utils'
import { createPinia, setActivePinia } from 'pinia'
// ... mocks ...

describe('LeafletMapStage', () => {
  describe('map initialization', () => { ... })
  describe('GeoJSON layer loading', () => { ... })
  describe('map click -> resolve flow', () => { ... })
  describe('boundary click -> saved point shortcut', () => { ... })
  describe('popup and drawer visibility', () => { ... })
  describe('highlight state transitions', () => { ... })
})
```

After writing specs, also delete or rename `WorldMapStage.spec.ts` to `WorldMapStage.spec.ts.legacy` so it doesn't run in the test suite and cause failures (it tests the old SVG component which is no longer mounted in App.vue). Alternatively, add `.skip` to all its describe blocks.
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm --filter @trip-map/web test src/components/LeafletMapStage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/LeafletMapStage.spec.ts exists
    - LeafletMapStage.spec.ts contains `vi.mock('../composables/useLeafletMap'`
    - LeafletMapStage.spec.ts contains `vi.mock('../composables/useGeoJsonLayers'`
    - LeafletMapStage.spec.ts contains test for "CN" and "OVERSEAS" addFeatures calls (GEOX-05 coverage)
    - LeafletMapStage.spec.ts contains test for resolveCanonicalPlace call on map click (MAP-04 coverage)
    - LeafletMapStage.spec.ts contains test for MapContextPopup rendering (UIX-01 coverage)
    - LeafletMapStage.spec.ts contains test for boundary click without resolve (D-12 coverage)
    - `pnpm --filter @trip-map/web test src/components/LeafletMapStage.spec.ts` exits 0
    - WorldMapStage.spec.ts is either renamed/deleted or has all describes skipped so full suite passes
    - `pnpm --filter @trip-map/web test` full suite exits 0
  </acceptance_criteria>
  <done>LeafletMapStage has comprehensive unit tests covering all 6 phase requirement IDs. Old WorldMapStage specs are retired. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Leaflet map with popup and highlights</name>
  <files>apps/web/src/components/LeafletMapStage.vue</files>
  <action>
Start dev server with `pnpm dev:web`. No code changes — this is a human verification checkpoint for the visual and interactive result of the Leaflet migration.

What was built: Complete Leaflet map migration — tile base map (Bing or CartoDB Positron), click-to-resolve flow with pending marker, popup appearing after resolve, boundary highlight for saved/selected states, drawer opening from popup.

How to verify:
1. Run `pnpm dev:web` and open the app in browser
2. Verify the map shows tile base map (CartoDB Positron if no Bing key configured) centered on China
3. Click on a location on the tile map — verify:
   - Pink pending marker appears at click position
   - After a moment, popup appears with place name and summary
   - Boundary of the resolved place highlights in pink (selected state)
4. If you have previously saved points, verify their boundaries show in blue (saved state)
5. Click a different location — verify:
   - Old pink highlight disappears (no double-highlight)
   - New pending marker + resolve + popup cycle works
6. Click "查看详情" in popup — verify drawer opens alongside popup
7. Close popup — verify highlight clears
8. Pan and zoom the map — verify popup follows if still open
9. Zoom out to world view, zoom back in — verify map behaves normally
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm --filter @trip-map/web exec vue-tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>User visually approves: Leaflet map renders with tile basemap, click-resolve-popup flow works, boundary highlights show correctly, no double-highlight residue, popup follows map movement.</done>
  <resume-signal>Type "approved" or describe any visual/interaction issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @trip-map/web test` — full test suite green (includes new LeafletMapStage specs, excludes old WorldMapStage specs)
- `pnpm --filter @trip-map/web exec vue-tsc --noEmit` — TypeScript passes
- Visual checkpoint: user confirms map, popup, highlights, and drawer work correctly
</verification>

<success_criteria>
- LeafletMapStage.spec.ts has 10+ test cases covering all 6 requirement IDs
- Full test suite passes (no regressions)
- User visually confirms Leaflet map works with the complete main flow
- No double-highlight residue (MAP-08)
- Popup follows map movement (D-14)
- CN and OVERSEAS layers loaded independently (GEOX-05)
</success_criteria>

<output>
After completion, create `.planning/phases/14-leaflet/14-03-SUMMARY.md`
</output>

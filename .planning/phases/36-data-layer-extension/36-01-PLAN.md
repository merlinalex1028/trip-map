---
phase: 36-data-layer-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/prisma/schema.prisma
  - packages/contracts/src/records.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "UserTravelRecord 模型包含 notes (String?) 和 tags (String[]) 字段"
    - "TravelRecord 接口包含 updatedAt、notes、tags 字段"
    - "UpdateTravelRecordRequest 接口存在，所有字段可选"
    - "contracts 包构建成功，类型可被下游消费"
  artifacts:
    - path: "apps/server/prisma/schema.prisma"
      provides: "UserTravelRecord 模型扩展 notes + tags 字段"
      contains: "notes"
    - path: "packages/contracts/src/records.ts"
      provides: "TravelRecord 接口扩展 + UpdateTravelRecordRequest 接口"
      contains: "UpdateTravelRecordRequest"
  key_links:
    - from: "packages/contracts/src/records.ts"
      to: "apps/server/prisma/schema.prisma"
      via: "字段名和类型一致性"
      pattern: "notes.*tags"
---

<objective>
扩展 Prisma schema 和 contracts 类型定义，为后续 PATCH/DELETE 端点提供数据模型基础。

Purpose: 数据层是整个 v7.0 的根基，schema 和 contracts 必须先行，后续 DTO、repository、service、controller 全部依赖这些类型。
Output: 更新后的 schema.prisma（含 notes/tags 字段）、更新后的 TravelRecord 接口（含 updatedAt/notes/tags）、新增 UpdateTravelRecordRequest 接口。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/server/prisma/schema.prisma
@packages/contracts/src/records.ts
@packages/contracts/src/index.ts
@.planning/phases/36-data-layer-extension/36-CONTEXT.md
@.planning/phases/36-data-layer-extension/36-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 Prisma schema — UserTravelRecord 添加 notes 和 tags 字段</name>
  <files>apps/server/prisma/schema.prisma</files>
  <read_first>
    - apps/server/prisma/schema.prisma (当前完整 schema，确认 UserTravelRecord 模型位置和字段顺序)
  </read_first>
  <action>
在 `apps/server/prisma/schema.prisma` 的 `UserTravelRecord` 模型中，在 `endDate` 字段（line 81）和 `createdAt` 字段（line 82）之间添加两个新字段：

```prisma
  notes          String?
  tags           String[]  @default([])
```

具体变更：
- `notes` 类型为 `String?`（nullable），与 SmokeRecord 的 `note` 字段模式一致
- `tags` 类型为 `String[]`（PostgreSQL 原生数组），默认值 `@default([])`
- 不修改任何现有字段、约束或索引
- 不修改其他模型（SmokeRecord、TravelRecord、User 等）

最终 UserTravelRecord 模型字段顺序应为：
id, userId, placeId, boundaryId, placeKind, datasetVersion, displayName, regionSystem, adminType, typeLabel, parentLabel, subtitle, startDate, endDate, **notes**, **tags**, createdAt, updatedAt
  </action>
  <verify>
    <automated>grep -n "notes\|tags" apps/server/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - apps/server/prisma/schema.prisma 包含 `notes          String?`
    - apps/server/prisma/schema.prisma 包含 `tags           String[]  @default([])`
    - notes 和 tags 字段位于 endDate 之后、createdAt 之前
    - schema 文件中 UserTravelRecord 模型的 @@unique 约束未被修改（仍为 `@@unique([userId, placeId, startDate, endDate])`）
    - 其他模型（SmokeRecord、TravelRecord、User、AuthSession）未被修改
  </acceptance_criteria>
  <done>UserTravelRecord 模型包含 notes (String?) 和 tags (String[] @default([])) 字段，位置正确，现有约束不变</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 contracts — TravelRecord 接口 + 新增 UpdateTravelRecordRequest</name>
  <files>packages/contracts/src/records.ts</files>
  <read_first>
    - packages/contracts/src/records.ts (当前 TravelRecord 接口和 CreateTravelRecordRequest 定义)
    - apps/server/prisma/schema.prisma (确认新增字段的 Prisma 类型)
  </read_first>
  <action>
修改 `packages/contracts/src/records.ts`：

1. 在 `TravelRecord` 接口的 `createdAt` 字段后添加三个新字段：
```typescript
  updatedAt: string
  notes: string | null
  tags: string[]
```

2. 在 `CreateTravelRecordRequest` 接口之后、`ImportTravelRecordsRequest` 接口之前，新增 `UpdateTravelRecordRequest` 接口：
```typescript
export interface UpdateTravelRecordRequest {
  startDate?: string | null
  endDate?: string | null
  notes?: string | null
  tags?: string[]
}
```

不修改现有的 `CreateTravelRecordRequest`、`ImportTravelRecordsRequest`、`ImportTravelRecordsResponse` 接口。
不修改 `packages/contracts/src/index.ts`（`export * from './records'` 已自动覆盖新接口）。
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm --filter @trip-map/contracts build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/contracts/src/records.ts 的 TravelRecord 接口包含 `updatedAt: string`
    - packages/contracts/src/records.ts 的 TravelRecord 接口包含 `notes: string | null`
    - packages/contracts/src/records.ts 的 TravelRecord 接口包含 `tags: string[]`
    - packages/contracts/src/records.ts 包含 `export interface UpdateTravelRecordRequest`
    - UpdateTravelRecordRequest 包含 `startDate?: string | null`
    - UpdateTravelRecordRequest 包含 `endDate?: string | null`
    - UpdateTravelRecordRequest 包含 `notes?: string | null`
    - UpdateTravelRecordRequest 包含 `tags?: string[]`
    - `pnpm --filter @trip-map/contracts build` 成功退出
    - CreateTravelRecordRequest、ImportTravelRecordsRequest、ImportTravelRecordsResponse 未被修改
  </acceptance_criteria>
  <done>TravelRecord 接口包含 updatedAt/notes/tags，UpdateTravelRecordRequest 接口存在且所有字段可选，contracts 构建成功</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema→contracts | Prisma 字段类型与 TypeScript 接口必须一致 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | schema.prisma | mitigate | 字段类型与 contracts 保持一致，后续 plan 的 DTO 验证层提供输入保护 |

本 plan 为纯类型定义层，不涉及用户输入处理，安全风险低。输入验证和 SQL 注入防护在 Plan 02 的 DTO + Service 层实现。
</threat_model>

<verification>
1. `pnpm --filter @trip-map/contracts build` 成功
2. grep 确认 schema 中 notes/tags 字段存在
3. grep 确认 contracts 中 updatedAt/notes/tags/UpdateTravelRecordRequest 存在
</verification>

<success_criteria>
- UserTravelRecord 模型包含 notes (String?) 和 tags (String[] @default([])) 字段
- TravelRecord 接口包含 updatedAt、notes、tags 字段
- UpdateTravelRecordRequest 接口存在，4 个字段全部可选
- contracts 包构建成功，无类型错误
</success_criteria>

<output>
After completion, create `.planning/phases/36-data-layer-extension/36-01-SUMMARY.md`
</output>

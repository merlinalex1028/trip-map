---
phase: 36-data-layer-extension
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - apps/server/src/modules/records/dto/update-travel-record.dto.ts
  - apps/server/src/modules/records/records.repository.ts
  - apps/server/src/modules/records/records.service.ts
  - apps/server/src/modules/records/records.controller.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "PATCH /records/:id 端点可更新 startDate、endDate、notes、tags，返回完整 TravelRecord"
    - "DELETE /records/record/:id 端点可删除单条记录，返回 204"
    - "编辑日期与同地点其他记录冲突时返回 409"
    - "备注超过 1000 字符或标签超过 10 个/每个超过 20 字符时返回 400"
    - "标签自动清洗：trim、去重、过滤空字符串"
    - "所有端点需要认证（SessionAuthGuard）"
  artifacts:
    - path: "apps/server/src/modules/records/dto/update-travel-record.dto.ts"
      provides: "UpdateTravelRecordDto — PATCH 请求体验证"
      contains: "UpdateTravelRecordDto"
    - path: "apps/server/src/modules/records/records.repository.ts"
      provides: "findTravelRecordById、updateTravelRecord、deleteTravelRecordById 方法"
      contains: "updateTravelRecord"
    - path: "apps/server/src/modules/records/records.service.ts"
      provides: "updateTravelRecord、deleteTravelRecord 业务逻辑 + toContractTravelRecord 扩展"
      contains: "ConflictException"
    - path: "apps/server/src/modules/records/records.controller.ts"
      provides: "PATCH /records/:id 和 DELETE /records/record/:id 端点"
      contains: "@Patch"
  key_links:
    - from: "apps/server/src/modules/records/records.controller.ts"
      to: "apps/server/src/modules/records/records.service.ts"
      via: "PATCH/DELETE 调用 service 方法"
      pattern: "recordsService\\.updateTravelRecord\\|recordsService\\.deleteTravelRecord"
    - from: "apps/server/src/modules/records/records.service.ts"
      to: "apps/server/src/modules/records/records.repository.ts"
      via: "service 调用 repository 数据访问方法"
      pattern: "recordsRepository\\.updateTravelRecord\\|recordsRepository\\.deleteTravelRecordById"
    - from: "apps/server/src/modules/records/records.service.ts"
      to: "packages/contracts/src/records.ts"
      via: "toContractTravelRecord 返回 TravelRecord 类型"
      pattern: "ContractTravelRecord"
---

<objective>
实现 PATCH 和 DELETE 后端端点，完成数据层扩展的完整 API 闭环。

Purpose: 前端 Phase 37 需要消费 PATCH/DELETE 端点来实现编辑/删除功能，本 plan 是前端开发的前提。
Output: UpdateTravelRecordDto、repository 新增 3 个方法、service 新增 2 个方法 + toContractTravelRecord 扩展、controller 新增 2 个端点、schema push 到数据库。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/server/prisma/schema.prisma
@packages/contracts/src/records.ts
@apps/server/src/modules/records/records.controller.ts
@apps/server/src/modules/records/records.service.ts
@apps/server/src/modules/records/records.repository.ts
@apps/server/src/modules/records/dto/create-travel-record.dto.ts
@.planning/phases/36-data-layer-extension/36-CONTEXT.md
@.planning/phases/36-data-layer-extension/36-PATTERNS.md
@.planning/phases/36-data-layer-extension/36-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 UpdateTravelRecordDto</name>
  <files>apps/server/src/modules/records/dto/update-travel-record.dto.ts</files>
  <read_first>
    - apps/server/src/modules/records/dto/create-travel-record.dto.ts (DTO 模式参考：class-validator 装饰器用法)
    - packages/contracts/src/records.ts (确认 UpdateTravelRecordRequest 接口定义)
  </read_first>
  <action>
创建 `apps/server/src/modules/records/dto/update-travel-record.dto.ts`，内容如下：

```typescript
import { IsOptional, IsString, Matches, MaxLength, ArrayMaxSize } from 'class-validator'
import { ApiProperty } from '@nestjs/swagger'

import type { UpdateTravelRecordRequest } from '@trip-map/contracts'

export class UpdateTravelRecordDto implements UpdateTravelRecordRequest {
  @ApiProperty({ nullable: true, example: '2025-10-01', required: false })
  @IsOptional()
  @Matches(/^\d{4}-\d{2}-\d{2}$/, { message: 'startDate must be YYYY-MM-DD' })
  startDate?: string | null

  @ApiProperty({ nullable: true, example: '2025-10-07', required: false })
  @IsOptional()
  @Matches(/^\d{4}-\d{2}-\d{2}$/, { message: 'endDate must be YYYY-MM-DD' })
  endDate?: string | null

  @ApiProperty({ nullable: true, example: '这是一条备注', required: false })
  @IsOptional()
  @IsString()
  @MaxLength(1000, { message: 'notes must be at most 1000 characters' })
  notes?: string | null

  @ApiProperty({ type: [String], required: false, example: ['美食', '文化'] })
  @IsOptional()
  @IsString({ each: true, message: 'each tag must be a string' })
  @MaxLength(20, { each: true, message: 'each tag must be at most 20 characters' })
  @ArrayMaxSize(10, { message: 'tags must contain at most 10 items' })
  tags?: string[]
}
```

验证规则说明：
- startDate/endDate: `@IsOptional()` + `@Matches(/^\d{4}-\d{2}-\d{2}$/)` — 日期格式 YYYY-MM-DD
- notes: `@IsOptional()` + `@IsString()` + `@MaxLength(1000)` — D-13 最大 1000 字符
- tags: `@IsOptional()` + `@IsString({ each: true })` + `@MaxLength(20, { each: true })` + `@ArrayMaxSize(10)` — 每个标签最长 20 字符，最多 10 个标签
  </action>
  <verify>
    <automated>grep -c "class UpdateTravelRecordDto" apps/server/src/modules/records/dto/update-travel-record.dto.ts</automated>
  </verify>
  <acceptance_criteria>
    - 文件 apps/server/src/modules/records/dto/update-travel-record.dto.ts 存在
    - 文件包含 `export class UpdateTravelRecordDto implements UpdateTravelRecordRequest`
    - startDate 字段有 `@Matches(/^\d{4}-\d{2}-\d{2}$/)` 装饰器
    - endDate 字段有 `@Matches(/^\d{4}-\d{2}-\d{2}$/)` 装饰器
    - notes 字段有 `@MaxLength(1000)` 装饰器
    - tags 字段有 `@IsString({ each: true })` 和 `@ArrayMaxSize(10)` 装饰器
    - tags 字段有 `@MaxLength(20, { each: true })` 装饰器
    - 所有 4 个字段都有 `@IsOptional()` 装饰器
    - import 来源为 `@trip-map/contracts`
  </acceptance_criteria>
  <done>UpdateTravelRecordDto 存在，所有字段验证规则正确实现</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 Repository — 添加 findTravelRecordById、updateTravelRecord、deleteTravelRecordById</name>
  <files>apps/server/src/modules/records/records.repository.ts</files>
  <read_first>
    - apps/server/src/modules/records/records.repository.ts (当前完整代码，确认现有方法签名和 Prisma 用法)
    - apps/server/prisma/schema.prisma (确认 UserTravelRecord 模型字段，特别是新增的 notes/tags)
  </read_first>
  <action>
修改 `apps/server/src/modules/records/records.repository.ts`，在 `deleteTravelRecordByPlaceId` 方法之后、`getTravelStats` 方法之前，添加三个新方法：

**1. findTravelRecordById** — 通过 ID 查找单条记录（含 userId 权限过滤）：
```typescript
async findTravelRecordById(userId: string, id: string): Promise<UserTravelRecord | null> {
  return this.prisma.userTravelRecord.findFirst({
    where: { id, userId },
  })
}
```

**2. updateTravelRecord** — 更新单条记录：
```typescript
async updateTravelRecord(
  userId: string,
  id: string,
  data: {
    startDate?: string | null
    endDate?: string | null
    notes?: string | null
    tags?: string[]
  },
): Promise<UserTravelRecord> {
  return this.prisma.userTravelRecord.update({
    where: { id, userId },
    data,
  })
}
```

**3. deleteTravelRecordById** — 按记录 ID 删除单条记录：
```typescript
async deleteTravelRecordById(userId: string, id: string): Promise<void> {
  await this.prisma.userTravelRecord.delete({
    where: { id, userId },
  })
}
```

注意：
- `findTravelRecordById` 使用 `findFirst`（而非 `findUnique`），因为需要同时匹配 `id` 和 `userId` 做行级权限检查
- `updateTravelRecord` 的 `where` 包含 `id` 和 `userId`，确保行级权限
- `deleteTravelRecordById` 使用 `prisma.delete()`（单条），与现有 `deleteMany`（按 placeId 批量）区分
- Prisma 的 `update`/`delete` 在记录不存在时抛出 `P2025`（RecordNotFoundError），由 service 层捕获处理
  </action>
  <verify>
    <automated>grep -c "findTravelRecordById\|updateTravelRecord\|deleteTravelRecordById" apps/server/src/modules/records/records.repository.ts</automated>
  </verify>
  <acceptance_criteria>
    - records.repository.ts 包含 `async findTravelRecordById(userId: string, id: string): Promise<UserTravelRecord | null>`
    - records.repository.ts 包含 `async updateTravelRecord(userId: string, id: string, data:`
    - records.repository.ts 包含 `async deleteTravelRecordById(userId: string, id: string): Promise<void>`
    - findTravelRecordById 使用 `this.prisma.userTravelRecord.findFirst`
    - updateTravelRecord 使用 `this.prisma.userTravelRecord.update`
    - deleteTravelRecordById 使用 `this.prisma.userTravelRecord.delete`（非 deleteMany）
    - 三个方法的 where 条件都包含 `{ id, userId }`（行级权限）
    - 现有方法（createSmokeRecord、findAllTravelRecords、createTravelRecord、importTravelRecords、deleteTravelRecordByPlaceId、getTravelStats）未被修改
  </acceptance_criteria>
  <done>Repository 新增 findTravelRecordById、updateTravelRecord、deleteTravelRecordById 三个方法，行级权限正确</done>
</task>

<task type="auto">
  <name>Task 3: 扩展 Service — 添加 updateTravelRecord、deleteTravelRecord + 更新 toContractTravelRecord</name>
  <files>apps/server/src/modules/records/records.service.ts</files>
  <read_first>
    - apps/server/src/modules/records/records.service.ts (当前完整代码，确认 toContractTravelRecord、assertValidDateRange、现有方法)
    - packages/contracts/src/records.ts (确认 TravelRecord 接口新增字段)
    - apps/server/src/modules/records/records.repository.ts (确认新增方法签名)
  </read_first>
  <action>
修改 `apps/server/src/modules/records/records.service.ts`：

**1. 更新 import** — 在文件顶部添加 `ConflictException` 和 `NotFoundException` 到 `@nestjs/common` 的 import，添加 `Prisma` 到 `@prisma/client` 的 import：
```typescript
import { BadRequestException, ConflictException, Inject, Injectable, NotFoundException } from '@nestjs/common'
import type { SmokeRecord, UserTravelRecord } from '@prisma/client'
import { Prisma } from '@prisma/client'
```

添加 `UpdateTravelRecordDto` 的 import：
```typescript
import type { UpdateTravelRecordDto } from './dto/update-travel-record.dto.js'
```

**2. 更新 toContractTravelRecord 函数** — 在 `createdAt` 行之后添加三个新字段：
```typescript
function toContractTravelRecord(record: UserTravelRecord): ContractTravelRecord {
  return {
    id: record.id,
    placeId: record.placeId,
    boundaryId: record.boundaryId,
    placeKind: record.placeKind as PlaceKind,
    datasetVersion: record.datasetVersion,
    displayName: record.displayName,
    regionSystem: record.regionSystem as ContractTravelRecord['regionSystem'],
    adminType: record.adminType as ContractTravelRecord['adminType'],
    typeLabel: record.typeLabel as ContractTravelRecord['typeLabel'],
    parentLabel: record.parentLabel as ContractTravelRecord['parentLabel'],
    subtitle: record.subtitle,
    startDate: record.startDate ?? null,
    endDate: record.endDate ?? null,
    createdAt: record.createdAt.toISOString(),
    updatedAt: record.updatedAt.toISOString(),
    notes: record.notes ?? null,
    tags: record.tags,
  }
}
```

**3. 添加 updateTravelRecord 方法** — 在 `deleteTravel` 方法之后、`getStats` 方法之前添加：
```typescript
async updateTravelRecord(userId: string, id: string, input: UpdateTravelRecordDto): Promise<ContractTravelRecord> {
  // 日期变更时做范围校验
  if (input.startDate !== undefined || input.endDate !== undefined) {
    const existing = await this.recordsRepository.findTravelRecordById(userId, id)
    if (!existing) {
      throw new NotFoundException(`Record ${id} not found`)
    }
    const effectiveStart = input.startDate ?? existing.startDate
    const effectiveEnd = input.endDate ?? existing.endDate
    this.assertValidDateRange({ startDate: effectiveStart, endDate: effectiveEnd })
  }

  // D-10: 标签清洗 — trim + 去重 + 过滤空字符串
  const cleanData = { ...input }
  if (cleanData.tags) {
    cleanData.tags = [...new Set(cleanData.tags.map(t => t.trim()).filter(t => t.length > 0))]
  }

  try {
    const record = await this.recordsRepository.updateTravelRecord(userId, id, cleanData)
    return toContractTravelRecord(record)
  } catch (error) {
    if (error instanceof Prisma.PrismaClientKnownRequestError && error.code === 'P2002') {
      throw new ConflictException('与已有记录冲突: 相同日期范围内已存在同地点旅行记录')
    }
    if (error instanceof Prisma.PrismaClientKnownRequestError && error.code === 'P2025') {
      throw new NotFoundException(`Record ${id} not found`)
    }
    throw error
  }
}
```

**4. 添加 deleteTravelRecord 方法** — 紧跟 updateTravelRecord 之后：
```typescript
async deleteTravelRecord(userId: string, id: string): Promise<void> {
  try {
    await this.recordsRepository.deleteTravelRecordById(userId, id)
  } catch (error) {
    if (error instanceof Prisma.PrismaClientKnownRequestError && error.code === 'P2025') {
      throw new NotFoundException(`Record ${id} not found`)
    }
    throw error
  }
}
```

注意：
- `assertValidDateRange` 签名需要适配 — 当前接受 `Pick<CreateTravelRecordDto, 'startDate' | 'endDate'>`，传入 `{ startDate, endDate }` 对象即可兼容（因为只读取这两个属性）
- `updateTravelRecord` 中，只有当 startDate 或 endDate 被传入时才做日期校验（PATCH 语义：只改备注/标签不触发日期校验）
- 标签清洗逻辑：trim → 过滤空字符串 → 去重，保持去重后原始顺序（D-11 不排序）
- Prisma P2002 转为 ConflictException (409)，message 包含冲突提示（D-06）
- Prisma P2025 转为 NotFoundException (404)
  </action>
  <verify>
    <automated>grep -c "updateTravelRecord\|deleteTravelRecord\|ConflictException\|P2002" apps/server/src/modules/records/records.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - records.service.ts 包含 `async updateTravelRecord(userId: string, id: string, input: UpdateTravelRecordDto)`
    - records.service.ts 包含 `async deleteTravelRecord(userId: string, id: string): Promise<void>`
    - records.service.ts 包含 `throw new ConflictException('与已有记录冲突`
    - records.service.ts 包含 `throw new NotFoundException`
    - records.service.ts 包含 Prisma P2002 和 P2025 错误码检查
    - records.service.ts 包含标签清洗逻辑：`cleanData.tags.map(t => t.trim()).filter(t => t.length > 0)`
    - records.service.ts 包含去重逻辑：`new Set(...)`
    - toContractTravelRecord 函数包含 `updatedAt: record.updatedAt.toISOString()`
    - toContractTravelRecord 函数包含 `notes: record.notes ?? null`
    - toContractTravelRecord 函数包含 `tags: record.tags`
    - import 包含 `ConflictException`、`NotFoundException`、`Prisma`
    - import 包含 `UpdateTravelRecordDto`
    - 现有方法（createSmoke、findAllTravel、createTravel、importTravel、deleteTravel、getStats）未被修改
  </acceptance_criteria>
  <done>Service 新增 updateTravelRecord（含日期校验、标签清洗、P2002→409）和 deleteTravelRecord（含 P2025→404），toContractTravelRecord 映射 3 个新字段</done>
</task>

<task type="auto">
  <name>Task 4: 扩展 Controller — 新增 PATCH /records/:id 和 DELETE /records/record/:id 端点</name>
  <files>apps/server/src/modules/records/records.controller.ts</files>
  <read_first>
    - apps/server/src/modules/records/records.controller.ts (当前完整代码，确认端点顺序、装饰器模式、import)
    - apps/server/src/modules/records/dto/update-travel-record.dto.ts (确认 DTO 类名)
  </read_first>
  <action>
修改 `apps/server/src/modules/records/records.controller.ts`：

**1. 更新 import** — 添加 `Patch` 到 `@nestjs/common` 的 import，添加 `UpdateTravelRecordDto` import：
```typescript
import {
  Body,
  Controller,
  Delete,
  Get,
  HttpCode,
  Inject,
  Param,
  Patch,
  Post,
  UsePipes,
  UseGuards,
  ValidationPipe,
} from '@nestjs/common'
```
在 DTO import 区域添加：
```typescript
import { UpdateTravelRecordDto } from './dto/update-travel-record.dto.js'
```

**2. 新增 PATCH 端点** — 在 `createTravel` 方法之后、`importTravel` 方法之前添加：
```typescript
@Patch(':id')
@ApiOperation({ summary: '更新旅行记录' })
@ApiOkResponse()
@UseGuards(SessionAuthGuard)
@UsePipes(new ValidationPipe({
  whitelist: true,
  transform: true,
  forbidNonWhitelisted: true,
  expectedType: UpdateTravelRecordDto,
}))
async updateTravel(
  @CurrentUser() user: AuthUser,
  @Param('id') id: string,
  @Body() body: UpdateTravelRecordDto,
): Promise<ContractTravelRecord> {
  return this.recordsService.updateTravelRecord(user.id, id, body)
}
```

**3. 新增 DELETE by recordId 端点** — 在现有 `deleteTravel`（按 placeId 删除）方法之后添加：
```typescript
@Delete('record/:id')
@HttpCode(204)
@ApiOperation({ summary: '按记录 ID 删除单条旅行记录' })
@ApiNoContentResponse()
@UseGuards(SessionAuthGuard)
async deleteTravelById(
  @CurrentUser() user: AuthUser,
  @Param('id') id: string,
): Promise<void> {
  return this.recordsService.deleteTravelRecord(user.id, id)
}
```

路由说明：
- PATCH 路径为 `/records/:id`，NestJS 路由匹配顺序：`record/:id` 优先于 `:placeId`（更具体的路径先匹配）
- DELETE `record/:id` 路径为 `/records/record/:id`，与现有 `/records/:placeId` 不冲突
- 现有 `DELETE :placeId` 端点保留不变（D-01 只是移除前端调用，后端保持兼容）
  </action>
  <verify>
    <automated>grep -c "@Patch\|deleteTravelById\|UpdateTravelRecordDto" apps/server/src/modules/records/records.controller.ts</automated>
  </verify>
  <acceptance_criteria>
    - records.controller.ts 包含 `@Patch(':id')`
    - records.controller.ts 包含 `async updateTravel(`
    - records.controller.ts 包含 `@Delete('record/:id')`
    - records.controller.ts 包含 `async deleteTravelById(`
    - records.controller.ts 包含 `import { UpdateTravelRecordDto }`
    - PATCH 端点使用 `@UsePipes(new ValidationPipe({` 并指定 `expectedType: UpdateTravelRecordDto`
    - PATCH 端点使用 `@UseGuards(SessionAuthGuard)`
    - DELETE by recordId 端点使用 `@HttpCode(204)` 和 `@UseGuards(SessionAuthGuard)`
    - PATCH 端点返回类型为 `Promise<ContractTravelRecord>`
    - DELETE by recordId 端点返回类型为 `Promise<void>`
    - 现有端点（smoke、stats、findAll、createTravel、importTravel、deleteTravel by placeId）未被修改
    - import 中包含 `Patch` 装饰器
  </acceptance_criteria>
  <done>Controller 新增 PATCH /records/:id（返回完整 TravelRecord）和 DELETE /records/record/:id（返回 204）两个端点，现有端点不变</done>
</task>

<task type="auto">
  <name>Task 5: [BLOCKING] Schema Push — 将 schema 变更推送到数据库</name>
  <files>apps/server/prisma/schema.prisma</files>
  <read_first>
    - apps/server/prisma/schema.prisma (确认当前 schema 状态，确保 notes/tags 字段已由 Plan 01 添加)
  </read_first>
  <action>
运行 Prisma schema push 命令，将 UserTravelRecord 模型的 notes 和 tags 字段同步到数据库：

```bash
cd /Users/huangjingping/i/trip-map/apps/server && npx prisma db push
```

如果因非交互式环境需要确认，使用：
```bash
cd /Users/huangjingping/i/trip-map/apps/server && npx prisma db push --accept-data-loss
```

然后生成 Prisma Client 以确保类型同步：
```bash
cd /Users/huangjingping/i/trip-map/apps/server && npx prisma generate
```

注意：
- `db push` 用于开发环境直接推送 schema 变更（不生成 migration 文件）
- `prisma generate` 确保 Prisma Client 类型包含新增的 notes/tags 字段
- 如果 push 失败（如数据库连接问题），报告错误信息以便排查
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map/apps/server && npx prisma db push 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` 成功退出（exit code 0）
    - 输出包含 `Your database is now in sync with your Prisma schema` 或类似成功信息
    - `npx prisma generate` 成功退出
    - 无错误信息（如 connection refused、migration conflict 等）
  </acceptance_criteria>
  <done>数据库 schema 已同步（notes + tags 字段已推送），Prisma Client 已重新生成</done>
</task>

<task type="auto">
  <name>Task 6: 验证 — 全量类型检查和构建</name>
  <files>
    - packages/contracts/src/records.ts
    - apps/server/src/modules/records/records.controller.ts
    - apps/server/src/modules/records/records.service.ts
    - apps/server/src/modules/records/records.repository.ts
    - apps/server/src/modules/records/dto/update-travel-record.dto.ts
  </files>
  <read_first>
    - apps/server/src/modules/records/records.controller.ts (确认所有 import 正确)
    - apps/server/src/modules/records/records.service.ts (确认 import 和方法签名)
  </read_first>
  <action>
运行全量类型检查和构建，确保所有变更无类型错误：

```bash
cd /Users/huangjingping/i/trip-map && pnpm typecheck
```

如果 typecheck 通过，运行全量构建：
```bash
cd /Users/huangjingping/i/trip-map && pnpm build
```

如果 typecheck 失败，根据错误信息修复类型问题（常见问题：import 路径缺少 .js 后缀、类型不匹配、遗漏字段映射）。
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` 成功退出（exit code 0），无 TypeScript 错误
    - `pnpm build` 成功退出（exit code 0），所有包构建完成
    - 无 import 错误、类型不匹配错误、遗漏字段错误
  </acceptance_criteria>
  <done>全量类型检查和构建通过，所有变更类型安全</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | PATCH/DELETE 请求体和路径参数来自前端，不可信 |
| API→DB | Prisma 查询参数经过 ORM 参数化，防 SQL 注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02 | Tampering | PATCH /records/:id | mitigate | ValidationPipe + class-validator 验证：notes ≤ 1000 字符、tags ≤ 10 个/每个 ≤ 20 字符、日期 YYYY-MM-DD 格式 |
| T-36-03 | Tampering | DELETE /records/record/:id | mitigate | 行级权限检查：where 条件同时匹配 id + userId，非 owner 返回 404 |
| T-36-04 | Information Disclosure | 错误响应 | mitigate | 使用 NestJS 标准异常（BadRequestException/NotFoundException/ConflictException），不暴露 Prisma 内部错误详情 |
| T-36-05 | Elevation of Privilege | records.repository.ts | mitigate | 所有 repository 方法 where 条件包含 userId，防止越权访问/修改他人记录 |
| T-36-06 | SQL Injection | Prisma queries | accept | Prisma ORM 参数化查询，无需额外防护 |
</threat_model>

<verification>
1. `npx prisma db push` 成功（schema 已同步到数据库）
2. `pnpm typecheck` 成功（全量类型检查通过）
3. `pnpm build` 成功（全量构建通过）
4. PATCH /records/:id 端点存在（grep 确认）
5. DELETE /records/record/:id 端点存在（grep 确认）
6. 日期冲突返回 409（grep 确认 ConflictException）
7. 标签清洗逻辑存在（grep 确认 trim + dedup + filter）
</verification>

<success_criteria>
- PATCH /records/:id 端点可更新 startDate、endDate、notes、tags，返回完整 TravelRecord（含 updatedAt）
- DELETE /records/record/:id 端点可删除单条记录，返回 204
- 日期冲突返回 409，message 包含冲突提示
- notes 超过 1000 字符返回 400，tags 超过 10 个或单个超过 20 字符返回 400
- 标签自动清洗（trim + 去重 + 过滤空字符串）
- 所有端点需要 SessionAuthGuard 认证
- 全量类型检查和构建通过
- 数据库 schema 已同步
</success_criteria>

<output>
After completion, create `.planning/phases/36-data-layer-extension/36-02-SUMMARY.md`
</output>

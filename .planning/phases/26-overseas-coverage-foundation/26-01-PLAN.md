---
phase: 26-overseas-coverage-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/scripts/geo/build-geometry-manifest.mjs
  - apps/web/scripts/geo/overseas-admin1-support.mjs
  - packages/contracts/src/generated/geometry-manifest.generated.ts
  - apps/server/test/canonical-resolve.e2e-spec.ts
autonomous: true
requirements: [OVRS-01]

must_haves:
  truths:
    - "首批海外 admin1 authoritative 支持面只覆盖 `JP / KR / TH / SG / MY / AE / AU / US`，不会继续把 Natural Earth 的全球 4557 个 admin1 全量暴露给 Phase 26"
    - "`AE-X01~` / `AE-X02~`、`AU-X02~` 等噪声条目不会进入 Phase 26 的 authoritative manifest；`SG` 的 5 个 CDC 会被显式视为本阶段接受的 admin1 语义"
    - "服务端 `/places/resolve` 会继续从过滤后的 manifest 做几何命中，因此支持国家内的 admin1 点击可 resolved，支持面外的海外点击仍落 `OUTSIDE_SUPPORTED_DATA`"
  artifacts:
    - path: "apps/web/scripts/geo/overseas-admin1-support.mjs"
      provides: "Phase 26 海外 priority-country support catalog 与国家级 allowlist/denylist"
    - path: "apps/web/scripts/geo/build-geometry-manifest.mjs"
      provides: "只为首批支持国家输出海外 authoritative layer 与 manifest entry"
    - path: "packages/contracts/src/generated/geometry-manifest.generated.ts"
      provides: "server/web 共用的过滤后 authoritative overseas manifest"
    - path: "apps/server/test/canonical-resolve.e2e-spec.ts"
      provides: "priority-country resolve / unsupported regression coverage"
  key_links:
    - from: "apps/web/scripts/geo/overseas-admin1-support.mjs"
      to: "apps/web/scripts/geo/build-geometry-manifest.mjs"
      via: "priority-country filter and country-specific override application"
      pattern: "JP, KR, TH, SG, MY, AE, AU, US"
    - from: "apps/web/scripts/geo/build-geometry-manifest.mjs"
      to: "packages/contracts/src/generated/geometry-manifest.generated.ts"
      via: "generated authoritative manifest"
      pattern: "GEOMETRY_MANIFEST"
    - from: "packages/contracts/src/generated/geometry-manifest.generated.ts"
      to: "apps/server/src/modules/canonical-places/canonical-places.service.ts"
      via: "manifest-backed overseas geometry lookup"
      pattern: "GEOMETRY_MANIFEST"
---

<objective>
把 Phase 26 的海外支持面收口成 authoritative priority-country catalog：只为 `JP / KR / TH / SG / MY / AE / AU / US` 输出海外 admin1 manifest，并用真实 resolve regression 锁住 Tokyo / Gangwon / California 等支持样例与支持面外的失败语义。

Purpose: 满足 `OVRS-01` 的第一性前提，不让“前端看起来像限制了范围”而服务端/manifest 仍在偷偷支持全球海外 admin1。
Output: priority-country support catalog、过滤后的海外 geometry manifest、canonical resolve 回归。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-overseas-coverage-foundation/26-CONTEXT.md
@.planning/phases/26-overseas-coverage-foundation/26-RESEARCH.md
@.planning/phases/26-overseas-coverage-foundation/26-UI-SPEC.md
@.planning/phases/26-overseas-coverage-foundation/26-VALIDATION.md
@apps/web/scripts/geo/build-geometry-manifest.mjs
@apps/web/scripts/geo/normalize-natural-earth.mjs
@apps/web/src/data/geo/sources/ne_10m_admin_1_states_provinces.json
@packages/contracts/src/generated/geometry-manifest.generated.ts
@apps/server/src/modules/canonical-places/canonical-places.service.ts
@apps/server/test/canonical-resolve.e2e-spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 Phase 26 的海外 priority-country support catalog 与 build 过滤规则</name>
  <files>apps/web/scripts/geo/overseas-admin1-support.mjs, apps/web/scripts/geo/build-geometry-manifest.mjs, packages/contracts/src/generated/geometry-manifest.generated.ts</files>
  <read_first>
    - .planning/phases/26-overseas-coverage-foundation/26-CONTEXT.md
    - .planning/phases/26-overseas-coverage-foundation/26-RESEARCH.md
    - .planning/phases/26-overseas-coverage-foundation/26-VALIDATION.md
    - apps/web/scripts/geo/build-geometry-manifest.mjs
    - apps/web/scripts/geo/normalize-natural-earth.mjs
    - apps/web/src/data/geo/sources/ne_10m_admin_1_states_provinces.json
    - packages/contracts/src/generated/geometry-manifest.generated.ts
  </read_first>
  <action>
1. 新建 `apps/web/scripts/geo/overseas-admin1-support.mjs`，把 Phase 26 的首批支持面显式写成 catalog，而不是继续隐含在整个 Natural Earth 源里：
   - `priorityCountries = ['JP', 'KR', 'TH', 'SG', 'MY', 'AE', 'AU', 'US']`
   - `AE` 只允许标准 emirate 代码：`AE-FU`, `AE-SH`, `AE-RK`, `AE-DU`, `AE-AZ`, `AE-UQ`, `AE-AJ`，排除 `AE-X01~`, `AE-X02~`
   - `AU` 只允许标准州/领地代码：`AU-WA`, `AU-NT`, `AU-SA`, `AU-QLD`, `AU-NSW`, `AU-VIC`, `AU-TAS`, `AU-ACT`，排除 `AU-X..` 与外岛噪声条目
   - `SG` 明确接受 Natural Earth 的 5 个 CDC：`SG-01` 至 `SG-05`
   - `JP / KR / TH / MY / US` 允许各自标准 admin1 全量进入本 phase
2. 更新 `apps/web/scripts/geo/build-geometry-manifest.mjs`，让 `buildOverseasLayer()` 在写 layer 与 manifest 之前调用 Task 1 的 catalog：
   - 只保留 catalog 允许的 feature
   - 对 `United States of America` 继续规范成 `United States`
   - 保持 `displayName` 走 `name_en || name`，`typeLabel` 固定 `一级行政区`，`subtitle` 固定 `英文国家名 · 一级行政区`
3. 运行 `pnpm --filter @trip-map/web run geo:build`，提交更新后的 `packages/contracts/src/generated/geometry-manifest.generated.ts`，确保 contracts 中的 `GEOMETRY_MANIFEST` 已经只包含过滤后的 overseas 条目。
4. 不要引入“前端单独判断支持范围”的第二真源；支持面必须先由生成产物收口，再由 server/web 共同消费。
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm --filter @trip-map/web run geo:build:check && rg -n "JP|KR|TH|SG|MY|AE|AU|US|AE-X02|AU-X02|SG-05" apps/web/scripts/geo/overseas-admin1-support.mjs packages/contracts/src/generated/geometry-manifest.generated.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/scripts/geo/overseas-admin1-support.mjs` 存在
    - `apps/web/scripts/geo/overseas-admin1-support.mjs` 包含 `JP`, `KR`, `TH`, `SG`, `MY`, `AE`, `AU`, `US`
    - `apps/web/scripts/geo/overseas-admin1-support.mjs` 明确排除 `AE-X02~` 或 `AU-X02~` 类噪声代码
    - `packages/contracts/src/generated/geometry-manifest.generated.ts` 更新后仍包含 `us-california`
    - `packages/contracts/src/generated/geometry-manifest.generated.ts` 不再出现 `AE-X02~` 或 `AU-X02~`
    - `pnpm --filter @trip-map/web run geo:build:check` 返回 exit code 0
  </acceptance_criteria>
  <done>Phase 26 的海外支持面已经从“全量 Natural Earth”收口成了可审计的 priority-country authoritative catalog。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用真实 resolve regression 锁住支持样例与支持面外失败语义</name>
  <files>apps/server/test/canonical-resolve.e2e-spec.ts</files>
  <read_first>
    - apps/server/test/canonical-resolve.e2e-spec.ts
    - apps/server/src/modules/canonical-places/canonical-places.service.ts
    - packages/contracts/src/generated/geometry-manifest.generated.ts
    - apps/web/src/data/geo/sources/ne_10m_admin_1_states_provinces.json
    - .planning/phases/26-overseas-coverage-foundation/26-RESEARCH.md
    - .planning/phases/26-overseas-coverage-foundation/26-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: 支持国家内的真实 admin1 点击会 resolved，例如 `Tokyo (35.6762, 139.6503)`、`Gangwon (37.8228, 128.1555)`、`California (36.7783, -119.4179)`
    - Test 2: `AE-X..` / `AU-X..` 这类噪声条目不会被当成 supported admin1
    - Test 3: 支持面外的海外点击仍返回 `OUTSIDE_SUPPORTED_DATA`，不偷偷 resolved 到本 phase 以外的全球地区
  </behavior>
  <action>
1. 更新 `apps/server/test/canonical-resolve.e2e-spec.ts`，在现有北京/香港/California 之外增加至少两个 Phase 26 支持国家样例：
   - `Tokyo` 点击 `lat: 35.6762, lng: 139.6503`，断言 `status: 'resolved'` 且 `place.displayName` 为 `Tokyo`
   - `Gangwon` 点击 `lat: 37.8228, lng: 128.1555`，断言 `status: 'resolved'` 且 `place.placeId` 含 `kr-` 前缀
2. 为 catalog 特殊国家加边界过滤断言：
   - `Dubai` 点击 `lat: 25.2048, lng: 55.2708` 应 resolved 到 `Emirate of Dubai`
   - 不为 `AE-X01~ / AE-X02~` 或 `AU-X..` 生成 placeId / boundaryId 断言路径
3. 增加一个明确的支持面外海外点击样例，例如 `Vancouver` 或 `Toronto`，断言 `/places/resolve` 返回 `status: 'failed'`、`reason: 'OUTSIDE_SUPPORTED_DATA'`，并且响应里没有 `place` 字段。
4. 保持 `/places/resolve` 继续只从 authoritative manifest + 几何命中产出结果，不回退到 client geocoder 或 fixture-only 的海外特殊分支。
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm --filter @trip-map/server test -- test/canonical-resolve.e2e-spec.ts && pnpm --filter @trip-map/server typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 包含 `35.6762` 与 `139.6503`
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 包含 `37.8228` 与 `128.1555`
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 包含 `25.2048` 与 `55.2708`
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 继续断言支持面外点击返回 `OUTSIDE_SUPPORTED_DATA`
    - `pnpm --filter @trip-map/server test -- test/canonical-resolve.e2e-spec.ts` 返回 exit code 0
    - `pnpm --filter @trip-map/server typecheck` 返回 exit code 0
  </acceptance_criteria>
  <done>支持国家内的 authoritative resolve 与支持面外的失败语义都已经被真实点击样例锁住，Phase 26 不会再退回“看起来能点，但支持面不清楚”的状态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Natural Earth raw admin1 source → product support surface | 原始源数据包含噪声条目与超出 Phase 26 范围的全球区域，不能直接产品化 |
| generated manifest → canonical resolve service | 如果 manifest 不先过滤，server 会把超出承诺范围的海外 admin1 当成 authoritative 支持面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | overseas support catalog / manifest generation | mitigate | 用显式 priority-country allowlist + `AE/AU` 特例过滤生成 authoritative overseas manifest |
| T-26-04 | Repudiation | canonical resolve support scope | mitigate | 用 Tokyo / Gangwon / Dubai / out-of-scope 点击回归，明确“支持哪些国家、拒绝哪些区域” |
</threat_model>

<verification>
- `pnpm --filter @trip-map/web run geo:build:check`
- `pnpm --filter @trip-map/web run geo:build`
- `pnpm --filter @trip-map/contracts build`
- `pnpm --filter @trip-map/server test -- test/canonical-resolve.e2e-spec.ts`
- `pnpm --filter @trip-map/server typecheck`
</verification>

<success_criteria>
- authoritative overseas manifest 只覆盖首批 8 个 priority countries
- `AE-X..` / `AU-X..` 噪声条目不再进入 Phase 26 支持面
- 至少两个非 California 的真实海外 admin1 resolve 样例通过 e2e
</success_criteria>

<output>
After completion, create `.planning/phases/26-overseas-coverage-foundation/26-01-SUMMARY.md`
</output>

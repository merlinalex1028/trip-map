---
phase: 26-overseas-coverage-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/server/src/modules/canonical-places/place-metadata-catalog.ts
  - apps/server/src/modules/canonical-places/canonical-places.service.ts
  - apps/server/src/modules/records/records.service.ts
  - apps/server/scripts/backfill-record-metadata.ts
  - apps/server/test/auth-bootstrap.e2e-spec.ts
  - apps/server/test/records-travel.e2e-spec.ts
  - apps/server/test/records-sync.e2e-spec.ts
  - apps/web/src/stores/map-points.spec.ts
autonomous: true
requirements: [OVRS-02]

must_haves:
  truths:
    - "海外 admin1 的 `displayName` / `typeLabel` / `parentLabel` / `subtitle` 只在 authoritative catalog 中确定一次，保存后通过 `UserTravelRecord` 与 `/auth/bootstrap` 原样回放"
    - "旧的 metadata backfill 不再依赖 4 个 Phase 12 fixture，而是从当前 manifest-backed place catalog 构建 lookup"
    - "server `/records` 对 `OVERSEAS_ADMIN1` 写入也会复用同一 authoritative catalog 校验 `placeId/boundaryId/文本字段`，不会因为伪造 payload 把 out-of-scope 地点落库"
    - "前端展示层继续消费持久化字段，不在 bootstrap 或 popup 阶段按 `placeId` 重算海外标题、副标题或类型标签"
  artifacts:
    - path: "apps/server/src/modules/canonical-places/place-metadata-catalog.ts"
      provides: "manifest-backed overseas canonical metadata lookup reusable by service and scripts"
    - path: "apps/server/scripts/backfill-record-metadata.ts"
      provides: "fixture-free metadata backfill for existing overseas records"
    - path: "apps/server/src/modules/records/records.service.ts"
      provides: "manifest-backed validation for overseas record create/import"
    - path: "apps/server/test/auth-bootstrap.e2e-spec.ts"
      provides: "overseas record text replay verification across bootstrap"
    - path: "apps/server/test/records-travel.e2e-spec.ts"
      provides: "out-of-scope overseas payload rejection and backfill verification"
    - path: "apps/server/test/records-sync.e2e-spec.ts"
      provides: "same-user multi-session overseas record consistency verification"
    - path: "apps/web/src/stores/map-points.spec.ts"
      provides: "record-to-display replay regression on persisted overseas text fields"
  key_links:
    - from: "apps/server/src/modules/canonical-places/place-metadata-catalog.ts"
      to: "apps/server/src/modules/records/records.service.ts"
      via: "authoritative support and metadata validation for POST /records and /records/import"
      pattern: "placeId/boundaryId/displayName/typeLabel/parentLabel/subtitle"
    - from: "apps/server/src/modules/canonical-places/place-metadata-catalog.ts"
      to: "apps/server/src/modules/canonical-places/canonical-places.service.ts"
      via: "shared authoritative metadata lookup"
      pattern: "placeId -> displayName/typeLabel/parentLabel/subtitle"
    - from: "apps/server/src/modules/canonical-places/place-metadata-catalog.ts"
      to: "apps/server/scripts/backfill-record-metadata.ts"
      via: "manifest-backed backfill input"
      pattern: "buildCanonicalMetadataLookup"
    - from: "apps/server/test/auth-bootstrap.e2e-spec.ts"
      to: "apps/web/src/stores/map-points.spec.ts"
      via: "bootstrap replayed text is rendered without recomputation"
      pattern: "displayName|typeLabel|subtitle"
---

<objective>
把 Phase 26 的海外文本稳定性做成真正的“持久化字段真源”闭环：复用 manifest-backed canonical metadata catalog，覆盖旧记录 backfill、`/auth/bootstrap` 回放和前端 `recordToDisplayPoint()` 展示，确保海外记录在刷新、重开、跨设备后标题/类型/副标题完全一致。

Purpose: 满足 `OVRS-02`，把“海外点亮后看起来正确”升级为“保存后任何回放路径都稳定一致”。
Output: reusable canonical metadata catalog、manifest-driven backfill、bootstrap + sync + store regressions。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-overseas-coverage-foundation/26-CONTEXT.md
@.planning/phases/26-overseas-coverage-foundation/26-RESEARCH.md
@.planning/phases/26-overseas-coverage-foundation/26-VALIDATION.md
@.planning/phases/26-overseas-coverage-foundation/26-01-PLAN.md
@apps/server/src/modules/canonical-places/canonical-places.service.ts
@apps/server/src/modules/auth/auth.service.ts
@apps/server/src/modules/records/records.repository.ts
@apps/server/scripts/backfill-record-metadata.ts
@apps/server/test/auth-bootstrap.e2e-spec.ts
@apps/server/test/records-sync.e2e-spec.ts
@apps/web/src/stores/map-points.ts
@apps/web/src/stores/map-points.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 提取 manifest-backed canonical metadata catalog 并替换 fixture-bound backfill</name>
  <files>apps/server/src/modules/canonical-places/place-metadata-catalog.ts, apps/server/src/modules/canonical-places/canonical-places.service.ts, apps/server/src/modules/records/records.service.ts, apps/server/scripts/backfill-record-metadata.ts, apps/server/test/records-travel.e2e-spec.ts</files>
  <read_first>
    - apps/server/src/modules/canonical-places/canonical-places.service.ts
    - apps/server/src/modules/records/records.service.ts
    - apps/server/src/modules/records/records.controller.ts
    - apps/server/scripts/backfill-record-metadata.ts
    - apps/server/src/modules/auth/auth.service.ts
    - apps/server/src/modules/records/records.repository.ts
    - apps/server/test/records-travel.e2e-spec.ts
    - packages/contracts/src/generated/geometry-manifest.generated.ts
    - .planning/phases/26-overseas-coverage-foundation/26-RESEARCH.md
  </read_first>
  <action>
1. 从 `apps/server/src/modules/canonical-places/canonical-places.service.ts` 中提取一个可复用的 manifest-backed metadata loader，例如 `apps/server/src/modules/canonical-places/place-metadata-catalog.ts`：
   - 继续从 geometry shard + `GEOMETRY_MANIFEST` 读取 `placeId`, `displayName`, `typeLabel`, `parentLabel`, `subtitle`, `regionSystem`, `adminType`
   - 暴露 `buildCanonicalMetadataLookup()` / `getCanonicalPlaceSummaryById()` 一类 helper，供服务端 runtime 和脚本共用
2. 更新 `apps/server/scripts/backfill-record-metadata.ts`：
   - 移除基于 `PHASE12_RESOLVED_*` 常量的 `CANONICAL_PLACE_CATALOG`
   - 改为调用 Task 1 的 manifest-backed lookup
   - 继续只回填 `regionSystem`, `adminType`, `typeLabel`, `parentLabel`, `subtitle`，不要引入 schema 变更
3. 更新 `apps/server/src/modules/records/records.service.ts`，为 `createTravel()` 与 `importTravel()` 加入 manifest-backed authoritative 校验：
   - 对 `OVERSEAS_ADMIN1` payload，要求 `placeId` 与 `boundaryId` 都能在 Task 1 的 catalog 中找到
   - 要求提交的 `displayName`, `regionSystem`, `adminType`, `typeLabel`, `parentLabel`, `subtitle` 与 authoritative metadata 完全一致
   - 对 out-of-scope 或 metadata 不匹配 payload 抛 `BadRequestException`，明确拒绝不在 Phase 26 支持面内的海外写入
4. 更新 `apps/server/src/modules/canonical-places/canonical-places.service.ts` 复用 Task 1 的 catalog helper，而不是保留另一套平行 metadata 组装逻辑。
5. 更新 `apps/server/test/records-travel.e2e-spec.ts`，补两类自动化验证：
   - 一个 out-of-scope overseas payload（例如 `ca-british-columbia`）对 `POST /records` 返回 400
   - 一个 backfill regression，证明 `backfillRecordMetadata()` 从 manifest-backed catalog 回填，而不是从 `PHASE12_RESOLVED_*` fixture lookup 回填
6. 保持 `AuthService.bootstrap()` 与 `RecordsRepository.createTravelRecord()` 的职责边界不变：bootstrap 只回放 `UserTravelRecord` 字段，repository 继续把通过 authoritative 校验的文本完整 upsert。
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm --filter @trip-map/server test -- test/records-travel.e2e-spec.ts && pnpm --filter @trip-map/server typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/modules/canonical-places/place-metadata-catalog.ts` 存在
    - `apps/server/scripts/backfill-record-metadata.ts` 不再引用 `PHASE12_RESOLVED_BEIJING` 或其他 `PHASE12_RESOLVED_*`
    - `apps/server/scripts/backfill-record-metadata.ts` 通过 Task 1 的 metadata catalog helper 获取 lookup
    - `apps/server/src/modules/records/records.service.ts` 对 out-of-scope overseas payload 抛 `BadRequestException`
    - `apps/server/test/records-travel.e2e-spec.ts` 断言 `POST /records` 拒绝不在支持面内的 overseas payload
    - `apps/server/test/records-travel.e2e-spec.ts` 断言 backfill 走 manifest-backed lookup，而不是 `PHASE12_RESOLVED_*`
    - `pnpm --filter @trip-map/server test -- test/records-travel.e2e-spec.ts` 返回 exit code 0
    - `pnpm --filter @trip-map/server typecheck` 返回 exit code 0
  </acceptance_criteria>
  <done>旧记录 metadata 修复和 `/records` 海外写入校验都已经切到同一个 manifest-backed canonical catalog，Phase 26 不再存在“支持面外 payload 仍能落库”的后门。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁海外记录跨 bootstrap / 跨设备 / 前端回放的一致性回归</name>
  <files>apps/server/test/auth-bootstrap.e2e-spec.ts, apps/server/test/records-sync.e2e-spec.ts, apps/web/src/stores/map-points.spec.ts</files>
  <read_first>
    - apps/server/test/auth-bootstrap.e2e-spec.ts
    - apps/server/test/records-sync.e2e-spec.ts
    - apps/web/src/stores/map-points.ts
    - apps/web/src/stores/map-points.spec.ts
    - apps/server/src/modules/auth/auth.service.ts
    - apps/server/src/modules/records/records.repository.ts
    - .planning/phases/26-overseas-coverage-foundation/26-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `/auth/bootstrap` 对已保存的海外记录回放 `displayName`, `typeLabel`, `subtitle` 时保持原样，例如 `Tokyo` / `Japan · 一级行政区`
    - Test 2: same-user 双 session 对海外记录 create 后重新 bootstrap 时，A/B 会话看到完全相同的文本字段
    - Test 3: `recordToDisplayPoint()` 继续使用记录中持久化的文本字段，而不是按 `placeId` 二次推导
  </behavior>
  <action>
1. 更新 `apps/server/test/auth-bootstrap.e2e-spec.ts`，新增一个海外记录样例，例如：
   - `placeId: 'jp-tokyo'`
   - `displayName: 'Tokyo'`
   - `regionSystem: 'OVERSEAS'`
   - `adminType: 'ADMIN1'`
   - `typeLabel: '一级行政区'`
   - `parentLabel: 'Japan'`
   - `subtitle: 'Japan · 一级行政区'`
   断言 `/auth/bootstrap` 原样返回这些字段。
2. 更新 `apps/server/test/records-sync.e2e-spec.ts`：
   - 用一个海外 payload（如 `jp-tokyo` 或 `kr-gangwon`）替换/补充现有 CN-only payload
   - 断言 session A 创建后，session B 通过 `/auth/bootstrap` 看到相同的 `displayName`, `typeLabel`, `subtitle`
3. 更新 `apps/web/src/stores/map-points.spec.ts`，增加 overseas `TravelRecord` 样例，断言：
   - `recordToDisplayPoint()` 的 `name === displayName`
   - `cityContextLabel === subtitle`
   - `typeLabel === typeLabel`
   - store 不会根据 `placeId` 重新拼 `Japan · 一级行政区`
4. 不要在 `auth.service.ts` 或 `map-points.ts` 新增“如果是海外就重算 title/subtitle”的分支；如果测试需要数据样例，只扩展测试 helper 和输入 payload。
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm --filter @trip-map/server test -- test/auth-bootstrap.e2e-spec.ts test/records-sync.e2e-spec.ts && pnpm --filter @trip-map/web test -- src/stores/map-points.spec.ts && pnpm --filter @trip-map/server typecheck && pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/test/auth-bootstrap.e2e-spec.ts` 包含 `jp-tokyo` 或 `kr-gangwon`
    - `apps/server/test/auth-bootstrap.e2e-spec.ts` 明确断言 `displayName`, `typeLabel`, `subtitle`
    - `apps/server/test/records-sync.e2e-spec.ts` 对海外 payload 断言多 session bootstrap 文本一致
    - `apps/web/src/stores/map-points.spec.ts` 对 overseas record 断言 `name`, `cityContextLabel`, `typeLabel` 直接来自 record
    - `pnpm --filter @trip-map/server test -- test/auth-bootstrap.e2e-spec.ts test/records-sync.e2e-spec.ts` 返回 exit code 0
    - `pnpm --filter @trip-map/web test -- src/stores/map-points.spec.ts` 返回 exit code 0
  </acceptance_criteria>
  <done>海外记录的标题、类型标签和副标题已经在 server bootstrap、多设备同步和前端回放三条路径上同时锁定一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoritative canonical metadata → persisted UserTravelRecord | 如果 metadata 只存在于一次 resolve 响应里，刷新和跨设备就会漂移 |
| legacy fixture lookup → backfill script | 若继续依赖 4 个 fixture，Phase 26 新增国家的旧记录无法被修复 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Tampering | persisted overseas record metadata | mitigate | 通过 shared manifest-backed metadata catalog 固定 `displayName/typeLabel/parentLabel/subtitle` 真源 |
| T-26-07 | Tampering | `/records` overseas create/import payload | mitigate | `/records` 对 `OVERSEAS_ADMIN1` 继续走 authoritative metadata/support validation，拒绝 out-of-scope 或文本被伪造的 payload |
| T-26-05 | Repudiation | bootstrap / multi-session replay semantics | mitigate | 用 auth-bootstrap + records-sync + map-points regressions 锁住 server replay 与 frontend rendering 一致性 |
</threat_model>

<verification>
- `pnpm --filter @trip-map/server test -- test/records-travel.e2e-spec.ts`
- `pnpm --filter @trip-map/server test -- test/auth-bootstrap.e2e-spec.ts test/records-sync.e2e-spec.ts`
- `pnpm --filter @trip-map/web test -- src/stores/map-points.spec.ts`
- `pnpm --filter @trip-map/server typecheck`
- `pnpm --filter @trip-map/web typecheck`
- 如本地有旧记录数据，执行 `pnpm --filter @trip-map/server exec tsx scripts/backfill-record-metadata.ts` 并检查 unmatched 列表
</verification>

<success_criteria>
- overseas `displayName/typeLabel/subtitle` 已由 shared canonical metadata catalog 锁定
- `/records` 已拒绝不在 Phase 26 支持面内或 metadata 被伪造的 overseas payload
- `backfill-record-metadata.ts` 不再绑定 4-fixture lookup
- bootstrap、多 session 与前端 store 回放都证明海外文本字段保持一致
</success_criteria>

<output>
After completion, create `.planning/phases/26-overseas-coverage-foundation/26-02-SUMMARY.md`
</output>

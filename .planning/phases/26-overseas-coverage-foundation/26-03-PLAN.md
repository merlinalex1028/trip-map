---
phase: 26-overseas-coverage-foundation
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/web/src/constants/overseas-support.ts
  - apps/web/src/components/LeafletMapStage.vue
  - apps/web/src/components/LeafletMapStage.spec.ts
  - apps/web/src/components/map-popup/PointSummaryCard.spec.ts
autonomous: true
requirements: [OVRS-01, OVRS-03]

must_haves:
  truths:
    - "已识别但不在 Phase 26 支持面内的海外区域会在当前 popup 内得到清楚解释，而不是再走全局 `interactionNotice` 主路径"
    - "单一明确的海外 canonical 命中仍直接进入正常 popup/点亮链路；只有 `ambiguous` 才显示 candidate-select"
    - "未支持地区的点亮按钮继续保持同一位置但禁用，`title/aria-label` 与 popup notice 都明确指向“该地区暂不支持点亮”"
  artifacts:
    - path: "apps/web/src/constants/overseas-support.ts"
      provides: "首批支持国家名单与 unsupported 文案 builder"
    - path: "apps/web/src/components/LeafletMapStage.vue"
      provides: "OUTSIDE_SUPPORTED_DATA -> popup fallback notice 路径与 illuminate gating semantics"
    - path: "apps/web/src/components/LeafletMapStage.spec.ts"
      provides: "unsupported popup-only feedback and non-ambiguous direct-resolve regressions"
    - path: "apps/web/src/components/map-popup/PointSummaryCard.spec.ts"
      provides: "disabled unsupported CTA contract verification"
  key_links:
    - from: "apps/web/src/constants/overseas-support.ts"
      to: "apps/web/src/components/LeafletMapStage.vue"
      via: "unsupported notice copy and supported-country list"
      pattern: "日本、韩国、泰国、新加坡、马来西亚、阿联酋、澳大利亚、美国"
    - from: "apps/web/src/components/LeafletMapStage.vue"
      to: "apps/web/src/components/map-popup/PointSummaryCard.vue"
      via: "fallbackNotice + disabled illuminate button semantics"
      pattern: "fallbackNotice|isIlluminatable"
    - from: "apps/web/src/components/LeafletMapStage.spec.ts"
      to: "apps/web/src/components/map-popup/PointSummaryCard.spec.ts"
      via: "popup-only unsupported explanation without global notice"
      pattern: "该地点暂不支持点亮"
---

<objective>
把 Phase 26 的未支持地区反馈做成贴在 popup 上下文里的可解释路径：`OUTSIDE_SUPPORTED_DATA` 时继续显示识别结果，但用明确的 unsupported notice 和禁用按钮说明“识别到了，但当前不支持点亮”，同时保持 ambiguous-only 的候选确认节奏。

Purpose: 满足 `OVRS-03`，并把 `OVRS-01` 的海外交互落实成“支持地区直接点亮、未支持地区可解释、模糊命中才确认”的单一路径。
Output: popup-only unsupported feedback、supported-country copy constant、LeafletMapStage / PointSummaryCard regressions。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-overseas-coverage-foundation/26-CONTEXT.md
@.planning/phases/26-overseas-coverage-foundation/26-RESEARCH.md
@.planning/phases/26-overseas-coverage-foundation/26-UI-SPEC.md
@.planning/phases/26-overseas-coverage-foundation/26-VALIDATION.md
@.planning/phases/26-overseas-coverage-foundation/26-01-PLAN.md
@apps/web/src/components/LeafletMapStage.vue
@apps/web/src/components/LeafletMapStage.spec.ts
@apps/web/src/components/map-popup/PointSummaryCard.vue
@apps/web/src/components/map-popup/PointSummaryCard.spec.ts
@apps/web/src/services/geo-lookup.ts
@apps/web/src/stores/map-points.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把 unsupported 海外反馈收敛到 popup notice，而不是全局 interactionNotice</name>
  <files>apps/web/src/constants/overseas-support.ts, apps/web/src/components/LeafletMapStage.vue, apps/web/src/components/LeafletMapStage.spec.ts</files>
  <read_first>
    - apps/web/src/components/LeafletMapStage.vue
    - apps/web/src/components/LeafletMapStage.spec.ts
    - apps/web/src/services/geo-lookup.ts
    - apps/web/src/stores/map-points.ts
    - .planning/phases/26-overseas-coverage-foundation/26-CONTEXT.md
    - .planning/phases/26-overseas-coverage-foundation/26-UI-SPEC.md
  </read_first>
  <action>
1. 新建 `apps/web/src/constants/overseas-support.ts`，集中声明：
   - Phase 26 支持国家中文列表：`日本、韩国、泰国、新加坡、马来西亚、阿联酋、澳大利亚、美国`
   - 一个 builder，例如 `buildUnsupportedOverseasNotice(regionName?: string)`，输出两句固定文案：
     - 第一行：`已识别到${regionName ?? '该地区'}。`
     - 第二行：`当前暂不支持点亮，请优先在日本、韩国、泰国、新加坡、马来西亚、阿联酋、澳大利亚、美国的一级行政区使用。`
2. 更新 `apps/web/src/components/LeafletMapStage.vue`：
   - 当 `/places/resolve` 返回 `OUTSIDE_SUPPORTED_DATA` 且 `lookupCountryRegionByCoordinates()` 命中海外地区时，把上面的 unsupported notice 写进 fallback draft point 的 `fallbackNotice`
   - 保留 popup 打开与 `buildFallbackDraftPoint()` 路径，不新增全局 toast 或新页面
   - 删除或停用 `handleIlluminate()` 中针对 `!isActivePointIlluminatable` 的 `setInteractionNotice({ message: '该地点暂不支持点亮' })` 主路径，改成直接返回，让 popup notice + disabled button 作为唯一说明
3. 更新 `apps/web/src/components/LeafletMapStage.spec.ts`：
   - 把现有“emit illuminate 后出现全局 `该地点暂不支持点亮` notice”的断言改成：popup draft point 带有 unsupported `fallbackNotice`，且 `mapUiStore.interactionNotice` 为空
   - 新增单一明确 overseas resolved 命中时不进入 `candidate-select` 的断言，确保 `ambiguous` 仍是唯一确认入口
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts && rg -n "日本、韩国、泰国、新加坡、马来西亚、阿联酋、澳大利亚、美国|OUTSIDE_SUPPORTED_DATA|candidate-select|interactionNotice" apps/web/src/constants/overseas-support.ts apps/web/src/components/LeafletMapStage.vue apps/web/src/components/LeafletMapStage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/constants/overseas-support.ts` 存在
    - `apps/web/src/constants/overseas-support.ts` 包含 `日本、韩国、泰国、新加坡、马来西亚、阿联酋、澳大利亚、美国`
    - `apps/web/src/components/LeafletMapStage.vue` 在 `OUTSIDE_SUPPORTED_DATA` 分支写入 unsupported `fallbackNotice`
    - `apps/web/src/components/LeafletMapStage.vue` 不再把 unsupported 说明作为全局 `interactionNotice` 主路径
    - `apps/web/src/components/LeafletMapStage.spec.ts` 断言 unsupported 说明停留在 popup
    - `apps/web/src/components/LeafletMapStage.spec.ts` 断言单一 resolved overseas 命中不进入 `candidate-select`
    - `pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts` 返回 exit code 0
  </acceptance_criteria>
  <done>未支持地区的反馈已经回到 popup 上下文里，用户能直接理解“识别到了，但当前不支持点亮”，而不是被全局 notice 打断。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁 disabled CTA 与 ambiguous-only 候选确认契约</name>
  <files>apps/web/src/components/map-popup/PointSummaryCard.spec.ts, apps/web/src/components/LeafletMapStage.spec.ts</files>
  <read_first>
    - apps/web/src/components/map-popup/PointSummaryCard.vue
    - apps/web/src/components/map-popup/PointSummaryCard.spec.ts
    - apps/web/src/components/LeafletMapStage.spec.ts
    - .planning/phases/26-overseas-coverage-foundation/26-UI-SPEC.md
    - .planning/phases/26-overseas-coverage-foundation/26-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: unsupported 地点的按钮继续显示在标题区，但保持 disabled，并带 `title/aria-label = 该地点暂不支持点亮`
    - Test 2: popup notice 的 unsupported 文案先于 boundary-missing notice 出现，不被“仅保存文本信息”类说明覆盖
    - Test 3: `candidate-select` 只在 server 返回 `ambiguous` 时出现；单一 `resolved` 海外命中仍直接展示详情并允许点亮
  </behavior>
  <action>
1. 更新 `apps/web/src/components/map-popup/PointSummaryCard.spec.ts`：
   - 保留现有 `isIlluminatable=false` 的 disabled 按钮断言
   - 补充断言 `title` 与 `aria-label` 都是 `该地点暂不支持点亮`
   - 用一个带 unsupported `fallbackNotice` 且 `boundarySupportState='missing'` 的 surface，断言 unsupported notice 先渲染，`boundarySupportNotice` 仍作为第二条说明
2. 补强 `apps/web/src/components/LeafletMapStage.spec.ts`：
   - 一个 `resolveCanonicalPlace -> ambiguous` 场景断言仍进入 `candidate-select`
   - 一个 `resolveCanonicalPlace -> resolved` 的 overseas 场景断言直接进入普通详情模式，并且 illuminate 按钮不被平白隐藏
3. 不改动 `PointSummaryCard.vue` 的整体视觉结构；只允许为了满足 `aria-label`、notice 顺序或测试契约做最小行为修正。
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/trip-map && pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts src/components/map-popup/PointSummaryCard.spec.ts && pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/map-popup/PointSummaryCard.spec.ts` 断言 `title` 与 `aria-label` 都为 `该地点暂不支持点亮`
    - `apps/web/src/components/map-popup/PointSummaryCard.spec.ts` 断言 unsupported notice 与 boundary-missing notice 的共存顺序
    - `apps/web/src/components/LeafletMapStage.spec.ts` 保留 `ambiguous -> candidate-select` 断言
    - `apps/web/src/components/LeafletMapStage.spec.ts` 新增 `resolved overseas -> normal detail surface` 断言
    - `pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts src/components/map-popup/PointSummaryCard.spec.ts` 返回 exit code 0
    - `pnpm --filter @trip-map/web typecheck` 返回 exit code 0
  </acceptance_criteria>
  <done>unsupported CTA 与 ambiguous-only 候选确认契约已经被组件和集成测试同时锁住，Phase 26 的海外交互节奏不会回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server resolve failure → popup interaction | 如果 unsupported 反馈跑到全局 notice，用户会误解为点击失效或通用错误 |
| candidate-select flow → normal overseas resolve flow | 若单一 resolved 命中也被强制确认，会把每次海外点亮都变成额外负担 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Repudiation | unsupported overseas feedback | mitigate | 把 unsupported 解释收敛到 popup `fallbackNotice` + disabled CTA，不再以全局 notice 为主 |
| T-26-06 | Denial of Service | candidate-select overuse | mitigate | 只在 `ambiguous` 时显示候选确认，单一 resolved 命中继续直接进入正常点亮路径 |
</threat_model>

<verification>
- `pnpm --filter @trip-map/web test -- src/components/LeafletMapStage.spec.ts src/components/map-popup/PointSummaryCard.spec.ts`
- `pnpm --filter @trip-map/web typecheck`
- 手动验证：点击一个支持国家 admin1 与一个不支持国家地区，确认前者可直接点亮、后者只在 popup 内看到 unsupported 说明且页面不弹全局 notice
</verification>

<success_criteria>
- unsupported 海外区域只在 popup 内解释，不再依赖全局 `interactionNotice`
- disabled CTA 与 supported-country 文案符合 UI-SPEC
- ambiguous-only 候选确认节奏被测试锁定
</success_criteria>

<output>
After completion, create `.planning/phases/26-overseas-coverage-foundation/26-03-SUMMARY.md`
</output>

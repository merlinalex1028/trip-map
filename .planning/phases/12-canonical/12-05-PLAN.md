---
phase: 12-canonical
plan: 05
type: execute
wave: 4
depends_on:
  - 12-02
  - 12-03
  - 12-04
files_modified:
  - apps/web/src/services/city-boundaries.ts
  - apps/web/src/services/city-boundaries.spec.ts
  - apps/web/src/stores/map-points.ts
  - apps/web/src/stores/map-points.spec.ts
  - apps/web/src/components/WorldMapStage.spec.ts
  - apps/web/src/components/PointPreviewDrawer.spec.ts
autonomous: true
requirements:
  - ARC-02
  - PLC-01
  - PLC-02
  - PLC-03
  - PLC-04
  - PLC-05
  - UIX-04
gap_closure: true
must_haves:
  truths:
    - "server 返回的 canonical boundaryId 在 web 侧要么能映射到现有可渲染几何，要么被明确判定为当前无边界支持。"
    - "popup、drawer、已保存记录与地图高亮的 boundary support 状态基于真实几何命中，而不是 boundaryId 非空。"
    - "用户关闭再重开已保存 canonical 点位后，若当前 web 数据集有对应几何，则地图仍恢复同一条边界高亮；若没有，则 UI 明确显示缺失提示。"
  artifacts:
    - "apps/web/src/services/city-boundaries.ts 提供 canonical boundaryId 到当前 renderable boundaryId 的显式解析入口。"
    - "apps/web/src/stores/map-points.ts 用真实几何命中结果派生 `activeBoundaryCoverageState`。"
    - "apps/web/src/components/WorldMapStage.spec.ts` 锁定 canonical boundary highlight 与 reopened canonical point 回归。"
    - "apps/web/src/components/PointPreviewDrawer.spec.ts` 锁定 canonical unsupported-boundary 提示不再靠手工注入 `supported`。"
  key_links:
    - "apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts 的 canonical boundaryId 必须通过 apps/web/src/services/city-boundaries.ts 被映射或显式判定 unsupported。"
    - "apps/web/src/stores/map-points.ts 的 `activeBoundaryCoverageState` 必须调用 city-boundaries 的真实命中 helper，而不是只判断 `boundaryId` 非空。"
    - "apps/web/src/components/WorldMapStage.spec.ts 必须断言 canonical saved/reopen 流程最终渲染的是当前 web 数据集的 boundaryId，例如 `cn-beijing-municipality`。"
---

<objective>
补齐 Phase 12 的 canonical boundary highlight 缺口：在不改写已交付 authoritative resolve、canonical placeId 持久化和 popup/drawer 类型语义的前提下，让 web 侧真正理解哪些 canonical `boundaryId` 现在可画、哪些只能明确提示 unsupported。

Purpose: 关闭 `PLC-05` 的剩余 blocker，让 canonical identity、边界恢复和用户反馈在当前 web 数据集下重新连通。
Output: boundaryId 映射层、基于真实 geometry 的 support state，以及 canonical highlight/reopen 回归测试。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-canonical/12-CONTEXT.md
@.planning/phases/12-canonical/12-RESEARCH.md
@.planning/phases/12-canonical/12-VERIFICATION.md
@.planning/phases/12-canonical/12-02-SUMMARY.md
@.planning/phases/12-canonical/12-03-SUMMARY.md
@.planning/phases/12-canonical/12-04-SUMMARY.md
@apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts
@apps/web/src/services/city-boundaries.ts
@apps/web/src/stores/map-points.ts
@apps/web/src/components/WorldMapStage.spec.ts
@apps/web/src/components/PointPreviewDrawer.spec.ts

<interfaces>
Current web boundary lookup shape from `apps/web/src/services/city-boundaries.ts`:

```ts
export function hasBoundaryCoverageForCityId(cityId: string): boolean
export function getBoundaryById(boundaryId: string): NormalizedCityBoundary | null
export function getBoundaryByCityId(cityId: string): NormalizedCityBoundary | null
```

Current broken store derivation from `apps/web/src/stores/map-points.ts`:

```ts
const activeBoundaryCoverageState = computed<'supported' | 'missing' | 'not-applicable'>(() => {
  if (!activePoint.value) return 'not-applicable'
  if (activePoint.value.boundaryId) return 'supported'
  if (hasCanonicalIdentity(activePoint.value)) return 'missing'
  if (activePoint.value.cityId && hasBoundaryCoverageForCityId(activePoint.value.cityId)) return 'supported'
  return 'not-applicable'
})
```

Server fixture ids called out by verification:

```ts
'datav-cn-beijing'
'datav-cn-hong-kong'
'datav-cn-aba'
'ne-admin1-us-california'
```

Current renderable ids already present in `apps/web/src/data/geo/city-boundaries.geo.json`:

```ts
'cn-beijing-municipality'
'hk-hong-kong-island-cluster'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 canonical boundaryId 映射层，并让 support state 以真实几何命中为准</name>
  <files>apps/web/src/services/city-boundaries.ts, apps/web/src/services/city-boundaries.spec.ts, apps/web/src/stores/map-points.ts, apps/web/src/stores/map-points.spec.ts</files>
  <read_first>
    - .planning/phases/12-canonical/12-VERIFICATION.md
    - .planning/phases/12-canonical/12-03-SUMMARY.md
    - apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts
    - apps/web/src/services/city-boundaries.ts
    - apps/web/src/services/city-boundaries.spec.ts
    - apps/web/src/stores/map-points.ts
    - apps/web/src/stores/map-points.spec.ts
  </read_first>
  <behavior>
    - Test 1: `getBoundaryById('datav-cn-beijing')` 命中 `cn-beijing-municipality`，`getBoundaryById('datav-cn-hong-kong')` 命中 `hk-hong-kong-island-cluster`。
    - Test 2: `getBoundaryById('datav-cn-aba')`、`getBoundaryById('datav-cn-tianjin')`、`getBoundaryById('ne-admin1-us-california')` 返回 `null`，明确表示当前 web 数据集没有可渲染几何。
    - Test 3: store 中 canonical 点位只有在 `getBoundaryById(boundaryId)` 真正命中几何时才是 `supported`；canonical `boundaryId` 非空但无映射或无几何时必须是 `missing`。
  </behavior>
  <action>
    在 `apps/web/src/services/city-boundaries.ts` 中新增精确常量 `CANONICAL_BOUNDARY_ID_TO_RENDERABLE_ID`，并至少写入以下键值，作为对 12-VERIFICATION gap 的显式修复：
    - `'datav-cn-beijing': 'cn-beijing-municipality'`
    - `'datav-cn-hong-kong': 'hk-hong-kong-island-cluster'`
    - `'datav-cn-aba': null`
    - `'datav-cn-tianjin': null`
    - `'datav-cn-langfang': null`
    - `'ne-admin1-us-ca': null`
    - `'ne-admin1-us-california': null`

    同文件新增两个导出 helper，名称必须精确为：
    - `resolveRenderableBoundaryId(boundaryId: string): string | null`
    - `hasBoundaryCoverageForBoundaryId(boundaryId: string | null | undefined): boolean`

    修改 `getBoundaryById(boundaryId)`，先通过 `resolveRenderableBoundaryId(boundaryId)` 把 canonical id 解析到当前 web 可渲染 id，再查 `boundaryFeatureById`；若解析结果为 `null`，直接返回 `null`。保留 `getBoundaryByCityId(cityId)` 与 `hasBoundaryCoverageForCityId(cityId)` 的既有 Phase 08/legacy 行为，不要改动 curated city coverage audit。

    在 `apps/web/src/stores/map-points.ts` 中，用 `hasBoundaryCoverageForBoundaryId(activePoint.value.boundaryId)` 替换当前“`boundaryId` 非空就 `supported`”的逻辑，落实 D-09 与 verification 缺口：
    - canonical point 有 `placeId/placeKind` 但当前 boundary helper 返回 `false` 时，`activeBoundaryCoverageState` 必须为 `missing`
    - legacy city point 仍可继续使用 `cityId + hasBoundaryCoverageForCityId(cityId)` 兜底
    - 不要修改 persisted canonical `boundaryId` 字段本身；保持 `placeId`、`boundaryId`、`datasetVersion` 的持久化合同不变，满足 PLC-02/PLC-03

    在 `apps/web/src/services/city-boundaries.spec.ts` 中新增显式断言上述 mapping 和 unsupported ids；在 `apps/web/src/stores/map-points.spec.ts` 中新增两个 store regression：
    - `PHASE12_RESOLVED_BEIJING` draft/save 后 `activeBoundaryCoverageState === 'supported'`
    - `PHASE12_RESOLVED_CALIFORNIA` draft/save 后 `activeBoundaryCoverageState === 'missing'`
  </action>
  <acceptance_criteria>
    - `apps/web/src/services/city-boundaries.ts` 包含精确字符串 `CANONICAL_BOUNDARY_ID_TO_RENDERABLE_ID`。
    - `apps/web/src/services/city-boundaries.ts` 包含精确字符串 `'datav-cn-beijing': 'cn-beijing-municipality'`。
    - `apps/web/src/services/city-boundaries.ts` 包含精确字符串 `'datav-cn-hong-kong': 'hk-hong-kong-island-cluster'`。
    - `apps/web/src/services/city-boundaries.ts` 包含精确字符串 `'ne-admin1-us-california': null`。
    - `apps/web/src/services/city-boundaries.ts` 包含精确字符串 `export function hasBoundaryCoverageForBoundaryId`。
    - `apps/web/src/stores/map-points.ts` 不再包含精确字符串 `if (activePoint.value.boundaryId) {`.
    - `apps/web/src/stores/map-points.ts` 包含精确字符串 `hasBoundaryCoverageForBoundaryId(activePoint.value.boundaryId)`。
    - `apps/web/src/stores/map-points.spec.ts` 包含精确字符串 `activeBoundaryCoverageState`、`PHASE12_RESOLVED_BEIJING`、`PHASE12_RESOLVED_CALIFORNIA`。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/web exec vitest run src/services/city-boundaries.spec.ts src/stores/map-points.spec.ts</automated>
  </verify>
  <done>canonical boundaryId 已经能在 web 侧被显式解析为可渲染 boundary，且 support state 不再把“有字符串”误判成“有几何”。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用 canonical 回归锁定高亮与 reopened 边界恢复</name>
  <files>apps/web/src/components/WorldMapStage.spec.ts, apps/web/src/components/PointPreviewDrawer.spec.ts</files>
  <read_first>
    - .planning/phases/12-canonical/12-VERIFICATION.md
    - .planning/phases/12-canonical/12-04-SUMMARY.md
    - packages/contracts/src/fixtures.ts
    - apps/web/src/components/WorldMapStage.spec.ts
    - apps/web/src/components/PointPreviewDrawer.spec.ts
    - apps/web/src/stores/map-points.ts
    - apps/web/src/services/city-boundaries.ts
  </read_first>
  <behavior>
    - Test 1: 使用 `PHASE12_RESOLVED_BEIJING` 保存后的 canonical 点位，在 `WorldMapStage` 中渲染出 `data-boundary-id="cn-beijing-municipality"` 的 selected boundary。
    - Test 2: 同一 canonical Beijing 点位在关闭后重开，仍恢复同一条 `cn-beijing-municipality` 边界，而不是只剩 title/type/subtitle 文本。
    - Test 3: 使用 `PHASE12_RESOLVED_CALIFORNIA` 重开 drawer 时，因当前无几何命中而显示 `当前地点暂不支持边界高亮，将仅保存 canonical 地点身份与文本信息`，且测试不再手工把 `boundarySupportState` 写死为 `supported`。
  </behavior>
  <action>
    在 `apps/web/src/components/WorldMapStage.spec.ts` 中新增一条 canonical highlight regression，不要继续只依赖 legacy `createCityDraft()`：
    - 用 `PHASE12_RESOLVED_BEIJING` 构造 canonical draft，执行 `startDraftFromDetection()` -> `saveDraftAsPoint()` -> `selectPointById(savedId)`，挂载 `WorldMapStage`
    - 断言 `.world-map-stage__boundary--selected` 的 `data-boundary-id` 精确等于 `cn-beijing-municipality`
    - 再执行关闭/重开链路，使用现有 store API 重新选回同一 saved point，并再次断言 `data-boundary-id === 'cn-beijing-municipality'`

    在同一个 `WorldMapStage.spec.ts` 里补一条 canonical missing-geometry regression：
    - 用 `PHASE12_RESOLVED_CALIFORNIA` 保存后挂载 `WorldMapStage`
    - 断言不存在 `.world-map-stage__boundary--selected`
    - 断言通过 store 派生的 summary/drawer 链路不会把该点标记成 supported

    更新 `apps/web/src/components/PointPreviewDrawer.spec.ts`：
    - 新增一个 reopened canonical unsupported case，使用 `PHASE12_RESOLVED_CALIFORNIA` 走 `startDraftFromDetection()` -> `saveDraftAsPoint()` -> `openDrawerView()`，直接断言抽屉文案包含 `当前地点暂不支持边界高亮`
    - 把现有 popup/drawer parity 断言改为从 store 的真实 `summarySurfaceState` 或 `activeBoundaryCoverageState` 派生，不要再在 spec 里手工写死 `boundarySupportState: 'supported'`

    保留 12-04 已交付的 title/typeLabel/subtitle 语义断言，不要回退到 city-first fixture，也不要改动 popup/drawer 文案结构，继续遵守 D-10、D-11 与已验证的 UIX-04。
  </action>
  <acceptance_criteria>
    - `apps/web/src/components/WorldMapStage.spec.ts` 包含精确字符串 `cn-beijing-municipality`。
    - `apps/web/src/components/WorldMapStage.spec.ts` 包含精确字符串 `PHASE12_RESOLVED_BEIJING` 与 `PHASE12_RESOLVED_CALIFORNIA`。
    - `apps/web/src/components/WorldMapStage.spec.ts` 包含 reopened 链路断言，至少出现精确字符串 `selectPointById(` 或 `openSavedPointForPlaceOrStartDraft(`。
    - `apps/web/src/components/PointPreviewDrawer.spec.ts` 包含精确字符串 `当前地点暂不支持边界高亮，将仅保存 canonical 地点身份与文本信息`。
    - `apps/web/src/components/PointPreviewDrawer.spec.ts` 包含精确字符串 `PHASE12_RESOLVED_CALIFORNIA`。
    - `apps/web/src/components/PointPreviewDrawer.spec.ts` 不再新增任何新的手工 `boundarySupportState: 'supported'` 注入用例。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/web exec vitest run src/components/WorldMapStage.spec.ts src/components/PointPreviewDrawer.spec.ts</automated>
  </verify>
  <done>Phase 12 现在有自动化证据证明 canonical 点位在当前 web 数据集下可以正确高亮、正确重开，或在无几何时明确提示 unsupported。</done>
</task>

</tasks>

<verification>
- `pnpm --dir apps/web exec vitest run src/services/city-boundaries.spec.ts src/stores/map-points.spec.ts src/components/WorldMapStage.spec.ts src/components/PointPreviewDrawer.spec.ts`
- `pnpm --dir apps/web exec tsc --noEmit`
</verification>

<success_criteria>
- canonical `boundaryId` 不再只是被保存的字符串，而是 web 可验证、可渲染、可提示 unsupported 的真实输入。
- reopened canonical point 至少在 Beijing 这类当前已有 geometry 的场景下恢复同一条边界高亮。
- California / Aba 这类当前无 geometry 的 canonical 点位不会再被 UI 误报为支持高亮。
</success_criteria>

<output>
After completion, create `.planning/phases/12-canonical/12-05-SUMMARY.md`
</output>

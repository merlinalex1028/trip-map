---
phase: 12-canonical
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/place.ts
  - packages/contracts/src/resolve.ts
  - packages/contracts/src/fixtures.ts
  - packages/contracts/src/index.ts
  - packages/contracts/src/contracts.spec.ts
autonomous: true
requirements:
  - PLC-01
  - PLC-02
  - PLC-04
  - UIX-04
must_haves:
  truths:
    - "共享契约可以明确表达 resolved / ambiguous / failed 三种 canonical resolve 结果。"
    - "中国侧 canonical 摘要携带真实行政类型语义，不再只有通用 city 标签。"
    - "稳定 canonical identity 与展示文案分离，placeId 不依赖 displayName。"
  artifacts:
    - "packages/contracts/src/place.ts 定义 canonical place taxonomy、真实类型标签字段和稳定 identity 字段。"
    - "packages/contracts/src/resolve.ts 定义 resolve / confirm 请求体与 discriminated union 响应。"
    - "packages/contracts/src/fixtures.ts 提供 Phase 12 的 resolved / ambiguous / failed fixture。"
  key_links:
    - "packages/contracts/src/index.ts 必须 re-export place 与 resolve 合同，供 server 和 web 共同消费。"
    - "packages/contracts/src/contracts.spec.ts 必须锁住 PlaceKind、ChinaAdminType、CanonicalResolveResponse 的导出与字段名。"
---

<objective>
固定 Phase 12 的 canonical place 合同，让后续 `server` authoritative resolve 和 `web` 消费链路都围绕同一套身份、真实类型语义和失败口径实现。

Purpose: 先锁 contracts，避免 `server` 与 `web` 各自发明 canonical 字段。
Output: 可复用的 canonical taxonomy、resolve union、Phase 12 fixtures 与 contracts 回归测试。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-canonical/12-CONTEXT.md
@.planning/phases/12-canonical/12-RESEARCH.md
@packages/contracts/src/place.ts
@packages/contracts/src/fixtures.ts
@packages/contracts/src/index.ts
@packages/contracts/src/contracts.spec.ts

<interfaces>
Current contract baseline from `packages/contracts/src/place.ts`:

```ts
export type PlaceKind = 'CN_CITY' | 'OVERSEAS_ADMIN1'

export interface CanonicalPlaceRef {
  placeId: string
  boundaryId: string
  placeKind: PlaceKind
  datasetVersion: string
}

export interface CanonicalPlaceSummary extends CanonicalPlaceRef {
  displayName: string
}
```

Target Phase 12 contract shape to implement:

```ts
export type PlaceKind = 'CN_ADMIN' | 'OVERSEAS_ADMIN1'
export type ChinaAdminType =
  | 'MUNICIPALITY'
  | 'SAR'
  | 'PREFECTURE_LEVEL_CITY'
  | 'AUTONOMOUS_PREFECTURE'
  | 'LEAGUE'
  | 'AREA'

export interface CanonicalPlaceSummary extends CanonicalPlaceRef {
  displayName: string
  regionSystem: 'CN' | 'OVERSEAS'
  adminType: ChinaAdminType | 'ADMIN1'
  typeLabel: string
  parentLabel: string
  subtitle: string
}

export type CanonicalResolveResponse =
  | { status: 'resolved'; click: { lat: number; lng: number }; place: CanonicalPlaceSummary }
  | {
      status: 'ambiguous'
      click: { lat: number; lng: number }
      prompt: string
      recommendedPlaceId: string | null
      candidates: CanonicalPlaceCandidate[]
    }
  | {
      status: 'failed'
      click: { lat: number; lng: number }
      reason: 'NO_CANONICAL_MATCH' | 'LOW_CONFIDENCE_BORDER' | 'OUTSIDE_SUPPORTED_DATA' | 'CANDIDATE_MISMATCH'
      message: string
    }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 canonical place taxonomy 与 resolve union</name>
  <files>packages/contracts/src/place.ts, packages/contracts/src/resolve.ts, packages/contracts/src/index.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/12-canonical/12-CONTEXT.md
    - .planning/phases/12-canonical/12-RESEARCH.md
    - packages/contracts/src/place.ts
    - packages/contracts/src/index.ts
  </read_first>
  <behavior>
    - Type 1: 中国侧 canonical summary 可区分“直辖市”“特别行政区”“自治州”“地区”“盟”“地级市”，并通过独立字段暴露真实类型标签。
    - Type 2: resolve 响应必须只能是 `resolved`、`ambiguous`、`failed` 三种分支之一，`failed` 不携带 fallback place。
    - Type 3: `placeId`、`boundaryId`、`placeKind`、`datasetVersion` 继续作为稳定 identity 字段，展示文案放在 `displayName`、`typeLabel`、`parentLabel`、`subtitle`。
  </behavior>
  <action>
    在 `packages/contracts/src/place.ts` 中把 `PlaceKind` 明确改为 `CN_ADMIN | OVERSEAS_ADMIN1`，并新增 `ChinaAdminType` 联合类型，固定为 `MUNICIPALITY | SAR | PREFECTURE_LEVEL_CITY | AUTONOMOUS_PREFECTURE | LEAGUE | AREA`；同时把 `CanonicalPlaceSummary` 扩展为必须包含 `regionSystem`, `adminType`, `typeLabel`, `parentLabel`, `subtitle` 五个字段，用于实现 D-01、D-02、D-03、D-04、D-10、D-11。

    新建 `packages/contracts/src/resolve.ts`，定义精确请求/响应合同：
    - `ResolveCanonicalPlaceRequest = { lat: number; lng: number }`
    - `ConfirmCanonicalPlaceRequest = { lat: number; lng: number; candidatePlaceId: string }`
    - `CanonicalPlaceCandidate extends CanonicalPlaceSummary` 并增加 `candidateHint: string`
    - `CanonicalResolveResponse` 为 discriminated union，字段名严格使用 `status`, `click`, `place`, `candidates`, `recommendedPlaceId`, `prompt`, `reason`, `message`
    - `CanonicalResolveFailedReason` 精确枚举为 `NO_CANONICAL_MATCH | LOW_CONFIDENCE_BORDER | OUTSIDE_SUPPORTED_DATA | CANDIDATE_MISMATCH`

    更新 `packages/contracts/src/index.ts`，从单一出口 re-export `./resolve`，不要把任何运行时 resolver 逻辑放进 contracts 包，继续遵守 Phase 11 的 thin contracts 决策。
  </action>
  <acceptance_criteria>
    - `packages/contracts/src/place.ts` 包含精确字符串 `export type PlaceKind = 'CN_ADMIN' | 'OVERSEAS_ADMIN1'`。
    - `packages/contracts/src/place.ts` 包含精确字段名 `typeLabel`, `parentLabel`, `subtitle`。
    - `packages/contracts/src/resolve.ts` 包含精确字符串 `status: 'ambiguous'` 与 `status: 'failed'`。
    - `packages/contracts/src/resolve.ts` 包含精确字符串 `CANDIDATE_MISMATCH`。
    - `packages/contracts/src/index.ts` 包含精确字符串 `export * from './resolve'`。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir packages/contracts exec vitest run src/contracts.spec.ts</automated>
  </verify>
  <done>contracts 包已经能从单一入口导出 Phase 12 需要的 canonical taxonomy、resolve request/response 类型和失败 reason 枚举。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补充 Phase 12 fixtures 与 contracts 回归断言</name>
  <files>packages/contracts/src/fixtures.ts, packages/contracts/src/contracts.spec.ts</files>
  <read_first>
    - .planning/phases/12-canonical/12-CONTEXT.md
    - packages/contracts/src/fixtures.ts
    - packages/contracts/src/contracts.spec.ts
    - packages/contracts/src/place.ts
    - packages/contracts/src/resolve.ts
  </read_first>
  <behavior>
    - Test 1: fixture 明确覆盖中国直辖市、特别行政区、海外一级行政区三类真实语义。
    - Test 2: `contracts.spec.ts` 对 `CanonicalResolveResponse` 做类型断言，并锁定 `recommendedPlaceId`、`candidates`、`reason` 等 Phase 12 字段名。
    - Test 3: ambiguous fixture 的候选数不超过 3。
  </behavior>
  <action>
    在 `packages/contracts/src/fixtures.ts` 中新增一组 Phase 12 fixture，精确命名为：
    - `PHASE12_RESOLVED_BEIJING`
    - `PHASE12_RESOLVED_HONG_KONG`
    - `PHASE12_RESOLVED_CALIFORNIA`
    - `PHASE12_AMBIGUOUS_RESOLVE`
    - `PHASE12_FAILED_RESOLVE`

    fixture 内容必须直接体现 D-02、D-03、D-08、D-12：
    - Beijing 的 `typeLabel` 为 `直辖市`
    - Hong Kong 的 `typeLabel` 为 `特别行政区`
    - California 的 `typeLabel` 为 `一级行政区`
    - ambiguous fixture 的 `candidates` 数组长度固定为 3 或更少
    - failed fixture 不包含 `place` 或 `candidates`

    更新 `packages/contracts/src/contracts.spec.ts`，补充 `expectTypeOf` 和 runtime `expect`，锁定：
    - `PlaceKind` 只允许 `CN_ADMIN | OVERSEAS_ADMIN1`
    - `CanonicalPlaceSummary` 必含 `typeLabel`, `parentLabel`, `subtitle`
    - `CanonicalResolveResponse` 覆盖三分支
    - ambiguous fixture 的 `candidates.length` 小于等于 3
    - failed fixture 的键集合不含 `place`
  </action>
  <acceptance_criteria>
    - `packages/contracts/src/fixtures.ts` 包含精确字符串 `PHASE12_RESOLVED_BEIJING`、`PHASE12_RESOLVED_HONG_KONG`、`PHASE12_FAILED_RESOLVE`。
    - `packages/contracts/src/fixtures.ts` 中存在精确文案 `直辖市`、`特别行政区`、`一级行政区`。
    - `packages/contracts/src/contracts.spec.ts` 包含精确字符串 `CanonicalResolveResponse`。
    - `packages/contracts/src/contracts.spec.ts` 包含对 `candidates.length` 的断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir packages/contracts exec vitest run src/contracts.spec.ts</automated>
  </verify>
  <done>Phase 12 contract fixtures 与 spec 已经能稳定保护 canonical 类型语义、候选上限和失败分支字段边界。</done>
</task>

</tasks>

<verification>
- `pnpm --dir packages/contracts exec vitest run src/contracts.spec.ts`
- `pnpm --dir packages/contracts exec tsc --noEmit`
</verification>

<success_criteria>
- `contracts` 明确输出 canonical taxonomy、resolve union、失败 reason 和 Phase 12 fixtures。
- `server` 与 `web` 后续不需要再发明自己的 place subtype 或失败枚举。
</success_criteria>

<output>
After completion, create `.planning/phases/12-canonical/12-01-SUMMARY.md`
</output>

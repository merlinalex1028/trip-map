---
phase: 12-canonical
plan: 04
type: execute
wave: 3
depends_on:
  - 12-03
files_modified:
  - apps/web/src/components/map-popup/PointSummaryCard.vue
  - apps/web/src/components/PointPreviewDrawer.vue
  - apps/web/src/components/map-popup/PointSummaryCard.spec.ts
  - apps/web/src/components/PointPreviewDrawer.spec.ts
autonomous: true
requirements:
  - UIX-04
  - PLC-05
must_haves:
  truths:
    - "popup 与 drawer 都展示同一 canonical title/type/subtitle 语义。"
    - "中国侧与海外侧地点不会再统一伪装成“城市”。"
    - "候选确认表面沿用 server candidates，不再提供国家/地区 fallback 继续记录。"
  artifacts:
    - "apps/web/src/components/map-popup/PointSummaryCard.vue 输出统一的 canonical summary 标题、类型标签和副标题。"
    - "apps/web/src/components/PointPreviewDrawer.vue 复用同一套 canonical 语义字段。"
    - "组件 spec 锁定真实行政称谓与跨表面一致性。"
  key_links:
    - "PointSummaryCard.vue 和 PointPreviewDrawer.vue 必须都直接读取 store 提供的 `typeLabel` / `subtitle`，不能各自硬编码 city 文案。"
    - "component specs 必须同时覆盖中国直辖市、特别行政区和海外一级行政区。"
---

<objective>
把 Phase 12 的 canonical 类型语义真正投影到 popup 与 drawer：标题、类型标签、副标题和候选卡片都统一消费 canonical summary，不再出现任何“默认城市”文案。

Purpose: 完成用户可见层面的 UIX-04 / PLC-05 收口，确保不同表面对同一地点的语义一致。
Output: canonical summary UI、去 fallback CTA 的候选表面，以及组件回归测试。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-canonical/12-CONTEXT.md
@.planning/phases/12-canonical/12-RESEARCH.md
@apps/web/src/types/map-point.ts
@apps/web/src/components/map-popup/PointSummaryCard.vue
@apps/web/src/components/PointPreviewDrawer.vue
@apps/web/src/components/map-popup/PointSummaryCard.spec.ts
@apps/web/src/components/PointPreviewDrawer.spec.ts

<interfaces>
The UI must consume these Phase 12 fields directly:

```ts
displayName
typeLabel
parentLabel
subtitle
placeId
boundaryId
placeKind
datasetVersion
```

Do not derive new city-only labels from `cityName` or `cityContextLabel` when `typeLabel` / `subtitle` are already present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 用 canonical summary 统一 popup 与 drawer 的真实类型展示</name>
  <files>apps/web/src/components/map-popup/PointSummaryCard.vue, apps/web/src/components/PointPreviewDrawer.vue</files>
  <read_first>
    - .planning/phases/12-canonical/12-CONTEXT.md
    - apps/web/src/types/map-point.ts
    - apps/web/src/components/map-popup/PointSummaryCard.vue
    - apps/web/src/components/PointPreviewDrawer.vue
    - apps/web/src/stores/map-points.ts
  </read_first>
  <behavior>
    - UI 1: 标题显示 `displayName`，标题旁固定显示 `typeLabel` 标签。
    - UI 2: 副标题显示 `subtitle`，例如 `中国 · 直辖市`、`中国 · 特别行政区`、`United States · 一级行政区`。
    - UI 3: 候选确认模式不再显示“按国家/地区继续记录”按钮，只展示 server candidates 与必要提示。
  </behavior>
  <action>
    更新 `apps/web/src/components/map-popup/PointSummaryCard.vue`：
    - summary header 改为消费 canonical fields：标题用 `displayName`，新增类型标签节点，精确添加 `data-place-type-label="true"`；副标题节点精确添加 `data-place-subtitle="true"`
    - candidate cards 显示 `candidate.displayName`, `candidate.typeLabel`, `candidate.subtitle`, `candidate.candidateHint`
    - 若候选项 `candidate.placeId === recommendedPlaceId`，添加精确属性 `data-candidate-recommended="true"`
    - 移除国家/地区 fallback CTA，不再渲染“按国家/地区继续记录”文案或按钮

    更新 `apps/web/src/components/PointPreviewDrawer.vue`：
    - header 和元信息统一使用 `displayName`, `typeLabel`, `subtitle`
    - 不再回退到 `countryName + cityContextLabel` 组合生成主语义
    - 如果当前点位来自 canonical failed 分支，不打开 drawer；drawer 只处理 resolved/view/edit surface
  </action>
  <acceptance_criteria>
    - `apps/web/src/components/map-popup/PointSummaryCard.vue` 包含精确字符串 `data-place-type-label="true"` 与 `data-place-subtitle="true"`。
    - `apps/web/src/components/map-popup/PointSummaryCard.vue` 包含精确字符串 `data-candidate-recommended="true"`。
    - `apps/web/src/components/map-popup/PointSummaryCard.vue` 不再包含精确文案 `按国家/地区继续记录`。
    - `apps/web/src/components/PointPreviewDrawer.vue` 包含对 `typeLabel` 与 `subtitle` 的渲染。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/web exec vitest run src/components/map-popup/PointSummaryCard.spec.ts src/components/PointPreviewDrawer.spec.ts</automated>
  </verify>
  <done>popup 与 drawer 已经共享同一套 canonical summary 渲染，不再在任何表面把中国与海外地点统一叫成城市。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用组件回归锁定真实行政称谓与跨表面一致性</name>
  <files>apps/web/src/components/map-popup/PointSummaryCard.spec.ts, apps/web/src/components/PointPreviewDrawer.spec.ts</files>
  <read_first>
    - .planning/phases/12-canonical/12-VALIDATION.md
    - apps/web/src/components/map-popup/PointSummaryCard.spec.ts
    - apps/web/src/components/PointPreviewDrawer.spec.ts
    - apps/web/src/components/map-popup/PointSummaryCard.vue
    - apps/web/src/components/PointPreviewDrawer.vue
  </read_first>
  <behavior>
    - Test 1: Beijing 显示 `直辖市`，Hong Kong 显示 `特别行政区`，California 显示 `一级行政区`。
    - Test 2: candidate mode 最多渲染 3 个候选，并标识 recommended item。
    - Test 3: popup 与 drawer 对同一 canonical point 渲染同样的 title/type/subtitle 组合。
  </behavior>
  <action>
    更新 `PointSummaryCard.spec.ts` 与 `PointPreviewDrawer.spec.ts`，不要继续使用“城市优先” fixture 命名或断言。至少新增以下断言：
    - Beijing fixture 文案包含 `直辖市` 与 `中国 · 直辖市`
    - Hong Kong fixture 文案包含 `特别行政区` 与 `中国 · 特别行政区`
    - California fixture 文案包含 `一级行政区` 与 `United States · 一级行政区`
    - candidate mode 中不存在 `按国家/地区继续记录`
    - candidate mode 的推荐候选带有 `data-candidate-recommended="true"`
    - 同一个 canonical point 在 popup 与 drawer 中都显示同样的 `displayName` / `typeLabel` / `subtitle`
  </action>
  <acceptance_criteria>
    - `apps/web/src/components/map-popup/PointSummaryCard.spec.ts` 包含精确文案 `直辖市`、`特别行政区`、`一级行政区`。
    - `apps/web/src/components/map-popup/PointSummaryCard.spec.ts` 包含对 `data-candidate-recommended` 的断言。
    - `apps/web/src/components/map-popup/PointSummaryCard.spec.ts` 不再包含精确文案 `按国家/地区继续记录` 的正向断言。
    - `apps/web/src/components/PointPreviewDrawer.spec.ts` 包含对 `subtitle` 的断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/web exec vitest run src/components/map-popup/PointSummaryCard.spec.ts src/components/PointPreviewDrawer.spec.ts</automated>
  </verify>
  <done>真实行政称谓、推荐候选提示和跨表面 summary 一致性都被组件回归测试锁住。</done>
</task>

</tasks>

<verification>
- `pnpm --dir apps/web exec vitest run src/components/map-popup/PointSummaryCard.spec.ts src/components/PointPreviewDrawer.spec.ts`
- `pnpm --dir apps/web exec tsc --noEmit`
</verification>

<success_criteria>
- 用户能在 popup 与 drawer 一眼分清中国侧真实行政单位和海外一级行政区。
- 同一 canonical point 在多个表面不再出现不同层级或不同标题语义。
</success_criteria>

<output>
After completion, create `.planning/phases/12-canonical/12-04-SUMMARY.md`
</output>

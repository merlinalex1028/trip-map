---
phase: 12-canonical
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - apps/server/src/app.module.ts
  - apps/server/src/modules/canonical-places/canonical-places.module.ts
  - apps/server/src/modules/canonical-places/canonical-places.controller.ts
  - apps/server/src/modules/canonical-places/canonical-places.service.ts
  - apps/server/src/modules/canonical-places/dto/resolve-canonical-place.dto.ts
  - apps/server/src/modules/canonical-places/dto/confirm-canonical-place.dto.ts
  - apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts
  - apps/server/test/canonical-resolve.e2e-spec.ts
autonomous: true
requirements:
  - ARC-02
  - PLC-01
  - PLC-02
  - PLC-04
must_haves:
  truths:
    - "`server` 成为 canonical resolve 的正式入口，web 不再需要自己做 authoritative ranking。"
    - "歧义点击返回最多 3 个 canonical candidates 和推荐项，而不是直接伪装成唯一结果。"
    - "无法可靠命中时返回 failed 分支，不创建 fallback 记录。"
  artifacts:
    - "apps/server/src/modules/canonical-places/* 提供 `/places/resolve` 与 `/places/confirm`。"
    - "apps/server/test/canonical-resolve.e2e-spec.ts` 固定 authoritative resolve 的回归样例。"
  key_links:
    - "apps/server/src/app.module.ts 必须导入 CanonicalPlacesModule。"
    - "controller 的 DTO 校验与 service 的 resolve/confirm 分支必须都遵守 contracts 的 union 字段名。"
---

<objective>
在 `apps/server` 中落地 authoritative canonical resolve 模块，用临时 resolver fixtures/adapters 支撑 Phase 12 的正式 HTTP 边界，而不提前引入 Phase 13 的几何与数据清单交付。

Purpose: 兑现 ARC-02，把 authoritative resolve 从前端本地逻辑迁到服务端边界。
Output: `/places/resolve`、`/places/confirm`、临时 resolver fixture catalog 与 e2e 回归。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-canonical/12-CONTEXT.md
@.planning/phases/12-canonical/12-RESEARCH.md
@.planning/phases/11-monorepo/11-03-SUMMARY.md
@packages/contracts/src/place.ts
@packages/contracts/src/resolve.ts
@apps/server/src/main.ts
@apps/server/src/app.module.ts
@apps/server/src/modules/records/records.controller.ts
@apps/server/src/modules/records/records.service.ts
@apps/server/test/records-contract.e2e-spec.ts

<interfaces>
Follow the existing Nest boundary pattern from `records`:

```ts
@Post('smoke')
@UsePipes(new ValidationPipe({
  whitelist: true,
  transform: true,
  forbidNonWhitelisted: true,
  expectedType: CreateSmokeRecordDto,
}))
async createSmoke(@Body() body: CreateSmokeRecordDto): Promise<SmokeRecordResponse>
```

Implement the new server contract with these exact routes:

```ts
POST /places/resolve
POST /places/confirm
```

And these exact DTO shapes:

```ts
interface ResolveCanonicalPlaceDto {
  lat: number
  lng: number
}

interface ConfirmCanonicalPlaceDto {
  lat: number
  lng: number
  candidatePlaceId: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 搭建 authoritative canonical resolve 模块与临时 resolver catalog</name>
  <files>apps/server/src/app.module.ts, apps/server/src/modules/canonical-places/canonical-places.module.ts, apps/server/src/modules/canonical-places/canonical-places.controller.ts, apps/server/src/modules/canonical-places/canonical-places.service.ts, apps/server/src/modules/canonical-places/dto/resolve-canonical-place.dto.ts, apps/server/src/modules/canonical-places/dto/confirm-canonical-place.dto.ts, apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts, apps/server/test/canonical-resolve.e2e-spec.ts</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/12-canonical/12-CONTEXT.md
    - packages/contracts/src/place.ts
    - packages/contracts/src/resolve.ts
    - apps/server/src/main.ts
    - apps/server/src/app.module.ts
    - apps/server/src/modules/records/records.controller.ts
    - apps/server/src/modules/records/records.service.ts
    - apps/server/test/records-contract.e2e-spec.ts
  </read_first>
  <behavior>
    - API 1: Task 1 先创建一个最小可运行的 `canonical-resolve.e2e-spec.ts`，覆盖 `POST /places/resolve` 的 resolved/ambiguous 与 `POST /places/confirm` 的 candidate mismatch 基线。
    - API 2: `POST /places/resolve` 对 Beijing 返回 `status: 'resolved'`，歧义点击返回 `status: 'ambiguous'` 且 `candidates.length <= 3`。
    - API 3: `/places/confirm` 对非法 `candidatePlaceId` 返回 `status: 'failed'` + `reason: 'CANDIDATE_MISMATCH'`，没有任何 fallback place 或 record payload。
  </behavior>
  <action>
    参考 `records` 模块模式新建 `apps/server/src/modules/canonical-places/`，并在 `apps/server/src/app.module.ts` 中导入 `CanonicalPlacesModule`。控制器必须提供两个路由：
    - `POST /places/resolve`
    - `POST /places/confirm`

    DTO 使用 `class-validator`，字段必须与 contracts 完全对齐：
    - `ResolveCanonicalPlaceDto` 只接受 `lat`、`lng`
    - `ConfirmCanonicalPlaceDto` 只接受 `lat`、`lng`、`candidatePlaceId`

    采用 Phase 12 允许的“临时 resolver fixtures/adapters”策略，不要引入 Phase 13 的 geometry manifest、GeoJSON 清单或正式数据源交付。具体做法：
    - 在 `fixtures/canonical-place-fixtures.ts` 中维护最小可验证 catalog，至少包含 `cn-beijing`, `cn-hong-kong`, `cn-aba`, `us-california`
    - service 负责把点击坐标映射到 `resolved | ambiguous | failed`
    - ambiguous 分支硬性裁剪到最多 3 个 candidates，遵守 D-08
    - failed 分支 message 明确说明失败原因，遵守 D-12、D-13、D-14
    - `/places/confirm` 重新计算同一点击的 candidate 集合，并只允许确认该集合内的 `candidatePlaceId`；不匹配则返回 `status: 'failed'` + `reason: 'CANDIDATE_MISMATCH'`

    同一个任务里先创建 `apps/server/test/canonical-resolve.e2e-spec.ts` 的最小可运行版本，再让实现代码跑通它，避免 `<verify>` 依赖 Task 2 才出现的文件。这个初始 spec 至少要覆盖：
    - `POST /places/resolve` 命中 Beijing 时返回 `status: 'resolved'`
    - `POST /places/resolve` 命中歧义样例时返回 `status: 'ambiguous'` 且 `candidates.length <= 3`
    - `POST /places/confirm` 使用非法 `candidatePlaceId` 时返回 `status: 'failed'` + `reason: 'CANDIDATE_MISMATCH'`

    Task 2 只负责在这个已存在的 spec 上扩展 Hong Kong、California、strict failed 和合法 confirm 回归，不再承担“首次创建可运行 spec”的职责。
  </action>
  <acceptance_criteria>
    - `apps/server/src/app.module.ts` 包含精确字符串 `CanonicalPlacesModule`。
    - `apps/server/src/modules/canonical-places/canonical-places.controller.ts` 包含精确字符串 `@Controller('places')`、`@Post('resolve')`、`@Post('confirm')`。
    - `apps/server/src/modules/canonical-places/canonical-places.service.ts` 包含精确字符串 `recommendedPlaceId` 与 `CANDIDATE_MISMATCH`。
    - `apps/server/src/modules/canonical-places/fixtures/canonical-place-fixtures.ts` 包含精确字符串 `cn-beijing`、`cn-hong-kong`、`us-california`。
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 已在 Task 1 中创建，并包含精确字符串 `/places/resolve`、`/places/confirm` 与 `CANDIDATE_MISMATCH`。
    - 任一 server 文件中不得出现国家/地区 fallback record 创建逻辑，例如 `continueWithFallback` 或 `fallbackPoint`。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/server exec vitest run test/canonical-resolve.e2e-spec.ts</automated>
  </verify>
  <done>`apps/server` 已经提供 authoritative canonical resolve/confirm HTTP 边界，并且内部没有国家/地区 fallback 记录路径。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用 e2e 锁定 resolved / ambiguous / failed 行为</name>
  <files>apps/server/test/canonical-resolve.e2e-spec.ts</files>
  <read_first>
    - .planning/phases/12-canonical/12-CONTEXT.md
    - .planning/phases/12-canonical/12-VALIDATION.md
    - packages/contracts/src/fixtures.ts
    - packages/contracts/src/resolve.ts
    - apps/server/test/records-contract.e2e-spec.ts
    - apps/server/src/modules/canonical-places/canonical-places.controller.ts
  </read_first>
  <behavior>
    - Test 1: Beijing 返回 `typeLabel: '直辖市'`，Hong Kong 返回 `typeLabel: '特别行政区'`，California 返回 `typeLabel: '一级行政区'`。
    - Test 2: ambiguous 样例最多返回 3 个 candidates，并且 confirm 同一点击后返回 `status: 'resolved'`。
    - Test 3: failed 样例返回 `status: 'failed'`，并且响应中没有 `place` 字段。
  </behavior>
  <action>
    扩展 Task 1 已创建的 `apps/server/test/canonical-resolve.e2e-spec.ts`，沿用 `createApp()` + `app.inject()` 模式。补齐并细化以下五个场景：
    1. `POST /places/resolve` 命中 Beijing，断言 `place.placeId === 'cn-beijing'` 且 `place.typeLabel === '直辖市'`
    2. `POST /places/resolve` 命中 Hong Kong，断言 `place.typeLabel === '特别行政区'`
    3. `POST /places/resolve` 命中 California，断言 `place.placeKind === 'OVERSEAS_ADMIN1'`
    4. `POST /places/resolve` 命中歧义样例，断言 `status === 'ambiguous'`、`candidates.length <= 3`、`recommendedPlaceId` 为非空或 null 明确值
    5. `POST /places/resolve` 命中失败样例，断言 `status === 'failed'` 且响应对象不含 `place`

    同时增加 `POST /places/confirm` 回归：
    - 使用歧义点击 + 合法 candidatePlaceId 返回 `status: 'resolved'`
    - 使用歧义点击 + 非法 candidatePlaceId 返回 `status: 'failed'` + `reason: 'CANDIDATE_MISMATCH'`
  </action>
  <acceptance_criteria>
    - `apps/server/test/canonical-resolve.e2e-spec.ts` 包含精确字符串 `/places/resolve` 和 `/places/confirm`。
    - spec 文件包含精确文案 `直辖市`、`特别行政区`、`一级行政区`。
    - spec 文件包含对 `candidates.length` 的上限断言。
    - spec 文件包含 `reason: 'CANDIDATE_MISMATCH'` 的断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir apps/server exec vitest run test/canonical-resolve.e2e-spec.ts</automated>
  </verify>
  <done>server e2e 已经能稳定证明 authoritative resolve、候选确认和严格失败边界成立。</done>
</task>

</tasks>

<verification>
- `pnpm --dir apps/server exec vitest run test/canonical-resolve.e2e-spec.ts`
- `pnpm --dir apps/server exec tsc --noEmit`
</verification>

<success_criteria>
- `server` 现在拥有 `/places/resolve` 与 `/places/confirm`，并成为 canonical resolve 的唯一真源。
- 歧义与失败场景都通过 HTTP 合同明确表达，不再依赖前端本地 fallback 口径。
</success_criteria>

<output>
After completion, create `.planning/phases/12-canonical/12-02-SUMMARY.md`
</output>

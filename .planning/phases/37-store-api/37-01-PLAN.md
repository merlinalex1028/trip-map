---
phase: 37
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/services/api/records.ts
  - apps/web/src/services/api/records.spec.ts
  - apps/web/src/views/StatisticsPageView.vue
  - apps/web/src/views/StatisticsPageView.spec.ts
  - apps/web/src/stores/map-points.ts
  - apps/web/src/stores/map-points.spec.ts
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - SYNC-04

must_haves:
  truths:
    - "编辑旅行记录后，timelineEntries computed 自动按新日期重新排序"
    - "删除旅行记录后，timelineEntries computed 自动移除对应条目"
    - "编辑或删除旅行记录后，StatisticsPageView 的 travelRecordRevision 变化触发统计刷新"
    - "updateRecord API 失败时，travelRecords 回滚到原始快照"
    - "deleteSingleRecord API 失败时，travelRecords 回滚到原始快照"
  artifacts:
    - path: "apps/web/src/services/api/records.ts"
      provides: "updateTravelRecord + deleteSingleRecord API 函数"
      exports: ["updateTravelRecord", "deleteSingleRecord"]
    - path: "apps/web/src/stores/map-points.ts"
      provides: "updateRecord + deleteSingleRecord store 方法（乐观更新 + 回滚）"
      exports: ["updateRecord", "deleteSingleRecord"]
    - path: "apps/web/src/views/StatisticsPageView.vue"
      provides: "扩展 travelRecordRevision 包含 startDate/endDate/notes/tags"
  key_links:
    - from: "apps/web/src/stores/map-points.ts"
      to: "apps/web/src/services/api/records.ts"
      via: "import { updateTravelRecord as updateTravelRecordApi, deleteSingleRecord as deleteSingleRecordApi }"
      pattern: "import.*updateTravelRecord.*from.*services/api/records"
    - from: "apps/web/src/views/StatisticsPageView.vue"
      to: "travelRecords"
      via: "travelRecordRevision computed + watch"
      pattern: "record\\.startDate.*record\\.endDate.*record\\.notes"

threat_model:
  trust_boundaries:
    - boundary: "frontend-store → backend-API"
      description: "Store 方法通过 fetch 调用后端 PATCH/DELETE 端点，需认证"
  threats:
    - id: "T-37-01"
      category: "Tampering"
      component: "updateRecord store method"
      description: "乐观更新使用 spread 合并 request，可能覆盖非预期字段"
      disposition: "mitigate"
      mitigation: "UpdateTravelRecordRequest 类型约束仅允许 startDate/endDate/notes/tags，TypeScript 编译期保证"
    - id: "T-37-02"
      category: "Information Disclosure"
      component: "乐观更新回滚"
      description: "回滚瞬间用户可能看到数据闪动"
      disposition: "accept"
      mitigation: "乐观更新是既定 UX 模式，闪动为预期行为"
    - id: "T-37-03"
      category: "Spoofing"
      component: "API 调用"
      description: "401 时正确走 handleUnauthorized 流程"
      disposition: "mitigate"
      mitigation: "复用 isUnauthorizedApiClientError + handleUnauthorized 已验证模式"
---

<objective>
前端 API 层新增 updateTravelRecord / deleteSingleRecord 函数，Store 层新增 updateRecord / deleteSingleRecord 乐观更新方法，扩展 StatisticsPageView 的 travelRecordRevision 以覆盖新字段。

Purpose: 为 Phase 38 时间轴编辑/删除 UI 提供底层数据操作能力，实现 SYNC-01~04 全部数据一致性要求。
Output: 完整的前端编辑/删除数据流，含乐观更新、回滚、时间轴重排序、统计自动刷新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-store-api/37-RESEARCH.md
@.planning/phases/37-store-api/37-PATTERNS.md

@packages/contracts/src/records.ts
@apps/web/src/services/api/client.ts
@apps/web/src/services/api/records.ts
@apps/web/src/stores/map-points.ts
@apps/web/src/stores/map-points.spec.ts
@apps/web/src/views/StatisticsPageView.vue
@apps/web/src/views/StatisticsPageView.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: API 层 — 新增 updateTravelRecord + deleteSingleRecord 函数及测试</name>
  <files>
    apps/web/src/services/api/records.ts,
    apps/web/src/services/api/records.spec.ts
  </files>
  <read_first>
    - apps/web/src/services/api/records.ts（当前 API 函数结构和 import 模式）
    - apps/web/src/services/api/client.ts（apiFetchJson 签名、ApiClientError、responseType: 'none' 处理）
    - packages/contracts/src/records.ts（UpdateTravelRecordRequest 类型定义）
    - apps/web/src/services/api/phase11-smoke.spec.ts（API 测试 mock 模式参考）
  </read_first>
  <action>
    **1. 修改 `apps/web/src/services/api/records.ts`：**

    在现有 import 中添加 `UpdateTravelRecordRequest` 类型：
    ```typescript
    import type {
      CreateTravelRecordRequest,
      ImportTravelRecordsRequest,
      ImportTravelRecordsResponse,
      TravelRecord,
      UpdateTravelRecordRequest,
    } from '@trip-map/contracts'
    ```

    在 `deleteTravelRecord` 函数之后、`importTravelRecords` 函数之前，新增两个函数：

    ```typescript
    export async function updateTravelRecord(
      id: string,
      request: UpdateTravelRecordRequest,
    ): Promise<TravelRecord> {
      return apiFetchJson<TravelRecord>(`/records/${encodeURIComponent(id)}`, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(request),
      })
    }

    export async function deleteSingleRecord(id: string): Promise<void> {
      return apiFetchJson<void>(
        `/records/record/${encodeURIComponent(id)}`,
        { method: 'DELETE' },
        { responseType: 'none' },
      )
    }
    ```

    **2. 新建 `apps/web/src/services/api/records.spec.ts`：**

    测试模式参照 `phase11-smoke.spec.ts`，使用 `vi.stubGlobal('fetch', fetchMock)` mock 全局 fetch。

    测试用例：
    - `updateTravelRecord` 发送 PATCH 请求到 `/records/{id}`，body 为 JSON.stringify(request)，包含 `Content-Type: application/json` header
    - `updateTravelRecord` 成功时解析 JSON 响应并返回 `TravelRecord`
    - `updateTravelRecord` 使用 `encodeURIComponent` 编码 id（传入含 `/` 的 id 验证）
    - `deleteSingleRecord` 发送 DELETE 请求到 `/records/record/{id}`
    - `deleteSingleRecord` 成功 204 时返回 `undefined`
    - `deleteSingleRecord` 使用 `encodeURIComponent` 编码 id
    - 两者在 401 响应时抛出 `ApiClientError` 且 code 为 `session-unauthorized`
    - 两者在非 ok 响应时抛出 `ApiClientError` 且 code 为 `http-error`
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web vitest run src/services/api/records.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export async function updateTravelRecord" apps/web/src/services/api/records.ts
    - grep -q "export async function deleteSingleRecord" apps/web/src/services/api/records.ts
    - grep -q "UpdateTravelRecordRequest" apps/web/src/services/api/records.ts
    - grep -q "PATCH" apps/web/src/services/api/records.ts
    - grep -q "/records/record/" apps/web/src/services/api/records.ts
    - grep -q "responseType: 'none'" apps/web/src/services/api/records.ts
    - grep -q "updateTravelRecord" apps/web/src/services/api/records.spec.ts
    - grep -q "deleteSingleRecord" apps/web/src/services/api/records.spec.ts
    - pnpm --filter @trip-map/web vitest run src/services/api/records.spec.ts 全部通过
  </acceptance_criteria>
  <done>
    updateTravelRecord 发送 PATCH /records/:id 返回 TravelRecord；deleteSingleRecord 发送 DELETE /records/record/:id 返回 void；401 抛出 ApiClientError；测试全部通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: Store 层 — updateRecord / deleteSingleRecord 乐观更新 + StatisticsPageView revision 扩展</name>
  <files>
    apps/web/src/stores/map-points.ts,
    apps/web/src/stores/map-points.spec.ts,
    apps/web/src/views/StatisticsPageView.vue,
    apps/web/src/views/StatisticsPageView.spec.ts
  </files>
  <read_first>
    - apps/web/src/stores/map-points.ts（illuminate/unilluminate 完整实现、hasSessionBoundaryChanged、import 结构、return 导出列表）
    - apps/web/src/stores/map-points.spec.ts（mock 声明模式 vi.hoisted、makeRecord 辅助函数、illuminate/unilluminate 测试用例模式）
    - apps/web/src/views/StatisticsPageView.vue（travelRecordRevision computed 完整实现、watcher 逻辑）
    - apps/web/src/views/StatisticsPageView.spec.ts（mountStatisticsPage 辅助函数、fetchStatsMock mock 模式）
    - apps/web/src/services/api/records.ts（确认 Task 1 已添加的函数签名）
  </read_first>
  <action>
    **A. 修改 `apps/web/src/stores/map-points.ts`：**

    1. 在 import 区新增（注意重命名避免与 store 方法名冲突）：
    ```typescript
    import {
      createTravelRecord,
      deleteTravelRecord,
      updateTravelRecord as updateTravelRecordApi,
      deleteSingleRecord as deleteSingleRecordApi,
    } from '../services/api/records'
    ```

    2. 在 `unilluminate` 函数之后、`isPlaceIlluminated` 函数之前，新增常量：
    ```typescript
    const RECORD_UPDATE_SUCCESS_NOTICE = '旅行记录已更新。'
    const RECORD_UPDATE_FAILED_NOTICE = '编辑失败，旅行记录暂时没有同步成功，请稍后重试。'
    const RECORD_SINGLE_DELETE_SUCCESS_NOTICE = '旅行记录已删除。'
    const RECORD_SINGLE_DELETE_FAILED_NOTICE = '删除失败，旅行记录暂时没有同步成功，请稍后重试。'
    ```

    3. 新增 `updateRecord` 方法（沿用 illuminate 的乐观更新模式）：
    ```typescript
    async function updateRecord(
      recordId: string,
      request: import('@trip-map/contracts').UpdateTravelRecordRequest,
    ) {
      const authSessionStore = useAuthSessionStore()
      const boundaryVersionAtStart = authSessionStore.boundaryVersion
      const previousRecords = [...travelRecords.value]
      const targetRecord = previousRecords.find((r) => r.id === recordId)
      if (!targetRecord) {
        return
      }

      // 乐观更新 — PATCH 语义：只覆盖传入字段
      const optimisticRecord: TravelRecord = {
        ...targetRecord,
        ...request,
        updatedAt: new Date().toISOString(),
      }
      travelRecords.value = previousRecords.map((r) =>
        r.id === recordId ? optimisticRecord : r,
      )

      try {
        const updatedRecord = await updateTravelRecordApi(recordId, request)

        if (hasSessionBoundaryChanged(boundaryVersionAtStart)) {
          return
        }

        // 用服务端权威数据替换
        travelRecords.value = travelRecords.value.map((r) =>
          r.id === recordId ? updatedRecord : r,
        )
        useMapUiStore().setInteractionNotice({
          tone: 'info',
          message: RECORD_UPDATE_SUCCESS_NOTICE,
        })
      } catch (error) {
        if (hasSessionBoundaryChanged(boundaryVersionAtStart)) {
          return
        }

        // 回滚到原始状态
        travelRecords.value = previousRecords

        if (isUnauthorizedApiClientError(error)) {
          if (authSessionStore.currentUser) {
            authSessionStore.handleUnauthorized()
          }
        } else {
          useMapUiStore().setInteractionNotice({
            tone: 'warning',
            message: RECORD_UPDATE_FAILED_NOTICE,
          })
        }
      }
    }
    ```

    4. 新增 `deleteSingleRecord` 方法（沿用 unilluminate 的快照 + 回滚模式）：
    ```typescript
    async function deleteSingleRecord(recordId: string) {
      const authSessionStore = useAuthSessionStore()
      const boundaryVersionAtStart = authSessionStore.boundaryVersion
      const previousRecords = [...travelRecords.value]
      const targetRecord = previousRecords.find((r) => r.id === recordId)
      if (!targetRecord) {
        return
      }

      // 乐观删除
      travelRecords.value = previousRecords.filter((r) => r.id !== recordId)

      try {
        await deleteSingleRecordApi(recordId)

        if (hasSessionBoundaryChanged(boundaryVersionAtStart)) {
          return
        }

        // 成功 — 确认删除（已是最新状态）
        useMapUiStore().setInteractionNotice({
          tone: 'info',
          message: RECORD_SINGLE_DELETE_SUCCESS_NOTICE,
        })
      } catch (error) {
        if (hasSessionBoundaryChanged(boundaryVersionAtStart)) {
          return
        }

        // 回滚
        travelRecords.value = previousRecords

        if (isUnauthorizedApiClientError(error)) {
          if (authSessionStore.currentUser) {
            authSessionStore.handleUnauthorized()
          }
        } else {
          useMapUiStore().setInteractionNotice({
            tone: 'warning',
            message: RECORD_SINGLE_DELETE_FAILED_NOTICE,
          })
        }
      }
    }
    ```

    5. 在 return 对象中添加 `updateRecord` 和 `deleteSingleRecord`。

    **B. 修改 `apps/web/src/views/StatisticsPageView.vue`：**

    扩展 `travelRecordRevision` computed，新增 `startDate`、`endDate`、`notes`、`tags` 字段：
    ```typescript
    const travelRecordRevision = computed(() =>
      travelRecords.value
        .map((record) => [
          record.id,
          record.placeId,
          record.createdAt,
          record.parentLabel,
          record.displayName,
          record.typeLabel,
          record.subtitle,
          record.startDate,
          record.endDate,
          record.notes,
          record.tags.join(','),
        ].join(' '))
        .join('|'),
    )
    ```

    **C. 修改 `apps/web/src/stores/map-points.spec.ts`：**

    1. 扩展 hoisted mock 声明，新增 `updateMock` 和 `deleteSingleMock`：
    ```typescript
    const { createMock, deleteMock, updateMock, deleteSingleMock } = vi.hoisted(() => {
      return {
        createMock: vi.fn<() => Promise<TravelRecord>>(),
        deleteMock: vi.fn<() => Promise<void>>(),
        updateMock: vi.fn<() => Promise<TravelRecord>>(),
        deleteSingleMock: vi.fn<() => Promise<void>>(),
      }
    })
    ```

    2. 扩展 vi.mock 模块：
    ```typescript
    vi.mock('../services/api/records', () => {
      return {
        createTravelRecord: createMock,
        deleteTravelRecord: deleteMock,
        updateTravelRecord: updateMock,
        deleteSingleRecord: deleteSingleMock,
      }
    })
    ```

    3. 在 beforeEach 中添加 `updateMock.mockReset()` 和 `deleteSingleMock.mockReset()`。

    4. 新增 `describe('updateRecord', ...)` 测试组，测试用例：
    - 乐观更新：使用 deferred promise 验证 API 调用前 travelRecords 已更新（startDate 变更）
    - 成功替换：API 返回后用服务端数据替换乐观数据
    - 失败回滚：mock reject 后 travelRecords 恢复原始状态 + warning notice
    - 401 处理：ApiClientError(401) 走 handleUnauthorized 而非通用 warning
    - session 边界变化：中间调用 handleUnauthorized() 后 API 返回不写入
    - 不存在的 recordId：静默返回，不调用 API

    5. 新增 `describe('deleteSingleRecord', ...)` 测试组，测试用例：
    - 乐观删除：API 调用前记录已从 travelRecords 移除
    - 成功确认：API resolve 后记录保持移除 + info notice
    - 失败回滚：mock reject 后记录恢复 + warning notice
    - 401 处理：ApiClientError(401) 走 handleUnauthorized
    - session 边界变化：中间 handleUnauthorized 后不回滚
    - 不存在的 recordId：静默返回，不调用 API
    - 删除后 timelineEntries 自动更新：验证 computed 属性响应 travelRecords 变化

    **D. 修改 `apps/web/src/views/StatisticsPageView.spec.ts`：**

    新增测试用例验证编辑 notes/tags 后触发统计刷新：
    - 创建一个带 notes 和 tags 的记录，替换为不同 notes 的记录，验证 fetchStatsMock 被再次调用
    - 创建一个带 tags 的记录，替换为不同 tags 的记录，验证 fetchStatsMock 被再次调用
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "updateRecord" apps/web/src/stores/map-points.ts
    - grep -q "deleteSingleRecord" apps/web/src/stores/map-points.ts
    - grep -q "updateTravelRecord as updateTravelRecordApi" apps/web/src/stores/map-points.ts
    - grep -q "deleteSingleRecord as deleteSingleRecordApi" apps/web/src/stores/map-points.ts
    - grep -q "previousRecords" apps/web/src/stores/map-points.ts（回滚模式）
    - grep -q "travelRecords.value = previousRecords" apps/web/src/stores/map-points.ts
    - grep -q "record.startDate" apps/web/src/views/StatisticsPageView.vue
    - grep -q "record.endDate" apps/web/src/views/StatisticsPageView.vue
    - grep -q "record.notes" apps/web/src/views/StatisticsPageView.vue
    - grep -q "record.tags.join" apps/web/src/views/StatisticsPageView.vue
    - grep -q "updateMock" apps/web/src/stores/map-points.spec.ts
    - grep -q "deleteSingleMock" apps/web/src/stores/map-points.spec.ts
    - grep -q "describe('updateRecord'" apps/web/src/stores/map-points.spec.ts
    - grep -q "describe('deleteSingleRecord'" apps/web/src/stores/map-points.spec.ts
    - pnpm --filter @trip-map/web vitest run 全部通过
  </acceptance_criteria>
  <done>
    store.updateRecord 乐观更新 + 回滚正确；store.deleteSingleRecord 乐观删除 + 回滚正确；timelineEntries 自动响应；travelRecordRevision 包含 startDate/endDate/notes/tags；全部测试通过。
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter @trip-map/web vitest run` — 全部测试通过
2. `pnpm --filter @trip-map/web typecheck` — TypeScript 类型检查通过（如有配置）
</verification>

<success_criteria>
1. updateTravelRecord API 函数发送 PATCH /records/:id，返回 TravelRecord
2. deleteSingleRecord API 函数发送 DELETE /records/record/:id，返回 void
3. store.updateRecord 乐观更新 travelRecords，API 失败时回滚到 previousRecords
4. store.deleteSingleRecord 乐观删除 travelRecords，API 失败时回滚到 previousRecords
5. StatisticsPageView 的 travelRecordRevision 包含 startDate/endDate/notes/tags
6. 全部新增测试通过，现有测试不被破坏
</success_criteria>

<output>
完成后创建 `.planning/phases/37-store-api/37-01-SUMMARY.md`
</output>

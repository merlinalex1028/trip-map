---
phase: 47-dashboard
plan: 04
type: execute
wave: 4
depends_on:
  - 47-01
  - 47-03
files_modified:
  - apps/web/src/components/memories/PopularFootprintsList.vue
  - apps/web/src/components/memories/PopularFootprintsList.spec.ts
  - apps/web/src/components/memories/MemoryPostcardStrip.vue
  - apps/web/src/components/memories/MemoryPostcardStrip.spec.ts
  - apps/web/src/views/StatisticsPageView.vue
  - apps/web/src/views/StatisticsPageView.spec.ts
autonomous: true
requirements:
  - MEM-04
  - MEM-05
  - MEM-06
  - MEM-07
must_haves:
  truths:
    - "D-09 through D-12: populated memories show a visual Top 5 footprint ranking with place, repeat count, and latest-date context rather than a table or leaderboard flow."
    - "D-13 through D-16: real dated memory seeds render a browse-only horizontal postcard strip with scenic decoration and visible place/date context."
    - "Ranking and postcards mount only from current stats payload on populated `/memories`; empty accounts still show no populated modules."
    - "No upload, viewer, deep-link, favorites, expanded ranking, sidebar, or Prisma schema work slips into the final dashboard band."
  artifacts:
    - path: "apps/web/src/components/memories/PopularFootprintsList.vue"
      provides: "Visual Top 5 popular footprint section"
      contains: "热门足迹排行"
    - path: "apps/web/src/components/memories/MemoryPostcardStrip.vue"
      provides: "Browse-only real-record postcard strip"
      contains: "珍藏回忆瞬间"
    - path: "apps/web/src/views/StatisticsPageView.vue"
      provides: "Final populated dashboard composition"
      contains: "MemoryPostcardStrip"
  key_links:
    - from: "apps/web/src/views/StatisticsPageView.vue"
      to: "apps/web/src/components/memories/PopularFootprintsList.vue"
      via: "`stats.memories.popularFootprints` prop"
      pattern: "popularFootprints"
    - from: "apps/web/src/views/StatisticsPageView.vue"
      to: "apps/web/src/components/memories/MemoryPostcardStrip.vue"
      via: "`stats.memories.postcards` prop"
      pattern: "postcards"
    - from: "apps/web/src/components/memories/MemoryPostcardStrip.vue"
      to: "apps/web/src/components/timeline/JournalPostcardThumb.vue"
      via: "decorative stable scenic variant rendering"
      pattern: "JournalPostcardThumb"
---

<objective>
Finish the populated memories dashboard with the Top 5 visual ranking and real-record postcard strip.

Purpose: Phase 47 is not complete until the dashboard exposes its ranking and visual memory band while empty accounts and deferred photo/detail flows stay honest.
Output: Ranking and postcard components plus final `/memories` wiring and focused verification.
</objective>

<scope_guard>
User constraint remains blocking: do not change `ShellSidebar.vue`, sidebar layout, or sidebar behavior. If final dashboard copy or route naming exposes sidebar friction, record that constraint instead of widening file ownership.
</scope_guard>

<schema_rationale>
This UI closure plan modifies memories components and the existing route view only. No schema-relevant Prisma file changes are planned, so no `prisma db push` task is required.
</schema_rationale>

<decision_traceability>
- D-09, D-10, D-11, D-12: the visual ranking consumes server-ordered Top 5 repeat footprints and does not add table, sort, or expansion affordances.
- D-13, D-14, D-15, D-16: postcards are real dated record slots with decorative scenic artwork, visible place/date copy, and browse-only interaction.
- D-03 and deferred ideas: the final dashboard band still has no time filter, favorites, upload, zoom viewer, journal deep-link, or detail route.
</decision_traceability>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-dashboard/47-CONTEXT.md
@.planning/phases/47-dashboard/47-RESEARCH.md
@.planning/phases/47-dashboard/47-PATTERNS.md
@.planning/phases/47-dashboard/47-VALIDATION.md
@.planning/phases/47-dashboard/47-UI-SPEC.md
@.planning/phases/46-travel-journal-refactor/46-CONTEXT.md
@packages/contracts/src/stats.ts
@apps/web/src/components/timeline/JournalPostcardThumb.vue
@apps/web/src/components/timeline/journal-thumbnails.ts
@apps/web/src/views/StatisticsPageView.vue
@apps/web/src/views/StatisticsPageView.spec.ts

<interfaces>
Server-owned presentation seeds from Plan 47-01:
```ts
export interface TravelPopularFootprint {
  placeId: string
  displayName: string
  parentLabel: string | null
  visitCount: number
  latestVisitDate: string | null
}

export interface TravelMemoryPostcardSeed {
  recordId: string
  placeId: string
  displayName: string
  parentLabel: string | null
  startDate: string
}
```

Decorative postcard atom to reuse without Phase 46 file edits:
```ts
type JournalPostcardVariant = 'river' | 'kyoto' | 'paris' | 'shanghai'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the visual Top 5 popular footprint list</name>
  <files>apps/web/src/components/memories/PopularFootprintsList.vue, apps/web/src/components/memories/PopularFootprintsList.spec.ts</files>
  <read_first>
    - apps/web/src/components/memories/PopularFootprintsList.vue
    - apps/web/src/components/memories/PopularFootprintsList.spec.ts
    - packages/contracts/src/stats.ts
    - apps/web/src/components/timeline/TimelineVisitCard.vue
    - apps/web/src/services/timeline.ts
    - .planning/phases/47-dashboard/47-CONTEXT.md
    - .planning/phases/47-dashboard/47-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: ranking title and visual list render rank markers, place names, repeat visit counts, and latest dates from ordered Top 5 props.
    - Test: a sixth supplied item does not render in the Phase 47 list.
    - Test: component contains no `<table>`, sort control, expand control, or row mutation action.
    - Test: null latest date renders an honest date-pending label instead of fabricating one.
  </behavior>
  <action>Create `PopularFootprintsList.vue` with `defineProps<{ items: TravelPopularFootprint[] }>()`, `data-region="popular-footprints"`, and UI-SPEC heading `热门足迹排行`. Treat server order as D-09/D-10 truth but defensively render only `items.slice(0, 5)`. Use an ordered visual memory list, not a `<table>`: every row needs visible rank marker, `displayName`, `visitCount` text such as `N 次足迹`, and `latestVisitDate` text; when no latest date exists, use exact honest copy `旅行日期待补充`. Parent label may support the place as secondary copy. Do not add `查看更多`, filter/sort controls, edit/delete links, or click-to-detail semantics because D-11 and D-12 keep this Phase 47 surface fixed and memory-oriented.</action>
  <acceptance_criteria>
    - `apps/web/src/components/memories/PopularFootprintsList.vue` contains `data-region="popular-footprints"`.
    - `apps/web/src/components/memories/PopularFootprintsList.vue` contains `热门足迹排行`.
    - `apps/web/src/components/memories/PopularFootprintsList.vue` contains `旅行日期待补充`.
    - `apps/web/src/components/memories/PopularFootprintsList.vue` contains `slice(0, 5)`.
    - `apps/web/src/components/memories/PopularFootprintsList.spec.ts` contains `visitCount`.
    - `rg -n "<table|查看更多|排序|filter|@click|RouterLink" apps/web/src/components/memories/PopularFootprintsList.vue` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/components/memories/PopularFootprintsList.spec.ts</automated>
  </verify>
  <done>The dashboard has a fixed visual Top 5 list that foregrounds repeat memory context without becoming a table or leaderboard flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the browse-only real-memory postcard strip</name>
  <files>apps/web/src/components/memories/MemoryPostcardStrip.vue, apps/web/src/components/memories/MemoryPostcardStrip.spec.ts</files>
  <read_first>
    - apps/web/src/components/memories/MemoryPostcardStrip.vue
    - apps/web/src/components/memories/MemoryPostcardStrip.spec.ts
    - packages/contracts/src/stats.ts
    - apps/web/src/components/timeline/JournalPostcardThumb.vue
    - apps/web/src/components/timeline/journal-thumbnails.ts
    - apps/web/src/components/timeline/journal-thumbnails.spec.ts
    - .planning/phases/47-dashboard/47-CONTEXT.md
    - .planning/phases/47-dashboard/47-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: every postcard card shows real `displayName` and `startDate` from postcard seed props.
    - Test: strip exposes horizontal browse semantics and decorative scenic artwork without image upload or viewer semantics.
    - Test: postcard cards are not links or buttons and have no journal deep-link/zoom detail action.
    - Test: stable variant selection uses record/place context so refreshes do not randomize scenic decoration.
  </behavior>
  <action>Create `MemoryPostcardStrip.vue` with `defineProps<{ items: TravelMemoryPostcardSeed[] }>()`, UI-SPEC heading `珍藏回忆瞬间`, and a keyboard-reachable horizontal strip hook `data-region="memory-postcard-strip"` with an accessible label. Render each D-13 through D-16 seed as a non-interactive article carrying visible `displayName` and `startDate` copy; rely on server ordering from D-14. Reuse `JournalPostcardThumb` decorative imagery without editing Phase 46 files. Inside this component, map each seed to a stable `JournalPostcardVariant` from the exact variant set `river`, `kyoto`, `paris`, `shanghai` by hashing `placeId`, `displayName`, and `parentLabel` context; never use `Math.random`. Keep scenic layers decorative and browse-only: no upload slot, image viewer, zoom, journal link, route link, drag editor, or photo semantics.</action>
  <acceptance_criteria>
    - `apps/web/src/components/memories/MemoryPostcardStrip.vue` contains `data-region="memory-postcard-strip"`.
    - `apps/web/src/components/memories/MemoryPostcardStrip.vue` contains `珍藏回忆瞬间`.
    - `apps/web/src/components/memories/MemoryPostcardStrip.vue` contains `JournalPostcardThumb`.
    - `apps/web/src/components/memories/MemoryPostcardStrip.vue` contains `'river'`.
    - `apps/web/src/components/memories/MemoryPostcardStrip.spec.ts` contains `startDate`.
    - `apps/web/src/components/memories/MemoryPostcardStrip.spec.ts` contains `displayName`.
    - `rg -n "Math\\.random|RouterLink|<a |<button|上传|upload|viewer|zoom|journal" apps/web/src/components/memories/MemoryPostcardStrip.vue` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/components/memories/MemoryPostcardStrip.spec.ts src/components/timeline/journal-thumbnails.spec.ts</automated>
  </verify>
  <done>The postcard band is a horizontal real-record memory strip with deterministic scenic decoration and no excluded photo/detail affordance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire final dashboard bands and run Phase 47 web gates</name>
  <files>apps/web/src/views/StatisticsPageView.vue, apps/web/src/views/StatisticsPageView.spec.ts</files>
  <read_first>
    - apps/web/src/views/StatisticsPageView.vue
    - apps/web/src/views/StatisticsPageView.spec.ts
    - apps/web/src/components/memories/PopularFootprintsList.vue
    - apps/web/src/components/memories/MemoryPostcardStrip.vue
    - apps/web/src/components/memories/MemoriesChartGrid.vue
    - .planning/phases/47-dashboard/47-VALIDATION.md
    - .planning/phases/47-dashboard/47-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: populated route mounts popular footprints and postcard strip from `stats.memories`.
    - Test: empty route still omits overview, chart grid, ranking, and postcard hooks.
    - Test: final populated DOM contains no time filter, photo upload/viewer, favorites, or expanded leaderboard affordance.
  </behavior>
  <action>Extend only the populated Phase 47 branch in `StatisticsPageView.vue` from Plan 47-03: import `PopularFootprintsList` and `MemoryPostcardStrip`, pass `stats!.memories.popularFootprints` and `stats!.memories.postcards`, and place the postcard band after the chart/ranking rhythm prescribed by UI-SPEC. Keep empty/anonymous/restoring/error branches untouched. Strengthen `StatisticsPageView.spec.ts` so populated fixtures assert `[data-region="popular-footprints"]` and `[data-region="memory-postcard-strip"]`, while zero-trip fixtures assert those hooks plus `[data-region="memories-overview"]` and `[data-region="memories-chart-grid"]` are absent. Run the focused web gate and build. If a Phase 47 gate fails, fix only plan-owned memories/view files; do not change sidebar files or add filtered/demo behavior to make a test pass.</action>
  <acceptance_criteria>
    - `apps/web/src/views/StatisticsPageView.vue` contains `PopularFootprintsList`.
    - `apps/web/src/views/StatisticsPageView.vue` contains `MemoryPostcardStrip`.
    - `apps/web/src/views/StatisticsPageView.spec.ts` contains `popular-footprints`.
    - `apps/web/src/views/StatisticsPageView.spec.ts` contains `memory-postcard-strip`.
    - `pnpm --filter @trip-map/web test -- src/views/StatisticsPageView.spec.ts src/stores/stats.spec.ts src/components/common/BaseChart.spec.ts src/components/memories/MemoriesOverviewGrid.spec.ts src/components/memories/MemoriesChartGrid.spec.ts src/components/memories/PopularFootprintsList.spec.ts src/components/memories/MemoryPostcardStrip.spec.ts` exits 0.
    - `pnpm --filter @trip-map/web build` exits 0.
    - `rg -n "全部时间|查看更多|上传照片|照片上传|viewer|我的收藏|收藏" apps/web/src/views/StatisticsPageView.vue apps/web/src/components/memories` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/views/StatisticsPageView.spec.ts src/stores/stats.spec.ts src/components/common/BaseChart.spec.ts src/components/memories/MemoriesOverviewGrid.spec.ts src/components/memories/MemoriesChartGrid.spec.ts src/components/memories/PopularFootprintsList.spec.ts src/components/memories/MemoryPostcardStrip.spec.ts</automated>
    <automated>pnpm --filter @trip-map/web build</automated>
  </verify>
  <done>The populated dashboard includes real Top 5 and postcard bands, empty state remains honest, and focused web/build gates close Phase 47 UI work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-ordered Top 5/postcard seeds -> visual components | Real record labels and dates become dashboard cards. |
| decorative image variants -> browser DOM | Scenic assets must stay non-interactive and non-upload surfaces. |
| populated route branch -> empty route branch | Private memory bands must not be spoofed with static data for empty accounts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04 | Tampering | ranking and postcard sections | mitigate | Render only stats-provided ranking/postcard props and assert empty-route absence plus fixed Top 5/card copy behavior. |
| T-47-12 | Spoofing | memory media semantics | mitigate | Keep postcard cards browse-only with decorative local art and visible real record place/date context; reject upload/viewer/deep-link affordances. |
| T-47-13 | Information Disclosure | ranking/postcard labels | mitigate | Use Vue interpolation over bounded aggregate labels/date strings and avoid rich HTML or cross-account fetches inside child components. |
</threat_model>

<verification>
Final Phase 47 web gate:

```bash
pnpm --filter @trip-map/web test -- src/views/StatisticsPageView.spec.ts src/stores/stats.spec.ts src/components/common/BaseChart.spec.ts src/components/memories/MemoriesOverviewGrid.spec.ts src/components/memories/MemoriesChartGrid.spec.ts src/components/memories/PopularFootprintsList.spec.ts src/components/memories/MemoryPostcardStrip.spec.ts
pnpm --filter @trip-map/web build
```

Forbidden final-band affordance audit:

```bash
rg -n "全部时间|查看更多|上传照片|照片上传|viewer|我的收藏|收藏" apps/web/src/views/StatisticsPageView.vue apps/web/src/components/memories
```
</verification>

<success_criteria>
- `/memories` populated state includes a visual Top 5 footprint ranking and browse-only postcard strip tied to real stats payload fields.
- Every ranking row foregrounds rank/place/count/latest-date context and every postcard exposes real place/date copy.
- Empty dashboards still omit overview, charts, ranking, and postcards rather than rendering static examples.
- Focused web tests and build pass without touching sidebar or Prisma schema files.
</success_criteria>

<output>
After completion, create `.planning/phases/47-dashboard/47-04-SUMMARY.md`.
</output>

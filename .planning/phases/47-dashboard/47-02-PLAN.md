---
phase: 47-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 47-01
files_modified:
  - apps/web/src/services/memories/memory-chart-options.ts
  - apps/web/src/services/memories/memory-chart-options.spec.ts
  - apps/web/src/components/memories/MemoriesChartGrid.vue
  - apps/web/src/components/memories/MemoriesChartGrid.spec.ts
autonomous: true
requirements:
  - MEM-03
  - MEM-06
  - MEM-07
must_haves:
  truths:
    - "Four memories chart families are built from typed stats aggregates and render through the existing Yume Kawaii `BaseChart` foundation."
    - "D-01, D-02, and D-04 stay visible in chart options and sparse panel states: trend charts use dated trip buckets, country distribution uses trip counts, and no missing date becomes a demo point."
    - "D-03: chart modules remain all-time displays and do not add `全部时间` controls or any time-range response behavior."
    - "D-05 through D-08: the radar panel is named and explained as `旅途回忆画像`, not an unsupported style-score report."
    - "D-06: radar option builders avoid unsupported scenery, culture, food, or style-score dimensions."
    - "D-07: radar labels and supporting copy explain the shape as a gentle memories profile without overstating precision."
    - "Chart components are prop-driven display surfaces; no chart component fetches `/records/stats` or embeds populated fake data."
  artifacts:
    - path: "apps/web/src/services/memories/memory-chart-options.ts"
      provides: "Pure ECharts option builders for Phase 47 aggregates"
      contains: "buildMonthlyTrendOption"
    - path: "apps/web/src/components/memories/MemoriesChartGrid.vue"
      provides: "Four-panel memories chart grid over `BaseChart`"
      contains: "旅途回忆画像"
    - path: "apps/web/src/components/memories/MemoriesChartGrid.spec.ts"
      provides: "Chart grid state and real-prop wiring coverage"
      contains: "BaseChart"
  key_links:
    - from: "apps/web/src/services/memories/memory-chart-options.ts"
      to: "packages/contracts/src/stats.ts"
      via: "typed trend, distribution, and profile aggregate inputs"
      pattern: "TravelTrendBucket"
    - from: "apps/web/src/components/memories/MemoriesChartGrid.vue"
      to: "apps/web/src/components/common/BaseChart.vue"
      via: "four option props and BaseChart empty states"
      pattern: "BaseChart"
---

<objective>
Build the display-only chart option layer and chart grid for Phase 47 real aggregate data.

Purpose: The route view should compose tested chart panels instead of mixing ECharts option derivation, sparse-data messaging, and auth side effects in one page.
Output: Pure option builders and a prop-driven four-panel `MemoriesChartGrid`.
</objective>

<scope_guard>
User constraint: Phase 47 does not alter sidebar implementation or behavior. This frontend chart plan owns only memories services/components and does not touch `ShellSidebar.vue`.
</scope_guard>

<schema_rationale>
No Prisma schema file is modified in this plan. The stats contract from Plan 47-01 is consumed as frontend input, so no database push task applies.
</schema_rationale>

<decision_traceability>
- D-01, D-02, D-04: chart builders consume server trip-count buckets/distribution without generating missing-date points.
- D-03: chart grid has no `全部时间` or time-range control.
- D-05, D-06, D-07, D-08: the radar panel exposes explainable profile labels and initial-profile copy from real dimensions only.
- D-06: option builders and panel copy do not surface unsupported scenery, culture, food, or style-score dimensions.
- D-07: the radar is titled and explained as `旅途回忆画像` so users read it as a gentle memory profile rather than a precision rating report.
</decision_traceability>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-dashboard/47-CONTEXT.md
@.planning/phases/47-dashboard/47-RESEARCH.md
@.planning/phases/47-dashboard/47-PATTERNS.md
@.planning/phases/47-dashboard/47-VALIDATION.md
@.planning/phases/47-dashboard/47-UI-SPEC.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-04-SUMMARY.md
@packages/contracts/src/stats.ts
@apps/web/src/components/common/BaseChart.vue
@apps/web/src/components/common/BaseChart.spec.ts
@apps/web/src/components/showcase/UiChartShowcase.vue

<interfaces>
Plan 47-01 stats contract consumed by these chart surfaces:
```ts
export interface TravelMemoriesDashboard {
  monthlyTrend: TravelTrendBucket[]
  yearlyTrend: TravelTrendBucket[]
  countryDistribution: TravelCountryTripCount[]
  profile: TravelMemoriesProfileDimension[]
}
```

Existing chart wrapper contract:
```ts
export type YumeChartOption = ComposeOption<
  | LineSeriesOption
  | PieSeriesOption
  | BarSeriesOption
  | RadarSeriesOption
  | GridComponentOption
  | LegendComponentOption
  | TitleComponentOption
  | TooltipComponentOption
>
```

Required grid input for the next route plan:
```ts
defineProps<{ dashboard: TravelMemoriesDashboard }>()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build pure chart options from memories aggregates</name>
  <files>apps/web/src/services/memories/memory-chart-options.ts, apps/web/src/services/memories/memory-chart-options.spec.ts</files>
  <read_first>
    - apps/web/src/services/memories/memory-chart-options.ts
    - apps/web/src/services/memories/memory-chart-options.spec.ts
    - apps/web/src/components/common/BaseChart.vue
    - apps/web/src/services/timeline.ts
    - apps/web/src/services/timeline.spec.ts
    - packages/contracts/src/stats.ts
    - .planning/phases/47-dashboard/47-CONTEXT.md
    - .planning/phases/47-dashboard/47-PATTERNS.md
  </read_first>
  <behavior>
    - Test: monthly line option maps `period` labels and `tripCount` values from D-01 trend buckets only.
    - Test: donut country option maps repeated D-02 trip counts instead of unique-country counts.
    - Test: yearly bar and profile radar options expose only caller-provided aggregate values and exact profile labels from the contract.
    - Test: empty arrays remain empty option data and never receive showcase/demo values.
  </behavior>
  <action>Create `memory-chart-options.ts` as a pure transform module following the timeline service test pattern. Export four exact functions returning `YumeChartOption`: `buildMonthlyTrendOption(points: TravelTrendBucket[])`, `buildCountryDistributionOption(items: TravelCountryTripCount[])`, `buildYearlyTrendOption(points: TravelTrendBucket[])`, and `buildMemoriesProfileOption(dimensions: TravelMemoriesProfileDimension[])`. Monthly and yearly options use `period` for category axes and one `旅行次数` series; country distribution uses pie data `{ name: countryLabel, value: tripCount }` with donut-style radius; profile uses `indicator` entries from `label` and `max` plus one radar series from `value`. Import only typed aggregate inputs from `@trip-map/contracts` and `YumeChartOption` from `BaseChart`; do not copy `UiChartShowcase` demo arrays, hard-code populated bucket labels, add time filters, or move chart fetches into this service.</action>
  <acceptance_criteria>
    - `apps/web/src/services/memories/memory-chart-options.ts` contains `buildMonthlyTrendOption`.
    - `apps/web/src/services/memories/memory-chart-options.ts` contains `buildCountryDistributionOption`.
    - `apps/web/src/services/memories/memory-chart-options.ts` contains `buildYearlyTrendOption`.
    - `apps/web/src/services/memories/memory-chart-options.ts` contains `buildMemoriesProfileOption`.
    - `apps/web/src/services/memories/memory-chart-options.ts` contains `name: '旅行次数'`.
    - `apps/web/src/services/memories/memory-chart-options.spec.ts` contains `tripCount`.
    - `apps/web/src/services/memories/memory-chart-options.spec.ts` contains `countryLabel`.
    - `rg -n "demoOption|全部时间|fetchStats|createdAt" apps/web/src/services/memories/memory-chart-options.ts` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/services/memories/memory-chart-options.spec.ts</automated>
  </verify>
  <done>Line, donut, bar, and radar option builders are pure, typed, and driven only by real stats aggregates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose four truthful memories chart panels</name>
  <files>apps/web/src/components/memories/MemoriesChartGrid.vue, apps/web/src/components/memories/MemoriesChartGrid.spec.ts</files>
  <read_first>
    - apps/web/src/components/memories/MemoriesChartGrid.vue
    - apps/web/src/components/memories/MemoriesChartGrid.spec.ts
    - apps/web/src/services/memories/memory-chart-options.ts
    - apps/web/src/components/common/BaseChart.vue
    - apps/web/src/components/common/BaseChart.spec.ts
    - packages/contracts/src/stats.ts
    - .planning/phases/47-dashboard/47-UI-SPEC.md
    - .planning/phases/47-dashboard/47-CONTEXT.md
  </read_first>
  <behavior>
    - Test: chart grid renders panel titles `旅途足迹趋势`, `足迹国家/地区分布`, `年度旅途趋势`, and `旅途回忆画像`.
    - Test: each chart panel renders `BaseChart` props built from the passed `dashboard`, not a component-local fetch or demo series.
    - Test: empty monthly/yearly bucket arrays show exact sparse-date copy `这些足迹还没有可用于趋势统计的旅行日期。`.
    - Test: a non-empty `profile` shows initial real-data copy `这是根据现有足迹描出的初始回忆画像。`.
  </behavior>
  <action>Create a typed `<script setup lang="ts">` `MemoriesChartGrid` that accepts `dashboard: TravelMemoriesDashboard`, computes four option objects from Task 1, and renders all canvases through `BaseChart`. Use stable panel hooks `data-region="memories-chart-grid"`, `data-chart-panel="monthly-trend"`, `data-chart-panel="country-distribution"`, `data-chart-panel="yearly-trend"`, and `data-chart-panel="memories-profile"`. Apply UI-SPEC titles exactly. When `monthlyTrend` or `yearlyTrend` is empty, set the relevant `BaseChart` `empty` state and render exact DOM copy `这些足迹还没有可用于趋势统计的旅行日期。`; do not synthesize the current month or year. Name and explain the radar as D-05 through D-08 `旅途回忆画像` and render exact sparse-profile helper copy `这是根据现有足迹描出的初始回忆画像。` whenever profile dimensions exist. Do not add a dropdown, filter toolbar, raw `VChart`, legend interactions that mutate records, or `v-html` tooltips.</action>
  <acceptance_criteria>
    - `apps/web/src/components/memories/MemoriesChartGrid.vue` contains `data-region="memories-chart-grid"`.
    - `apps/web/src/components/memories/MemoriesChartGrid.vue` contains `旅途足迹趋势`.
    - `apps/web/src/components/memories/MemoriesChartGrid.vue` contains `足迹国家/地区分布`.
    - `apps/web/src/components/memories/MemoriesChartGrid.vue` contains `年度旅途趋势`.
    - `apps/web/src/components/memories/MemoriesChartGrid.vue` contains `旅途回忆画像`.
    - `apps/web/src/components/memories/MemoriesChartGrid.vue` contains `这些足迹还没有可用于趋势统计的旅行日期。`.
    - `apps/web/src/components/memories/MemoriesChartGrid.vue` contains `这是根据现有足迹描出的初始回忆画像。`.
    - `rg -n "VChart|v-html|全部时间|fetchStats" apps/web/src/components/memories/MemoriesChartGrid.vue` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/services/memories/memory-chart-options.spec.ts src/components/memories/MemoriesChartGrid.spec.ts src/components/common/BaseChart.spec.ts</automated>
  </verify>
  <done>The chart grid exposes four real-data chart panels with truthful sparse states and a gentle memories-profile radar contract ready for route composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared stats payload -> chart option builders | Account aggregate labels and values become chart series and tooltips. |
| chart option builders -> `BaseChart` | ECharts receives bounded real data and state flags from display code. |
| dashboard copy -> user | Profile labels must not spoof unsupported record meaning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02 | Tampering | `memory-chart-options.ts` | mitigate | Build all chart series from typed aggregate arrays and test empty inputs so no fake showcase values leak into production. |
| T-47-08 | Spoofing | radar panel labels | mitigate | Use real profile dimensions and explicit `旅途回忆画像` copy per D-05 through D-08 instead of mock style scores. |
| T-47-09 | Information Disclosure | chart markup/tooltips | mitigate | Render text with Vue interpolation and `BaseChart`; do not add `v-html` or free-form notes/tag HTML tooltip content. |
</threat_model>

<verification>
Chart helper and chart-grid gate:

```bash
pnpm --filter @trip-map/web test -- src/services/memories/memory-chart-options.spec.ts src/components/memories/MemoriesChartGrid.spec.ts src/components/common/BaseChart.spec.ts
```
</verification>

<success_criteria>
- Four option builder functions map typed Plan 47-01 aggregates into line, donut, bar, and radar ECharts options.
- `MemoriesChartGrid` renders all four UI-SPEC panels through `BaseChart`.
- Sparse trend/profile copy remains truthful and no populated fake data, time filter, sidebar change, or Prisma schema change is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/47-dashboard/47-02-SUMMARY.md`.
</output>

---
phase: 47-dashboard
plan: 03
type: execute
wave: 3
depends_on:
  - 47-01
  - 47-02
files_modified:
  - apps/web/src/components/memories/MemoriesOverviewGrid.vue
  - apps/web/src/components/memories/MemoriesOverviewGrid.spec.ts
  - apps/web/src/views/StatisticsPageView.vue
  - apps/web/src/views/StatisticsPageView.spec.ts
  - apps/web/src/stores/stats.spec.ts
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-06
  - MEM-07
must_haves:
  truths:
    - "Authenticated `/memories` presents the user-facing `旅途回忆` dashboard title, four real overview values, and chart grid while retaining existing auth/restoring/error/empty states."
    - "Empty accounts keep one map CTA state and do not mount chart, ranking, or postcard populated modules."
    - "Stats store/session and record-revision refresh protections remain the only dashboard data lifecycle path."
    - "MEM-01 is handled without reviving legacy `/statistics` route behavior or touching sidebar navigation."
  artifacts:
    - path: "apps/web/src/components/memories/MemoriesOverviewGrid.vue"
      provides: "Four-card Phase 47 overview section"
      contains: "去过城市或行政区"
    - path: "apps/web/src/views/StatisticsPageView.vue"
      provides: "`/memories` route state orchestration plus overview/chart composition"
      contains: "MemoriesChartGrid"
    - path: "apps/web/src/views/StatisticsPageView.spec.ts"
      provides: "Route populated/empty/freshness behavior coverage"
      contains: "memories-overview"
  key_links:
    - from: "apps/web/src/stores/stats.ts"
      to: "apps/web/src/views/StatisticsPageView.vue"
      via: "existing `stats`, loading, error, and auth-bound refresh refs"
      pattern: "useStatsStore"
    - from: "apps/web/src/views/StatisticsPageView.vue"
      to: "apps/web/src/components/memories/MemoriesOverviewGrid.vue"
      via: "real `TravelStatsResponse` prop"
      pattern: "MemoriesOverviewGrid"
    - from: "apps/web/src/views/StatisticsPageView.vue"
      to: "apps/web/src/components/memories/MemoriesChartGrid.vue"
      via: "`stats.memories` prop"
      pattern: "MemoriesChartGrid"
---

<objective>
Upgrade the `/memories` route composition with the four-card overview and real chart grid while preserving its account-bound state machine.

Purpose: The user-visible dashboard must become `旅途回忆` without losing the existing auth, stale-response, record-revision, and honest-empty protections.
Output: Overview section, route composition changes, and store/view tests over the expanded stats payload.
</objective>

<scope_guard>
User constraint: do not pull sidebar rename or navigation behavior work into this phase. Keep MEM-01 work in the `/memories` view and tests; `ShellSidebar.vue` stays outside this plan.
</scope_guard>

<schema_rationale>
This plan consumes the stats response in Vue files only. It modifies no Prisma schema path and therefore has no schema push task.
</schema_rationale>

<decision_traceability>
- D-01 through D-04: the route composes charts from Plan 47-02 real stats buckets and does not add all-time filter controls.
- D-05 through D-08: `旅途回忆画像` stays part of the real dashboard chart grid with initial-profile copy.
- Deferred context: empty state does not display fake charts, upload, favorites, achievements, or new trip creation UI.
</decision_traceability>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-dashboard/47-CONTEXT.md
@.planning/phases/47-dashboard/47-RESEARCH.md
@.planning/phases/47-dashboard/47-PATTERNS.md
@.planning/phases/47-dashboard/47-VALIDATION.md
@.planning/phases/47-dashboard/47-UI-SPEC.md
@.planning/phases/46-travel-journal-refactor/46-04-SUMMARY.md
@packages/contracts/src/stats.ts
@apps/web/src/components/statistics/StatCard.vue
@apps/web/src/stores/stats.ts
@apps/web/src/stores/stats.spec.ts
@apps/web/src/views/StatisticsPageView.vue
@apps/web/src/views/StatisticsPageView.spec.ts
@apps/web/src/router/index.ts

<interfaces>
Expanded stats response expected from Plan 47-01:
```ts
export interface TravelStatsResponse {
  totalTrips: number
  uniquePlaces: number
  visitedAdministrativeAreas: number
  visitedCountries: number
  totalSupportedCountries: number
  memories: TravelMemoriesDashboard
}
```

Existing route state guard to preserve:
```ts
const shouldShowEmptyState = computed(() =>
  status.value === 'authenticated'
  && !isLoading.value
  && error.value === null
  && (stats.value?.totalTrips ?? 0) === 0,
)
```

Chart grid interface from Plan 47-02:
```ts
defineProps<{ dashboard: TravelMemoriesDashboard }>()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the four-card real overview section</name>
  <files>apps/web/src/components/memories/MemoriesOverviewGrid.vue, apps/web/src/components/memories/MemoriesOverviewGrid.spec.ts</files>
  <read_first>
    - apps/web/src/components/memories/MemoriesOverviewGrid.vue
    - apps/web/src/components/memories/MemoriesOverviewGrid.spec.ts
    - apps/web/src/components/statistics/StatCard.vue
    - apps/web/src/components/statistics/StatCard.spec.ts
    - packages/contracts/src/stats.ts
    - .planning/phases/47-dashboard/47-UI-SPEC.md
    - .planning/phases/47-dashboard/47-RESEARCH.md
  </read_first>
  <behavior>
    - Test: overview renders exactly four card labels `总旅行次数`, `去过地点`, `去过城市或行政区`, and `去过国家/地区`.
    - Test: card values come from `totalTrips`, `uniquePlaces`, `visitedAdministrativeAreas`, and `visitedCountries`.
    - Test: overview accepts typed stats props and has no secondary fake deltas or achievement badges.
  </behavior>
  <action>Create `MemoriesOverviewGrid.vue` as a focused typed display section with `defineProps<{ stats: TravelStatsResponse }>()`. Use `data-region="memories-overview"` and keep one card per UI-SPEC label in exact order: `总旅行次数`, `去过地点`, `去过城市或行政区`, `去过国家/地区`. Reuse the existing `StatCard` display pattern where it fits; the values must read `stats.totalTrips`, `stats.uniquePlaces`, `stats.visitedAdministrativeAreas`, and `stats.visitedCountries` without manufacturing comparison percentages or achievements. Use compact semantic units such as `次旅行`, `个地点`, `个城市/行政区`, and `个国家/地区`; no `ShellSidebar` changes are required for these labels.</action>
  <acceptance_criteria>
    - `apps/web/src/components/memories/MemoriesOverviewGrid.vue` contains `data-region="memories-overview"`.
    - `apps/web/src/components/memories/MemoriesOverviewGrid.vue` contains `总旅行次数`.
    - `apps/web/src/components/memories/MemoriesOverviewGrid.vue` contains `去过城市或行政区`.
    - `apps/web/src/components/memories/MemoriesOverviewGrid.vue` contains `visitedAdministrativeAreas`.
    - `apps/web/src/components/memories/MemoriesOverviewGrid.spec.ts` contains `去过国家/地区`.
    - `rg -n "achievement|徽章|环比|同比|假的" apps/web/src/components/memories/MemoriesOverviewGrid.vue` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/components/memories/MemoriesOverviewGrid.spec.ts src/components/statistics/StatCard.spec.ts</automated>
  </verify>
  <done>The dashboard overview has four real aggregate cards and the city/admin card is backed by the explicit `visitedAdministrativeAreas` contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose overview and charts into the protected memories route</name>
  <files>apps/web/src/views/StatisticsPageView.vue, apps/web/src/views/StatisticsPageView.spec.ts, apps/web/src/stores/stats.spec.ts</files>
  <read_first>
    - apps/web/src/views/StatisticsPageView.vue
    - apps/web/src/views/StatisticsPageView.spec.ts
    - apps/web/src/stores/stats.ts
    - apps/web/src/stores/stats.spec.ts
    - apps/web/src/components/memories/MemoriesOverviewGrid.vue
    - apps/web/src/components/memories/MemoriesChartGrid.vue
    - apps/web/src/router/index.ts
    - .planning/phases/47-dashboard/47-CONTEXT.md
    - .planning/phases/47-dashboard/47-VALIDATION.md
    - .planning/phases/47-dashboard/47-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: populated authenticated route renders `旅途回忆`, `[data-region="memories-overview"]`, and `[data-region="memories-chart-grid"]` from a stats payload.
    - Test: zero trips keep existing empty copy and do not render overview/chart populated sections.
    - Test: the stats store still ignores stale responses after auth boundary changes while accepting the expanded memories payload.
    - Test: travel-record revision refresh behavior continues to fetch a fresh expanded stats payload.
  </behavior>
  <action>Keep `StatisticsPageView.vue` as the route/state orchestrator from the pattern map: retain authenticated-only fetch, boundary reset, `travelRecordRevision` follow-up refresh, restoring/anonymous/error/empty branches, and existing empty CTA copy `去世界足迹留下足迹`. Replace the old populated three-card branch with a Phase 47 populated composition that renders the route title `旅途回忆`, one concise dashboard support sentence, `MemoriesOverviewGrid :stats="stats!"`, and `MemoriesChartGrid :dashboard="stats!.memories"`. Do not add a legacy `/statistics` route, time filter, local static series, new travel CTA outside the empty recovery state, or sidebar work. Update view/store fixtures and assertions to include `visitedAdministrativeAreas` and full `memories` arrays so the existing Pinia lifecycle proves the expanded payload crosses session and record revisions through the same `fetchStats` boundary.</action>
  <acceptance_criteria>
    - `apps/web/src/views/StatisticsPageView.vue` contains `MemoriesOverviewGrid`.
    - `apps/web/src/views/StatisticsPageView.vue` contains `MemoriesChartGrid`.
    - `apps/web/src/views/StatisticsPageView.vue` contains `旅途回忆`.
    - `apps/web/src/views/StatisticsPageView.spec.ts` contains `memories-overview`.
    - `apps/web/src/views/StatisticsPageView.spec.ts` contains `memories-chart-grid`.
    - `apps/web/src/views/StatisticsPageView.spec.ts` contains `visitedAdministrativeAreas`.
    - `apps/web/src/stores/stats.spec.ts` contains `visitedAdministrativeAreas`.
    - `rg -n "全部时间|旅行统计|/statistics" apps/web/src/views/StatisticsPageView.vue` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @trip-map/web test -- src/stores/stats.spec.ts src/views/StatisticsPageView.spec.ts src/router/index.spec.ts src/components/memories/MemoriesOverviewGrid.spec.ts src/components/memories/MemoriesChartGrid.spec.ts</automated>
  </verify>
  <done>`/memories` shows real overview and chart content only for populated authenticated accounts while empty and refresh protections remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth/session store -> `/memories` view | Private aggregate data is rendered only under authenticated route states. |
| stats store fetch lifecycle -> expanded dashboard props | Stale responses after logout/account switch must stay suppressed. |
| route state branch -> empty/populated sections | Empty accounts must not receive misleading fake populated content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | Information Disclosure | `StatisticsPageView.vue` and stats store | mitigate | Preserve existing auth boundary reset, request-id behavior, and authenticated-only route composition; extend tests with expanded payloads. |
| T-47-10 | Tampering | overview/chart populated branch | mitigate | Render only stats payload fields and assert zero-trip empty branch does not mount populated modules. |
| T-47-11 | Spoofing | route/copy semantics | mitigate | Keep user-facing title `旅途回忆` and avoid reviving `/statistics`, static BI deltas, or unsupported dashboard controls. |
</threat_model>

<verification>
Route, store, router, and memories section gate:

```bash
pnpm --filter @trip-map/web test -- src/stores/stats.spec.ts src/views/StatisticsPageView.spec.ts src/router/index.spec.ts src/components/memories/MemoriesOverviewGrid.spec.ts src/components/memories/MemoriesChartGrid.spec.ts
```
</verification>

<success_criteria>
- Four overview values and four chart panels are visible in populated `/memories` through real stats props.
- Empty, anonymous, loading/restoring, and error states remain route-owned and avoid fake modules.
- Existing stats refresh/session protections stay covered by store and view tests.
- Sidebar and Prisma schema surfaces remain outside file ownership.
</success_criteria>

<output>
After completion, create `.planning/phases/47-dashboard/47-03-SUMMARY.md`.
</output>

---
phase: 34-nyquist-validation-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/27-multi-visit-record-foundation/27-VALIDATION.md
  - .planning/phases/29-timeline-page-and-account-entry/29-VALIDATION.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Phase 27 VALIDATION.md 的 frontmatter 已标记 nyquist_compliant: true 和 wave_0_complete: true"
    - "Phase 27 VALIDATION.md 的 Per-Task Verification Map 中每行的 File Exists 和 Status 已根据实际 SUMMARY 证据更新"
    - "Phase 27 VALIDATION.md 的 Validation Sign-Off checklist 已全部勾选"
    - "Phase 29 VALIDATION.md 的 frontmatter 已标记 nyquist_compliant: true 和 wave_0_complete: true"
    - "Phase 29 VALIDATION.md 的 Per-Task Verification Map 中每行的 File Exists 和 Status 已根据实际 SUMMARY 证据更新"
    - "Phase 29 VALIDATION.md 的 Validation Sign-Off checklist 已全部勾选"
  artifacts:
    - path: ".planning/phases/27-multi-visit-record-foundation/27-VALIDATION.md"
      provides: "Phase 27 full Nyquist compliance validator"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/29-timeline-page-and-account-entry/29-VALIDATION.md"
      provides: "Phase 29 full Nyquist compliance validator"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "27-VALIDATION.md"
      to: "27-01-SUMMARY.md 至 27-06-SUMMARY.md"
      via: "Per-Task Verification Map 中的 Automated Command 结果来自 SUMMARY 文件中的实际测试输出"
      pattern: "退出码 0|✅|75 tests"
    - from: "29-VALIDATION.md"
      to: "29-01-SUMMARY.md 至 29-04-SUMMARY.md"
      via: "Per-Task Verification Map 中的 Automated Command 结果来自 SUMMARY 文件中的实际测试输出"
      pattern: "退出码 0|test|spec"
---

<objective>
更新 Phase 27 和 Phase 29 的 VALIDATION.md 文件使其达到完整 Nyquist 合规标准。

**Purpose:** 关闭 v6.0 milestone audit 中识别的 Nyquist 验证技术债务。Phase 27 和 Phase 29 的 VALIDATION.md 当前均为 draft 状态，frontmatter 中 nyquist_compliant=false, wave_0_complete=false。本计划将其升级为与 Phase 28 一致的完整合规状态。

**Output:**
- `.planning/phases/27-multi-visit-record-foundation/27-VALIDATION.md`（已修改）— frontmatter status 改为 approved，nyquist_compliant + wave_0_complete 为 true，Per-Task Verification Map 填完，Sign-Off 勾选
- `.planning/phases/29-timeline-page-and-account-entry/29-VALIDATION.md`（已修改）— 同上
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-nyquist-validation-coverage/34-CONTEXT.md
@.planning/phases/28-overseas-coverage-expansion/28-VALIDATION.md

<prior_evidence>
## Phase 27 测试结果摘要（来自 27-01 至 27-06 SUMMARY.md）

**Plan 01 (Schema + Contracts):**
- `pnpm --filter @trip-map/contracts build` → 0
- `pnpm --filter @trip-map/contracts typecheck` → 0
- `pnpm --filter @trip-map/server exec prisma validate --schema=prisma/schema.prisma` → 0
- `pnpm --filter @trip-map/server exec prisma db push --accept-data-loss` → 0

**Plan 02 (Backend Records + Auth):**
- `pnpm --filter @trip-map/server test --run src/modules/records` — 2 files, 9 tests ✅
- `pnpm --filter @trip-map/server test` — 15 spec files, 85 tests ✅
- `pnpm --filter @trip-map/server typecheck` → 0

**Plan 03 (Store + Legacy Migration):**
- `pnpm --filter @trip-map/web test --run src/stores/map-points.spec.ts` — 35/35 ✅
- `pnpm --filter @trip-map/web test` — 28 specs, 239 tests ✅
- `pnpm --filter @trip-map/web typecheck` → 0

**Plan 04 (TripDateForm UI + Popup):**
- `pnpm --filter @trip-map/web test --run src/components/map-popup/TripDateForm.spec.ts src/components/map-popup/PointSummaryCard.spec.ts src/components/LeafletMapStage.spec.ts` — 3 specs, 49 tests ✅
- `pnpm --filter @trip-map/web typecheck` → 0

**Plan 05 (Latest-trip-by-date gap closure):**
- `pnpm --filter @trip-map/web test --run src/components/LeafletMapStage.spec.ts -t "selects the latest trip by travel date"` — RED→GREEN ✅
- `pnpm --filter @trip-map/web test` — 29 files, 255 tests ✅

**Plan 06 (DTO binding + unique constraint gap closure):**
- `pnpm --filter @trip-map/contracts build` → 0
- `pnpm --filter @trip-map/server typecheck` → 0
- `pnpm --filter @trip-map/server test` — 15 files, 88 tests ✅
- `pnpm --filter @trip-map/web typecheck` → 0
- `pnpm --filter @trip-map/web test --run src/services/legacy-point-storage.spec.ts src/stores/map-points.spec.ts` — 2 files, 39 tests ✅

## Phase 29 测试结果摘要（来自 29-01 至 29-04 SUMMARY.md）

**Plan 01 (Router + Account Menu Entry):**
- `pnpm --filter @trip-map/web typecheck` → 0
- Router config and AuthTopbarControl navigation created

**Plan 02 (Timeline Data Layer):**
- `pnpm --filter @trip-map/web test --run src/services/timeline.spec.ts src/stores/map-points.spec.ts` — timeline + store tests ✅
- `pnpm --filter @trip-map/web typecheck` → 0

**Plan 03 (Timeline Page UI):**
- `pnpm --filter @trip-map/web typecheck` → 0
- TimelinePageView four-state rendering implemented

**Plan 04 (Regression Tests):**
- `pnpm --filter @trip-map/web test -- src/App.spec.ts src/components/auth/AuthTopbarControl.spec.ts src/views/TimelinePageView.spec.ts src/services/timeline.spec.ts` — 4 specs ✅
- `pnpm --filter @trip-map/web typecheck` → 0
</prior_evidence>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 升级 Phase 27 VALIDATION.md 至 Nyquist 完整合规</name>
  <files>
    .planning/phases/27-multi-visit-record-foundation/27-VALIDATION.md
  </files>

  <read_first>
  - .planning/phases/27-multi-visit-record-foundation/27-VALIDATION.md（目标文件，读取当前状态）
  - .planning/phases/28-overseas-coverage-expansion/28-VALIDATION.md（权威模板 — nyquist_compliant: true 的参考实现）
  - .planning/phases/27-multi-visit-record-foundation/27-01-SUMMARY.md 至 27-06-SUMMARY.md（SUMMARY 中记录的实际测试结果）
  - .planning/phases/34-nyquist-validation-coverage/34-CONTEXT.md（D-01 并行执行，D-02 完整合规标准）
  </read_first>

  <action>
  修改 `.planning/phases/27-multi-visit-record-foundation/27-VALIDATION.md`，按以下步骤操作：

  ### 1. 更新 Frontmatter
  将 frontmatter 从：
  ```yaml
  status: draft
  nyquist_compliant: false
  wave_0_complete: false
  ```
  改为：
  ```yaml
  status: approved
  nyquist_compliant: true
  wave_0_complete: true
  ```

  ### 2. 更新 Per-Task Verification Map
  Per D-02 要求：每个 VALIDATION.md 的 Per-Task Verification Map 必须 fill all rows（threat ref, secure behavior, test type, automated command）。

  现有 8 行记录的表格（27-01-01 至 27-04-02）需要根据实际 SUMMARY 证据更新 File Exists 和 Status 列。对于每行：

  - **File Exists 从 `❌ W0` 改为 `✅`**：根据 27-01 至 27-06 SUMMARY.md 中的测试验证结果，所有命令文件均已创建并可运行。
    - 27-01-01：`pnpm --filter @trip-map/server test` — 15 spec files / 88 tests 通过（27-06-SUMMARY）
    - 27-01-02：`pnpm --filter @trip-map/contracts build` — 退出码 0（27-01-SUMMARY）
    - 27-02-01：`pnpm --filter @trip-map/server test` — records.service.spec.ts + repository.spec.ts 通过（27-02-SUMMARY）
    - 27-02-02：`pnpm --filter @trip-map/server test` — auth-bootstrap e2e 通过（27-02-SUMMARY）
    - 27-03-01：`pnpm --filter @trip-map/web test` — map-points.spec.ts 35/35 通过（27-03-SUMMARY）
    - 27-03-02：`pnpm --filter @trip-map/web test` — 28 specs / 239 tests 通过（27-03-SUMMARY）
    - 27-04-01：`pnpm --filter @trip-map/web test` — TripDateForm.spec.ts 测试通过（27-04-SUMMARY）
    - 27-04-02：`pnpm --filter @trip-map/web test` — PointSummaryCard.spec.ts + LeafletMapStage.spec.ts 测试通过（27-04-SUMMARY）

  - **Status 从 `⬜ pending` 改为 `✅ green`**：所有测试在对应 Plan 执行时均已通过。

  - **保持 Secure Behavior 和 Test Type 不变** — 这些是设计时的验证意图，不需要修改。

  最终表格样式（参考 28-VALIDATION.md）：每行的状态列使用 `✅ green`，File Exists 使用 `✅`。

  ### 3. 更新 Wave 0 Requirements
  当前 Wave 0 列表中的文件路径与实际仓库路径不完全一致。检查每个路径：

  - `apps/server/src/modules/records/__tests__/records.service.spec.ts` → 实际路径为 `apps/server/src/modules/records/records.service.spec.ts`（已创建，在 27-02-SUMMARY 中确认）
  - `apps/server/src/modules/auth/__tests__/auth.service.spec.ts` → 实际路径为 `apps/server/src/modules/auth/auth.service.spec.ts`（已创建，在 27-02-SUMMARY 中确认）
  - `apps/web/src/stores/__tests__/map-points.spec.ts` → 实际路径为 `apps/web/src/stores/map-points.spec.ts`（已创建，在 27-03-SUMMARY 中确认）
  - `apps/web/src/components/map-popup/__tests__/PointSummaryCard.spec.ts` → 实际路径为 `apps/web/src/components/map-popup/PointSummaryCard.spec.ts`（已创建，在 27-04-SUMMARY 中确认）

  将每条 Wave 0 路径修正为实际仓库路径，并将所有 checkbox `[ ]` 改为 `[x]`。

  ### 4. 更新 Validation Sign-Off
  将 Sign-Off 检查清单中的所有 `[ ]` 改为 `[x]`：

  - [x] All tasks have `<automated>` verify or Wave 0 dependencies
  - [x] Sampling continuity: no 3 consecutive tasks without automated verify
  - [x] Wave 0 covers all MISSING references
  - [x] No watch-mode flags
  - [x] Feedback latency < 30s
  - [x] `nyquist_compliant: true` set in frontmatter

  将 **Approval:** 从 `pending` 改为 `approved 2026-04-28`

  ### 5. 保留现有内容
  **不删除**现有的 Manual-Only Verifications 表格、Test Infrastructure 表和 Sampling Rate 部分。这些内容已经完整，只需与升级后的 frontmatter 共存。
  </action>

  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/27-multi-visit-record-foundation/27-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/27-multi-visit-record-foundation/27-VALIDATION.md && grep -q "status: approved" .planning/phases/27-multi-visit-record-foundation/27-VALIDATION.md && grep -q "✅ green" .planning/phases/27-multi-visit-record-foundation/27-VALIDATION.md && grep -q "Approval:.*approved" .planning/phases/27-multi-visit-record-foundation/27-VALIDATION.md</automated>
  </verify>

  <acceptance_criteria>
  - `27-VALIDATION.md` frontmatter 包含 `nyquist_compliant: true`
  - `27-VALIDATION.md` frontmatter 包含 `wave_0_complete: true`
  - `27-VALIDATION.md` frontmatter 包含 `status: approved`
  - Per-Task Verification Map 中所有 Status 单元格为 `✅ green`
  - Wave 0 列表所有 checkbox 为 `[x]`
  - Validation Sign-Off 所有 checkbox 为 `[x]`
  - Approval 行包含日期（如 `approved 2026-04-28`）
  - 所有文件路径已修正为实际仓库路径（不含 `__tests__/` 子目录）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 升级 Phase 29 VALIDATION.md 至 Nyquist 完整合规</name>
  <files>
    .planning/phases/29-timeline-page-and-account-entry/29-VALIDATION.md
  </files>

  <read_first>
  - .planning/phases/29-timeline-page-and-account-entry/29-VALIDATION.md（目标文件，读取当前状态）
  - .planning/phases/28-overseas-coverage-expansion/28-VALIDATION.md（权威模板）
  - .planning/phases/29-timeline-page-and-account-entry/29-01-SUMMARY.md 至 29-04-SUMMARY.md
  - .planning/phases/34-nyquist-validation-coverage/34-CONTEXT.md
  </read_first>

  <action>
  修改 `.planning/phases/29-timeline-page-and-account-entry/29-VALIDATION.md`，按以下步骤操作：

  ### 1. 更新 Frontmatter
  将 frontmatter 从：
  ```yaml
  status: draft
  nyquist_compliant: false
  wave_0_complete: false
  ```
  改为：
  ```yaml
  status: approved
  nyquist_compliant: true
  wave_0_complete: true
  ```

  ### 2. 更新 Per-Task Verification Map
  现有 5 行（29-01-01 至 29-04-01）。更新 File Exists 和 Status：

  - **29-01-01** （Route setup keeps timeline in SPA shell）:
    - Threat Ref: 原 `—` → 无变化（无安全边界）
    - Automated Command: `pnpm --filter @trip-map/web typecheck` — 退出码 0（29-01-SUMMARY）
    - File Exists: `✅`（router/index.ts 和 views 均在 29-01 创建）
    - Status: `✅ green`

  - **29-01-02** （Authenticated menu shows timeline CTA）:
    - Automated Command: `pnpm --filter @trip-map/web test -- src/components/auth/AuthTopbarControl.spec.ts` — 在 29-04 中创建并通过（29-04-SUMMARY）
    - File Exists: `✅`（AuthTopbarControl.spec.ts 在 29-04 中创建）
    - Status: `✅ green`

  - **29-02-01** （Timeline entries preserve one record per trip）:
    - Automated Command: `pnpm --filter @trip-map/web test -- src/services/timeline.spec.ts src/stores/map-points.spec.ts` — 在 29-02 中创建并通过（29-02-SUMMARY）
    - File Exists: `✅`（timeline.spec.ts 在 29-02 创建）
    - Status: `✅ green`

  - **29-03-01** （Timeline page renders authenticated list, anonymous CTA, empty/restoring states）:
    - Automated Command: `pnpm --filter @trip-map/web test -- src/views/TimelinePageView.spec.ts` — 在 29-04 中创建并通过（29-04-SUMMARY）
    - File Exists: `✅`（TimelinePageView.spec.ts 在 29-04 创建）
    - Status: `✅ green`

  - **29-04-01** （Route, menu, and timeline ordering stay correct end-to-end）:
    - Automated Command: `pnpm --filter @trip-map/web test -- src/App.spec.ts src/components/auth/AuthTopbarControl.spec.ts src/views/TimelinePageView.spec.ts src/services/timeline.spec.ts` — 在 29-04 中通过（29-04-SUMMARY）
    - File Exists: `✅`（4 个 spec 文件均已在 Phase 29 中创建）
    - Status: `✅ green`

  ### 3. 更新 Wave 0 Requirements
  当前 Wave 0 列表：

  - `apps/web/src/components/auth/AuthTopbarControl.spec.ts` — ✅ 在 29-04 创建：commit 0be15f8
  - `apps/web/src/views/TimelinePageView.spec.ts` — ✅ 在 29-04 创建：commit 6badc58
  - `apps/web/src/services/timeline.spec.ts` — ✅ 在 29-02 创建：commit 15b8e84

  将每条 checkbox 从 `[ ]` 改为 `[x]`。路径无需修改（已准确）。

  ### 4. 更新 Validation Sign-Off
  将检查清单中的所有 `[ ]` 改为 `[x]`，将 Approval 从 `pending` 改为 `approved 2026-04-28`。

  ### 5. 保留现有内容
  保留 Test Infrastructure、Sampling Rate、Manual-Only Verifications 表格不变。
  </action>

  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/29-timeline-page-and-account-entry/29-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/29-timeline-page-and-account-entry/29-VALIDATION.md && grep -q "status: approved" .planning/phases/29-timeline-page-and-account-entry/29-VALIDATION.md && grep -q "✅ green" .planning/phases/29-timeline-page-and-account-entry/29-VALIDATION.md && grep -q "Approval:.*approved" .planning/phases/29-timeline-page-and-account-entry/29-VALIDATION.md</automated>
  </verify>

  <acceptance_criteria>
  - `29-VALIDATION.md` frontmatter 包含 `nyquist_compliant: true`
  - `29-VALIDATION.md` frontmatter 包含 `wave_0_complete: true`
  - `29-VALIDATION.md` frontmatter 包含 `status: approved`
  - Per-Task Verification Map 中所有 5 行的 Status 为 `✅ green`
  - Wave 0 列表所有 checkbox 为 `[x]`
  - Validation Sign-Off 所有 checkbox 为 `[x]`
  - Approval 行包含日期
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 34 is a documentation-only tech debt closure phase. No runtime code, API endpoints, or data flows are created or modified. No trust boundaries apply.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | VALIDATION.md files | mitigate | VALIDATION.md changes are tracked via git; frontmatter must match expected pattern (grep verification in automated verify) |
| T-34-02 | Spoofing | VALIDATION.md metadata | accept | VALIDATION.md is a planning artifact, not runtime configuration; no security impact |
</threat_model>

<verification>
1. All 4 target VALIDATION.md files updated: 27 and 29 in this plan, 30 and 32 in Plan 02
2. Each updated VALIDATION.md has nyquist_compliant: true + wave_0_complete: true frontmatter
3. Each updated VALIDATION.md has complete Per-Task Verification Map with ✅ green status on all rows
4. Each updated VALIDATION.md has complete Validation Sign-Off
5. grep verification passes for all frontmatter flags
</verification>

<success_criteria>
1. Phase 27 VALIDATION.md status: approved, nyquist_compliant: true, wave_0_complete: true
2. Phase 29 VALIDATION.md status: approved, nyquist_compliant: true, wave_0_complete: true
3. All Per-Task Verification Map entries in both files have correct File Exists (✅) and Status (✅ green)
4. All Wave 0 Requirements checked off with correct file paths
5. All Validation Sign-Off items checked; Approval dates recorded
6. Manual-Only Verifications preserved from original files
</success_criteria>

<output>
After completion, create `.planning/phases/34-nyquist-validation-coverage/34-01-SUMMARY.md`
</output>

---
phase: 28-overseas-coverage-expansion
plan: 05
type: execute
wave: 2
depends_on:
  - 28-04
files_modified:
  - apps/server/scripts/backfill-record-metadata.ts
  - apps/server/test/record-metadata-backfill.e2e-spec.ts
  - apps/server/test/auth-bootstrap.e2e-spec.ts
  - apps/server/test/records-sync.e2e-spec.ts
autonomous: true
requirements:
  - GEOX-02
tags:
  - gap-closure
  - phase-28
  - server
  - backfill
  - userTravelRecord
  - bootstrap
  - sync
gap_closure: true
closes_gaps:
  - "既有 overseas 记录会被回填到 Phase 28 metadata，并在 bootstrap / same-user sync / 后续 timeline 和统计消费者中一致复用"
user_setup: []
must_haves:
  truths:
    - "`backfill-record-metadata.ts` 会扫描并更新 `userTravelRecord`，而不再只处理 `travelRecord` / `smokeRecord`。"
    - "旧 `userTravelRecord` 在执行 backfill 后，会被覆盖成 Phase 28 canonical metadata：`datasetVersion`、`displayName`、`typeLabel`、`parentLabel`、`subtitle` 都与 canonical summary 一致。"
    - "`/auth/bootstrap` 与 same-user sync 的回放路径会直接看到升级后的 persisted metadata，而不是继续返回旧 `一级行政区` 文案。"
    - "不新增 consumer-side fallback，也不修改 `auth.service.ts` / `records.repository.ts` 的真源选择；继续复用 persisted `userTravelRecord`。"
  artifacts:
    - path: "apps/server/scripts/backfill-record-metadata.ts"
      provides: "覆盖 `userTravelRecord` 的 authoritative metadata backfill 脚本"
      contains: "matchedUserTravelRows"
    - path: "apps/server/test/auth-bootstrap.e2e-spec.ts"
      provides: "旧 `userTravelRecord -> backfill -> /auth/bootstrap` 迁移 e2e"
      contains: "backfillRecordMetadata"
    - path: "apps/server/test/records-sync.e2e-spec.ts"
      provides: "旧 `userTravelRecord -> backfill -> same-user sync` 迁移 e2e"
      contains: "backfillRecordMetadata"
  key_links:
    - from: "apps/server/scripts/backfill-record-metadata.ts"
      to: "apps/server/src/modules/auth/auth.service.ts"
      via: "bootstrap 直接回放 backfilled `userTravelRecord`"
      pattern: "findUserTravelRecordsByUserId"
    - from: "apps/server/scripts/backfill-record-metadata.ts"
      to: "apps/server/src/modules/records/records.repository.ts"
      via: "create/import 主链路也写 `userTravelRecord`"
      pattern: "userTravelRecord"
    - from: "apps/server/test/auth-bootstrap.e2e-spec.ts"
      to: "apps/server/test/records-sync.e2e-spec.ts"
      via: "共用 legacy overseas seed + backfill + replay 迁移断言"
      pattern: "一级行政区"
---

<objective>
Phase 28 gap closure Wave 2：
在 `28-04` 修正 canonical datasetVersion 语义之后，把 authoritative metadata backfill 真正扩展到登录主链路使用的 `userTravelRecord`，并补齐 verifier 要求的“旧 `userTravelRecord` -> backfill -> bootstrap/sync 回放”迁移 e2e。

本 plan 只解决 blocker 2：
1. backfill 脚本必须扫描并更新 `userTravelRecord`；
2. `/auth/bootstrap` 与 same-user sync 的自动化测试必须证明 legacy overseas records 经 backfill 后会回放升级后的 Phase 28 metadata。

Purpose: GEOX-02 不只要求新记录正确，还要求老用户已有 overseas records 被 authoritative 地升级，否则时间轴与统计会继续消费旧文案和旧 datasetVersion。
Output: 扩展后的 backfill 脚本、包含 `userTravelRecord` 计数的 CLI 输出，以及两条迁移 e2e（bootstrap / same-user sync）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-overseas-coverage-expansion/28-VERIFICATION.md
@.planning/phases/28-overseas-coverage-expansion/28-REVIEW.md
@.planning/phases/28-overseas-coverage-expansion/28-04-PLAN.md
@.planning/phases/28-overseas-coverage-expansion/28-02-SUMMARY.md

<interfaces>
From apps/server/scripts/backfill-record-metadata.ts:
```typescript
type BackfillSummary = {
  matchedTravelRows: number
  unmatchedTravelRows: string[]
  matchedSmokeRows: number
  unmatchedSmokeRows: string[]
}

const travelRows = await prisma.travelRecord.findMany(...)
const smokeRows = await prisma.smokeRecord.findMany(...)
```

From apps/server/src/modules/auth/auth.service.ts:
```typescript
const records = await this.authRepository.findUserTravelRecordsByUserId(userId)
return {
  response: {
    authenticated: true,
    records: records.map(toContractTravelRecord),
  },
}
```

From apps/server/src/modules/records/records.repository.ts:
```typescript
async createTravelRecord(userId: string, input: CreateTravelRecordDto) {
  return this.prisma.userTravelRecord.create(...)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 authoritative metadata backfill 到 userTravelRecord，并把脚本输出显式化</name>
  <files>
    apps/server/scripts/backfill-record-metadata.ts
    apps/server/test/record-metadata-backfill.e2e-spec.ts
  </files>
  <read_first>
    - apps/server/scripts/backfill-record-metadata.ts（当前只处理 `travelRecord` / `smokeRecord`）
    - apps/server/src/modules/auth/auth.service.ts（`/auth/bootstrap` 直接回放 `userTravelRecord`）
    - apps/server/src/modules/records/records.repository.ts（`/records` create/import 也写 `userTravelRecord`）
    - apps/server/test/record-metadata-backfill.e2e-spec.ts（当前 helper regression 尚未覆盖 `userTravelRecord`）
    - .planning/phases/28-overseas-coverage-expansion/28-VERIFICATION.md（blocker 2 的 missing 清单）
  </read_first>
  <action>
    在不改变 authoritative 真源的前提下，把 backfill 脚本扩展到真正的登录主链路表：

    1. 修改 `apps/server/scripts/backfill-record-metadata.ts`
       - 扩展 `BackfillSummary`，新增：
         - `matchedUserTravelRows: number`
         - `unmatchedUserTravelRows: string[]`
       - 读取 `prisma.userTravelRecord.findMany({ select: { id: true, placeId: true } })`
       - 对命中 canonical lookup 的每条 `userTravelRecord` 无条件覆盖以下字段：
         - `datasetVersion`
         - `displayName`
         - `regionSystem`
         - `adminType`
         - `typeLabel`
         - `parentLabel`
         - `subtitle`
       - 未命中的 `placeId` 继续进入 `unmatchedUserTravelRows`，不要猜测 label，也不要改未知 placeId。
       - `main()` 输出的 JSON 摘要同步追加 `matchedUserTravelRows` 与 `unmatchedUserTravelRows`。

    2. `apps/server/test/record-metadata-backfill.e2e-spec.ts`
       - 增加 `userTravelRecord` 对应的 helper regression，要求它和 `travelRecord` / `smokeRecord` 使用同一份 canonical summary。
       - 如果当前 test 通过 `buildTravelMetadataUpdate()` / `buildSmokeMetadataUpdate()` 断言 canonical metadata，则新增 `buildUserTravelMetadataUpdate()` 或等价 helper，并断言：
         - `buildUserTravelMetadataUpdate('us-california', lookup)?.datasetVersion === 'canonical-authoritative-2026-04-21'`
         - `buildUserTravelMetadataUpdate('jp-tokyo', lookup)?.subtitle === 'Japan · Prefecture'`
       - 不要把此文件改成只靠 grep；保持它仍是可运行的 automated regression。

    3. 不修改 `auth.service.ts`、`records.repository.ts` 的业务逻辑；它们已经以 `userTravelRecord` 为真源，本任务只负责把真源写正确。
  </action>
  <verify>
    <automated>cd apps/server &amp;&amp; pnpm test test/record-metadata-backfill.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "matchedUserTravelRows|unmatchedUserTravelRows" apps/server/scripts/backfill-record-metadata.ts` 至少命中 4 行
    - `rg -n "prisma\\.userTravelRecord\\.findMany|prisma\\.userTravelRecord\\.update" apps/server/scripts/backfill-record-metadata.ts` 至少命中 2 行
    - `rg -n "buildUserTravelMetadataUpdate|userTravel" apps/server/test/record-metadata-backfill.e2e-spec.ts` 至少命中 2 行
    - `rg -n "canonical-authoritative-2026-04-21" apps/server/test/record-metadata-backfill.e2e-spec.ts` 至少命中 2 行
    - `cd apps/server && pnpm test test/record-metadata-backfill.e2e-spec.ts` 退出码为 0
  </acceptance_criteria>
  <done>backfill 脚本已经覆盖 `userTravelRecord`，并且 helper regression 能证明 `userTravelRecord` 也会拿到与 canonical summary 完全一致的 Phase 28 metadata。</done>
</task>

<task type="auto">
  <name>Task 2: 补旧 userTravelRecord -> backfill -> bootstrap/sync 回放迁移 e2e</name>
  <files>
    apps/server/test/auth-bootstrap.e2e-spec.ts
    apps/server/test/records-sync.e2e-spec.ts
  </files>
  <read_first>
    - apps/server/test/auth-bootstrap.e2e-spec.ts（当前只验证“已正确记录被正确回放”，没有覆盖旧 metadata 迁移）
    - apps/server/test/records-sync.e2e-spec.ts（当前 same-user sync 只验证新写 authoritative records）
    - apps/server/scripts/backfill-record-metadata.ts（Task 1 后的 `userTravelRecord` backfill 入口）
    - apps/server/src/modules/auth/auth.service.ts（`/auth/bootstrap` 直接回放 `userTravelRecord`）
    - .planning/phases/28-overseas-coverage-expansion/28-VERIFICATION.md（明确要求“旧 userTravelRecord -> backfill -> bootstrap/sync 回放”的迁移 e2e）
  </read_first>
  <action>
    在现有 e2e 套件里补两条迁移回归，使用明确的 legacy overseas seed，证明 backfill 后回放字段被 authoritative 升级：

    1. 在两个测试文件里共用同一组 legacy seed 值。至少覆盖以下 2 条已存在于 Phase 26/28 支持范围内的 overseas records：
       - Tokyo legacy row
         - `placeId: 'jp-tokyo'`
         - `boundaryId: 'ne-admin1-jp-tokyo'`
         - `datasetVersion: '2026-04-02-geo-v2'`
         - `displayName: 'Tokyo'`
         - `regionSystem: 'OVERSEAS'`
         - `adminType: 'ADMIN1'`
         - `typeLabel: '一级行政区'`
         - `parentLabel: 'Japan'`
         - `subtitle: 'Japan · 一级行政区'`
       - California legacy row
         - `placeId: 'us-california'`
         - `boundaryId: 'ne-admin1-us-california'`
         - `datasetVersion: '2026-04-02-geo-v2'`
         - `displayName: 'California'`
         - `regionSystem: 'OVERSEAS'`
         - `adminType: 'ADMIN1'`
         - `typeLabel: '一级行政区'`
         - `parentLabel: 'United States'`
         - `subtitle: 'United States · 一级行政区'`

    2. `apps/server/test/auth-bootstrap.e2e-spec.ts`
       - 新增一条迁移测试，流程固定为：
         1. 注册用户并拿到 sid
         2. 直接向 `prisma.userTravelRecord` 写入上述 2 条 legacy rows
         3. 调用 `await backfillRecordMetadata(prisma)`
         4. `GET /auth/bootstrap`
         5. 断言返回的 2 条 records 已升级为当前 canonical summary：
            - `datasetVersion === 'canonical-authoritative-2026-04-21'`
            - Tokyo: `typeLabel === 'Prefecture'`, `subtitle === 'Japan · Prefecture'`
            - California: `typeLabel === 'State'`, `subtitle === 'United States · State'`
            - 结果中不再包含 `一级行政区`

    3. `apps/server/test/records-sync.e2e-spec.ts`
       - 新增一条 same-user migration 测试，保持该文件既有“两 session 同一用户”的语义：
         1. 准备 session A / session B
         2. 直接向当前用户的 `userTravelRecord` 写入 Tokyo + California legacy rows
         3. 调用 `await backfillRecordMetadata(prisma)`
         4. 让 session B 通过 `GET /auth/bootstrap` 读取
         5. 断言 session B 看到的 2 条 overseas records 与 canonical summary 完全一致，且两个 `subtitle` 都不再包含 `一级行政区`

    4. 两个 migration tests 都必须只种 `userTravelRecord`，不要再往 `travelRecord` 塞同样的数据；本 blocker 的目的就是证明登录主链路的真实表已被修正。
  </action>
  <verify>
    <automated>cd apps/server &amp;&amp; pnpm test test/auth-bootstrap.e2e-spec.ts &amp;&amp; pnpm test test/records-sync.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "backfillRecordMetadata" apps/server/test/auth-bootstrap.e2e-spec.ts apps/server/test/records-sync.e2e-spec.ts` 至少命中 2 行
    - `rg -n "2026-04-02-geo-v2|Japan · 一级行政区|United States · 一级行政区" apps/server/test/auth-bootstrap.e2e-spec.ts apps/server/test/records-sync.e2e-spec.ts` 至少命中 4 行
    - `rg -n "canonical-authoritative-2026-04-21|Japan · Prefecture|United States · State" apps/server/test/auth-bootstrap.e2e-spec.ts apps/server/test/records-sync.e2e-spec.ts` 至少命中 4 行
    - `rg -n "userTravelRecord\\.createMany|userTravelRecord\\.create" apps/server/test/auth-bootstrap.e2e-spec.ts apps/server/test/records-sync.e2e-spec.ts` 至少命中 2 行
    - `cd apps/server && pnpm test test/auth-bootstrap.e2e-spec.ts` 与 `cd apps/server && pnpm test test/records-sync.e2e-spec.ts` 均退出码为 0
  </acceptance_criteria>
  <done>旧 `userTravelRecord` 经 backfill 后，`/auth/bootstrap` 与 same-user sync 都会回放升级后的 Phase 28 metadata，blocker 2 的真实迁移路径被自动化锁住。</done>
</task>

</tasks>

<must_haves>
Truths:
- backfill 脚本会处理 `userTravelRecord`，不再遗漏登录主链路。
- 旧 overseas `userTravelRecord` 执行 backfill 后，bootstrap/sync 返回的是 Phase 28 canonical metadata。
- 回放链路继续只读 persisted metadata，不新增 consumer-side fallback。

Artifacts:
- `apps/server/scripts/backfill-record-metadata.ts`
- `apps/server/test/auth-bootstrap.e2e-spec.ts`
- `apps/server/test/records-sync.e2e-spec.ts`

Key links:
- backfill script -> `userTravelRecord`
- `userTravelRecord` -> `/auth/bootstrap`
- `userTravelRecord` -> same-user sync replay
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical lookup -> persisted `userTravelRecord` | backfill 会把 canonical metadata 写回真实登录主链路表 |
| persisted `userTravelRecord` -> `/auth/bootstrap` / same-user sync | 客户端恢复依赖这些持久化字段，不会自行纠正旧 metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | T | `apps/server/scripts/backfill-record-metadata.ts` | mitigate | 新增 `userTravelRecord` 扫描与 update loop，并把 unmatched placeId 继续显式记录，避免猜测 metadata。 |
| T-28-05-02 | T | `apps/server/test/auth-bootstrap.e2e-spec.ts` | mitigate | 用 legacy `userTravelRecord` seed + `backfillRecordMetadata(prisma)` + `/auth/bootstrap` 断言，锁住真实迁移路径。 |
| T-28-05-03 | T | `apps/server/test/records-sync.e2e-spec.ts` | mitigate | 用 two-session same-user replay 回归，证明跨 session 不会继续回放旧 `一级行政区` 文案。 |
| T-28-05-04 | R | CLI backfill summary | mitigate | 输出 `matchedUserTravelRows` / `unmatchedUserTravelRows`，让实际执行时能审计 `userTravelRecord` 是否被处理。 |
</threat_model>

<verification>
- `cd apps/server && pnpm test test/record-metadata-backfill.e2e-spec.ts`
- `cd apps/server && pnpm test test/auth-bootstrap.e2e-spec.ts`
- `cd apps/server && pnpm test test/records-sync.e2e-spec.ts`
</verification>

<success_criteria>
- `userTravelRecord` 被纳入 authoritative metadata backfill，CLI 输出能看见 matched/unmatched 计数。
- verifier blocker 2 所要求的“旧 `userTravelRecord` -> backfill -> bootstrap/sync 回放”迁移路径有自动化回归覆盖。
- Phase 29/30 将消费到升级后的 persisted metadata，而不是继续收到 legacy `一级行政区` 字段。
</success_criteria>

<output>
After completion, create `.planning/phases/28-overseas-coverage-expansion/28-05-SUMMARY.md`
</output>

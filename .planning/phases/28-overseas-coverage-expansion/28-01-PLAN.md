---
phase: 28-overseas-coverage-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/scripts/geo/overseas-admin1-support.mjs
  - apps/web/scripts/geo/overseas-support/index.mjs
  - apps/web/scripts/geo/overseas-support/asia.mjs
  - apps/web/scripts/geo/overseas-support/europe.mjs
  - apps/web/scripts/geo/overseas-support/americas.mjs
  - apps/web/scripts/geo/overseas-support/africa.mjs
  - apps/web/scripts/geo/overseas-support/middle-east.mjs
  - apps/web/scripts/geo/overseas-support/oceania.mjs
  - apps/web/scripts/geo/build-geometry-manifest.mjs
  - apps/web/public/geo/2026-04-21-geo-v3/manifest.json
  - apps/web/public/geo/2026-04-21-geo-v3/cn/layer.json
  - apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json
  - packages/contracts/src/generated/geometry-manifest.generated.ts
autonomous: true
requirements:
  - GEOX-01
  - GEOX-02
tags:
  - geo
  - manifest
  - build-pipeline
  - overseas
user_setup: []
must_haves:
  truths:
    - "Phase 28 的 authoritative overseas support matrix 被明确锁定为 21 国，且按区域拆分 authoring 文件实现 D-01 / D-03，而不是继续堆在单一脚本里。"
    - "所有新增国家都只放行当前 Natural Earth 快照中 `gadm_level === 1` 的 admin1 边界，满足 D-02，不接受国家级 fallback。"
    - "build-time metadata 不再写入统一的 `一级行政区`；overseas feature metadata 必须直接生成标准英文 `typeLabel` 与 `${parentLabel} · ${typeLabel}` subtitle（显式落实 D-06）。"
    - "geometry dataset 版本升级到 `2026-04-21-geo-v3`，canonical dataset 版本升级到 `canonical-authoritative-2026-04-21`，避免在旧版本号下静默改写 authoritative 数据。"
    - "generated contracts 里同时产出 `GEOMETRY_MANIFEST` 和 supported-country summary，后续 web/server/Phase 29/30 消费者不需要临时推导覆盖名单。"
  artifacts:
    - path: "apps/web/scripts/geo/overseas-support/index.mjs"
      provides: "Phase 28 regionalized overseas authoring aggregation"
      contains: "supportedCountrySummaries"
    - path: "apps/web/scripts/geo/build-geometry-manifest.mjs"
      provides: "Phase 28 metadata generation with normalized type labels"
      contains: "2026-04-21-geo-v3"
    - path: "packages/contracts/src/generated/geometry-manifest.generated.ts"
      provides: "Generated manifest plus supported-country summary constants"
      contains: "SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH"
  key_links:
    - from: "apps/web/scripts/geo/overseas-support/*.mjs"
      to: "apps/web/scripts/geo/build-geometry-manifest.mjs"
      via: "aggregated support matrix + label policy imports"
      pattern: "supportedCountrySummaries"
    - from: "apps/web/scripts/geo/build-geometry-manifest.mjs"
      to: "packages/contracts/src/generated/geometry-manifest.generated.ts"
      via: "geo:build generated output"
      pattern: "SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES"
    - from: "packages/contracts/src/generated/geometry-manifest.generated.ts"
      to: "apps/web/src/constants/overseas-support.ts"
      via: "generated supported-country summary reuse"
      pattern: "SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH"
---

<objective>
Phase 28 Wave 1：把 overseas authoritative support 的真正真源收口到 build-time authoring 层，并一次性生成 Phase 28 需要的扩容产物。

本计划必须同时解决三个核心张力：
1. 按 D-01 / D-03 锁定一个明确、区域均衡、一次性完成的扩容目标集合；
2. 按 D-06 把 `typeLabel` / `subtitle` 政策从统一 `一级行政区` 升级为标准英文标签；
3. 按 D-07 只在 build-time authoring 层拆分文件，不新建 runtime catalog。

Purpose: 真实 authoritative 链路从 support rules 开始。如果这一步不先完成，后面的 server exact-match、backfill、bootstrap replay 和 web unsupported-copy 都会继续漂移。
Output: 区域化 support authoring 文件、升级后的 `build-geometry-manifest.mjs`、新的 `2026-04-21-geo-v3` 产物目录、以及带 supported-country summary 的 generated contracts 文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-overseas-coverage-expansion/28-CONTEXT.md
@.planning/phases/28-overseas-coverage-expansion/28-RESEARCH.md
@.planning/PROJECT.md

<interfaces>
<!-- Source: apps/web/scripts/geo/overseas-admin1-support.mjs [VERIFIED 2026-04-21] -->

```javascript
const priorityCountries = ['JP', 'KR', 'TH', 'SG', 'MY', 'AE', 'AU', 'US']

export function isSupportedOverseasAdmin1Feature(featureProperties) {
  // 当前 support 真源仍在单文件里；Phase 28 需要改成区域化 authoring。
}
```

<!-- Source: apps/web/scripts/geo/build-geometry-manifest.mjs [VERIFIED 2026-04-21] -->

```javascript
const GEOMETRY_DATASET_VERSION = '2026-04-02-geo-v2'
const CANONICAL_DATASET_VERSION = 'canonical-authoritative-2026-04-02'

function createOverseasFeatureMetadata(featureProperties) {
  return {
    typeLabel: '一级行政区',
    subtitle: `${parentLabel} · 一级行政区`,
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 拆分 overseas authoring 文件并锁定 Phase 28 的 21 国支持矩阵</name>
  <files>
    apps/web/scripts/geo/overseas-admin1-support.mjs
    apps/web/scripts/geo/overseas-support/index.mjs
    apps/web/scripts/geo/overseas-support/asia.mjs
    apps/web/scripts/geo/overseas-support/europe.mjs
    apps/web/scripts/geo/overseas-support/americas.mjs
    apps/web/scripts/geo/overseas-support/africa.mjs
    apps/web/scripts/geo/overseas-support/middle-east.mjs
    apps/web/scripts/geo/overseas-support/oceania.mjs
  </files>
  <read_first>
    - apps/web/scripts/geo/overseas-admin1-support.mjs（当前唯一 authoring 真源；保留 SG/AE/AU 的特殊 allow/deny 逻辑）
    - .planning/phases/28-overseas-coverage-expansion/28-CONTEXT.md（D-01, D-02, D-03, D-07）
    - .planning/phases/28-overseas-coverage-expansion/28-RESEARCH.md（真实链路、stale path 警告、Natural Earth admin1 数据盘点）
  </read_first>
  <action>
    把 `apps/web/scripts/geo/overseas-admin1-support.mjs` 从“所有国家全堆一个文件”改成“区域模块 + 聚合入口”的 build-time authoring 结构，但保持现有 import path 可用：

    1. 新建 `apps/web/scripts/geo/overseas-support/` 目录，并按区域拆分为 `asia.mjs`、`middle-east.mjs`、`oceania.mjs`、`americas.mjs`、`europe.mjs`、`africa.mjs` 六个文件；每个文件导出本区域国家定义数组，字段至少包含：
       - `iso2`
       - `iso3`
       - `labelEn`
       - `labelZh`
       - `regionKey`
       - `mode`
       - `defaultTypeLabel`
       - `matchesFeature(featureProperties)` 或等价 country-level guard

    2. 用以下**完整且一次性**的 Phase 28 支持矩阵取代当前 8 国名单；不要额外添加任何未在此处列出的国家（D-03）：
       - Asia: `JP`, `KR`, `TH`, `SG`, `MY`, `IN`, `ID`
       - Middle East: `AE`, `SA`
       - Oceania: `AU`, `PG`
       - Americas: `US`, `CA`, `BR`, `AR`
       - Europe: `DE`, `PL`, `CZ`
       - Africa: `EG`, `MA`, `ZA`

    3. 新增国家必须满足 D-02：admin1-only。实现要求：
       - 所有新增国家只接受当前 Natural Earth 快照里 `Number(featureProperties.gadm_level) === 1` 的 feature。
       - 不为任何缺失 admin1 的国家做国家级 fallback。
       - 保留现有国家里的特殊 guard：`SG` 继续 allow-list 5 个 sourceFeatureId；`AE` 继续 allow-list + deny `AE-X01~` / `AE-X02~`；`AU` 继续 allow-list 8 个州/领地并保留 `adm0_a3 === 'AUS' && gadm_level === 1`。

    4. 在 `index.mjs` 中聚合所有区域模块，并统一导出：
       - `priorityCountries`
       - `priorityCountryByIso3`
       - `supportedCountrySummaries`
       - `overseasAdmin1SupportCatalog`
       - `getOverseasAdmin1SourceFeatureId`
       - `isSupportedOverseasAdmin1Feature`
       - `getOverseasAdmin1CountryDefinition`（或等价 helper，供 build script 读取 `defaultTypeLabel` / labelZh）

    5. 把 `apps/web/scripts/geo/overseas-admin1-support.mjs` 改成 thin wrapper，只做 re-export，避免 build script 和现有命令入口换路径。不要把这套 authoring 数据复制到 `apps/web/src/` 或 `apps/server/src/`，也不要新建 runtime JSON catalog（D-07 明确禁止）。
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./apps/web/scripts/geo/overseas-admin1-support.mjs').then((m) => { const required = ['JP','KR','TH','SG','MY','IN','ID','AE','SA','AU','PG','US','CA','BR','AR','DE','PL','CZ','EG','MA','ZA']; for (const code of required) { if (!m.priorityCountries.includes(code)) throw new Error(code) } if (m.priorityCountries.length !== required.length) throw new Error('priorityCountries length mismatch') })"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/scripts/geo/overseas-support/index.mjs` 存在，并且 grep `supportedCountrySummaries` 命中至少 1 行
    - 六个区域文件都存在，且 `asia.mjs` 含 `IN` / `ID`，`europe.mjs` 含 `DE` / `PL` / `CZ`，`americas.mjs` 含 `CA` / `BR` / `AR`，`africa.mjs` 含 `EG` / `MA` / `ZA`
    - grep `Number(featureProperties.gadm_level) === 1` 六个区域文件合计命中至少 1 行（新增国家明确 admin1-only）
    - grep `export \* from './overseas-support/index.mjs'` `apps/web/scripts/geo/overseas-admin1-support.mjs` 命中 1 行
    - 不新增任何 `apps/web/src/stores/overseas-catalog.ts`、`apps/server/src/modules/overseas/` 等 stale runtime 路径
  </acceptance_criteria>
  <done>Phase 28 的 build-time support 真源已经区域化，21 国支持矩阵被明确锁定，且所有新增国家都以 admin1-only guard 落地。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 升级 build script 的 metadata 生成策略并产出 v3 geometry manifest</name>
  <files>
    apps/web/scripts/geo/build-geometry-manifest.mjs
    apps/web/public/geo/2026-04-21-geo-v3/manifest.json
    apps/web/public/geo/2026-04-21-geo-v3/cn/layer.json
    apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json
    packages/contracts/src/generated/geometry-manifest.generated.ts
  </files>
  <read_first>
    - apps/web/scripts/geo/build-geometry-manifest.mjs（当前 datasetVersion / typeLabel / subtitle 生成方式）
    - apps/web/scripts/geo/overseas-support/index.mjs（Task 1 刚建立的 authoring 聚合入口）
    - .planning/phases/28-overseas-coverage-expansion/28-CONTEXT.md（D-04, D-05, D-06）
    - .planning/phases/28-overseas-coverage-expansion/28-RESEARCH.md（authoritative chain、D-06 冲突、不要新建 runtime truth source）
  </read_first>
  <action>
    在 `apps/web/scripts/geo/build-geometry-manifest.mjs` 中把 Phase 28 的 authoritative metadata 规则一次性收口：

    1. 版本升级必须使用**具体日期值**，不要继续覆盖 `2026-04-02-geo-v2`：
       - `GEOMETRY_DATASET_VERSION = '2026-04-21-geo-v3'`
       - `CANONICAL_DATASET_VERSION = 'canonical-authoritative-2026-04-21'`

    2. overseas metadata 不再从 raw `type_en` 或统一 `一级行政区` 直接出 runtime 文案，而是使用 country-level canonical label（显式解决 D-06）：
       - `JP -> Prefecture`
       - `KR -> Province`
       - `TH -> Province`
       - `SG -> Region`
       - `MY -> State`
       - `AE -> Emirate`
       - `AU -> State`
       - `US -> State`
       - `IN -> State`
       - `ID -> Province`
       - `DE -> State`
       - `PL -> Province`
       - `CZ -> Region`
       - `CA -> Province`
       - `BR -> State`
       - `AR -> Province`
       - `EG -> Governorate`
       - `MA -> Region`
       - `ZA -> Province`
       - `SA -> Region`
       - `PG -> Province`

       要求：
       - `displayName` 继续使用 `name_en ?? name`
       - `typeLabel` 直接取该国家 canonical label
       - `subtitle` 必须统一为 `${parentLabel} · ${typeLabel}`
       - `placeKind`、`regionSystem`、`adminType` 继续沿用当前 Phase 26/27 的 manifest-backed 结构，不要改 runtime shape

    3. 在 generated contracts 文件里一并产出 Phase 28 覆盖摘要常量，命名固定为：
       - `SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES`
       - `SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH`

       `SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES` 的顺序必须与 authoring matrix 保持一致，并保留 `iso2`, `labelEn`, `labelZh`, `regionKey`, `defaultTypeLabel` 字段；`SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH` 直接是 `日本、韩国、泰国、新加坡、马来西亚、印度、印度尼西亚、阿联酋、沙特阿拉伯、澳大利亚、巴布亚新几内亚、美国、加拿大、巴西、阿根廷、德国、波兰、捷克、埃及、摩洛哥、南非` 的 join 结果。

    4. 运行：
       - `pnpm --filter @trip-map/web geo:build`
       - `pnpm --filter @trip-map/contracts build`

       让新的 public geo 目录与 generated contracts 真正落盘。不要手改 generated TS/JSON 文件后跳过 `geo:build`。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web geo:build &amp;&amp; pnpm --filter @trip-map/contracts build</automated>
  </verify>
  <acceptance_criteria>
    - grep `2026-04-21-geo-v3` `apps/web/scripts/geo/build-geometry-manifest.mjs` 命中至少 1 行
    - grep `canonical-authoritative-2026-04-21` `apps/web/scripts/geo/build-geometry-manifest.mjs` 命中至少 1 行
    - grep `SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES` `packages/contracts/src/generated/geometry-manifest.generated.ts` 命中至少 1 行
    - grep `SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH` `packages/contracts/src/generated/geometry-manifest.generated.ts` 命中至少 1 行
    - `rg -n "一级行政区" apps/web/scripts/geo/build-geometry-manifest.mjs` 结果为 0 行
    - `rg -n "\"typeLabel\": \"一级行政区\"" apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json` 结果为 0 行
    - `rg -n "\"iso2\": \"CA\"|\"iso2\": \"DE\"|\"iso2\": \"SA\"|\"iso2\": \"ZA\"|\"iso2\": \"PG\"" packages/contracts/src/generated/geometry-manifest.generated.ts` 至少命中 5 行
  </acceptance_criteria>
  <done>Phase 28 的 v3 geometry dataset 和 generated contracts 已落盘；metadata 生成规则与 supported-country summary 都从 build-time 真源导出。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 更新 geometry-manifest 回归测试，锁住新版本和代表性新增国家</name>
  <files>
    apps/web/src/services/geometry-manifest.spec.ts
  </files>
  <read_first>
    - apps/web/src/services/geometry-manifest.spec.ts（当前断言 `2026-04-02-geo-v2` 和 California 单点）
    - packages/contracts/src/generated/geometry-manifest.generated.ts（Task 2 生成后的 boundaryId / datasetVersion / supported-country summary）
  </read_first>
  <action>
    修改 `apps/web/src/services/geometry-manifest.spec.ts`，把 Phase 28 的 build output 锁进单测：

    1. 把所有 dataset version 断言从 `2026-04-02-geo-v2` 改成 `2026-04-21-geo-v3`。
    2. 保留对现有 `datav-cn-beijing` / `ne-admin1-us-california` 的基础断言，证明旧入口仍存在。
    3. 新增至少 6 个 representative overseas entry 断言，覆盖新增地区：
       - `ne-admin1-ca-british-columbia`
       - `ne-admin1-de-bavaria`
       - `ne-admin1-br-rio-grande-do-sul`
       - `ne-admin1-eg-aswan`
       - `ne-admin1-sa-eastern`
       - `ne-admin1-pg-morobe`
    4. 再加一条总量断言：`listGeometryManifestEntriesByLayer('OVERSEAS').length` 必须严格大于当前 Phase 26 的 `228`，避免扩容退化成只改 authoring 文案不改真实 manifest。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web test src/services/geometry-manifest.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `2026-04-21-geo-v3` `apps/web/src/services/geometry-manifest.spec.ts` 命中至少 1 行
    - grep `ne-admin1-ca-british-columbia` `apps/web/src/services/geometry-manifest.spec.ts` 命中 1 行
    - grep `ne-admin1-de-bavaria` `apps/web/src/services/geometry-manifest.spec.ts` 命中 1 行
    - grep `ne-admin1-sa-eastern` `apps/web/src/services/geometry-manifest.spec.ts` 命中 1 行
    - grep `228` `apps/web/src/services/geometry-manifest.spec.ts` 命中至少 1 行（明确比较基线）
    - `pnpm --filter @trip-map/web test src/services/geometry-manifest.spec.ts` 退出码为 0
  </acceptance_criteria>
  <done>web 端 manifest 回归测试已经锁住 Phase 28 新版本、旧入口不回退、以及代表性新增国家真实进入 authoritative manifest。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time authoring -> generated manifest | 开发者维护的国家矩阵和标签策略会直接变成 runtime authoritative 数据。 |
| Natural Earth source -> metadata normalization | 外部 GeoJSON 属性跨入 canonical `displayName` / `typeLabel` / `subtitle`。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | T | `apps/web/scripts/geo/overseas-support/*.mjs` | mitigate | 用固定 21 国清单和 `gadm_level === 1` guard 锁死 D-01/D-02/D-03；Task 1 的模块加载校验命令必须覆盖全部 ISO2。 |
| T-28-01-02 | T | `apps/web/scripts/geo/build-geometry-manifest.mjs` | mitigate | 只允许 build script 生成 `packages/contracts/src/generated/geometry-manifest.generated.ts` 与 public geo 产物；Task 2 强制跑 `geo:build`，禁止手改 generated 文件。 |
| T-28-01-03 | R | dataset versioning | mitigate | 显式升级到 `2026-04-21-geo-v3` / `canonical-authoritative-2026-04-21`，避免在旧版本号下悄悄篡改 authoritative 数据。 |
| T-28-01-04 | I | supported-country runtime summary | mitigate | supported-country summary 作为 generated contracts 常量输出，后续 web 文案直接导入，不再双写静态名单。 |
</threat_model>

<verification>
- `pnpm --filter @trip-map/web geo:verify-sources`
- `pnpm --filter @trip-map/web geo:build`
- `pnpm --filter @trip-map/contracts build`
- `pnpm --filter @trip-map/web test src/services/geometry-manifest.spec.ts`
</verification>

<success_criteria>
- Phase 28 的 21 国扩容矩阵以区域模块形式落在 `apps/web/scripts/geo/overseas-support/`。
- 新的 v3 geometry dataset 与 generated contracts 已生成，且 overseas metadata 不再使用 `一级行政区`。
- web 端 manifest spec 能证明代表性新增国家真实进入 authoritative manifest，而不是只停留在 authoring 层。
</success_criteria>

<output>
After completion, create `.planning/phases/28-overseas-coverage-expansion/28-01-SUMMARY.md`
</output>

---
phase: 28-overseas-coverage-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - apps/server/src/modules/records/records.service.ts
  - apps/server/scripts/backfill-record-metadata.ts
  - apps/server/test/phase28-overseas-cases.ts
  - apps/server/test/canonical-resolve.e2e-spec.ts
  - apps/server/test/records-travel.e2e-spec.ts
  - apps/server/test/records-import.e2e-spec.ts
  - apps/server/test/auth-bootstrap.e2e-spec.ts
  - apps/server/test/records-sync.e2e-spec.ts
  - apps/server/test/record-metadata-backfill.e2e-spec.ts
autonomous: true
requirements:
  - GEOX-01
  - GEOX-02
tags:
  - server
  - authoritative-validation
  - backfill
  - e2e
user_setup: []
must_haves:
  truths:
    - "server resolve/create/import/bootstrap 路径全部基于 Phase 28 manifest-backed metadata 工作，不再接受旧的 `一级行政区` payload 混入 authoritative overseas records。"
    - "既有 8 国 overseas 记录会通过 backfill 被显式升级到新的 datasetVersion / displayName / typeLabel / parentLabel / subtitle，而不是保留旧 subtitle。"
    - "13 个新增国家（IN、ID、SA、PG、CA、BR、AR、DE、PL、CZ、EG、MA、ZA）都通过同一张表驱动矩阵完成 `/places/resolve` + metadata correctness 验证，矩阵长度和 ISO2 集合都被显式锁定，不能静默缩水。"
    - "新增 13 国会在 import / bootstrap / same-user sync 路径里批量回放同一套标准标签 metadata，不会在 persisted text fields 上退回旧 `一级行政区`。"
    - "records create/import 对 forged overseas metadata 仍保持 exact-match 防线，高风险 tampering 不被放行。"
  artifacts:
    - path: "apps/server/scripts/backfill-record-metadata.ts"
      provides: "Phase 28 authoritative metadata backfill logic"
      contains: "datasetVersion"
    - path: "apps/server/test/phase28-overseas-cases.ts"
      provides: "Phase 28 new-country verification matrix with 13 concrete probe cases"
      contains: "PHASE28_NEW_COUNTRY_CASES"
    - path: "apps/server/src/modules/records/records.service.ts"
      provides: "Server-side exact-match guard aligned to current authoritative catalog wording"
      contains: "current authoritative overseas support catalog"
    - path: "apps/server/test/canonical-resolve.e2e-spec.ts"
      provides: "Table-driven resolve regression for every newly added country plus new type labels"
      contains: "PHASE28_NEW_COUNTRY_CASES"
  key_links:
    - from: "packages/contracts/src/generated/geometry-manifest.generated.ts"
      to: "apps/server/src/modules/canonical-places/place-metadata-catalog.ts"
      via: "manifest-backed canonical summary lookup"
      pattern: "GEOMETRY_MANIFEST"
    - from: "apps/server/test/phase28-overseas-cases.ts"
      to: "apps/server/test/canonical-resolve.e2e-spec.ts"
      via: "shared 13-country resolve/bootstrap/import/sync matrix"
      pattern: "PHASE28_NEW_COUNTRY_CASES"
    - from: "apps/server/scripts/backfill-record-metadata.ts"
      to: "apps/server/src/modules/auth/auth.service.ts"
      via: "bootstrap replay consumes overwritten persisted metadata"
      pattern: "records.map\\(toContractTravelRecord\\)"
    - from: "apps/server/src/modules/records/records.service.ts"
      to: "apps/server/test/records-travel.e2e-spec.ts"
      via: "exact-match rejection for stale labels"
      pattern: "metadata must match authoritative catalog"
---

<objective>
Phase 28 Wave 2A：把 server authoritative 链路和 persisted metadata 升级到 Phase 28 的 coverage / label policy。

本计划不新增新的 runtime catalog，也不让 web consumer 临时兜底。它只做两件事：
1. 让 manifest-backed server guard 和 metadata backfill 真正承认 Phase 28 的新 dataset / 新 label policy；
2. 用 e2e 回归锁住 resolve、records create/import、bootstrap replay、same-user sync 和 backfill 行为。

Purpose: GEOX-02 的高风险点不在 UI，而在“旧记录继续持有旧标签”以及“服务端 exact-match/回放链路没有同步升级”。这一步必须先把 authoritative 数据面修正干净。
Output: 更新后的 server guard / backfill 脚本，以及覆盖新国家与旧 8 国 backfill 的 e2e 测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-overseas-coverage-expansion/28-CONTEXT.md
@.planning/phases/28-overseas-coverage-expansion/28-RESEARCH.md
@.planning/phases/27-multi-visit-record-foundation/27-CONTEXT.md
@.planning/phases/28-overseas-coverage-expansion/28-01-PLAN.md

<interfaces>
<!-- Source: apps/server/src/modules/records/records.service.ts [VERIFIED 2026-04-21] -->

```typescript
const mismatchedFields = [
  ['datasetVersion', input.datasetVersion, placeSummary.datasetVersion],
  ['displayName', input.displayName, placeSummary.displayName],
  ['regionSystem', input.regionSystem, placeSummary.regionSystem],
  ['adminType', input.adminType, placeSummary.adminType],
  ['typeLabel', input.typeLabel, placeSummary.typeLabel],
  ['parentLabel', input.parentLabel, placeSummary.parentLabel],
  ['subtitle', input.subtitle, placeSummary.subtitle],
]
```

<!-- Source: apps/server/scripts/backfill-record-metadata.ts [VERIFIED 2026-04-21] -->

```typescript
await prisma.travelRecord.update({
  data: {
    regionSystem: metadata.regionSystem,
    adminType: metadata.adminType,
    typeLabel: metadata.typeLabel,
    parentLabel: metadata.parentLabel,
    subtitle: row.subtitle.trim().length > 0 ? row.subtitle : metadata.subtitle,
  },
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 修正 authoritative server/backfill 逻辑，让 Phase 28 label policy 真正回填旧记录</name>
  <files>
    apps/server/src/modules/records/records.service.ts
    apps/server/scripts/backfill-record-metadata.ts
  </files>
  <read_first>
    - apps/server/src/modules/records/records.service.ts（当前 exact-match 字段与 `Phase 26` 硬编码文案）
    - apps/server/scripts/backfill-record-metadata.ts（当前保留旧 subtitle 的逻辑会阻止 D-06 落地）
    - packages/contracts/src/generated/geometry-manifest.generated.ts（Phase 28 新 datasetVersion 和 canonical labels）
    - .planning/phases/28-overseas-coverage-expansion/28-RESEARCH.md（D-06 与现状冲突、backfill 风险）
  </read_first>
  <action>
    在 server 侧做最小但必须的 authoritative 收口：

    1. `apps/server/src/modules/records/records.service.ts`
       - 保留现有 exact-match 字段集，不删 `datasetVersion` / `displayName` / `typeLabel` / `subtitle` 校验。
       - 把错误文案从 `Phase 26 authoritative support catalog` 改成不带 phase number 的稳定表述，例如 `current authoritative overseas support catalog`，避免 Phase 28 以后继续保留旧 phase 文案。

    2. `apps/server/scripts/backfill-record-metadata.ts`
       - 把 `CanonicalMetadata` / lookup / update payload 扩展到至少包含：
         - `datasetVersion`
         - `displayName`
         - `regionSystem`
         - `adminType`
         - `typeLabel`
         - `parentLabel`
         - `subtitle`
       - 对匹配到 canonical summary 的 `travelRecord` 与 `smokeRecord` **无条件覆盖**上述字段；不要再保留 `row.subtitle.trim().length > 0 ? row.subtitle : metadata.subtitle` 这种“旧 subtitle 优先”的逻辑，因为 Phase 28 正是要把旧 8 国 `一级行政区` 记录显式升级到 `Prefecture` / `State` / `Region` 等新标签。
       - 不增加 placeId 推导逻辑；继续只在 canonical lookup 命中时回填，未知 placeId 仍返回 unmatched。

    3. 不修改 `place-metadata-catalog.ts` 和 `auth.service.ts`；它们已经会消费 manifest-backed summary 和 persisted metadata，Phase 28 的关键是把 persisted metadata 先写正确。
  </action>
  <verify>
    <automated>cd apps/server &amp;&amp; pnpm test test/record-metadata-backfill.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `current authoritative overseas support catalog` `apps/server/src/modules/records/records.service.ts` 命中至少 1 行
    - grep `datasetVersion` `apps/server/scripts/backfill-record-metadata.ts` 命中至少 3 行（type/lookup/update 都已纳入）
    - grep `displayName` `apps/server/scripts/backfill-record-metadata.ts` 命中至少 3 行
    - grep `subtitle: metadata.subtitle` `apps/server/scripts/backfill-record-metadata.ts` 命中至少 2 行（travel + smoke）
    - grep `row.subtitle.trim().length > 0 ? row.subtitle : metadata.subtitle` `apps/server/scripts/backfill-record-metadata.ts` 结果为 0 行
    - `cd apps/server && pnpm test test/record-metadata-backfill.e2e-spec.ts` 退出码为 0
  </acceptance_criteria>
  <done>backfill 逻辑已经会把旧 overseas 记录彻底升级到 Phase 28 canonical metadata；server guard 的 authoritative 文案也不再引用旧 phase。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 更新 server e2e，覆盖新国家 resolve/save/import/bootstrap/sync 与旧标签拒绝</name>
  <files>
    apps/server/test/phase28-overseas-cases.ts
    apps/server/test/canonical-resolve.e2e-spec.ts
    apps/server/test/records-travel.e2e-spec.ts
    apps/server/test/records-import.e2e-spec.ts
    apps/server/test/auth-bootstrap.e2e-spec.ts
    apps/server/test/records-sync.e2e-spec.ts
    apps/server/test/record-metadata-backfill.e2e-spec.ts
  </files>
  <read_first>
    - apps/web/src/data/geo/sources/ne_10m_admin_1_states_provinces.json（新增 13 国的稳定 probe 名称 / latitude / longitude 来源）
    - apps/server/test/canonical-resolve.e2e-spec.ts（当前仍断言 `一级行政区` 和 v2 dataset）
    - apps/server/test/records-travel.e2e-spec.ts（当前 authoritative overseas payload 示例仍是 `Japan · 一级行政区`）
    - apps/server/test/records-import.e2e-spec.ts（当前 import authoritative 示例与新 label policy 不一致）
    - apps/server/test/auth-bootstrap.e2e-spec.ts（bootstrap replay 仍断言旧标签）
    - apps/server/test/records-sync.e2e-spec.ts（same-user multi-session overseas payload 仍断言旧标签）
    - packages/contracts/src/generated/geometry-manifest.generated.ts（Phase 28 新 datasetVersion、新 label、新 placeId / boundaryId）
  </read_first>
  <action>
    把 server regression suite 全面切到 Phase 28 的 authoritative reality，并把 D-08 的“完整验证”写成**不可缩水**的表驱动矩阵：

    1. 新增 `apps/server/test/phase28-overseas-cases.ts`，导出 `PHASE28_NEW_COUNTRY_CASES`。该文件是 Phase 28 server regression 的唯一新增国家真源，必须锁定为**恰好 13 条**，且 ISO2 集合必须严格等于：
       `['IN','ID','SA','PG','CA','BR','AR','DE','PL','CZ','EG','MA','ZA']`

       每条 case 至少包含：
       - `iso2`
       - `countryLabel`
       - `displayName`
       - `lat`
       - `lng`
       - `expectedTypeLabel`
       - `expectedPlaceId`
       - `expectedBoundaryId`

       具体 probe 点固定使用以下 13 组（来自当前 vendored Natural Earth snapshot 的 admin1 `latitude` / `longitude`）：
       - `IN` / `West Bengal` / `(23.0523, 87.7289)` / `State`
       - `ID` / `East Java` / `(-7.88129, 112.616)` / `Province`
       - `SA` / `Eastern` / `(22.9875, 50.1714)` / `Region`
       - `PG` / `Morobe` / `(-6.3992, 146.83)` / `Province`
       - `CA` / `British Columbia` / `(54.6943, -124.662)` / `Province`
       - `BR` / `Rio Grande do Sul` / `(-29.7277, -53.656)` / `State`
       - `AR` / `Entre Ríos` / `(-32.0275, -59.2824)` / `Province`
       - `DE` / `Bavaria` / `(49.0056, 11.3966)` / `State`
       - `PL` / `Silesian Voivodeship` / `(50.1978, 18.9924)` / `Province`
       - `CZ` / `Ústí nad Labem` / `(50.4996, 13.8424)` / `Region`
       - `EG` / `Aswan` / `(22.6543, 32.2794)` / `Governorate`
       - `MA` / `Tangier-Tetouan` / `(35.2884, -5.35664)` / `Region`
       - `ZA` / `Western Cape` / `(-33.5035, 20.9745)` / `Province`

       `expectedPlaceId` / `expectedBoundaryId` 必须从 Phase 28 生成后的 v3 canonical catalog / manifest 实际读取，不允许手写猜 slug；若任何一个国家在 v3 输出里缺 summary，则测试初始化直接失败。

    2. `canonical-resolve.e2e-spec.ts`
       - 保留现有 Beijing / Hong Kong / California / Tokyo / Gangwon / Dubai 等基线断言，但全部切到新 label policy：
         - California -> `State`
         - Tokyo -> `Prefecture`
         - Gangwon -> `Province`
         - Dubai -> `Emirate`
       - 导入 `PHASE28_NEW_COUNTRY_CASES` 并对全部 13 条执行表驱动 `/places/resolve` 断言。每条都必须验证：
         - `status === 'resolved'`
         - `place.placeKind === 'OVERSEAS_ADMIN1'`
         - `place.datasetVersion === 'canonical-authoritative-2026-04-21'`
         - `place.displayName === case.displayName`
         - `place.typeLabel === case.expectedTypeLabel`
         - `place.parentLabel === case.countryLabel`
         - `place.subtitle === ${parentLabel} · ${typeLabel}`
         - `place.placeId === case.expectedPlaceId`
         - `place.boundaryId === case.expectedBoundaryId`
         - `place.geometryRef.assetKey === 'overseas/layer.json'`
         - `place.geometryRef.geometryDatasetVersion === '2026-04-21-geo-v3'`

    3. `records-import.e2e-spec.ts`
       - 用 `PHASE28_NEW_COUNTRY_CASES` + `getCanonicalPlaceSummaryById()` 生成 13 条 authoritative import payload，并在同一条 `/records/import` 请求中批量导入。
       - 断言返回结果与数据库持久化里 13 条记录全部存在，且 `datasetVersion` / `displayName` / `typeLabel` / `parentLabel` / `subtitle` 完整匹配 canonical summary。
       - 保留一条 stale label rejection：对其中任一 case 把 `typeLabel` / `subtitle` 改回 `一级行政区` 风格，期望 400 且报 `metadata must match authoritative catalog`。

    4. `auth-bootstrap.e2e-spec.ts`
       - 使用 `PHASE28_NEW_COUNTRY_CASES` 为同一用户一次性写入 13 条新增国家 authoritative overseas records（可直接写库，也可通过现有 helper/接口写入，但最终 bootstrap 断言必须覆盖 13 条全部 case）。
       - `GET /auth/bootstrap` 必须按同一张矩阵逐条断言 `placeId` / `displayName` / `typeLabel` / `parentLabel` / `subtitle` 完全一致。这是 D-08 对新增 country entry 的 bootstrap consistency 闭环，不再只验证 `jp-tokyo` 一个旧国家。

    5. `records-sync.e2e-spec.ts`
       - 让 session A 基于 `PHASE28_NEW_COUNTRY_CASES` 创建全部 13 条 Phase 28 overseas records；不允许只取子集，也不允许因为测试时长而缩水。
       - session B 通过 `/auth/bootstrap` 读回这些记录时，必须断言 text fields 与 session A 保存值逐条相同，不发生 `一级行政区` 回退或按 `placeId` 重新推导。

    6. `records-travel.e2e-spec.ts`
       - authoritative overseas create payload 改成从 `getCanonicalPlaceSummaryById()` 读取 Phase 28 summary。
       - 保留一个拒绝旧标签的测试：对 `jp-tokyo` 或任一 `PHASE28_NEW_COUNTRY_CASES` 成员构造 `typeLabel: '一级行政区'` / 旧 subtitle 的 payload，期望 400 且报 `metadata must match authoritative catalog`。

    7. `record-metadata-backfill.e2e-spec.ts`
       - 把 `us-california` 的期望改成 `State / United States · State`。
       - 新增 `jp-tokyo -> Prefecture / Japan · Prefecture` 的回填断言，显式证明 D-06 冲突被处理，而不是只验证 California 一个样本。
  </action>
  <verify>
    <automated>cd apps/server &amp;&amp; pnpm test test/canonical-resolve.e2e-spec.ts &amp;&amp; pnpm test test/records-travel.e2e-spec.ts &amp;&amp; pnpm test test/records-import.e2e-spec.ts &amp;&amp; pnpm test test/auth-bootstrap.e2e-spec.ts &amp;&amp; pnpm test test/records-sync.e2e-spec.ts &amp;&amp; pnpm test test/record-metadata-backfill.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/test/phase28-overseas-cases.ts` 存在，且 `rg -n "PHASE28_NEW_COUNTRY_CASES|West Bengal|East Java|Eastern|Morobe|British Columbia|Rio Grande do Sul|Entre Ríos|Bavaria|Silesian Voivodeship|Ústí nad Labem|Aswan|Tangier-Tetouan|Western Cape" apps/server/test/phase28-overseas-cases.ts` 至少命中 14 行
    - `rg -n "PHASE28_NEW_COUNTRY_CASES" apps/server/test/canonical-resolve.e2e-spec.ts apps/server/test/records-import.e2e-spec.ts apps/server/test/auth-bootstrap.e2e-spec.ts apps/server/test/records-sync.e2e-spec.ts` 至少命中 4 行
    - `rg -n "canonical-authoritative-2026-04-21|2026-04-21-geo-v3|overseas/layer.json" apps/server/test/canonical-resolve.e2e-spec.ts apps/server/test/records-import.e2e-spec.ts apps/server/test/auth-bootstrap.e2e-spec.ts apps/server/test/records-sync.e2e-spec.ts` 至少命中 6 行
    - grep `Prefecture` `apps/server/test/canonical-resolve.e2e-spec.ts` 命中至少 1 行
    - grep `Governorate` `apps/server/test/canonical-resolve.e2e-spec.ts` 命中至少 1 行
    - grep `Emirate` `apps/server/test/canonical-resolve.e2e-spec.ts` 命中至少 1 行
    - grep `metadata must match authoritative catalog` `apps/server/test/records-travel.e2e-spec.ts` 命中至少 1 行
    - `rg -n "一级行政区" apps/server/test/canonical-resolve.e2e-spec.ts apps/server/test/records-travel.e2e-spec.ts apps/server/test/records-import.e2e-spec.ts apps/server/test/auth-bootstrap.e2e-spec.ts apps/server/test/records-sync.e2e-spec.ts apps/server/test/record-metadata-backfill.e2e-spec.ts` 结果为 0 行
    - 上述 6 条 server e2e 命令全部退出码为 0
  </acceptance_criteria>
  <done>server e2e 已经用 13 国共享矩阵锁住 Phase 28 的新增覆盖、新 label policy，以及 import / bootstrap / sync 不得回退到旧 `一级行政区` metadata 的行为。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client payload -> `/records` / `/records/import` | 不可信的海外旅行记录 metadata 跨入 authoritative server write path。 |
| generated manifest -> backfill script -> database | build 产出的 canonical metadata 会覆盖已持久化记录。 |
| database -> `/auth/bootstrap` | persisted metadata 被直接回放到 web consumer，不会在客户端重新推导。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | T | `apps/server/src/modules/records/records.service.ts` | mitigate | 保留 exact-match guard，对 `datasetVersion` / `displayName` / `typeLabel` / `parentLabel` / `subtitle` 逐项比对；Task 2 新增 stale `一级行政区` payload 拒绝回归。 |
| T-28-02-02 | T | `apps/server/scripts/backfill-record-metadata.ts` | mitigate | backfill 无条件覆盖 authoritative fields，显式消除旧 8 国记录残留旧 label 的高严重度一致性风险。 |
| T-28-02-03 | R | phase-specific error text | mitigate | 移除 `Phase 26` 硬编码，避免 Phase 28 后错误日志/返回文案继续指向过时 catalog。 |
| T-28-02-04 | I | `/auth/bootstrap` / multi-session replay | mitigate | 用 `auth-bootstrap.e2e` + `records-sync.e2e` 锁定 replay 只消费更新后的 persisted metadata，不做临时兜底推导。 |
</threat_model>

<verification>
- `cd apps/server && pnpm test test/canonical-resolve.e2e-spec.ts`
- `cd apps/server && pnpm test test/records-travel.e2e-spec.ts`
- `cd apps/server && pnpm test test/records-import.e2e-spec.ts`
- `cd apps/server && pnpm test test/auth-bootstrap.e2e-spec.ts`
- `cd apps/server && pnpm test test/records-sync.e2e-spec.ts`
- `cd apps/server && pnpm test test/record-metadata-backfill.e2e-spec.ts`
</verification>

<success_criteria>
- server resolve/create/import/bootstrap/sync 全链路都转到 Phase 28 authoritative metadata。
- 旧 8 国记录的 `一级行政区` 残留会被 backfill 修正，而不是继续作为 persisted truth 回放。
- 至少一批来自新增区域的国家通过 resolve/save/import regression 被验证为真正可识别、可保存、可回放。
</success_criteria>

<output>
After completion, create `.planning/phases/28-overseas-coverage-expansion/28-02-SUMMARY.md`
</output>

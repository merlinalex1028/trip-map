---
phase: 28-overseas-coverage-expansion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/scripts/geo/build-geometry-manifest.mjs
  - apps/web/public/geo/2026-04-21-geo-v3/manifest.json
  - apps/web/public/geo/2026-04-21-geo-v3/cn/layer.json
  - apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json
  - packages/contracts/src/generated/geometry-manifest.generated.ts
  - packages/contracts/src/fixtures.ts
  - packages/contracts/src/contracts.spec.ts
  - apps/server/test/phase28-overseas-cases.ts
  - apps/server/test/canonical-resolve.e2e-spec.ts
  - apps/server/test/records-travel.e2e-spec.ts
  - apps/server/test/records-import.e2e-spec.ts
  - apps/server/test/record-metadata-backfill.e2e-spec.ts
autonomous: true
requirements:
  - GEOX-01
  - GEOX-02
tags:
  - gap-closure
  - phase-28
  - geo
  - dataset-version
  - contracts
  - server-tests
gap_closure: true
closes_gaps:
  - "canonical datasetVersion 在 build、server 与 contracts 之间保持一致，并与 geometry dataset version 明确分离"
  - "WR-03: 历史 `PHASE11_*` / `PHASE12_*` fixtures 被直接改写，破坏了版本化基线"
user_setup: []
must_haves:
  truths:
    - "geometry shard feature properties 里的 `datasetVersion` 恢复为 canonical `canonical-authoritative-2026-04-21`，不再被 `2026-04-21-geo-v3` 覆盖。"
    - "server resolve / create / import / metadata lookup 的断言统一把 canonical datasetVersion 当作 authoritative 字段，geometry 版本只保留在 `geometryRef.geometryDatasetVersion`。"
    - "`PHASE11_*` / `PHASE12_*` fixtures 恢复历史语义，Phase 28 语义继续只由 `PHASE28_*` fixtures 承担。"
    - "不新增任何新的 runtime truth source；Phase 28 仍沿用 build-time authoring + manifest-backed catalog（D-04, D-05, D-08）。"
  artifacts:
    - path: "apps/web/scripts/geo/build-geometry-manifest.mjs"
      provides: "canonical datasetVersion 与 geometry dataset version 分离后的生成逻辑"
      contains: "geometryDatasetVersion"
    - path: "apps/server/test/phase28-overseas-cases.ts"
      provides: "13 国共享矩阵对 canonical datasetVersion 的 authoritative 断言"
      contains: "canonical-authoritative-2026-04-21"
    - path: "packages/contracts/src/fixtures.ts"
      provides: "恢复历史 baseline 后的 `PHASE11_*` / `PHASE12_*` fixtures 与继续复用的 `PHASE28_*` fixtures"
      contains: "PHASE28_"
  key_links:
    - from: "apps/web/scripts/geo/build-geometry-manifest.mjs"
      to: "apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json"
      via: "geo:build 生成 feature metadata"
      pattern: "datasetVersion"
    - from: "apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json"
      to: "apps/server/src/modules/canonical-places/place-metadata-catalog.ts"
      via: "feature.properties.datasetVersion"
      pattern: "datasetVersion"
    - from: "packages/contracts/src/fixtures.ts"
      to: "packages/contracts/src/contracts.spec.ts"
      via: "历史 baseline 与 Phase 28 fixtures 的分离断言"
      pattern: "PHASE11_|PHASE12_|PHASE28_"
---

<objective>
Phase 28 gap closure Wave 1：
修复 verifier blocker 中最根因的一条 split-brain：`datasetVersion` 在 build、server 与 contracts 之间混用了 canonical 语义和 geometry 语义，同时把 review 里指出的历史 fixture baseline 漂移一起收口。

本 plan 只做两类工作：
1. 停止在 geometry shard feature 上覆盖 canonical `datasetVersion`，并重新生成 Phase 28 产物；
2. 把 server/contracts 的断言和 fixture 基线切回正确语义：canonical datasetVersion 是 authoritative metadata，geometry version 只通过 `geometryRef.geometryDatasetVersion` 暴露。

Purpose: 如果这条 split-brain 不先修掉，`userTravelRecord` backfill、bootstrap/sync 回放和后续 Phase 29/30 consumer 都会继续围绕错误字段语义运转，gap 无法真正关闭。
Output: 修正后的 build script、重新生成的 v3 geometry shards、更新后的 server/contracts 回归，以及恢复历史语义的 `PHASE11_*` / `PHASE12_*` fixtures。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-overseas-coverage-expansion/28-VERIFICATION.md
@.planning/phases/28-overseas-coverage-expansion/28-REVIEW.md
@.planning/phases/28-overseas-coverage-expansion/28-01-SUMMARY.md
@.planning/phases/28-overseas-coverage-expansion/28-02-SUMMARY.md
@.planning/phases/28-overseas-coverage-expansion/28-03-SUMMARY.md

<interfaces>
From apps/web/scripts/geo/build-geometry-manifest.mjs:
```javascript
function createOverseasFeatureMetadata(featureProperties) {
  return {
    datasetVersion: CANONICAL_DATASET_VERSION,
  }
}

function enrichFeature(feature, metadata) {
  return {
    ...feature,
    properties: {
      ...feature.properties,
      ...metadata,
      datasetVersion: GEOMETRY_DATASET_VERSION,
    },
  }
}
```

From apps/server/test/phase28-overseas-cases.ts:
```typescript
if (summary.datasetVersion !== '2026-04-21-geo-v3') {
  throw new Error(...)
}
```

From packages/contracts/src/fixtures.ts:
```typescript
export const PHASE11_SMOKE_RECORD_REQUEST = { ... }
export const PHASE12_RESOLVED_CALIFORNIA = { ... }
export const PHASE28_RESOLVED_CALIFORNIA = { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 停止 shard feature 覆盖 canonical datasetVersion，并重新生成 Phase 28 产物</name>
  <files>
    apps/web/scripts/geo/build-geometry-manifest.mjs
    apps/web/public/geo/2026-04-21-geo-v3/manifest.json
    apps/web/public/geo/2026-04-21-geo-v3/cn/layer.json
    apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json
    packages/contracts/src/generated/geometry-manifest.generated.ts
  </files>
  <read_first>
    - apps/web/scripts/geo/build-geometry-manifest.mjs（当前 `create*FeatureMetadata()` 与 `enrichFeature()` 的 `datasetVersion` 双写冲突）
    - apps/server/src/modules/canonical-places/place-metadata-catalog.ts（server 直接读取 shard `feature.properties.datasetVersion`，不要引入第二份 runtime truth source）
    - packages/contracts/src/generated/geometry-manifest.generated.ts（当前 generated contracts 仍被 verifier 标记为 drifted artifact，必须跟随 geo:build 一起核对）
    - .planning/phases/28-overseas-coverage-expansion/28-VERIFICATION.md（blocker 1 的 truth / reason / missing 清单）
    - .planning/phases/28-overseas-coverage-expansion/28-RESEARCH.md（继续保持 build-time authoring + manifest-backed catalog，禁止另发明 canonical dataset 字段）
  </read_first>
  <action>
    只在现有 build pipeline 内修 split-brain，不新增任何字段或新的 runtime truth source：

    1. 修改 `apps/web/scripts/geo/build-geometry-manifest.mjs`：
       - 保留 `createCnFeatureMetadata()` 和 `createOverseasFeatureMetadata()` 中的 `datasetVersion: CANONICAL_DATASET_VERSION`。
       - 删除 `enrichFeature()` 里 `datasetVersion: GEOMETRY_DATASET_VERSION` 这一行，避免把 canonical metadata 字段覆盖成 geometry 版本。
       - 保留 `createManifestEntry()` 里 `geometryDatasetVersion: GEOMETRY_DATASET_VERSION` 不变；geometry 版本仍然只能通过 manifest / `geometryRef.geometryDatasetVersion` 暴露。
       - 不新增 `canonicalDatasetVersion`、`metadataDatasetVersion`、`featureDatasetVersion` 之类的新字段名；当前 gap 要求是恢复既有字段语义，而不是扩张 contract。

    2. 重新生成 Phase 28 geo 产物：
       - 运行 `pnpm --filter @trip-map/web geo:build`
       - 运行 `pnpm --filter @trip-map/contracts build`
       - 让 `apps/web/public/geo/2026-04-21-geo-v3/manifest.json` 与 `packages/contracts/src/generated/geometry-manifest.generated.ts` 也作为 `geo:build` 的直接产物重新落盘并进入本 plan 的显式核对范围。
       - 让 `apps/web/public/geo/2026-04-21-geo-v3/cn/layer.json` 与 `apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json` 重新落盘。

    3. 生成后的目标状态必须是：
       - shard feature properties 里的 `datasetVersion` = `canonical-authoritative-2026-04-21`
       - manifest entries / `geometryRef.geometryDatasetVersion` 继续是 `2026-04-21-geo-v3`
       - `packages/contracts/src/generated/geometry-manifest.generated.ts` 中 Phase 28 generated contract 继续导出当前 authoritative `GEOMETRY_MANIFEST` / `SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES`，且不把 `datasetVersion` 回退到 geometry version。
       - 不改 21 国支持矩阵、不改 admin1-only guard、不改 `SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES` 的来源（继续遵守 D-01, D-02, D-03, D-07）。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web geo:build &amp;&amp; pnpm --filter @trip-map/contracts build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "datasetVersion: GEOMETRY_DATASET_VERSION" apps/web/scripts/geo/build-geometry-manifest.mjs` 结果为 0 行
    - `rg -n "geometryDatasetVersion: GEOMETRY_DATASET_VERSION" apps/web/scripts/geo/build-geometry-manifest.mjs` 至少命中 1 行
    - `rg -n "\"datasetVersion\": \"canonical-authoritative-2026-04-21\"" apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json` 至少命中 1 行
    - `rg -n "\"datasetVersion\": \"canonical-authoritative-2026-04-21\"" apps/web/public/geo/2026-04-21-geo-v3/cn/layer.json` 至少命中 1 行
    - `rg -n "\"datasetVersion\": \"2026-04-21-geo-v3\"" apps/web/public/geo/2026-04-21-geo-v3/cn/layer.json apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json` 结果为 0 行
    - `rg -n "\"geometryDatasetVersion\": \"2026-04-21-geo-v3\"" apps/web/public/geo/2026-04-21-geo-v3/manifest.json` 至少命中 1 行
    - `rg -n "canonical-authoritative-2026-04-21|SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES|GEOMETRY_MANIFEST" packages/contracts/src/generated/geometry-manifest.generated.ts` 至少命中 3 行
    - `pnpm --filter @trip-map/web geo:build` 与 `pnpm --filter @trip-map/contracts build` 均退出码为 0
  </acceptance_criteria>
  <done>v3 geometry shards 已恢复 canonical `datasetVersion` 语义，geometry version 只保留在 manifest / `geometryRef`，split-brain 的 build 侧根因被移除。</done>
</task>

<task type="auto">
  <name>Task 2: 统一 server/contracts 断言到 canonical datasetVersion，并恢复历史 fixture baseline</name>
  <files>
    apps/server/test/phase28-overseas-cases.ts
    apps/server/test/canonical-resolve.e2e-spec.ts
    apps/server/test/records-travel.e2e-spec.ts
    apps/server/test/records-import.e2e-spec.ts
    apps/server/test/record-metadata-backfill.e2e-spec.ts
    packages/contracts/src/fixtures.ts
    packages/contracts/src/contracts.spec.ts
  </files>
  <read_first>
    - apps/server/test/phase28-overseas-cases.ts（当前把 `2026-04-21-geo-v3` 当 authoritative datasetVersion）
    - apps/server/test/canonical-resolve.e2e-spec.ts（当前 resolved place 断言里混入了 geometry 版本）
    - apps/server/test/records-travel.e2e-spec.ts（当前 legacy/stale overseas payload 用 geo version 构造）
    - apps/server/test/records-import.e2e-spec.ts（当前 import authoritative metadata 断言仍跟随错误语义）
    - apps/server/test/record-metadata-backfill.e2e-spec.ts（当前 helper spec 对 canonical lookup 的 datasetVersion 断言错误）
    - packages/contracts/src/fixtures.ts（恢复 `PHASE11_*` / `PHASE12_*` 历史 baseline，并继续复用 `PHASE28_*` fixtures）
    - packages/contracts/src/contracts.spec.ts（历史 fixture 断言和 Phase 28 fixture 断言需要分离）
    - .planning/phases/28-overseas-coverage-expansion/28-REVIEW.md（WR-03：历史 fixture baseline 漂移）
  </read_first>
  <action>
    把所有 test/fixture 语义切回 verifier 要求的 canonical reality，同时恢复历史 baseline：

    1. `apps/server/test/phase28-overseas-cases.ts`
       - 把 `summary.datasetVersion` 的硬编码断言从 `2026-04-21-geo-v3` 改为 `canonical-authoritative-2026-04-21`。
       - 继续要求 `expectedPlaceId` / `expectedBoundaryId` 从 live canonical lookup 解析，不能手写 slug 猜测。

    2. `apps/server/test/canonical-resolve.e2e-spec.ts`
       - 所有 resolved place 的 `datasetVersion` 断言改成 `canonical-authoritative-2026-04-21`。
       - 保留 `geometryRef.geometryDatasetVersion === '2026-04-21-geo-v3'` 断言不变。
       - 13 国表驱动回归继续完整保留，不得缩减 case 数量或 ISO2 集合。

    3. `apps/server/test/records-travel.e2e-spec.ts` 与 `apps/server/test/records-import.e2e-spec.ts`
       - authoritative overseas payload / persisted record 断言统一改成 canonical datasetVersion。
       - legacy rejection 仍然通过篡改 `typeLabel: '一级行政区'` / legacy subtitle 触发；不要把 geometry 版本继续塞进 legacy payload。

    4. `apps/server/test/record-metadata-backfill.e2e-spec.ts`
       - helper 断言统一改成 canonical datasetVersion，确保 `buildCanonicalMetadataLookup()` 返回的是 canonical metadata，而不是 geometry version。

    5. `packages/contracts/src/fixtures.ts`
       - 恢复 `PHASE11_SMOKE_RECORD_REQUEST` 的历史语义：
         - `typeLabel: '一级行政区'`
         - `subtitle: 'Phase 11 Demo Country · 一级行政区'`
       - 恢复 `PHASE12_RESOLVED_CALIFORNIA` 的历史语义：
         - `datasetVersion: 'phase12-canonical-fixture-v1'`
         - `typeLabel: '一级行政区'`
         - `subtitle: 'United States · 一级行政区'`
         - `geometryRef.assetKey: 'overseas/us.json'` 保持历史 baseline 不变
       - 保留并继续复用 `PHASE28_RESOLVED_CALIFORNIA`、`PHASE28_RESOLVED_TOKYO`、`PHASE28_NEW_OVERSEAS_RECORD_FIXTURES`；Phase 28 的 authoritative 语义只应出现在这些 `PHASE28_*` fixtures 中。

    6. `packages/contracts/src/contracts.spec.ts`
       - 显式把 `PHASE11_*` / `PHASE12_*` 断言写成“历史 fixture 仍保持历史语义”。
       - 单独保留 `PHASE28_*` assertions 来验证：
         - `datasetVersion === 'canonical-authoritative-2026-04-21'`
         - `geometryRef.geometryDatasetVersion === '2026-04-21-geo-v3'`
         - `geometryRef.assetKey === 'overseas/layer.json'`
       - 不要再让历史 fixture 断言被 Phase 28 英文标签污染。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/contracts test &amp;&amp; pnpm --filter @trip-map/web test src/stores/map-points.spec.ts &amp;&amp; pnpm --filter @trip-map/web test src/components/LeafletMapStage.spec.ts &amp;&amp; pnpm --filter @trip-map/web test src/components/map-popup/PointSummaryCard.spec.ts &amp;&amp; cd apps/server &amp;&amp; pnpm test test/record-metadata-backfill.e2e-spec.ts &amp;&amp; pnpm test test/canonical-resolve.e2e-spec.ts &amp;&amp; pnpm test test/records-travel.e2e-spec.ts &amp;&amp; pnpm test test/records-import.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "canonical-authoritative-2026-04-21" apps/server/test/phase28-overseas-cases.ts apps/server/test/canonical-resolve.e2e-spec.ts apps/server/test/records-travel.e2e-spec.ts apps/server/test/records-import.e2e-spec.ts apps/server/test/record-metadata-backfill.e2e-spec.ts` 至少命中 5 行
    - `rg -n "datasetVersion: '2026-04-21-geo-v3'|datasetVersion: \\\"2026-04-21-geo-v3\\\"" apps/server/test/phase28-overseas-cases.ts apps/server/test/canonical-resolve.e2e-spec.ts apps/server/test/records-travel.e2e-spec.ts apps/server/test/records-import.e2e-spec.ts apps/server/test/record-metadata-backfill.e2e-spec.ts` 结果为 0 行
    - `rg -n "Phase 11 Demo Country · 一级行政区|United States · 一级行政区" packages/contracts/src/fixtures.ts packages/contracts/src/contracts.spec.ts` 至少命中 2 行
    - `rg -n "PHASE28_RESOLVED_CALIFORNIA|PHASE28_RESOLVED_TOKYO|PHASE28_NEW_OVERSEAS_RECORD_FIXTURES" packages/contracts/src/fixtures.ts packages/contracts/src/contracts.spec.ts` 至少命中 5 行
    - `rg -n "overseas/us.json" packages/contracts/src/fixtures.ts packages/contracts/src/contracts.spec.ts` 至少命中 2 行
    - `pnpm --filter @trip-map/web test src/stores/map-points.spec.ts`、`pnpm --filter @trip-map/web test src/components/LeafletMapStage.spec.ts`、`pnpm --filter @trip-map/web test src/components/map-popup/PointSummaryCard.spec.ts` 均退出码为 0
    - `pnpm --filter @trip-map/contracts test` 以及四条 server targeted tests 均退出码为 0
  </acceptance_criteria>
  <done>server/contracts 断言已统一回 canonical datasetVersion，geometry 版本只保留在 `geometryRef`；历史 fixtures 恢复为历史语义，Phase 28 fixtures 继续独立承载当前 authoritative baseline。</done>
</task>

</tasks>

<must_haves>
Truths:
- geometry shards 与 server canonical lookup 对 `datasetVersion` 的语义重新一致，且与 `geometryDatasetVersion` 明确分离。
- Phase 28 server 回归不再把 `2026-04-21-geo-v3` 误当 authoritative metadata。
- `PHASE11_*` / `PHASE12_*` 历史 fixtures 恢复旧语义，`PHASE28_*` fixtures 保持当前 authoritative 语义。

Artifacts:
- `apps/web/scripts/geo/build-geometry-manifest.mjs`
- `apps/server/test/phase28-overseas-cases.ts`
- `packages/contracts/src/fixtures.ts`

Key links:
- build script -> regenerated `overseas/layer.json`
- regenerated shard metadata -> server canonical place lookup
- historical / current fixtures -> contracts specs
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time geo pipeline -> server canonical lookup | server 直接信任 geo shard feature metadata 作为 authoritative summary 的来源 |
| contracts fixtures -> downstream regression suites | 历史 fixture 一旦被污染，后续 regression 会把错误语义继续固化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | T | `apps/web/scripts/geo/build-geometry-manifest.mjs` | mitigate | 删除 `enrichFeature()` 对 `datasetVersion` 的 geometry 覆盖，恢复 canonical 字段语义。 |
| T-28-04-02 | T | `apps/server/test/phase28-overseas-cases.ts` | mitigate | shared 13 国矩阵显式断言 canonical datasetVersion，并保留 geometryRef 的 geometry version 断言。 |
| T-28-04-03 | R | `packages/contracts/src/fixtures.ts` | mitigate | 恢复 `PHASE11_*` / `PHASE12_*` 历史 baseline，避免 review 指出的语义污染继续扩散。 |
| T-28-04-04 | I | geo shard metadata | accept | 这里不涉及用户私密数据；主要风险是 contract 漂移而不是敏感信息泄露，因此以语义一致性修复为主。 |
</threat_model>

<verification>
- `pnpm --filter @trip-map/web geo:build`
- `pnpm --filter @trip-map/contracts build`
- `pnpm --filter @trip-map/contracts test`
- `cd apps/server && pnpm test test/record-metadata-backfill.e2e-spec.ts`
- `cd apps/server && pnpm test test/canonical-resolve.e2e-spec.ts`
- `cd apps/server && pnpm test test/records-travel.e2e-spec.ts`
- `cd apps/server && pnpm test test/records-import.e2e-spec.ts`
</verification>

<success_criteria>
- verifier blocker 1 所需的 three-way split-brain 被移除：canonical datasetVersion 在 build、server 与 contracts 之间重新一致。
- geometry version 只通过 `geometryRef.geometryDatasetVersion` / manifest entry 暴露，不再伪装成 authoritative metadata。
- review 提到的历史 fixture baseline 漂移被恢复，后续 Phase 29/30 可以继续区分历史回归与 Phase 28 当前契约。
</success_criteria>

<output>
After completion, create `.planning/phases/28-overseas-coverage-expansion/28-04-SUMMARY.md`
</output>

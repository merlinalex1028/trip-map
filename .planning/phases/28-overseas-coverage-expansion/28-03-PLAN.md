---
phase: 28-overseas-coverage-expansion
plan: 03
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - apps/web/src/constants/overseas-support.ts
  - apps/web/src/components/LeafletMapStage.spec.ts
  - apps/web/src/components/map-popup/PointSummaryCard.spec.ts
  - apps/web/src/stores/map-points.spec.ts
  - packages/contracts/src/fixtures.ts
  - packages/contracts/src/contracts.spec.ts
autonomous: true
requirements:
  - GEOX-02
tags:
  - web
  - contracts
  - consumer-compatibility
  - unsupported-copy
user_setup: []
must_haves:
  truths:
    - "unsupported-country copy 直接消费 generated supported-country summary，而不是继续双写 `PHASE26_SUPPORTED_OVERSEAS_COUNTRIES`。"
    - "web map consumer compatibility 继续以 persisted `TravelRecord.displayName/typeLabel/subtitle` 为真源，不做 placeId 级 fallback derivation。"
    - "contracts fixtures 与 web tests 都对齐 Phase 28 的标准英文标签，给 Phase 29/30 提供稳定前置基线。"
  artifacts:
    - path: "apps/web/src/constants/overseas-support.ts"
      provides: "Generated-summary-backed unsupported overseas notice helper"
      contains: "SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH"
    - path: "packages/contracts/src/fixtures.ts"
      provides: "Phase 28-aligned overseas contract fixtures"
      contains: "State"
    - path: "apps/web/src/stores/map-points.spec.ts"
      provides: "Persisted metadata replay compatibility regression"
      contains: "持久化"
  key_links:
    - from: "packages/contracts/src/generated/geometry-manifest.generated.ts"
      to: "apps/web/src/constants/overseas-support.ts"
      via: "supported-country summary import"
      pattern: "SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH"
    - from: "packages/contracts/src/fixtures.ts"
      to: "apps/web/src/stores/map-points.spec.ts"
      via: "standardized overseas fixture labels"
      pattern: "State"
    - from: "apps/web/src/constants/overseas-support.ts"
      to: "apps/web/src/components/LeafletMapStage.spec.ts"
      via: "unsupported notice text assertion"
      pattern: "buildUnsupportedOverseasNotice"
---

<objective>
Phase 28 Wave 2B：把有限的前端/共享契约表面升级到 Phase 28 authoritative reality，但不发明任何新的 runtime metadata derivation。

这个计划只触碰两类内容：
1. unsupported-country 文案，必须从 generated summary 取源，消除静态 Phase 26 名单漂移；
2. contracts / web consumer compatibility regression，必须证明地图、popup 和后续 Phase 29/30 将继续消费 persisted metadata，而不是继续从 `placeId` 或旧 fixture 推导标签。

Purpose: GEOX-02 不只要求 server 数据正确，还要求 web / contracts 消费者没有“看起来工作、实际还在旧文案和旧标签上”的隐形漂移。
Output: generated-summary-backed web notice helper、Phase 28 对齐后的 contracts fixtures，以及覆盖 map/popup notice/consumer replay 的测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-overseas-coverage-expansion/28-CONTEXT.md
@.planning/phases/28-overseas-coverage-expansion/28-RESEARCH.md
@.planning/phases/28-overseas-coverage-expansion/28-01-PLAN.md
@.planning/phases/27-multi-visit-record-foundation/27-CONTEXT.md

<interfaces>
<!-- Source: apps/web/src/constants/overseas-support.ts [VERIFIED 2026-04-21] -->

```typescript
export const PHASE26_SUPPORTED_OVERSEAS_COUNTRIES = [
  '日本', '韩国', '泰国', '新加坡', '马来西亚', '阿联酋', '澳大利亚', '美国',
] as const

export function buildUnsupportedOverseasNotice(regionName?: string | null) {
  return `已识别到${detectedRegionName}。 当前暂不支持点亮，请优先在${PHASE26_SUPPORTED_OVERSEAS_COUNTRY_LABEL}的一级行政区使用。`
}
```

<!-- Source: apps/web/src/stores/map-points.ts [VERIFIED 2026-04-21] -->

```typescript
function recordToDisplayPoint(record: TravelRecord): MapPointDisplay {
  return {
    name: record.displayName,
    cityContextLabel: record.subtitle,
    typeLabel: record.typeLabel,
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 把 unsupported-country copy 改成 generated summary 驱动，移除 Phase 26 静态名单</name>
  <files>
    apps/web/src/constants/overseas-support.ts
  </files>
  <read_first>
    - apps/web/src/constants/overseas-support.ts（当前静态 8 国名单和 notice 文案）
    - packages/contracts/src/generated/geometry-manifest.generated.ts（Phase 28 生成的 `SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES` / `SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH`）
    - .planning/phases/28-overseas-coverage-expansion/28-RESEARCH.md（unsupported copy 双写 drift 风险）
  </read_first>
  <action>
    只改 `apps/web/src/constants/overseas-support.ts`，保持 `buildUnsupportedOverseasNotice(regionName?: string | null)` 的对外签名不变：

    1. 删除 `PHASE26_SUPPORTED_OVERSEAS_COUNTRIES` 和 `PHASE26_SUPPORTED_OVERSEAS_COUNTRY_LABEL` 这两份静态导出，不再在 web 端硬编码 8 国名单。
    2. 改为从 `@trip-map/contracts` 导入：
       - `SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES`
       - `SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH`
    3. 继续在本文件内只保留轻量 helper：
       - `SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES` 若需要二次导出，可直接 re-export
       - `buildUnsupportedOverseasNotice()` 内部使用 `SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH`
    4. notice 文案改成中文层级描述，避免把 D-06 的英文字段直接硬塞进提示句。固定为：
       `已识别到${detectedRegionName}。 当前暂不支持点亮，请优先在${SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH}的一级行政区级别边界使用。`

       这样既继续强调 D-02 的 admin1-only 范围，也不要求 UI 在 unsupported 流程里枚举 `State / Province / Region`。

    5. 不修改 `LeafletMapStage.vue` / `PointSummaryCard.vue` 组件实现；它们已经只依赖 helper 返回值，本任务只收口 helper 的数据来源。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/contracts build</automated>
  </verify>
  <acceptance_criteria>
    - grep `SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH` `apps/web/src/constants/overseas-support.ts` 命中至少 1 行
    - grep `SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES` `apps/web/src/constants/overseas-support.ts` 命中至少 1 行
    - grep `PHASE26_SUPPORTED_OVERSEAS_COUNTRIES` `apps/web/src/constants/overseas-support.ts` 结果为 0 行
    - grep `一级行政区级别边界使用` `apps/web/src/constants/overseas-support.ts` 命中 1 行
    - `pnpm --filter @trip-map/contracts build` 退出码为 0
  </acceptance_criteria>
  <done>unsupported-country 文案已经切到 generated summary 真源，不再受静态 8 国名单漂移影响。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 对齐 contracts/web fixtures 与 consumer compatibility regression 到 Phase 28 标签政策</name>
  <files>
    packages/contracts/src/fixtures.ts
    packages/contracts/src/contracts.spec.ts
    apps/web/src/stores/map-points.spec.ts
    apps/web/src/components/LeafletMapStage.spec.ts
    apps/web/src/components/map-popup/PointSummaryCard.spec.ts
  </files>
  <read_first>
    - packages/contracts/src/fixtures.ts（当前 California fixture 仍是 `一级行政区`）
    - packages/contracts/src/contracts.spec.ts（当前 contracts fixture expectations 仍锁死旧标签）
    - apps/web/src/stores/map-points.spec.ts（persisted metadata replay 用例）
    - apps/web/src/components/LeafletMapStage.spec.ts（unsupported notice helper 与 popup/fixture expectations）
    - apps/web/src/components/map-popup/PointSummaryCard.spec.ts（California 文案仍断言 `一级行政区`）
  </read_first>
  <action>
    把共享 fixture 和 web regression 一次性切到 Phase 28 的标准标签政策：

    1. `packages/contracts/src/fixtures.ts`
       - `PHASE12_RESOLVED_CALIFORNIA` 改为：
         - `typeLabel: 'State'`
         - `subtitle: 'United States · State'`
       - `PHASE11_SMOKE_RECORD_REQUEST` 改成标准英文标签风格，例如：
         - `typeLabel: 'Region'`
         - `subtitle: 'Phase 11 Demo Country · Region'`
       - 不强行把历史 fixture 的 `geometryRef.assetKey` / `geometryDatasetVersion` 同步到 v3；这里只对齐标签风格，避免额外扩大 fixture 语义范围。

    2. `packages/contracts/src/contracts.spec.ts`
       - 把所有 overseas fixture 相关期望从 `一级行政区` 改成对应新标签。
       - 至少显式断言一次 `PHASE12_RESOLVED_CALIFORNIA.subtitle === 'United States · State'`。

    3. `apps/web/src/stores/map-points.spec.ts`
       - 把 persisted overseas record 的示例标签改成标准英文，例如 `State（持久化）` 或 `Prefecture（持久化）`。
       - 保留测试目标不变：验证 store 继续直接使用 persisted `displayName` / `subtitle` / `typeLabel`，而不是按 placeId 重新算 label。

    4. `apps/web/src/components/LeafletMapStage.spec.ts` 与 `apps/web/src/components/map-popup/PointSummaryCard.spec.ts`
       - unsupported notice 相关断言改为匹配新的 helper 输出（来源是 generated summary，不再是静态 8 国）。
       - California / overseas fixture 的文案断言从 `一级行政区` 改成 `State` 及 `United States · State`。

    5. 不新增任何 consumer-side metadata derivation helper；所有测试都应继续围绕 persisted / generated data 验证，而不是引入新的 fallback 逻辑。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/contracts build &amp;&amp; pnpm --filter @trip-map/contracts test &amp;&amp; pnpm --filter @trip-map/web test src/stores/map-points.spec.ts &amp;&amp; pnpm --filter @trip-map/web test src/components/LeafletMapStage.spec.ts &amp;&amp; pnpm --filter @trip-map/web test src/components/map-popup/PointSummaryCard.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `United States · State` `packages/contracts/src/fixtures.ts` 命中至少 1 行
    - grep `State（持久化）\|Prefecture（持久化）` `apps/web/src/stores/map-points.spec.ts` 命中至少 1 行
    - grep `SUPPORTED_OVERSEAS_COUNTRY_LABEL_ZH` `apps/web/src/components/LeafletMapStage.spec.ts` 结果可以为 0，但 `buildUnsupportedOverseasNotice(` 的相关断言必须仍存在
    - `rg -n "一级行政区" packages/contracts/src/fixtures.ts packages/contracts/src/contracts.spec.ts apps/web/src/stores/map-points.spec.ts apps/web/src/components/LeafletMapStage.spec.ts apps/web/src/components/map-popup/PointSummaryCard.spec.ts` 结果为 0 行
    - `pnpm --filter @trip-map/contracts test` 退出码为 0
    - 三条 web test 命令全部退出码为 0
  </acceptance_criteria>
  <done>contracts fixtures、web regression 和 unsupported-copy 全部对齐到 Phase 28 authoritative label policy，Phase 29/30 不需要再为这些消费者做额外 fallback。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated contracts -> web copy helper | build 输出的 supported-country summary 会直接展示给用户。 |
| persisted `TravelRecord` -> map/popup consumer tests | 地图侧 UI 直接消费保存下来的 metadata，不会在客户端重新推导。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | T | `apps/web/src/constants/overseas-support.ts` | mitigate | 改为直接导入 generated summary 常量，移除 `PHASE26_SUPPORTED_*` 双写名单，阻断 unsupported 文案与真实覆盖范围漂移。 |
| T-28-03-02 | T | `apps/web/src/stores/map-points.spec.ts` | mitigate | regression 明确要求 persisted `displayName/typeLabel/subtitle` 直出，禁止 consumer 侧按 `placeId` fallback 导出新标签。 |
| T-28-03-03 | R | `packages/contracts/src/fixtures.ts` | mitigate | contracts fixtures 与 spec 同步升级到 Phase 28 标签策略，避免下游 phase 继续拿旧 `一级行政区` fixture 当基线。 |
| T-28-03-04 | I | unsupported user notice | accept | 文案只展示支持国家摘要，不暴露用户私有记录或 server 内部状态；主要风险是准确性而非敏感信息泄露，已由 generated summary import 缓解。 |
</threat_model>

<verification>
- `pnpm --filter @trip-map/contracts build`
- `pnpm --filter @trip-map/contracts test`
- `pnpm --filter @trip-map/web test src/stores/map-points.spec.ts`
- `pnpm --filter @trip-map/web test src/components/LeafletMapStage.spec.ts`
- `pnpm --filter @trip-map/web test src/components/map-popup/PointSummaryCard.spec.ts`
</verification>

<success_criteria>
- unsupported-country copy 直接使用 generated supported-country summary，不再依赖静态 Phase 26 名单。
- contracts fixtures 与 web regression 都升级到 Phase 28 的标准英文标签风格。
- 地图/popup 相关测试继续证明 consumer 只吃 persisted metadata，不需要任何 ad hoc fallback derivation。
</success_criteria>

<output>
After completion, create `.planning/phases/28-overseas-coverage-expansion/28-03-SUMMARY.md`
</output>

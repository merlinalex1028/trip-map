---
phase: 28-overseas-coverage-expansion
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/scripts/geo/build-geometry-manifest.mjs
  - apps/web/public/geo/2026-04-21-geo-v3/manifest.json
  - apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json
  - packages/contracts/src/generated/geometry-manifest.generated.ts
  - apps/server/src/modules/canonical-places/place-metadata-catalog.ts
  - apps/server/test/phase28-overseas-cases.ts
  - apps/server/test/canonical-resolve.e2e-spec.ts
  - apps/server/test/records-travel.e2e-spec.ts
autonomous: true
requirements:
  - GEOX-01
  - GEOX-02
tags:
  - gap-closure
  - geo
  - canonical-lookup
  - identity
  - regression
gap_closure: true
closes_gaps:
  - "扩容后的 overseas admin1 identity 在 generated artifacts 与 canonical lookup 中保持全局唯一，新增国家可以稳定 resolve 并保存旅行记录"
must_haves:
  truths:
    - "US-WA、US-DC、AR-B、AR-C 在 Phase 28 生成产物里拥有互不冲突的 `placeId`、`boundaryId` 与 `renderableId`。"
    - "`geo:build` 会对 overseas identity 做 fail-fast 全局唯一校验，不再把冲突静默写进 manifest、layer 和 generated contracts。"
    - "canonical metadata lookup 在 shard lookupId 或 summary key 冲突时会直接抛错，而不是继续 `find()` + `Map.set()` 静默覆盖。"
    - "Washington / District of Columbia / Buenos Aires Province / Buenos Aires City 的 resolve 与 save 回归明确验证 distinct identity，同时保持 `datasetVersion = canonical-authoritative-2026-04-21` 与 `geometryRef.geometryDatasetVersion = 2026-04-21-geo-v3`。"
  artifacts:
    - path: "apps/web/scripts/geo/build-geometry-manifest.mjs"
      provides: "sourceFeatureId-aware overseas identity generation and build-time uniqueness guards"
      contains: "US-WA"
    - path: "apps/server/src/modules/canonical-places/place-metadata-catalog.ts"
      provides: "fail-fast canonical lookup indexing instead of silent `find()` collisions"
      contains: "Duplicate canonical lookupId"
    - path: "apps/server/test/phase28-overseas-cases.ts"
      provides: "explicit collision probe matrix for US-WA / US-DC / AR-B / AR-C"
      contains: "PHASE28_IDENTITY_COLLISION_CASES"
  key_links:
    - from: "apps/web/scripts/geo/build-geometry-manifest.mjs"
      to: "apps/web/public/geo/2026-04-21-geo-v3/manifest.json"
      via: "geo:build generated overseas identity artifacts"
      pattern: "sourceFeatureId"
    - from: "apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json"
      to: "apps/server/src/modules/canonical-places/place-metadata-catalog.ts"
      via: "renderableId / boundaryId canonical lookup"
      pattern: "renderableId"
    - from: "apps/server/test/phase28-overseas-cases.ts"
      to: "apps/server/test/canonical-resolve.e2e-spec.ts"
      via: "shared collision probe matrix"
      pattern: "PHASE28_IDENTITY_COLLISION_CASES"
---

<objective>
Phase 28 additive gap closure：修复 overseas identity collision，把 generated artifacts 与 canonical lookup 都收敛到“唯一且可失败”的状态，同时保留 28-04 已经修好的 canonical datasetVersion 语义。

Purpose: 这是 `GEOX-01` 当前剩余的根因 blocker。只要 `placeId` / `boundaryId` / `renderableId` 还能在 build 产物里冲突，server resolve/save 和后续时间轴/统计都没有稳定输入。
Output: 基于 `sourceFeatureId` 的歧义 identity override、build-time 唯一性守卫、fail-fast canonical lookup，以及覆盖 Washington/DC 与 Buenos Aires 冲突场景的 resolve/save regression。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-overseas-coverage-expansion/28-CONTEXT.md
@.planning/phases/28-overseas-coverage-expansion/28-RESEARCH.md
@.planning/phases/28-overseas-coverage-expansion/28-VERIFICATION.md
@.planning/phases/28-overseas-coverage-expansion/28-REVIEW.md
@.planning/phases/28-overseas-coverage-expansion/28-04-SUMMARY.md

<interfaces>
From apps/web/scripts/geo/build-geometry-manifest.mjs:
```javascript
const OVERSEAS_SPECIAL_IDENTITIES = new Map([
  ['US:California', { placeId: 'us-california', boundaryId: 'ne-admin1-us-california' }],
])

function buildOverseasIdentity(featureProperties) {
  const admin1Name = featureProperties.name_en ?? featureProperties.name
  const specialKey = `${featureProperties.iso_a2 ?? featureProperties.adm0_a3}:${admin1Name}`
  const special = OVERSEAS_SPECIAL_IDENTITIES.get(specialKey)
  // ...
}
```

From apps/web/scripts/geo/overseas-support/index.mjs:
```javascript
export function getOverseasAdmin1SourceFeatureId(featureProperties) {
  const admin1Name = featureProperties.name_en ?? featureProperties.name
  return featureProperties.iso_3166_2 ?? `${featureProperties.adm0_a3}/${admin1Name}`
}
```

From apps/server/src/modules/canonical-places/place-metadata-catalog.ts:
```typescript
const feature = shard.features.find(
  candidate =>
    candidate.properties.renderableId === lookupId
    || candidate.properties.boundaryId === lookupId,
)

byPlaceId.set(summary.placeId, summary)
byBoundaryId.set(summary.boundaryId, summary)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用 sourceFeatureId 修复歧义 overseas identity，并为 generated artifacts 增加 fail-fast 唯一性守卫</name>
  <files>
    apps/web/scripts/geo/build-geometry-manifest.mjs
    apps/web/public/geo/2026-04-21-geo-v3/manifest.json
    apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json
    packages/contracts/src/generated/geometry-manifest.generated.ts
  </files>
  <read_first>
    - apps/web/scripts/geo/build-geometry-manifest.mjs（当前 `OVERSEAS_SPECIAL_IDENTITIES`、`buildOverseasIdentity()` 与 `createManifestEntry()`）
    - apps/web/scripts/geo/overseas-support/index.mjs（`getOverseasAdmin1SourceFeatureId()` 是 sourceFeatureId 真源）
    - .planning/phases/28-overseas-coverage-expansion/28-VERIFICATION.md（identity collision blocker 的 `reason` / `missing`）
    - .planning/phases/28-overseas-coverage-expansion/28-REVIEW.md（WR-01 给出的 collision 证据）
  </read_first>
  <action>
    在不改动 21 国支持矩阵、不重开 28-04 canonical datasetVersion 语义的前提下，按 D-04、D-05、D-08 直接修正 overseas identity authoring：

    1. 把 `apps/web/scripts/geo/build-geometry-manifest.mjs` 中 `OVERSEAS_SPECIAL_IDENTITIES` 的 key 从 `${iso}:${admin1Name}` 迁移到 `getOverseasAdmin1SourceFeatureId(featureProperties)`，不要再靠同名 `name_en` 判别歧义 admin1。

    2. 在 `OVERSEAS_SPECIAL_IDENTITIES` 中显式加入以下 4 组 override，命名固定为下列 target ids，不要另发明第二套 truth source：
       - `US-WA` -> `placeId: 'us-washington-state'`, `boundaryId: 'ne-admin1-us-washington-state'`
       - `US-DC` -> `placeId: 'us-district-of-columbia'`, `boundaryId: 'ne-admin1-us-district-of-columbia'`
       - `AR-B` -> `placeId: 'ar-buenos-aires-province'`, `boundaryId: 'ne-admin1-ar-buenos-aires-province'`
       - `AR-C` -> `placeId: 'ar-buenos-aires-city'`, `boundaryId: 'ne-admin1-ar-buenos-aires-city'`

       其余非歧义国家/地区继续保留现有 `slugify(name_en ?? name)` fallback；不要顺手改 California、Tokyo 等已稳定 id。

    3. 在 build 流程内新增明确的 overseas 唯一性守卫，建议命名为 `assertUniqueOverseasIdentity()` 或等价 helper。它必须在写出 `layerFeatures` / `manifestEntries` 之前对以下三类 key 做 `sourceFeatureId` 级别的全局唯一校验：
       - `placeId`
       - `boundaryId`
       - `renderableId`

       若任一 key 重复，`geo:build` 直接抛错并给出冲突的 `sourceFeatureId`（例如 `US-WA` 与 `US-DC`），禁止再把冲突静默落盘到 generated artifacts。

    4. 重新生成 Phase 28 authoritative 产物：
       - `pnpm --filter @trip-map/web geo:build`
       - `pnpm --filter @trip-map/contracts build`

       并确认以下语义保持不变，不得因 identity 修复而回退：
       - `feature.properties.datasetVersion === 'canonical-authoritative-2026-04-21'`
       - `manifest entry.geometryDatasetVersion === '2026-04-21-geo-v3'`
       - `SUPPORTED_OVERSEAS_COUNTRY_SUMMARIES` 仍来自既有 build-time authoring
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web geo:build &amp;&amp; pnpm --filter @trip-map/contracts build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "US-WA|US-DC|AR-B|AR-C" apps/web/scripts/geo/build-geometry-manifest.mjs` 至少命中 4 行
    - `rg -n "us-washington-state|us-district-of-columbia|ar-buenos-aires-province|ar-buenos-aires-city" apps/web/public/geo/2026-04-21-geo-v3/manifest.json apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json packages/contracts/src/generated/geometry-manifest.generated.ts` 至少命中 8 行
    - `rg -n "\"placeId\": \"us-washington\"|\"placeId\": \"ar-buenos-aires\"|\"boundaryId\": \"ne-admin1-us-washington\"|\"boundaryId\": \"ne-admin1-ar-buenos-aires\"" apps/web/public/geo/2026-04-21-geo-v3/manifest.json apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json packages/contracts/src/generated/geometry-manifest.generated.ts` 结果为 0 行
    - `rg -n "assertUniqueOverseasIdentity|Duplicate overseas" apps/web/scripts/geo/build-geometry-manifest.mjs` 至少命中 1 行
    - `rg -n "\"datasetVersion\": \"canonical-authoritative-2026-04-21\"" apps/web/public/geo/2026-04-21-geo-v3/overseas/layer.json` 至少命中 1 行
    - `pnpm --filter @trip-map/web geo:build` 与 `pnpm --filter @trip-map/contracts build` 均退出码为 0
  </acceptance_criteria>
  <done>generated manifest、overseas layer 与 generated contracts 中的歧义 overseas admin1 已全部拥有唯一 identity，且 build 会对未来重复 key 直接失败。</done>
</task>

<task type="auto">
  <name>Task 2: 让 canonical lookup 对 collision fail-fast，并补 Washington/DC + Buenos Aires resolve/save regression</name>
  <files>
    apps/server/src/modules/canonical-places/place-metadata-catalog.ts
    apps/server/test/phase28-overseas-cases.ts
    apps/server/test/canonical-resolve.e2e-spec.ts
    apps/server/test/records-travel.e2e-spec.ts
  </files>
  <read_first>
    - apps/server/src/modules/canonical-places/place-metadata-catalog.ts（当前 `find()` + `Map.set()` 静默覆盖风险）
    - apps/server/test/phase28-overseas-cases.ts（当前只有 13 国 happy-path matrix，没有 collision matrix）
    - apps/server/test/canonical-resolve.e2e-spec.ts（Phase 28 resolve regression 入口）
    - apps/server/test/records-travel.e2e-spec.ts（authoritative save regression 入口）
    - .planning/phases/28-overseas-coverage-expansion/28-VERIFICATION.md（明确要求补 Washington/DC 与 Buenos Aires resolve/save regression）
  </read_first>
  <action>
    在 server authoritative 消费侧把 collision 从“静默覆盖”改成“立即失败”，并用明确的歧义场景锁住 `GEOX-01` / `GEOX-02`：

    1. 修改 `apps/server/src/modules/canonical-places/place-metadata-catalog.ts`：
       - 不再对 shard feature 使用 `shard.features.find(...)`。
       - 为每个 shard 预构建 `lookupId -> feature` 索引；索引 key 同时覆盖 `renderableId` 与 `boundaryId`。
       - 若同一 shard 出现重复 `lookupId`，直接抛错，错误文案需包含重复 key（例如 `Duplicate canonical lookupId "ne-admin1-us-washington-state"`）。
       - 在写入 `byPlaceId` / `byBoundaryId` 时也做重复 key 守卫；若同一 `placeId` 或 `boundaryId` 已存在且指向不同 summary，直接抛错，禁止继续 `Map.set()` 覆盖。

    2. 扩展 `apps/server/test/phase28-overseas-cases.ts`，新增 `PHASE28_IDENTITY_COLLISION_CASES`，固定包含以下 4 条 probe，并在模块加载时校验 expected ids 全局唯一：
       - `US-WA` / `Washington` / `(47.4865, -120.361)` / `expectedTypeLabel: 'State'` / `expectedPlaceId: 'us-washington-state'` / `expectedBoundaryId: 'ne-admin1-us-washington-state'`
       - `US-DC` / `Washington` / `(38.8922, -77.0113)` / `expectedTypeLabel: 'State'` / `expectedPlaceId: 'us-district-of-columbia'` / `expectedBoundaryId: 'ne-admin1-us-district-of-columbia'`
       - `AR-B` / `Buenos Aires` / `(-36.6734, -60.1133)` / `expectedTypeLabel: 'Province'` / `expectedPlaceId: 'ar-buenos-aires-province'` / `expectedBoundaryId: 'ne-admin1-ar-buenos-aires-province'`
       - `AR-C` / `Buenos Aires` / `(-34.6202, -58.4527)` / `expectedTypeLabel: 'Province'` / `expectedPlaceId: 'ar-buenos-aires-city'` / `expectedBoundaryId: 'ne-admin1-ar-buenos-aires-city'`

       这些 case 继续沿用当前 canonical datasetVersion 断言：`canonical-authoritative-2026-04-21`。不要把 geometry version 塞回 `datasetVersion`。

    3. 在 `apps/server/test/canonical-resolve.e2e-spec.ts` 增加表驱动回归，逐条验证 4 个 collision cases：
       - `/places/resolve` 返回 `status === 'resolved'`
       - `place.placeId` / `boundaryId` 与 `PHASE28_IDENTITY_COLLISION_CASES` 中的 expected ids 一致
       - `datasetVersion === 'canonical-authoritative-2026-04-21'`
       - `geometryRef.assetKey === 'overseas/layer.json'`
       - `geometryRef.geometryDatasetVersion === '2026-04-21-geo-v3'`

    4. 在 `apps/server/test/records-travel.e2e-spec.ts` 增加 save regression：
       - 以 `getCanonicalPlaceSummaryById(case.expectedPlaceId)` 或等价 canonical lookup 生成 4 个 authoritative create payload
       - 断言 4 次 create 都成功，且持久化/响应里的 `placeId` / `boundaryId` 全部 distinct
       - 增加 1 条 negative case：把 `US-DC` payload 的 id 强行改回旧的 `us-washington` / `ne-admin1-us-washington`，期望 400 且继续命中 `metadata must match authoritative catalog` / 等价 exact-match 错误

    5. 不修改 label policy、supported-country matrix 或 `userTravelRecord` backfill 逻辑；本计划只解决 identity uniqueness 与 canonical lookup。
  </action>
  <verify>
    <automated>cd apps/server &amp;&amp; pnpm test test/canonical-resolve.e2e-spec.ts &amp;&amp; pnpm test test/records-travel.e2e-spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Duplicate canonical lookupId|Duplicate canonical placeId|Duplicate canonical boundaryId" apps/server/src/modules/canonical-places/place-metadata-catalog.ts` 至少命中 2 行
    - `rg -n "PHASE28_IDENTITY_COLLISION_CASES|US-WA|US-DC|AR-B|AR-C" apps/server/test/phase28-overseas-cases.ts apps/server/test/canonical-resolve.e2e-spec.ts apps/server/test/records-travel.e2e-spec.ts` 至少命中 8 行
    - `rg -n "us-washington-state|us-district-of-columbia|ar-buenos-aires-province|ar-buenos-aires-city" apps/server/test/phase28-overseas-cases.ts apps/server/test/canonical-resolve.e2e-spec.ts apps/server/test/records-travel.e2e-spec.ts` 至少命中 8 行
    - `rg -n "canonical-authoritative-2026-04-21" apps/server/test/phase28-overseas-cases.ts apps/server/test/canonical-resolve.e2e-spec.ts apps/server/test/records-travel.e2e-spec.ts` 至少命中 4 行
    - `cd apps/server && pnpm test test/canonical-resolve.e2e-spec.ts` 与 `cd apps/server && pnpm test test/records-travel.e2e-spec.ts` 均退出码为 0
  </acceptance_criteria>
  <done>canonical lookup 对重复 key 不再静默覆盖，Washington/DC 与 Buenos Aires 的 4 个 collision cases 已被自动化证明可唯一区分、可 resolve、可保存。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time authoring -> generated artifacts | `build-geometry-manifest.mjs` 生成的 identity 直接决定 manifest、layer 与 generated contracts 是否唯一。 |
| generated artifacts -> canonical lookup | server 从 geometry shards 读 canonical metadata；若 lookupId 或 summary key 冲突，会破坏 resolve/save authoritative 语义。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-01 | T | `apps/web/scripts/geo/build-geometry-manifest.mjs` | mitigate | 对 `placeId` / `boundaryId` / `renderableId` 增加 sourceFeatureId 级别的 fail-fast 唯一性守卫，阻断 collision 产物落盘。 |
| T-28-06-02 | T | `apps/server/src/modules/canonical-places/place-metadata-catalog.ts` | mitigate | 用 shard 索引 + duplicate key guard 取代 `find()` + `Map.set()`，任何 collision 直接抛错而非静默覆盖。 |
| T-28-06-03 | R | Phase 28 resolve/save regression | mitigate | 新增 `PHASE28_IDENTITY_COLLISION_CASES`，把 US-WA / US-DC / AR-B / AR-C 的 resolve/save 作为固定 regression 面。 |
| T-28-06-04 | I | generated contracts / manifest auditability | accept | identity 修复不引入新敏感数据；风险主要是错误映射而非泄露，已通过 generated artifact 与 server regression 双重验证缓解。 |
</threat_model>

<verification>
- `pnpm --filter @trip-map/web geo:build`
- `pnpm --filter @trip-map/contracts build`
- `cd apps/server && pnpm test test/canonical-resolve.e2e-spec.ts`
- `cd apps/server && pnpm test test/records-travel.e2e-spec.ts`
</verification>

<success_criteria>
- overseas generated artifacts 不再包含 `us-washington` / `ar-buenos-aires` 这类 collision ids。
- canonical lookup 对 duplicate lookupId 与 duplicate summary key 会 fail-fast，而不是静默覆盖。
- Washington / District of Columbia / Buenos Aires Province / Buenos Aires City 四个歧义 admin1 已被自动化验证为 distinct authoritative records。
</success_criteria>

<output>
After completion, create `.planning/phases/28-overseas-coverage-expansion/28-06-SUMMARY.md`
</output>

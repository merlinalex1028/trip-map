---
phase: 09-popup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/map-point.ts
  - src/stores/map-points.ts
  - src/stores/map-points.spec.ts
  - src/components/map-popup/PointSummaryCard.vue
  - src/components/PointPreviewDrawer.vue
  - src/components/PointPreviewDrawer.spec.ts
autonomous: true
requirements: [POP-02]
must_haves:
  truths:
    - "候选确认、草稿摘要和已保存点摘要共享同一套轻交互状态，而不是继续绑死在 drawer 主入口上。"
    - "用户可以从轻量摘要态显式接力到 drawer 查看或编辑，popup 本身不承载长表单。"
    - "点亮状态切换和破坏性动作在轻量摘要层有明确动作契约，不再依赖浏览器阻塞式 confirm。"
  artifacts:
    - path: "src/types/map-point.ts"
      provides: "summary/deep surface 类型契约"
      contains: "export type SummaryMode"
    - path: "src/stores/map-points.ts"
      provides: "summaryMode、drawerMode 与 handoff action"
      contains: "function openDrawerView"
    - path: "src/components/map-popup/PointSummaryCard.vue"
      provides: "共享摘要卡与快捷操作区"
      contains: "查看详情"
    - path: "src/components/PointPreviewDrawer.vue"
      provides: "仅承接 deep view/edit 的 drawer"
      contains: "放弃编辑"
  key_links:
    - from: "src/stores/map-points.ts"
      to: "src/components/map-popup/PointSummaryCard.vue"
      via: "summary surface state + action callbacks"
      pattern: "summaryMode|openDrawerView|toggleActivePointFeatured"
    - from: "src/stores/map-points.ts"
      to: "src/components/PointPreviewDrawer.vue"
      via: "drawerMode deep-surface handoff"
      pattern: "drawerMode|closeDrawer|enterEditMode|exitEditMode"
---

<objective>
把“轻量摘要态”和“deep drawer 态”彻底拆开，先把状态契约、快捷动作和共享摘要卡落稳，为后续地图内 popup/peek 提供可直接消费的接口。

Purpose: 落实 D-02、D-03、D-05、D-06、D-07、D-11、D-12、D-13，让 popup 成为主入口前先有明确的状态和组件边界。
Output: 更新后的 `map-points` surface 状态模型、可复用 `PointSummaryCard`、只承接 deep view/edit 的 `PointPreviewDrawer`。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-popup/09-CONTEXT.md
@.planning/phases/09-popup/09-RESEARCH.md
@.planning/phases/09-popup/09-UI-SPEC.md
@.planning/phases/07-城市选择与兼容基线/07-05-SUMMARY.md
@.planning/phases/08-城市边界高亮语义/08-04-SUMMARY.md
@src/stores/map-points.ts
@src/components/PointPreviewDrawer.vue
@src/types/map-point.ts

<interfaces>
From `src/types/map-point.ts`:
```ts
export interface MapPointDisplay extends BaseMapPoint {
  source: MapPointSource
}

export interface DraftMapPoint extends BaseMapPoint {
  source: 'detected'
}

export type DrawerMode = 'candidate-select' | 'detected-preview' | 'view' | 'edit'
```

From `src/stores/map-points.ts`:
```ts
const pendingCitySelection = shallowRef<PendingCitySelection | null>(null)
const selectedPointId = shallowRef<string | null>(null)
const drawerMode = shallowRef<DrawerMode | null>(null)

function startPendingCitySelection(fallbackPoint: DraftMapPoint, cityCandidates: GeoCityCandidate[]) { ... }
function saveDraftAsPoint(snapshot?: EditablePointSnapshot) { ... }
function enterEditMode() { ... }
function exitEditMode() { ... }
function deleteUserPoint(id: string) { ... }
function hideSeedPoint(id: string) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 summary/deep surface 契约并补齐 store handoff 动作</name>
  <read_first>
src/types/map-point.ts
src/stores/map-points.ts
src/stores/map-points.spec.ts
.planning/phases/09-popup/09-CONTEXT.md
.planning/phases/09-popup/09-RESEARCH.md
  </read_first>
  <files>src/types/map-point.ts, src/stores/map-points.ts, src/stores/map-points.spec.ts</files>
  <behavior>
    - Test 1: `startPendingCitySelection()` 进入 `summaryMode === 'candidate-select'`，同时 `drawerMode === null`。
    - Test 2: 确认候选后，已保存点进入 `summaryMode === 'view'`，未保存草稿进入 `summaryMode === 'detected-preview'`。
    - Test 3: `openDrawerView()` / `closeDrawer()` 只切 deep drawer，不清掉 summary state 或 `selectedBoundaryId`。
    - Test 4: `toggleActivePointFeatured()` 能对 draft、saved、seed 三类当前点位切换 `isFeatured`。
  </behavior>
  <action>按 D-02、D-03、D-05、D-07 修改 surface 状态模型：在 `src/types/map-point.ts` 中新增 `SummaryMode = 'candidate-select' | 'detected-preview' | 'view'`，把 `DrawerMode` 收窄为 `'view' | 'edit'`，并新增 `SummarySurfaceState` 联合类型，精确覆盖 `{ mode: 'candidate-select'; fallbackPoint: DraftMapPoint; cityCandidates: GeoCityCandidate[] }` 与 `{ mode: 'detected-preview' | 'view'; point: MapPointDisplay; boundarySupportState: 'supported' | 'missing' | 'not-applicable' }` 两类 shape。随后在 `src/stores/map-points.ts` 中引入 `summaryMode`、`summarySurfaceState`、`openDrawerView()`、`closeDrawer()`、`toggleActivePointFeatured()`；`startPendingCitySelection()` 必须写入 `summaryMode = 'candidate-select'` 且 `drawerMode = null`，`startDraftFromDetection()` / `replaceDraftFromDetection()` 写入 `summaryMode = 'detected-preview'`，`selectPointById()` / saved-city reuse 写入 `summaryMode = 'view'`。`clearActivePoint()` 继续作为真正清空选中态的唯一出口，但 `closeDrawer()` 只能把 `drawerMode` 置空，不得顺手清掉 summary state 或边界高亮语义。把这些分流和 handoff 行为写入 `src/stores/map-points.spec.ts`，覆盖候选确认、saved reuse、drawer handoff、featured toggle 四条主链路。</action>
  <acceptance_criteria>
    - `src/types/map-point.ts` 包含精确字符串 `export type SummaryMode = 'candidate-select' | 'detected-preview' | 'view'`。
    - `src/types/map-point.ts` 包含精确字符串 `export type DrawerMode = 'view' | 'edit'`。
    - `src/stores/map-points.ts` 包含 `const summaryMode = shallowRef<SummaryMode | null>(null)`、`function openDrawerView(`、`function closeDrawer(`、`function toggleActivePointFeatured(`。
    - `src/stores/map-points.spec.ts` 对 `summaryMode`、`drawerMode`、`selectedBoundaryId` 和 `isFeatured` 有新增断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/stores/map-points.spec.ts</automated>
  </verify>
  <done>store 对 summary surface 和 deep drawer 的职责边界已经独立，后续 popup/peek 可以只消费 `summarySurfaceState` 和 handoff action，不再反向依赖旧的 drawer-only 模式。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 抽共享摘要卡并把 drawer 缩回 deep view/edit</name>
  <read_first>
src/components/PointPreviewDrawer.vue
src/components/PointPreviewDrawer.spec.ts
src/stores/map-points.ts
.planning/phases/09-popup/09-CONTEXT.md
.planning/phases/09-popup/09-UI-SPEC.md
  </read_first>
  <files>src/components/map-popup/PointSummaryCard.vue, src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts</files>
  <behavior>
    - Test 1: `PointSummaryCard` 在 candidate/detected/view 三态分别展示 `确认城市`、`保存为地点`、`查看详情` 等精确按钮文案。
    - Test 2: `PointSummaryCard` 的删除/隐藏确认采用内联确认文案，不调用 `window.confirm()`。
    - Test 3: `PointPreviewDrawer` 不再渲染候选搜索或摘要快捷操作；它只在 `drawerMode === 'view' || drawerMode === 'edit'` 时渲染 deep surface。
    - Test 4: deep edit drawer 的取消按钮文案为 `放弃编辑`，并继续保留既有 unsaved-change guard。
  </behavior>
  <action>按 D-04、D-05、D-06、D-11、D-12、D-13 新建 `src/components/map-popup/PointSummaryCard.vue`，让它承接三类 summary state 的身份信息、fallback/boundary notice、候选列表、快捷动作和内联确认。按钮文案必须直接使用 `09-UI-SPEC.md` 中锁定值：candidate 态包含 `确认城市` 候选项和 `按国家/地区继续记录`；draft 态包含 `保存为地点`、`查看详情`、`点亮状态`；saved/view 态包含 `查看详情`、`编辑地点`、`点亮状态`，以及按 source 分流的 `删除地点` 或 `隐藏预置地点`。删除/隐藏确认必须是卡片内部二次点击或确认行，精确文案为 `删除地点：确认删除这个地点？` / `隐藏预置地点：确认隐藏这个预置地点？`，禁止使用 `window.confirm()`。随后把 `src/components/PointPreviewDrawer.vue` 改成 deep-only surface：`drawerMode === 'view'` 时显示完整详情、坐标、描述和 deep action；`drawerMode === 'edit'` 时继续显示表单，但取消按钮文本改为 `放弃编辑`。candidate 搜索框、候选列表、draft summary CTA、view summary CTA 都必须从 drawer 模板中移除。更新 `src/components/PointPreviewDrawer.spec.ts`，覆盖 deep-only 渲染、`放弃编辑` 文案、以及从 store handoff 进入 drawer 的焦点/关闭基线。</action>
  <acceptance_criteria>
    - `src/components/map-popup/PointSummaryCard.vue` 包含精确字符串 `查看详情`、`编辑地点`、`点亮状态`、`删除地点`、`隐藏预置地点`、`按国家/地区继续记录`。
    - `src/components/map-popup/PointSummaryCard.vue` 不包含 `window.confirm`。
    - `src/components/PointPreviewDrawer.vue` 包含 `放弃编辑`，且不再包含 `placeholder="搜索城市"`。
    - `src/components/PointPreviewDrawer.spec.ts` 对 `放弃编辑` 和 deep-only 渲染有新增断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/components/PointPreviewDrawer.spec.ts</automated>
  </verify>
  <done>共享摘要内容已经从 drawer 中独立出来，drawer 只剩完整查看/编辑职责，popup/peek 后续只需包一层壳即可接管主入口。</done>
</task>

</tasks>

<verification>
- `pnpm test -- src/stores/map-points.spec.ts src/components/PointPreviewDrawer.spec.ts`
- `pnpm build`
</verification>

<success_criteria>
- `map-points` 已暴露 summary surface 契约与 deep drawer handoff action。
- `PointSummaryCard` 已具备 candidate/draft/view 三态和精确动作文案。
- `PointPreviewDrawer` 不再承担候选确认和轻量摘要主入口，只保留 deep view/edit。
</success_criteria>

<output>
After completion, create `.planning/phases/09-popup/09-popup-01-SUMMARY.md`
</output>

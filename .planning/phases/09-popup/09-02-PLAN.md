---
phase: 09-popup
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/composables/usePopupAnchoring.ts
  - src/components/map-popup/MapContextPopup.vue
  - src/components/map-popup/MapContextPopup.spec.ts
  - src/components/SeedMarkerLayer.vue
  - src/components/WorldMapStage.vue
  - src/components/WorldMapStage.spec.ts
autonomous: true
requirements: [POP-01, POP-02]
must_haves:
  truths:
    - "桌面端 summary surface 在地图舞台内部以 anchored popup 形式出现，而不是回退到右侧或角落浮层。"
    - "popup 标题、动作和当前城市边界高亮消费同一份 store 身份，不制造独立记忆态。"
    - "popup 内可以直接完成确认城市、保存草稿、查看详情、编辑、点亮状态切换与破坏性动作。"
  artifacts:
    - path: "src/composables/usePopupAnchoring.ts"
      provides: "Floating UI 锚点定位封装"
      contains: "computePosition"
    - path: "src/components/map-popup/MapContextPopup.vue"
      provides: "桌面 anchored popup shell"
      contains: "role=\"dialog\""
    - path: "src/components/WorldMapStage.vue"
      provides: "popup 装配与 anchor precedence"
      contains: "MapContextPopup"
  key_links:
    - from: "src/components/WorldMapStage.vue"
      to: "src/composables/usePopupAnchoring.ts"
      via: "reference/floating refs + collision state"
      pattern: "usePopupAnchoring"
    - from: "src/components/WorldMapStage.vue"
      to: "src/components/map-popup/PointSummaryCard.vue"
      via: "MapContextPopup renders summary content"
      pattern: "PointSummaryCard"
    - from: "src/components/SeedMarkerLayer.vue"
      to: "src/components/WorldMapStage.vue"
      via: "data-point-id anchor lookup"
      pattern: "data-point-id"
---

<objective>
在桌面端把 summary surface 真正搬进地图舞台：为 popup 接入锚点定位、碰撞处理和 anchor precedence，同时直接复用 Phase 09 的共享摘要卡和 store 动作。

Purpose: 落实 D-01、D-02、D-05、D-08、D-10 与 POP-01，让“地图内 popup 成为主入口”先在桌面端成立。
Output: `@floating-ui/dom` 依赖、`usePopupAnchoring`、`MapContextPopup`、接入 anchored popup 的 `WorldMapStage`。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-popup/09-CONTEXT.md
@.planning/phases/09-popup/09-RESEARCH.md
@.planning/phases/09-popup/09-UI-SPEC.md
@.planning/phases/09-popup/09-01-PLAN.md
@src/components/WorldMapStage.vue
@src/components/SeedMarkerLayer.vue
@src/components/map-popup/PointSummaryCard.vue

<interfaces>
From `src/components/map-popup/PointSummaryCard.vue` after Plan 01:
```ts
// Shared summary content for candidate-select / detected-preview / view
// Emits: confirm-candidate, continue-fallback, save-point, open-detail, edit-point, toggle-featured, confirm-destructive
```

From `src/stores/map-points.ts` after Plan 01:
```ts
const summarySurfaceState = computed<SummarySurfaceState | null>(...)
function openDrawerView() { ... }
function toggleActivePointFeatured() { ... }
function confirmPendingCitySelection(candidate: GeoCityCandidate) { ... }
function continuePendingWithFallback() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 引入 Floating UI 锚点封装并实现桌面 popup shell</name>
  <read_first>
package.json
pnpm-lock.yaml
src/components/map-popup/PointSummaryCard.vue
.planning/phases/09-popup/09-RESEARCH.md
.planning/phases/09-popup/09-UI-SPEC.md
  </read_first>
  <files>package.json, pnpm-lock.yaml, src/composables/usePopupAnchoring.ts, src/components/map-popup/MapContextPopup.vue, src/components/map-popup/MapContextPopup.spec.ts</files>
  <behavior>
    - Test 1: `usePopupAnchoring` 使用 `computePosition` + `autoUpdate`，并应用 `offset(12)`、`flip({ padding: 16 })`、`shift({ padding: 16 })`、`size({ padding: 16 })`。
    - Test 2: `MapContextPopup` 以 `role="dialog"`、`aria-modal="false"` 和箭头 affordance 渲染，宽度限制在 `280px` 到 `360px`。
    - Test 3: popup 打开时聚焦标题或首个可交互控件，符合现有 drawer 焦点入口基线。
  </behavior>
  <action>按 `09-RESEARCH.md` 的标准栈用 `pnpm add @floating-ui/dom` 更新 `package.json` 与 `pnpm-lock.yaml`。新增 `src/composables/usePopupAnchoring.ts`，封装 `computePosition()`、`autoUpdate()`、`offset(12)`、`flip({ padding: 16, fallbackAxisSideDirection: 'end' })`、`shift({ padding: 16 })`、`size({ padding: 16, apply(...) { maxWidth clamp 280-360px; maxHeight = availableHeight } })`，并返回 `floatingStyles`、`placement`、`collisionState`、`updatePosition()`、cleanup。随后新增 `src/components/map-popup/MapContextPopup.vue` 作为桌面壳层：根节点 class 固定为 `map-context-popup`，保留 `role="dialog"` 与 `aria-modal="false"`，渲染箭头元素 `map-context-popup__arrow`，内部直接消费 `PointSummaryCard`，并把焦点入口放到标题或首个按钮。新增 `src/components/map-popup/MapContextPopup.spec.ts`，黑盒断言 role、宽度约束 class/style、箭头存在和初始焦点行为。</action>
  <acceptance_criteria>
    - `package.json` 增加 `"@floating-ui/dom"` 依赖。
    - `src/composables/usePopupAnchoring.ts` 包含 `offset(12)`、`flip({`、`shift({ padding: 16 })`、`size({`。
    - `src/components/map-popup/MapContextPopup.vue` 包含 `class="map-context-popup"`、`role="dialog"`、`aria-modal="false"`、`map-context-popup__arrow`。
    - `src/components/map-popup/MapContextPopup.spec.ts` 对 role、arrow、焦点入口有断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/components/map-popup/MapContextPopup.spec.ts</automated>
  </verify>
  <done>桌面 popup 的定位和壳层已经独立存在，后续只需把真实 anchor 和 store state 接入世界地图舞台。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把桌面 anchored popup 接入 WorldMapStage 并锁住 anchor precedence</name>
  <read_first>
src/components/WorldMapStage.vue
src/components/WorldMapStage.spec.ts
src/components/SeedMarkerLayer.vue
src/stores/map-points.ts
src/composables/usePopupAnchoring.ts
src/components/map-popup/MapContextPopup.vue
.planning/phases/09-popup/09-CONTEXT.md
  </read_first>
  <files>src/components/SeedMarkerLayer.vue, src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts</files>
  <behavior>
    - Test 1: candidate-select、detected-preview、view 三类 summary state 在桌面端都会在 `.world-map-stage__surface` 内渲染 popup，而不是依赖 drawer。
    - Test 2: anchor precedence 固定为 `marker -> pending hit -> boundary center`，且 popup 根节点暴露 `data-popup-anchor-source`。
    - Test 3: popup 标题与当前 `selectedBoundaryId` / `activePoint.cityId` 一致；关闭或切换时不产生额外 remembered highlight。
  </behavior>
  <action>按 D-08、D-10 和 `09-RESEARCH.md` 的 Pattern 2 改造地图舞台。先在 `src/components/SeedMarkerLayer.vue` 给 marker 按钮增加 `:data-point-id="point.id"`，便于 `WorldMapStage` 查询实际 anchor 元素。然后在 `src/components/WorldMapStage.vue` 中新增 summary anchor 解析逻辑，优先顺序必须写死为：1) 当前 `summarySurfaceState` 对应点位的 marker button `[data-point-id="..."]`；2) pending 识别命中 marker `[data-pending-marker="true"]`；3) 当前 boundary 的视觉中心。桌面模式下，当 `summarySurfaceState` 存在且 `drawerMode === null` 时，在 `.world-map-stage__surface` 内渲染 `<MapContextPopup />`，并把 `confirmPendingCitySelection`、`continuePendingWithFallback`、`saveDraftAsPoint`、`openDrawerView`、`enterEditMode`、`toggleActivePointFeatured`、`deleteUserPoint`、`hideSeedPoint` 接到 popup 的动作出口。popup 根节点必须暴露 `data-popup-anchor-source="marker" | "pending" | "boundary"` 以便测试。更新 `src/components/WorldMapStage.spec.ts`，断言桌面 summary popup 出现于 map stage 内、anchor source 顺序、以及 popup 标题/边界高亮身份同步。</action>
  <acceptance_criteria>
    - `src/components/SeedMarkerLayer.vue` 包含 `data-point-id`。
    - `src/components/WorldMapStage.vue` 包含 `MapContextPopup`、`data-popup-anchor-source`，并显式查询 `[data-point-id=` 与 `[data-pending-marker="true"]`。
    - `src/components/WorldMapStage.spec.ts` 对 popup 是否出现在 `.world-map-stage__surface`、anchor source、boundary identity 同步有断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/components/WorldMapStage.spec.ts</automated>
  </verify>
  <done>桌面端 summary 主入口已经从边缘 drawer 迁到地图内 anchored popup，且 popup 与 boundary 高亮继续共享同一份 store 身份。</done>
</task>

</tasks>

<verification>
- `pnpm test -- src/components/map-popup/MapContextPopup.spec.ts src/components/WorldMapStage.spec.ts`
- `pnpm build`
</verification>

<success_criteria>
- `@floating-ui/dom` 已接入且锚点定位逻辑集中在 `usePopupAnchoring.ts`。
- `WorldMapStage` 在桌面端能直接渲染 anchored popup，并按 marker/pending/boundary 优先级选锚点。
- popup 快捷操作直接驱动既有 store action，不再回退到“先打开 drawer 才能操作”的旧模式。
</success_criteria>

<output>
After completion, create `.planning/phases/09-popup/09-popup-02-SUMMARY.md`
</output>

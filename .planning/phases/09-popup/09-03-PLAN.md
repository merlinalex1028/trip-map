---
phase: 09-popup
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - src/components/map-popup/MobilePeekSheet.vue
  - src/components/map-popup/MobilePeekSheet.spec.ts
  - src/components/WorldMapStage.vue
  - src/components/WorldMapStage.spec.ts
  - src/App.vue
  - src/App.spec.ts
autonomous: true
requirements: [POP-03, POP-02]
must_haves:
  truths:
    - "移动端或锚点不安全时，summary surface 会回退到底部 peek，而不是把 anchored popup 硬挤到边缘。"
    - "summary surface 不再导致 `App.vue` 为 drawer 预留右侧/底部空间；只有 deep drawer 打开时舞台才让位。"
    - "peek、popup、drawer 之间切换不会破坏 Phase 8 的边界高亮稳定性。"
  artifacts:
    - path: "src/components/map-popup/MobilePeekSheet.vue"
      provides: "移动端/unsafe fallback shell"
      contains: "关闭"
    - path: "src/components/WorldMapStage.vue"
      provides: "desktop popup / mobile peek 分流"
      contains: "shouldUsePeek"
    - path: "src/App.vue"
      provides: "summary-vs-drawer 布局分离"
      contains: "poster-shell__experience--drawer-open"
  key_links:
    - from: "src/components/WorldMapStage.vue"
      to: "src/components/map-popup/MobilePeekSheet.vue"
      via: "viewport + collision fallback"
      pattern: "shouldUsePeek"
    - from: "src/App.vue"
      to: "src/stores/map-points.ts"
      via: "drawer padding depends on drawerMode only"
      pattern: "drawerMode"
    - from: "src/components/WorldMapStage.spec.ts"
      to: "src/App.spec.ts"
      via: "peek fallback + handoff regressions"
      pattern: "peek|查看详情|selectedBoundaryId"
---

<objective>
补齐移动端和 unsafe 场景的底部 peek 回退，并把应用布局从“为 summary drawer 预留空间”切换到“只为 deep drawer 让位”，完成 popup 主舞台交互的收口。

Purpose: 落实 D-03、D-09、D-10 与 POP-03，同时把 Phase 8 的高亮稳定性保护到 popup/peek/drawer 三态切换里。
Output: `MobilePeekSheet`、带 fallback 判定的 `WorldMapStage`、只按 deep drawer 让位的 `App.vue` 与回归测试。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-popup/09-CONTEXT.md
@.planning/phases/09-popup/09-RESEARCH.md
@.planning/phases/09-popup/09-UI-SPEC.md
@.planning/phases/08-城市边界高亮语义/08-04-SUMMARY.md
@src/components/WorldMapStage.vue
@src/App.vue
@src/components/map-popup/MapContextPopup.vue
@src/components/map-popup/PointSummaryCard.vue

<interfaces>
From `09-UI-SPEC.md`:
```md
Desktop breakpoint: min-width 960px
Peek sizing: full width minus 16px side insets; max-height min(32rem, calc(100vh - 8.5rem))
Close affordance label remains `关闭` or `关闭面板`; do not use icon-only close on mobile peek.
```

From `src/composables/usePopupAnchoring.ts` after Plan 02:
```ts
const collisionState = ref<'safe' | 'unsafe'>(...)
const availableHeight = ref<number>(...)

return {
  floatingStyles,
  placement,
  collisionState,
  availableHeight,
  updatePosition,
}
```

From `src/App.vue` before this plan:
```ts
'poster-shell__experience--drawer-open': Boolean(activePoint) || drawerMode === 'candidate-select',
'poster-shell__experience--drawer-edit': drawerMode === 'edit'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 mobile peek fallback 并接入 WorldMapStage 分流</name>
  <read_first>
src/components/WorldMapStage.vue
src/components/WorldMapStage.spec.ts
src/components/map-popup/MapContextPopup.vue
src/components/map-popup/PointSummaryCard.vue
.planning/phases/09-popup/09-RESEARCH.md
.planning/phases/09-popup/09-UI-SPEC.md
  </read_first>
  <files>src/components/map-popup/MobilePeekSheet.vue, src/components/map-popup/MobilePeekSheet.spec.ts, src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts</files>
  <behavior>
    - Test 1: 当视口宽度 `< 960px` 时，summary surface 渲染 `MobilePeekSheet`，不渲染 desktop `MapContextPopup`。
    - Test 2: 当 collisionState 为 `unsafe` 或可用高度不足时，即使是桌面宽度也会切到底部 peek。
    - Test 3: peek 保留 `关闭` 按钮、safe-area padding 和换行动作区，并继续复用 `PointSummaryCard`。
  </behavior>
  <action>按 D-09 和 `09-UI-SPEC.md` 新增 `src/components/map-popup/MobilePeekSheet.vue`，根节点 class 固定为 `mobile-peek-sheet`，宽度规则固定为“左右各 16px inset”，底部 padding 固定为 `max(16px, env(safe-area-inset-bottom))`，最大高度固定为 `min(32rem, calc(100vh - 8.5rem))`，并提供可见文本按钮 `关闭`。在 `src/components/WorldMapStage.vue` 中增加 `shouldUsePeek` 条件，精确判断为 `viewportWidth < 960 || collisionState === 'unsafe' || availableHeight < 260`；当 `shouldUsePeek` 为真且 `summarySurfaceState` 存在、`drawerMode === null` 时渲染 `MobilePeekSheet`，否则继续渲染 `MapContextPopup`。保持所有 close/switch/reuse 行为都走既有 store action，不得在 peek shell 内新增 remembered highlight 状态。新增 `src/components/map-popup/MobilePeekSheet.spec.ts` 和 `src/components/WorldMapStage.spec.ts` 断言 mobile/unsafe fallback、safe-area class/style 与 close 行为。</action>
  <acceptance_criteria>
    - `src/components/map-popup/MobilePeekSheet.vue` 包含 `class="mobile-peek-sheet"`、`关闭`、`env(safe-area-inset-bottom)`。
    - `src/components/WorldMapStage.vue` 包含精确标识 `shouldUsePeek`，并比较 `viewportWidth < 960`、`collisionState === 'unsafe'`、`availableHeight < 260`。
    - `src/components/map-popup/MobilePeekSheet.spec.ts` 存在且断言 mobile peek 渲染。
    - `src/components/WorldMapStage.spec.ts` 对 `MapContextPopup` / `MobilePeekSheet` 二选一分流有断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/components/map-popup/MobilePeekSheet.spec.ts src/components/WorldMapStage.spec.ts</automated>
  </verify>
  <done>移动端和 unsafe 场景已经有安全的底部 peek 回退，summary 主入口不再因为视口边缘问题失去可读性或可点击性。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 App 布局切到 deep-drawer-only，并补 handoff/高亮回归</name>
  <read_first>
src/App.vue
src/App.spec.ts
src/stores/map-points.ts
src/components/WorldMapStage.vue
.planning/phases/08-城市边界高亮语义/08-04-SUMMARY.md
.planning/phases/09-popup/09-UI-SPEC.md
  </read_first>
  <files>src/App.vue, src/App.spec.ts, src/components/WorldMapStage.spec.ts</files>
  <behavior>
    - Test 1: 仅当 `drawerMode !== null` 时，`poster-shell__experience` 才带 `--drawer-open` / `--drawer-edit` 样式类。
    - Test 2: 仅处于 summary popup/peek 状态时，App 不再给 stage 预留右侧或底部 drawer 空间。
    - Test 3: 从 popup/peek 点击 `查看详情` 或 `编辑地点` 后，drawer 打开；关闭 drawer 后 summary surface 恢复，但 `selectedBoundaryId` 仍与当前点位一致。
  </behavior>
  <action>按 D-03 和 D-10 修改 `src/App.vue`：`poster-shell__experience--drawer-open` 与 `poster-shell__experience--drawer-edit` 只能依赖 `drawerMode !== null` 与 `drawerMode === 'edit'`，不能再因为 `activePoint`、`summaryMode` 或旧的 `candidate-select` 自动给舞台预留空间。保留 `PointPreviewDrawer` 组件，但其显示与 padding 只由 deep drawer handoff 触发。更新 `src/App.spec.ts`，覆盖“summary 状态不占 drawer 布局、点击 `查看详情` 后才出现 drawer padding、关闭 drawer 后 summary 恢复”的链路；同时在 `src/components/WorldMapStage.spec.ts` 增补 close/switch/reopen 回归，确保 Phase 8 的 `selectedBoundaryId` / saved weak highlights 在 popup、peek、drawer 之间切换时仍稳定。</action>
  <acceptance_criteria>
    - `src/App.vue` 中 `poster-shell__experience--drawer-open` 不再包含 `Boolean(activePoint)` 或 `candidate-select` 判断。
    - `src/App.spec.ts` 对 `poster-shell__experience--drawer-open` 的出现时机和 `查看详情` handoff 有断言。
    - `src/components/WorldMapStage.spec.ts` 对 `selectedBoundaryId` 在 close/switch/reopen 后的稳定性有断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/App.spec.ts src/components/WorldMapStage.spec.ts</automated>
  </verify>
  <done>应用布局已经完全从“drawer 主入口”切到“summary 主入口 + deep drawer 接力”，并且 popup/peek/drawer 的切换不会破坏边界高亮稳定性。</done>
</task>

</tasks>

<verification>
- `pnpm test -- src/components/map-popup/MobilePeekSheet.spec.ts src/components/WorldMapStage.spec.ts src/App.spec.ts`
- `pnpm build`
</verification>

<success_criteria>
- 视口 `< 960px` 或锚点不安全时，summary surface 会可靠回退到底部 peek。
- `App.vue` 只在 deep drawer 打开时为舞台让位，summary popup/peek 保持地图主舞台感。
- popup、peek、drawer 的 handoff 和 close/switch/reopen 回归都锁住了边界高亮一致性。
</success_criteria>

<output>
After completion, create `.planning/phases/09-popup/09-popup-03-SUMMARY.md`
</output>

---
phase: 09-popup
plan: 05
type: execute
wave: 5
depends_on:
  - 09-04
files_modified:
  - .planning/phases/09-popup/09-VALIDATION.md
  - src/components/map-popup/PointSummaryCard.vue
  - src/components/map-popup/PointSummaryCard.spec.ts
  - src/components/map-popup/MapContextPopup.vue
  - src/components/map-popup/MapContextPopup.spec.ts
  - src/components/map-popup/MobilePeekSheet.vue
  - src/components/map-popup/MobilePeekSheet.spec.ts
  - src/components/WorldMapStage.spec.ts
autonomous: true
requirements:
  - POP-01
  - POP-03
gap_closure: true
must_haves:
  truths:
    - "长内容场景下，popup / peek 只有中间简介或候选内容区滚动，头部身份信息和底部动作保持稳定可见。"
    - "popup / peek 仍然只是 summary surface，deep detail/edit handoff 不变。"
    - "桌面 anchored popup 与移动端 peek 的 shell 继续负责高度约束与定位，不再承担整卡滚动。"
  artifacts:
    - path: "src/components/map-popup/PointSummaryCard.vue"
      provides: "summary card 内部的滚动分区"
      contains: "point-summary-card__scroll-region"
    - path: "src/components/map-popup/PointSummaryCard.vue"
      provides: "稳定的底部动作区"
      contains: "point-summary-card__footer"
    - path: "src/components/map-popup/MapContextPopup.vue"
      provides: "shell 只负责裁切与高度约束"
      contains: "overflow: hidden"
    - path: "src/components/map-popup/MobilePeekSheet.vue"
      provides: "peek shell 只负责裁切与安全区"
      contains: "mobile-peek-sheet__body"
  key_links:
    - from: "src/components/map-popup/PointSummaryCard.vue"
      to: "src/components/PointPreviewDrawer.vue"
      via: "沿用已有 scroll-region 分区范式"
      pattern: "point-preview-drawer__scroll-region"
    - from: "src/components/map-popup/MapContextPopup.vue"
      to: "src/components/map-popup/PointSummaryCard.vue"
      via: "shell 提供 max-height，card 内部决定滚动边界"
      pattern: "PointSummaryCard"
---

<objective>
Close the remaining Phase 09 UAT gap by moving the scroll boundary from the popup / peek shell to the middle content region inside the shared summary card, so only the introduction/content area scrolls while header and action footer stay stable.

Purpose: 09-04 fixed “没有滚动” but still leaves the wrong scroll scope. The user expects the same interaction rhythm as the drawer-style partition: stable shell + middle scroll region, not whole-card scrolling.
Output: a partitioned `PointSummaryCard`, shell containers that only constrain height, and regressions proving long content scrolls in the middle region only.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-popup/09-CONTEXT.md
@.planning/phases/09-popup/09-UI-SPEC.md
@.planning/phases/09-popup/09-UAT.md
@.planning/debug/09-popup-middle-scroll.md
@.planning/phases/09-popup/09-04-PLAN.md
@.planning/phases/09-popup/09-popup-04-SUMMARY.md
@src/components/PointPreviewDrawer.vue
@src/components/map-popup/PointSummaryCard.vue
@src/components/map-popup/MapContextPopup.vue
@src/components/map-popup/MobilePeekSheet.vue

<interfaces>
From `src/components/map-popup/PointSummaryCard.vue`:
```vue
<article class="point-summary-card">
  <header class="point-summary-card__header">...</header>
  <div v-if="isCandidateMode" class="point-summary-card__section">...</div>
  <p v-else class="point-summary-card__description">...</p>
  <div class="point-summary-card__actions">...</div>
</article>
```

From `src/components/PointPreviewDrawer.vue`:
```vue
<div class="point-preview-drawer__content">
  <div class="point-preview-drawer__scroll-region" data-scroll-region="true">...</div>
</div>
<div class="point-preview-drawer__actions">...</div>
```

From `.planning/debug/09-popup-middle-scroll.md`:
```md
body 内包住了整个 PointSummaryCard，导致 header、描述、动作区作为一个整体一起滚动，而不是只有中间简介区滚动
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Partition PointSummaryCard into stable header/footer plus middle scroll region</name>
  <read_first>
src/components/map-popup/PointSummaryCard.vue
src/components/map-popup/PointSummaryCard.spec.ts
src/components/PointPreviewDrawer.vue
src/components/map-popup/MapContextPopup.vue
src/components/map-popup/MobilePeekSheet.vue
.planning/phases/09-popup/09-UAT.md
.planning/debug/09-popup-middle-scroll.md
  </read_first>
  <files>src/components/map-popup/PointSummaryCard.vue, src/components/map-popup/PointSummaryCard.spec.ts, src/components/map-popup/MapContextPopup.vue, src/components/map-popup/MapContextPopup.spec.ts, src/components/map-popup/MobilePeekSheet.vue, src/components/map-popup/MobilePeekSheet.spec.ts, src/components/WorldMapStage.spec.ts, .planning/phases/09-popup/09-VALIDATION.md</files>
  <behavior>
    - Test 1: 长描述 view 态中，popup / peek 的 badge、标题与底部动作保持稳定，中间简介区单独滚动。
    - Test 2: candidate-select 长候选列表或长提示文案时，只有候选内容区滚动，“按国家/地区继续记录”等底部动作不跟着整体滚走。
    - Test 3: shell 仍保留 dialog 语义、arrow / close affordance 和 safe-area / anchoring 约束，但 `overflow-y: auto` 不再定义整张卡片的滚动体验。
  </behavior>
  <action>重构 `src/components/map-popup/PointSummaryCard.vue`，将其拆成稳定布局分区：保留 header 承接 badge、标题、国家/上下文与 notice；新增中部 `point-summary-card__content` / `point-summary-card__scroll-region` 用于候选列表、描述文本和长通知；将 `point-summary-card__actions` 与 confirm row 收束到稳定 footer，例如 `point-summary-card__footer`。样式上需要给 card 本身 `min-height: 0`，并让 scroll-region 使用 `min-height: 0`、`overflow-y: auto`、必要的 `padding-inline-end`，从而让长内容只在中部滚动。同步收紧 `MapContextPopup.vue` 与 `MobilePeekSheet.vue`：shell body 继续保留高度约束、裁切与安全区，但不要再依赖 shell body 定义“整卡滚动”；如果需要，可把 body 改为简单的 `min-height: 0` 容器，由 `PointSummaryCard` 自己承担 scroll partition。补 `PointSummaryCard.spec.ts`、`MapContextPopup.spec.ts`、`MobilePeekSheet.spec.ts` 与 `WorldMapStage.spec.ts`，锁住中部 scroll-region 存在、动作区仍可见、以及长文本场景下不再表现为整卡滚动。最后把这条验证加入 `09-VALIDATION.md` 的任务映射。</action>
  <acceptance_criteria>
    - `src/components/map-popup/PointSummaryCard.vue` contains `point-summary-card__scroll-region`
    - `src/components/map-popup/PointSummaryCard.vue` contains `point-summary-card__footer`
    - `src/components/map-popup/PointSummaryCard.vue` contains `overflow-y: auto`
    - `src/components/map-popup/MapContextPopup.vue` contains `map-context-popup__body`
    - `src/components/map-popup/MobilePeekSheet.vue` contains `mobile-peek-sheet__body`
    - `src/components/map-popup/PointSummaryCard.spec.ts` contains `point-summary-card__scroll-region`
    - `src/components/map-popup/MapContextPopup.spec.ts` contains `point-summary-card__footer`
    - `src/components/map-popup/MobilePeekSheet.spec.ts` contains `point-summary-card__footer`
    - `src/components/WorldMapStage.spec.ts` contains `point-summary-card__scroll-region`
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts src/components/map-popup/MobilePeekSheet.spec.ts src/components/WorldMapStage.spec.ts</automated>
  </verify>
  <done>popup / peek 的滚动范围被正确收敛到中部内容区，头部身份信息和底部动作保持稳定，不再出现整张 popup 一起滚的体验。</done>
</task>

</tasks>

<verification>
- `pnpm test -- src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts src/components/map-popup/MobilePeekSheet.spec.ts src/components/WorldMapStage.spec.ts`
- `pnpm build`
- Confirm `.planning/phases/09-popup/09-UAT.md` test 6 is resolved as “only middle content scrolls”
</verification>

<success_criteria>
- 长内容时，滚动只发生在 `PointSummaryCard` 中部内容区
- popup / peek 头部身份信息与底部动作稳定可见
- Phase 09 的 popup-summary + drawer-deep 分工保持不变
</success_criteria>

<output>
After completion, create `.planning/phases/09-popup/09-popup-05-SUMMARY.md`
</output>

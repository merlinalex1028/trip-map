---
phase: 09-popup
plan: 04
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - src/components/map-popup/MapContextPopup.vue
  - src/components/map-popup/MapContextPopup.spec.ts
  - src/components/map-popup/MobilePeekSheet.vue
  - src/components/map-popup/MobilePeekSheet.spec.ts
  - src/components/WorldMapStage.spec.ts
autonomous: true
requirements:
  - POP-01
  - POP-03
gap_closure: true
must_haves:
  truths:
    - "当 popup 或 peek 中的描述文本、候选列表或通知内容超出当前可用高度时，内容区会出现真实可用的纵向滚动，底部动作仍然可触达。"
    - "桌面 anchored popup 继续保持地图内锚定、箭头与 dialog 语义；移动端/unsafe fallback 继续使用 peek。"
    - "Phase 09 既有契约保持不变：popup/peek 仍是 summary 主入口，drawer 继续承接 deep detail/edit。"
  artifacts:
    - path: "src/components/map-popup/MapContextPopup.vue"
      provides: "桌面 popup 的可滚动 body 容器"
      contains: "map-context-popup__body"
    - path: "src/components/map-popup/MobilePeekSheet.vue"
      provides: "移动端 peek 的可滚动 body 容器"
      contains: "mobile-peek-sheet__body"
    - path: "src/components/WorldMapStage.spec.ts"
      provides: "长内容场景下 popup / peek 仍可达的集成回归"
      contains: "long text"
  key_links:
    - from: "src/components/map-popup/MapContextPopup.vue"
      to: "src/components/map-popup/PointSummaryCard.vue"
      via: "scroll body 承载 summary card"
      pattern: "PointSummaryCard"
    - from: "src/components/map-popup/MobilePeekSheet.vue"
      to: "src/components/map-popup/PointSummaryCard.vue"
      via: "peek body 承载 summary card"
      pattern: "PointSummaryCard"
    - from: "src/components/WorldMapStage.spec.ts"
      to: "src/components/map-popup/MapContextPopup.spec.ts"
      via: "集成层验证 shell 仍在地图语境中工作"
      pattern: "overflow-y: auto"
---

<objective>
Close the real executable Phase 09 UAT gap by fixing long-content scrolling inside popup and peek shells without changing the established summary/deep handoff contract.

Purpose: UAT 暴露的是一个真实交互缺陷: `usePopupAnchoring` 已给 popup 写入 `maxHeight`，但 popup / peek shell 没有真正的可滚动 body，导致长内容被裁切却不能滚动。用户提出“所有 drawer 都改成 popup”的反馈属于新的产品决策，不在本次 gap closure 执行范围内。
Output: scroll-safe popup / peek shells, targeted regressions for long-content reachability, and no regression to the existing popup-summary + drawer-deep split.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-popup/09-CONTEXT.md
@.planning/phases/09-popup/09-UI-SPEC.md
@.planning/phases/09-popup/09-UAT.md
@.planning/phases/09-popup/09-VALIDATION.md
@.planning/phases/09-popup/09-popup-02-SUMMARY.md
@.planning/phases/09-popup/09-popup-03-SUMMARY.md
@src/components/map-popup/PointSummaryCard.vue
@src/components/map-popup/MapContextPopup.vue
@src/components/map-popup/MobilePeekSheet.vue
@src/components/WorldMapStage.vue

<interfaces>
From `src/components/map-popup/MapContextPopup.vue`:
```ts
const props = defineProps<{
  surface: SummarySurfaceState
  anchorSource: 'marker' | 'pending' | 'boundary'
  floatingStyles?: CSSProperties | null
}>()
```

From `src/components/map-popup/MobilePeekSheet.vue`:
```ts
const peekStyles = computed(() => ({
  '--mobile-peek-safe-bottom': 'max(16px, env(safe-area-inset-bottom))',
  '--mobile-peek-max-height': 'min(32rem, calc(100vh - 8.5rem))'
}))
```

From `09-UAT.md` gap 6:
```md
当 popup 或 peek 中的描述文本、候选列表或通知内容超过当前可用高度时，内容区应保持可滚动，用户仍能触达底部动作按钮，不会出现内容被裁切但无法滚动的情况。
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dedicated scroll bodies to popup and peek shells</name>
  <read_first>
src/components/map-popup/MapContextPopup.vue
src/components/map-popup/MapContextPopup.spec.ts
src/components/map-popup/MobilePeekSheet.vue
src/components/map-popup/MobilePeekSheet.spec.ts
src/components/map-popup/PointSummaryCard.vue
src/components/WorldMapStage.vue
.planning/phases/09-popup/09-UAT.md
.planning/phases/09-popup/09-UI-SPEC.md
  </read_first>
  <files>src/components/map-popup/MapContextPopup.vue, src/components/map-popup/MapContextPopup.spec.ts, src/components/map-popup/MobilePeekSheet.vue, src/components/map-popup/MobilePeekSheet.spec.ts, src/components/WorldMapStage.spec.ts</files>
  <behavior>
    - Test 1: 桌面 anchored popup 在长描述、长候选列表或通知内容场景下，body 区域具备纵向滚动，底部动作仍然可达。
    - Test 2: 移动端/unsafe peek 在内容超出 `min(32rem, calc(100vh - 8.5rem))` 时，内部 body 具备纵向滚动，不再被地图 surface 简单裁切。
    - Test 3: popup 的箭头、dialog 语义与自动聚焦行为不回退；peek 的关闭按钮与 safe-area 约束继续保留。
  </behavior>
  <action>在 `src/components/map-popup/MapContextPopup.vue` 中新增独立的 `map-context-popup__body` 容器，让 popup shell 自身转为 `display: flex` / `flex-direction: column` / `min-height: 0` / `overflow: hidden`，并把 `overflow-y: auto`、`overscroll-behavior: contain`、`min-height: 0` 放到 body 容器上，使 `usePopupAnchoring.ts` 写入的 `maxHeight` 能实际转化为内部滚动区域。对 `src/components/map-popup/MobilePeekSheet.vue` 做等价处理：保留顶部 `关闭` 控件，新增 `mobile-peek-sheet__body` 作为滚动区，并把底部 safe-area padding 放在可滚动区域内，确保底部动作按钮可滚动到。补充 `src/components/map-popup/MapContextPopup.spec.ts` 与 `src/components/map-popup/MobilePeekSheet.spec.ts`，通过长文本夹具与 body 容器断言锁住 shell 结构；在 `src/components/WorldMapStage.spec.ts` 中加入至少一个长内容集成回归，证明 popup / peek 仍然出现在地图语境中且 body 容器存在。不要改动 drawer handoff 语义，不要移除 `PointPreviewDrawer` 主链路。</action>
  <acceptance_criteria>
    - `src/components/map-popup/MapContextPopup.vue` contains `map-context-popup__body`
    - `src/components/map-popup/MapContextPopup.vue` contains `overflow-y: auto`
    - `src/components/map-popup/MobilePeekSheet.vue` contains `mobile-peek-sheet__body`
    - `src/components/map-popup/MobilePeekSheet.vue` contains `overflow-y: auto`
    - `src/components/map-popup/MobilePeekSheet.vue` contains `min-height: 0`
    - `src/components/map-popup/MapContextPopup.spec.ts` contains `map-context-popup__body`
    - `src/components/map-popup/MobilePeekSheet.spec.ts` contains `mobile-peek-sheet__body`
    - `src/components/WorldMapStage.spec.ts` contains `long text`
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/components/map-popup/MapContextPopup.spec.ts src/components/map-popup/MobilePeekSheet.spec.ts src/components/WorldMapStage.spec.ts</automated>
  </verify>
  <done>长内容不会再因为 popup / peek 外层只有 `maxHeight` 而被静态裁切，用户可以在当前地图上下文中滚动并触达底部动作。</done>
</task>

</tasks>

<verification>
- `pnpm test -- src/components/map-popup/MapContextPopup.spec.ts src/components/map-popup/MobilePeekSheet.spec.ts src/components/WorldMapStage.spec.ts`
- `pnpm build`
- Confirm `.planning/phases/09-popup/09-UAT.md` gap 6 is addressed and no regression is introduced to the popup-summary + drawer-deep contract
</verification>

<success_criteria>
- 桌面 popup 与移动端 peek 都具备真实可用的内部滚动区域
- 长内容场景下底部动作仍可触达，不再出现“被裁切但无法滚动”的失败体验
- summary popup / peek 与 deep drawer 的既有分工保持不变
</success_criteria>

<output>
After completion, create `.planning/phases/09-popup/09-popup-04-SUMMARY.md`
</output>

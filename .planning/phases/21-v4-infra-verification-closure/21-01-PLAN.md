---
phase: 21-v4-infra-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-tailwind-token/19-VALIDATION.md
autonomous: true
gap_closure: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - STYLE-01
  - STYLE-02
must_haves:
  truths:
    - "`19-VALIDATION.md` 不再是 `draft`，并与 2026-04-09 已完成的自动化门禁和浏览器冒烟证据一致。"
    - "Phase 19 的 Wave 0 / task verification map / sign-off 都指向真实存在的 spec、命令和人工审批记录。"
    - "重新审计时，`INFRA-04` 不会再因为 `19-VALIDATION.md` 仍为草稿或 `wave_0_complete:false` 而被卡住。"
  artifacts:
    - path: ".planning/phases/19-tailwind-token/19-VALIDATION.md"
      provides: "Approved validation artifact aligned to actual Phase 19 evidence"
  key_links:
    - from: ".planning/phases/19-tailwind-token/19-VALIDATION.md"
      to: ".planning/phases/19-tailwind-token/19-03-SUMMARY.md"
      via: "manual verification record"
      pattern: "approved|Leaflet|127.0.0.1:4173"
    - from: ".planning/phases/19-tailwind-token/19-VALIDATION.md"
      to: "apps/web/src/tailwind-token.spec.ts"
      via: "verification map and automated evidence"
      pattern: "tailwind-token.spec.ts"
    - from: ".planning/phases/19-tailwind-token/19-VALIDATION.md"
      to: "apps/web/src/App.spec.ts"
      via: "verification map and automated evidence"
      pattern: "App.spec.ts"
---

<objective>
把 Phase 19 的 validation 证据文档从陈旧草稿刷新为可审计状态，只回填已发生的证据，不重新打开实现范围。

Purpose: v4.0 审计已经确认代码集成通过，当前 blocker 是 `19-VALIDATION.md` 仍停留在草稿态且未完整承接 2026-04-09 的真实 evidence。
Output: 与 `19-01/02/03-SUMMARY.md`、milestone audit 一致的 `.planning/phases/19-tailwind-token/19-VALIDATION.md`。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v4.0-v4.0-MILESTONE-AUDIT.md
@.planning/phases/19-tailwind-token/19-VALIDATION.md
@.planning/phases/19-tailwind-token/19-01-SUMMARY.md
@.planning/phases/19-tailwind-token/19-02-SUMMARY.md
@.planning/phases/19-tailwind-token/19-03-SUMMARY.md
@.planning/phases/20-kawaii/20-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把 Phase 19 validation frontmatter、Wave 0 与 task map 刷新为真实完成状态</name>
  <read_first>
.planning/phases/19-tailwind-token/19-VALIDATION.md
.planning/v4.0-v4.0-MILESTONE-AUDIT.md
.planning/phases/19-tailwind-token/19-01-SUMMARY.md
.planning/phases/19-tailwind-token/19-02-SUMMARY.md
.planning/phases/19-tailwind-token/19-03-SUMMARY.md
.planning/phases/20-kawaii/20-VALIDATION.md
  </read_first>
  <files>.planning/phases/19-tailwind-token/19-VALIDATION.md</files>
  <action>仅基于现有 Phase 19 证据重写 `19-VALIDATION.md` 的状态层，不新增未发生的验证。把 frontmatter 明确改成 `status: approved`、`wave_0_complete: true`，并补上 `updated: 2026-04-09`。随后重写 `Per-Task Verification Map` 与 `Wave 0 Requirements`：保留且只保留真实 task 行 `19-01-01`、`19-02-01`、`19-02-02`、`19-03-01`、`19-03-02`，把它们的 requirement 映射固定为 `INFRA-01`、`INFRA-02/INFRA-03/STYLE-01/STYLE-02`、`STYLE-01/STYLE-02`、`INFRA-04`、`INFRA-04`，并把 `File Exists` / `Status` 全部翻成完成态。删除所有与草稿态一致的占位符，包括 `⬜ pending`、`❌ created in 19-01`、`❌ summary created in 19-03`、`wave_0_complete:false` 这类陈旧内容，不要保留任何暗示“还没闭环”的旧字符串。</action>
  <acceptance_criteria>
    - `.planning/phases/19-tailwind-token/19-VALIDATION.md` 包含精确字符串 `status: approved`。
    - `.planning/phases/19-tailwind-token/19-VALIDATION.md` 包含精确字符串 `wave_0_complete: true`。
    - `.planning/phases/19-tailwind-token/19-VALIDATION.md` 包含精确字符串 `updated: 2026-04-09`。
    - `.planning/phases/19-tailwind-token/19-VALIDATION.md` 同时包含 `19-01-01`、`19-02-01`、`19-02-02`、`19-03-01`、`19-03-02` 五个 task id。
    - `rg -n "⬜ pending|❌ created in 19-01|❌ summary created in 19-03|wave_0_complete: false" .planning/phases/19-tailwind-token/19-VALIDATION.md` 返回空结果。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^status: approved$" .planning/phases/19-tailwind-token/19-VALIDATION.md && rg -n "^wave_0_complete: true$" .planning/phases/19-tailwind-token/19-VALIDATION.md && rg -n "^updated: 2026-04-09$" .planning/phases/19-tailwind-token/19-VALIDATION.md && rg -n "19-01-01|19-02-01|19-02-02|19-03-01|19-03-02" .planning/phases/19-tailwind-token/19-VALIDATION.md && ! rg -n "⬜ pending|❌ created in 19-01|❌ summary created in 19-03|wave_0_complete: false" .planning/phases/19-tailwind-token/19-VALIDATION.md</automated>
  </verify>
  <done>`19-VALIDATION.md` 的 frontmatter、Wave 0 和 task map 已完全对齐到 2026-04-09 的真实 Phase 19 结果，不再保留 draft 占位。</done>
</task>

<task type="auto">
  <name>Task 2: 回填 Automated Evidence、Manual Verification Record 与最终 sign-off</name>
  <read_first>
.planning/phases/19-tailwind-token/19-VALIDATION.md
.planning/phases/19-tailwind-token/19-03-SUMMARY.md
.planning/v4.0-v4.0-MILESTONE-AUDIT.md
.planning/phases/20-kawaii/20-VALIDATION.md
  </read_first>
  <files>.planning/phases/19-tailwind-token/19-VALIDATION.md</files>
  <action>在同一份 `19-VALIDATION.md` 中补齐最终 evidence 段落，格式参照已完成的 `20-VALIDATION.md`，但内容严格使用 Phase 19 已存在证据。新增 `## Automated Evidence`，至少写入 2026-04-09 的三条命令结果：`pnpm --filter @trip-map/web exec vitest run src/App.spec.ts src/tailwind-token.spec.ts`、`pnpm --filter @trip-map/web exec vue-tsc --noEmit`、`pnpm --filter @trip-map/web build`，结果统一记为 pass。把人工部分改成显式的 `Manual Verification Record` 或等效段落，必须写出 `Environment: pnpm --filter @trip-map/web dev --host 127.0.0.1 --port 4173`、`URL: http://127.0.0.1:4173/`、`Initial result: failed`、`Final result: approved`、`Approved at: 2026-04-09`，并在 `Validation Sign-Off` 处把最终审批写成 `Approval: approved (2026-04-09)`。不要发明截图、日志或新的浏览器结论；只沉淀 `19-03-SUMMARY.md` 已记录的审批链。</action>
  <acceptance_criteria>
    - `.planning/phases/19-tailwind-token/19-VALIDATION.md` 包含标题 `## Automated Evidence`。
    - `.planning/phases/19-tailwind-token/19-VALIDATION.md` 同时包含 `pnpm --filter @trip-map/web exec vitest run src/App.spec.ts src/tailwind-token.spec.ts`、`pnpm --filter @trip-map/web exec vue-tsc --noEmit`、`pnpm --filter @trip-map/web build`。
    - `.planning/phases/19-tailwind-token/19-VALIDATION.md` 包含 `Environment: pnpm --filter @trip-map/web dev --host 127.0.0.1 --port 4173`。
    - `.planning/phases/19-tailwind-token/19-VALIDATION.md` 包含 `Final result: approved`。
    - `.planning/phases/19-tailwind-token/19-VALIDATION.md` 包含 `Approval: approved (2026-04-09)`。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^## Automated Evidence$" .planning/phases/19-tailwind-token/19-VALIDATION.md && rg -n "pnpm --filter @trip-map/web exec vitest run src/App.spec.ts src/tailwind-token.spec.ts" .planning/phases/19-tailwind-token/19-VALIDATION.md && rg -n "pnpm --filter @trip-map/web exec vue-tsc --noEmit" .planning/phases/19-tailwind-token/19-VALIDATION.md && rg -n "pnpm --filter @trip-map/web build" .planning/phases/19-tailwind-token/19-VALIDATION.md && rg -n "Environment: pnpm --filter @trip-map/web dev --host 127.0.0.1 --port 4173" .planning/phases/19-tailwind-token/19-VALIDATION.md && rg -n "Final result: approved" .planning/phases/19-tailwind-token/19-VALIDATION.md && rg -n "Approval: approved \\(2026-04-09\\)" .planning/phases/19-tailwind-token/19-VALIDATION.md</automated>
  </verify>
  <done>`19-VALIDATION.md` 已具备自动化证据、人工审批记录和最终 sign-off，能直接作为 Phase 19 verification 的上游来源。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing summary evidence → validation artifact | 若把 summary 中的真实结论漏写或误写进 validation，后续审计会继续基于错误状态判断 |
| human approval record → audit-consumable document | 若人工验收结果没有显式落盘，`INFRA-04` 仍会在 re-audit 中缺证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | T | `.planning/phases/19-tailwind-token/19-VALIDATION.md` | mitigate | 只允许从 `19-01/02/03-SUMMARY.md` 和 audit 已存在证据回填，不允许创造新的命令结果或人工结论。 |
| T-21-02 | R | Phase 19 manual smoke evidence | mitigate | 将 `Environment`、`URL`、`Initial result`、`Final result`、`Approved at` 精确写入 validation，避免“口头通过”无审计来源。 |
</threat_model>

<verification>
通过 `rg` 精确校验 `19-VALIDATION.md` 中的 frontmatter、task map、自动化命令、人工审批字符串和陈旧占位删除情况。
</verification>

<success_criteria>
- `19-VALIDATION.md` 从 `draft` 升级为 `approved`，且 `wave_0_complete: true`。
- 文档中存在 2026-04-09 的自动化与人工证据，不再依赖外推。
- Phase 19 的 validation 不再被 milestone audit 判定为 stale / partial。
</success_criteria>

<output>
After completion, create `.planning/phases/21-v4-infra-verification-closure/21-01-SUMMARY.md`
</output>

---
phase: 04-可用性打磨与增强能力
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SeedMarkerLayer.vue
  - src/components/SeedMarkerLayer.spec.ts
  - src/components/PointPreviewDrawer.vue
  - src/components/PointPreviewDrawer.spec.ts
  - src/App.vue
  - src/App.spec.ts
autonomous: true
requirements:
  - PNT-04
  - DRW-04
  - UX-01
  - UX-02
user_setup: []
must_haves:
  truths:
    - Selected points, draft points, and ordinary highlighted points are visually distinct without overpowering the poster-style map.
    - Drawer open, close, keyboard focus, and long-text behavior remain stable on both desktop and mobile layouts.
    - Keyboard users can clearly perceive focus state on map markers and stay inside the drawer interaction loop until it is closed.
  artifacts:
    - updated marker hierarchy and accessible label behavior
    - drawer focus-trap and close-behavior implementation
    - regression tests for focus, Esc close, and long-text handling
  key_links:
    - SeedMarkerLayer exposes stronger selected/draft semantics while keeping saved points visible in the background
    - PointPreviewDrawer owns focus trap, initial focus target, and scroll partitioning for long content
    - App shell layout still keeps the map as the main visual anchor even when the drawer is open
---

<objective>
Harden the map interaction layer so marker hierarchy, keyboard focus, drawer closing, and long-text layouts feel reliable instead of merely functional.

Purpose: Phase 4 starts by making the existing UI trustworthy for real use, especially for keyboard users and mobile reading/editing flows.
Output: Distinct marker states, accessible marker semantics, a stable drawer focus loop, and long-text/mobile layouts that do not break the map-first experience.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@PRD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-可用性打磨与增强能力/04-CONTEXT.md
@.planning/phases/04-可用性打磨与增强能力/04-RESEARCH.md
@.planning/phases/04-可用性打磨与增强能力/04-VALIDATION.md
@.planning/phases/04-可用性打磨与增强能力/04-UI-SPEC.md
@src/components/SeedMarkerLayer.vue
@src/components/PointPreviewDrawer.vue
@src/App.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strengthen marker hierarchy, keyboard semantics, and label behavior on the map layer</name>
  <files>src/components/SeedMarkerLayer.vue, src/components/SeedMarkerLayer.spec.ts</files>
  <read_first>src/components/SeedMarkerLayer.vue, src/stores/map-points.ts, src/types/map-point.ts, .planning/phases/04-可用性打磨与增强能力/04-CONTEXT.md, .planning/phases/04-可用性打磨与增强能力/04-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: Saved points remain clearly visible at medium intensity, while a selected point gains a stronger ring/glow and non-selected points step back slightly.
    - Test 2: Draft points render as warmer temporary points with motion and expose an accessible "未保存地点" semantic in their label text.
    - Test 3: Hover, keyboard focus, and selected state each reveal the point label without forcing all labels to remain permanently visible.
  </behavior>
  <action>Update `src/components/SeedMarkerLayer.vue` so marker buttons expose exact accessibility and hierarchy semantics instead of only visual classes. Add an aria-label builder that includes `point.name`、`point.countryName`、`point.coordinatesLabel`, and for `source === 'detected'` append the exact phrase `未保存地点`. Keep buttons as the keyboard trigger surface and rely on native `Enter` activation. When `selectedPointId` is set, apply a dimmed visual treatment to all non-selected markers while preserving their visibility; selected markers must gain a stronger outer ring and glow, and draft markers must remain warmer and more active than saved markers. Change label visibility so labels appear for selected markers, hover/focus-visible markers, and any explicitly featured seed markers, but not for every saved point at rest. Create `src/components/SeedMarkerLayer.spec.ts` to assert the full aria-label contract, dimming/selected classes, and focus-triggered label visibility.</action>
  <verify>test -f src/components/SeedMarkerLayer.spec.ts && rg '未保存地点|aria-label' src/components/SeedMarkerLayer.vue && rg 'selected-point-id|focus-visible|label' src/components/SeedMarkerLayer.spec.ts && pnpm test -- src/components/SeedMarkerLayer.spec.ts</verify>
  <acceptance_criteria>
    - src/components/SeedMarkerLayer.vue contains `未保存地点`
    - src/components/SeedMarkerLayer.vue contains `aria-label`
    - src/components/SeedMarkerLayer.vue contains `seed-marker--selected`
    - src/components/SeedMarkerLayer.spec.ts contains `focus-visible`
    - src/components/SeedMarkerLayer.spec.ts contains `coordinatesLabel`
  </acceptance_criteria>
  <done>The map layer now makes selected, draft, and ordinary points legible to both pointer and keyboard users without flooding the map with labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add drawer focus management, explicit close behavior, and long-text/mobile layout hardening</name>
  <files>src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/App.vue, src/App.spec.ts</files>
  <read_first>src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/App.vue, src/App.spec.ts, src/stores/map-points.ts, .planning/phases/04-可用性打磨与增强能力/04-CONTEXT.md, .planning/phases/04-可用性打磨与增强能力/04-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: Opening the drawer moves focus to the drawer title or container and traps `Tab` navigation inside the drawer until close.
    - Test 2: Pressing `Escape` closes the drawer immediately when there are no unsaved edits, but still confirms before losing unsaved changes.
    - Test 3: Long descriptions scroll within a dedicated content region while the title and action area stay stable; mobile edit mode keeps actions fixed at the bottom.
  </behavior>
  <action>Refactor `src/components/PointPreviewDrawer.vue` into an explicitly accessible dialog-like panel. Add `role=\"dialog\"`, an accessible label/labelledby relationship, and a focus target on the drawer title or panel container so opening the drawer sends focus there immediately. Implement a minimal focus trap that cycles `Tab` and `Shift+Tab` within the close button, body controls, and action buttons while the drawer is open. Keep the existing unsaved-close confirmation string `你有未保存的更改，确定关闭吗？`, but ensure `Escape` bypasses confirmation only when there are no unsaved changes. Strengthen the close control so it remains a clearly visible button, not low-contrast utility text. Split the drawer layout into a stable header/body/actions structure: the description or long content area must scroll independently, while the action area stays stable and mobile edit mode uses a fixed/sticky bottom action region. Update `src/App.vue` layout hooks only as needed to keep the map as the main visual anchor while the drawer is open. Extend `src/components/PointPreviewDrawer.spec.ts` and `src/App.spec.ts` to cover initial focus placement, focus trap behavior, immediate `Escape` close without dirty state, guarded `Escape` with dirty state, and long-text scroll-region rendering.</action>
  <verify>rg 'role=\"dialog\"|你有未保存的更改，确定关闭吗？|Escape' src/components/PointPreviewDrawer.vue && rg 'focus trap|Escape|long text|dialog' src/components/PointPreviewDrawer.spec.ts src/App.spec.ts && pnpm test -- src/components/PointPreviewDrawer.spec.ts src/App.spec.ts</verify>
  <acceptance_criteria>
    - src/components/PointPreviewDrawer.vue contains `role="dialog"`
    - src/components/PointPreviewDrawer.vue contains `你有未保存的更改，确定关闭吗？`
    - src/components/PointPreviewDrawer.vue contains `keydown`
    - src/components/PointPreviewDrawer.spec.ts contains `Escape`
    - src/components/PointPreviewDrawer.spec.ts contains `focus trap`
    - src/App.spec.ts contains `long text`
  </acceptance_criteria>
  <done>The drawer now behaves like a reliable interaction surface for keyboard, mobile, and long-content scenarios instead of a fragile floating panel.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test -- src/components/SeedMarkerLayer.spec.ts
- [ ] pnpm test -- src/components/PointPreviewDrawer.spec.ts src/App.spec.ts
- [ ] rg '未保存地点' src/components/SeedMarkerLayer.vue
- [ ] rg 'role="dialog"' src/components/PointPreviewDrawer.vue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can clearly distinguish selected, draft, and ordinary markers
- Drawer close, focus, and long-text behavior stay stable across desktop and mobile
- Keyboard users can activate markers and stay oriented while the drawer is open

</success_criteria>

<output>
After completion, create `.planning/phases/04-可用性打磨与增强能力/04-01-SUMMARY.md`
</output>

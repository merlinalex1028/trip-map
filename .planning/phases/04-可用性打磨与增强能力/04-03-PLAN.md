---
phase: 04-可用性打磨与增强能力
plan: 03
type: execute
wave: 3
depends_on: []
gap_closure: true
files_modified:
  - src/data/geo/city-candidates.ts
  - src/services/geo-lookup.ts
  - src/services/geo-lookup.spec.ts
  - src/components/WorldMapStage.spec.ts
  - .planning/phases/04-可用性打磨与增强能力/04-UAT.md
autonomous: true
requirements:
  - GEO-04
  - UX-03
user_setup: []
must_haves:
  truths:
    - High-confidence city enrichment is realistically triggerable at the current full-world map scale instead of requiring near-pixel-perfect clicks.
    - Country-level fallback remains reliable when a user clicks near, but not precisely on, a city candidate.
    - Regression tests represent user-operable click tolerances for city enrichment and fallback behavior.
  artifacts:
    - scale-aware city hit tolerance in geo lookup
    - regression coverage for city-near clicks and fallback behavior
    - updated Phase 4 UAT gap resolution notes
  key_links:
    - city candidate thresholds match the interaction scale of the poster map
    - geo-lookup and WorldMapStage tests cover the UAT-reported “hard to hit city” regression
---

<objective>
Close the Phase 4 UAT gaps where city enhancement exists in code but is too hard to trigger in real use.

Purpose: The current city-enrichment layer is technically functional but tuned to real-world city radii that are far too small for a non-zoomable full-world poster map. Users need a scale-aware interaction tolerance so they can intentionally hit city enhancement and country fallback paths.
Output: Easier-to-trigger city matching, regression tests that reflect real click tolerance, and updated UAT gap documentation.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-可用性打磨与增强能力/04-UAT.md
@.planning/debug/04-city-hit-radius-too-small.md
@src/data/geo/city-candidates.ts
@src/services/geo-lookup.ts
@src/services/geo-lookup.spec.ts
@src/components/WorldMapStage.spec.ts
@src/services/map-projection.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make city enrichment tolerances match the world-map interaction scale</name>
  <files>src/data/geo/city-candidates.ts, src/services/geo-lookup.ts, src/services/geo-lookup.spec.ts</files>
  <behavior>
    - Test 1: Clicking near Tokyo or Kyoto within a realistic hand-controlled tolerance still yields a `city-high` or intentional city enhancement result.
    - Test 2: Clicking in the same country but outside the city-high tolerance still returns a valid country/region result with the exact fallback copy `未识别到更精确城市，已回退到国家/地区`.
    - Test 3: Existing country/region recognition for unrelated areas remains unchanged.
  </behavior>
  <action>Adjust the city candidate matching strategy so it is no longer based only on ultra-small real-world radii. You may increase `highRadiusKm` / `possibleRadiusKm`, derive a minimum tolerance from the current map projection scale, or combine both approaches, but keep the behavior conservative enough that cities do not swallow whole countries. Update `geo-lookup.spec.ts` with near-city samples that represent user-operable click tolerance instead of only perfect center hits.</action>
  <verify>rg 'highRadiusKm|possibleRadiusKm|未识别到更精确城市，已回退到国家/地区' src/data/geo/city-candidates.ts src/services/geo-lookup.ts && pnpm test -- src/services/geo-lookup.spec.ts</verify>
  <done>Users can intentionally trigger city enhancement without needing near-pixel-perfect clicks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add interaction-level regression coverage and update UAT resolution notes</name>
  <files>src/components/WorldMapStage.spec.ts, .planning/phases/04-可用性打磨与增强能力/04-UAT.md</files>
  <behavior>
    - Test 1: A realistic click near a city candidate produces the expected city-enhanced draft flow.
    - Test 2: A realistic click near-but-not-on the candidate produces the country fallback flow instead of failing to create a point.
    - Test 3: Phase 4 UAT gaps record the implemented resolution so the next verify run has historical context.
  </behavior>
  <action>Extend `WorldMapStage.spec.ts` to represent the real click path for city-near interactions and fallback interactions at the current map scale. After implementation, update the two diagnosed gaps in `04-UAT.md` with concrete resolution notes describing how the tolerance issue was fixed and what the new regression coverage protects.</action>
  <verify>pnpm test -- src/components/WorldMapStage.spec.ts && rg 'resolution:' .planning/phases/04-可用性打磨与增强能力/04-UAT.md</verify>
  <done>The original UAT complaints are captured by interaction-level tests and documented as resolved once fixed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test -- src/services/geo-lookup.spec.ts src/components/WorldMapStage.spec.ts
- [ ] pnpm test
- [ ] pnpm build
- [ ] realistic near-city clicks are covered by regression tests
- [ ] `04-UAT.md` includes resolution notes for both diagnosed gaps
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- City enhancement is meaningfully triggerable for users at the current poster-map scale
- Country fallback remains trustworthy when city precision is unavailable
- Phase 4 UAT history explicitly records how the issue was resolved

</success_criteria>

<output>
After completion, create `.planning/phases/04-可用性打磨与增强能力/04-03-SUMMARY.md`
</output>

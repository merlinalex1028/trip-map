---
phase: 04-可用性打磨与增强能力
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/data/geo/city-candidates.ts
  - src/types/geo.ts
  - src/types/map-point.ts
  - src/services/geo-lookup.ts
  - src/services/geo-lookup.spec.ts
  - src/services/point-storage.ts
  - src/services/point-storage.spec.ts
  - src/stores/map-points.ts
  - src/stores/map-points.spec.ts
  - src/components/WorldMapStage.vue
  - src/components/WorldMapStage.spec.ts
  - src/components/PointPreviewDrawer.vue
  - src/App.vue
  - src/App.spec.ts
autonomous: true
requirements:
  - GEO-04
  - DAT-04
  - UX-03
user_setup: []
must_haves:
  truths:
    - City-level enhancement can enrich a point when confidence is high, but failure to find a city never blocks country/region creation.
    - Corrupt, empty, or incompatible local snapshots still fall back safely without breaking the app or losing the default seed experience.
    - Edge clicks and degradation messages remain explicit and user-comprehensible instead of silently mutating or dropping state.
  artifacts:
    - optional city candidate lookup layer and expanded geo/map-point types
    - persistence support for high-confidence city metadata
    - fallback messaging and regression tests for country-first degradation
  key_links:
    - geo-lookup enriches detections with optional city metadata without weakening the existing country/region confidence gate
    - point-storage persists new city fields while preserving the current corrupt/empty recovery contract
    - drawer and stage UI present exact fallback copy when city precision is unavailable but country/region precision is valid
---

<objective>
Finish the degradation and enhancement layer so the app explains failures better, safely extends to city-level enrichment, and keeps country/region creation trustworthy.

Purpose: Phase 4 must improve reliability without breaking the Phase 2-3 core promise that valid land clicks still create meaningful travel points.
Output: Optional city enrichment metadata, safe persistence for the richer point shape, clearer degradation copy, and edge-click behavior that stays inside the map contract.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@PRD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-可用性打磨与增强能力/04-CONTEXT.md
@.planning/phases/04-可用性打磨与增强能力/04-RESEARCH.md
@.planning/phases/04-可用性打磨与增强能力/04-VALIDATION.md
@.planning/phases/04-可用性打磨与增强能力/04-UI-SPEC.md
@src/services/geo-lookup.ts
@src/services/point-storage.ts
@src/stores/map-points.ts
@src/components/WorldMapStage.vue
@src/components/PointPreviewDrawer.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add conservative city-enrichment metadata without weakening country-first detection</name>
  <files>src/data/geo/city-candidates.ts, src/types/geo.ts, src/types/map-point.ts, src/services/geo-lookup.ts, src/services/geo-lookup.spec.ts, src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts</files>
  <read_first>src/services/geo-lookup.ts, src/services/geo-lookup.spec.ts, src/components/WorldMapStage.vue, src/types/geo.ts, src/types/map-point.ts, .planning/phases/04-可用性打磨与增强能力/04-CONTEXT.md, .planning/phases/04-可用性打磨与增强能力/04-RESEARCH.md, .planning/phases/04-可用性打磨与增强能力/04-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: A high-confidence city candidate enriches the result so the point can use city title + country/region subtitle.
    - Test 2: If no reliable city candidate is found, the existing country/region result still succeeds and emits the exact fallback copy `未识别到更精确城市，已回退到国家/地区`.
    - Test 3: Edge clicks that already pass the current country/region confidence gate still render inside the map container instead of overflowing or being dropped only for visual reasons.
  </behavior>
  <action>Create `src/data/geo/city-candidates.ts` as a lightweight local candidate table keyed by country/region context, storing exact fields `name`, `countryCode`, `lat`, `lng`, and a conservative radius or precision marker that lets the lookup layer distinguish `city-high` from `city-possible`. Expand `src/types/geo.ts` and `src/types/map-point.ts` so detection and display models can carry `precision`, `cityName`, and `fallbackNotice` fields with exact allowed values `country`, `region`, `city-high`, and `city-possible`. Update `src/services/geo-lookup.ts` to enrich an existing country/region detection with optional city metadata only when the city candidate is reliable; if not reliable, keep the existing country/region detection and set the fallback copy to the exact string `未识别到更精确城市，已回退到国家/地区`. Update `src/components/WorldMapStage.vue` so new draft points carry the richer precision metadata while still using the existing valid-land flow; do not block point creation when city enrichment fails. Extend `src/services/geo-lookup.spec.ts` and `src/components/WorldMapStage.spec.ts` to cover both the high-confidence city path and the country/region fallback path, including an edge-position rendering assertion that the draft marker still clamps within the map surface.</action>
  <verify>test -f src/data/geo/city-candidates.ts && rg 'city-high|city-possible|未识别到更精确城市，已回退到国家/地区' src/types/geo.ts src/types/map-point.ts src/services/geo-lookup.ts src/components/WorldMapStage.vue && pnpm test -- src/services/geo-lookup.spec.ts src/components/WorldMapStage.spec.ts</verify>
  <acceptance_criteria>
    - src/data/geo/city-candidates.ts contains `countryCode`
    - src/types/geo.ts contains `city-high`
    - src/types/map-point.ts contains `fallbackNotice`
    - src/services/geo-lookup.ts contains `未识别到更精确城市，已回退到国家/地区`
    - src/services/geo-lookup.spec.ts contains `city`
    - src/components/WorldMapStage.spec.ts contains `fallback`
  </acceptance_criteria>
  <done>The app can enrich detections with city-level metadata conservatively while preserving the existing trustworthy country/region flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist enriched point metadata and harden degradation messaging and storage recovery</name>
  <files>src/services/point-storage.ts, src/services/point-storage.spec.ts, src/stores/map-points.ts, src/stores/map-points.spec.ts, src/components/PointPreviewDrawer.vue, src/App.vue, src/App.spec.ts</files>
  <read_first>src/services/point-storage.ts, src/services/point-storage.spec.ts, src/stores/map-points.ts, src/stores/map-points.spec.ts, src/components/PointPreviewDrawer.vue, src/App.vue, src/types/map-point.ts, .planning/phases/04-可用性打磨与增强能力/04-CONTEXT.md, .planning/phases/04-可用性打磨与增强能力/04-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: High-confidence city metadata survives save, reload, and subsequent point viewing.
    - Test 2: Empty, corrupt, and wrong-version snapshots all fall back safely without crashing and still expose the existing storage recovery path.
    - Test 3: The drawer surfaces city title/subtitle/fallback messaging in the exact UI contract while country/region saving still works when no city is present.
  </behavior>
  <action>Extend `src/services/point-storage.ts` validation and serialization to persist the new point fields `precision`, `cityName`, and `fallbackNotice` for both saved points and restored snapshots while preserving the existing `trip-map:point-state:v1` storage key and corrupt/empty result contract. Update `src/stores/map-points.ts` so saving a draft or updating a saved point retains high-confidence city metadata and fallback messaging instead of stripping the richer fields on persistence. Keep `storageHealth` behavior explicit for empty, corrupt, and incompatible snapshots so `DAT-04` is satisfied even when the stored shape is missing new Phase 4 fields. Update `src/components/PointPreviewDrawer.vue` to render the city title path, country/region subtitle, and the exact fallback notice when `fallbackNotice` is present, while continuing to support ordinary country/region-only points. Extend `src/services/point-storage.spec.ts`, `src/stores/map-points.spec.ts`, and `src/App.spec.ts` to assert high-confidence city persistence after remount, wrong-version safety fallback, and that corrupted snapshots still clear through the existing `清空本地存档` path.</action>
  <verify>rg 'precision|cityName|fallbackNotice' src/services/point-storage.ts src/stores/map-points.ts src/components/PointPreviewDrawer.vue && pnpm test -- src/services/point-storage.spec.ts src/stores/map-points.spec.ts src/App.spec.ts && pnpm build</verify>
  <acceptance_criteria>
    - src/services/point-storage.ts contains `precision`
    - src/services/point-storage.ts contains `cityName`
    - src/stores/map-points.ts contains `fallbackNotice`
    - src/components/PointPreviewDrawer.vue contains `未识别到更精确城市，已回退到国家/地区`
    - src/services/point-storage.spec.ts contains `wrong version`
    - src/stores/map-points.spec.ts contains `city`
    - src/App.spec.ts contains `清空本地存档`
  </acceptance_criteria>
  <done>The richer city/degradation model now survives persistence and appears consistently in the drawer and recovery paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test -- src/services/geo-lookup.spec.ts src/components/WorldMapStage.spec.ts
- [ ] pnpm test -- src/services/point-storage.spec.ts src/stores/map-points.spec.ts src/App.spec.ts
- [ ] pnpm build
- [ ] rg '未识别到更精确城市，已回退到国家/地区' src/services/geo-lookup.ts src/components/PointPreviewDrawer.vue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- High-confidence city enrichment can be stored and displayed
- Country/region creation still succeeds when city precision is unavailable
- Storage recovery and edge-click behavior remain explicit and safe

</success_criteria>

<output>
After completion, create `.planning/phases/04-可用性打磨与增强能力/04-02-SUMMARY.md`
</output>

---
phase: 10-可爱风格与可读性收口
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/components/map-popup/PointSummaryCard.vue
  - src/components/map-popup/PointSummaryCard.spec.ts
  - src/components/map-popup/MapContextPopup.vue
  - src/components/map-popup/MapContextPopup.spec.ts
  - src/components/PointPreviewDrawer.vue
  - src/components/PointPreviewDrawer.spec.ts
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
user_setup: []
must_haves:
  truths:
    - "popup summary surface 和 deep drawer 共享同一套暖粉/淡蓝/圆角语言，但仍保持 Phase 9 的 summary/deep 结构分工。"
    - "fallback、unsupported-boundary、destructive action 都有明确的 notice/button tone，不会被装饰化外观稀释。"
    - "popup footer、drawer action row 和 edit input 保持稳定、可读、可点，不会被新装饰层抢走命中或滚动位置。"
  artifacts:
    - path: "src/components/map-popup/PointSummaryCard.vue"
      provides: "summary card 状态钩子、notice tone 与 CTA tone"
      contains: "data-summary-mode"
    - path: "src/components/map-popup/MapContextPopup.vue"
      provides: "anchored popup 壳层的圆角和箭头视觉"
      contains: "--map-context-popup-min-width"
    - path: "src/components/PointPreviewDrawer.vue"
      provides: "calmer deep surface with edit-safe controls"
      contains: "data-drawer-mode"
  key_links:
    - from: "src/components/map-popup/MapContextPopup.vue"
      to: "src/components/map-popup/PointSummaryCard.vue"
      via: "popup shell hosts the shared summary card"
      pattern: "PointSummaryCard"
    - from: "src/components/PointPreviewDrawer.vue"
      to: "src/stores/map-points.ts"
      via: "drawer consumes active point and boundary support notice"
      pattern: "activeBoundaryCoverageState"
    - from: "src/components/map-popup/PointSummaryCard.spec.ts"
      to: "src/components/map-popup/MapContextPopup.spec.ts"
      via: "summary hooks remain visible through the popup shell"
      pattern: "data-summary-mode"
---

<objective>
Polish popup and drawer surfaces into one coherent cute-system while preserving the established Phase 9 contract: popup remains the lightweight summary entry and drawer remains the deep view/edit surface.

Purpose: Phase 10 visual polish will fail if the summary card, popup shell, and drawer still read like separate products or if decorative upgrades weaken fallback notices, destructive actions, or scroll-safe action rows.
Output: State-aware popup and drawer surfaces with explicit semantic hooks, shared rounded styling, and unchanged summary/deep interaction boundaries.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/09-popup/09-CONTEXT.md
@.planning/phases/09-popup/09-popup-01-SUMMARY.md
@.planning/phases/09-popup/09-popup-05-SUMMARY.md
@.planning/phases/10-可爱风格与可读性收口/10-CONTEXT.md
@.planning/phases/10-可爱风格与可读性收口/10-RESEARCH.md
@.planning/phases/10-可爱风格与可读性收口/10-UI-SPEC.md
@src/styles/tokens.css
@src/types/map-point.ts
@src/components/map-popup/PointSummaryCard.vue
@src/components/map-popup/MapContextPopup.vue
@src/components/PointPreviewDrawer.vue
@src/components/map-popup/PointSummaryCard.spec.ts
@src/components/map-popup/MapContextPopup.spec.ts
@src/components/PointPreviewDrawer.spec.ts

<interfaces>
From `src/types/map-point.ts`:
```ts
export type SummarySurfaceState =
  | {
      mode: 'candidate-select'
      fallbackPoint: DraftMapPoint
      cityCandidates: GeoCityCandidate[]
    }
  | {
      mode: 'detected-preview' | 'view'
      point: MapPointDisplay
      boundarySupportState: 'supported' | 'missing' | 'not-applicable'
    }
```

From `src/components/map-popup/MapContextPopup.vue`:
```ts
const popupStyles = computed(() => ({
  '--map-context-popup-min-width': '280px',
  '--map-context-popup-max-width': '360px',
  ...(props.floatingStyles ?? {})
}))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restyle the shared summary card and popup shell with explicit semantic hooks</name>
  <files>src/components/map-popup/PointSummaryCard.vue, src/components/map-popup/PointSummaryCard.spec.ts, src/components/map-popup/MapContextPopup.vue, src/components/map-popup/MapContextPopup.spec.ts</files>
  <read_first>src/components/map-popup/PointSummaryCard.vue, src/components/map-popup/PointSummaryCard.spec.ts, src/components/map-popup/MapContextPopup.vue, src/components/map-popup/MapContextPopup.spec.ts, src/styles/tokens.css, .planning/phases/09-popup/09-UI-SPEC.md, .planning/phases/10-可爱风格与可读性收口/10-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: The summary card exposes explicit hooks for mode, record source, fallback notice, and CTA tone without breaking existing emits.
    - Test 2: The popup shell keeps its `280px`-`360px` width contract and the footer still sits outside the middle scroll region.
    - Test 3: Saved reuse hints, fallback notices, and destructive actions remain readable and visually distinct from the selected primary CTA.
  </behavior>
  <action>Per D-03, D-07, D-08, D-09, D-12, and D-14, update `src/components/map-popup/PointSummaryCard.vue` to add exact semantic hooks: the root article must expose `data-summary-mode="candidate-select|detected-preview|view"` and `data-record-source="saved|seed|detected|none"`, fallback and unsupported-boundary notices must expose `data-notice-tone="fallback"`, primary CTA and confirm buttons must expose `data-cta-tone="selected"`, destructive buttons must expose `data-cta-tone="destructive"`, and each candidate row must expose `data-candidate-status="saved"` when `statusHint === '已存在记录'` or `data-candidate-status="available"` otherwise. Restyle the card with `border-radius: var(--radius-surface)`, border color `rgba(199, 171, 200, 0.56)`, and background `linear-gradient(180deg, rgba(255, 252, 253, 0.96), rgba(232, 244, 251, 0.94))`, while keeping the existing header/content/footer split and all current emits. In `src/components/map-popup/MapContextPopup.vue`, keep `--map-context-popup-min-width: 280px` and `--map-context-popup-max-width: 360px`, but move the shell and arrow to the new rounded palette, ensure the arrow shares the shell background family, and keep the dialog semantics and focus behavior unchanged. Extend `src/components/map-popup/PointSummaryCard.spec.ts` and `src/components/map-popup/MapContextPopup.spec.ts` to assert the new semantic hooks, the saved reuse hint state, the fallback notice tone, and the stable footer-outside-scroll contract. Do not alter popup width, emit names, or drawer handoff behavior.</action>
  <verify>
    rg -n 'data-summary-mode|data-record-source|data-notice-tone|data-cta-tone|data-candidate-status|--map-context-popup-min-width|--map-context-popup-max-width' src/components/map-popup/PointSummaryCard.vue src/components/map-popup/MapContextPopup.vue
    <automated>npm test -- src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/map-popup/PointSummaryCard.vue` contains `data-summary-mode`
    - `src/components/map-popup/PointSummaryCard.vue` contains `data-record-source`
    - `src/components/map-popup/PointSummaryCard.vue` contains `data-notice-tone="fallback"`
    - `src/components/map-popup/PointSummaryCard.vue` contains `data-cta-tone="selected"`
    - `src/components/map-popup/PointSummaryCard.vue` contains `data-cta-tone="destructive"`
    - `src/components/map-popup/PointSummaryCard.vue` contains `data-candidate-status`
    - `src/components/map-popup/MapContextPopup.vue` contains `--map-context-popup-min-width': '280px'`
    - `src/components/map-popup/MapContextPopup.vue` contains `--map-context-popup-max-width': '360px'`
    - `src/components/map-popup/PointSummaryCard.spec.ts` contains `data-summary-mode`
    - `src/components/map-popup/MapContextPopup.spec.ts` contains `point-summary-card__footer`
  </acceptance_criteria>
  <done>The summary card and popup shell share explicit visual semantics and hooks, while keeping the existing summary content structure and popup sizing contract intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Calm the deep drawer ornament density while preserving edit clarity and guardrails</name>
  <files>src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts</files>
  <read_first>src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/styles/tokens.css, src/stores/map-points.ts, .planning/phases/10-可爱风格与可读性收口/10-CONTEXT.md, .planning/phases/10-可爱风格与可读性收口/10-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: The drawer exposes explicit hooks for `view` vs `edit` mode and keeps the focus trap / unsaved-change guard working.
    - Test 2: Fallback and unsupported-boundary notices remain visible with a dedicated fallback tone.
    - Test 3: Inputs and action buttons use the rounded system without changing the current edit/save flow.
  </behavior>
  <action>Per D-05, D-09, D-10, and D-14, restyle `src/components/PointPreviewDrawer.vue` as the calmer member of the same visual family. Add exact `data-drawer-mode="view|edit"` to the root aside, add `data-notice-tone="fallback"` to both fallback notice paragraphs, and keep the existing `point-preview-drawer__fallback--boundary` class for unsupported-boundary messaging. Update the drawer shell to use `border-radius: var(--radius-surface)`, border color `rgba(199, 171, 200, 0.58)`, and background `linear-gradient(180deg, rgba(255, 249, 252, 0.98), rgba(232, 244, 251, 0.92))`; set inputs and buttons to `border-radius: var(--radius-control)` and keep the current focus-visible outline behavior. Keep the action bar and scroll-region structure unchanged so long descriptions and edit forms remain readable. Extend `src/components/PointPreviewDrawer.spec.ts` to assert `data-drawer-mode`, `data-notice-tone="fallback"`, the existing focus trap, and the unsaved-change guard flow. Do not change `window.confirm()` semantics, store method names, or move any edit fields into popup surfaces.</action>
  <verify>
    rg -n 'data-drawer-mode|data-notice-tone="fallback"|border-radius: var\\(--radius-surface\\)|border-radius: var\\(--radius-control\\)' src/components/PointPreviewDrawer.vue
    <automated>npm test -- src/components/PointPreviewDrawer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/PointPreviewDrawer.vue` contains `data-drawer-mode`
    - `src/components/PointPreviewDrawer.vue` contains `data-notice-tone="fallback"`
    - `src/components/PointPreviewDrawer.vue` contains `border-radius: var(--radius-surface)`
    - `src/components/PointPreviewDrawer.vue` contains `border-radius: var(--radius-control)`
    - `src/components/PointPreviewDrawer.spec.ts` contains `data-drawer-mode`
    - `src/components/PointPreviewDrawer.spec.ts` contains `data-notice-tone="fallback"`
    - `src/components/PointPreviewDrawer.spec.ts` contains `你有未保存的更改，确定关闭吗？`
  </acceptance_criteria>
  <done>The deep drawer visually belongs to the cute system but remains calmer, edit-safe, and semantically explicit for fallback and unsupported states.</done>
</task>

</tasks>

<verification>
- `npm test -- src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts src/components/PointPreviewDrawer.spec.ts`
- `rg -n 'data-summary-mode|data-record-source|data-notice-tone|data-cta-tone|data-drawer-mode' src/components/map-popup/PointSummaryCard.vue src/components/map-popup/MapContextPopup.vue src/components/PointPreviewDrawer.vue`
- Confirm popup header/content/footer split and drawer scroll region remain intact after styling changes
</verification>

<success_criteria>
- Popup and drawer surfaces read as one coherent visual system without collapsing the summary/deep separation
- Fallback, unsupported-boundary, saved reuse, and destructive states remain explicit through semantic hooks and styling
- Popup/footer and drawer action rows stay stable and reachable after the visual refresh
</success_criteria>

<output>
After completion, create `.planning/phases/10-可爱风格与可读性收口/10-03-SUMMARY.md`
</output>

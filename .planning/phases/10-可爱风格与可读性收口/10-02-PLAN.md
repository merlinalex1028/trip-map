---
phase: 10-可爱风格与可读性收口
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/components/WorldMapStage.vue
  - src/components/WorldMapStage.spec.ts
  - src/components/SeedMarkerLayer.vue
  - src/components/SeedMarkerLayer.spec.ts
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
user_setup: []
must_haves:
  truths:
    - "地图舞台、城市边界和 marker 在不改变现有交互结构的前提下，进入统一的暖粉/淡蓝可爱风语言。"
    - "未记录、已记录、当前选中和草稿/低置信相关状态在 marker 与 boundary 层至少通过颜色以外的第二线索拉开。"
    - "所有舞台装饰层都保持 inert，不会抢占地图点击、marker 点击或 popup 锚点命中。"
  artifacts:
    - path: "src/components/WorldMapStage.vue"
      provides: "地图 frame、底图 wash、boundary/pending marker 状态样式"
      contains: "data-highlight-state"
    - path: "src/components/SeedMarkerLayer.vue"
      provides: "marker 四态语义与 reduced-motion 护栏"
      contains: "data-marker-state"
    - path: "src/components/WorldMapStage.spec.ts"
      provides: "舞台状态与 inert overlay 回归"
      contains: "data-highlight-state"
  key_links:
    - from: "src/components/WorldMapStage.vue"
      to: "src/components/SeedMarkerLayer.vue"
      via: "stage overlay below marker hit targets"
      pattern: "pointer-events: none"
    - from: "src/components/WorldMapStage.vue"
      to: "src/components/WorldMapStage.spec.ts"
      via: "saved/selected boundary hooks"
      pattern: "data-highlight-state"
    - from: "src/components/SeedMarkerLayer.vue"
      to: "src/components/SeedMarkerLayer.spec.ts"
      via: "marker semantic hooks"
      pattern: "data-marker-state"
---

<objective>
Polish the map stage itself: make the world frame, boundary overlay, pending marker, and point markers visually cute and semantically legible while preserving the Phase 8/9 interaction hierarchy.

Purpose: Phase 10 cannot claim a unified visual system if the stage remains on the old parchment palette or if boundary/marker styling drifts away from the new state contract. This plan changes surface-level styling only; it does not re-architect anchoring, boundary logic, or store semantics.
Output: A state-aware map stage and marker layer that consume the shared tokens, respect reduced-motion, and keep all decoration outside clickable layers.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-城市边界高亮语义/08-CONTEXT.md
@.planning/phases/08-城市边界高亮语义/08-03-SUMMARY.md
@.planning/phases/09-popup/09-popup-02-SUMMARY.md
@.planning/phases/10-可爱风格与可读性收口/10-CONTEXT.md
@.planning/phases/10-可爱风格与可读性收口/10-RESEARCH.md
@.planning/phases/10-可爱风格与可读性收口/10-UI-SPEC.md
@src/styles/tokens.css
@src/components/WorldMapStage.vue
@src/components/WorldMapStage.spec.ts
@src/components/SeedMarkerLayer.vue
@src/components/SeedMarkerLayer.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restyle the world map frame, boundary overlay, and pending marker without changing interaction precedence</name>
  <files>src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts</files>
  <read_first>src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts, src/styles/tokens.css, .planning/phases/08-城市边界高亮语义/08-CONTEXT.md, .planning/phases/09-popup/09-CONTEXT.md, .planning/phases/10-可爱风格与可读性收口/10-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: Saved and selected boundaries still render with distinct hooks and keep the same selected-over-saved precedence.
    - Test 2: Fallback-to-country or unsupported flows still do not fabricate a city boundary highlight.
    - Test 3: Decorative layers stay inert and reduced-motion disables stage emphasis transitions instead of changing interaction behavior.
  </behavior>
  <action>Per D-04 through D-13, restyle `src/components/WorldMapStage.vue` without changing any computed state, popup anchor priority, or click handlers. In the stage frame, use `border-radius: 32px`, `padding: 24px`, border color `rgba(199, 171, 200, 0.72)`, and a scrapbook wash `linear-gradient(180deg, rgba(255, 247, 251, 0.92), rgba(232, 244, 251, 0.88))`. In the map surface, use `border-radius: 28px`, keep `overflow: hidden`, and replace the old brown background with the exact layered wash `radial-gradient(circle at 18% 18%, rgba(255, 255, 255, 0.56), transparent 24%), linear-gradient(180deg, rgba(255, 238, 246, 0.78), rgba(232, 244, 251, 0.86))`. Keep `.world-map-stage__overlay { pointer-events: none; }` and apply the same rule to any new decorative pseudo-element. Update saved boundary styling to `fill: rgba(132, 199, 216, 0.24)` and `stroke: rgba(132, 199, 216, 0.82)`; update selected boundary styling to `fill: rgba(244, 143, 177, 0.28)` and `stroke: rgba(244, 143, 177, 0.96)`; keep `data-highlight-state="saved"` and `data-highlight-state="selected"` unchanged. Restyle the pending overlay marker to use the selected palette and a softer ring, but keep `data-pending-marker="true"` and the existing recognition flow. Add `@media (prefers-reduced-motion: reduce)` that removes boundary transition emphasis and any pending-marker animation while preserving state visibility. Extend `src/components/WorldMapStage.spec.ts` to keep asserting saved/selected hooks, fallback no-boundary behavior, and the inert overlay contract. Do not change `handleMapClick`, `resolvePopupAnchor`, `MapContextPopup`, or `PointPreviewDrawer` logic in this plan.</action>
  <verify>
    rg -n 'data-highlight-state="saved"|data-highlight-state="selected"|pointer-events: none|prefers-reduced-motion|rgba\(132, 199, 216, 0.24\)|rgba\(244, 143, 177, 0.28\)' src/components/WorldMapStage.vue
    <automated>npm test -- src/components/WorldMapStage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/WorldMapStage.vue` contains `data-highlight-state="saved"`
    - `src/components/WorldMapStage.vue` contains `data-highlight-state="selected"`
    - `src/components/WorldMapStage.vue` contains `pointer-events: none;`
    - `src/components/WorldMapStage.vue` contains `@media (prefers-reduced-motion: reduce)`
    - `src/components/WorldMapStage.vue` contains `rgba(132, 199, 216, 0.24)`
    - `src/components/WorldMapStage.vue` contains `rgba(244, 143, 177, 0.28)`
    - `src/components/WorldMapStage.spec.ts` contains `data-highlight-state="selected"`
    - `src/components/WorldMapStage.spec.ts` contains `world-map-stage__boundary-layer`
  </acceptance_criteria>
  <done>The map stage reads as part of the new cute system while still obeying the original boundary semantics, popup anchoring order, and inert overlay rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add four-state semantic hooks and cute marker styling without shrinking hit targets</name>
  <files>src/components/SeedMarkerLayer.vue, src/components/SeedMarkerLayer.spec.ts</files>
  <read_first>src/components/SeedMarkerLayer.vue, src/components/SeedMarkerLayer.spec.ts, src/styles/tokens.css, .planning/phases/10-可爱风格与可读性收口/10-CONTEXT.md, .planning/phases/10-可爱风格与可读性收口/10-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: Marker roots expose explicit semantic hooks for selected, draft, saved, and neutral states.
    - Test 2: Selected and saved markers remain distinguishable even when the marker label is hidden.
    - Test 3: Buttons keep the `44px` hit target and reduced-motion disables the continuous draft pulse.
  </behavior>
  <action>Per D-07, D-08, D-10, D-11, and D-12, update `src/components/SeedMarkerLayer.vue` so every marker root emits an exact `data-marker-state` value: `selected` when `point.id === props.selectedPointId`, `draft` when `point.source === 'detected'` and not selected, `saved` when `point.source === 'saved'` and not selected, and `neutral` for all remaining visible points. Keep the existing selected/dimmed classes and `aria-label` logic. Restyle the marker dot and label to use the new palette: selected uses `var(--color-state-selected)` plus a layered halo, saved uses `var(--color-state-saved)` with static cool outline, draft keeps the selected-family palette but uses a single pulse animation at `1.75s ease-in-out infinite` with max scale `1.08`, and neutral uses `var(--color-state-neutral)` with `var(--color-state-neutral-outline)`. Keep `.seed-marker__button` at exact `width: 44px; height: 44px;` and keep labels `pointer-events: none`. Set marker label chips to rounded pill styling with `border-radius: var(--radius-pill)`. Add `@media (prefers-reduced-motion: reduce)` that removes `draft-marker-pulse` while preserving the draft palette. Extend `src/components/SeedMarkerLayer.spec.ts` to assert `data-marker-state`, preserve the existing aria-label semantics, and keep the focus-driven label reveal behavior. Do not change point selection handlers or store usage.</action>
  <verify>
    rg -n 'data-marker-state|width: 44px|height: 44px|prefers-reduced-motion|1.75s ease-in-out infinite' src/components/SeedMarkerLayer.vue
    <automated>npm test -- src/components/SeedMarkerLayer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SeedMarkerLayer.vue` contains `data-marker-state`
    - `src/components/SeedMarkerLayer.vue` contains `'selected'`
    - `src/components/SeedMarkerLayer.vue` contains `'draft'`
    - `src/components/SeedMarkerLayer.vue` contains `'saved'`
    - `src/components/SeedMarkerLayer.vue` contains `'neutral'`
    - `src/components/SeedMarkerLayer.vue` contains `width: 44px`
    - `src/components/SeedMarkerLayer.vue` contains `height: 44px`
    - `src/components/SeedMarkerLayer.vue` contains `@media (prefers-reduced-motion: reduce)`
    - `src/components/SeedMarkerLayer.spec.ts` contains `data-marker-state`
  </acceptance_criteria>
  <done>Markers now participate in the shared state contract with explicit hooks, cute styling, and unchanged click safety.</done>
</task>

</tasks>

<verification>
- `npm test -- src/components/WorldMapStage.spec.ts src/components/SeedMarkerLayer.spec.ts`
- `rg -n 'data-highlight-state|data-marker-state|prefers-reduced-motion|pointer-events: none' src/components/WorldMapStage.vue src/components/SeedMarkerLayer.vue`
- Confirm no new clickable decoration node was introduced above `.seed-marker__button` or the popup anchor layer
</verification>

<success_criteria>
- The map stage, boundary overlay, and markers share the new cute-system palette and rounded visual language
- Marker and boundary states remain distinguishable through multiple cues, not color alone
- Overlay decoration remains inert and reduced-motion support is present for continuous emphasis
</success_criteria>

<output>
After completion, create `.planning/phases/10-可爱风格与可读性收口/10-02-SUMMARY.md`
</output>

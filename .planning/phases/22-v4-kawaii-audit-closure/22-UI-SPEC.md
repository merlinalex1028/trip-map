---
phase: 22
slug: v4-kawaii-audit-closure
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-09
---

# Phase 22 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

本 phase 只负责把 Phase 20 已实现的 kawaii shell、popup、cloud card 与 CTA motion 合同冻结为可审计设计真源；不新增 UI 功能、不引入新组件库、不改写现有视觉方向。来源以 `22-CONTEXT.md`、`22-RESEARCH.md`、`REQUIREMENTS.md`、Phase 20 summaries/validation 与当前 web 代码为准。

---

## Design System

| Property | Value |
|----------|-------|
| Tool | `none`；沿用仓库现有 Vue 3 + Tailwind v4 自定义 token 体系（source: repo scan, `apps/web/src/style.css`） |
| Preset | `not applicable`；仓库无 `components.json`，Phase 22 不执行 shadcn 初始化（source: repo scan, `22-RESEARCH.md`） |
| Component library | `none`；使用自定义 Vue SFC、Tailwind utilities、Leaflet popup shell（source: `22-RESEARCH.md`, `App.vue`, `MapContextPopup.vue`, `PointSummaryCard.vue`） |
| Icon library | `none`；当前主路径未声明 icon package（source: `apps/web/package.json`, repo grep） |
| Font | `Nunito Variable` + `Noto Sans SC`/`PingFang SC` 等中文回退（source: `apps/web/src/style.css`, `apps/web/src/styles/tokens.css`） |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | popup light shell `p-1`、微型 inline gap（source: `tokens.css`, `MapContextPopup.vue`） |
| sm | 8px | 顶栏英文 tag、紧凑 pill 内边距（source: `tokens.css`, `App.vue`） |
| md | 16px | 默认组件间距、notice/CTA 水平 padding（source: `tokens.css`, `App.vue`, `PointSummaryCard.vue`） |
| lg | 24px | section padding、标题区与内容区留白（source: `tokens.css`, `PointSummaryCard.vue`） |
| xl | 32px | popup/card 圆润外形与主 shell 呼吸感的最小版心间距（source: `tokens.css`, Phase 20 summaries） |
| 2xl | 48px | 主要区块断点间距；仅用于页面层与大卡片之间的视觉断开（source: `tokens.css`, default freeze） |
| 3xl | 64px | 页面级留白上限；仅用于桌面大视口节奏，不下放到 popup 内部（source: `tokens.css`, default freeze） |

Exceptions: `min-h-11`/`44px` 保留给 primary / secondary CTA 命中高度（source: `PointSummaryCard.kawaii.spec.ts`）；顶栏英文 tag 的 `py-[0.14rem]` 属现有微调遗留，只允许留在当前 topbar，不向其他 surface 扩散（source: `App.vue`）。

---

## Typography

本 phase 冻结 4 档字号与 2 档字重，不再扩展新的展示层级；现有更重的源码写法按“视觉既有事实”保留，但后续实现应向这套精简契约收口。

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px（source: `tokens.css`） | 400 | 1.5 |
| Label | 14px（source: `tokens.css`） | 600 | 1.4 |
| Heading | 24px（source: `tokens.css`, `PointSummaryCard.vue` title band） | 600 | 1.2 |
| Display | 28px（default freeze for topbar brand + popup hero emphasis; 当前实现允许 clamp 到约 24-28px） | 600 | 1.2 |

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FAFAFA` | 页面背景、地图外层背景、整体奶油白基底（source: `REQUIREMENTS.md`, `style.css`, `tokens.css`） |
| Secondary (30%) | `#FFF5F5` | topbar、map shell、popup/card 的柔和二级表面与浅色 chrome（source: `style.css`, `App.vue`, `tokens.css`） |
| Accent (10%) | `#FF78AD` | 主 CTA `点亮/已点亮`、推荐候选强调、focus/selection 高亮；不得用于地图宿主、popup 外壳或静态说明文本（source: `PointSummaryCard.vue`, `tokens.css`） |
| Destructive | `#C86464` | 仅保留给未来 destructive action；Phase 22 不新增 destructive UI（source: `tokens.css`, `22-CONTEXT.md`） |

Accent reserved for: `点亮/已点亮` 主按钮、推荐候选卡强调边界/CTA 文案、focus-visible outline、文本选区；绝不扩散到 Leaflet map host、popup light shell、badge/type pill 或普通 notice。

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | `点亮`（source: `PointSummaryCard.vue`） |
| Empty state heading | `暂无可确认候选地点`（source: `PointSummaryCard.vue`） |
| Empty state body | `请稍后重试。若仍无候选，保留当前 canonical 地点身份与文本信息。`（前半句 source: `PointSummaryCard.vue`；后半句为 Phase 20 fallback 流程归纳，source: `20-CONTEXT.md`, `22-RESEARCH.md`） |
| Error state | `记录服务暂不可用，请先启动 pnpm dev 或单独运行 pnpm dev:server。`；交互失败时沿用 `确认地点失败，请稍后重试` / `识别请求失败，请稍后重试`（source: `LeafletMapStage.vue`, repo grep） |
| Destructive confirmation | `无`；Phase 22 不新增 destructive action、确认弹层或危险文案（source: `22-CONTEXT.md`） |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `none` | not applicable — no shadcn initialization in this Vue phase |
| third-party | `none` | not applicable — no registry imports declared as of 2026-04-09 |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

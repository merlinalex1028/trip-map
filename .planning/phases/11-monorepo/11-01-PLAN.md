---
phase: 11-monorepo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - packages/contracts/package.json
  - packages/contracts/tsconfig.json
  - packages/contracts/vitest.config.ts
  - packages/contracts/src/place.ts
  - packages/contracts/src/records.ts
  - packages/contracts/src/health.ts
  - packages/contracts/src/fixtures.ts
  - packages/contracts/src/index.ts
  - packages/contracts/src/contracts.spec.ts
autonomous: true
requirements:
  - ARC-01
  - ARC-04
user_setup: []
must_haves:
  truths:
    - "仓库根目录不再是单个 Vite 应用入口，而是通过 pnpm workspace + turbo 编排 web、server 和 contracts。"
    - "跨端最小真实链路需要的 `placeId`、`boundaryId`、`placeKind`、`datasetVersion` 字段只在一处 contracts 包定义。"
    - "contracts 包只暴露类型、schema 和关键常量，不引入 Pinia、NestJS、Prisma 或浏览器运行时代码。"
  artifacts:
    - path: "package.json"
      provides: "workspace 级 dev/build/test/typecheck 入口"
      contains: "\"turbo run build\""
    - path: "pnpm-workspace.yaml"
      provides: "apps 与 packages workspace 收口"
      contains: "\"apps/*\""
    - path: "packages/contracts/src/place.ts"
      provides: "canonical place 字段与 PlaceKind 枚举"
      contains: "export type PlaceKind = 'CN_CITY' | 'OVERSEAS_ADMIN1'"
    - path: "packages/contracts/src/records.ts"
      provides: "records smoke 请求/响应 DTO"
      contains: "export interface SmokeRecordCreateRequest"
  key_links:
    - from: "package.json"
      to: "turbo.json"
      via: "root scripts delegate all orchestration to turbo"
      pattern: "turbo run"
    - from: "packages/contracts/src/index.ts"
      to: "packages/contracts/src/place.ts"
      via: "contracts package exports canonical place fields"
      pattern: "from './place'"
    - from: "packages/contracts/src/index.ts"
      to: "packages/contracts/src/records.ts"
      via: "contracts package exports smoke DTOs"
      pattern: "from './records'"
---

<objective>
把仓库根目录重构为 `pnpm workspace + turbo` 的 orchestration layer，并先写出后续 Phase 11 其余计划都要依赖的 thin contracts 包。

Purpose: 如果 Phase 11 一开始不先锁住 workspace 任务图和单一 contracts 真源，后面的 `apps/web`、`apps/server` 和 smoke path 会继续复制字段定义，Phase 12-15 会在契约边界上反复漂移。
Output: workspace 根配置、`turbo.json`、`tsconfig.base.json`，以及一个可独立 build/test/typecheck 的 `packages/contracts`。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/FEATURES.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/08-城市边界高亮语义/08-04-SUMMARY.md
@package.json
@pnpm-workspace.yaml
@tsconfig.json
@src/types/map-point.ts
@src/types/geo.ts

<interfaces>
From `src/types/map-point.ts`:
```ts
interface BaseMapPoint {
  cityId: string | null
  boundaryId: string | null
  boundaryDatasetVersion: string | null
}
```

From `src/types/geo.ts`:
```ts
export interface CityBoundaryFeatureProperties {
  boundaryId: string
  cityId: string
  cityName: string
  datasetVersion: string
}
```

Target exports for `packages/contracts`:
```ts
export type PlaceKind = 'CN_CITY' | 'OVERSEAS_ADMIN1'

export interface CanonicalPlaceRef {
  placeId: string
  boundaryId: string
  placeKind: PlaceKind
  datasetVersion: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe the repo root as a pnpm workspace + turbo orchestrator</name>
  <files>package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-CONTEXT.md, .planning/phases/11-monorepo/11-RESEARCH.md, package.json, pnpm-workspace.yaml, tsconfig.json</read_first>
  <action>Per D-01, D-02, and D-03, replace the current single-app root scripts with workspace orchestration only. Update `package.json` so the root stays `private: true`, adds `packageManager: "pnpm@10.33.0"`, moves `turbo` to root `devDependencies`, and exposes exact scripts: `dev: turbo run dev --parallel`, `dev:web: turbo run dev --filter=@trip-map/web`, `dev:server: turbo run dev --filter=@trip-map/server`, `build: turbo run build`, `test: turbo run test`, `typecheck: turbo run typecheck`. Expand `pnpm-workspace.yaml` to keep the existing `allowBuilds.esbuild: true` and add exact package globs `apps/*` and `packages/*`. Create `turbo.json` with `build` depending on `^build` and outputting `dist/**`, `test` depending on `^build` with no outputs, `typecheck` depending on `^typecheck` with no outputs, and `dev` marked `cache: false` and `persistent: true`. Add `tsconfig.base.json` by lifting the current strict TypeScript settings out of root `tsconfig.json`, so each app/package can extend one shared base without the repo root owning source files anymore. Do not keep root-level `vite`, `vitest`, `vue`, `pinia`, or app runtime scripts after this task; the root must become an orchestrator layer only.</action>
  <verify>
    <automated>pnpm exec turbo --version</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"dev": "turbo run dev --parallel"`
    - `package.json` contains `"dev:web": "turbo run dev --filter=@trip-map/web"`
    - `package.json` contains `"dev:server": "turbo run dev --filter=@trip-map/server"`
    - `package.json` contains `"build": "turbo run build"`
    - `pnpm-workspace.yaml` contains `apps/*`
    - `pnpm-workspace.yaml` contains `packages/*`
    - `pnpm-workspace.yaml` contains `allowBuilds:`
    - `turbo.json` contains `"build"`
    - `turbo.json` contains `"dependsOn": ["^build"]`
    - `turbo.json` contains `"persistent": true`
    - `tsconfig.base.json` contains `"moduleResolution": "Bundler"`
    - `tsconfig.base.json` contains `"strict": true`
  </acceptance_criteria>
  <done>根目录已经从单应用运行入口收口为 workspace 编排层，并具备后续 `apps/web`、`apps/server`、`packages/contracts` 的统一任务图。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the thin contracts package for canonical place and smoke record fields</name>
  <files>packages/contracts/package.json, packages/contracts/tsconfig.json, packages/contracts/vitest.config.ts, packages/contracts/src/place.ts, packages/contracts/src/records.ts, packages/contracts/src/health.ts, packages/contracts/src/fixtures.ts, packages/contracts/src/index.ts, packages/contracts/src/contracts.spec.ts</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-CONTEXT.md, .planning/phases/11-monorepo/11-RESEARCH.md, src/types/map-point.ts, src/types/geo.ts, .planning/phases/08-城市边界高亮语义/08-04-SUMMARY.md</read_first>
  <behavior>
    - Test 1: `@trip-map/contracts` exports `PlaceKind`, `CanonicalPlaceRef`, `SmokeRecordCreateRequest`, `SmokeRecordResponse`, and `HealthStatusResponse` from one index file.
    - Test 2: The canonical fields use exact names `placeId`, `boundaryId`, `placeKind`, and `datasetVersion`; there is no `boundaryDatasetVersion` alias inside contracts.
    - Test 3: The package stays thin: no imports from `pinia`, `vue`, `@nestjs/*`, `prisma`, or browser globals.
  </behavior>
  <action>Per D-04, D-05, D-06, and ARC-04, create `packages/contracts` as a compiled internal package named `@trip-map/contracts` with scripts `build`, `test`, and `typecheck`. In `src/place.ts`, define exact exports `PlaceKind = 'CN_CITY' | 'OVERSEAS_ADMIN1'`, `CanonicalPlaceRef { placeId, boundaryId, placeKind, datasetVersion }`, and `CanonicalPlaceSummary { placeId, boundaryId, placeKind, datasetVersion, displayName }`. In `src/records.ts`, define `SmokeRecordCreateRequest` with exact fields `placeId`, `boundaryId`, `placeKind`, `datasetVersion`, `displayName`, and optional `note`; define `SmokeRecordResponse` with those same fields plus exact `id`, `createdAt`, and `updatedAt`. In `src/health.ts`, define `PHASE11_CONTRACTS_VERSION = 'phase11-v1'` and `HealthStatusResponse { status: 'ok'; service: 'server'; contractsVersion: string; database: 'up' | 'down' }`. In `src/fixtures.ts`, define `PHASE11_SMOKE_RECORD_REQUEST` using a stable fixture payload with `placeId: 'phase11-demo-place'`, `boundaryId: 'phase11-demo-boundary'`, `placeKind: 'OVERSEAS_ADMIN1'`, `datasetVersion: 'phase11-smoke-v1'`, and `displayName: 'Phase 11 Demo Place'`. Re-export every public symbol from `src/index.ts`. Add `contracts.spec.ts` to assert the fixture shape, the exported version constant, and the exact canonical field names. Do not add runtime dependencies beyond TypeScript/Vitest package infrastructure, and do not move store logic, formatting helpers, or server decorators into this package.</action>
  <verify>
    <automated>pnpm --filter @trip-map/contracts test && pnpm --filter @trip-map/contracts typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/contracts/package.json` contains `"name": "@trip-map/contracts"`
    - `packages/contracts/src/place.ts` contains `export type PlaceKind = 'CN_CITY' | 'OVERSEAS_ADMIN1'`
    - `packages/contracts/src/place.ts` contains `placeId: string`
    - `packages/contracts/src/place.ts` contains `datasetVersion: string`
    - `packages/contracts/src/records.ts` contains `export interface SmokeRecordCreateRequest`
    - `packages/contracts/src/records.ts` contains `displayName: string`
    - `packages/contracts/src/health.ts` contains `PHASE11_CONTRACTS_VERSION = 'phase11-v1'`
    - `packages/contracts/src/fixtures.ts` contains `phase11-demo-place`
    - `packages/contracts/src/index.ts` contains `from './place'`
    - `packages/contracts/src/index.ts` contains `from './records'`
    - `packages/contracts/src/index.ts` contains `from './health'`
    - `packages/contracts/src/index.ts` contains `from './fixtures'`
    - `rg -n 'boundaryDatasetVersion' packages/contracts/src` returns no matches
    - `rg -n 'pinia|vue|@nestjs|prisma|window\\.' packages/contracts/src` returns no matches
  </acceptance_criteria>
  <done>`packages/contracts` 已经成为 Phase 11 之后的单一契约真源，后续 web/server 可以直接按 package name 引用相同的 canonical 字段和 smoke DTO。</done>
</task>

</tasks>

<verification>
- `pnpm --filter @trip-map/contracts test`
- `pnpm --filter @trip-map/contracts typecheck`
- `rg -n '"turbo run|@trip-map/contracts|PHASE11_CONTRACTS_VERSION|SmokeRecordCreateRequest|PlaceKind' package.json turbo.json packages/contracts`
- Confirm `rg -n 'pinia|vue|@nestjs|prisma|boundaryDatasetVersion' packages/contracts/src` returns no matches
</verification>

<success_criteria>
- Root scripts and workspace globs make the repository ready for multi-app execution
- Contracts package becomes the only exported source for Phase 11 canonical place and smoke record fields
- No runtime business logic or framework dependencies leak into `packages/contracts`
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-01-SUMMARY.md`
</output>

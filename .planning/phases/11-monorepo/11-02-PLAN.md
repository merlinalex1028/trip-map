---
phase: 11-monorepo
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/index.html
  - apps/web/vite.config.ts
  - apps/web/vitest.config.ts
  - apps/web/src/main.ts
  - apps/web/src/legacy-entry.ts
autonomous: true
requirements:
  - ARC-01
user_setup: []
must_haves:
  truths:
    - "`apps/web` 已经作为独立 workspace package 存在，并拥有自己的 package/config/bootstrap 入口。"
    - "repo root 不再直接承担 web 运行壳层，所有 web 启动、构建与类型检查入口都收口到 `apps/web`。"
    - "在完整迁移源码前，`apps/web` 对 legacy root source 的过渡桥接是显式且临时的，后续计划可以无歧义移除它。"
  artifacts:
    - path: "apps/web/package.json"
      provides: "独立的 web workspace package 与本地脚本入口"
      contains: "\"name\": \"@trip-map/web\""
    - path: "apps/web/src/main.ts"
      provides: "迁移后的 web bootstrap 入口"
      contains: "legacy-entry"
    - path: "apps/web/vitest.config.ts"
      provides: "迁移后的 web 测试入口"
      contains: "happy-dom"
  key_links:
    - from: "apps/web/src/main.ts"
      to: "apps/web/src/legacy-entry.ts"
      via: "new package bootstrap delegates to one temporary bridge into the legacy app"
      pattern: "from './legacy-entry'"
    - from: "apps/web/package.json"
      to: "packages/contracts/package.json"
      via: "web declares contracts as a workspace dependency"
      pattern: "@trip-map/contracts"
    - from: "package.json"
      to: "apps/web/package.json"
      via: "root turbo scripts now target the web package rather than the repo root app"
      pattern: "@trip-map/web"
---

<objective>
先把 `apps/web` 搭成真正的 workspace 应用壳层与工具链入口，再用一层显式过渡桥接接住当前 root source。

Purpose: checker 已明确指出原 `11-02` 把 package bootstrap、源码迁移和回归稳定揉成一个过大的执行单元。这个修订版只负责先把 `apps/web` 的 package/config/bootstrap 与过渡桥接建立起来，为下一份计划专门处理源码与 spec 迁移。
Output: `apps/web` package、独立的 Vite/Vitest/TypeScript 配置、以及一个临时 `legacy-entry` 过渡桥。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/09-popup/09-popup-05-SUMMARY.md
@.planning/phases/10-可爱风格与可读性收口/10-03-SUMMARY.md
@src/App.vue
@src/main.ts
@vite.config.ts
@vitest.config.ts

<interfaces>
From `src/main.ts`:
```ts
createApp(App).use(createPinia()).mount('#app')
```

From `src/App.vue`:
```ts
mapPointsStore.bootstrapPoints()
```

Target package metadata:
```json
{
  "name": "@trip-map/web",
  "dependencies": {
    "@trip-map/contracts": "workspace:*"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold `apps/web` as a standalone package and add a temporary bridge to the legacy root app</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/web/index.html, apps/web/vite.config.ts, apps/web/vitest.config.ts, apps/web/src/main.ts, apps/web/src/legacy-entry.ts</files>
  <read_first>AGENTS.md, .planning/phases/11-monorepo/11-CONTEXT.md, .planning/phases/09-popup/09-popup-05-SUMMARY.md, .planning/phases/10-可爱风格与可读性收口/10-03-SUMMARY.md, src/App.vue, src/main.ts, vite.config.ts, vitest.config.ts</read_first>
  <action>Per D-01, D-02, D-03, and the checker split request, keep this plan narrowly focused on the `apps/web` package shell. Create `apps/web/package.json` named `@trip-map/web` with exact scripts `dev: vite`, `build: vue-tsc --noEmit && vite build`, `test: vitest run`, and `typecheck: vue-tsc --noEmit`; copy the current Vue/Vite/Pinia/browser-facing dependencies into this package and add `@trip-map/contracts: workspace:*`. Create `apps/web/tsconfig.json` extending `../../tsconfig.base.json`. Move ownership of `index.html`, `vite.config.ts`, and `vitest.config.ts` into `apps/web`, updating root-relative paths so the package can be executed from its own directory. Add `apps/web/src/main.ts` as the new bootstrap entry and create `apps/web/src/legacy-entry.ts` as a temporary, explicit bridge into the still-rooted legacy source tree. This bridge may import from the current root `src/` during the transition, but it must be clearly labeled as temporary and scoped only to bootstrapping; do not relocate the full runtime source tree, specs, or assets in this plan, and do not let repo root remain the primary app shell after this task.</action>
  <verify>
    <automated>pnpm -C apps/web build && pnpm -C apps/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` contains `"name": "@trip-map/web"`
    - `apps/web/package.json` contains `"dev": "vite"`
    - `apps/web/package.json` contains `"build": "vue-tsc --noEmit && vite build"`
    - `apps/web/package.json` contains `"test": "vitest run"`
    - `apps/web/package.json` contains `"typecheck": "vue-tsc --noEmit"`
    - `apps/web/package.json` contains `"@trip-map/contracts": "workspace:*"`
    - `apps/web/tsconfig.json` contains `"extends": "../../tsconfig.base.json"`
    - `apps/web/src/main.ts` contains `legacy-entry`
    - `apps/web/src/legacy-entry.ts` contains `temporary`
    - `apps/web/vitest.config.ts` contains `happy-dom`
  </acceptance_criteria>
  <done>`apps/web` 已经先行成为可独立执行的 workspace package，并通过明确的过渡桥接承接 legacy app，为下一份计划专门做源码与 spec 迁移腾出空间。</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web build`
- `pnpm -C apps/web typecheck`
- `rg -n '"name": "@trip-map/web"|legacy-entry|@trip-map/contracts|happy-dom' apps/web`
- Confirm repo root no longer owns the web package metadata/config entrypoints
</verification>

<success_criteria>
- `apps/web` becomes a standalone workspace package with its own config/bootstrap shell
- Root orchestration points at `apps/web` instead of treating the repo root as the app runtime
- The temporary legacy bridge is explicit and isolated so a later plan can remove it cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-02-SUMMARY.md`
</output>

---
phase: 11-monorepo
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/index.html
  - apps/web/vite.config.ts
  - apps/web/vitest.config.ts
  - apps/web/src/**/*
  - apps/web/src/**/*.spec.ts
autonomous: true
requirements:
  - ARC-01
user_setup: []
must_haves:
  truths:
    - "现有 Vue 地图应用可以在 `apps/web` 下独立运行、构建和测试，不再依赖 repo root 作为应用入口。"
    - "Phase 7-10 已交付的地图、popup、drawer、localStorage 警告等行为在迁移目录后不发生回归。"
    - "web 应用已经作为 workspace package 准备好引用 `@trip-map/contracts`，但不会把 Pinia/store 逻辑搬进 contracts。"
  artifacts:
    - path: "apps/web/package.json"
      provides: "独立的 web workspace package"
      contains: "\"name\": \"@trip-map/web\""
    - path: "apps/web/src/App.vue"
      provides: "迁移后的应用壳层"
      contains: "WorldMapStage"
    - path: "apps/web/vitest.config.ts"
      provides: "迁移后的 web 测试入口"
      contains: "happy-dom"
  key_links:
    - from: "apps/web/src/main.ts"
      to: "apps/web/src/App.vue"
      via: "web package bootstrap still mounts the existing Vue app"
      pattern: "createApp\\(App\\)"
    - from: "apps/web/src/App.vue"
      to: "apps/web/src/components/WorldMapStage.vue"
      via: "Phase 10 main experience remains wired into the app shell"
      pattern: "WorldMapStage"
    - from: "apps/web/package.json"
      to: "packages/contracts/package.json"
      via: "web declares contracts as a workspace dependency"
      pattern: "@trip-map/contracts"
---

<objective>
把当前根目录下的 Vue/Vite 应用完整迁入 `apps/web`，让 web 成为一个独立的 workspace app，同时保留当前 v2.0 主链路行为。

Purpose: Phase 11 的 monorepo 拆分不能只把 server 加进来，`web` 也必须从 repo root 解耦成真正的独立应用，否则 ARC-01 只完成了一半。
Output: `apps/web` package、迁移后的源码/测试/配置文件，以及仍然通过的 web build/test/typecheck。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/09-popup/09-popup-05-SUMMARY.md
@.planning/phases/10-可爱风格与可读性收口/10-03-SUMMARY.md
@src/App.vue
@src/main.ts
@src/components/WorldMapStage.vue
@src/components/PointPreviewDrawer.vue
@vite.config.ts
@vitest.config.ts

<interfaces>
From `src/main.ts`:
```ts
createApp(App).use(createPinia()).mount('#app')
```

From `src/App.vue`:
```ts
mapPointsStore.bootstrapPoints()
```

Target package metadata:
```json
{
  "name": "@trip-map/web",
  "dependencies": {
    "@trip-map/contracts": "workspace:*"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift the current Vite/Vue application into `apps/web` without behavior regressions</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/web/index.html, apps/web/vite.config.ts, apps/web/vitest.config.ts, apps/web/src/**/*, apps/web/src/**/*.spec.ts</files>
  <read_first>AGENTS.md, .planning/phases/11-monorepo/11-CONTEXT.md, .planning/phases/09-popup/09-popup-05-SUMMARY.md, .planning/phases/10-可爱风格与可读性收口/10-03-SUMMARY.md, src/App.vue, src/main.ts, src/components/WorldMapStage.vue, src/components/PointPreviewDrawer.vue, src/stores/map-points.ts, vite.config.ts, vitest.config.ts</read_first>
  <action>Per D-02 and D-03, move the existing root web runtime into `apps/web` instead of rewriting it. Create `apps/web/package.json` named `@trip-map/web` with exact scripts `dev: vite`, `build: vue-tsc --noEmit && vite build`, `test: vitest run`, and `typecheck: vue-tsc --noEmit`; copy the current Vue/Vite/Pinia/browser-facing dependencies into this package and add `@trip-map/contracts: workspace:*`. Create `apps/web/tsconfig.json` extending `../../tsconfig.base.json`. Move the current `index.html`, `vite.config.ts`, `vitest.config.ts`, `src/`, static assets, and test files into `apps/web`, updating any relative import paths that break because the app is no longer rooted at `/Users/huangjingping/i/trip-map`. Keep the existing `App.vue -> WorldMapStage -> popup/drawer/store` structure unchanged, and keep the current Phase 10 semantic hooks and storage warning behavior intact. After the move, the repo root must no longer be the runtime location of the web app; all web execution should happen through `apps/web` and root turbo scripts.</action>
  <verify>
    <automated>pnpm --filter @trip-map/web test && pnpm --filter @trip-map/web build && pnpm --filter @trip-map/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` contains `"name": "@trip-map/web"`
    - `apps/web/package.json` contains `"dev": "vite"`
    - `apps/web/package.json` contains `"build": "vue-tsc --noEmit && vite build"`
    - `apps/web/package.json` contains `"test": "vitest run"`
    - `apps/web/package.json` contains `"typecheck": "vue-tsc --noEmit"`
    - `apps/web/package.json` contains `"@trip-map/contracts": "workspace:*"`
    - `apps/web/tsconfig.json` contains `"extends": "../../tsconfig.base.json"`
    - `apps/web/src/App.vue` contains `WorldMapStage`
    - `apps/web/src/main.ts` contains `createApp(App).use(createPinia()).mount('#app')`
    - `apps/web/vitest.config.ts` contains `happy-dom`
  </acceptance_criteria>
  <done>现有 web 应用已经在 `apps/web` 下成为独立 package，并保持当前地图/popup/drawer 主链路行为与测试覆盖。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebase the web package onto workspace conventions while preserving current UI-only boundaries</name>
  <files>apps/web/package.json, apps/web/vite.config.ts, apps/web/vitest.config.ts, apps/web/src/App.spec.ts, apps/web/src/components/**/*.spec.ts, apps/web/src/services/**/*.spec.ts</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-CONTEXT.md, package.json, turbo.json, packages/contracts/package.json, apps/web/package.json, apps/web/vite.config.ts, apps/web/vitest.config.ts, apps/web/src/App.spec.ts</read_first>
  <behavior>
    - Test 1: The moved web package still runs the existing app/component/service specs from its new location.
    - Test 2: The web package depends on `@trip-map/contracts` by workspace reference, but no Pinia/store logic is moved into contracts.
    - Test 3: Root `pnpm build`, `pnpm test`, and `pnpm typecheck` can target the web app through turbo instead of root-local Vite scripts.
  </behavior>
  <action>Keep this plan scoped to ARC-01 rather than Phase 12/15 semantics. Update `apps/web/vite.config.ts` and `apps/web/vitest.config.ts` so they resolve correctly from the new directory and continue to include `src/**/*.spec.ts`. Repoint any remaining spec imports or helper imports that still assume the repo root `src/` location. Keep all UI state, `localStorage` access, popup/drawer logic, and map interaction code in `apps/web`; do not move any of that logic into `packages/contracts`. Ensure the web package is fully runnable through root turbo orchestration and package-local scripts, but do not add the backend smoke UI in this plan yet. The only contracts-related change in this plan should be the workspace dependency declaration that prepares plan `11-04` to import shared DTOs.</action>
  <verify>
    <automated>pnpm test --filter=@trip-map/web && pnpm typecheck --filter=@trip-map/web</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/vite.config.ts` contains `defineConfig`
    - `apps/web/vitest.config.ts` contains `include: ['src/**/*.spec.ts']`
    - `apps/web/package.json` contains `"@trip-map/contracts": "workspace:*"`
    - `rg -n '@trip-map/contracts' apps/web` returns at least one match in package/config/test setup after this task
    - `rg -n 'pinia|useMapPointsStore|localStorage' packages/contracts` returns no matches
    - `package.json` contains `turbo run test`
  </acceptance_criteria>
  <done>`apps/web` 已经完全接上 workspace 规范并为后续 server smoke path 做好依赖准备，同时保持当前 UI/Pinia/localStorage 边界不被 contracts 侵入。</done>
</task>

</tasks>

<verification>
- `pnpm --filter @trip-map/web test`
- `pnpm --filter @trip-map/web build`
- `pnpm --filter @trip-map/web typecheck`
- `pnpm test --filter=@trip-map/web`
- Confirm the moved web package, not repo root, owns `App.vue`, `main.ts`, and all existing specs
</verification>

<success_criteria>
- `apps/web` becomes the only runnable/buildable/testable location of the current Vue map app
- Current v2.0 UI behavior remains intact after the move
- The web package is ready to import shared contracts without leaking UI logic into `packages/contracts`
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-02-SUMMARY.md`
</output>

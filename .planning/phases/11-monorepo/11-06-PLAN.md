---
phase: 11-monorepo
plan: 06
type: execute
wave: 3
depends_on:
  - 11-03
files_modified:
  - apps/server/.env.example
  - apps/server/src/prisma/prisma.module.ts
  - apps/server/src/prisma/prisma.service.ts
  - apps/server/src/modules/records/records.service.ts
  - apps/server/src/modules/records/records.repository.ts
  - apps/server/test/records-smoke.e2e-spec.ts
  - apps/server/prisma.config.ts
  - apps/server/prisma/schema.prisma
  - apps/server/prisma/migrations/**/migration.sql
autonomous: true
requirements:
  - ARC-03
  - API-04
user_setup:
  - "Provide `apps/server/.env` with `DATABASE_URL`, `DIRECT_URL`, and, if Prisma `migrate dev` needs it, `SHADOW_DATABASE_URL`, using a Supabase-hosted PostgreSQL connection string or any other PostgreSQL-compatible instance."
must_haves:
  truths:
    - "`POST /records/smoke` 通过 Prisma 真实读写 PostgreSQL，并返回与请求一致的 canonical 字段名。"
    - "server 持久化层只依赖标准 PostgreSQL 连接串、Prisma schema 与 migrations，不绑定 Supabase 私有平台能力。"
    - "环境说明、migration 与 DB-backed smoke test 足以证明 Phase 11 的最小真实持久化链路已经成立。"
  artifacts:
    - path: "apps/server/prisma/schema.prisma"
      provides: "portable PostgreSQL datasource and smoke record model"
      contains: "provider  = \"postgresql\""
    - path: "apps/server/.env.example"
      provides: "portable database env contract"
      contains: "DATABASE_URL="
    - path: "apps/server/test/records-smoke.e2e-spec.ts"
      provides: "DB-backed canonical-field round-trip verification"
      contains: "phase11-smoke-v1"
  key_links:
    - from: "apps/server/src/modules/records/records.repository.ts"
      to: "apps/server/prisma/schema.prisma"
      via: "repository persists the smoke record model via Prisma"
      pattern: "prisma\\.smokeRecord"
    - from: "apps/server/src/modules/records/records.service.ts"
      to: "apps/server/src/modules/records/records.repository.ts"
      via: "service maps Prisma rows back to `SmokeRecordResponse`"
      pattern: "createSmokeRecord"
    - from: "apps/server/test/records-smoke.e2e-spec.ts"
      to: "packages/contracts/src/fixtures.ts"
      via: "DB smoke test posts the shared request fixture"
      pattern: "PHASE11_SMOKE_RECORD_REQUEST"
---

<objective>
为 `apps/server` 接上可移植的 Prisma/PostgreSQL 持久化、migration 与 DB-backed records smoke 测试。

Purpose: checker 已要求把 server bootstrap 与 Prisma 持久化拆开。这份计划只处理数据库层、环境说明与 smoke persistence，不重做 NestJS scaffold 或 web smoke UI。
Output: Prisma datasource/schema/migration、`.env.example`、repository wiring，以及真实 DB-backed `records-smoke.e2e-spec.ts`。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/11-monorepo/11-03-PLAN.md
@packages/contracts/src/records.ts
@packages/contracts/src/fixtures.ts
@apps/server/src/modules/records/records.controller.ts
@apps/server/src/modules/records/records.service.ts

<interfaces>
From `packages/contracts/src/records.ts`:
```ts
export interface SmokeRecordCreateRequest extends CanonicalPlaceRef {
  displayName: string
  note?: string
}
```

Shared fixture:
```ts
export const PHASE11_SMOKE_RECORD_REQUEST = {
  placeId: 'phase11-demo-place',
  boundaryId: 'phase11-demo-boundary',
  placeKind: 'OVERSEAS_ADMIN1',
  datasetVersion: 'phase11-smoke-v1',
  displayName: 'Phase 11 Demo Place',
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add portable Prisma persistence and DB-backed smoke verification</name>
  <files>apps/server/.env.example, apps/server/src/prisma/prisma.module.ts, apps/server/src/prisma/prisma.service.ts, apps/server/src/modules/records/records.service.ts, apps/server/src/modules/records/records.repository.ts, apps/server/test/records-smoke.e2e-spec.ts, apps/server/prisma.config.ts, apps/server/prisma/schema.prisma, apps/server/prisma/migrations/**/migration.sql</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-CONTEXT.md, .planning/phases/11-monorepo/11-RESEARCH.md, .planning/phases/11-monorepo/11-03-PLAN.md, apps/server/src/modules/records/records.controller.ts, apps/server/src/modules/records/records.service.ts, packages/contracts/src/records.ts, packages/contracts/src/fixtures.ts</read_first>
  <behavior>
    - Test 1: The smoke record route persists one row in PostgreSQL and returns the same `placeId`, `boundaryId`, `placeKind`, and `datasetVersion` it received.
    - Test 2: Prisma uses only standard PostgreSQL datasource URLs and migrations; there is no Supabase SDK, Redis, BullMQ, PostGIS, or object storage dependency.
    - Test 3: `.env.example` documents the exact DB variables needed for the records smoke path.
  </behavior>
  <action>Per D-07, D-08, D-09, D-10, D-11, and D-12, keep persistence portable and intentionally minimal. Create `apps/server/prisma/schema.prisma` with datasource `db` using exact `provider  = "postgresql"`, `url = env("DATABASE_URL")`, and `directUrl = env("DIRECT_URL")`; do not add extensions, PostGIS types, RLS assumptions, or Supabase-specific helpers. Add a single model named `SmokeRecord` with exact columns `id String @id @default(cuid())`, `placeId String`, `boundaryId String`, `placeKind String`, `datasetVersion String`, `displayName String`, `note String?`, `createdAt DateTime @default(now())`, and `updatedAt DateTime @updatedAt`. Create `prisma.config.ts` that points to `prisma/schema.prisma` and `prisma/migrations`. Add `PrismaModule` / `PrismaService`, then replace the contract-only records service implementation with repository-backed persistence that maps Prisma rows back to `SmokeRecordResponse` without renaming any canonical field. Add an initial migration SQL file under `apps/server/prisma/migrations/**/migration.sql`. Create `apps/server/.env.example` with exact keys `DATABASE_URL=`, `DIRECT_URL=`, and `SHADOW_DATABASE_URL=` plus a short comment that the connection should come from a PostgreSQL-compatible database and not use Supabase platform APIs. Add `records-smoke.e2e-spec.ts` that uses the shared `PHASE11_SMOKE_RECORD_REQUEST` fixture, posts it to `/records/smoke`, expects HTTP `201`, and asserts the response body echoes `placeId`, `boundaryId`, `placeKind`, `datasetVersion`, and `displayName`. Do not add Redis, BullMQ, `@supabase/supabase-js`, object-storage SDKs, background jobs, or extra records CRUD routes in this plan.</action>
  <verify>
    <automated>pnpm -C apps/server test -- test/records-smoke.e2e-spec.ts && pnpm -C apps/server exec prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/prisma/schema.prisma` contains `provider  = "postgresql"`
    - `apps/server/prisma/schema.prisma` contains `url       = env("DATABASE_URL")`
    - `apps/server/prisma/schema.prisma` contains `directUrl = env("DIRECT_URL")`
    - `apps/server/prisma/schema.prisma` contains `model SmokeRecord`
    - `apps/server/prisma/schema.prisma` contains `placeId String`
    - `apps/server/prisma/schema.prisma` contains `datasetVersion String`
    - `apps/server/.env.example` contains `DATABASE_URL=`
    - `apps/server/.env.example` contains `DIRECT_URL=`
    - `apps/server/.env.example` contains `SHADOW_DATABASE_URL=`
    - `apps/server/test/records-smoke.e2e-spec.ts` contains `phase11-demo-place`
    - `rg -n '@supabase/supabase-js|redis|bullmq|postgis|s3|r2' apps/server` returns no matches
  </acceptance_criteria>
  <done>server 的 smoke 路由已经通过 Prisma 和标准 PostgreSQL 真实持久化，且环境说明与测试清楚地限制在普通数据库能力范围内。</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/server test -- test/records-smoke.e2e-spec.ts`
- `pnpm -C apps/server exec prisma validate`
- `rg -n 'provider  = "postgresql"|Post\\('"'smoke'"'\\)|DATABASE_URL|DIRECT_URL|SHADOW_DATABASE_URL' apps/server`
- Confirm `rg -n '@supabase/supabase-js|redis|bullmq|postgis|object storage|s3|r2' apps/server` returns no matches
</verification>

<success_criteria>
- The records smoke route uses real PostgreSQL persistence through Prisma and preserves canonical field names end to end
- The server persistence baseline remains portable across PostgreSQL-compatible providers
- No Supabase-specific platform coupling or extra infrastructure leaks into Phase 11
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-06-SUMMARY.md`
</output>

---
phase: 11-monorepo
plan: 06
type: execute
wave: 3
depends_on:
  - 11-03
files_modified:
  - apps/server/package.json
  - pnpm-lock.yaml
  - apps/server/.env.example
  - apps/server/src/app.module.ts
  - apps/server/src/prisma/prisma.module.ts
  - apps/server/src/prisma/prisma.service.ts
  - apps/server/src/modules/records/records.module.ts
  - apps/server/src/modules/records/records.service.ts
  - apps/server/src/modules/records/records.repository.ts
  - apps/server/test/records-smoke.e2e-spec.ts
  - apps/server/prisma.config.ts
  - apps/server/prisma/schema.prisma
  - apps/server/prisma/migrations/**/migration.sql
autonomous: true
requirements:
  - ARC-03
  - API-04
user_setup:
  - "Copy `apps/server/.env.example` to `apps/server/.env` and fill `DATABASE_URL`, `DIRECT_URL`, and `SHADOW_DATABASE_URL` with a Supabase-hosted PostgreSQL connection string or any other PostgreSQL-compatible instance before running the DB-backed smoke verification."
must_haves:
  truths:
    - "`apps/server` workspace 明确声明 `prisma` 与 `@prisma/client` 依赖、相关脚本和 lockfile 变更，Prisma 安装面不再隐含在 Phase 11 之外。"
    - "`POST /records/smoke` 通过 `AppModule -> RecordsModule -> PrismaModule -> RecordsRepository -> RecordsService` 的 Nest DI 链路真实读写 PostgreSQL，并返回与请求一致的 canonical 字段名。"
    - "环境说明、`prisma generate`、migration apply 与 DB-backed smoke test 足以证明 Phase 11 的最小真实持久化链路已经成立。"
  artifacts:
    - path: "apps/server/package.json"
      provides: "Prisma dependency and script surface for the server workspace"
      contains: "@prisma/client"
    - path: "apps/server/src/modules/records/records.module.ts"
      provides: "records DI wiring that imports PrismaModule and registers repository/service"
      contains: "imports: [PrismaModule]"
    - path: "apps/server/prisma/schema.prisma"
      provides: "portable PostgreSQL datasource and smoke record model"
      contains: "provider  = \"postgresql\""
    - path: "apps/server/test/records-smoke.e2e-spec.ts"
      provides: "DB-backed canonical-field round-trip verification"
      contains: "phase11-smoke-v1"
  key_links:
    - from: "apps/server/src/app.module.ts"
      to: "apps/server/src/modules/records/records.module.ts"
      via: "Nest app imports the records module so `/records/smoke` reaches the wired persistence chain"
      pattern: "RecordsModule"
    - from: "apps/server/src/modules/records/records.module.ts"
      to: "apps/server/src/prisma/prisma.module.ts"
      via: "records module imports PrismaModule and provides repository/service"
      pattern: "PrismaModule"
    - from: "apps/server/src/modules/records/records.repository.ts"
      to: "apps/server/prisma/schema.prisma"
      via: "repository persists the smoke record model via Prisma"
      pattern: "prisma\\.smokeRecord"
    - from: "apps/server/test/records-smoke.e2e-spec.ts"
      to: "packages/contracts/src/fixtures.ts"
      via: "DB smoke test posts the shared request fixture after generate + migrate apply"
      pattern: "PHASE11_SMOKE_RECORD_REQUEST"
---

<objective>
为 `apps/server` 接上可移植的 Prisma/PostgreSQL 持久化、明确的 Prisma 依赖安装面，以及 DB-backed records smoke 验证。

Purpose: checker 已要求把 Prisma 依赖、Nest DI wiring 与 migration/apply 验证补齐。这份计划专门完成数据库层、环境说明与 smoke persistence，不回退到重新设计 server scaffold 或扩大到 Phase 12-15 范围。
Output: Prisma dependency/scripts + lockfile、datasource/schema/migration、`PrismaModule`/`RecordsModule` wiring，以及真实 DB-backed `records-smoke.e2e-spec.ts`。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/11-monorepo/11-03-PLAN.md
@packages/contracts/src/records.ts
@packages/contracts/src/fixtures.ts
@apps/server/package.json
@apps/server/src/app.module.ts
@apps/server/src/modules/records/records.module.ts
@apps/server/src/modules/records/records.controller.ts
@apps/server/src/modules/records/records.service.ts

<interfaces>
From `packages/contracts/src/records.ts`:
```ts
export interface SmokeRecordCreateRequest extends CanonicalPlaceRef {
  displayName: string
  note?: string
}
```

Shared fixture:
```ts
export const PHASE11_SMOKE_RECORD_REQUEST = {
  placeId: 'phase11-demo-place',
  boundaryId: 'phase11-demo-boundary',
  placeKind: 'OVERSEAS_ADMIN1',
  datasetVersion: 'phase11-smoke-v1',
  displayName: 'Phase 11 Demo Place',
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Prisma dependency surface, Nest DI wiring, and DB-backed smoke verification</name>
  <files>apps/server/package.json, pnpm-lock.yaml, apps/server/.env.example, apps/server/src/app.module.ts, apps/server/src/prisma/prisma.module.ts, apps/server/src/prisma/prisma.service.ts, apps/server/src/modules/records/records.module.ts, apps/server/src/modules/records/records.service.ts, apps/server/src/modules/records/records.repository.ts, apps/server/test/records-smoke.e2e-spec.ts, apps/server/prisma.config.ts, apps/server/prisma/schema.prisma, apps/server/prisma/migrations/**/migration.sql</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-CONTEXT.md, .planning/phases/11-monorepo/11-RESEARCH.md, .planning/phases/11-monorepo/11-03-PLAN.md, apps/server/package.json, apps/server/src/app.module.ts, apps/server/src/modules/records/records.module.ts, apps/server/src/modules/records/records.controller.ts, apps/server/src/modules/records/records.service.ts, packages/contracts/src/records.ts, packages/contracts/src/fixtures.ts</read_first>
  <behavior>
    - Test 1: The smoke record route persists one row in PostgreSQL and returns the same `placeId`, `boundaryId`, `placeKind`, and `datasetVersion` it received.
    - Test 2: `apps/server/package.json` and `pnpm-lock.yaml` explicitly include Prisma runtime/CLI dependencies and server scripts for generate + migrate apply.
    - Test 3: The Nest DI path is explicit: `AppModule` imports `RecordsModule`, `RecordsModule` imports `PrismaModule`, `RecordsRepository` uses `PrismaService`, and `RecordsService` maps the persisted row back to `SmokeRecordResponse`.
    - Test 4: Prisma uses only standard PostgreSQL datasource URLs and migrations; there is no Supabase SDK, Redis, BullMQ, PostGIS, or object storage dependency.
  </behavior>
  <action>Per D-07, D-08, D-09, D-10, D-11, and D-12, keep persistence portable and intentionally minimal. Update `apps/server/package.json` to add explicit dependencies `@prisma/client` and dev dependency `prisma`, plus exact scripts `prisma:generate`, `prisma:migrate:deploy`, and `prisma:validate` that target `prisma/schema.prisma`; include the resulting `pnpm-lock.yaml` change in this plan because `11-03` intentionally excluded Prisma. Create `apps/server/prisma/schema.prisma` with datasource `db` using exact `provider  = "postgresql"`, `url = env("DATABASE_URL")`, and `directUrl = env("DIRECT_URL")`; do not add extensions, PostGIS types, RLS assumptions, or Supabase-specific helpers. Add a single model named `SmokeRecord` with exact columns `id String @id @default(cuid())`, `placeId String`, `boundaryId String`, `placeKind String`, `datasetVersion String`, `displayName String`, `note String?`, `createdAt DateTime @default(now())`, and `updatedAt DateTime @updatedAt`. Create `prisma.config.ts` that points to `prisma/schema.prisma` and `prisma/migrations`. Add `PrismaModule` / `PrismaService`, update `apps/server/src/app.module.ts` to import `RecordsModule`, and update `apps/server/src/modules/records/records.module.ts` so it imports `PrismaModule` and wires `RecordsRepository` plus `RecordsService` together explicitly. Replace the contract-only records service implementation with repository-backed persistence that maps Prisma rows back to `SmokeRecordResponse` without renaming any canonical field. Add an initial migration SQL file under `apps/server/prisma/migrations/**/migration.sql`. Create `apps/server/.env.example` with exact keys `DATABASE_URL=`, `DIRECT_URL=`, and `SHADOW_DATABASE_URL=` plus a short comment that the connection should come from a PostgreSQL-compatible database and not use Supabase platform APIs. Add `records-smoke.e2e-spec.ts` that uses the shared `PHASE11_SMOKE_RECORD_REQUEST` fixture, posts it to `/records/smoke`, expects HTTP `201`, and asserts the response body echoes `placeId`, `boundaryId`, `placeKind`, `datasetVersion`, and `displayName`. Do not add Redis, BullMQ, `@supabase/supabase-js`, object-storage SDKs, background jobs, extra records CRUD routes, or Phase 12 canonical resolve behavior in this plan.</action>
  <verify>
    <manual_prep>Copy `apps/server/.env.example` to `apps/server/.env`, fill the PostgreSQL URLs, and ensure the target database is reachable before running the automated steps.</manual_prep>
    <automated>pnpm -C apps/server exec prisma generate --schema prisma/schema.prisma && pnpm -C apps/server exec prisma migrate deploy --schema prisma/schema.prisma && pnpm -C apps/server test -- test/records-smoke.e2e-spec.ts && pnpm -C apps/server exec prisma validate --schema prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/package.json` contains `@prisma/client`
    - `apps/server/package.json` contains `"prisma:generate"`
    - `apps/server/package.json` contains `"prisma:migrate:deploy"`
    - `pnpm-lock.yaml` contains `@prisma/client`
    - `apps/server/src/app.module.ts` contains `RecordsModule`
    - `apps/server/src/modules/records/records.module.ts` contains `imports: [PrismaModule]`
    - `apps/server/src/modules/records/records.module.ts` contains `RecordsRepository`
    - `apps/server/prisma/schema.prisma` contains `provider  = "postgresql"`
    - `apps/server/prisma/schema.prisma` contains `url       = env("DATABASE_URL")`
    - `apps/server/prisma/schema.prisma` contains `directUrl = env("DIRECT_URL")`
    - `apps/server/prisma/schema.prisma` contains `model SmokeRecord`
    - `apps/server/.env.example` contains `DATABASE_URL=`
    - `apps/server/.env.example` contains `DIRECT_URL=`
    - `apps/server/.env.example` contains `SHADOW_DATABASE_URL=`
    - `apps/server/test/records-smoke.e2e-spec.ts` contains `phase11-demo-place`
    - `rg -n '@supabase/supabase-js|redis|bullmq|postgis|s3|r2' apps/server` returns no matches
  </acceptance_criteria>
  <done>server 的 smoke 路由已经通过 Prisma 与标准 PostgreSQL 真实持久化，Prisma 依赖/脚本/lockfile 与 DI wiring 都在 Phase 11 计划内显式固定。</done>
</task>

</tasks>

<verification>
- Populate `apps/server/.env` from `apps/server/.env.example`
- `pnpm -C apps/server exec prisma generate --schema prisma/schema.prisma`
- `pnpm -C apps/server exec prisma migrate deploy --schema prisma/schema.prisma`
- `pnpm -C apps/server test -- test/records-smoke.e2e-spec.ts`
- `pnpm -C apps/server exec prisma validate --schema prisma/schema.prisma`
- `rg -n 'provider  = "postgresql"|DATABASE_URL|DIRECT_URL|SHADOW_DATABASE_URL|RecordsModule|PrismaModule|@prisma/client' apps/server pnpm-lock.yaml`
- Confirm `rg -n '@supabase/supabase-js|redis|bullmq|postgis|object storage|s3|r2' apps/server` returns no matches
</verification>

<success_criteria>
- The records smoke route uses real PostgreSQL persistence through Prisma and preserves canonical field names end to end
- The server workspace explicitly owns Prisma dependencies, scripts, and DI wiring needed for the smoke path
- No Supabase-specific platform coupling or extra infrastructure leaks into Phase 11
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-06-SUMMARY.md`
</output>

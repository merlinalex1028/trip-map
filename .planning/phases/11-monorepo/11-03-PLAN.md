---
phase: 11-monorepo
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - apps/server/package.json
  - apps/server/tsconfig.json
  - apps/server/tsconfig.build.json
  - apps/server/vitest.config.ts
  - apps/server/src/main.ts
  - apps/server/src/app.module.ts
  - apps/server/src/health/health.module.ts
  - apps/server/src/health/health.controller.ts
  - apps/server/src/modules/records/dto/create-smoke-record.dto.ts
  - apps/server/src/modules/records/records.module.ts
  - apps/server/src/modules/records/records.controller.ts
  - apps/server/src/modules/records/records.service.ts
  - apps/server/test/health.e2e-spec.ts
  - apps/server/test/records-contract.e2e-spec.ts
autonomous: true
requirements:
  - ARC-01
  - ARC-04
user_setup: []
must_haves:
  truths:
    - "`apps/server` 已经作为独立的 NestJS workspace package 启动，并提供 `GET /health` 与 contract-backed 的 `POST /records/smoke` 入口。"
    - "server 侧请求校验使用 Nest `ValidationPipe` 和本地 DTO 类实现 shared contracts，而不是把 Nest/runtime 依赖塞进 `packages/contracts`。"
    - "在接入 Prisma 前，records 路由边界、模块拆分和 health 契约已经固定，后续持久化计划只需要替换 service/repository 实现。"
  artifacts:
    - path: "apps/server/src/main.ts"
      provides: "NestJS bootstrap with ValidationPipe"
      contains: "ValidationPipe"
    - path: "apps/server/src/modules/records/records.controller.ts"
      provides: "records smoke HTTP entry"
      contains: "Post('smoke')"
    - path: "apps/server/test/records-contract.e2e-spec.ts"
      provides: "contract and validation coverage before persistence"
      contains: "forbidNonWhitelisted"
  key_links:
    - from: "apps/server/src/modules/records/records.controller.ts"
      to: "packages/contracts/src/records.ts"
      via: "server route validates and returns shared smoke DTO fields"
      pattern: "SmokeRecordCreateRequest"
    - from: "apps/server/src/health/health.controller.ts"
      to: "packages/contracts/src/health.ts"
      via: "health payload returns shared contracts version"
      pattern: "PHASE11_CONTRACTS_VERSION"
    - from: "apps/server/src/main.ts"
      to: "apps/server/src/modules/records/dto/create-smoke-record.dto.ts"
      via: "global ValidationPipe guards records input at the app boundary"
      pattern: "forbidNonWhitelisted"
---

<objective>
先建立 `apps/server` 的 NestJS package、health 入口和 contract-backed records 路由骨架，再把 Prisma 持久化拆到下一份计划。

Purpose: checker 已明确指出原 `11-03` 把 server scaffold、Prisma、migration 和 DB-backed e2e 捆成一个超预算 plan。这个修订版只锁住 server 的框架边界、health 合同和 records contract layer，把数据库持久化与 migration 留给后续专门计划。
Output: `apps/server` package、NestJS bootstrap/module/controller/service 骨架，以及 health/records contract tests。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@packages/contracts/src/place.ts
@packages/contracts/src/records.ts
@packages/contracts/src/health.ts

<interfaces>
From `packages/contracts/src/place.ts`:
```ts
export interface CanonicalPlaceRef {
  placeId: string
  boundaryId: string
  placeKind: PlaceKind
  datasetVersion: string
}
```

From `packages/contracts/src/records.ts`:
```ts
export interface SmokeRecordCreateRequest extends CanonicalPlaceRef {
  displayName: string
  note?: string
}
```

Target server routes:
```ts
GET /health -> HealthStatusResponse
POST /records/smoke -> contract-backed validation boundary before persistence lands
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold the NestJS server package, health route, and contract-backed records boundary</name>
  <files>apps/server/package.json, apps/server/tsconfig.json, apps/server/tsconfig.build.json, apps/server/vitest.config.ts, apps/server/src/main.ts, apps/server/src/app.module.ts, apps/server/src/health/health.module.ts, apps/server/src/health/health.controller.ts, apps/server/src/modules/records/dto/create-smoke-record.dto.ts, apps/server/src/modules/records/records.module.ts, apps/server/src/modules/records/records.controller.ts, apps/server/src/modules/records/records.service.ts, apps/server/test/health.e2e-spec.ts, apps/server/test/records-contract.e2e-spec.ts</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-CONTEXT.md, .planning/phases/11-monorepo/11-RESEARCH.md, packages/contracts/src/place.ts, packages/contracts/src/records.ts, packages/contracts/src/health.ts</read_first>
  <behavior>
    - Test 1: `GET /health` returns `status: 'ok'`, `service: 'server'`, and `contractsVersion: 'phase11-v1'`.
    - Test 2: `POST /records/smoke` accepts the shared canonical place fields and rejects unknown fields through Nest `ValidationPipe`.
    - Test 3: The server package uses NestJS as the framework and Fastify only as the HTTP adapter, not as a framework replacement.
  </behavior>
  <action>Per D-01, D-03, D-04, D-05, D-06, and the checker split request, create `apps/server` as package `@trip-map/server` with exact scripts `dev: tsx watch src/main.ts`, `build: tsc -p tsconfig.build.json`, `test: vitest run`, and `typecheck: tsc --noEmit`. Install and wire `@nestjs/common`, `@nestjs/core`, `@nestjs/platform-fastify`, `@nestjs/config`, `class-transformer`, `class-validator`, `reflect-metadata`, `rxjs`, `tsx`, `@nestjs/testing`, `supertest`, and `vitest`; do not add Prisma or DB migration files in this plan. In `src/main.ts`, bootstrap with `NestFactory.create()` using `FastifyAdapter`, enable a global `ValidationPipe({ whitelist: true, transform: true, forbidNonWhitelisted: true })`, and listen on port `4000`. Create `AppModule`, `HealthModule`, and `RecordsModule`; in `health.controller.ts`, implement `GET /health` returning the shared `HealthStatusResponse` shape and `PHASE11_CONTRACTS_VERSION`. In `modules/records/dto/create-smoke-record.dto.ts`, define a Nest DTO class implementing `SmokeRecordCreateRequest` with `@IsString()` on `placeId`, `boundaryId`, `datasetVersion`, and `displayName`, `@IsIn(['CN_CITY', 'OVERSEAS_ADMIN1'])` on `placeKind`, and optional `note`. Keep DTO classes inside `apps/server`; do not export Nest decorators through `packages/contracts`. Add `health.e2e-spec.ts` plus `records-contract.e2e-spec.ts` to assert the route contract, `ValidationPipe`, and canonical field names before persistence lands.</action>
  <verify>
    <automated>pnpm -C apps/server test -- test/health.e2e-spec.ts test/records-contract.e2e-spec.ts && pnpm -C apps/server typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/package.json` contains `"name": "@trip-map/server"`
    - `apps/server/package.json` contains `"dev": "tsx watch src/main.ts"`
    - `apps/server/src/main.ts` contains `ValidationPipe`
    - `apps/server/src/main.ts` contains `FastifyAdapter`
    - `apps/server/src/health/health.controller.ts` contains `Get()`
    - `apps/server/src/health/health.controller.ts` contains `PHASE11_CONTRACTS_VERSION`
    - `apps/server/src/modules/records/records.controller.ts` contains `Post('smoke')`
    - `apps/server/src/modules/records/dto/create-smoke-record.dto.ts` contains `@IsIn(['CN_CITY', 'OVERSEAS_ADMIN1'])`
    - `apps/server/test/health.e2e-spec.ts` contains `phase11-v1`
    - `apps/server/test/records-contract.e2e-spec.ts` contains `forbidNonWhitelisted`
  </acceptance_criteria>
  <done>`apps/server` 已成为可独立运行的 NestJS app，health 与 records contract 边界已固定，后续持久化只需要接入 Prisma 层而不必重做路由骨架。</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/server test -- test/health.e2e-spec.ts test/records-contract.e2e-spec.ts`
- `pnpm -C apps/server typecheck`
- `rg -n 'ValidationPipe|FastifyAdapter|Post\\('"'smoke'"'\\)|PHASE11_CONTRACTS_VERSION|forbidNonWhitelisted' apps/server`
- Confirm `rg -n '@supabase/supabase-js|prisma|redis|bullmq|postgis|object storage|s3|r2' apps/server/src apps/server/test` returns no matches
</verification>

<success_criteria>
- `apps/server` can boot independently and serve the shared-contract `health` and `records` HTTP surface
- Shared contract field names are enforced at the server boundary before any Prisma persistence work begins
- The server scaffold remains free of database/platform coupling until the dedicated persistence plan lands
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-03-SUMMARY.md`
</output>

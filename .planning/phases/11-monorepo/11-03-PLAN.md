---
phase: 11-monorepo
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - apps/server/package.json
  - apps/server/tsconfig.json
  - apps/server/tsconfig.build.json
  - apps/server/vitest.config.ts
  - apps/server/.env.example
  - apps/server/src/main.ts
  - apps/server/src/app.module.ts
  - apps/server/src/health/health.module.ts
  - apps/server/src/health/health.controller.ts
  - apps/server/src/prisma/prisma.module.ts
  - apps/server/src/prisma/prisma.service.ts
  - apps/server/src/modules/records/dto/create-smoke-record.dto.ts
  - apps/server/src/modules/records/records.module.ts
  - apps/server/src/modules/records/records.controller.ts
  - apps/server/src/modules/records/records.service.ts
  - apps/server/src/modules/records/records.repository.ts
  - apps/server/test/health.e2e-spec.ts
  - apps/server/test/records-smoke.e2e-spec.ts
  - apps/server/prisma.config.ts
  - apps/server/prisma/schema.prisma
  - apps/server/prisma/migrations/**/migration.sql
autonomous: true
requirements:
  - ARC-03
  - API-04
user_setup:
  - "Provide `apps/server/.env` with `DATABASE_URL`, `DIRECT_URL`, and, if Prisma `migrate dev` needs it, `SHADOW_DATABASE_URL`, using a Supabase-hosted PostgreSQL connection string or any other PostgreSQL-compatible instance."
must_haves:
  truths:
    - "`apps/server` 可以独立启动为 NestJS 服务，并提供 `GET /health` 与 `POST /records/smoke`。"
    - "records smoke 路由通过 Prisma 真实读写 PostgreSQL，而不是用内存假实现或 Supabase 专有 API。"
    - "server 基线不依赖 PostGIS、Redis、BullMQ、对象存储或 Supabase 平台能力。"
  artifacts:
    - path: "apps/server/src/main.ts"
      provides: "NestJS bootstrap with ValidationPipe"
      contains: "ValidationPipe"
    - path: "apps/server/src/modules/records/records.controller.ts"
      provides: "records smoke HTTP entry"
      contains: "Post('smoke')"
    - path: "apps/server/prisma/schema.prisma"
      provides: "portable PostgreSQL datasource and smoke record model"
      contains: "provider  = \"postgresql\""
    - path: "apps/server/test/records-smoke.e2e-spec.ts"
      provides: "DB-backed canonical-field round-trip verification"
      contains: "phase11-smoke-v1"
  key_links:
    - from: "apps/server/src/modules/records/records.controller.ts"
      to: "packages/contracts/src/records.ts"
      via: "server route validates and returns shared smoke DTO fields"
      pattern: "SmokeRecordCreateRequest"
    - from: "apps/server/src/modules/records/records.repository.ts"
      to: "apps/server/prisma/schema.prisma"
      via: "repository persists the smoke record model via Prisma"
      pattern: "prisma\\.smokeRecord"
    - from: "apps/server/src/health/health.controller.ts"
      to: "packages/contracts/src/health.ts"
      via: "health payload returns shared contracts version"
      pattern: "PHASE11_CONTRACTS_VERSION"
---

<objective>
建立 `apps/server` 的 NestJS + Prisma + PostgreSQL 基线，并让它能够用 shared contracts 跑通最小真实 records smoke 路由。

Purpose: Phase 11 不能停留在 monorepo 目录层级；必须把一个可独立运行、可测试、可迁移到任意 PostgreSQL 的服务端骨架做成事实，后续 canonical resolve 和 records API 才有稳定底座。
Output: `apps/server` package、NestJS module/controller/service/repository 骨架、Prisma schema/migration、`.env.example`，以及 DB-backed health/records e2e smoke tests。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@packages/contracts/src/place.ts
@packages/contracts/src/records.ts
@packages/contracts/src/health.ts
@packages/contracts/src/fixtures.ts

<interfaces>
From `packages/contracts/src/place.ts`:
```ts
export interface CanonicalPlaceRef {
  placeId: string
  boundaryId: string
  placeKind: PlaceKind
  datasetVersion: string
}
```

From `packages/contracts/src/records.ts`:
```ts
export interface SmokeRecordCreateRequest extends CanonicalPlaceRef {
  displayName: string
  note?: string
}
```

Target server routes:
```ts
GET /health -> HealthStatusResponse
POST /records/smoke -> 201 + SmokeRecordResponse
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold the NestJS server package and contract-backed HTTP routes</name>
  <files>apps/server/package.json, apps/server/tsconfig.json, apps/server/tsconfig.build.json, apps/server/vitest.config.ts, apps/server/src/main.ts, apps/server/src/app.module.ts, apps/server/src/health/health.module.ts, apps/server/src/health/health.controller.ts, apps/server/src/prisma/prisma.module.ts, apps/server/src/prisma/prisma.service.ts, apps/server/src/modules/records/dto/create-smoke-record.dto.ts, apps/server/src/modules/records/records.module.ts, apps/server/src/modules/records/records.controller.ts, apps/server/src/modules/records/records.service.ts, apps/server/test/health.e2e-spec.ts</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-CONTEXT.md, .planning/phases/11-monorepo/11-RESEARCH.md, packages/contracts/src/place.ts, packages/contracts/src/records.ts, packages/contracts/src/health.ts</read_first>
  <behavior>
    - Test 1: `GET /health` returns `status: 'ok'`, `service: 'server'`, and `contractsVersion: 'phase11-v1'`.
    - Test 2: `POST /records/smoke` accepts the shared canonical place fields and rejects unknown fields through Nest `ValidationPipe`.
    - Test 3: The server package uses NestJS as the framework and Fastify only as the HTTP adapter, not as a framework replacement.
  </behavior>
  <action>Per D-07, D-09, and the research recommendation, create `apps/server` as package `@trip-map/server` with exact scripts `dev: tsx watch src/main.ts`, `build: tsc -p tsconfig.build.json`, `test: vitest run`, `typecheck: tsc --noEmit`, `prisma:generate: prisma generate`, `prisma:migrate:dev: prisma migrate dev`, and `prisma:migrate:deploy: prisma migrate deploy`. Install and wire `@nestjs/common`, `@nestjs/core`, `@nestjs/platform-fastify`, `@nestjs/config`, `class-transformer`, `class-validator`, `reflect-metadata`, `rxjs`, `@prisma/client`, `prisma`, `tsx`, `@nestjs/testing`, `supertest`, and `vitest`. In `src/main.ts`, bootstrap with `NestFactory.create()` using `FastifyAdapter`, enable a global `ValidationPipe({ whitelist: true, transform: true, forbidNonWhitelisted: true })`, and listen on port `4000`. Create `AppModule`, `HealthModule`, and `RecordsModule`; in `health.controller.ts`, implement `GET /health` returning the shared `HealthStatusResponse` shape and `PHASE11_CONTRACTS_VERSION`. In `modules/records/dto/create-smoke-record.dto.ts`, define a Nest DTO class implementing `SmokeRecordCreateRequest` with `@IsString()` on `placeId`, `boundaryId`, `datasetVersion`, and `displayName`, `@IsIn(['CN_CITY', 'OVERSEAS_ADMIN1'])` on `placeKind`, and optional `note`. Keep DTO classes inside `apps/server`; do not export Nest decorators through `packages/contracts`. Add `health.e2e-spec.ts` to assert the route contract and validation behavior.</action>
  <verify>
    <automated>pnpm --filter @trip-map/server test -- apps/server/test/health.e2e-spec.ts && pnpm --filter @trip-map/server typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/package.json` contains `"name": "@trip-map/server"`
    - `apps/server/package.json` contains `"dev": "tsx watch src/main.ts"`
    - `apps/server/package.json` contains `"prisma:migrate:dev": "prisma migrate dev"`
    - `apps/server/src/main.ts` contains `ValidationPipe`
    - `apps/server/src/main.ts` contains `FastifyAdapter`
    - `apps/server/src/health/health.controller.ts` contains `Get()`
    - `apps/server/src/health/health.controller.ts` contains `PHASE11_CONTRACTS_VERSION`
    - `apps/server/src/modules/records/records.controller.ts` contains `Post('smoke')`
    - `apps/server/src/modules/records/dto/create-smoke-record.dto.ts` contains `@IsIn(['CN_CITY', 'OVERSEAS_ADMIN1'])`
    - `apps/server/test/health.e2e-spec.ts` contains `phase11-v1`
  </acceptance_criteria>
  <done>`apps/server` 已成为一个可独立运行的 NestJS app，并通过 shared contracts 暴露最小 health 和 records smoke 路由。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add portable Prisma persistence, migration scaffolding, and DB-backed smoke tests</name>
  <files>apps/server/.env.example, apps/server/src/modules/records/records.service.ts, apps/server/src/modules/records/records.repository.ts, apps/server/test/records-smoke.e2e-spec.ts, apps/server/prisma.config.ts, apps/server/prisma/schema.prisma, apps/server/prisma/migrations/**/migration.sql</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-CONTEXT.md, .planning/phases/11-monorepo/11-RESEARCH.md, apps/server/package.json, apps/server/src/modules/records/records.controller.ts, packages/contracts/src/records.ts, packages/contracts/src/fixtures.ts</read_first>
  <behavior>
    - Test 1: The smoke record route persists one row in PostgreSQL and returns the same `placeId`, `boundaryId`, `placeKind`, and `datasetVersion` it received.
    - Test 2: Prisma uses only standard PostgreSQL datasource URLs and migrations; there is no Supabase SDK, Redis, BullMQ, PostGIS, or object storage dependency.
    - Test 3: `.env.example` documents the exact DB variables needed for the records smoke path.
  </behavior>
  <action>Per D-07, D-08, D-09, D-10, D-11, and D-12, keep persistence portable and intentionally minimal. Create `apps/server/prisma/schema.prisma` with datasource `db` using exact `provider  = "postgresql"`, `url = env("DATABASE_URL")`, and `directUrl = env("DIRECT_URL")`; do not add extensions, PostGIS types, RLS assumptions, or Supabase-specific helpers. Add a single model named `SmokeRecord` with exact columns `id String @id @default(cuid())`, `placeId String`, `boundaryId String`, `placeKind String`, `datasetVersion String`, `displayName String`, `note String?`, `createdAt DateTime @default(now())`, and `updatedAt DateTime @updatedAt`. Create `prisma.config.ts` that points to `prisma/schema.prisma` and `prisma/migrations`. In `records.repository.ts`, persist and read this model through `PrismaService`; in `records.service.ts`, map Prisma rows back to `SmokeRecordResponse` without renaming any canonical field. Add an initial migration SQL file under `apps/server/prisma/migrations/**/migration.sql`. Create `apps/server/.env.example` with exact keys `DATABASE_URL=`, `DIRECT_URL=`, and `SHADOW_DATABASE_URL=` plus a short comment that the connection should come from a PostgreSQL-compatible database and not use Supabase platform APIs. Add `records-smoke.e2e-spec.ts` that uses the shared `PHASE11_SMOKE_RECORD_REQUEST` fixture, posts it to `/records/smoke`, expects HTTP `201`, and asserts the response body echoes `placeId`, `boundaryId`, `placeKind`, `datasetVersion`, and `displayName`. Do not add Redis, BullMQ, `@supabase/supabase-js`, object-storage SDKs, background jobs, or extra records CRUD routes in this plan.</action>
  <verify>
    <automated>pnpm --filter @trip-map/server test -- apps/server/test/records-smoke.e2e-spec.ts && pnpm --filter @trip-map/server exec prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/prisma/schema.prisma` contains `provider  = "postgresql"`
    - `apps/server/prisma/schema.prisma` contains `url       = env("DATABASE_URL")`
    - `apps/server/prisma/schema.prisma` contains `directUrl = env("DIRECT_URL")`
    - `apps/server/prisma/schema.prisma` contains `model SmokeRecord`
    - `apps/server/prisma/schema.prisma` contains `placeId String`
    - `apps/server/prisma/schema.prisma` contains `datasetVersion String`
    - `apps/server/.env.example` contains `DATABASE_URL=`
    - `apps/server/.env.example` contains `DIRECT_URL=`
    - `apps/server/.env.example` contains `SHADOW_DATABASE_URL=`
    - `apps/server/test/records-smoke.e2e-spec.ts` contains `phase11-demo-place`
    - `rg -n '@supabase/supabase-js|redis|bullmq|postgis|s3|r2' apps/server` returns no matches
  </acceptance_criteria>
  <done>server 的 smoke 路由已经通过 Prisma 和标准 PostgreSQL 真实持久化，且环境说明与测试清楚地限制在普通数据库能力范围内。</done>
</task>

</tasks>

<verification>
- `pnpm --filter @trip-map/server test`
- `pnpm --filter @trip-map/server typecheck`
- `pnpm --filter @trip-map/server exec prisma validate`
- `rg -n 'provider  = "postgresql"|Post\\('"'smoke'"'\\)|PHASE11_CONTRACTS_VERSION|DATABASE_URL|DIRECT_URL' apps/server`
- Confirm `rg -n '@supabase/supabase-js|redis|bullmq|postgis|object storage|s3|r2' apps/server` returns no matches
</verification>

<success_criteria>
- `apps/server` can boot independently and serve shared-contract HTTP routes
- The smoke record route uses real PostgreSQL persistence through Prisma and preserves canonical field names end to end
- The server baseline remains free of Supabase-specific platform coupling and extra infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-03-SUMMARY.md`
</output>

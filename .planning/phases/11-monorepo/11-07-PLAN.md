---
phase: 11-monorepo
plan: 07
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - apps/web/src/services/map-projection.ts
  - apps/web/src/services/point-storage.ts
  - apps/web/src/services/city-boundaries.ts
  - apps/web/src/services/geo-lookup.ts
  - apps/web/src/services/city-search.ts
  - apps/web/src/data/seed-points.ts
  - apps/web/src/data/preview-points.ts
  - apps/web/src/data/geo/city-candidates.ts
  - apps/web/src/data/geo/city-boundaries.geo.json
  - apps/web/src/data/geo/country-regions.geo.json
  - apps/web/src/types/geo.ts
  - apps/web/src/types/map-point.ts
autonomous: true
requirements:
  - ARC-01
  - ARC-04
user_setup: []
must_haves:
  truths:
    - "支撑 web runtime 的 browser services、geo data 与 web-local types 已迁入 `apps/web`，并可以作为后续 runtime wiring 的 package-local来源。"
    - "关键字段语义仍由 `packages/contracts` 约束；本计划只迁移 web 内部 services/data/types，不把这些逻辑抽进 contracts。"
    - "styles、assets 与 UI regression specs 不在本计划内，会由后续专门计划继续迁移与收尾。"
  artifacts:
    - path: "apps/web/src/services/point-storage.ts"
      provides: "迁移后的浏览器存储服务"
      contains: "loadStoredPoints"
    - path: "apps/web/src/types/map-point.ts"
      provides: "迁移后的 web-only map point types"
      contains: "boundaryId"
    - path: "apps/web/src/data/geo/city-boundaries.geo.json"
      provides: "迁移后的静态 geo asset"
      contains: "\"FeatureCollection\""
  key_links:
    - from: "apps/web/src/services/point-storage.ts"
      to: "apps/web/src/types/map-point.ts"
      via: "browser storage service continues to serialize the moved map-point shape"
      pattern: "boundaryId"
    - from: "apps/web/src/services/city-boundaries.ts"
      to: "apps/web/src/data/geo/city-boundaries.geo.json"
      via: "boundary service keeps reading the package-local boundary dataset"
      pattern: "FeatureCollection"
    - from: "apps/web/src/services/geo-lookup.ts"
      to: "apps/web/src/types/geo.ts"
      via: "geo lookup continues to emit the moved geo type contracts"
      pattern: "datasetVersion"
---

<objective>
把 legacy root 下支撑 web runtime 的 services、data 与 types 迁入 `apps/web`。

Purpose: `11-05` 只迁 app shell 仍不足以完成 package-local 运行边界。这个补充计划单独承接 supporting services/data/types，避免把 styles/assets 和 runtime wiring 一次性揉成超大迁移单元。
Output: package-local 的 web services、geo/data files 与 web-local type definitions。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/11-monorepo/11-05-PLAN.md
@src/services/point-storage.ts
@src/services/geo-lookup.ts
@src/data/geo/city-boundaries.geo.json
@src/types/map-point.ts
@src/types/geo.ts

<interfaces>
From `src/services/point-storage.ts`:
```ts
export function loadStoredPoints(): StoredMapPoint[]
```

From `src/types/map-point.ts`:
```ts
boundaryId: string | null
boundaryDatasetVersion: string | null
```

From `src/types/geo.ts`:
```ts
datasetVersion: string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate supporting web services, geo data, and web-local types into `apps/web`</name>
  <files>apps/web/src/services/map-projection.ts, apps/web/src/services/point-storage.ts, apps/web/src/services/city-boundaries.ts, apps/web/src/services/geo-lookup.ts, apps/web/src/services/city-search.ts, apps/web/src/data/seed-points.ts, apps/web/src/data/preview-points.ts, apps/web/src/data/geo/city-candidates.ts, apps/web/src/data/geo/city-boundaries.geo.json, apps/web/src/data/geo/country-regions.geo.json, apps/web/src/types/geo.ts, apps/web/src/types/map-point.ts</files>
  <read_first>AGENTS.md, .planning/phases/11-monorepo/11-05-PLAN.md, src/services/map-projection.ts, src/services/point-storage.ts, src/services/city-boundaries.ts, src/services/geo-lookup.ts, src/services/city-search.ts, src/data/seed-points.ts, src/data/preview-points.ts, src/data/geo/city-candidates.ts, src/data/geo/city-boundaries.geo.json, src/data/geo/country-regions.geo.json, src/types/geo.ts, src/types/map-point.ts</read_first>
  <action>Per D-02, D-03, D-04, D-05, D-06, and D-13, move the supporting web-only services, local geo/data files, and web-local type definitions into `apps/web` after the package shell exists. Keep this plan intentionally narrow: copy or relocate `map-projection.ts`, `point-storage.ts`, `city-boundaries.ts`, `geo-lookup.ts`, `city-search.ts`, the current `seed-points.ts` / `preview-points.ts`, the three geo datasets, and `geo.ts` / `map-point.ts` into `apps/web`. Do not claim consumer rewiring in this plan; `apps/web` runtime files are allowed to keep temporary bridge imports until `11-09` updates them. Keep the canonical cross-app DTO boundary in `packages/contracts`; do not move Pinia/runtime business behavior into contracts, and do not introduce styles/assets, server calls, canonical resolve logic, or Phase 12-15 semantics while relocating these modules.</action>
  <verify>
    <automated>test -f apps/web/src/services/point-storage.ts && test -f apps/web/src/types/map-point.ts && rg -n 'loadStoredPoints|datasetVersion|FeatureCollection' apps/web/src/services apps/web/src/data apps/web/src/types</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/services/point-storage.ts` contains `loadStoredPoints`
    - `apps/web/src/services/geo-lookup.ts` exists
    - `apps/web/src/data/geo/city-boundaries.geo.json` exists
    - `apps/web/src/data/geo/country-regions.geo.json` exists
    - `apps/web/src/types/map-point.ts` contains `boundaryId`
    - `apps/web/src/types/geo.ts` contains `datasetVersion`
    - `rg -n 'pinia|defineStore|@nestjs|prisma' apps/web/src/services apps/web/src/data apps/web/src/types` returns no unexpected framework drift beyond existing web-local types
  </acceptance_criteria>
  <done>supporting services、data 与 types 已迁入 `apps/web`，后续只需要继续做 runtime rewiring、spec/test-helper 迁移，以及 styles/assets/bridge cleanup 收尾。</done>
</task>

</tasks>

<verification>
- `test -f apps/web/src/services/point-storage.ts`
- `test -f apps/web/src/types/map-point.ts`
- `rg -n 'loadStoredPoints|datasetVersion|FeatureCollection' apps/web/src/services apps/web/src/data apps/web/src/types`
- Confirm `packages/contracts` remains thin and does not absorb web-local services/data logic
</verification>

<success_criteria>
- `apps/web` owns the supporting services/data/types needed by the moved runtime shell
- Consumer rewiring is deferred explicitly to a later plan instead of being implied without files
- Thin-contract boundaries remain intact while web-only internals are relocated
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-07-SUMMARY.md`
</output>

---
phase: 11-monorepo
plan: 07
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - apps/web/src/services/map-projection.ts
  - apps/web/src/services/point-storage.ts
  - apps/web/src/services/city-boundaries.ts
  - apps/web/src/services/geo-lookup.ts
  - apps/web/src/services/city-search.ts
  - apps/web/src/data/seed-points.ts
  - apps/web/src/data/preview-points.ts
  - apps/web/src/data/geo/city-candidates.ts
  - apps/web/src/data/geo/city-boundaries.geo.json
  - apps/web/src/data/geo/country-regions.geo.json
  - apps/web/src/types/geo.ts
  - apps/web/src/types/map-point.ts
  - apps/web/src/styles/tokens.css
  - apps/web/src/styles/global.css
  - apps/web/src/assets/world-map.svg
  - src/services/map-projection.ts
  - src/services/point-storage.ts
  - src/services/city-boundaries.ts
  - src/services/geo-lookup.ts
  - src/services/city-search.ts
  - src/data/seed-points.ts
  - src/data/preview-points.ts
  - src/data/geo/city-candidates.ts
  - src/data/geo/city-boundaries.geo.json
  - src/data/geo/country-regions.geo.json
  - src/types/geo.ts
  - src/types/map-point.ts
  - src/styles/tokens.css
  - src/styles/global.css
  - src/assets/world-map.svg
autonomous: true
requirements:
  - ARC-01
  - ARC-04
user_setup: []
must_haves:
  truths:
    - "支撑 web app shell 的 browser services、geo data、types、styles 与 static asset 已迁入 `apps/web`，不再继续把 repo root 当成 active source-of-truth。"
    - "关键字段语义仍由 `packages/contracts` 约束；本计划只迁移 web 内部 domain/data 模块，不把这些逻辑抽进 contracts。"
    - "迁移后 `apps/web` 可以只依赖 package-local 的 services/data/types/assets 继续构建，为后续 spec 迁移与 bridge cleanup 清出边界。"
  artifacts:
    - path: "apps/web/src/services/point-storage.ts"
      provides: "迁移后的浏览器存储服务"
      contains: "loadStoredPoints"
    - path: "apps/web/src/types/map-point.ts"
      provides: "迁移后的 web-only map point types"
      contains: "boundaryId"
    - path: "apps/web/src/data/geo/city-boundaries.geo.json"
      provides: "迁移后的静态 geo asset"
      contains: "\"FeatureCollection\""
  key_links:
    - from: "apps/web/src/stores/map-points.ts"
      to: "apps/web/src/services/point-storage.ts"
      via: "moved Pinia runtime consumes the moved browser storage service"
      pattern: "point-storage"
    - from: "apps/web/src/components/WorldMapStage.vue"
      to: "apps/web/src/types/map-point.ts"
      via: "map stage continues to consume the web-local map point types"
      pattern: "MapPoint"
    - from: "apps/web/src/styles/global.css"
      to: "apps/web/src/assets/world-map.svg"
      via: "package-local styling/assets no longer rely on repo-root ownership"
      pattern: "world-map"
---

<objective>
把 legacy root 下支撑 web runtime 的 services、data、types、styles 与 asset 迁入 `apps/web`。

Purpose: `11-05` 只迁 app shell 仍不足以完全摆脱 repo root。这个补充计划单独承接 supporting modules，避免把 runtime shell 与 domain/data/assets 一次性揉成超大迁移单元。
Output: package-local 的 web services、geo/data files、types、styles 和静态资源。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/11-monorepo/11-05-PLAN.md
@src/services/point-storage.ts
@src/services/geo-lookup.ts
@src/data/geo/city-boundaries.geo.json
@src/types/map-point.ts
@src/types/geo.ts

<interfaces>
From `src/services/point-storage.ts`:
```ts
export function loadStoredPoints(): StoredMapPoint[]
```

From `src/types/map-point.ts`:
```ts
boundaryId: string | null
boundaryDatasetVersion: string | null
```

From `src/types/geo.ts`:
```ts
datasetVersion: string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate supporting web services, data, types, styles, and static assets into `apps/web`</name>
  <files>apps/web/src/services/map-projection.ts, apps/web/src/services/point-storage.ts, apps/web/src/services/city-boundaries.ts, apps/web/src/services/geo-lookup.ts, apps/web/src/services/city-search.ts, apps/web/src/data/seed-points.ts, apps/web/src/data/preview-points.ts, apps/web/src/data/geo/city-candidates.ts, apps/web/src/data/geo/city-boundaries.geo.json, apps/web/src/data/geo/country-regions.geo.json, apps/web/src/types/geo.ts, apps/web/src/types/map-point.ts, apps/web/src/styles/tokens.css, apps/web/src/styles/global.css, apps/web/src/assets/world-map.svg, src/services/map-projection.ts, src/services/point-storage.ts, src/services/city-boundaries.ts, src/services/geo-lookup.ts, src/services/city-search.ts, src/data/seed-points.ts, src/data/preview-points.ts, src/data/geo/city-candidates.ts, src/data/geo/city-boundaries.geo.json, src/data/geo/country-regions.geo.json, src/types/geo.ts, src/types/map-point.ts, src/styles/tokens.css, src/styles/global.css, src/assets/world-map.svg</files>
  <read_first>AGENTS.md, .planning/phases/11-monorepo/11-05-PLAN.md, src/services/map-projection.ts, src/services/point-storage.ts, src/services/city-boundaries.ts, src/services/geo-lookup.ts, src/services/city-search.ts, src/data/geo/city-candidates.ts, src/data/geo/city-boundaries.geo.json, src/data/geo/country-regions.geo.json, src/types/geo.ts, src/types/map-point.ts, src/styles/tokens.css, src/styles/global.css, src/assets/world-map.svg</read_first>
  <action>Per D-02, D-03, D-04, D-05, D-06, and D-13, move the supporting web-only domain modules into `apps/web` after the package shell exists. This includes browser-facing services, current local geo/data files, web-local type definitions, CSS tokens/global styles, and the static map asset. Update the package-local imports so the moved runtime from `11-05` can consume these files without reaching back into repo root. Keep the canonical cross-app DTO boundary in `packages/contracts`; do not move Pinia/runtime business behavior into contracts, and do not introduce server calls, canonical resolve logic, or Phase 12-15 semantics while relocating these web-local modules.</action>
  <verify>
    <automated>pnpm -C apps/web build && pnpm -C apps/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/services/point-storage.ts` contains `loadStoredPoints`
    - `apps/web/src/services/geo-lookup.ts` exists
    - `apps/web/src/data/geo/city-boundaries.geo.json` exists
    - `apps/web/src/data/geo/country-regions.geo.json` exists
    - `apps/web/src/types/map-point.ts` contains `boundaryId`
    - `apps/web/src/types/geo.ts` contains `datasetVersion`
    - `apps/web/src/styles/global.css` exists
    - `apps/web/src/assets/world-map.svg` exists
    - `rg -n 'pinia|defineStore|@nestjs|prisma' apps/web/src/services apps/web/src/data apps/web/src/types` returns no unexpected framework drift beyond existing web-local types
  </acceptance_criteria>
  <done>supporting services、data、types、styles 与 assets 已迁入 `apps/web`，后续只剩 specs/test-helper 与 bridge cleanup 收尾。</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web build`
- `pnpm -C apps/web typecheck`
- `rg -n 'loadStoredPoints|datasetVersion|world-map' apps/web/src`
- Confirm `packages/contracts` remains thin and does not absorb web-local services/data logic
</verification>

<success_criteria>
- `apps/web` owns the supporting modules needed by the moved runtime shell
- Repo root is no longer the active owner of web services/data/types/assets
- Thin-contract boundaries remain intact while web-only internals are relocated
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-07-SUMMARY.md`
</output>

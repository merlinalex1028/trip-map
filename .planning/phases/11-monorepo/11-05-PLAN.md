---
phase: 11-monorepo
plan: 05
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - apps/web/src/**/*
  - apps/web/src/**/*.spec.ts
  - apps/web/public/**/*
  - src/**/*
  - public/**/*
autonomous: true
requirements:
  - ARC-01
user_setup: []
must_haves:
  truths:
    - "现有 Vue 地图应用的 runtime source、assets 和 specs 都已经归属到 `apps/web`，不再由 repo root 持有。"
    - "Phase 7-10 已交付的地图、popup、drawer 与 localStorage 相关行为在迁移目录后不发生回归。"
    - "`apps/web` 早期建立的 temporary legacy bridge 已被移除，后续 smoke UI 变更只需要在 package 内完成。"
  artifacts:
    - path: "apps/web/src/App.vue"
      provides: "迁移后的应用壳层"
      contains: "WorldMapStage"
    - path: "apps/web/src/main.ts"
      provides: "迁移后的正式 bootstrap"
      contains: "createApp(App).use(createPinia()).mount('#app')"
    - path: "apps/web/src/App.spec.ts"
      provides: "迁移后的 app regression entry"
      contains: "App"
  key_links:
    - from: "apps/web/src/main.ts"
      to: "apps/web/src/App.vue"
      via: "web package bootstrap now mounts the moved Vue app directly"
      pattern: "createApp\\(App\\)"
    - from: "apps/web/src/App.vue"
      to: "apps/web/src/components/WorldMapStage.vue"
      via: "Phase 10 main experience remains wired into the app shell"
      pattern: "WorldMapStage"
    - from: "apps/web/src/App.spec.ts"
      to: "apps/web/src/App.vue"
      via: "package-local regression tests cover the moved app shell"
      pattern: "mount"
---

<objective>
把 legacy root 下的 web runtime/source/spec surface 真正迁入 `apps/web`，并移除 `11-02` 引入的过渡桥接。

Purpose: 原 `11-02` 被 checker 认定过大，必须把 package bootstrap 与大规模源码/spec 迁移拆开执行。这份计划专门完成源码迁移与回归稳定，不再混入 package scaffolding 或 backend smoke UI 工作。
Output: 完整归属到 `apps/web` 的 `src/`、assets 和 specs，以及移除后的 temporary legacy bridge。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/09-popup/09-popup-05-SUMMARY.md
@.planning/phases/10-可爱风格与可读性收口/10-03-SUMMARY.md
@.planning/phases/11-monorepo/11-02-PLAN.md
@src/App.vue
@src/main.ts
@src/components/WorldMapStage.vue
@src/components/PointPreviewDrawer.vue
@src/stores/map-points.ts
@src/services/point-storage.ts

<interfaces>
From `src/main.ts`:
```ts
createApp(App).use(createPinia()).mount('#app')
```

From `src/App.vue`:
```ts
mapPointsStore.bootstrapPoints()
```

From `src/services/point-storage.ts`:
```ts
export function loadStoredPoints(): StoredMapPoint[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the legacy web runtime source and assets into `apps/web`</name>
  <files>apps/web/src/**/*, apps/web/public/**/*, src/**/*, public/**/*</files>
  <read_first>AGENTS.md, .planning/phases/11-monorepo/11-02-PLAN.md, .planning/phases/09-popup/09-popup-05-SUMMARY.md, .planning/phases/10-可爱风格与可读性收口/10-03-SUMMARY.md, src/App.vue, src/main.ts, src/components/WorldMapStage.vue, src/components/PointPreviewDrawer.vue, src/stores/map-points.ts, src/services/point-storage.ts</read_first>
  <action>Per D-02, D-03, and D-13, move the actual runtime source tree, static assets, and browser-facing services from repo root into `apps/web` with minimal code churn. Preserve the current `App.vue -> WorldMapStage -> popup/drawer/store` structure, `localStorage` warning behavior, and Phase 10 semantic hooks exactly as they exist today. Update relative imports, aliases, and asset references so the code runs fully from `apps/web`. Remove the temporary `legacy-entry` bridge from `11-02` once the package-local runtime boots directly from moved files. Do not add backend smoke UI, server calls, or Phase 12-15 semantics in this plan.</action>
  <verify>
    <automated>pnpm -C apps/web build && pnpm -C apps/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/App.vue` contains `WorldMapStage`
    - `apps/web/src/main.ts` contains `createApp(App).use(createPinia()).mount('#app')`
    - `apps/web/src/components/PointPreviewDrawer.vue` exists
    - `apps/web/src/stores/map-points.ts` exists
    - `apps/web/src/services/point-storage.ts` exists
    - `rg -n 'legacy-entry' apps/web/src src` returns no matches after the move
  </acceptance_criteria>
  <done>web runtime source 与 assets 已经完整归属到 `apps/web`，并且不再依赖过渡桥接或 repo root runtime 入口。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rehome the spec surface and stabilize package-local regression coverage</name>
  <files>apps/web/src/**/*.spec.ts, apps/web/src/App.spec.ts, src/**/*.spec.ts</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-RESEARCH.md, apps/web/vitest.config.ts, src/App.spec.ts, src/components/**/*.spec.ts, src/services/**/*.spec.ts</read_first>
  <behavior>
    - Test 1: Existing app/component/service specs all run from `apps/web` without assuming repo-root `src/`.
    - Test 2: Package-local build, test, and typecheck succeed after the source move.
    - Test 3: No UI state, Pinia logic, or `localStorage` helpers are moved into `packages/contracts`.
  </behavior>
  <action>Move the current web spec files and any test helpers into `apps/web`, then repoint imports so the suite no longer assumes root-level `src/`. Keep this plan scoped to ARC-01 only: stabilize the moved test surface, confirm the package-local scripts work, and keep all UI state, `localStorage`, popup/drawer logic, and map interaction code inside `apps/web`. Do not introduce Playwright, backend smoke panel code, canonical resolve work, or any contracts expansion beyond using the existing workspace dependency declared in `11-02`.</action>
  <verify>
    <automated>pnpm -C apps/web test && pnpm -C apps/web build && pnpm -C apps/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/vitest.config.ts` contains `include: ['src/**/*.spec.ts']`
    - `apps/web/src/App.spec.ts` exists
    - `rg -n 'from .*/src/' apps/web/src/**/*.spec.ts` returns no matches that point back to repo-root source
    - `rg -n 'pinia|useMapPointsStore|localStorage' packages/contracts` returns no matches
  </acceptance_criteria>
  <done>`apps/web` 的回归测试面已经迁入 package 内并稳定运行，后续 smoke path 改动不再需要碰 repo root web 残留文件。</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web test`
- `pnpm -C apps/web build`
- `pnpm -C apps/web typecheck`
- `rg -n 'WorldMapStage|PointPreviewDrawer|bootstrapPoints|localStorage' apps/web/src`
- Confirm repo root no longer owns the active web runtime or spec files
</verification>

<success_criteria>
- The current Vue map app is fully runnable/buildable/testable from `apps/web`
- Existing v2.0 UI behavior survives the directory move without scope creep into later phases
- The temporary legacy bridge introduced in `11-02` is removed cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-05-SUMMARY.md`
</output>

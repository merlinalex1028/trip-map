---
phase: 11-monorepo
plan: 05
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - apps/web/src/App.vue
  - apps/web/src/components/WorldMapStage.vue
  - apps/web/src/components/PosterTitleBlock.vue
  - apps/web/src/components/PointPreviewDrawer.vue
  - apps/web/src/components/SeedMarkerLayer.vue
  - apps/web/src/components/map-popup/MapContextPopup.vue
  - apps/web/src/components/map-popup/PointSummaryCard.vue
  - apps/web/src/composables/usePopupAnchoring.ts
  - apps/web/src/stores/map-ui.ts
  - apps/web/src/stores/map-points.ts
  - src/App.vue
  - src/components/WorldMapStage.vue
  - src/components/PosterTitleBlock.vue
  - src/components/PointPreviewDrawer.vue
  - src/components/SeedMarkerLayer.vue
  - src/components/map-popup/MapContextPopup.vue
  - src/components/map-popup/PointSummaryCard.vue
  - src/composables/usePopupAnchoring.ts
  - src/stores/map-ui.ts
  - src/stores/map-points.ts
autonomous: true
requirements:
  - ARC-01
user_setup: []
must_haves:
  truths:
    - "现有 Vue 地图应用的 app shell、交互组件、Pinia store 与 popup/drawer 相关 composable 已经归属到 `apps/web`，不再由 repo root 持有这些 active runtime 模块。"
    - "Phase 7-10 已交付的地图、popup、drawer 与 store 协作链路在迁移目录后不发生回归。"
    - "本计划只完成 app runtime shell 的迁移，不夹带 specs、test helpers、Prisma、server smoke UI 或 Phase 12-15 语义。"
  artifacts:
    - path: "apps/web/src/App.vue"
      provides: "迁移后的应用壳层"
      contains: "WorldMapStage"
    - path: "apps/web/src/components/WorldMapStage.vue"
      provides: "迁移后的主地图舞台"
      contains: "PointPreviewDrawer"
    - path: "apps/web/src/stores/map-points.ts"
      provides: "迁移后的 map points store"
      contains: "bootstrapPoints"
  key_links:
    - from: "apps/web/src/App.vue"
      to: "apps/web/src/components/WorldMapStage.vue"
      via: "package-local app shell continues to mount the Phase 10 main map experience"
      pattern: "WorldMapStage"
    - from: "apps/web/src/components/WorldMapStage.vue"
      to: "apps/web/src/components/PointPreviewDrawer.vue"
      via: "main stage keeps the popup/drawer interaction chain after relocation"
      pattern: "PointPreviewDrawer"
    - from: "apps/web/src/stores/map-points.ts"
      to: "apps/web/src/App.vue"
      via: "app shell still bootstraps points through the moved store"
      pattern: "bootstrapPoints"
---

<objective>
把 legacy root 下的 app shell、交互组件与 Pinia/composable runtime 真正迁入 `apps/web`。

Purpose: checker 已指出原 `11-05` 同时吞下 runtime、specs、assets 与 bridge cleanup，规模过大。这份修订版只负责把当前用户可见的 app runtime shell 迁到 `apps/web`，为后续 domain/assets 迁移与 bridge cleanup 留出独立计划。
Output: 完整归属到 `apps/web` 的 `App.vue`、主要 Vue 组件、相关 composable 与 Pinia stores。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/09-popup/09-popup-05-SUMMARY.md
@.planning/phases/10-可爱风格与可读性收口/10-03-SUMMARY.md
@.planning/phases/11-monorepo/11-02-PLAN.md
@src/App.vue
@src/components/WorldMapStage.vue
@src/components/PointPreviewDrawer.vue
@src/components/map-popup/MapContextPopup.vue
@src/stores/map-points.ts
@src/composables/usePopupAnchoring.ts

<interfaces>
From `src/App.vue`:
```ts
mapPointsStore.bootstrapPoints()
```

From `src/components/WorldMapStage.vue`:
```ts
PointPreviewDrawer
MapContextPopup
```

From `src/stores/map-points.ts`:
```ts
export const useMapPointsStore = defineStore('map-points', ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the app shell, interaction components, and Pinia runtime into `apps/web`</name>
  <files>apps/web/src/App.vue, apps/web/src/components/WorldMapStage.vue, apps/web/src/components/PosterTitleBlock.vue, apps/web/src/components/PointPreviewDrawer.vue, apps/web/src/components/SeedMarkerLayer.vue, apps/web/src/components/map-popup/MapContextPopup.vue, apps/web/src/components/map-popup/PointSummaryCard.vue, apps/web/src/composables/usePopupAnchoring.ts, apps/web/src/stores/map-ui.ts, apps/web/src/stores/map-points.ts, src/App.vue, src/components/WorldMapStage.vue, src/components/PosterTitleBlock.vue, src/components/PointPreviewDrawer.vue, src/components/SeedMarkerLayer.vue, src/components/map-popup/MapContextPopup.vue, src/components/map-popup/PointSummaryCard.vue, src/composables/usePopupAnchoring.ts, src/stores/map-ui.ts, src/stores/map-points.ts</files>
  <read_first>AGENTS.md, .planning/phases/11-monorepo/11-02-PLAN.md, .planning/phases/09-popup/09-popup-05-SUMMARY.md, .planning/phases/10-可爱风格与可读性收口/10-03-SUMMARY.md, src/App.vue, src/components/WorldMapStage.vue, src/components/PointPreviewDrawer.vue, src/components/map-popup/MapContextPopup.vue, src/composables/usePopupAnchoring.ts, src/stores/map-ui.ts, src/stores/map-points.ts</read_first>
  <action>Per D-02, D-03, and D-13, move only the active app runtime shell from repo root into `apps/web`: `App.vue`, the user-facing Vue components that sustain the Phase 10 main flow, the popup/drawer composable, and the Pinia stores that coordinate that flow. Keep the current `App.vue -> WorldMapStage -> popup/drawer/store` structure and semantic hooks intact. Update imports so these moved modules can still reference root-level services/data/types temporarily if needed; those dependencies are intentionally left for the next plan instead of being folded into this one. Do not migrate specs, test helpers, static assets, geo data files, shared types, browser services, Prisma work, or backend smoke UI here.</action>
  <verify>
    <automated>pnpm -C apps/web build && pnpm -C apps/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/App.vue` contains `WorldMapStage`
    - `apps/web/src/components/WorldMapStage.vue` exists
    - `apps/web/src/components/PointPreviewDrawer.vue` exists
    - `apps/web/src/components/map-popup/MapContextPopup.vue` exists
    - `apps/web/src/stores/map-points.ts` contains `bootstrapPoints`
    - `apps/web/src/composables/usePopupAnchoring.ts` exists
    - `rg -n 'WorldMapStage|PointPreviewDrawer|MapContextPopup|bootstrapPoints' apps/web/src` returns matches in the moved package-local runtime files
  </acceptance_criteria>
  <done>web app shell、核心交互组件、composable 与 Pinia runtime 已迁入 `apps/web`，后续计划只需要继续迁移 supporting modules 并最终移除 bridge。</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web build`
- `pnpm -C apps/web typecheck`
- `rg -n 'WorldMapStage|PointPreviewDrawer|MapContextPopup|bootstrapPoints' apps/web/src`
- Confirm this plan does not also move specs, services/data, or the legacy bridge cleanup
</verification>

<success_criteria>
- The current Vue map app shell and its main interaction surface live inside `apps/web`
- Existing v2.0 UI behavior survives the directory move without scope creep into later phases
- Runtime-shell migration is no longer coupled to specs, data/assets, or bridge removal
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-05-SUMMARY.md`
</output>

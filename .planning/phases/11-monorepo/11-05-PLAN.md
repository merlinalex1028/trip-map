---
phase: 11-monorepo
plan: 05
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - apps/web/src/App.vue
  - apps/web/src/components/WorldMapStage.vue
  - apps/web/src/components/PosterTitleBlock.vue
  - apps/web/src/components/PointPreviewDrawer.vue
  - apps/web/src/components/SeedMarkerLayer.vue
  - apps/web/src/components/map-popup/MapContextPopup.vue
  - apps/web/src/components/map-popup/PointSummaryCard.vue
autonomous: true
requirements:
  - ARC-01
user_setup: []
must_haves:
  truths:
    - "现有 Vue 地图应用的 app shell 与顶层交互组件已经归属到 `apps/web`，不再由 repo root 持有这些用户可见表面。"
    - "在 Pinia store / composable 还未迁入前，这些组件可以通过显式临时 bridge 继续引用 legacy runtime；这种过渡会在后续计划中收口。"
    - "本计划只完成 app shell 与顶层组件迁移，不夹带 Pinia/composable、specs、services/data/types、Prisma 或 Phase 12-15 语义。"
  artifacts:
    - path: "apps/web/src/App.vue"
      provides: "迁移后的应用壳层"
      contains: "WorldMapStage"
    - path: "apps/web/src/components/WorldMapStage.vue"
      provides: "迁移后的主地图舞台"
      contains: "PointPreviewDrawer"
  key_links:
    - from: "apps/web/src/App.vue"
      to: "apps/web/src/components/WorldMapStage.vue"
      via: "package-local app shell continues to mount the Phase 10 main map experience"
      pattern: "WorldMapStage"
    - from: "apps/web/src/components/WorldMapStage.vue"
      to: "apps/web/src/components/PointPreviewDrawer.vue"
      via: "main stage keeps the popup/drawer interaction chain after relocation"
      pattern: "PointPreviewDrawer"
    - from: "apps/web/src/components/map-popup/MapContextPopup.vue"
      to: "apps/web/src/components/map-popup/PointSummaryCard.vue"
      via: "popup shell still delegates summary rendering through the moved popup card"
      pattern: "PointSummaryCard"
---

<objective>
把 legacy root 下的 app shell 与顶层交互组件先迁入 `apps/web`。

Purpose: checker 已指出原 `11-05` 同时吞下 runtime、specs、assets 与 bridge cleanup，规模过大。按人工收口方案，这份计划只先搬运用户可见的 app shell 和顶层组件，把 Pinia/composable 运行时 glue 留给后续独立计划处理。
Output: 归属到 `apps/web` 的 `App.vue` 与主要 Vue 交互组件，但暂时允许它们显式桥接到 legacy runtime 支撑层。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/09-popup/09-popup-05-SUMMARY.md
@.planning/phases/10-可爱风格与可读性收口/10-03-SUMMARY.md
@.planning/phases/11-monorepo/11-02-PLAN.md
@src/App.vue
@src/components/WorldMapStage.vue
@src/components/PointPreviewDrawer.vue
@src/components/map-popup/MapContextPopup.vue
@src/stores/map-points.ts
@src/composables/usePopupAnchoring.ts

<interfaces>
From `src/components/WorldMapStage.vue`:
```ts
PointPreviewDrawer
MapContextPopup
```

From `src/stores/map-points.ts`:
```ts
export const useMapPointsStore = defineStore('map-points', ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the app shell and top-level interaction components into `apps/web`</name>
  <files>apps/web/src/App.vue, apps/web/src/components/WorldMapStage.vue, apps/web/src/components/PosterTitleBlock.vue, apps/web/src/components/PointPreviewDrawer.vue, apps/web/src/components/SeedMarkerLayer.vue, apps/web/src/components/map-popup/MapContextPopup.vue, apps/web/src/components/map-popup/PointSummaryCard.vue</files>
  <read_first>AGENTS.md, .planning/phases/11-monorepo/11-02-PLAN.md, .planning/phases/09-popup/09-popup-05-SUMMARY.md, .planning/phases/10-可爱风格与可读性收口/10-03-SUMMARY.md, src/App.vue, src/components/WorldMapStage.vue, src/components/PosterTitleBlock.vue, src/components/PointPreviewDrawer.vue, src/components/SeedMarkerLayer.vue, src/components/map-popup/MapContextPopup.vue, src/components/map-popup/PointSummaryCard.vue, src/stores/map-points.ts, src/stores/map-ui.ts, src/composables/usePopupAnchoring.ts</read_first>
  <action>Per D-02, D-03, and D-13, move only the user-visible app shell and top-level interaction components into `apps/web`: `App.vue`, `WorldMapStage.vue`, `PosterTitleBlock.vue`, `PointPreviewDrawer.vue`, `SeedMarkerLayer.vue`, `MapContextPopup.vue`, and `PointSummaryCard.vue`. Preserve the current `App.vue -> WorldMapStage -> popup/drawer` structure and Phase 9/10 interaction semantics. Because Pinia stores, composables, services, and data stay out of scope for this plan, it is acceptable for the moved components to keep explicit temporary bridge imports back to the legacy runtime layer until `11-07` and `11-09` relocate those dependencies. Do not migrate specs, test helpers, styles/assets, services/data/types, Prisma work, or backend smoke UI in this plan.</action>
  <verify>
    <automated>test -f apps/web/src/App.vue && test -f apps/web/src/components/WorldMapStage.vue && rg -n 'WorldMapStage|PointPreviewDrawer|MapContextPopup|PointSummaryCard' apps/web/src/App.vue apps/web/src/components</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/App.vue` contains `WorldMapStage`
    - `apps/web/src/components/WorldMapStage.vue` exists
    - `apps/web/src/components/PosterTitleBlock.vue` exists
    - `apps/web/src/components/PointPreviewDrawer.vue` exists
    - `apps/web/src/components/SeedMarkerLayer.vue` exists
    - `apps/web/src/components/map-popup/MapContextPopup.vue` exists
    - `apps/web/src/components/map-popup/PointSummaryCard.vue` exists
    - `rg -n 'WorldMapStage|PointPreviewDrawer|MapContextPopup|PointSummaryCard' apps/web/src` returns matches in the moved package-local component files
  </acceptance_criteria>
  <done>web app shell 与顶层交互组件已迁入 `apps/web`，后续计划只需要继续迁移 supporting modules、runtime wiring，并最终移除 bootstrap bridge。</done>
</task>

</tasks>

<verification>
- `test -f apps/web/src/App.vue`
- `test -f apps/web/src/components/WorldMapStage.vue`
- `rg -n 'WorldMapStage|PointPreviewDrawer|MapContextPopup|PointSummaryCard' apps/web/src`
- Confirm this plan does not also move Pinia/composables, services/data/types, specs, or the legacy bridge cleanup
</verification>

<success_criteria>
- The current Vue map app shell and its main interaction surface live inside `apps/web`
- Existing v2.0 UI structure survives the directory move without scope creep into later phases
- App-shell migration is no longer coupled to Pinia/composables, data/services, specs, or bridge removal
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-05-SUMMARY.md`
</output>

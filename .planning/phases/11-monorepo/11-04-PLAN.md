---
phase: 11-monorepo
plan: 04
type: execute
wave: 5
depends_on:
  - 11-06
  - 11-08
files_modified:
  - apps/web/vite.config.ts
  - apps/web/src/App.vue
  - apps/web/src/App.spec.ts
  - apps/web/src/components/BackendBaselinePanel.vue
  - apps/web/src/components/BackendBaselinePanel.spec.ts
  - apps/web/src/services/api/client.ts
  - apps/web/src/services/api/phase11-smoke.ts
  - apps/web/src/services/api/phase11-smoke.spec.ts
autonomous: true
requirements:
  - ARC-01
  - ARC-04
user_setup: []
must_haves:
  truths:
    - "用户从 `apps/web` 打开的应用可以看到 server health 状态，并能触发一条真实的 smoke record 写链路。"
    - "web smoke 面板、server 路由和 DB 测试使用同一份 contracts fixture 与字段命名，不会出现 `boundaryDatasetVersion` 这类跨端漂移。"
    - "本地开发时 web 不硬编码绝对后端地址，而是通过统一 API client 和 dev proxy 接入 server。"
  artifacts:
    - path: "apps/web/src/components/BackendBaselinePanel.vue"
      provides: "Phase 11 backend baseline status + smoke action"
      contains: "创建 smoke record"
    - path: "apps/web/src/services/api/phase11-smoke.ts"
      provides: "shared-contract web API adapter"
      contains: "PHASE11_SMOKE_RECORD_REQUEST"
    - path: "apps/web/vite.config.ts"
      provides: "dev proxy from web to server"
      contains: "/api"
  key_links:
    - from: "apps/web/src/components/BackendBaselinePanel.vue"
      to: "apps/web/src/services/api/phase11-smoke.ts"
      via: "UI triggers health + smoke requests through one API adapter"
      pattern: "createSmokeRecord"
    - from: "apps/web/src/services/api/phase11-smoke.ts"
      to: "packages/contracts/src/fixtures.ts"
      via: "web submits the shared Phase 11 smoke fixture"
      pattern: "PHASE11_SMOKE_RECORD_REQUEST"
    - from: "apps/web/vite.config.ts"
      to: "apps/server/src/main.ts"
      via: "dev proxy forwards /api requests to port 4000"
      pattern: "127.0.0.1:4000"
---

<objective>
把 `apps/web`、`apps/server` 和 `packages/contracts` 接成 Phase 11 需要的最小真实 smoke path，并让它在 web 表面可见、可测、可回归。

Purpose: 只有当用户能从 `apps/web` 实际触发一条 `web -> server -> DB/contracts` 链路，Phase 11 才证明“全栈边界已经成立”，而不只是多了一个 server 目录。
Output: web API client、backend baseline 面板、dev proxy、shared-contract smoke tests，以及 root 级联合验证入口。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/11-monorepo/11-08-PLAN.md
@packages/contracts/src/health.ts
@packages/contracts/src/records.ts
@packages/contracts/src/fixtures.ts
@apps/web/src/App.vue
@apps/web/src/App.spec.ts
@apps/web/vite.config.ts
@apps/server/src/health/health.controller.ts
@apps/server/src/modules/records/records.controller.ts

<interfaces>
From `packages/contracts/src/health.ts`:
```ts
export interface HealthStatusResponse {
  status: 'ok'
  service: 'server'
  contractsVersion: string
  database: 'up' | 'down'
}
```

From `packages/contracts/src/records.ts`:
```ts
export interface SmokeRecordResponse {
  id: string
  placeId: string
  boundaryId: string
  placeKind: PlaceKind
  datasetVersion: string
  displayName: string
  createdAt: string
  updatedAt: string
}
```

Shared fixture:
```ts
export const PHASE11_SMOKE_RECORD_REQUEST = {
  placeId: 'phase11-demo-place',
  boundaryId: 'phase11-demo-boundary',
  placeKind: 'OVERSEAS_ADMIN1',
  datasetVersion: 'phase11-smoke-v1',
  displayName: 'Phase 11 Demo Place',
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a contract-backed backend baseline panel and API adapter inside `apps/web`</name>
  <files>apps/web/src/App.vue, apps/web/src/components/BackendBaselinePanel.vue, apps/web/src/services/api/client.ts, apps/web/src/services/api/phase11-smoke.ts</files>
  <read_first>AGENTS.md, .planning/phases/11-monorepo/11-CONTEXT.md, .planning/phases/11-monorepo/11-RESEARCH.md, packages/contracts/src/health.ts, packages/contracts/src/records.ts, packages/contracts/src/fixtures.ts, apps/web/src/App.vue, apps/web/src/components/WorldMapStage.vue, apps/server/src/health/health.controller.ts, apps/server/src/modules/records/records.controller.ts</read_first>
  <behavior>
    - Test 1: The panel fetches `/api/health` on mount and renders the server status plus contracts version.
    - Test 2: Clicking the smoke action posts the shared `PHASE11_SMOKE_RECORD_REQUEST` fixture and renders the returned `placeId`, `boundaryId`, `placeKind`, and `datasetVersion`.
    - Test 3: The new panel is additive; it does not replace the existing map stage, popup, or drawer interaction flow.
  </behavior>
  <action>Per D-11, D-12, and ARC-04, create a minimal but real web-facing smoke surface instead of hardcoding fetches inside existing map components. Add `apps/web/src/services/api/client.ts` with one shared `createApiUrl(path: string)` helper that prefixes requests with `import.meta.env.VITE_API_BASE_URL ?? '/api'`. In `apps/web/src/services/api/phase11-smoke.ts`, export exact functions `fetchHealthStatus(): Promise<HealthStatusResponse>` and `createSmokeRecord(): Promise<SmokeRecordResponse>`; `createSmokeRecord()` must send the shared `PHASE11_SMOKE_RECORD_REQUEST` fixture from `@trip-map/contracts`, not a duplicated local payload. Add `apps/web/src/components/BackendBaselinePanel.vue` that, on mount, calls `fetchHealthStatus()` and shows `status`, `database`, and `contractsVersion`; render one explicit button labeled `创建 smoke record` that calls `createSmokeRecord()` and then shows the returned canonical fields. Mount this panel in `apps/web/src/App.vue` above the existing `WorldMapStage` so the current map experience remains intact. Do not connect this plan to current `localStorage` records, canonical resolve, popup candidate flows, or Phase 15 CRUD; the panel is only the Phase 11 baseline proof.</action>
  <verify>
    <automated>pnpm -C apps/web test -- src/components/BackendBaselinePanel.spec.ts src/services/api/phase11-smoke.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/services/api/client.ts` contains `VITE_API_BASE_URL`
    - `apps/web/src/services/api/client.ts` contains `'/api'`
    - `apps/web/src/services/api/phase11-smoke.ts` contains `fetchHealthStatus`
    - `apps/web/src/services/api/phase11-smoke.ts` contains `createSmokeRecord`
    - `apps/web/src/services/api/phase11-smoke.ts` contains `PHASE11_SMOKE_RECORD_REQUEST`
    - `apps/web/src/components/BackendBaselinePanel.vue` contains `创建 smoke record`
    - `apps/web/src/components/BackendBaselinePanel.vue` contains `contractsVersion`
    - `apps/web/src/App.vue` contains `BackendBaselinePanel`
    - `rg -n 'phase11-demo-place|phase11-smoke-v1' apps/web` returns matches only in the API adapter/tests, not duplicated UI constants
  </acceptance_criteria>
  <done>web 侧已经具备一个使用 shared contracts 的 backend baseline 面板，能真实读取 server health 并触发 smoke record 写链路。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire dev proxy and regression tests for the Phase 11 cross-app smoke path</name>
  <files>apps/web/vite.config.ts, apps/web/src/App.spec.ts, apps/web/src/components/BackendBaselinePanel.spec.ts, apps/web/src/services/api/phase11-smoke.spec.ts</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-RESEARCH.md, apps/web/vite.config.ts, apps/web/src/App.spec.ts, apps/web/src/components/BackendBaselinePanel.vue, apps/web/src/services/api/client.ts, apps/web/src/services/api/phase11-smoke.ts</read_first>
  <behavior>
    - Test 1: `apps/web` proxies `/api` to the server on `127.0.0.1:4000` during local development, avoiding hardcoded absolute fetch URLs in components.
    - Test 2: The panel tests assert health rendering and smoke-record response rendering using the shared contract field names.
    - Test 3: Root workspace verification can run `build`, `test`, and `typecheck` across all three packages after this wiring lands.
  </behavior>
  <action>Update `apps/web/vite.config.ts` to add an exact dev proxy rule for `/api` targeting `http://127.0.0.1:4000`, with `changeOrigin: true`, so web/server can be run independently without sprinkling localhost strings through the UI. Extend `apps/web/src/components/BackendBaselinePanel.spec.ts` and `apps/web/src/services/api/phase11-smoke.spec.ts` to assert that the health response renders `phase11-v1`, and the smoke response renders `placeId`, `boundaryId`, `placeKind`, and `datasetVersion` from the shared fixture. Update `apps/web/src/App.spec.ts` so it verifies the new panel coexists with the existing app shell and map stage rather than replacing them. Keep this plan narrow: do not add Playwright, do not rewrite existing map tests to hit the real server, and do not introduce full records CRUD or canonical resolve coupling.</action>
  <verify>
    <automated>pnpm -C apps/web test && pnpm build && pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/vite.config.ts` contains `/api`
    - `apps/web/vite.config.ts` contains `127.0.0.1:4000`
    - `apps/web/vite.config.ts` contains `changeOrigin: true`
    - `apps/web/src/components/BackendBaselinePanel.spec.ts` contains `phase11-v1`
    - `apps/web/src/components/BackendBaselinePanel.spec.ts` contains `datasetVersion`
    - `apps/web/src/services/api/phase11-smoke.spec.ts` contains `PHASE11_SMOKE_RECORD_REQUEST`
    - `apps/web/src/App.spec.ts` contains `BackendBaselinePanel`
    - `package.json` contains `"build": "turbo run build"`
    - `package.json` contains `"test": "turbo run test"`
    - `package.json` contains `"typecheck": "turbo run typecheck"`
  </acceptance_criteria>
  <done>Phase 11 的 cross-app smoke path 已经通过统一 API client、dev proxy 和 web 回归测试被固定下来，并能从 root workspace 命令统一验证。</done>
</task>

</tasks>

<verification>
- `pnpm --filter @trip-map/web test`
- `pnpm build`
- `pnpm test`
- `pnpm typecheck`
- `rg -n '/api|127.0.0.1:4000|PHASE11_SMOKE_RECORD_REQUEST|contractsVersion|datasetVersion' apps/web`
- Manually run `pnpm dev:server` and `pnpm dev:web`, open the web app, confirm the backend baseline panel loads health and the `创建 smoke record` button returns the canonical fields
</verification>

<success_criteria>
- A user can prove the split architecture from the web surface, not only from server tests
- Web, server, and contracts all use the same canonical field names in the smoke path
- The development setup avoids hardcoded backend URLs and remains runnable through workspace scripts
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-04-SUMMARY.md`
</output>

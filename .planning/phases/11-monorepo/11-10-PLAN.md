---
phase: 11-monorepo
plan: 10
type: execute
wave: 6
depends_on:
  - 11-08
  - 11-09
files_modified:
  - apps/web/src/main.ts
  - apps/web/src/App.spec.ts
  - apps/web/src/components/SeedMarkerLayer.spec.ts
  - apps/web/src/components/WorldMapStage.spec.ts
  - apps/web/src/components/PointPreviewDrawer.spec.ts
  - apps/web/src/components/map-popup/PointSummaryCard.spec.ts
  - apps/web/src/components/map-popup/MapContextPopup.spec.ts
  - apps/web/src/styles/tokens.css
  - apps/web/src/styles/global.css
  - apps/web/src/assets/world-map.svg
  - apps/web/src/legacy-entry.ts
autonomous: true
requirements:
  - ARC-01
user_setup: []
must_haves:
  truths:
    - "`apps/web` 正式 bootstrap 直接挂载 package-local `App.vue`，不再通过 `legacy-entry` 过渡桥。"
    - "剩余 UI regression specs、styles 与静态地图资源已迁入 `apps/web`，package-local suite 可以覆盖当前 v2.0 主链路。"
    - "bridge cleanup 完成后，web 侧后续 smoke path 变更只需要在 `apps/web` 内完成。"
  artifacts:
    - path: "apps/web/src/main.ts"
      provides: "bridge-free bootstrap that mounts the moved app directly"
      contains: "createApp(App).use(createPinia()).mount('#app')"
    - path: "apps/web/src/App.spec.ts"
      provides: "package-local app-shell regression entry"
      contains: "App"
    - path: "apps/web/src/styles/global.css"
      provides: "package-local global styling surface"
      contains: "app-shell"
  key_links:
    - from: "apps/web/src/main.ts"
      to: "apps/web/src/App.vue"
      via: "web package bootstrap now mounts the moved Vue app directly"
      pattern: "createApp\\(App\\)"
    - from: "apps/web/src/App.spec.ts"
      to: "apps/web/src/App.vue"
      via: "package-local regression tests cover the moved app shell"
      pattern: "mount"
    - from: "apps/web/src/styles/global.css"
      to: "apps/web/src/assets/world-map.svg"
      via: "package-local styling/assets now live beside the moved app"
      pattern: "world-map"
---

<objective>
迁移剩余 UI regression、styles/assets，并移除 `11-02` 引入的 `legacy-entry` 过渡桥。

Purpose: `11-08` 已经处理 non-UI spec/test-helper，`11-09` 已处理 runtime wiring。这个收尾计划负责把剩余 UI regression 与视觉资源迁入 `apps/web`，并把 bootstrap 最终切到 package-local `App.vue`。
Output: `apps/web` 内的 app/component regression specs、styles/assets，以及不再依赖 `legacy-entry` 的正式 bootstrap。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/11-monorepo/11-08-PLAN.md
@.planning/phases/11-monorepo/11-09-PLAN.md
@src/main.ts
@src/App.spec.ts
@src/components/SeedMarkerLayer.spec.ts
@src/components/WorldMapStage.spec.ts
@src/components/PointPreviewDrawer.spec.ts
@src/components/map-popup/PointSummaryCard.spec.ts
@src/components/map-popup/MapContextPopup.spec.ts
@src/styles/tokens.css
@src/styles/global.css
@src/assets/world-map.svg

<interfaces>
From `src/main.ts`:
```ts
createApp(App).use(createPinia()).mount('#app')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rehome the UI regression surface, package-local styles/assets, and remove the bootstrap bridge</name>
  <files>apps/web/src/main.ts, apps/web/src/App.spec.ts, apps/web/src/components/SeedMarkerLayer.spec.ts, apps/web/src/components/WorldMapStage.spec.ts, apps/web/src/components/PointPreviewDrawer.spec.ts, apps/web/src/components/map-popup/PointSummaryCard.spec.ts, apps/web/src/components/map-popup/MapContextPopup.spec.ts, apps/web/src/styles/tokens.css, apps/web/src/styles/global.css, apps/web/src/assets/world-map.svg, apps/web/src/legacy-entry.ts</files>
  <read_first>AGENTS.md, .planning/phases/11-monorepo/11-08-PLAN.md, .planning/phases/11-monorepo/11-09-PLAN.md, src/main.ts, src/App.spec.ts, src/components/SeedMarkerLayer.spec.ts, src/components/WorldMapStage.spec.ts, src/components/PointPreviewDrawer.spec.ts, src/components/map-popup/PointSummaryCard.spec.ts, src/components/map-popup/MapContextPopup.spec.ts, src/styles/tokens.css, src/styles/global.css, src/assets/world-map.svg</read_first>
  <behavior>
    - Test 1: `apps/web/src/main.ts` mounts `App.vue` directly and `legacy-entry` is no longer part of the active bootstrap path.
    - Test 2: The moved app/component specs run inside `apps/web` and keep covering the current v2.0 map/popup/drawer shell.
    - Test 3: Package-local build, UI regression, and typecheck succeed after the final bridge cleanup.
  </behavior>
  <action>Move the remaining UI-facing regression surface into `apps/web`: `App.spec.ts`, `SeedMarkerLayer.spec.ts`, `WorldMapStage.spec.ts`, `PointPreviewDrawer.spec.ts`, `PointSummaryCard.spec.ts`, and `MapContextPopup.spec.ts`. Move `tokens.css`, `global.css`, and `world-map.svg` into `apps/web` so the package-local app owns its visual shell. Update `apps/web/src/main.ts` to mount the migrated `App.vue` directly with `createApp(App).use(createPinia()).mount('#app')`, and retire `apps/web/src/legacy-entry.ts` from the active bootstrap path; if the file must linger briefly, it must be dead code and no longer imported. Keep this plan scoped to ARC-01 only: no backend smoke panel work, no contracts expansion, no server changes, and no Phase 12-15 semantics.</action>
  <verify>
    <automated>pnpm -C apps/web test -- src/App.spec.ts src/components/SeedMarkerLayer.spec.ts src/components/WorldMapStage.spec.ts src/components/PointPreviewDrawer.spec.ts src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts && pnpm -C apps/web build && pnpm -C apps/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/main.ts` contains `createApp(App).use(createPinia()).mount('#app')`
    - `apps/web/src/App.spec.ts` exists
    - `apps/web/src/components/WorldMapStage.spec.ts` exists
    - `apps/web/src/styles/tokens.css` exists
    - `apps/web/src/styles/global.css` exists
    - `apps/web/src/assets/world-map.svg` exists
    - `rg -n 'legacy-entry' apps/web/src/main.ts apps/web/src/App.vue apps/web/src/**/*.spec.ts` returns no matches
    - `rg -n 'from .*/src/' apps/web/src/App.spec.ts apps/web/src/components/**/*.spec.ts` returns no matches that point back to repo-root source
  </acceptance_criteria>
  <done>`apps/web` 的 UI regression、styles/assets 与 bootstrap 入口已经 package-local 化，`legacy-entry` 过渡桥已退出主链路。</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web test -- src/App.spec.ts src/components/SeedMarkerLayer.spec.ts src/components/WorldMapStage.spec.ts src/components/PointPreviewDrawer.spec.ts src/components/map-popup/PointSummaryCard.spec.ts src/components/map-popup/MapContextPopup.spec.ts`
- `pnpm -C apps/web build`
- `pnpm -C apps/web typecheck`
- `rg -n 'legacy-entry' apps/web/src/main.ts apps/web/src/App.vue apps/web/src/**/*.spec.ts`
</verification>

<success_criteria>
- The current Vue map app is fully runnable/buildable/testable from `apps/web`
- Existing v2.0 UI behavior survives the migration and bridge cleanup without scope creep into later phases
- The temporary legacy bridge introduced in `11-02` is removed cleanly from the active bootstrap path
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-10-SUMMARY.md`
</output>

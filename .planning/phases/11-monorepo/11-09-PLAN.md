---
phase: 11-monorepo
plan: 09
type: execute
wave: 4
depends_on:
  - 11-05
  - 11-07
files_modified:
  - apps/web/src/App.vue
  - apps/web/src/components/WorldMapStage.vue
  - apps/web/src/components/PointPreviewDrawer.vue
  - apps/web/src/components/SeedMarkerLayer.vue
  - apps/web/src/composables/usePopupAnchoring.ts
  - apps/web/src/stores/map-ui.ts
  - apps/web/src/stores/map-points.ts
autonomous: true
requirements:
  - ARC-01
user_setup: []
must_haves:
  truths:
    - "Pinia store、popup anchoring composable 与其 consumer runtime 已迁入 `apps/web`，不再依赖 repo-root runtime glue。"
    - "`apps/web` 内的 app shell 和交互组件现在通过 package-local stores/composable/services/types 协作，而不是继续走临时 legacy bridge。"
    - "本计划只处理 runtime wiring，不夹带 specs、styles/assets、Prisma 或 backend smoke UI。"
  artifacts:
    - path: "apps/web/src/stores/map-points.ts"
      provides: "package-local map points runtime store"
      contains: "bootstrapPoints"
    - path: "apps/web/src/composables/usePopupAnchoring.ts"
      provides: "package-local popup anchoring composable"
      contains: "usePopupAnchoring"
    - path: "apps/web/src/App.vue"
      provides: "app shell rewired to the moved runtime glue"
      contains: "bootstrapPoints"
  key_links:
    - from: "apps/web/src/App.vue"
      to: "apps/web/src/stores/map-points.ts"
      via: "app shell bootstraps points through the moved package-local store"
      pattern: "bootstrapPoints"
    - from: "apps/web/src/components/WorldMapStage.vue"
      to: "apps/web/src/composables/usePopupAnchoring.ts"
      via: "main stage now consumes the moved package-local anchoring composable"
      pattern: "usePopupAnchoring"
    - from: "apps/web/src/stores/map-points.ts"
      to: "apps/web/src/services/point-storage.ts"
      via: "moved runtime store persists through the package-local storage service"
      pattern: "point-storage"
---

<objective>
把 Pinia/composable runtime glue 迁入 `apps/web`，并把 `11-05` 的 app shell / 顶层交互组件 rewiring 到 package-local supporting modules。

Purpose: `11-05` 只迁用户可见表面，`11-07` 只迁 supporting modules。这个计划负责把二者真正接起来，清掉 “组件仍桥接到 repo-root runtime” 这层临时状态。
Output: package-local 的 stores/composable，以及已经改为消费 `apps/web` 内 services/types 的 `App.vue` / `WorldMapStage.vue` / `PointPreviewDrawer.vue` / `SeedMarkerLayer.vue`。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/11-monorepo/11-05-PLAN.md
@.planning/phases/11-monorepo/11-07-PLAN.md
@src/composables/usePopupAnchoring.ts
@src/stores/map-ui.ts
@src/stores/map-points.ts
@apps/web/src/App.vue
@apps/web/src/components/WorldMapStage.vue
@apps/web/src/components/PointPreviewDrawer.vue
@apps/web/src/components/SeedMarkerLayer.vue

<interfaces>
From `src/stores/map-points.ts`:
```ts
export const useMapPointsStore = defineStore('map-points', ...)
```

From `src/composables/usePopupAnchoring.ts`:
```ts
export function usePopupAnchoring(options: UsePopupAnchoringOptions)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Pinia/composable runtime glue into `apps/web` and rewire the moved app shell</name>
  <files>apps/web/src/App.vue, apps/web/src/components/WorldMapStage.vue, apps/web/src/components/PointPreviewDrawer.vue, apps/web/src/components/SeedMarkerLayer.vue, apps/web/src/composables/usePopupAnchoring.ts, apps/web/src/stores/map-ui.ts, apps/web/src/stores/map-points.ts</files>
  <read_first>AGENTS.md, .planning/phases/11-monorepo/11-05-PLAN.md, .planning/phases/11-monorepo/11-07-PLAN.md, src/composables/usePopupAnchoring.ts, src/stores/map-ui.ts, src/stores/map-points.ts, apps/web/src/App.vue, apps/web/src/components/WorldMapStage.vue, apps/web/src/components/PointPreviewDrawer.vue, apps/web/src/components/SeedMarkerLayer.vue, apps/web/src/services/point-storage.ts, apps/web/src/types/map-point.ts</read_first>
  <action>Per D-02, D-03, and D-13, move the runtime glue into `apps/web` by creating package-local copies of `usePopupAnchoring.ts`, `map-ui.ts`, and `map-points.ts`, then update `apps/web/src/App.vue`, `WorldMapStage.vue`, `PointPreviewDrawer.vue`, and `SeedMarkerLayer.vue` so they consume the moved package-local stores/composable/services/types instead of continuing to bridge back into repo-root runtime files. Keep the current `bootstrapPoints`, popup/drawer state machine, and Phase 9/10 interaction semantics intact. Do not migrate specs, styles/assets, backend smoke panel files, or add new records/canonical-resolve behavior here.</action>
  <verify>
    <automated>pnpm -C apps/web build && pnpm -C apps/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/stores/map-points.ts` contains `bootstrapPoints`
    - `apps/web/src/stores/map-ui.ts` contains `useMapUiStore`
    - `apps/web/src/composables/usePopupAnchoring.ts` contains `usePopupAnchoring`
    - `apps/web/src/App.vue` contains `./stores/map-points`
    - `apps/web/src/components/WorldMapStage.vue` contains `../composables/usePopupAnchoring`
    - `apps/web/src/components/PointPreviewDrawer.vue` contains `../stores/map-points`
    - `apps/web/src/components/SeedMarkerLayer.vue` contains `../stores/map-points`
    - `rg -n '/src/stores|/src/composables' apps/web/src/App.vue apps/web/src/components apps/web/src/stores apps/web/src/composables` returns no matches
  </acceptance_criteria>
  <done>`apps/web` 的 runtime glue 已 package-local 化，app shell 与顶层交互组件不再通过 legacy root runtime 协作。</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web build`
- `pnpm -C apps/web typecheck`
- `rg -n '/src/stores|/src/composables' apps/web/src/App.vue apps/web/src/components apps/web/src/stores apps/web/src/composables`
- Confirm app shell/components now point to package-local stores/composable/services/types
</verification>

<success_criteria>
- `apps/web` owns the Pinia/composable runtime glue behind the Phase 10 main flow
- The moved app shell/components are rewired to package-local stores/composable/supporting modules
- Runtime wiring is no longer coupled to spec migration, styles/assets relocation, or bridge cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-09-SUMMARY.md`
</output>

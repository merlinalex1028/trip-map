---
phase: 11-monorepo
plan: 08
type: execute
wave: 4
depends_on:
  - 11-05
  - 11-07
files_modified:
  - apps/web/src/main.ts
  - apps/web/src/App.spec.ts
  - apps/web/src/test-utils/mountComposable.ts
  - apps/web/src/services/map-projection.spec.ts
  - apps/web/src/services/point-storage.spec.ts
  - apps/web/src/services/city-boundaries.spec.ts
  - apps/web/src/services/geo-lookup.spec.ts
  - apps/web/src/services/city-search.spec.ts
  - apps/web/src/data/geo/city-candidates.spec.ts
  - apps/web/src/composables/usePopupAnchoring.spec.ts
  - apps/web/src/stores/map-points.spec.ts
  - apps/web/src/components/SeedMarkerLayer.spec.ts
  - apps/web/src/components/WorldMapStage.spec.ts
  - apps/web/src/components/PointPreviewDrawer.spec.ts
  - apps/web/src/components/map-popup/PointSummaryCard.spec.ts
  - apps/web/src/components/map-popup/MapContextPopup.spec.ts
  - apps/web/src/legacy-entry.ts
  - src/main.ts
  - src/App.spec.ts
  - src/test-utils/mountComposable.ts
  - src/services/map-projection.spec.ts
  - src/services/point-storage.spec.ts
  - src/services/city-boundaries.spec.ts
  - src/services/geo-lookup.spec.ts
  - src/services/city-search.spec.ts
  - src/data/geo/city-candidates.spec.ts
  - src/composables/usePopupAnchoring.spec.ts
  - src/stores/map-points.spec.ts
  - src/components/SeedMarkerLayer.spec.ts
  - src/components/WorldMapStage.spec.ts
  - src/components/PointPreviewDrawer.spec.ts
  - src/components/map-popup/PointSummaryCard.spec.ts
  - src/components/map-popup/MapContextPopup.spec.ts
autonomous: true
requirements:
  - ARC-01
user_setup: []
must_haves:
  truths:
    - "现有 web regression specs 与 test helper 已迁入 `apps/web`，package-local suite 不再依赖 repo-root `src/`。"
    - "`apps/web` 正式 bootstrap 直接挂载迁移后的 package-local app，不再通过 `legacy-entry` 过渡桥。"
    - "bridge cleanup 之后，后续 smoke UI 变更只需要在 `apps/web` 内完成，不会再碰 root-level web runtime/spec 残留。"
  artifacts:
    - path: "apps/web/src/main.ts"
      provides: "bridge-free bootstrap that mounts the moved app directly"
      contains: "createApp(App).use(createPinia()).mount('#app')"
    - path: "apps/web/src/App.spec.ts"
      provides: "package-local app regression entry"
      contains: "App"
    - path: "apps/web/src/test-utils/mountComposable.ts"
      provides: "package-local test helper surface"
      contains: "mountComposable"
  key_links:
    - from: "apps/web/src/main.ts"
      to: "apps/web/src/App.vue"
      via: "web package bootstrap now mounts the moved Vue app directly"
      pattern: "createApp\\(App\\)"
    - from: "apps/web/src/App.spec.ts"
      to: "apps/web/src/App.vue"
      via: "package-local regression tests cover the moved app shell"
      pattern: "mount"
    - from: "apps/web/src/test-utils/mountComposable.ts"
      to: "apps/web/src/composables/usePopupAnchoring.spec.ts"
      via: "moved test helpers power the package-local composable specs"
      pattern: "mountComposable"
---

<objective>
把 legacy web 的 specs/test-helper 迁入 `apps/web`，并移除 `11-02` 引入的 `legacy-entry` 过渡桥。

Purpose: checker 点名要求把 spec/test-helper move 与 bridge cleanup 从原 `11-05` 中拆出来。这个收尾计划只做测试面迁移和 bootstrap bridge removal，不再继续吞 runtime/data/service 迁移。
Output: `apps/web` 内部的 package-local regression suite、test helpers，以及不再依赖 `legacy-entry` 的正式 bootstrap。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/11-monorepo/11-05-PLAN.md
@.planning/phases/11-monorepo/11-07-PLAN.md
@.planning/phases/11-monorepo/11-02-PLAN.md
@src/main.ts
@src/App.spec.ts
@src/test-utils/mountComposable.ts
@src/components/WorldMapStage.spec.ts
@src/services/point-storage.spec.ts

<interfaces>
From `src/main.ts`:
```ts
createApp(App).use(createPinia()).mount('#app')
```

From `src/test-utils/mountComposable.ts`:
```ts
export function mountComposable<T>(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rehome the spec/test-helper surface and remove the `legacy-entry` bridge</name>
  <files>apps/web/src/main.ts, apps/web/src/App.spec.ts, apps/web/src/test-utils/mountComposable.ts, apps/web/src/services/map-projection.spec.ts, apps/web/src/services/point-storage.spec.ts, apps/web/src/services/city-boundaries.spec.ts, apps/web/src/services/geo-lookup.spec.ts, apps/web/src/services/city-search.spec.ts, apps/web/src/data/geo/city-candidates.spec.ts, apps/web/src/composables/usePopupAnchoring.spec.ts, apps/web/src/stores/map-points.spec.ts, apps/web/src/components/SeedMarkerLayer.spec.ts, apps/web/src/components/WorldMapStage.spec.ts, apps/web/src/components/PointPreviewDrawer.spec.ts, apps/web/src/components/map-popup/PointSummaryCard.spec.ts, apps/web/src/components/map-popup/MapContextPopup.spec.ts, apps/web/src/legacy-entry.ts, src/main.ts, src/App.spec.ts, src/test-utils/mountComposable.ts, src/services/map-projection.spec.ts, src/services/point-storage.spec.ts, src/services/city-boundaries.spec.ts, src/services/geo-lookup.spec.ts, src/services/city-search.spec.ts, src/data/geo/city-candidates.spec.ts, src/composables/usePopupAnchoring.spec.ts, src/stores/map-points.spec.ts, src/components/SeedMarkerLayer.spec.ts, src/components/WorldMapStage.spec.ts, src/components/PointPreviewDrawer.spec.ts, src/components/map-popup/PointSummaryCard.spec.ts, src/components/map-popup/MapContextPopup.spec.ts</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-02-PLAN.md, .planning/phases/11-monorepo/11-05-PLAN.md, .planning/phases/11-monorepo/11-07-PLAN.md, apps/web/vitest.config.ts, src/main.ts, src/App.spec.ts, src/test-utils/mountComposable.ts, src/components/**/*.spec.ts, src/services/**/*.spec.ts</read_first>
  <behavior>
    - Test 1: Existing app/component/service/composable specs all run from `apps/web` without assuming repo-root `src/`.
    - Test 2: `apps/web/src/main.ts` mounts `App.vue` directly and `apps/web/src/legacy-entry.ts` is removed from the active bootstrap path.
    - Test 3: Package-local build, test, and typecheck succeed after the source/spec move.
  </behavior>
  <action>Move the current web spec files and `src/test-utils/mountComposable.ts` into `apps/web`, then repoint imports so the suite no longer assumes root-level `src/`. Update `apps/web/src/main.ts` to mount the migrated `App.vue` directly with `createApp(App).use(createPinia()).mount('#app')`, and remove `legacy-entry` from the active bootstrap path entirely. If a placeholder file must remain temporarily for deletion semantics, ensure nothing imports it and the acceptance criteria still prove the bridge is dead. Keep this plan scoped to ARC-01 only: stabilize the moved test surface, confirm the package-local scripts work, and remove the temporary bridge introduced in `11-02`. Do not introduce Playwright, backend smoke panel code, canonical resolve work, or any contracts expansion beyond using the existing workspace dependency.</action>
  <verify>
    <automated>pnpm -C apps/web test && pnpm -C apps/web build && pnpm -C apps/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/main.ts` contains `createApp(App).use(createPinia()).mount('#app')`
    - `apps/web/src/App.spec.ts` exists
    - `apps/web/src/test-utils/mountComposable.ts` exists
    - `rg -n 'legacy-entry' apps/web/src src` returns no matches after the cleanup
    - `rg -n 'from .*/src/' apps/web/src/**/*.spec.ts` returns no matches that point back to repo-root source
    - `rg -n 'pinia|useMapPointsStore|localStorage' packages/contracts` returns no matches
  </acceptance_criteria>
  <done>`apps/web` 的回归测试面已迁入 package 内并稳定运行，且 `legacy-entry` 过渡桥已被移除，后续 smoke path 变更不再需要碰 repo root web 残留。</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web test`
- `pnpm -C apps/web build`
- `pnpm -C apps/web typecheck`
- `rg -n 'legacy-entry' apps/web/src src`
- Confirm repo root no longer owns the active web runtime bootstrap or regression suite
</verification>

<success_criteria>
- The current Vue map app is fully runnable/buildable/testable from `apps/web`
- Existing v2.0 UI behavior survives the migration and bridge cleanup without scope creep into later phases
- The temporary legacy bridge introduced in `11-02` is removed cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-08-SUMMARY.md`
</output>

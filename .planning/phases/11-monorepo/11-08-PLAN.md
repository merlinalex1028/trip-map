---
phase: 11-monorepo
plan: 08
type: execute
wave: 5
depends_on:
  - 11-07
  - 11-09
files_modified:
  - apps/web/src/test-utils/mountComposable.ts
  - apps/web/src/services/map-projection.spec.ts
  - apps/web/src/services/point-storage.spec.ts
  - apps/web/src/services/city-boundaries.spec.ts
  - apps/web/src/services/geo-lookup.spec.ts
  - apps/web/src/services/city-search.spec.ts
  - apps/web/src/data/geo/city-candidates.spec.ts
  - apps/web/src/composables/usePopupAnchoring.spec.ts
  - apps/web/src/stores/map-points.spec.ts
autonomous: true
requirements:
  - ARC-01
user_setup: []
must_haves:
  truths:
    - "services/data/composable/store 相关的 regression specs 与 test helper 已迁入 `apps/web`，非 UI 回归面不再依赖 repo-root `src/`。"
    - "这份计划不处理 `App.spec`、组件 UI regression、styles/assets 或 `legacy-entry` cleanup；这些会在后续收尾计划中完成。"
    - "package-local test helper surface 已为后续 `apps/web` 测试统一入口打底。"
  artifacts:
    - path: "apps/web/src/test-utils/mountComposable.ts"
      provides: "package-local test helper surface"
      contains: "mountComposable"
    - path: "apps/web/src/services/point-storage.spec.ts"
      provides: "package-local storage regression spec"
      contains: "loadStoredPoints"
  key_links:
    - from: "apps/web/src/test-utils/mountComposable.ts"
      to: "apps/web/src/composables/usePopupAnchoring.spec.ts"
      via: "moved test helpers power the package-local composable specs"
      pattern: "mountComposable"
    - from: "apps/web/src/stores/map-points.spec.ts"
      to: "apps/web/src/stores/map-points.ts"
      via: "package-local store regression now runs against the moved runtime store"
      pattern: "useMapPointsStore"
---

<objective>
把 legacy web 的 non-UI specs 与 test-helper 迁入 `apps/web`。

Purpose: checker 点名要求把 spec/test-helper move 与 bridge cleanup 分开处理。这一份只承接 services/data/composable/store 侧的 regression suite 和 test helper，避免继续把入口切换、组件回归和 UI 资源迁移揉成超大计划。
Output: `apps/web` 内部的 package-local non-UI regression suite 与 test helpers。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monorepo/11-CONTEXT.md
@.planning/phases/11-monorepo/11-RESEARCH.md
@.planning/phases/11-monorepo/11-07-PLAN.md
@.planning/phases/11-monorepo/11-09-PLAN.md
@src/test-utils/mountComposable.ts
@src/services/point-storage.spec.ts

<interfaces>
From `src/test-utils/mountComposable.ts`:
```ts
export function mountComposable<T>(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rehome non-UI specs and test helpers into `apps/web`</name>
  <files>apps/web/src/test-utils/mountComposable.ts, apps/web/src/services/map-projection.spec.ts, apps/web/src/services/point-storage.spec.ts, apps/web/src/services/city-boundaries.spec.ts, apps/web/src/services/geo-lookup.spec.ts, apps/web/src/services/city-search.spec.ts, apps/web/src/data/geo/city-candidates.spec.ts, apps/web/src/composables/usePopupAnchoring.spec.ts, apps/web/src/stores/map-points.spec.ts</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/11-monorepo/11-07-PLAN.md, .planning/phases/11-monorepo/11-09-PLAN.md, apps/web/vitest.config.ts, src/test-utils/mountComposable.ts, src/services/map-projection.spec.ts, src/services/point-storage.spec.ts, src/services/city-boundaries.spec.ts, src/services/geo-lookup.spec.ts, src/services/city-search.spec.ts, src/data/geo/city-candidates.spec.ts, src/composables/usePopupAnchoring.spec.ts, src/stores/map-points.spec.ts</read_first>
  <behavior>
    - Test 1: service/data/composable/store specs run from `apps/web` without assuming repo-root `src/`.
    - Test 2: `apps/web/src/test-utils/mountComposable.ts` becomes the package-local helper imported by the moved composable/spec surface.
    - Test 3: Package-local targeted regression commands succeed without needing the legacy root spec files.
  </behavior>
  <action>Move the current non-UI web spec files and `src/test-utils/mountComposable.ts` into `apps/web`, then repoint imports so the service/data/composable/store suite no longer assumes root-level `src/`. This plan should cover `map-projection.spec.ts`, `point-storage.spec.ts`, `city-boundaries.spec.ts`, `geo-lookup.spec.ts`, `city-search.spec.ts`, `city-candidates.spec.ts`, `usePopupAnchoring.spec.ts`, and `map-points.spec.ts` only. Keep it scoped to ARC-01: stabilize the moved helper + non-UI regression surface and prove `apps/web` can run those targeted specs locally. Do not touch `main.ts`, `App.spec.ts`, UI component specs, styles/assets, backend smoke panel code, or `legacy-entry` cleanup here.</action>
  <verify>
    <automated>pnpm -C apps/web test -- src/services/map-projection.spec.ts src/services/point-storage.spec.ts src/services/city-boundaries.spec.ts src/services/geo-lookup.spec.ts src/services/city-search.spec.ts src/data/geo/city-candidates.spec.ts src/composables/usePopupAnchoring.spec.ts src/stores/map-points.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/test-utils/mountComposable.ts` exists
    - `apps/web/src/services/point-storage.spec.ts` exists
    - `apps/web/src/composables/usePopupAnchoring.spec.ts` exists
    - `apps/web/src/stores/map-points.spec.ts` exists
    - `rg -n 'from .*/src/' apps/web/src/services/*.spec.ts apps/web/src/data/geo/*.spec.ts apps/web/src/composables/*.spec.ts apps/web/src/stores/*.spec.ts` returns no matches that point back to repo-root source
    - `rg -n 'pinia|useMapPointsStore|localStorage' packages/contracts` returns no matches
  </acceptance_criteria>
  <done>`apps/web` 的 non-UI regression suite 与 test helper 已迁入 package 内并稳定运行，后续只剩 UI regression、styles/assets 与 bootstrap bridge cleanup 收尾。</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web test -- src/services/map-projection.spec.ts src/services/point-storage.spec.ts src/services/city-boundaries.spec.ts src/services/geo-lookup.spec.ts src/services/city-search.spec.ts src/data/geo/city-candidates.spec.ts src/composables/usePopupAnchoring.spec.ts src/stores/map-points.spec.ts`
- `rg -n 'from .*/src/' apps/web/src/services/*.spec.ts apps/web/src/data/geo/*.spec.ts apps/web/src/composables/*.spec.ts apps/web/src/stores/*.spec.ts`
- Confirm this plan does not also touch `main.ts`, `App.spec.ts`, component specs, styles/assets, or `legacy-entry`
</verification>

<success_criteria>
- The non-UI regression suite and helper surface run from `apps/web`
- Existing service/data/composable/store behavior remains covered after migration
- Test-helper migration is no longer coupled to UI regression or bridge cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/11-monorepo/11-08-SUMMARY.md`
</output>

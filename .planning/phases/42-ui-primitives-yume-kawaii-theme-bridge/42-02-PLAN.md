---
phase: 42-ui-primitives-yume-kawaii-theme-bridge
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - apps/web/src/styles/tokens.css
  - apps/web/src/style.css
  - apps/web/src/tailwind-token.spec.ts
  - apps/web/src/components/ui/button/*
  - apps/web/src/components/ui/card/*
  - apps/web/src/components/ui/dialog/*
  - apps/web/src/components/ui/popover/*
  - apps/web/src/components/ui/calendar/*
  - apps/web/src/components/ui/tabs/*
  - apps/web/src/components/ui/sidebar/*
  - apps/web/src/components/ui/dropdown-menu/*
  - apps/web/src/components/ui/skeleton/*
  - apps/web/src/components/ui/scroll-area/*
  - apps/web/src/components/showcase/UiPrimitiveShowcase.vue
  - apps/web/src/components/showcase/UiPrimitiveShowcase.spec.ts
autonomous: true
requirements: [DS-02, DS-05]
must_haves:
  truths:
    - "Generated primitives default to Soft Pastel Glassmorphism instead of neutral shadcn black/white/gray."
    - "Theme tokens expose the Phase 42 Yume Kawaii palette, radius, shadow, typography, and motion baseline."
    - "A primitive state matrix exists for default, disabled, loading/skeleton, focus-visible, destructive/error, and interactive states."
  artifacts:
    - "apps/web/src/styles/tokens.css contains the v8 token baseline from 42-UI-SPEC.md."
    - "apps/web/src/style.css Tailwind v4 @theme values match the v8 token baseline."
    - "apps/web/src/components/showcase/UiPrimitiveShowcase.vue renders the locked primitive set."
  key_links:
    - "Generated primitives consume tokens such as var(--color-accent), var(--radius-pill), and var(--shadow-button)."
    - "UiPrimitiveShowcase.vue imports primitives through @/components/ui/*."
    - "UiPrimitiveShowcase.spec.ts verifies operable overlay primitives with Teleport stubbing or document.body attachment."
---

<objective>
Recalibrate the existing theme token layer and generated primitive defaults to the Phase 42 Yume Kawaii / Soft Pastel Glassmorphism contract.

Purpose: This prevents shadcn-vue neutral defaults from leaking into v8 and creates a local state matrix that later phases can reuse for visual and interaction checks.
Output: Updated global tokens, Tailwind v4 theme values, themed primitive source defaults, and a focused primitive showcase component with smoke coverage.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-CONTEXT.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-RESEARCH.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-VALIDATION.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-PATTERNS.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-UI-SPEC.md
@apps/web/src/style.css
@apps/web/src/styles/tokens.css
@apps/web/src/tailwind-token.spec.ts

<interfaces>
Required token targets from `42-UI-SPEC.md`:
```text
Accent #F75A9B, supporting accents #8B6FEF #5EA7F2 #7ED9B6 #F5A354,
ink strong #25146F, ink muted #6F5B99, frame #E8DDF6,
button/pill radius 999px or 18-22px, card radius 24-32px, dialog/popover radius 32px,
motion feedback 140-180ms.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recalibrate global tokens and CSS contract tests</name>
  <files>apps/web/src/styles/tokens.css, apps/web/src/style.css, apps/web/src/tailwind-token.spec.ts</files>
  <read_first>
    - apps/web/src/styles/tokens.css
    - apps/web/src/style.css
    - apps/web/src/tailwind-token.spec.ts
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-CONTEXT.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-UI-SPEC.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `tailwind-token.spec.ts` asserts `--color-page: #FFF8FD;`, `--color-accent: #F75A9B;`, `--color-ink-strong: #25146F;`, `--color-ink-muted: #6F5B99;`, and `--color-frame: #E8DDF6;`.
    - Test 2: `tailwind-token.spec.ts` asserts `--radius-control: 22px;`, `--radius-card: 28px;`, `--radius-bubble: 32px;`, `--radius-pill: 999px;`, `--motion-quick: 140ms;`, and `--motion-emphasis: 180ms;`.
    - Test 3: `tailwind-token.spec.ts` asserts `apps/web/src/style.css` contains Tailwind v4 `@theme` color values matching the same v8 palette, including `--color-yume-accent: #F75A9B;`.
  </behavior>
  <action>
    Update the CSS contract test first, then recalibrate `apps/web/src/styles/tokens.css` in place per D-06/D-07. Set these exact baseline values: `--color-page: #FFF8FD;`, `--color-page-bottom: #FAFAFF;`, `--color-surface: rgba(255, 255, 255, 0.78);`, `--color-surface-soft: #FFF0F7;`, `--color-surface-alt: #EAF6FF;`, `--color-surface-raised: rgba(255, 255, 255, 0.86);`, `--color-accent: #F75A9B;`, `--color-accent-strong: #E94486;`, `--color-accent-soft: #FFF0F7;`, `--color-secondary: #5EA7F2;`, `--color-secondary-strong: #2F86D8;`, `--color-secondary-soft: #EAF6FF;`, `--color-lilac: #F2E8FF;`, `--color-purple: #8B6FEF;`, `--color-mint: #7ED9B6;`, `--color-warm: #F5A354;`, `--color-destructive: #C86464;`, `--color-frame: #E8DDF6;`, `--color-frame-strong: #C9B7EE;`, `--color-ink-strong: #25146F;`, `--color-ink-muted: #6F5B99;`, `--color-ink-soft: #9A86C7;`, `--radius-control: 22px;`, `--radius-card: 28px;`, `--radius-bubble: 32px;`, `--radius-pill: 999px;`, `--motion-quick: 140ms;`, and `--motion-emphasis: 180ms;`. Update gradients and shadows to use these values, including `--gradient-selected` with `#F75A9B` and `--shadow-button` with a soft pink/lavender lift. In `apps/web/src/style.css`, keep the current import order and add/update `@theme` values for `--color-yume-page: #FFF8FD;`, `--color-yume-accent: #F75A9B;`, `--color-yume-purple: #8B6FEF;`, `--color-yume-sky: #5EA7F2;`, `--color-yume-mint: #7ED9B6;`, `--color-yume-warm: #F5A354;`, `--color-yume-ink: #25146F;`, and `--color-yume-frame: #E8DDF6;`. Do not add a second token file or Tailwind v3 config.
  </action>
  <verify>
    <automated>npm --prefix apps/web run test -- src/tailwind-token.spec.ts</automated>
    <automated>npm --prefix apps/web run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/styles/tokens.css` contains `--color-page: #FFF8FD;`.
    - `apps/web/src/styles/tokens.css` contains `--color-accent: #F75A9B;`.
    - `apps/web/src/styles/tokens.css` contains `--color-ink-strong: #25146F;`.
    - `apps/web/src/styles/tokens.css` contains `--color-ink-muted: #6F5B99;`.
    - `apps/web/src/styles/tokens.css` contains `--color-frame: #E8DDF6;`.
    - `apps/web/src/styles/tokens.css` contains `--radius-control: 22px;`.
    - `apps/web/src/style.css` contains `--color-yume-accent: #F75A9B;`.
    - `apps/web/src/style.css` still contains `@import "tailwindcss";` before `@import 'leaflet/dist/leaflet.css';`.
  </acceptance_criteria>
  <done>The existing token system becomes the v8 shared baseline and the CSS contract test proves concrete color/radius/motion values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Theme generated primitives and build primitive state matrix</name>
  <files>apps/web/src/components/ui/button/*, apps/web/src/components/ui/card/*, apps/web/src/components/ui/dialog/*, apps/web/src/components/ui/popover/*, apps/web/src/components/ui/calendar/*, apps/web/src/components/ui/tabs/*, apps/web/src/components/ui/sidebar/*, apps/web/src/components/ui/dropdown-menu/*, apps/web/src/components/ui/skeleton/*, apps/web/src/components/ui/scroll-area/*, apps/web/src/components/showcase/UiPrimitiveShowcase.vue, apps/web/src/components/showcase/UiPrimitiveShowcase.spec.ts</files>
  <read_first>
    - apps/web/src/components/ui/button/*
    - apps/web/src/components/ui/card/*
    - apps/web/src/components/ui/dialog/*
    - apps/web/src/components/ui/popover/*
    - apps/web/src/components/ui/calendar/*
    - apps/web/src/components/ui/tabs/*
    - apps/web/src/components/ui/sidebar/*
    - apps/web/src/components/ui/dropdown-menu/*
    - apps/web/src/components/ui/skeleton/*
    - apps/web/src/components/ui/scroll-area/*
    - apps/web/src/styles/tokens.css
    - apps/web/vitest.config.ts
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-CONTEXT.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-VALIDATION.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-PATTERNS.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `UiPrimitiveShowcase.spec.ts` renders text `UI Primitives` and `Yume Kawaii Theme`.
    - Test 2: the spec clicks `[data-testid="showcase-dialog-trigger"]` and observes dialog content after opening.
    - Test 3: the spec clicks `[data-testid="showcase-popover-trigger"]` and observes popover content after opening.
    - Test 4: the spec verifies skeleton/loading and disabled examples exist via `data-state="loading"` and `disabled` controls.
  </behavior>
  <action>
    Edit generated primitive defaults after reading their actual shadcn-vue output. Button default styling must include `rounded-[var(--radius-pill)]`, `bg-[var(--gradient-selected)]`, `text-white`, `shadow-[var(--shadow-button)]`, and a visible focus style using `focus-visible:ring-[color:var(--color-accent)]` or an equivalent token-backed focus class. Card primitives must default to `rounded-[var(--radius-card)]`, `border-white/80`, `bg-[var(--color-surface-raised)]`, `text-[var(--color-ink-strong)]`, and `shadow-[var(--shadow-surface)]`. Dialog, Popover, Dropdown Menu, and Sidebar surface primitives must use `rounded-[var(--radius-bubble)]` or `rounded-[32px]`, translucent white/pink/lavender backgrounds, `border-[color:var(--color-frame)]`, `shadow-[var(--shadow-stage)]`, and `backdrop-blur-xl` where the primitive surface supports it. Calendar selected day must use `bg-[var(--color-accent)] text-white` plus a shape/border distinction, not color alone. Skeleton must use stable dimensions and a pastel shimmer/wash. Create `apps/web/src/components/showcase/UiPrimitiveShowcase.vue` as a focused component that imports all primitives through `@/components/ui/*`, renders default/disabled/loading/focus-visible/destructive examples, and provides operable Dialog, Popover, Dropdown Menu, Calendar, Tabs, Sidebar, Skeleton, and Scroll Area demos. Add `apps/web/src/components/showcase/UiPrimitiveShowcase.spec.ts` using Vue Test Utils with `global.stubs: { Teleport: true }` or `attachTo: document.body`; avoid snapshot-only tests.
  </action>
  <verify>
    <automated>npm --prefix apps/web run test -- src/components/showcase/UiPrimitiveShowcase.spec.ts</automated>
    <automated>npm --prefix apps/web run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/components/showcase/UiPrimitiveShowcase.vue` contains `data-testid="showcase-dialog-trigger"`.
    - `apps/web/src/components/showcase/UiPrimitiveShowcase.vue` contains `data-testid="showcase-popover-trigger"`.
    - `apps/web/src/components/showcase/UiPrimitiveShowcase.vue` contains `data-state="loading"`.
    - `apps/web/src/components/showcase/UiPrimitiveShowcase.vue` imports from `@/components/ui/button`, `@/components/ui/card`, `@/components/ui/dialog`, `@/components/ui/popover`, `@/components/ui/calendar`, `@/components/ui/tabs`, `@/components/ui/sidebar`, `@/components/ui/dropdown-menu`, `@/components/ui/skeleton`, and `@/components/ui/scroll-area`.
    - `rg -n "neutral-|slate-|zinc-|gray-" apps/web/src/components/ui apps/web/src/components/showcase/UiPrimitiveShowcase.vue` is reviewed and any remaining neutral token is either removed or documented in the summary as generated dependency state not visible as the default theme.
    - `rg -n "var\\(--color-accent\\)|var\\(--gradient-selected\\)|var\\(--shadow-button\\)|var\\(--color-frame\\)" apps/web/src/components/ui apps/web/src/components/showcase/UiPrimitiveShowcase.vue` returns matches.
  </acceptance_criteria>
  <done>The primitive layer displays v8 token-backed defaults and the state matrix proves key generated primitives render and open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated primitive source -> developer showcase | Local third-party primitive code becomes visible through a developer validation component. |
| user keyboard/mouse -> overlay primitives | Interaction events cross into Dialog, Popover, Dropdown, Sidebar, Calendar, and Tabs behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Spoofing / UX integrity | Dialog, Popover, Dropdown, Sidebar, Calendar, Tabs | mitigate | Preserve shadcn-vue/Reka behavior and add smoke tests that open overlays and verify visible focus/selected/loading states. |
| T-42-04 | Tampering | generated primitive source | mitigate | Edit generated source minimally to consume local tokens; run typecheck and focused primitive smoke test after changes. |
| T-42-03 | Information Disclosure | showcase component | transfer | This plan creates only the component; Plan 05 owns route-level `import.meta.env.DEV` production redirect. |
</threat_model>

<verification>
Run after this plan:
- `npm --prefix apps/web run test -- src/tailwind-token.spec.ts src/components/showcase/UiPrimitiveShowcase.spec.ts`
- `npm --prefix apps/web run typecheck`
</verification>

<success_criteria>
- Token contract tests assert the concrete v8 palette and radius/motion values.
- Generated primitives use token-backed pastel/glass defaults.
- Primitive showcase renders all locked primitives and exercises overlay interactions.
</success_criteria>

<output>
After completion, create `.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-02-SUMMARY.md`.
</output>

---
phase: 42-ui-primitives-yume-kawaii-theme-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/components.json
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/src/lib/utils.ts
  - apps/web/src/components/ui/button/*
  - apps/web/src/components/ui/card/*
  - apps/web/src/components/ui/dialog/*
  - apps/web/src/components/ui/popover/*
  - apps/web/src/components/ui/calendar/*
  - apps/web/src/components/ui/tabs/*
  - apps/web/src/components/ui/sidebar/*
  - apps/web/src/components/ui/dropdown-menu/*
  - apps/web/src/components/ui/skeleton/*
  - apps/web/src/components/ui/scroll-area/*
autonomous: true
requirements: [DS-01, DS-02]
must_haves:
  truths:
    - "Developer can import generated shadcn-vue primitives from @/components/ui/* inside apps/web."
    - "Button, Card, Dialog, Popover, Calendar, Tabs, Sidebar, Dropdown Menu, Skeleton, and Scroll Area exist as local source primitives."
    - "Dependency versions are pinned to the Phase 42 researched target values."
  artifacts:
    - "apps/web/components.json configures shadcn-vue with style=new-york, TypeScript, src/style.css, and @ aliases."
    - "apps/web/src/lib/utils.ts exports cn(...inputs: ClassValue[]) using clsx and tailwind-merge."
    - "apps/web/src/components/ui/* contains the generated locked primitive set."
  key_links:
    - "apps/web/vite.config.ts maps '@' to fromWebRoot('./src')."
    - "apps/web/tsconfig.json maps '@/*' to ./src/*."
    - "apps/web/components.json aliases ui to @/components/ui and utils to @/lib/utils."
---

<objective>
Create the dependency, alias, shadcn-vue configuration, and local primitive source foundation required by Phase 42.

Purpose: This establishes the importable UI primitive baseline for DS-01 and DS-02 before any theme, icon, chart, or showcase work depends on it.
Output: Pinned package changes, `@` source aliases, `components.json`, `cn()` utility, and the full locked shadcn-vue primitive set.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-CONTEXT.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-RESEARCH.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-VALIDATION.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-PATTERNS.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-UI-SPEC.md
@apps/web/package.json
@apps/web/vite.config.ts
@apps/web/tsconfig.json
@apps/web/vitest.config.ts

<interfaces>
Current Vite alias contract from `apps/web/vite.config.ts`:
```ts
const fromWebRoot = (path: string) => new URL(path, import.meta.url).pathname
```

Current web scripts from `apps/web/package.json`:
```json
"build": "vue-tsc --noEmit && vite build",
"test": "vitest run",
"typecheck": "vue-tsc --noEmit"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Phase 42 UI foundation dependencies</name>
  <files>apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/web/package.json
    - package.json
    - pnpm-workspace.yaml
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-RESEARCH.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-VALIDATION.md
  </read_first>
  <action>
    Install the exact researched dependency set into `@trip-map/web` using project-standard pnpm commands: `pnpm --filter @trip-map/web add echarts@6.0.0 vue-echarts@8.0.1 @iconify/vue@5.0.1 @internationalized/date@3.12.1 clsx@2.1.1 tailwind-merge@3.6.0 class-variance-authority@0.7.1 reka-ui@2.9.7`; then install dev-only icon extraction packages with `pnpm --filter @trip-map/web add -D @iconify/utils@3.1.3 @iconify-json/flat-color-icons@1.2.3 @iconify-json/solar@1.2.5`. If `pnpm --version` fails with `[ERROR] fetch failed` or Corepack/network errors, request the required network/Corepack approval and retry the same pnpm commands. Do not install these packages at the repository root. This implements DS-01/DS-02 dependency readiness and uses pinned versions per the dependency constraints.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync('apps/web/package.json','utf8')); const d=p.dependencies||{}; const dev=p.devDependencies||{}; for (const [name, version] of Object.entries({echarts:'6.0.0','vue-echarts':'8.0.1','@iconify/vue':'5.0.1','@internationalized/date':'3.12.1',clsx:'2.1.1','tailwind-merge':'3.6.0','class-variance-authority':'0.7.1','reka-ui':'2.9.7'})) { if (d[name] !== version) throw new Error(name+' expected '+version+' got '+d[name]); } for (const [name, version] of Object.entries({'@iconify/utils':'3.1.3','@iconify-json/flat-color-icons':'1.2.3','@iconify-json/solar':'1.2.5'})) { if (dev[name] !== version) throw new Error(name+' expected '+version+' got '+dev[name]); }"</automated>
    <automated>npm --prefix apps/web run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` contains `"echarts": "6.0.0"`.
    - `apps/web/package.json` contains `"vue-echarts": "8.0.1"`.
    - `apps/web/package.json` contains `"@iconify/vue": "5.0.1"`.
    - `apps/web/package.json` contains `"@internationalized/date": "3.12.1"`.
    - `apps/web/package.json` contains `"clsx": "2.1.1"` and `"tailwind-merge": "3.6.0"`.
    - `apps/web/package.json` contains devDependencies `"@iconify-json/flat-color-icons": "1.2.3"` and `"@iconify-json/solar": "1.2.5"`.
    - `pnpm-lock.yaml` contains `echarts@6.0.0`, `vue-echarts@8.0.1`, and `@iconify/vue@5.0.1`.
  </acceptance_criteria>
  <done>All Phase 42 foundation dependencies are pinned in `apps/web/package.json`, lockfile is updated by pnpm, and web typecheck still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Configure @ alias and generate locked shadcn-vue primitives</name>
  <files>apps/web/components.json, apps/web/vite.config.ts, apps/web/tsconfig.json, apps/web/src/lib/utils.ts, apps/web/src/components/ui/button/*, apps/web/src/components/ui/card/*, apps/web/src/components/ui/dialog/*, apps/web/src/components/ui/popover/*, apps/web/src/components/ui/calendar/*, apps/web/src/components/ui/tabs/*, apps/web/src/components/ui/sidebar/*, apps/web/src/components/ui/dropdown-menu/*, apps/web/src/components/ui/skeleton/*, apps/web/src/components/ui/scroll-area/*</files>
  <read_first>
    - apps/web/vite.config.ts
    - apps/web/tsconfig.json
    - apps/web/package.json
    - apps/web/vitest.config.ts
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-CONTEXT.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-RESEARCH.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-PATTERNS.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-UI-SPEC.md
  </read_first>
  <action>
    Add the source alias to existing config without removing current explicit package aliases: in `apps/web/vite.config.ts`, add `'@': fromWebRoot('./src')` inside `resolve.alias`; in `apps/web/tsconfig.json`, add `"@/*": ["./src/*"]` under `compilerOptions.paths`. Create `apps/web/components.json` with exact values: `$schema` set to `https://shadcn-vue.com/schema.json`, `style` set to `new-york`, `typescript` set to `true`, `tailwind.config` set to an empty string, `tailwind.css` set to `src/style.css`, `tailwind.baseColor` set to `slate`, `tailwind.cssVariables` set to `true`, `tailwind.prefix` set to an empty string, and aliases `components: "@/components"`, `utils: "@/lib/utils"`, `ui: "@/components/ui"`, `lib: "@/lib"`, `composables: "@/composables"`. Create `apps/web/src/lib/utils.ts` exporting the exact `cn(...inputs: ClassValue[])` helper using `clsx` and `twMerge`. Run `pnpm --filter @trip-map/web dlx shadcn-vue@2.6.2 add button card dialog popover calendar tabs sidebar dropdown-menu skeleton scroll-area` so the locked primitive set from D-02 is generated under `apps/web/src/components/ui/*`. Per D-01 and D-04, do not rewrite `AuthDialog`, `ConfirmDialog`, `TripDateForm`, or map popup business components.
  </action>
  <verify>
    <automated>npm --prefix apps/web run typecheck</automated>
    <automated>test -f apps/web/components.json && test -f apps/web/src/lib/utils.ts && test -d apps/web/src/components/ui/button && test -d apps/web/src/components/ui/card && test -d apps/web/src/components/ui/dialog && test -d apps/web/src/components/ui/popover && test -d apps/web/src/components/ui/calendar && test -d apps/web/src/components/ui/tabs && test -d apps/web/src/components/ui/sidebar && test -d apps/web/src/components/ui/dropdown-menu && test -d apps/web/src/components/ui/skeleton && test -d apps/web/src/components/ui/scroll-area</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/vite.config.ts` contains `"'@': fromWebRoot('./src')"`.
    - `apps/web/tsconfig.json` contains `"@/*": ["./src/*"]`.
    - `apps/web/components.json` contains `"style": "new-york"` and `"css": "src/style.css"`.
    - `apps/web/components.json` contains `"ui": "@/components/ui"` and `"utils": "@/lib/utils"`.
    - `apps/web/src/lib/utils.ts` contains `export function cn(...inputs: ClassValue[])`.
    - Each required primitive directory exists under `apps/web/src/components/ui/`: `button`, `card`, `dialog`, `popover`, `calendar`, `tabs`, `sidebar`, `dropdown-menu`, `skeleton`, `scroll-area`.
    - `rg -n "AuthDialog|ConfirmDialog|TripDateForm" apps/web/src/components/ui apps/web/src/lib/utils.ts apps/web/components.json` returns no matches.
  </acceptance_criteria>
  <done>`@` imports resolve for generated local shadcn-vue primitives, all locked primitives exist locally, and no business UI migration happened in Phase 42 Plan 01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> workspace | Third-party generated dependencies and source enter `apps/web`. |
| generated primitive source -> app runtime | Generated UI code becomes trusted local source used by later pages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Tampering | `apps/web/package.json`, `pnpm-lock.yaml`, generated shadcn files | mitigate | Pin exact dependency versions from 42-RESEARCH.md and verify package.json/lockfile strings before typecheck. |
| T-42-05 | Spoofing / UX integrity | shadcn-vue Dialog, Popover, Dropdown, Sidebar, Calendar | mitigate | Generate Reka-backed primitives instead of custom overlay/focus behavior; Plan 02 adds keyboard/state smoke coverage. |
| T-42-03 | Information Disclosure | `/__ui` route | transfer | Route exposure is not introduced in this plan; Plan 05 owns the `import.meta.env.DEV` production redirect. |
</threat_model>

<verification>
Run after this plan:
- `npm --prefix apps/web run typecheck`
- `test -f apps/web/components.json && test -d apps/web/src/components/ui/button`
</verification>

<success_criteria>
- `@/components/ui/...` and `@/lib/utils` imports are resolvable by TypeScript and Vite.
- The full D-02 primitive list exists as local source under `apps/web/src/components/ui/*`.
- Dependency versions match the researched concrete target values.
</success_criteria>

<output>
After completion, create `.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-01-SUMMARY.md`.
</output>

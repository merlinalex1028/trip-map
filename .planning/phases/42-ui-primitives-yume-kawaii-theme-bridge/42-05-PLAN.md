---
phase: 42-ui-primitives-yume-kawaii-theme-bridge
plan: 05
type: execute
wave: 3
depends_on: [42-02, 42-03, 42-04]
files_modified:
  - apps/web/src/components/showcase/UiIconShowcase.vue
  - apps/web/src/components/showcase/UiChartShowcase.vue
  - apps/web/src/views/UiShowcaseView.vue
  - apps/web/src/views/UiShowcaseView.spec.ts
  - apps/web/src/router/index.ts
  - apps/web/src/router/index.spec.ts
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04, DS-05]
must_haves:
  truths:
    - "Developer can open /__ui in dev to see all Phase 42 primitives, icons, and chart foundation examples."
    - "Production access to /__ui redirects to / and the showcase is not added to product navigation."
    - "Full web tests and build pass with the Phase 42 dependency, token, primitive, icon, chart, and route changes."
  artifacts:
    - "apps/web/src/views/UiShowcaseView.vue composes focused showcase sections."
    - "apps/web/src/router/index.ts contains a dev-only /__ui route before the catch-all redirect."
    - "apps/web/src/views/UiShowcaseView.spec.ts covers primitive/icon/chart showcase rendering."
  key_links:
    - "UiShowcaseView.vue imports UiPrimitiveShowcase, UiIconShowcase, and UiChartShowcase."
    - "router/index.ts lazy-loads ../views/UiShowcaseView.vue only through the /__ui route."
    - "router/index.spec.ts verifies /__ui production redirect behavior."
---

<objective>
Wire the Phase 42 developer showcase route and final smoke gate.

Purpose: This makes the primitive, icon, and chart foundations inspectable in development while mitigating the high-severity production exposure risk for `/__ui`.
Output: Focused showcase sections, thin `UiShowcaseView`, dev-only route guard, router/showcase smoke tests, and full phase gate commands.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-CONTEXT.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-RESEARCH.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-VALIDATION.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-PATTERNS.md
@.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-UI-SPEC.md
@apps/web/src/components/showcase/UiPrimitiveShowcase.vue
@apps/web/src/components/common/KawaiiIcon.vue
@apps/web/src/components/common/BaseChart.vue
@apps/web/src/router/index.ts
@apps/web/src/router/index.spec.ts
@apps/web/vitest.config.ts

<interfaces>
Route guard contract:
```ts
{
  path: '/__ui',
  name: 'ui-showcase',
  beforeEnter: () => (import.meta.env.DEV ? true : { path: '/' }),
  component: () => import('../views/UiShowcaseView.vue'),
}
```

Showcase section imports:
```ts
import UiPrimitiveShowcase from '@/components/showcase/UiPrimitiveShowcase.vue'
import UiIconShowcase from '@/components/showcase/UiIconShowcase.vue'
import UiChartShowcase from '@/components/showcase/UiChartShowcase.vue'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assemble thin /__ui showcase view</name>
  <files>apps/web/src/components/showcase/UiIconShowcase.vue, apps/web/src/components/showcase/UiChartShowcase.vue, apps/web/src/views/UiShowcaseView.vue, apps/web/src/views/UiShowcaseView.spec.ts</files>
  <read_first>
    - apps/web/src/components/showcase/UiPrimitiveShowcase.vue
    - apps/web/src/components/common/KawaiiIcon.vue
    - apps/web/src/components/common/BaseChart.vue
    - apps/web/src/lib/charts/theme.ts
    - apps/web/src/lib/icons/semantic-icons.ts
    - apps/web/src/views/TimelinePageView.vue
    - apps/web/src/views/StatisticsPageView.vue
    - apps/web/vitest.config.ts
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-CONTEXT.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-VALIDATION.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-PATTERNS.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `UiShowcaseView.spec.ts` renders `UI Primitives`, `Yume Kawaii Theme`, `Semantic Icons`, and `Chart Foundation`.
    - Test 2: the spec observes icon examples with `data-icon-name="map"`, `data-icon-name="journal"`, and `data-icon-name="memories"`.
    - Test 3: the spec observes chart examples for `data-state="loading"`, `data-state="empty"`, `data-state="error"`, and a demo option surface.
    - Test 4: the spec clicks the primitive dialog/popover triggers inherited from `UiPrimitiveShowcase`.
  </behavior>
  <action>
    Create `apps/web/src/components/showcase/UiIconShowcase.vue` that imports `KawaiiIcon` from `@/components/common/KawaiiIcon.vue`, renders semantic examples for `map`, `journal`, `memories`, `calendar`, `star`, `camera`, `badge`, and `pin`, and uses product preview labels `世界足迹`, `旅途手账`, `旅途回忆`, `日历`, `星星`, `相机`, `奖章`, and `地点`. Create `apps/web/src/components/showcase/UiChartShowcase.vue` that imports `BaseChart` from `@/components/common/BaseChart.vue`, defines a clearly labeled demo-only ECharts option inside the component, and renders four foundation states: a demo line chart, loading, empty, and error with exact error text `暂时没有加载成功，请稍后重试。`. Create `apps/web/src/views/UiShowcaseView.vue` as a thin route-level composition surface importing `UiPrimitiveShowcase`, `UiIconShowcase`, and `UiChartShowcase`; it must not import Pinia stores, APIs, or real travel records. Use copy `UI Primitives`, `Yume Kawaii Theme`, `Semantic Icons`, and `Chart Foundation`. Add `apps/web/src/views/UiShowcaseView.spec.ts` using Vue Test Utils, Teleport stubbing or document body attachment, and a `vue-echarts` mock if needed through the existing `BaseChart` test pattern.
  </action>
  <verify>
    <automated>npm --prefix apps/web run test -- src/views/UiShowcaseView.spec.ts</automated>
    <automated>npm --prefix apps/web run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/views/UiShowcaseView.vue` contains `UiPrimitiveShowcase`, `UiIconShowcase`, and `UiChartShowcase`.
    - `apps/web/src/views/UiShowcaseView.vue` contains `UI Primitives` and `Yume Kawaii Theme`.
    - `apps/web/src/components/showcase/UiIconShowcase.vue` contains `name="map"`, `name="journal"`, and `name="memories"`.
    - `apps/web/src/components/showcase/UiChartShowcase.vue` contains `Chart Foundation`.
    - `apps/web/src/components/showcase/UiChartShowcase.vue` contains `暂时没有加载成功，请稍后重试。`.
    - `rg -n "use[A-Za-z]*Store|fetch\\(|/api|TravelRecord" apps/web/src/views/UiShowcaseView.vue apps/web/src/components/showcase/UiIconShowcase.vue apps/web/src/components/showcase/UiChartShowcase.vue` returns no matches.
  </acceptance_criteria>
  <done>`/__ui` has a thin development showcase view that composes primitive, semantic icon, and chart foundation demos without real app data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dev-only /__ui route guard and phase gate</name>
  <files>apps/web/src/router/index.ts, apps/web/src/router/index.spec.ts</files>
  <read_first>
    - apps/web/src/router/index.ts
    - apps/web/src/router/index.spec.ts
    - apps/web/src/views/UiShowcaseView.vue
    - apps/web/vitest.config.ts
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-CONTEXT.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-RESEARCH.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-VALIDATION.md
    - .planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: router spec verifies `/__ui` route exists before the catch-all route.
    - Test 2: router spec stubs `DEV` false with Vitest environment utilities where feasible and verifies pushing `/__ui` redirects to `/`.
    - Test 3: existing auth guard tests for `/timeline` and `/statistics` still pass.
  </behavior>
  <action>
    In `apps/web/src/router/index.ts`, add the `/__ui` route before `/:pathMatch(.*)*` with exact path `/__ui`, name `ui-showcase`, route-level `beforeEnter: () => (import.meta.env.DEV ? true : { path: '/' })`, and lazy component `() => import('../views/UiShowcaseView.vue')`. Do not add `/__ui` to any product navigation, auth navigation, sidebar, map popup, timeline, or statistics view. Extend `apps/web/src/router/index.spec.ts` with a production exposure test for T-42-03; use `vi.stubEnv('DEV', false)` and `vi.unstubAllEnvs()` if supported by the current Vitest runtime, or isolate the route guard function in the spec with the same `beforeEnter` behavior if direct `import.meta.env.DEV` stubbing is not reliable. Keep the existing auth guard tests intact.
  </action>
  <verify>
    <automated>npm --prefix apps/web run test -- src/router/index.spec.ts src/views/UiShowcaseView.spec.ts</automated>
    <automated>npm --prefix apps/web run test</automated>
    <automated>npm --prefix apps/web run build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/router/index.ts` contains `path: '/__ui'`.
    - `apps/web/src/router/index.ts` contains `name: 'ui-showcase'`.
    - `apps/web/src/router/index.ts` contains `import.meta.env.DEV ? true : { path: '/' }`.
    - `apps/web/src/router/index.ts` contains `() => import('../views/UiShowcaseView.vue')`.
    - `apps/web/src/router/index.spec.ts` contains `/__ui`.
    - `apps/web/src/router/index.spec.ts` contains `redirect` or `redirects`.
    - `rg -n "/__ui|ui-showcase" apps/web/src | grep -v "apps/web/src/router/index.ts" | grep -v "apps/web/src/router/index.spec.ts" | grep -v "apps/web/src/views/UiShowcaseView" | grep -v "apps/web/src/components/showcase" | grep -v "apps/web/src/views/UiShowcaseView.spec.ts"` returns no product-navigation matches.
  </acceptance_criteria>
  <done>`/__ui` is available as a development validation route, redirects in production, and the full web test/build gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public route path -> developer showcase | Any browser user can request `/__ui`; route guard must block production exposure. |
| demo options/icons -> local components | Demo data crosses into icon/chart/primitive renderers but must remain static and non-user-data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Information Disclosure | `apps/web/src/router/index.ts`, `/__ui` | mitigate | Add route-level `beforeEnter` using `import.meta.env.DEV`; verify production redirect in router spec; grep to ensure no product navigation references. |
| T-42-01 | Tampering / XSS | icon showcase | mitigate | Render icons only through `KawaiiIcon` semantic names created in Plan 03; no raw Iconify ids in showcase code. |
| T-42-02 | Tampering / XSS | chart showcase | mitigate | Use static demo-only ECharts options and no user HTML, stores, APIs, or travel records. |
| T-42-05 | Spoofing / UX integrity | interactive showcase examples | mitigate | Reuse shadcn-vue/Reka primitives and focused smoke tests for Dialog/Popover interactions. |
</threat_model>

<verification>
Run after this plan:
- `npm --prefix apps/web run test -- src/router/index.spec.ts src/views/UiShowcaseView.spec.ts`
- `npm --prefix apps/web run test`
- `npm --prefix apps/web run build`
</verification>

<success_criteria>
- `/__ui` displays all Phase 42 primitive, icon, and chart foundations in development.
- `/__ui` redirects to `/` in production mode.
- Full `apps/web` tests and build pass after all Phase 42 plans.
</success_criteria>

<output>
After completion, create `.planning/phases/42-ui-primitives-yume-kawaii-theme-bridge/42-05-SUMMARY.md`.
</output>

---
phase: 02-国家级真实地点识别
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/assets/world-map.svg
  - src/types/geo.ts
  - src/types/map-point.ts
  - src/data/seed-points.ts
  - src/services/map-projection.ts
  - src/services/map-projection.spec.ts
  - src/stores/map-ui.ts
  - src/components/WorldMapStage.vue
  - src/App.spec.ts
autonomous: true
requirements:
  - GEO-01
user_setup: []
must_haves:
  truths:
    - The map stage uses one explicit projection contract for both rendering and click inversion.
    - Clicking the map can produce stable `lat/lng` output from container-relative coordinates.
    - The current decorative world poster asset is replaced or rewritten into a projection-faithful map surface before country lookup work starts.
  artifacts:
    - src/services/map-projection.ts
    - src/services/map-projection.spec.ts
    - src/types/geo.ts
    - src/components/WorldMapStage.vue
    - src/assets/world-map.svg
  key_links:
    - WorldMapStage uses the projection service instead of ad-hoc click math
    - Seed points carry real geographic metadata that matches the projection contract
    - map-ui store exposes pending geographic click state for later lookup work
---

<objective>
Replace the non-geographic placeholder map contract with a projection-faithful world stage and wire click inversion so Phase 2 has a trustworthy `x/y <-> lng/lat` foundation.

Purpose: Phase 2 cannot deliver real country recognition until the visual map surface and projection math share one contract.
Output: A projection service, geographic seed metadata, and map click sampling that yields stable coordinates and lightweight “resolving” feedback.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@PRD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-国家级真实地点识别/02-CONTEXT.md
@.planning/phases/02-国家级真实地点识别/02-RESEARCH.md
@.planning/phases/02-国家级真实地点识别/02-VALIDATION.md
@src/assets/world-map.svg
@src/components/WorldMapStage.vue
@src/data/seed-points.ts
@src/stores/map-ui.ts
@src/types/map-point.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the fixed projection contract and replace the decorative map asset</name>
  <files>package.json, pnpm-lock.yaml, src/assets/world-map.svg, src/types/geo.ts, src/services/map-projection.ts, src/services/map-projection.spec.ts</files>
  <read_first>PRD.md, .planning/phases/02-国家级真实地点识别/02-CONTEXT.md, .planning/phases/02-国家级真实地点识别/02-RESEARCH.md, src/assets/world-map.svg</read_first>
  <behavior>
    - Test 1: The app defines one reusable projection config for forward and inverse coordinate conversion.
    - Test 2: The rendered world map asset and projection config now share the same geographic frame instead of the abstract Phase 1 poster blobs.
    - Test 3: Projection helpers are unit-tested on center, edge, and clamp cases.
  </behavior>
  <action>Add `d3-geo` to `package.json` and refresh `pnpm-lock.yaml`. Replace `src/assets/world-map.svg` with a projection-faithful equirectangular poster asset whose root tag contains `viewBox="0 0 1600 800"` and whose visible geographic plotting frame is `x=40`, `y=80`, `width=1520`, `height=640`. Create `src/types/geo.ts` exporting `GeoCoordinates`, `NormalizedPoint`, `ProjectionConfig`, and `GeoLookupStatus`. Create `src/services/map-projection.ts` exporting the exact constant `WORLD_PROJECTION_CONFIG = { viewBoxWidth: 1600, viewBoxHeight: 800, plotLeft: 40, plotTop: 80, plotWidth: 1520, plotHeight: 640, lngMin: -180, lngMax: 180, latMin: -90, latMax: 90 }` plus pure functions `clampNormalizedPoint`, `normalizedPointToGeoCoordinates`, and `geoCoordinatesToNormalizedPoint`. Create `src/services/map-projection.spec.ts` asserting `(0.5, 0.5)` maps near `lng=0, lat=0`, that left/right extremes map near `-180/180`, and that out-of-range normalized inputs are clamped into `[0,1]`.</action>
  <verify>test -f src/types/geo.ts && test -f src/services/map-projection.ts && test -f src/services/map-projection.spec.ts && rg 'WORLD_PROJECTION_CONFIG|normalizedPointToGeoCoordinates|geoCoordinatesToNormalizedPoint' src/services/map-projection.ts && rg 'viewBox=\"0 0 1600 800\"' src/assets/world-map.svg && pnpm test -- src/services/map-projection.spec.ts</verify>
  <acceptance_criteria>
    - package.json contains `d3-geo`
    - src/types/geo.ts contains `ProjectionConfig`
    - src/services/map-projection.ts contains `WORLD_PROJECTION_CONFIG`
    - src/services/map-projection.ts contains `normalizedPointToGeoCoordinates`
    - src/services/map-projection.ts contains `geoCoordinatesToNormalizedPoint`
    - src/assets/world-map.svg contains `viewBox="0 0 1600 800"`
    - src/services/map-projection.spec.ts contains `clamps`
  </acceptance_criteria>
  <done>The project has one explicit world projection contract and the visual map asset no longer undermines real geographic inversion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade seed points and map click handling to emit real geographic coordinates</name>
  <files>src/types/map-point.ts, src/data/seed-points.ts, src/stores/map-ui.ts, src/components/WorldMapStage.vue, src/App.spec.ts, src/services/map-projection.ts</files>
  <read_first>src/types/map-point.ts, src/data/seed-points.ts, src/stores/map-ui.ts, src/components/WorldMapStage.vue, src/services/map-projection.ts, .planning/phases/02-国家级真实地点识别/02-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Seed points now carry real geographic metadata that matches the projection contract.
    - Test 2: Clicking the map stage emits a temporary geographic hit and “resolving” state without yet performing country lookup.
    - Test 3: Existing seed-point selection remains intact while background clicks use the new projection service.
  </behavior>
  <action>Update `src/types/map-point.ts` so `MapPointDisplay` includes `countryCode`, `lat`, and `lng` in addition to the existing preview fields. Update `src/data/seed-points.ts` so Lisbon, Cairo, Kyoto, and Buenos Aires each define concrete `countryCode`, `lat`, and `lng` values, and compute their `x/y` from `geoCoordinatesToNormalizedPoint(...)` instead of hard-coded decorative positions. Extend `src/stores/map-ui.ts` with exactly these new public state pieces and actions: `pendingGeoHit`, `isRecognizing`, `setPendingGeoHit`, `startRecognition`, `finishRecognition`, and `clearPendingGeoHit`. Update `src/components/WorldMapStage.vue` so clicks on the map surface compute container-relative normalized coordinates, call `clampNormalizedPoint`, then `normalizedPointToGeoCoordinates`, and write the result into `pendingGeoHit` while toggling `isRecognizing` to true. Ignore clicks that originate from existing marker buttons, and render a lightweight pulse marker from `pendingGeoHit` while lookup is still pending. Update `src/App.spec.ts` to continue mounting Pinia successfully after the new store state is added.</action>
  <verify>rg 'countryCode|lat|lng' src/types/map-point.ts && rg 'geoCoordinatesToNormalizedPoint|countryCode' src/data/seed-points.ts && rg 'pendingGeoHit|isRecognizing|setPendingGeoHit|startRecognition|finishRecognition|clearPendingGeoHit' src/stores/map-ui.ts && rg 'normalizedPointToGeoCoordinates|pendingGeoHit|isRecognizing' src/components/WorldMapStage.vue && pnpm test</verify>
  <acceptance_criteria>
    - src/types/map-point.ts contains `countryCode`
    - src/types/map-point.ts contains `lat`
    - src/types/map-point.ts contains `lng`
    - src/data/seed-points.ts contains `geoCoordinatesToNormalizedPoint`
    - src/stores/map-ui.ts contains `pendingGeoHit`
    - src/stores/map-ui.ts contains `isRecognizing`
    - src/components/WorldMapStage.vue contains `normalizedPointToGeoCoordinates`
    - src/components/WorldMapStage.vue contains `pendingGeoHit`
  </acceptance_criteria>
  <done>The map stage can now convert clicks into stable geographic coordinates and exposes a reusable pending-hit contract for the country lookup phase.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test
- [ ] rg 'WORLD_PROJECTION_CONFIG' src/services/map-projection.ts
- [ ] rg 'pendingGeoHit|isRecognizing' src/stores/map-ui.ts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository now has a trustworthy projection contract instead of a purely decorative map
- Phase 2 can hand real `lat/lng` data into a later country lookup step without rewriting the map stage

</success_criteria>

<output>
After completion, create `.planning/phases/02-国家级真实地点识别/02-01-SUMMARY.md`
</output>

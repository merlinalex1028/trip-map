---
phase: 02-国家级真实地点识别
plan: 03
type: execute
wave: 3
depends_on: []
gap_closure: true
files_modified:
  - src/assets/world-map.svg
  - src/services/map-projection.ts
  - src/services/map-projection.spec.ts
  - src/services/geo-lookup.spec.ts
  - src/components/WorldMapStage.vue
  - .planning/phases/02-国家级真实地点识别/02-UAT.md
autonomous: true
requirements:
  - GEO-01
  - GEO-02
user_setup: []
must_haves:
  truths:
    - The rendered SVG map frame and projection config use the same horizontal extent.
    - Clicking Japan and Hong Kong no longer resolves to inland Asia countries.
    - Regression coverage protects the East Asia alignment issue from returning.
  artifacts:
    - src/assets/world-map.svg
    - src/services/map-projection.ts
    - src/services/geo-lookup.spec.ts
  key_links:
    - The visible map frame matches the click inversion contract exactly
    - User-reported UAT gaps for Japan and Hong Kong are covered by tests
---

<objective>
Close the Phase 2 UAT gaps caused by projection-frame mismatch so East Asia clicks resolve to the correct country or region.

Purpose: The current SVG asset visually looks correct, but its real sphere frame does not match the `WORLD_PROJECTION_CONFIG`, causing systematic horizontal misalignment in click recognition.
Output: A corrected map frame, regression tests for Japan and Hong Kong, and Phase 2 geo detection that matches what users click.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-国家级真实地点识别/02-UAT.md
@.planning/debug/02-projection-frame-mismatch.md
@src/assets/world-map.svg
@src/services/map-projection.ts
@src/services/geo-lookup.spec.ts
@src/components/WorldMapStage.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align the rendered world frame with the fixed projection contract</name>
  <files>src/assets/world-map.svg, src/services/map-projection.ts, src/services/map-projection.spec.ts</files>
  <behavior>
    - Test 1: The visible sphere frame in `world-map.svg` spans the same horizontal extent as `WORLD_PROJECTION_CONFIG`.
    - Test 2: Forward/inverse projection assumptions still map center and edge points correctly after the fix.
    - Test 3: East Asia is no longer shifted west relative to user clicks.
  </behavior>
  <action>Update the world map asset so the actual geographic frame really occupies `x=40, y=80, width=1520, height=640`, or adjust the projection config to the exact rendered frame if that is the better fix. Keep the result projection-faithful and visually consistent with the existing poster style. Extend `map-projection.spec.ts` with a guard that would fail if the horizontal plotting frame drifts again.</action>
  <verify>rg 'viewBox=\"0 0 1600 800\"' src/assets/world-map.svg && pnpm test -- src/services/map-projection.spec.ts</verify>
  <done>The SVG asset and projection math now describe the same geographic surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression coverage for user-reported Japan and Hong Kong misidentification</name>
  <files>src/services/geo-lookup.spec.ts, src/components/WorldMapStage.vue, .planning/phases/02-国家级真实地点识别/02-UAT.md</files>
  <behavior>
    - Test 1: A Japan sample resolves to Japan rather than China.
    - Test 2: A Hong Kong sample resolves to Hong Kong rather than Myanmar.
    - Test 3: Existing invalid-area and boundary-conservative behaviors remain unchanged.
  </behavior>
  <action>Add concrete regression tests around the coordinates that represent obvious Japan and Hong Kong clicks under the corrected frame. If necessary, add a focused interaction test or helper in the map stage flow so the user-reported failures are represented in automated coverage. After fixing, update `02-UAT.md` gaps with the implemented root-cause resolution notes.</action>
  <verify>pnpm test -- src/services/geo-lookup.spec.ts && pnpm test</verify>
  <done>The specific UAT failures are reproducible in tests and fixed in code.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test
- [ ] pnpm build
- [ ] Japan sample resolves to Japan in tests
- [ ] Hong Kong sample resolves to Hong Kong in tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 2 click recognition no longer shows the user-reported East Asia offset
- UAT gap documentation is updated to reflect the implemented fix

</success_criteria>

---
phase: 02-国家级真实地点识别
plan: 04
type: execute
wave: 4
depends_on: []
gap_closure: true
files_modified:
  - src/components/WorldMapStage.vue
  - src/assets/world-map.svg
  - src/services/map-projection.ts
  - src/services/map-projection.spec.ts
  - src/services/geo-lookup.spec.ts
  - src/App.spec.ts
  - .planning/phases/02-国家级真实地点识别/02-UAT.md
autonomous: true
requirements:
  - GEO-01
  - GEO-02
user_setup: []
must_haves:
  truths:
    - User clicks align with the displayed map pixels, not an inferred frame that can drift.
    - Detected preview markers appear on or extremely near the actual click location across the map.
    - Automated coverage catches future click-to-render misalignment at the interaction level.
  artifacts:
    - src/components/WorldMapStage.vue
    - src/services/map-projection.ts
    - updated interaction/regression tests
  key_links:
    - The display layer and click inversion share one explicit rendered-frame contract
    - UAT-reported global left-up drift is reproduced and guarded by tests
---

<objective>
Fix the remaining global marker offset so user clicks line up with the recognized result across the whole map, not just in a few sample countries.

Purpose: Manual UAT shows the result marker is still generally offset up-left from the click position, which means the current mapping remains visually untrustworthy even after the East Asia constant fix.
Output: A rendering-aligned click mapping, interaction-level regression tests, and updated UAT gaps reflecting the true fix.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-国家级真实地点识别/02-UAT.md
@.planning/debug/02-projection-frame-mismatch.md
@src/components/WorldMapStage.vue
@src/assets/world-map.svg
@src/services/map-projection.ts
@src/services/map-projection.spec.ts
@src/services/geo-lookup.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace inferred click-frame math with a render-aligned frame contract</name>
  <files>src/components/WorldMapStage.vue, src/assets/world-map.svg, src/services/map-projection.ts, src/services/map-projection.spec.ts</files>
  <behavior>
    - Test 1: The click-to-geo mapping is derived from the actual rendered geographic frame, not a separately guessed box.
    - Test 2: Clicking the map no longer produces a generally up-left shifted detected marker.
    - Test 3: Projection tests fail if the displayed frame and inversion contract drift again.
  </behavior>
  <action>Refactor the map stage so the clickable geographic frame is explicitly shared with the rendered map surface. This can be done by exposing exact geographic frame metadata from the SVG/render contract, or by restructuring the rendered map so the interactive layer and the visible geographic plot use the same coordinates directly. Avoid another “magic constant” patch unless it is derived from the render source of truth.</action>
  <verify>pnpm test -- src/services/map-projection.spec.ts</verify>
  <done>The map no longer relies on an inferred frame that can visually drift from the rendered geography.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add interaction-level regression coverage for global click alignment</name>
  <files>src/components/WorldMapStage.vue, src/services/geo-lookup.spec.ts, src/App.spec.ts, .planning/phases/02-国家级真实地点识别/02-UAT.md</files>
  <behavior>
    - Test 1: A simulated click on an obvious land position creates a detected point close to the click position.
    - Test 2: Japan and Hong Kong remain correctly recognized after the alignment fix.
    - Test 3: Existing ocean-invalid and conservative-boundary behaviors still pass.
  </behavior>
  <action>Add regression coverage that exercises the real map interaction path instead of only service math. Prefer a component-level or integration-style test that clicks the rendered stage and asserts the resulting marker/selection stays near the clicked coordinates. Keep the existing Japan and Hong Kong regressions, but add at least one test that would fail on a global left-up drift. After implementation, update `02-UAT.md` resolutions to reflect the final fix status.</action>
  <verify>pnpm test -- src/services/geo-lookup.spec.ts && pnpm test</verify>
  <done>The user-reported global offset is represented in automated tests and fixed in the shipped interaction flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test
- [ ] pnpm build
- [ ] At least one interaction-level test covers click-to-marker alignment
- [ ] UAT gap notes updated with the final implemented fix
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Manual UAT no longer shows a general up-left marker offset
- Future regressions in click alignment are caught automatically

</success_criteria>

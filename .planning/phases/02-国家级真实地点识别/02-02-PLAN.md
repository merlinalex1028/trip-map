---
phase: 02-国家级真实地点识别
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/types/geo.ts
  - src/types/map-point.ts
  - src/data/geo/country-regions.geo.json
  - src/services/geo-lookup.ts
  - src/services/geo-lookup.spec.ts
  - src/stores/map-ui.ts
  - src/components/WorldMapStage.vue
  - src/components/PointPreviewDrawer.vue
  - src/App.vue
  - src/App.spec.ts
autonomous: true
requirements:
  - GEO-02
  - GEO-03
user_setup: []
must_haves:
  truths:
    - Valid land clicks return a country or region result from local static boundary data.
    - Invalid clicks (ocean, low-confidence coast, unresolved boundaries) do not create a point and show the approved gentle toast.
    - Successful detection reuses the existing map selection and drawer flow without introducing persistence or full CRUD yet.
  artifacts:
    - src/data/geo/country-regions.geo.json
    - src/services/geo-lookup.ts
    - src/services/geo-lookup.spec.ts
    - updated WorldMapStage / PointPreviewDrawer / map-ui flow
  key_links:
    - WorldMapStage chains projection inversion into geo lookup
    - geo-lookup returns a standardized result shaped for detected preview points
    - invalid detection updates a toast-like notice instead of leaving a stale selected point
---

<objective>
Integrate offline country and region lookup, then connect the result into the existing map-preview UX so valid clicks succeed and invalid clicks fail safely.

Purpose: Deliver the actual Phase 2 product value — trustworthy country/region recognition with conservative failure behavior.
Output: Local static boundary data, a geo lookup service, and a map interaction flow that opens a detected preview on success or a gentle toast on failure.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@PRD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-国家级真实地点识别/02-CONTEXT.md
@.planning/phases/02-国家级真实地点识别/02-RESEARCH.md
@.planning/phases/02-国家级真实地点识别/02-VALIDATION.md
@src/services/map-projection.ts
@src/components/WorldMapStage.vue
@src/components/PointPreviewDrawer.vue
@src/stores/map-ui.ts
@src/types/map-point.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add static country-region boundary data and a standardized geo lookup service</name>
  <files>src/types/geo.ts, src/data/geo/country-regions.geo.json, src/services/geo-lookup.ts, src/services/geo-lookup.spec.ts</files>
  <read_first>.planning/phases/02-国家级真实地点识别/02-CONTEXT.md, .planning/phases/02-国家级真实地点识别/02-RESEARCH.md, src/types/geo.ts, src/services/map-projection.ts</read_first>
  <behavior>
    - Test 1: The repo contains local boundary data with enough metadata to return country/region display names and codes.
    - Test 2: Geo lookup handles normal countries, special regions, and ocean misses.
    - Test 3: Lookup can reject low-confidence or unresolved hits instead of guessing.
  </behavior>
  <action>Create `src/data/geo/country-regions.geo.json` as the local boundary dataset used by Phase 2. Each feature must include `properties.countryCode`, `properties.countryName`, `properties.regionName`, `properties.displayName`, and `properties.bbox`. Include separate display metadata for at least Hong Kong (`HK`), Macau (`MO`), and Greenland (`GL`) so the lookup service can honor the Phase 2 region-first display rule. Update `src/types/geo.ts` to export `GeoDetectionResult` with the exact fields `kind`, `countryCode`, `countryName`, `regionName`, `displayName`, `lat`, `lng`, and `confidence`. Create `src/services/geo-lookup.ts` exporting `lookupCountryRegionByCoordinates(geo: GeoCoordinates)` and `isLowConfidenceBoundaryHit(geo: GeoCoordinates, result: GeoDetectionResult | null)`. The lookup must first filter features by `bbox`, then run precise polygon/multipolygon containment using a mature library or `d3-geo`, and must return `null` for ocean or unresolved clicks. Create `src/services/geo-lookup.spec.ts` with concrete cases for Lisbon -> Portugal/PT, Cairo -> Egypt/EG, Hong Kong -> HK with display name `Hong Kong`, Greenland -> GL with display name `Greenland`, and an Atlantic Ocean sample that returns `null`.</action>
  <verify>test -f src/data/geo/country-regions.geo.json && test -f src/services/geo-lookup.ts && test -f src/services/geo-lookup.spec.ts && rg 'countryCode|displayName|bbox' src/data/geo/country-regions.geo.json && rg 'lookupCountryRegionByCoordinates|isLowConfidenceBoundaryHit' src/services/geo-lookup.ts && pnpm test -- src/services/geo-lookup.spec.ts</verify>
  <acceptance_criteria>
    - src/data/geo/country-regions.geo.json contains `countryCode`
    - src/data/geo/country-regions.geo.json contains `"HK"`
    - src/data/geo/country-regions.geo.json contains `"MO"`
    - src/data/geo/country-regions.geo.json contains `"GL"`
    - src/services/geo-lookup.ts contains `lookupCountryRegionByCoordinates`
    - src/services/geo-lookup.ts contains `isLowConfidenceBoundaryHit`
    - src/services/geo-lookup.spec.ts contains `Hong Kong`
    - src/services/geo-lookup.spec.ts contains `Atlantic`
  </acceptance_criteria>
  <done>The project can now answer “which country or region is this click in?” from local static data without guessing on invalid samples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connect successful and failed geo detection into the map preview flow</name>
  <files>src/types/map-point.ts, src/stores/map-ui.ts, src/components/WorldMapStage.vue, src/components/PointPreviewDrawer.vue, src/App.vue, src/App.spec.ts, src/services/geo-lookup.ts</files>
  <read_first>src/types/map-point.ts, src/stores/map-ui.ts, src/components/WorldMapStage.vue, src/components/PointPreviewDrawer.vue, src/App.vue, src/services/geo-lookup.ts, .planning/phases/02-国家级真实地点识别/02-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Successful detection creates a non-persistent detected preview point and opens the existing drawer flow.
    - Test 2: Invalid or low-confidence detection clears the pending pulse, shows the approved toast, and leaves no selected detected point behind.
    - Test 3: Existing seed-point selection still works after the detection flow is added.
  </behavior>
  <action>Update `src/types/map-point.ts` so `MapPointSource` includes `'detected'`. Extend `src/stores/map-ui.ts` with `interactionNotice`, `setInteractionNotice`, `clearInteractionNotice`, and any minimal computed helpers needed to distinguish a temporary detected preview from seed/saved points. Update `src/components/WorldMapStage.vue` so background clicks run `normalizedPointToGeoCoordinates(...)`, then `lookupCountryRegionByCoordinates(...)`, and finally either: (a) build a non-persistent detected preview point with `id` in the format `detected-${countryCode}-${Math.round(lat * 100)}-${Math.round(lng * 100)}`, `source: 'detected'`, `name: displayName`, `countryName: displayName`, `countryCode`, `lat`, `lng`, projected `x/y`, `coordinatesLabel` formatted from the detected coordinates, `description: '识别成功，下一阶段可补充地点内容。'`, and `isFeatured: false`, then call `selectPoint`; or (b) clear the pending hit, call `clearSelection`, and set `interactionNotice.message` to `请点击有效陆地区域` or `请点击更靠近目标区域的位置` for low-confidence boundary clicks. Update `src/components/PointPreviewDrawer.vue` so the drawer renders detected preview points without edit controls and still shows title, country/region, coordinates, and description. Update `src/App.vue` to render the notice surface and keep it visually light, and update `src/App.spec.ts` so the app still mounts with Pinia after the new notice flow exists.</action>
  <verify>rg \"'detected'\" src/types/map-point.ts && rg 'interactionNotice|setInteractionNotice|clearInteractionNotice' src/stores/map-ui.ts && rg 'lookupCountryRegionByCoordinates|请点击有效陆地区域|请点击更靠近目标区域的位置' src/components/WorldMapStage.vue && rg 'coordinatesLabel|description' src/components/PointPreviewDrawer.vue && pnpm test</verify>
  <acceptance_criteria>
    - src/types/map-point.ts contains `'detected'`
    - src/stores/map-ui.ts contains `interactionNotice`
    - src/stores/map-ui.ts contains `setInteractionNotice`
    - src/components/WorldMapStage.vue contains `lookupCountryRegionByCoordinates`
    - src/components/WorldMapStage.vue contains `请点击有效陆地区域`
    - src/components/WorldMapStage.vue contains `请点击更靠近目标区域的位置`
    - src/components/PointPreviewDrawer.vue contains `coordinatesLabel`
    - src/App.vue contains `interactionNotice`
  </acceptance_criteria>
  <done>Valid clicks now produce a trustworthy detected preview result, while invalid clicks fail gently and leave no false point behind.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test
- [ ] rg 'lookupCountryRegionByCoordinates' src/services/geo-lookup.ts
- [ ] rg '请点击有效陆地区域|请点击更靠近目标区域的位置' src/components/WorldMapStage.vue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Land clicks return a country or region result from local data
- Ocean and low-confidence boundary clicks do not create false points and show the approved gentle guidance

</success_criteria>

<output>
After completion, create `.planning/phases/02-国家级真实地点识别/02-02-SUMMARY.md`
</output>

---
phase: 07-城市选择与兼容基线
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/geo/city-candidates.ts
  - src/types/geo.ts
  - src/types/map-point.ts
  - src/services/geo-lookup.ts
  - src/services/geo-lookup.spec.ts
  - src/services/point-storage.ts
  - src/services/point-storage.spec.ts
  - src/stores/map-points.ts
  - src/stores/map-points.spec.ts
autonomous: true
requirements:
  - DEST-01
  - DEST-04
  - DEST-05
  - DAT-05
user_setup: []
must_haves:
  truths:
    - Detection results expose stable city identity and ranked candidate semantics instead of only a single ad-hoc city string.
    - Saved v2 city records can persist stable city identity while v1 legacy snapshots still load safely with null city fields.
    - Domain-state logic can reuse an existing saved point by stable city ID before any duplicate draft is created.
  artifacts:
    - expanded city candidate dataset with explicit IDs and context labels
    - updated geo detection / point model types
    - point-storage normalization for legacy-safe city fields
    - map-points reuse helpers and regression tests
  key_links:
    - `geo-lookup.ts` emits candidate / fallback metadata that Phase 7 UI can render directly
    - `point-storage.ts` treats new city fields as optional for old snapshots but required for new city saves
    - `map-points.ts` becomes the single source of truth for saved-city reuse decisions
---

<objective>
Create the Phase 7 data backbone for city-first selection: stable city identity, ranked candidate metadata, reuse detection, and legacy-safe persistence.

Purpose: The UI cannot reliably present candidates or reuse old records until service, storage, and store layers all agree on what a city identity is.
Output: Stable `cityId`-based candidate contracts, compatibility-safe point persistence, and store helpers that can decide between reuse and new draft creation.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PRD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-城市选择与兼容基线/07-CONTEXT.md
@.planning/phases/07-城市选择与兼容基线/07-RESEARCH.md
@.planning/phases/07-城市选择与兼容基线/07-VALIDATION.md
@src/data/geo/city-candidates.ts
@src/types/geo.ts
@src/types/map-point.ts
@src/services/geo-lookup.ts
@src/services/point-storage.ts
@src/stores/map-points.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand city candidate and geo-lookup contracts into ranked candidate outputs with stable city identity</name>
  <files>src/data/geo/city-candidates.ts, src/types/geo.ts, src/services/geo-lookup.ts, src/services/geo-lookup.spec.ts</files>
  <read_first>src/data/geo/city-candidates.ts, src/types/geo.ts, src/services/geo-lookup.ts, src/services/geo-lookup.spec.ts, .planning/phases/07-城市选择与兼容基线/07-CONTEXT.md, .planning/phases/07-城市选择与兼容基线/07-RESEARCH.md</read_first>
  <behavior>
    - Test 1: High-confidence clicks still prioritize a city-first result, but the service also exposes a ranked candidate list that the UI can render.
    - Test 2: Same-name or nearby city candidates carry enough context to disambiguate them without relying on raw distance only.
    - Test 3: Low-confidence city matches are preserved as candidates instead of being silently collapsed into a plain country-only string.
  </behavior>
  <action>Refactor `src/data/geo/city-candidates.ts` so every city record has an explicit stable `id`, `name`, `countryCode`, and a human-readable `contextLabel` for disambiguation. Expand `src/types/geo.ts` with a concrete candidate contract such as `GeoCityCandidate`, including exact fields `cityId`, `cityName`, `contextLabel`, `matchLevel`, `distanceKm`, and `statusHint`. Update `src/services/geo-lookup.ts` so `lookupCountryRegionByCoordinates()` no longer relies on a single `cityName` mutation path alone: it must build a ranked candidate array ordered by click proximity, set exact status hints `更接近点击位置` for reliable candidates and `可能位置，需要确认` for low-confidence ones, and keep the fallback copy `未能可靠确认城市，已提供国家/地区继续记录` available when no candidate is reliable. Preserve the existing country/region safety checks and boundary conservatism. Extend `src/services/geo-lookup.spec.ts` with assertions that the returned result includes stable `cityId` values, ordered candidate arrays, and the exact low-confidence / fallback strings.</action>
  <verify>
    rg 'cityId|contextLabel|statusHint' src/types/geo.ts src/services/geo-lookup.ts src/data/geo/city-candidates.ts
    rg '更接近点击位置|可能位置，需要确认|未能可靠确认城市，已提供国家/地区继续记录' src/services/geo-lookup.ts src/services/geo-lookup.spec.ts
    <automated>pnpm test -- src/services/geo-lookup.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/data/geo/city-candidates.ts contains `id:`
    - src/data/geo/city-candidates.ts contains `contextLabel`
    - src/types/geo.ts contains `GeoCityCandidate`
    - src/services/geo-lookup.ts contains `cityId`
    - src/services/geo-lookup.ts contains `更接近点击位置`
    - src/services/geo-lookup.ts contains `可能位置，需要确认`
    - src/services/geo-lookup.ts contains `未能可靠确认城市，已提供国家/地区继续记录`
    - src/services/geo-lookup.spec.ts contains `cityId`
  </acceptance_criteria>
  <done>Phase 7 now has a service-layer contract that can power candidate-first city confirmation instead of guessing one city too early.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend persisted point models and map-point state for legacy-safe city identity and reuse indexing</name>
  <files>src/types/map-point.ts, src/services/point-storage.ts, src/services/point-storage.spec.ts, src/stores/map-points.ts, src/stores/map-points.spec.ts</files>
  <read_first>src/types/map-point.ts, src/services/point-storage.ts, src/services/point-storage.spec.ts, src/stores/map-points.ts, src/stores/map-points.spec.ts, src/data/seed-points.ts, .planning/phases/07-城市选择与兼容基线/07-CONTEXT.md, .planning/phases/07-城市选择与兼容基线/07-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Existing v1 snapshots without city identity fields still load as valid saved points with null city metadata.
    - Test 2: New v2 city records persist stable city identity fields alongside existing `lat/lng`, `x/y`, and display content.
    - Test 3: Store-level helpers can find and reopen an existing saved point by `cityId` before creating a duplicate draft.
  </behavior>
  <action>Expand `src/types/map-point.ts` so persisted/draft/display point shapes include exact nullable fields `cityId` and `cityContextLabel` while preserving existing `cityName`, `countryName`, `countryCode`, `lat`, `lng`, `x`, and `y`. Update `src/services/point-storage.ts` to normalize these exact fields from storage: if `cityId` or `cityContextLabel` are missing in a legacy snapshot, default them to `null` rather than treating the snapshot as corrupt. Keep snapshot version `1` for this phase, but make the new fields optional in the loader and always write them back for v2 city records. In `src/stores/map-points.ts`, add a concrete helper for city reuse such as `findSavedPointByCityId` and a higher-level action that can decide whether to open an existing saved point or proceed to draft creation. Ensure the reuse path is based on exact `cityId` equality, not `cityName` text. Extend `src/services/point-storage.spec.ts` and `src/stores/map-points.spec.ts` to assert null-safe legacy loading, stable `cityId` persistence, and city-ID reuse behavior.</action>
  <verify>
    rg 'cityId|cityContextLabel' src/types/map-point.ts src/services/point-storage.ts src/stores/map-points.ts
    rg 'legacy|cityId|reuse' src/services/point-storage.spec.ts src/stores/map-points.spec.ts
    <automated>pnpm test -- src/services/point-storage.spec.ts src/stores/map-points.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/types/map-point.ts contains `cityId`
    - src/types/map-point.ts contains `cityContextLabel`
    - src/services/point-storage.ts contains `cityId`
    - src/services/point-storage.ts contains `cityContextLabel`
    - src/stores/map-points.ts contains `findSavedPointByCityId`
    - src/stores/map-points.ts contains `cityId`
    - src/services/point-storage.spec.ts contains `legacy`
    - src/stores/map-points.spec.ts contains `cityId`
  </acceptance_criteria>
  <done>The data and state layers now support stable city identity, old-snapshot compatibility, and duplicate-city reuse before the UI flow is wired.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test -- src/services/geo-lookup.spec.ts
- [ ] pnpm test -- src/services/point-storage.spec.ts src/stores/map-points.spec.ts
- [ ] rg 'cityId' src/services/geo-lookup.ts src/types/map-point.ts src/stores/map-points.ts
- [ ] rg '未能可靠确认城市，已提供国家/地区继续记录' src/services/geo-lookup.ts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Detection results can describe ranked city candidates with stable identity
- New city records persist stable city identity while old records remain valid
- Existing saved cities can be found by `cityId` before duplicate draft creation

</success_criteria>

<output>
After completion, create `.planning/phases/07-城市选择与兼容基线/07-01-SUMMARY.md`
</output>

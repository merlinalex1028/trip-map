---
phase: 07-城市选择与兼容基线
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/data/geo/city-candidates.ts
  - src/services/geo-lookup.ts
  - src/services/geo-lookup.spec.ts
  - src/services/city-search.ts
  - src/services/city-search.spec.ts
  - src/components/PointPreviewDrawer.vue
  - src/components/PointPreviewDrawer.spec.ts
  - src/components/WorldMapStage.spec.ts
autonomous: true
requirements:
  - DEST-01
  - DEST-02
  - DEST-04
gap_closure: true
must_haves:
  truths:
    - 点击常见真实城市位置后，候选确认抽屉默认能显示最多 3 个城市候选，而不是大面积空列表。
    - 在候选确认抽屉输入中文或英文城市名后，结果会基于更完整的离线城市索引收窄，而不是只在当前极小候选池里过滤。
    - 从搜索结果确认城市时，仍走和默认候选相同的确认/复用链路，不会分叉成单独流程。
  artifacts:
    - 覆盖常见国家/地区的离线城市索引与别名数据
    - 由更完整城市索引补充的 geo lookup 候选输出
    - 面向 drawer 的离线城市搜索 helper 与中文输入回归测试
    - 覆盖非 Kyoto/Lisbon/Cairo 样例的组件与服务回归测试
  key_links:
    - `src/services/geo-lookup.ts` 必须从扩充后的离线城市索引产出候选，供 `candidate-select` 抽屉直接消费
    - `src/components/PointPreviewDrawer.vue` 必须把搜索查询接到离线城市索引，而不是只过滤 `pendingCitySelection.cityCandidates`
    - 搜索命中的结果必须继续调用 `confirmPendingCitySelection`，保持点击候选与搜索结果同一条复用/建草稿链路
---

<objective>
Close the two diagnosed Phase 7 UAT gaps by making city candidates and city search usable outside the tiny demo dataset.

Purpose: Phase 7 currently passes synthetic tests but fails real use because city lookup only covers a handful of cities and drawer search only filters the in-memory candidate pool.
Output: A broader offline city index, geo lookup candidates sourced from that index, and drawer search that supports real Chinese/English city queries without introducing a second selection flow.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-城市选择与兼容基线/07-CONTEXT.md
@.planning/phases/07-城市选择与兼容基线/07-RESEARCH.md
@.planning/phases/07-城市选择与兼容基线/07-UAT.md
@.planning/phases/07-城市选择与兼容基线/07-01-SUMMARY.md
@.planning/phases/07-城市选择与兼容基线/07-02-SUMMARY.md
@src/data/geo/city-candidates.ts
@src/services/geo-lookup.ts
@src/components/PointPreviewDrawer.vue
@src/components/PointPreviewDrawer.spec.ts
@src/components/WorldMapStage.vue

<interfaces>
From `src/types/geo.ts`:
```ts
export interface GeoCityCandidate {
  cityId: string
  cityName: string
  contextLabel: string
  matchLevel: 'high' | 'possible'
  distanceKm: number
  statusHint: '更接近点击位置' | '可能位置，需要确认'
}
```

From `src/stores/map-points.ts`:
```ts
function startPendingCitySelection(fallbackPoint: DraftMapPoint, cityCandidates: GeoCityCandidate[])
function findSavedPointByCityId(cityId: string)
function confirmPendingCitySelection(candidate: GeoCityCandidate)
function continuePendingWithFallback()
```

Current constraint from `src/components/PointPreviewDrawer.vue`:
```ts
const cityCandidates = pendingCitySelection.value?.cityCandidates ?? []
return cityCandidates
  .filter((candidate) => candidate.cityName.toLowerCase().includes(normalizedQuery))
  .slice(0, 3)
```
This exact local filtering behavior is the gap to replace.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand offline city coverage and feed geo lookup candidates from the broader index</name>
  <files>src/data/geo/city-candidates.ts, src/services/geo-lookup.ts, src/services/geo-lookup.spec.ts</files>
  <behavior>
    - Test 1: Clicking real locations in non-demo countries still returns ranked city candidates instead of an empty candidate list and immediate fallback.
    - Test 2: Candidate ranking remains proximity-first per D-06, while the UI can continue showing at most three options per D-03.
    - Test 3: When a country truly has no usable city coverage, the existing explanatory fallback copy still appears instead of silent emptiness.
  </behavior>
  <action>Expand `src/data/geo/city-candidates.ts` from the current 5-city demo table into a broader offline index that covers the common countries you use for regression, and give each city explicit alias/search metadata for later query matching. Do not change the stable `cityId` contract from 07-01. In `src/services/geo-lookup.ts`, stop treating the tiny context table as the only candidate source: build ranked candidates from the expanded offline index, preferably scoped by `countryCode` first and supplemented by context keys when available, so typical clicks produce candidate rows with `cityId`, `cityName`, `contextLabel`, `matchLevel`, `distanceKm`, and status hints already populated. Preserve D-04 and D-06 semantics: candidates still carry context labels and remain distance-sorted; fallback copy stays exactly `未能可靠确认城市，已提供国家/地区继续记录` when no reliable city can be surfaced. Extend `src/services/geo-lookup.spec.ts` with realistic non-sample cases, not just Kyoto/Lisbon/Cairo, so this gap cannot regress back to demo-only coverage.</action>
  <verify>
    <automated>pnpm test -- src/services/geo-lookup.spec.ts</automated>
  </verify>
  <done>Geo lookup now returns usable candidate data for common real clicks beyond the original demo cities, while true no-city cases still fall back cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace candidate-pool filtering with offline city search that supports Chinese and English input</name>
  <files>src/services/city-search.ts, src/services/city-search.spec.ts, src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/components/WorldMapStage.spec.ts</files>
  <behavior>
    - Test 1: With an empty or sparse pending candidate pool, typing a city query still returns offline search results from the broader city index.
    - Test 2: Chinese input and English input can both hit the intended city aliases per D-02.
    - Test 3: Clicking a search result still goes through `confirmPendingCitySelection` per D-11, so existing-city reuse and new-draft creation stay unified.
  </behavior>
  <action>Create `src/services/city-search.ts` as the single offline search helper over the expanded city index. Normalize query matching for Chinese and English aliases, prioritize the current pending country/region context when ranking results, and return `GeoCityCandidate`-shaped results so the drawer can hand them directly to `confirmPendingCitySelection`. In `src/components/PointPreviewDrawer.vue`, keep D-02 and D-03 intact: no query still shows the default top 3 pending candidates, but once a query exists the UI must use the offline search helper instead of filtering only `pendingCitySelection.cityCandidates`. Preserve the existing `已存在记录` override by checking `findSavedPointByCityId`, and do not create a separate search-only save path. Add regressions in `src/components/PointPreviewDrawer.spec.ts`, `src/components/WorldMapStage.spec.ts`, and `src/services/city-search.spec.ts` for Chinese input, empty-pool rescue, and search-result confirmation using the same reuse/draft flow as default candidates.</action>
  <verify>
    <automated>pnpm test -- src/services/city-search.spec.ts src/components/PointPreviewDrawer.spec.ts src/components/WorldMapStage.spec.ts</automated>
  </verify>
  <done>The drawer search is now backed by the offline city index, supports Chinese/English input, and still reuses the same city confirmation behavior as default candidates.</done>
</task>

</tasks>

<verification>
- `pnpm test -- src/services/geo-lookup.spec.ts src/services/city-search.spec.ts src/components/PointPreviewDrawer.spec.ts src/components/WorldMapStage.spec.ts`
- `pnpm build`
- Confirm at least one regression covers a non-demo country click and one regression covers Chinese city search input.
</verification>

<success_criteria>
- Phase 7 UAT gap 1 is closed: clicking common supported cities opens a candidate-first drawer with visible candidates instead of an empty state.
- Phase 7 UAT gap 2 is closed: searching in the drawer can find cities from the broader offline index, including Chinese input.
- Clicked candidates and searched candidates still share the same reuse-or-draft behavior and do not bypass existing Phase 7 rules.
</success_criteria>

<output>
After completion, create `.planning/phases/07-城市选择与兼容基线/07-03-SUMMARY.md`
</output>

---
phase: 07-城市选择与兼容基线
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/components/WorldMapStage.vue
  - src/components/WorldMapStage.spec.ts
  - src/components/PointPreviewDrawer.vue
  - src/components/PointPreviewDrawer.spec.ts
  - src/stores/map-points.ts
  - src/stores/map-points.spec.ts
  - src/stores/map-ui.ts
  - src/App.vue
  - src/App.spec.ts
autonomous: true
requirements:
  - DEST-01
  - DEST-02
  - DEST-03
  - DEST-04
  - DEST-05
  - DAT-05
user_setup: []
must_haves:
  truths:
    - Clicking the map opens a candidate-first confirmation surface with at most three candidates and a visible search input.
    - When no city is reliable, users can explicitly continue as country/region with explanatory copy instead of being blocked.
    - Both click-picked and search-picked cities reuse existing records by `cityId` with a light notice, otherwise they create a new draft.
  artifacts:
    - candidate-first drawer UI with search and fallback CTA
    - updated stage/store wiring for candidate confirmation and reuse
    - regression tests for candidate ordering, fallback CTA, and reuse notice
  key_links:
    - `WorldMapStage.vue` feeds ranked candidate results into the confirmation UI instead of immediately materializing one draft
    - `PointPreviewDrawer.vue` becomes the lightweight candidate/search/fallback surface for Phase 7
    - `map-points.ts` unifies click and search selection through the same reuse-or-draft decision path
---

<objective>
Wire the candidate-first user flow for Phase 7: lightweight search, explanatory fallback, and duplicate-city reuse through the existing drawer-driven experience.

Purpose: Once stable city identity exists, the user still needs a trustworthy interaction surface that makes city-first selection feel deliberate rather than magical.
Output: A candidate-first confirmation drawer, explicit country/region fallback action, and unified reuse-or-draft behavior for both click and search.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@PRD.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-城市选择与兼容基线/07-CONTEXT.md
@.planning/phases/07-城市选择与兼容基线/07-RESEARCH.md
@.planning/phases/07-城市选择与兼容基线/07-VALIDATION.md
@src/components/WorldMapStage.vue
@src/components/PointPreviewDrawer.vue
@src/stores/map-points.ts
@src/stores/map-ui.ts
@src/App.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace immediate draft creation with a candidate-first confirmation flow and lightweight city search</name>
  <files>src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts, src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/stores/map-points.ts, src/stores/map-points.spec.ts</files>
  <read_first>src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts, src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/stores/map-points.ts, src/stores/map-points.spec.ts, src/services/geo-lookup.ts, .planning/phases/07-城市选择与兼容基线/07-CONTEXT.md, .planning/phases/07-城市选择与兼容基线/07-RESEARCH.md</read_first>
  <behavior>
    - Test 1: A map click opens a candidate-first confirmation state with at most three candidates and a visible lightweight search input instead of directly saving a city draft.
    - Test 2: Candidate rows show city name, country/upper-region context, and status hints such as `更接近点击位置`, `可能位置，需要确认`, or `已存在记录`.
    - Test 3: Search results go through the same selection path as click candidates rather than a special-case branch.
  </behavior>
  <action>Refactor `src/components/WorldMapStage.vue` and `src/stores/map-points.ts` so a successful detection no longer immediately calls `startDraftFromDetection(...)` with a single city assumption. Introduce an explicit pending city-selection state that stores the ranked candidate results plus the fallback country/region target. Update `src/components/PointPreviewDrawer.vue` so when this state is active it renders a candidate-first panel with: an input whose placeholder is exactly `搜索城市`, a list of at most three default candidates, and per-row metadata showing city name, context label, and one exact status hint from the candidate contract. Add a unified selection action in `map-points.ts` that both candidate clicks and search result clicks call. When a selected candidate has no saved point by `cityId`, that action should create the draft point from the selected city candidate and then return the drawer to the existing `detected-preview` flow. Extend `src/components/WorldMapStage.spec.ts`, `src/components/PointPreviewDrawer.spec.ts`, and `src/stores/map-points.spec.ts` to assert the search input, candidate cap of three, status-hint rendering, and shared click/search selection path.</action>
  <verify>rg '搜索城市|更接近点击位置|可能位置，需要确认|已存在记录' src/components/PointPreviewDrawer.vue src/stores/map-points.ts && rg 'at most three|搜索城市|已存在记录' src/components/PointPreviewDrawer.spec.ts src/components/WorldMapStage.spec.ts src/stores/map-points.spec.ts && pnpm test -- src/components/WorldMapStage.spec.ts src/components/PointPreviewDrawer.spec.ts src/stores/map-points.spec.ts</verify>
  <acceptance_criteria>
    - src/components/PointPreviewDrawer.vue contains `搜索城市`
    - src/components/PointPreviewDrawer.vue contains `更接近点击位置`
    - src/components/PointPreviewDrawer.vue contains `可能位置，需要确认`
    - src/components/PointPreviewDrawer.vue contains `已存在记录`
    - src/stores/map-points.ts contains `cityId`
    - src/stores/map-points.ts contains `startDraftFromDetection`
    - src/components/PointPreviewDrawer.spec.ts contains `搜索城市`
    - src/components/WorldMapStage.spec.ts contains `已存在记录`
  </acceptance_criteria>
  <done>The Phase 7 UI now presents city-first confirmation and search as a lightweight, candidate-driven step instead of auto-guessing a single city.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit country fallback and unified reuse notice behavior for click and search selections</name>
  <files>src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts, src/stores/map-points.ts, src/stores/map-ui.ts, src/App.vue, src/App.spec.ts</files>
  <read_first>src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/components/WorldMapStage.vue, src/components/WorldMapStage.spec.ts, src/stores/map-points.ts, src/stores/map-ui.ts, src/App.vue, src/App.spec.ts, .planning/phases/07-城市选择与兼容基线/07-CONTEXT.md</read_first>
  <behavior>
    - Test 1: When no city is reliable, the primary action is an explicit country/region fallback CTA rather than a dead-end state.
    - Test 2: Both click-selected and search-selected existing cities reopen the saved record directly and emit a light reuse notice.
    - Test 3: Legacy saved points without city identity still remain viewable/editable and are never forced into the city-confirmation path.
  </behavior>
  <action>Complete the Phase 7 confirmation flow by adding an explicit fallback CTA to `src/components/PointPreviewDrawer.vue` with the exact action label `按国家/地区继续记录` and the exact explanatory copy `未能可靠确认城市，已提供国家/地区继续记录`. Wire that CTA through `src/stores/map-points.ts` so it creates the draft from the fallback country/region target and then reuses the existing `detected-preview` / save flow. In the same store, implement the unified reuse notice string prefix `已打开你记录过的` and make both click-picked candidates and search-picked candidates call the same reuse-or-draft action: if `cityId` already exists in a saved point, open that point in view mode and send the light notice through `map-ui.ts`; otherwise create a new draft. Keep legacy saved points without `cityId` out of forced candidate confirmation and preserve their existing view/edit behavior in `App.vue` and drawer rendering. Extend `src/components/PointPreviewDrawer.spec.ts`, `src/components/WorldMapStage.spec.ts`, and `src/App.spec.ts` to assert the exact fallback CTA/copy, the reuse notice prefix, and that legacy points can still open normally without city identity.</action>
  <verify>rg '按国家/地区继续记录|未能可靠确认城市，已提供国家/地区继续记录|已打开你记录过的' src/components/PointPreviewDrawer.vue src/stores/map-points.ts src/stores/map-ui.ts && rg '按国家/地区继续记录|已打开你记录过的|legacy' src/components/PointPreviewDrawer.spec.ts src/components/WorldMapStage.spec.ts src/App.spec.ts && pnpm test -- src/components/WorldMapStage.spec.ts src/components/PointPreviewDrawer.spec.ts src/App.spec.ts</verify>
  <acceptance_criteria>
    - src/components/PointPreviewDrawer.vue contains `按国家/地区继续记录`
    - src/components/PointPreviewDrawer.vue contains `未能可靠确认城市，已提供国家/地区继续记录`
    - src/stores/map-points.ts contains `已打开你记录过的`
    - src/stores/map-points.ts contains `cityId`
    - src/components/PointPreviewDrawer.spec.ts contains `按国家/地区继续记录`
    - src/components/WorldMapStage.spec.ts contains `已打开你记录过的`
    - src/App.spec.ts contains `legacy`
  </acceptance_criteria>
  <done>Country fallback, reuse notices, and legacy compatibility now behave as one coherent Phase 7 flow instead of separate ad-hoc exceptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm test -- src/components/WorldMapStage.spec.ts src/components/PointPreviewDrawer.spec.ts src/App.spec.ts
- [ ] rg '按国家/地区继续记录' src/components/PointPreviewDrawer.vue
- [ ] rg '已打开你记录过的' src/stores/map-points.ts
- [ ] rg '搜索城市' src/components/PointPreviewDrawer.vue
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Map clicks open a candidate-first flow with search instead of prematurely committing one city
- No-reliable-city paths offer explicit country/region continuation with explanatory copy
- Existing saved cities are reused consistently across click and search with a light notice

</success_criteria>

<output>
After completion, create `.planning/phases/07-城市选择与兼容基线/07-02-SUMMARY.md`
</output>

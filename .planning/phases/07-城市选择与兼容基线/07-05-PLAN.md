---
phase: 07-城市选择与兼容基线
plan: 05
type: execute
wave: 5
depends_on:
  - 04
files_modified:
  - src/services/geo-lookup.ts
  - src/services/geo-lookup.spec.ts
  - src/services/city-search.ts
  - src/services/city-search.spec.ts
  - src/components/PointPreviewDrawer.vue
  - src/components/PointPreviewDrawer.spec.ts
  - src/components/WorldMapStage.spec.ts
autonomous: true
requirements:
  - DEST-01
  - DEST-02
  - DEST-04
  - DEST-05
gap_closure: true
must_haves:
  truths:
    - "常见真实城市点击会进入候选确认抽屉并看到最多 3 个候选，而不是因为样例数据稀疏直接落入空候选。"
    - "输入中文或英文城市名时，搜索会命中扩展后的离线目录，并继续走同一条确认 / 复用链路。"
    - "当仍无可靠城市时，系统继续显示明确 fallback 提示与国家/地区回退动作，不会因为扩容而破坏保守兜底。"
  artifacts:
    - path: "src/services/geo-lookup.ts"
      provides: "基于扩展目录的候选构建与排序"
      contains: "buildRankedCityCandidates"
    - path: "src/services/city-search.ts"
      provides: "面向抽屉的扩展离线搜索"
      contains: "searchOfflineCities"
    - path: "src/components/PointPreviewDrawer.vue"
      provides: "默认候选与搜索结果共用同一确认入口"
      contains: "confirmPendingCitySelection"
  key_links:
    - from: "src/services/geo-lookup.ts"
      to: "src/data/geo/city-candidates.ts"
      via: "expanded grouped indexes"
      pattern: "cityCandidatesBy"
    - from: "src/components/PointPreviewDrawer.vue"
      to: "src/services/city-search.ts"
      via: "query -> offline search results"
      pattern: "searchOfflineCities"
    - from: "src/components/PointPreviewDrawer.vue"
      to: "src/stores/map-points.ts"
      via: "candidate/search result -> confirmPendingCitySelection"
      pattern: "confirmPendingCitySelection"
---

<objective>
Consume the expanded offline city catalog in lookup, search, and drawer regressions so the remaining Phase 07 UAT gap is closed in actual behavior, not just data volume.

Purpose: The catalog alone is insufficient unless map clicks, drawer search, and reuse logic all use it consistently under the existing city-first flow.
Output: Geo lookup, city search, and drawer behavior that all run on the expanded offline catalog while preserving Phase 7's stable `cityId`, fallback, and reuse semantics.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-城市选择与兼容基线/07-CONTEXT.md
@.planning/phases/07-城市选择与兼容基线/07-RESEARCH.md
@.planning/phases/07-城市选择与兼容基线/07-UAT.md
@.planning/debug/phase07-remaining-city-coverage-gap.md
@src/data/geo/city-candidates.ts
@src/services/geo-lookup.ts
@src/services/city-search.ts
@src/components/PointPreviewDrawer.vue
@src/components/WorldMapStage.vue

<interfaces>
From `src/types/geo.ts`:
```ts
export interface GeoCityCandidate {
  cityId: string
  cityName: string
  contextLabel: string
  matchLevel: 'high' | 'possible'
  distanceKm: number
  statusHint: '更接近点击位置' | '可能位置，需要确认'
}
```

From `src/stores/map-points.ts`:
```ts
function findSavedPointByCityId(cityId: string)
function confirmPendingCitySelection(candidate: GeoCityCandidate)
function continuePendingWithFallback()
```

From `src/data/geo/city-candidates.ts` after 07-04:
```ts
export const cityCandidates
export const cityCandidatesByCountry
export const cityCandidatesByContext
```

Plan 05 must consume the expanded catalog through these existing contracts rather than creating a new search-only or lookup-only data path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Feed geo lookup from the expanded offline catalog and harden multi-country click regressions</name>
  <files>src/services/geo-lookup.ts, src/services/geo-lookup.spec.ts</files>
  <behavior>
    - Test 1: Realistic clicks in multiple non-demo countries now return ranked city candidates instead of immediate fallback-only results.
    - Test 2: Candidate ranking remains proximity-first per D-06, candidate rows keep context labels per D-04, and the default candidate list exposed to the UI stays capped at three items per D-03.
    - Test 3: Truly unsupported or too-distant cases still preserve the exact fallback copy `未能可靠确认城市，已提供国家/地区继续记录` per D-07 and D-08.
  </behavior>
  <action>Rework `src/services/geo-lookup.ts` to consume the expanded offline catalog from 07-04 without assuming a tiny sample pool. Keep the current `GeoCityCandidate` and `GeoDetectionResult` contracts intact, continue using stable IDs from D-10, and preserve `city-high` / `city-possible` semantics. Rank candidates by distance first per D-06, keep context labels visible per D-04, and only expose the top three ranked candidates to the drawer per D-03. Extend `src/services/geo-lookup.spec.ts` with multi-continent realistic click samples beyond the original Phase 7 demo set, plus a true fallback case that proves the explanatory copy remains unchanged. Do not introduce online APIs, reverse geocoding, or separate fallback heuristics outside the existing offline path.</action>
  <verify>
    <automated>pnpm test -- src/services/geo-lookup.spec.ts</automated>
  </verify>
  <done>Geo lookup reliably surfaces top-three city candidates from the expanded offline catalog for common real clicks while preserving the exact fallback behavior for unsupported cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route drawer search and UAT-style regressions through the same expanded catalog</name>
  <files>src/services/city-search.ts, src/services/city-search.spec.ts, src/components/PointPreviewDrawer.vue, src/components/PointPreviewDrawer.spec.ts, src/components/WorldMapStage.spec.ts</files>
  <behavior>
    - Test 1: Chinese and English queries for cities outside the original 43-city set still return results from the expanded catalog even when the pending pool is empty or sparse, satisfying D-02.
    - Test 2: With no query, the drawer still shows the default top three pending candidates per D-03; candidate rows retain context labels and saved-point override status per D-04 and D-12.
    - Test 3: Clicking a searched result still calls `confirmPendingCitySelection` and reuses existing saved cities by `cityId` per D-09, D-10, and D-11, rather than creating a separate search-only branch.
  </behavior>
  <action>Update `src/services/city-search.ts` so search ranking consumes the expanded catalog from 07-04 and returns `GeoCityCandidate` results compatible with the existing Phase 7 flow. In `src/components/PointPreviewDrawer.vue`, keep the candidate-first UX locked by D-02 and D-03: blank query shows the pending top-three candidates, non-blank query uses `searchOfflineCities`, and both default candidates and search hits pass through `confirmPendingCitySelection`. Preserve the `已存在记录` override by continuing to check `findSavedPointByCityId`, and do not alter legacy saved-point behavior, popup work, or boundary work. Extend `src/services/city-search.spec.ts`, `src/components/PointPreviewDrawer.spec.ts`, and `src/components/WorldMapStage.spec.ts` with UAT-style regressions covering at least one Chinese query, one English query, one sparse-country click, and one existing-city reuse case so this exact coverage gap cannot hide behind happy-path samples again.</action>
  <verify>
    <automated>pnpm test -- src/services/city-search.spec.ts src/components/PointPreviewDrawer.spec.ts src/components/WorldMapStage.spec.ts && pnpm build</automated>
  </verify>
  <done>Drawer search, click candidates, and saved-city reuse all consume the expanded offline catalog through one shared confirmation path, with UAT-style regressions guarding the fix.</done>
</task>

</tasks>

<verification>
- `pnpm test -- src/services/geo-lookup.spec.ts src/services/city-search.spec.ts src/components/PointPreviewDrawer.spec.ts src/components/WorldMapStage.spec.ts`
- `pnpm build`
- Confirm the new regressions include both Chinese/English search and multiple realistic non-demo click locations.
</verification>

<success_criteria>
- Phase 07's remaining UAT gap is closed at behavior level: common real clicks and searches now surface usable city candidates instead of sample-data dead ends.
- Default candidates and search results still share the same `cityId`-based reuse or new-draft logic.
- Unsupported cases still present the exact explanatory fallback path rather than silent emptiness or overconfident guesses.
</success_criteria>

<output>
After completion, create `.planning/phases/07-城市选择与兼容基线/07-05-SUMMARY.md`
</output>

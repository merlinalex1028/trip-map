---
phase: 07-城市选择与兼容基线
plan: 04
type: execute
wave: 4
depends_on:
  - 03
files_modified:
  - src/data/geo/city-candidates.ts
  - src/data/geo/city-candidates.spec.ts
autonomous: true
requirements:
  - DEST-01
  - DEST-02
  - DEST-04
gap_closure: true
must_haves:
  truths:
    - "Phase 07 不再依赖 43 城 / 21 国的演示级离线索引，城市基线覆盖对世界边界国家显著提升。"
    - "扩展后的离线城市目录继续提供稳定 `cityId`、上下文标签与别名，供点击候选和搜索共用。"
    - "自动化能够量化覆盖规模与数据完整性，防止城市数据再次退回少量样例。"
  artifacts:
    - path: "src/data/geo/city-candidates.ts"
      provides: "扩展后的离线城市目录、分组索引与覆盖统计导出"
      contains: "export const cityCandidates"
    - path: "src/data/geo/city-candidates.spec.ts"
      provides: "覆盖规模、唯一性与国家映射回归"
      contains: "describe('city candidate catalog'"
  key_links:
    - from: "src/data/geo/city-candidates.ts"
      to: "src/data/geo/country-regions.geo.json"
      via: "coverage stats / countryCode validation"
      pattern: "countryCode"
    - from: "src/data/geo/city-candidates.ts"
      to: "src/services/geo-lookup.ts"
      via: "cityCandidatesByCountry / cityCandidatesByContext exports"
      pattern: "export const cityCandidatesBy"
---

<objective>
Close the diagnosed Phase 07 root cause by replacing the demo-scale city sample table with a coverage-audited offline city catalog.

Purpose: The current gap is no longer about drawer behavior; it is about the offline city source remaining too small to support real map clicks and search.
Output: A materially larger offline city catalog with stable IDs, reusable grouped indexes, and automated coverage assertions that make future shrinkage visible.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-城市选择与兼容基线/07-CONTEXT.md
@.planning/phases/07-城市选择与兼容基线/07-RESEARCH.md
@.planning/phases/07-城市选择与兼容基线/07-UAT.md
@.planning/phases/07-城市选择与兼容基线/07-03-SUMMARY.md
@.planning/phases/07-城市选择与兼容基线/07-UAT.md
@src/data/geo/city-candidates.ts
@src/data/geo/country-regions.geo.json

<interfaces>
From `src/data/geo/city-candidates.ts`:
```ts
export interface CityCandidate {
  id: string
  name: string
  countryCode: string
  contextLabel: string
  lat: number
  lng: number
  highRadiusKm: number
  possibleRadiusKm: number
  aliases: string[]
  contextKeys: string[]
}

export const cityCandidates = cityCandidateDefinitions
export const cityCandidatesByCountry = groupByCountry(cityCandidateDefinitions)
export const cityCandidatesByContext = groupByContext(cityCandidateDefinitions)
```

These exports are already consumed by `geo-lookup` and `city-search`. Preserve them so downstream plans do not need interface churn.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the demo-scale city sample with a coverage-audited offline city catalog</name>
  <files>src/data/geo/city-candidates.ts, src/data/geo/city-candidates.spec.ts</files>
  <behavior>
    - Test 1: The catalog exposes coverage stats showing at least 180 supported country/region codes and at least 280 total cities, preventing regression back to the current 43 / 21 scale.
    - Test 2: Every candidate keeps a unique stable `cityId` per D-10, uses a `countryCode` present in `country-regions.geo.json`, has a non-empty `contextLabel`, and remains searchable through `name` plus aliases per D-02 and D-04.
    - Test 3: Travel-heavy countries already central to Phase 7 and Phase 8 flows (JP, US, FR, GB, CN, IN, AU, CA) expose multiple geographically distinct cities so the product does not collapse into one-capital-only behavior.
  </behavior>
  <action>Expand `src/data/geo/city-candidates.ts` from the current 43-city hand-curated sample into a structured offline catalog sized for real-world usage. Keep all existing shipped IDs stable per D-10 and D-15; entries such as `jp-kyoto`, `fr-paris`, and `us-new-york` must not be renamed. Preserve the current export contract (`cityCandidates`, `cityCandidatesByCountry`, `cityCandidatesByContext`) and add explicit coverage helpers such as `cityCoverageStats` and `countriesMissingCityCoverage` so tests can assert scale. Stay fully offline per PROJECT/ROADMAP: do not add runtime fetching, online reverse geocoding, or pseudo-city placeholders. For tiny territories, a single real capital/primary city is acceptable; for large or travel-heavy countries, include multiple real cities across different regions so D-02 and D-04 remain meaningful in later ranking. Add `src/data/geo/city-candidates.spec.ts` to lock down coverage thresholds, unique IDs, valid country-code mapping, and multi-city expectations for the key countries above.</action>
  <verify>
    <automated>pnpm test -- src/data/geo/city-candidates.spec.ts</automated>
  </verify>
  <done>The offline city catalog remains backward-compatible, grows far beyond the current sample-scale dataset, and has automated coverage/integrity checks that make shrinkage visible.</done>
</task>

</tasks>

<verification>
- `pnpm test -- src/data/geo/city-candidates.spec.ts`
- Confirm the spec asserts both coverage scale and data integrity, not just a handful of sample cities.
</verification>

<success_criteria>
- The repository contains a clearly larger offline city baseline that is no longer a 43-city demo table.
- Coverage and integrity are measurable in automated tests, including supported-country counts and unique stable IDs.
- Downstream consumers can keep using `cityCandidates`, `cityCandidatesByCountry`, and `cityCandidatesByContext` without interface churn.
</success_criteria>

<output>
After completion, create `.planning/phases/07-城市选择与兼容基线/07-04-SUMMARY.md`
</output>

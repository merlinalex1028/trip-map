---
phase: 48-visual-qa-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/scripts/seed-visual-qa.mjs
  - .planning/phases/48-visual-qa-accessibility/evidence/seed-data.md
  - .planning/phases/48-visual-qa-accessibility/evidence/desktop-checklist.md
  - .planning/phases/48-visual-qa-accessibility/evidence/desktop-landing.png
  - .planning/phases/48-visual-qa-accessibility/evidence/desktop-map.png
  - .planning/phases/48-visual-qa-accessibility/evidence/desktop-footprint-dialog.png
  - .planning/phases/48-visual-qa-accessibility/evidence/desktop-journal.png
  - .planning/phases/48-visual-qa-accessibility/evidence/desktop-memories.png
autonomous: true
requirements: [QA-01, QA-02, QA-03, QA-04]
must_haves:
  truths:
    - "Executor has a reproducible populated QA account or seeded dataset before claiming screenshot coverage."
    - "Desktop screenshot evidence exists for landing, authenticated map, opened footprint dialog, populated journal, and populated memories."
    - "Checklist records overlap, truncation, unreadable text, long-text risk, missing map visuals, missing marker visuals, missing chart visuals, keyboard/focus notes, and reduced-motion notes."
  artifacts:
    - path: "apps/server/scripts/seed-visual-qa.mjs"
      provides: "repeatable visual QA account and records seeding"
    - path: ".planning/phases/48-visual-qa-accessibility/evidence/seed-data.md"
      provides: "exact seed account and environment notes"
    - path: ".planning/phases/48-visual-qa-accessibility/evidence/desktop-checklist.md"
      provides: "desktop QA checklist and repair queue"
    - path: ".planning/phases/48-visual-qa-accessibility/evidence/desktop-landing.png"
      provides: "landing screenshot"
    - path: ".planning/phases/48-visual-qa-accessibility/evidence/desktop-map.png"
      provides: "map screenshot with Leaflet surface and saved star markers"
    - path: ".planning/phases/48-visual-qa-accessibility/evidence/desktop-footprint-dialog.png"
      provides: "opened footprint date dialog screenshot"
    - path: ".planning/phases/48-visual-qa-accessibility/evidence/desktop-journal.png"
      provides: "populated journal screenshot"
    - path: ".planning/phases/48-visual-qa-accessibility/evidence/desktop-memories.png"
      provides: "populated memories screenshot with four chart panels"
  key_links:
    - from: "apps/server/scripts/seed-visual-qa.mjs"
      to: ".planning/phases/48-visual-qa-accessibility/evidence/seed-data.md"
      via: "documents VISUAL_QA_EMAIL, VISUAL_QA_PASSWORD, record fixture set, and DB availability"
      pattern: "visual-qa@example.test"
    - from: ".planning/phases/48-visual-qa-accessibility/evidence/desktop-checklist.md"
      to: ".planning/phases/48-visual-qa-accessibility/evidence/desktop-*.png"
      via: "one checklist row per screenshot"
      pattern: "desktop-(landing|map|footprint-dialog|journal|memories)\\.png"
---

<objective>
Establish the Phase 48 evidence harness before any visual, accessibility, or regression claims are accepted.

Purpose: satisfy D-01 through D-09 by making the populated desktop QA path reproducible, screenshot-backed, and checklist-backed.
Output: seed script/procedure, evidence directory, five screenshots, and a repair queue for downstream plans.
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-visual-qa-accessibility/48-CONTEXT.md
@.planning/phases/48-visual-qa-accessibility/48-RESEARCH.md
@.planning/phases/48-visual-qa-accessibility/48-VALIDATION.md
@.planning/phases/48-visual-qa-accessibility/48-PATTERNS.md
@apps/server/prisma/schema.prisma
@apps/server/package.json
@apps/web/package.json
@apps/web/src/router/index.ts
@apps/web/src/views/LandingPageView.vue
@apps/web/src/views/MapHomeView.vue
@apps/web/src/views/TimelinePageView.vue
@apps/web/src/views/StatisticsPageView.vue
@apps/web/src/components/LeafletMapStage.vue
@apps/web/src/components/map-popup/FootprintDateDialog.vue

<interfaces>
Seed target constants:
- VISUAL_QA_EMAIL: visual-qa@example.test
- VISUAL_QA_PASSWORD: VisualQa2026!
- VISUAL_QA_USERNAME: 视觉 QA 长用户名用于验证侧栏文本不会溢出
- Required routes: `/`, `/map`, `/journal`, `/memories`
- Required screenshots: `desktop-landing.png`, `desktop-map.png`, `desktop-footprint-dialog.png`, `desktop-journal.png`, `desktop-memories.png`
- Required checklist: `.planning/phases/48-visual-qa-accessibility/evidence/desktop-checklist.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repeatable QA seed procedure</name>
  <files>apps/server/scripts/seed-visual-qa.mjs, .planning/phases/48-visual-qa-accessibility/evidence/seed-data.md</files>
  <read_first>
    @.planning/phases/48-visual-qa-accessibility/48-CONTEXT.md
    @.planning/phases/48-visual-qa-accessibility/48-VALIDATION.md
    @.planning/phases/48-visual-qa-accessibility/48-PATTERNS.md
    @apps/server/prisma/schema.prisma
    @apps/server/package.json
    @packages/contracts/src/fixtures.ts
    @apps/server/src/modules/records/records.repository.ts
    @apps/server/src/modules/records/records.service.ts
  </read_first>
  <action>Create `apps/server/scripts/seed-visual-qa.mjs` to seed exactly one user `visual-qa@example.test` with password `VisualQa2026!` and username `视觉 QA 长用户名用于验证侧栏文本不会溢出`. Use Prisma `User` and `UserTravelRecord` models, `argon2.hash()`, and an idempotent flow: upsert the user, delete that user's existing `UserTravelRecord` rows, then create records for `cn-beijing`, `us-california`, `jp-tokyo`, and `de-saxony` with varied `startDate`, `notes`, and `tags`. Include `--dry-run` mode that validates the fixture array and prints the target email without touching the DB. Create `evidence/seed-data.md` documenting the exact command `pnpm --filter @trip-map/server exec node scripts/seed-visual-qa.mjs`, the dry-run command, account credentials, and the rule from D-09 that screenshots must use this seeded account or an equivalent fixed account recorded in the same file. Per D-02, write only desktop scope language.</action>
  <verify>
    <automated>pnpm --filter @trip-map/server exec node scripts/seed-visual-qa.mjs --dry-run</automated>
    <automated>test -f .planning/phases/48-visual-qa-accessibility/evidence/seed-data.md</automated>
  </verify>
  <done>`--dry-run` exits 0, prints `visual-qa@example.test`, validates at least four dated records, and `seed-data.md` records credentials plus DB availability notes.</done>
  <acceptance_criteria>
    - Source assertion: `apps/server/scripts/seed-visual-qa.mjs` contains `visual-qa@example.test`, `VisualQa2026!`, `cn-beijing`, `us-california`, `jp-tokyo`, and `de-saxony`.
    - CLI assertion: `pnpm --filter @trip-map/server exec node scripts/seed-visual-qa.mjs --dry-run` exits 0 without requiring `DATABASE_URL`.
    - File assertion: `.planning/phases/48-visual-qa-accessibility/evidence/seed-data.md` contains the dry-run command, real seed command, account email, and whether DB seeding ran or was unavailable.
    - Scope assertion: `seed-data.md` describes only the desktop QA account and does not add any other device class or screenshot matrix.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Capture desktop core-state screenshots and checklist</name>
  <files>.planning/phases/48-visual-qa-accessibility/evidence/desktop-checklist.md, .planning/phases/48-visual-qa-accessibility/evidence/desktop-landing.png, .planning/phases/48-visual-qa-accessibility/evidence/desktop-map.png, .planning/phases/48-visual-qa-accessibility/evidence/desktop-footprint-dialog.png, .planning/phases/48-visual-qa-accessibility/evidence/desktop-journal.png, .planning/phases/48-visual-qa-accessibility/evidence/desktop-memories.png</files>
  <read_first>
    @.planning/phases/48-visual-qa-accessibility/48-CONTEXT.md
    @.planning/phases/48-visual-qa-accessibility/48-VALIDATION.md
    @.planning/phases/48-visual-qa-accessibility/48-PATTERNS.md
    @.planning/phases/48-visual-qa-accessibility/evidence/seed-data.md
    @apps/web/package.json
    @apps/server/package.json
    @apps/web/src/router/index.ts
    @apps/web/src/views/LandingPageView.vue
    @apps/web/src/views/MapHomeView.vue
    @apps/web/src/views/TimelinePageView.vue
    @apps/web/src/views/StatisticsPageView.vue
    @apps/web/src/components/LeafletMapStage.vue
    @apps/web/src/components/map-popup/FootprintDateDialog.vue
  </read_first>
  <action>Create `.planning/phases/48-visual-qa-accessibility/evidence/desktop-checklist.md` with rows for `desktop-landing.png`, `desktop-map.png`, `desktop-footprint-dialog.png`, `desktop-journal.png`, and `desktop-memories.png`. For each row record route/state, screenshot path, core visuals present, overlap, truncation, unreadable text, long-text risk, keyboard/focus observation, reduced-motion observation, and repair owner. Start the local app with the documented seed account, capture the five desktop screenshots, and record any blocker as `repair-needed` with a concrete owner file such as `ShellSidebar.vue`, `LeafletMapStage.vue`, `FootprintDateDialog.vue`, `TimelinePageView.vue`, or `StatisticsPageView.vue`. Per D-01/D-03, capture only the populated main path plus opened `留下足迹` date dialog. Per D-06/D-08, mark map as passing only when the Leaflet surface and star markers are visible, and mark memories as passing only when monthly trend, country/region distribution, yearly trend, and memories-profile radar panels all show visible chart graphics.</action>
  <verify>
    <automated>test -f .planning/phases/48-visual-qa-accessibility/evidence/desktop-checklist.md && test -f .planning/phases/48-visual-qa-accessibility/evidence/desktop-landing.png && test -f .planning/phases/48-visual-qa-accessibility/evidence/desktop-map.png && test -f .planning/phases/48-visual-qa-accessibility/evidence/desktop-footprint-dialog.png && test -f .planning/phases/48-visual-qa-accessibility/evidence/desktop-journal.png && test -f .planning/phases/48-visual-qa-accessibility/evidence/desktop-memories.png</automated>
    <automated>grep -E "desktop-(landing|map|footprint-dialog|journal|memories)\\.png" .planning/phases/48-visual-qa-accessibility/evidence/desktop-checklist.md</automated>
  </verify>
  <done>All five screenshot files exist, checklist contains one completed row per file, and every observed blocker has an owner file for downstream plans.</done>
  <acceptance_criteria>
    - File assertion: all five screenshot paths from `48-VALIDATION.md` exist under `.planning/phases/48-visual-qa-accessibility/evidence/`.
    - Checklist assertion: `desktop-checklist.md` contains `landing`, `/map`, `footprint dialog`, `/journal`, and `/memories` rows.
    - Behavior assertion: checklist explicitly states whether Leaflet surface, saved star markers, and all four ECharts chart panels are visible.
    - Repair queue assertion: every `repair-needed` row names at least one concrete source file.
    - Scope assertion: `desktop-checklist.md` contains only the five required desktop screenshot rows.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QA operator -> local auth flow | Screenshot evidence must authenticate through normal login using the fixed QA account, not by bypassing route guards. |
| Seed script -> local database | Seed data writes must be scoped to `visual-qa@example.test` and must not alter other users. |
| Evidence checklist -> release decision | Missing screenshots or ambiguous checklist notes can falsely certify visual quality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Spoofing | screenshot capture/auth setup | mitigate | Use `visual-qa@example.test` through the normal auth modal; do not set auth store state directly for evidence. |
| T-48-02 | Tampering | `apps/server/scripts/seed-visual-qa.mjs` | mitigate | Script must delete/create records only where `user.email === visual-qa@example.test`; dry-run must not touch DB. |
| T-48-03 | Repudiation | `desktop-checklist.md` | mitigate | Each checklist row must include screenshot path, pass/fail state, and owner file for any blocker. |
| T-48-04 | Information Disclosure | evidence screenshots | accept | Seed account contains artificial QA records only; no real user account or production data should be used. |
| T-48-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs are allowed in this plan. |
</threat_model>

<verification>
Plan-level gate:
- `pnpm --filter @trip-map/server exec node scripts/seed-visual-qa.mjs --dry-run`
- `test -f` checks for all evidence files listed above
- `grep -E "desktop-(landing|map|footprint-dialog|journal|memories)\\.png" .planning/phases/48-visual-qa-accessibility/evidence/desktop-checklist.md`
</verification>

<success_criteria>
Phase 48 can proceed only after the seed procedure and desktop evidence matrix exist, and after the checklist identifies pass/fail state plus owner files for every observed blocker.
</success_criteria>

<output>
Create `.planning/phases/48-visual-qa-accessibility/48-01-SUMMARY.md` when done.
</output>

---
phase: 17-正式验证闭环与人-UAT-复验
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-leaflet/14-HUMAN-UAT.md
  - .planning/phases/14-leaflet/VERIFICATION.md
autonomous: false
gap_closure: true
requirements:
  - GEOX-05
  - MAP-04
  - MAP-05
  - MAP-06
  - MAP-08
  - UIX-01
user_setup:
  - `pnpm dev` 或等价的 web + server 本地运行环境
  - 可在浏览器访问的真实地图页面
  - 有效的 `VITE_BING_MAPS_KEY`
  - `apps/web/public/geo/*` 下可访问的几何资产文件

must_haves:
  truths:
    - "Phase 14 的 3 项人工 Nyquist 验收必须有独立原始记录，不能继续只停留在 `VERIFICATION.md` 的待办描述里。"
    - "popup 拖拽锚点、Bing 瓦片、已点亮边界启动预加载三项都必须有 `pass / issue / blocked` 的明确结论与证据。"
    - "只有三项都 `pass` 时，Phase 14 才能宣称人工 Nyquist 完成；否则 `VERIFICATION.md` 必须保留阻塞说明。"
  artifacts:
    - path: ".planning/phases/14-leaflet/14-HUMAN-UAT.md"
      provides: "Phase 14 真实浏览器人工验收记录"
      contains: "Popup 跟随地图拖动时锚点实时更新"
    - path: ".planning/phases/14-leaflet/VERIFICATION.md"
      provides: "回填人验结果后的 Phase 14 正式验证报告"
      contains: "## Human Re-verification"
  key_links:
    - from: ".planning/phases/14-leaflet/14-HUMAN-UAT.md"
      to: ".planning/phases/14-leaflet/VERIFICATION.md"
      via: "三项 Nyquist 结果同步回填到正式验证报告"
      pattern: "Bing Maps CanvasLight 瓦片加载"
    - from: ".planning/phases/14-leaflet/VERIFICATION.md"
      to: "apps/web/src/components/LeafletMapStage.vue"
      via: "真实 Leaflet 运行时交互的人工闭环"
      pattern: "LeafletMapStage"
---

<objective>
完成 Phase 14 的 3 项人工 Nyquist 验收，并把结果正式写回 `VERIFICATION.md`。

Purpose: Phase 14 的自动化已经通过，但 popup 锚点拖动、Bing 瓦片、启动预加载这些行为天然依赖真实浏览器和真实地图环境。如果不把这三项单独验完并落文档，Phase 14 就一直停留在“自动化通过、人工待补”的状态。
Output: 一份新的 `14-HUMAN-UAT.md` 原始记录，以及回填人验结果后的 `VERIFICATION.md`。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@package.json
@apps/web/package.json
@.planning/ROADMAP.md
@.planning/phases/14-leaflet/14-03-PLAN.md
@.planning/phases/14-leaflet/14-03-SUMMARY.md
@.planning/phases/14-leaflet/14-UAT.md
@.planning/phases/14-leaflet/14-VALIDATION.md
@.planning/phases/14-leaflet/VERIFICATION.md
@apps/web/src/components/LeafletMapStage.vue
@apps/web/public/geo/2026-03-31-geo-v1/manifest.json
@apps/web/public/geo/2026-04-02-geo-v2/manifest.json

<interfaces>
Existing open items in `.planning/phases/14-leaflet/VERIFICATION.md`:
```md
## Human Verification Required
1. Popup 跟随地图拖动
2. Bing Maps CanvasLight 瓦片（需要 API Key）
3. 已点亮边界启动时预加载（需要几何资产）
```

Available local commands:
```json
{
  "dev": "turbo run dev --parallel",
  "dev:web": "turbo run dev --filter=@trip-map/web",
  "dev:server": "turbo run dev --filter=@trip-map/server"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 `14-HUMAN-UAT.md`，把 3 项 Nyquist 测试改成可执行清单</name>
  <files>
    .planning/phases/14-leaflet/14-HUMAN-UAT.md
  </files>
  <read_first>
    .planning/phases/14-leaflet/VERIFICATION.md
    .planning/phases/14-leaflet/14-UAT.md
    .planning/phases/14-leaflet/14-03-PLAN.md
    package.json
    apps/web/package.json
  </read_first>
  <behavior>
    - `14-HUMAN-UAT.md` 必须包含环境前置、三项测试、逐项结果、证据、阻塞原因与统计汇总。
    - 三项测试名称必须与 `VERIFICATION.md` 中的待办项逐字一致，避免后续文档难以比对。
  </behavior>
  <action>
    1. 新建 `.planning/phases/14-leaflet/14-HUMAN-UAT.md`，frontmatter 至少包含：
       - `status: pending`
       - `phase: 14-leaflet`
       - `source:` 包含 `VERIFICATION.md`、`14-UAT.md`、`14-03-SUMMARY.md`
       - `started:` 当前时间
       - `updated:` 当前时间
    2. 在正文中加入 `## Environment` 小节，写出并等待填写这些字段：
       - `app_url:`
       - `browser:`
       - `bing_key: present|missing`
       - `geometry_assets: present|missing`
       - `seed_record_ready: yes|no`
    3. 在 `## Tests` 下创建这 3 个精确标题，每项都包含 `expected:`、`steps:`、`result: pending`、`evidence:`、`notes:`：
       - `Popup 跟随地图拖动时锚点实时更新`
       - `Bing Maps CanvasLight 瓦片加载（需要 API Key）`
       - `已点亮边界启动时预加载`
    4. 在 `## Summary` 中预置：
       - `total: 3`
       - `passed: 0`
       - `issues: 0`
       - `pending: 3`
       - `blocked: 0`
    5. 在 `## Gaps` 小节中预置一行 `None yet.`，供后续人工测试填写具体问题。
  </action>
  <verify>
    <automated>rg -n "## Environment|Popup 跟随地图拖动时锚点实时更新|Bing Maps CanvasLight 瓦片加载（需要 API Key）|已点亮边界启动时预加载|total: 3|pending: 3" .planning/phases/14-leaflet/14-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/14-leaflet/14-HUMAN-UAT.md` exists
    - `14-HUMAN-UAT.md` contains `## Environment`
    - `14-HUMAN-UAT.md` contains `bing_key: present|missing`
    - `14-HUMAN-UAT.md` contains `geometry_assets: present|missing`
    - `14-HUMAN-UAT.md` contains `Popup 跟随地图拖动时锚点实时更新`
    - `14-HUMAN-UAT.md` contains `Bing Maps CanvasLight 瓦片加载（需要 API Key）`
    - `14-HUMAN-UAT.md` contains `已点亮边界启动时预加载`
    - `14-HUMAN-UAT.md` contains `pending: 3`
  </acceptance_criteria>
  <done>Phase 14 的人工 Nyquist 已从口头待办转成可执行、可记录、可回填的清单文件。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 在真实浏览器完成 3 项 Phase 14 Nyquist 验收</name>
  <files>
    .planning/phases/14-leaflet/14-HUMAN-UAT.md
  </files>
  <read_first>
    .planning/phases/14-leaflet/14-HUMAN-UAT.md
    .planning/phases/14-leaflet/VERIFICATION.md
    package.json
    apps/web/package.json
  </read_first>
  <action>
    1. 启动应用：优先运行 `pnpm dev`，确保 `web` 与 `server` 都在本地可访问；如果已有独立进程，也可以分别运行 `pnpm dev:web` 与 `pnpm dev:server`。
    2. 在 `.planning/phases/14-leaflet/14-HUMAN-UAT.md` 的 `## Environment` 里填写实际 `app_url`、浏览器、`bing_key` 是否存在、几何资产是否可访问。
    3. 完成 `Popup 跟随地图拖动时锚点实时更新`：
       - 打开地图，点击一个已识别地点让 popup 出现
       - 连续拖动、缩放地图 10 秒以上
       - 验证 popup 始终跟随点击点，不漂移、不滞后
       - 在该测试项中填写 `result: pass|issue|blocked`，并把观察写进 `evidence:` / `notes:`
    4. 完成 `Bing Maps CanvasLight 瓦片加载（需要 API Key）`：
       - 仅在 `VITE_BING_MAPS_KEY` 可用时执行
       - 验证底图切到 Bing CanvasLight，能看到中英双语地名，且无 broken tiles
       - 若缺 key 或请求失败，写 `result: blocked`，并在 `notes:` 中写明是 key 缺失还是网络/配额问题
    5. 完成 `已点亮边界启动时预加载`：
       - 若当前没有 saved place，先用现有应用链路创建至少一个已点亮地点
       - 刷新页面，等待地图 ready
       - 验证 saved boundary 在未手动再次点击前就已显示
       - 在 `14-HUMAN-UAT.md` 中记录 `result`、`evidence`、`notes`
    6. 每完成一项，立即把 `14-HUMAN-UAT.md` 对应小节从 `pending` 改成真实结果，不要全部做完后再回忆补写。
  </action>
  <verify>
    1. `14-HUMAN-UAT.md` 中三项测试都不再是 `result: pending`
    2. 每项测试都有非空的 `evidence:` 说明
    3. 若存在 `blocked`，`notes:` 中必须写清缺失前置条件
  </verify>
  <acceptance_criteria>
    - `.planning/phases/14-leaflet/14-HUMAN-UAT.md` contains exactly 3 non-pending `result:` lines for the 3 Nyquist tests
    - `14-HUMAN-UAT.md` contains `evidence:` under all 3 tests
    - If `14-HUMAN-UAT.md` contains `result: blocked`, it must also contain `notes:` explaining the blocker
  </acceptance_criteria>
  <done>Phase 14 的 3 项人工 Nyquist 已在真实浏览器环境中跑完，并把原始结果逐项记录到 `14-HUMAN-UAT.md`。</done>
  <resume-signal>Type `phase14-human-done` after the file is updated, or describe the exact blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 把 Phase 14 的人验结论回填到 `VERIFICATION.md`</name>
  <files>
    .planning/phases/14-leaflet/VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/14-leaflet/14-HUMAN-UAT.md
    .planning/phases/14-leaflet/VERIFICATION.md
  </read_first>
  <behavior>
    - 如果三项都是 `pass`，`VERIFICATION.md` 必须显式写出人工复验完成，不再保留“仍需人工验证”的未闭环措辞。
    - 如果存在 `issue` 或 `blocked`，`VERIFICATION.md` 必须保留相应 blocker，不能假装 Phase 14 已完全闭环。
  </behavior>
  <action>
    1. 读取 `.planning/phases/14-leaflet/14-HUMAN-UAT.md` 的三项结果，并在 `.planning/phases/14-leaflet/VERIFICATION.md` frontmatter 增加 `human_reverification: passed|gaps_found|blocked`：
       - 3 项都 `pass` -> `passed`
       - 只要有 `issue` -> `gaps_found`
       - 没有 `issue` 但有 `blocked` -> `blocked`
    2. 用新的 `## Human Re-verification` 小节替换原来的 `## Human Verification Required` 小节，逐项同步写入 `result`、`evidence`、`notes`。
    3. 若三项都 `pass`，在 `Gaps Summary` 中明确写 `No remaining human Nyquist gaps.`；若存在 `issue` 或 `blocked`，则在 `Gaps Summary` 中列出剩余 blocker，并指向 `14-HUMAN-UAT.md` 的具体测试项。
    4. 不要重写已有自动化 6/6 verification 内容；只更新人工闭环相关段落与 frontmatter。
  </action>
  <verify>
    <automated>rg -n "human_reverification:|## Human Re-verification|Popup 跟随地图拖动时锚点实时更新|Bing Maps CanvasLight 瓦片加载|已点亮边界启动时预加载|No remaining human Nyquist gaps" .planning/phases/14-leaflet/VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/14-leaflet/VERIFICATION.md` contains `human_reverification:`
    - `VERIFICATION.md` contains `## Human Re-verification`
    - `VERIFICATION.md` does not contain `## Human Verification Required`
    - `VERIFICATION.md` contains all 3 human test titles verbatim
    - If `.planning/phases/14-leaflet/14-HUMAN-UAT.md` contains 3 passes, `VERIFICATION.md` contains `No remaining human Nyquist gaps`
  </acceptance_criteria>
  <done>Phase 14 的正式验证报告已与真实人验结果对齐，不再只是停留在待办状态。</done>
</task>

</tasks>

<verification>
- `rg -n "result: |passed: |issues: |blocked:" .planning/phases/14-leaflet/14-HUMAN-UAT.md`
- `rg -n "human_reverification:|## Human Re-verification" .planning/phases/14-leaflet/VERIFICATION.md`
- Confirm all three Nyquist test titles appear in both files verbatim
</verification>

<success_criteria>
- Phase 14 的 3 项人工 Nyquist 有独立原始记录与正式报告回填
- 所有阻塞都被显式写出，或全部通过后明确宣告无剩余人验 gap
- 后续不需要再靠口头解释“Phase 14 其实只差 Bing/拖拽/预加载”
</success_criteria>

<output>
After completion, create `.planning/phases/14-leaflet/14-HUMAN-UAT-SUMMARY.md`
</output>

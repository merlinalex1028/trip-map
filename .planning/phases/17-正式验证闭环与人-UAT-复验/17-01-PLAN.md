---
phase: 17-正式验证闭环与人-UAT-复验
plan: 01
type: execute
wave: 3
depends_on:
  - 17-02
files_modified:
  - .planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/v3.0-MILESTONE-AUDIT.md
autonomous: true
gap_closure: true
requirements:
  - API-01
  - API-02
  - MAP-07
  - UIX-02
  - UIX-03
  - UIX-05

must_haves:
  truths:
    - "Phase 15 的正式验证报告必须明确串起 15-01/15-02/15-03 的实现、15-UAT 的缺口诊断、Phase 16 的代码修复，以及 Phase 17 的人验结果。"
    - "只有当 `16-HUMAN-UAT.md` 为 3/3 pass 且 `16-VERIFICATION.md` 已更新为正式闭环状态时，`API-01` / `API-02` 才能从 partial 改成 satisfied。"
    - "如果 Phase 16 人验仍有 issue / blocked，`15-VERIFICATION.md` 必须如实记录 blocker，且不得提前修改 `REQUIREMENTS.md` 与 `v3.0-MILESTONE-AUDIT.md` 的结论。"
  artifacts:
    - path: ".planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md"
      provides: "Phase 15 的正式闭环报告"
      contains: "Phase 16 gap closure evidence"
    - path: ".planning/REQUIREMENTS.md"
      provides: "API-01 / API-02 的最终 requirement 状态"
      contains: "API-01"
    - path: ".planning/v3.0-MILESTONE-AUDIT.md"
      provides: "v3.0 audit 对 API-01 / API-02 的最终判定"
      contains: "API-01"
  key_links:
    - from: ".planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md"
      to: ".planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VERIFICATION.md"
      via: "Phase 16 修复与人验结果构成 Phase 15 最终闭环证据"
      pattern: "Human Re-verification"
    - from: ".planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md"
      to: ".planning/phases/15-服务端记录与点亮闭环/15-UAT.md"
      via: "Phase 15 原始 gap 诊断必须在正式报告中被回指并关闭"
      pattern: "点亮按钮点击无效果"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/v3.0-MILESTONE-AUDIT.md"
      via: "API-01 / API-02 的 requirement 状态与 audit 结论必须一致"
      pattern: "API-02"
---

<objective>
为 Phase 15 补齐正式 `15-VERIFICATION.md`，并在证据完整时把 `API-01` / `API-02` 从 milestone audit 的 partial 改成 satisfied。

Purpose: 当前 v3.0 的关键缺口不是代码未实现，而是 Phase 15 缺少正式闭环文档，导致 requirement 与 milestone audit 仍停留在 partial。这个 plan 要把 Phase 15 的代码实现、Phase 16 的 gap closure 和 Phase 17 的人验串成一条正式证据链。
Output: 一份可以独立阅读的 `15-VERIFICATION.md`，以及与其一致的 `REQUIREMENTS.md` / `v3.0-MILESTONE-AUDIT.md` 更新。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v3.0-MILESTONE-AUDIT.md
@.planning/phases/15-服务端记录与点亮闭环/15-01-SUMMARY.md
@.planning/phases/15-服务端记录与点亮闭环/15-02-SUMMARY.md
@.planning/phases/15-服务端记录与点亮闭环/15-03-SUMMARY.md
@.planning/phases/15-服务端记录与点亮闭环/15-UAT.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VERIFICATION.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md
@.planning/phases/17-正式验证闭环与人-UAT-复验/17-02-PLAN.md

<interfaces>
Phase 15 requirement closure targets in `.planning/REQUIREMENTS.md`:
```md
- [ ] **API-01**: 用户的旅行记录读取、创建、更新、删除，以及点亮 / 取消点亮动作，都通过 `server` API 持久化
- [ ] **API-02**: 点亮 / 取消点亮动作以 canonical `placeId` 为目标，而不是依赖前端临时点位或展示名
```

Expected upstream closure signal from Phase 16 docs:
```md
status: passed
## Human Re-verification
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 生成 Phase 15 正式验证报告，并据实判断是否具备 closure 条件</name>
  <files>
    .planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/15-服务端记录与点亮闭环/15-01-SUMMARY.md
    .planning/phases/15-服务端记录与点亮闭环/15-02-SUMMARY.md
    .planning/phases/15-服务端记录与点亮闭环/15-03-SUMMARY.md
    .planning/phases/15-服务端记录与点亮闭环/15-UAT.md
    .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VERIFICATION.md
    .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md
    .planning/REQUIREMENTS.md
    .planning/v3.0-MILESTONE-AUDIT.md
  </read_first>
  <behavior>
    - `15-VERIFICATION.md` 不能只复述 Phase 15 原始计划，必须显式说明 Phase 15 的两个 major gaps 是如何由 Phase 16 修复并由 Phase 17 人验关闭的。
    - 如果 `16-HUMAN-UAT.md` 中任一测试不是 `result: pass`，`15-VERIFICATION.md` 只能写成 `status: gaps_found` 或 `status: human_needed`，不能宣称 API-01 / API-02 已 satisfied。
    - 如果 `16-HUMAN-UAT.md` 是 3/3 pass 且 `16-VERIFICATION.md` 已更新为正式闭环，`15-VERIFICATION.md` 必须把 API-01 / API-02 标记为 satisfied，并明确引用对应证据文件与关键测试。
  </behavior>
  <action>
    1. 新建 `.planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md`，沿用仓库内其他 `*-VERIFICATION.md` 的结构，至少包含 frontmatter、Goal Achievement、Required Artifacts、Key Link Verification、Behavioral Spot-Checks、Requirements Coverage、Gaps Summary 这些章节。
    2. 在 frontmatter 中写入：
       - `phase: 15-服务端记录与点亮闭环`
       - `verified:` 当前时间戳
       - `status:` 根据上游证据二选一：若 `16-HUMAN-UAT.md` 为 3/3 pass 且 `16-VERIFICATION.md` 已有 `## Human Re-verification`，写 `passed`；否则写 `gaps_found`
       - `closure_source:` 必须列出 `15-UAT.md`、`16-VERIFICATION.md`、`16-HUMAN-UAT.md`
    3. 在 `Goal Achievement` 里至少写出 6 条 observable truths，并把以下具体事实写进正文，而不是只笼统说“已修复”：
       - `15-UAT.md` 中 Gap 1 是 fallback 点位点亮按钮 silent guard
       - `15-UAT.md` 中 Gap 2 是 `SmokeRecord` 缺少 `regionSystem/adminType/typeLabel/parentLabel/subtitle`
       - `16-02` 修掉了 fallback 假可点击与 overlay load
       - `16-01` 修掉了 canonical metadata round-trip
       - `17-04` 的人验结果是最终 closure 依据
    4. 在 `Requirements Coverage` 章节中，至少覆盖 `API-01`、`API-02`、`MAP-07`、`UIX-02`、`UIX-03`、`UIX-05`。当且仅当上游 human UAT 为 3/3 pass 时，把 `API-01` 和 `API-02` 标成 `✓ SATISFIED`；否则明确写成 `? NEEDS HUMAN` 或 `✗ BLOCKED`，并指出是哪个 human test 未过。
    5. 在 `Behavioral Spot-Checks` 中引用已有自动化证据，至少点名这些命令或等价证据来源：
       - `pnpm --filter @trip-map/server exec vitest run test/records-contract.e2e-spec.ts test/records-travel.e2e-spec.ts test/records-smoke.e2e-spec.ts`
       - `pnpm --filter @trip-map/web exec vitest run src/stores/map-points.spec.ts src/components/map-popup/PointSummaryCard.spec.ts src/components/LeafletMapStage.spec.ts`
       - `pnpm test`
       - `pnpm typecheck`
       如果本次 docs wave 没有重跑命令，就明确写“证据来自上游报告”，不要伪造新执行结果。
    6. 在 `Gaps Summary` 末尾加入精确小节标题 `## Phase 16 gap closure evidence`，总结 `16-01`、`16-02`、`17-04` 三类证据如何共同关闭 Phase 15 的 formal gap。
  </action>
  <verify>
    <automated>rg -n "closure_source:|## Goal Achievement|## Required Artifacts|## Key Link Verification|## Behavioral Spot-Checks|## Requirements Coverage|## Phase 16 gap closure evidence|API-01|API-02" .planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md` exists
    - `15-VERIFICATION.md` contains `closure_source:`
    - `15-VERIFICATION.md` contains `## Phase 16 gap closure evidence`
    - `15-VERIFICATION.md` contains `API-01`
    - `15-VERIFICATION.md` contains `API-02`
    - `15-VERIFICATION.md` contains `点亮按钮点击无效果`
    - `15-VERIFICATION.md` contains `regionSystem`
    - `15-VERIFICATION.md` contains `16-HUMAN-UAT.md`
  </acceptance_criteria>
  <done>Phase 15 拥有正式验证报告，并且文档层已经明确回答“API-01 / API-02 现在是否能正式闭环”。</done>
</task>

<task type="auto">
  <name>Task 2: 仅在 closure 条件满足时，同步更新 requirements 与 milestone audit</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/v3.0-MILESTONE-AUDIT.md
  </files>
  <read_first>
    .planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md
    .planning/REQUIREMENTS.md
    .planning/v3.0-MILESTONE-AUDIT.md
  </read_first>
  <behavior>
    - 如果 `15-VERIFICATION.md` frontmatter `status: passed`，`REQUIREMENTS.md` 与 `v3.0-MILESTONE-AUDIT.md` 必须同步翻转 API-01 / API-02 的最终结论。
    - 如果 `15-VERIFICATION.md` 不是 `passed`，这两个文件必须保持现状，只能在 `15-VERIFICATION.md` 里留下 blocker，不得制造假 closure。
  </behavior>
  <action>
    1. 先读取 `.planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md` frontmatter。
    2. 只有当 `status: passed` 时，才执行以下更新：
       - 在 `.planning/REQUIREMENTS.md` 中把 `API-01` / `API-02` 的 checkbox 从 `[ ]` 改成 `[x]`
       - 在 `.planning/REQUIREMENTS.md` 的 Traceability 表里，把 `API-01 | Phase 17 | Pending` 与 `API-02 | Phase 17 | Pending` 改成 `Complete`
       - 在 `.planning/v3.0-MILESTONE-AUDIT.md` 中，把 `API-01` / `API-02` 从 `status: partial` 改成 `status: satisfied`
       - 更新 audit 的 Executive Summary / Recommendation / Partial-with-gaps 表述，移除“API-01、API-02 仍 partial”的描述
    3. 如果 `15-VERIFICATION.md` 不是 `passed`，不要修改 `REQUIREMENTS.md` 或 `v3.0-MILESTONE-AUDIT.md`；只在执行 summary 中说明 closure 未满足、原因来自哪个 human test。
    4. 修改 audit 时不要顺手处理 `ARC-03` 或 Phase 14/15 VALIDATION tech debt；本 plan 只收口 `API-01` / `API-02`。
  </action>
  <verify>
    <automated>rg -n "\\[x\\] \\*\\*API-01|\\[x\\] \\*\\*API-02|API-01 \\| Phase 17 \\| Complete|API-02 \\| Phase 17 \\| Complete|status: satisfied|API-01|API-02" .planning/REQUIREMENTS.md .planning/v3.0-MILESTONE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - If `.planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md` contains `status: passed`, `.planning/REQUIREMENTS.md` contains `[x] **API-01**`
    - If `.planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md` contains `status: passed`, `.planning/REQUIREMENTS.md` contains `[x] **API-02**`
    - If `.planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md` contains `status: passed`, `.planning/REQUIREMENTS.md` contains `API-01 | Phase 17 | Complete`
    - If `.planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md` contains `status: passed`, `.planning/REQUIREMENTS.md` contains `API-02 | Phase 17 | Complete`
    - If `.planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md` contains `status: passed`, `.planning/v3.0-MILESTONE-AUDIT.md` contains `status: satisfied`
    - If `.planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md` does not contain `status: passed`, `.planning/REQUIREMENTS.md` must not change `API-01` / `API-02` away from pending
  </acceptance_criteria>
  <done>`REQUIREMENTS.md` 与 `v3.0-MILESTONE-AUDIT.md` 已与 Phase 15 的正式闭环结论保持一致，不再出现“报告已过但 requirement 仍 partial”的状态漂移。</done>
</task>

</tasks>

<verification>
- `rg -n "status:|API-01|API-02|Human Re-verification|Phase 16 gap closure evidence" .planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION.md .planning/REQUIREMENTS.md .planning/v3.0-MILESTONE-AUDIT.md`
- Confirm `15-VERIFICATION.md` only claims closure when `16-HUMAN-UAT.md` is 3/3 pass
</verification>

<success_criteria>
- `15-VERIFICATION.md` 能独立解释 Phase 15 为何现在正式闭环，而不是只靠口头说明“Phase 16 修过了”
- `API-01` / `API-02` 的 requirement 状态与 milestone audit 结论一致
- 如果 human UAT 仍未完成，文档会如实保留 blocker，而不是提前宣告完成
</success_criteria>

<output>
After completion, create `.planning/phases/15-服务端记录与点亮闭环/15-VERIFICATION-SUMMARY.md`
</output>

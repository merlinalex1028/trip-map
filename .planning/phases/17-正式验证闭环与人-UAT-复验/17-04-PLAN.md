---
phase: 17-正式验证闭环与人-UAT-复验
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md
autonomous: false
gap_closure: true
requirements:
  - REQ-16-01
  - REQ-16-02
  - REQ-16-05
user_setup:
  - `pnpm dev` 或等价的 web + server 本地运行环境
  - 可在浏览器访问的真实地图页面
  - 至少一个可识别且可点亮的 canonical 地点
  - 可复现 fallback 点位与 California 区域点击的测试路径

must_haves:
  truths:
    - "Phase 16 的 3 项 human-verify 必须从 `pending` 变成有结论的 `pass / issue / blocked`，不能继续停留在占位状态。"
    - "fallback 反馈、saved overlay 可见性、California 标签一致性三项都必须附带真实浏览器观察证据，而不是只复制自动化结论。"
    - "`16-HUMAN-UAT.md` 的 summary 统计必须由实际结果推导出来，不能手写一个与测试明细不一致的总数。"
  artifacts:
    - path: ".planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md"
      provides: "Phase 16 三项人工复验的原始记录与最终统计"
      contains: "California 真实区域点击体验"
  key_links:
    - from: ".planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md"
      to: ".planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VERIFICATION.md"
      via: "17-02 会直接消费这份 human source 更新正式验证结论"
      pattern: "result:"
---

<objective>
完成 Phase 16 的 3 项 human-verify UAT 复验，并把结果写入 `16-HUMAN-UAT.md`。

Purpose: `16-VERIFICATION.md` 已经证明代码与自动化链路闭合，但 Phase 16 是否真正完成闭环，最终取决于真实地图体验。这个 plan 不负责写正式验证结论，它只负责把三项人工复验跑完并把原始结果记录准确。
Output: 一份完成统计、逐项写明 evidence 的 `16-HUMAN-UAT.md`。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@package.json
@apps/web/package.json
@apps/server/package.json
@.planning/ROADMAP.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VERIFICATION.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VALIDATION.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-02-SUMMARY.md
@.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-03-SUMMARY.md
@apps/web/src/components/LeafletMapStage.vue
@apps/web/src/components/map-popup/PointSummaryCard.vue

<interfaces>
Current pending shape in `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md`:
```md
result: pending
passed: 0
pending: 3
```

Relevant local commands:
```json
{
  "dev": "turbo run dev --parallel",
  "dev:web": "turbo run dev --filter=@trip-map/web",
  "dev:server": "turbo run dev --filter=@trip-map/server"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把 `16-HUMAN-UAT.md` 重写成可执行、可统计的人工复验表</name>
  <files>
    .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md
  </files>
  <read_first>
    .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md
    .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VERIFICATION.md
    .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VALIDATION.md
  </read_first>
  <behavior>
    - 原始文件中的 3 条测试必须保留同名，但需要补充环境、步骤、证据与统计字段，方便后续正式验证消费。
    - 重写后文件应当还能容纳 `pass / issue / blocked` 三类结果，而不是只适配 `pending/pass`。
  </behavior>
  <action>
    1. 更新 `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md` frontmatter：
       - `status: pending`
       - `source:` 保留 `16-VERIFICATION.md`
       - `updated:` 改成当前时间
    2. 在正文新增 `## Environment` 小节，写出待填写字段：
       - `app_url:`
       - `browser:`
       - `server_env: local|staging`
       - `fallback_path_ready: yes|no`
       - `saved_record_ready: yes|no`
       - `california_click_points:`
    3. 把三项测试扩成统一模板：每项都必须包含 `expected:`、`steps:`、`result: pending`、`evidence:`、`notes:`：
       - `fallback 点位的点亮入口反馈`
       - `点亮后 saved overlay 的真实可见性`
       - `California 真实区域点击体验`
    4. 在 `## Summary` 里预置：
       - `total: 3`
       - `passed: 0`
       - `issues: 0`
       - `pending: 3`
       - `blocked: 0`
    5. 在 `## Gaps` 中预置 `None yet.`，供人工测试阶段填入具体问题。
  </action>
  <verify>
    <automated>rg -n "## Environment|fallback 点位的点亮入口反馈|点亮后 saved overlay 的真实可见性|California 真实区域点击体验|issues: 0|pending: 3|blocked: 0" .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md` contains `## Environment`
    - `16-HUMAN-UAT.md` contains `fallback_path_ready: yes|no`
    - `16-HUMAN-UAT.md` contains `saved_record_ready: yes|no`
    - `16-HUMAN-UAT.md` contains `california_click_points:`
    - `16-HUMAN-UAT.md` contains all 3 test titles verbatim
    - `16-HUMAN-UAT.md` contains `issues: 0`
    - `16-HUMAN-UAT.md` contains `blocked: 0`
  </acceptance_criteria>
  <done>Phase 16 的 human UAT 记录文件已经从简陋占位升级为可执行、可统计、可供后续 verification 消费的模板。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 在真实地图上完成 Phase 16 的 3 项人工复验</name>
  <files>
    .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md
  </files>
  <read_first>
    .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md
    .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-VERIFICATION.md
    apps/web/src/components/LeafletMapStage.vue
    apps/web/src/components/map-popup/PointSummaryCard.vue
  </read_first>
  <action>
    1. 启动应用：优先运行 `pnpm dev`，确保 web 与 server 都可访问；如果已有独立进程，也可以分别运行 `pnpm dev:web` 与 `pnpm dev:server`。
    2. 在 `16-HUMAN-UAT.md` 的 `## Environment` 里填写真实 `app_url`、浏览器、server 环境，以及 fallback / saved record / California 测试点是否就绪。
    3. 完成 `fallback 点位的点亮入口反馈`：
       - 打开一个 `OUTSIDE_SUPPORTED_DATA` 或其它 fallback surface
       - 观察点亮入口是否为禁用态，或尝试点击后是否出现 `该地点暂不支持点亮`
       - 把结果写成 `result: pass|issue|blocked`，并在 `evidence:` 里写具体表现
    4. 完成 `点亮后 saved overlay 的真实可见性`：
       - 选择一个可识别且有边界数据的 canonical 地点
       - 点亮后立即观察 saved overlay 是否出现
       - 关闭 popup、重新打开、刷新页面，再观察 overlay 是否保持正确
       - 在文件中记录 `result`、`evidence`、`notes`
    5. 完成 `California 真实区域点击体验`：
       - 至少在 California 区域点击 3 个不同位置，并把点击位置记录到 `california_click_points:`
       - 逐次确认 popup 文案保持 `一级行政区` 与 `United States · 一级行政区`
       - 将结果与观察写入该测试项
    6. 每项测试做完就立刻更新 `16-HUMAN-UAT.md`；不要等三项全做完再统一回忆填写。
  </action>
  <verify>
    1. `16-HUMAN-UAT.md` 中 3 项测试都不再是 `result: pending`
    2. 每项测试都有非空的 `evidence:`
    3. `california_click_points:` 中记录了至少 3 个点击点描述
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md` contains exactly 3 non-pending `result:` lines for the 3 human tests
    - `16-HUMAN-UAT.md` contains `evidence:` under all 3 tests
    - `16-HUMAN-UAT.md` contains `california_click_points:` with at least 3 click descriptions
    - If `16-HUMAN-UAT.md` contains `result: blocked`, it must also contain `notes:` describing the blocker
  </acceptance_criteria>
  <done>Phase 16 的三项 human-verify 已在真实地图里执行完成，并逐项留下了原始观察证据。</done>
  <resume-signal>Type `phase16-human-done` after the file is updated, or describe the exact blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 根据实际结果回算 `16-HUMAN-UAT.md` 的 summary 与 frontmatter 状态</name>
  <files>
    .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md
  </files>
  <read_first>
    .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md
  </read_first>
  <behavior>
    - summary 统计必须严格来自三项测试的 `result`，不能手工编一个总数。
    - 只有 3 项都 `pass` 时，frontmatter `status` 才能写成 `passed`；否则至少是 `partial`。
  </behavior>
  <action>
    1. 重新读取 `16-HUMAN-UAT.md` 中三项测试的 `result:`，按真实数量回写 `## Summary`：
       - `passed:` = `result: pass` 的数量
       - `issues:` = `result: issue` 的数量
       - `blocked:` = `result: blocked` 的数量
       - `pending:` = `result: pending` 的数量
    2. 更新 frontmatter `status:`：
       - 3 项都 `pass` -> `status: passed`
       - 只要还有 `issue` 或 `blocked` -> `status: partial`
    3. 更新 frontmatter `updated:` 为当前时间。
    4. 若 `issues > 0` 或 `blocked > 0`，把 `## Gaps` 从 `None yet.` 改成逐条列出未通过项及其原因；若 3 项都 pass，则把 `## Gaps` 改成 `None.`。
  </action>
  <verify>
    <automated>rg -n "status: |passed: |issues: |pending: |blocked: |## Gaps" .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - If all 3 tests in `16-HUMAN-UAT.md` are `result: pass`, the file contains `status: passed`
    - If any test in `16-HUMAN-UAT.md` is `result: issue` or `result: blocked`, the file contains `status: partial`
    - `passed:` / `issues:` / `pending:` / `blocked:` sum to `total: 3`
    - If all 3 tests pass, `16-HUMAN-UAT.md` contains `None.` under `## Gaps`
  </acceptance_criteria>
  <done>`16-HUMAN-UAT.md` 的总数、状态与逐项结果已经一致，可被 17-02 直接消费。</done>
</task>

</tasks>

<verification>
- `rg -n "result: |status: |passed: |issues: |pending: |blocked:" .planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT.md`
- Confirm all three test titles remain unchanged and each now has `evidence:`
</verification>

<success_criteria>
- Phase 16 三项 human-verify 都已经有明确结果与证据
- `16-HUMAN-UAT.md` 的统计与实际结果一致
- 17-02 可以直接基于这份原始记录更新正式验证结论
</success_criteria>

<output>
After completion, create `.planning/phases/16-uat-gap-fallback-smoke-record-schema-typelabel-california/16-HUMAN-UAT-SUMMARY.md`
</output>

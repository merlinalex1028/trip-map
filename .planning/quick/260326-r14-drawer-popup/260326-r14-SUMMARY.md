# Quick Task 260326-r14 Summary

## Task

将剩余的 drawer 详情查看/编辑链路也改成使用地图内 popup。

## Outcome

- 将 deep view/edit 的承载位置从 `App.vue` 挪回 `WorldMapStage.vue`，不再由页面壳层为 drawer 预留额外布局空间。
- 把 `PointPreviewDrawer.vue` 改造成可锚定的 popup shell，复用现有 popup 的 `floatingStyles` 和 anchor source，同时保留原有查看、编辑、保存、放弃编辑、焦点陷阱等交互。
- 为地图浮层补上 `@click.stop`，避免点击 popup 内部按钮时误触地图识别。

## Verification

- `pnpm exec vitest run src/App.spec.ts src/components/WorldMapStage.spec.ts src/components/PointPreviewDrawer.spec.ts src/components/map-popup/MapContextPopup.spec.ts src/components/map-popup/MobilePeekSheet.spec.ts`
- `pnpm build`

## Code Commit

- `f3af22d` `refactor(popup): 将深层详情从 drawer 切到地图 popup`

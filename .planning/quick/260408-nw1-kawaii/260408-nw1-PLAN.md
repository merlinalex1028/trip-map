---
phase: quick-260408-nw1-kawaii
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/App.vue
autonomous: false
requirements:
  - QUICK-KAWAII-TOPBAR-COMPACT
must_haves:
  truths:
    - "顶部栏仍保留 kawaii 品牌语气，但首屏不再因头部过高而压缩地图可视区域。"
    - "页面首次打开时，无需滚动即可看到顶部栏下方的大块地图内容。"
    - "交互提示浮层与装饰元素继续贴合顶部栏位置，不与头部或地图发生遮挡冲突。"
  artifacts:
    - path: "apps/web/src/App.vue"
      provides: "更紧凑的 topbar 高度、与之同步的壳层留白，以及恢复首屏地图可视区域的布局样式"
  key_links:
    - from: "apps/web/src/App.vue"
      to: "apps/web/src/App.vue"
      via: "topbar 高度变量同步驱动 poster-shell padding、notice 顶部偏移与 spark 装饰定位"
      pattern: "--topbar-height|poster-shell__topbar|poster-shell|app-shell__notice|app-shell__spark--left"
    - from: "apps/web/src/App.vue"
      to: "LeafletMapStage"
      via: "poster-shell__experience / poster-shell__stage 剩余高度分配"
      pattern: "poster-shell__experience|poster-shell__stage|LeafletMapStage"
---

<objective>
修复 kawaii 主题下顶部栏过高导致地图首屏被明显挤压的问题，压缩头部占位并恢复首页地图可视面积。

Purpose: 当前 `App.vue` 中 fixed topbar 的实际渲染高度、内部间距与壳层顶部留白没有严格对齐，导致顶部栏视觉上偏厚，地图第一屏可见区域不足。
Output: 一个只聚焦 `App.vue` 的执行计划，完成顶部栏压缩、相关偏移联动调整，以及首页首屏可视区域的回归验证。
</objective>

<execution_context>
@/Users/huangjingping/.codex/get-shit-done/workflows/execute-plan.md
@/Users/huangjingping/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@CLAUDE.md
@apps/web/src/App.vue
@apps/web/src/styles/tokens.css
@apps/web/src/styles/global.css
@.planning/quick/260408-mom-topbar-title/260408-mom-SUMMARY.md

<interfaces>
当前 `App.vue` 里与该问题直接相关的壳层结构：

```vue
<main class="poster-shell">
  <header class="poster-shell__topbar" data-region="topbar">
    <div class="poster-shell__brand">
      <p class="poster-shell__brand-kicker">Travel Diary</p>
      <h1 class="poster-shell__brand-title" data-display="true">旅记</h1>
      <p class="poster-shell__brand-subtitle">收集每次落点的心动坐标</p>
    </div>
    <div class="poster-shell__topbar-slot" aria-hidden="true"></div>
  </header>

  <div v-if="interactionNotice" class="app-shell__notice">...</div>

  <section class="poster-shell__experience" data-region="map-shell">
    <LeafletMapStage class="poster-shell__stage" />
  </section>
</main>
```

当前 CSS 关系的关键点：

```css
.app-shell {
  --topbar-height: 4.5rem;
  --topbar-height-mobile: 4rem;
}

.poster-shell {
  padding: calc(var(--topbar-height) + var(--space-lg)) var(--space-xl) var(--space-xl);
}

.poster-shell__topbar {
  min-height: var(--topbar-height);
  padding: 0.9rem var(--space-xl) 0.82rem;
}

.app-shell__notice {
  top: calc(var(--topbar-height) + var(--space-md));
}
```

执行时以这组联动为修复中心：压缩 topbar 本体后，必须同步更新 `poster-shell` 顶部留白、notice 顶部偏移、左侧 spark 装饰位置，避免留下新的空白或重叠。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 App.vue 内压缩顶部栏占位并回收地图首屏高度</name>
  <files>apps/web/src/App.vue</files>
  <action>
仅在 `App.vue` 内做最小布局修复，不改 `LeafletMapStage`、Pinia、notice 定时器或全局 token 文件，按下面顺序收紧壳层：

1. 重新定义顶部栏的占位策略，让“保留给壳层的高度”与“头部真实渲染高度”一致；不要继续使用会叠加出额外高度的 `min-height + 较大上下 padding` 组合。优先把顶部栏目标高度压到紧凑的一档，并为桌面/窄屏分别保留明确值。
2. 收紧 `poster-shell__topbar` 的垂直 padding、`poster-shell__brand` 的 gap，以及 `brand-kicker / brand-title / brand-subtitle` 的字号和行高，保留 kawaii 语气与现有文案，但让头部整体更贴近“轻量导航栏”而不是“海报头图”。如窄屏仍过高，可只在窄屏隐藏或弱化 subtitle；主标题“旅记”必须保留。
3. 同步更新 `poster-shell` 顶部 padding、`app-shell__notice` 顶部锚点、`app-shell__spark--left` 起始位置，以及任何依赖 `--topbar-height` 的偏移值，确保头部缩短后不会留下一截空白带，也不会让提示浮层压住顶部栏。
4. 保持 `poster-shell__experience` 和 `poster-shell__stage` 的剩余高度分配逻辑，让回收的空间直接返还给地图首屏；不要改变地图容器层级、不要引入新的包裹节点、不要触碰地图内部逻辑。

本任务要显式遵守 quick 约束：改动只留在 `App.vue`，视觉方向延续现有 kawaii theme，不新增设计系统层面的 token 或重构。
  </action>
  <verify>
    <automated>pnpm --filter @trip-map/web exec vue-tsc --noEmit</automated>
  </verify>
  <done>`App.vue` 中顶部栏明显变矮，壳层保留的顶部空间与实际头部高度一致；地图首屏可见区域恢复，且提示浮层、装饰 spark 与顶部栏之间没有新的重叠或空白断层。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 验证首页首屏地图可视区域已恢复</name>
  <what-built>已在 `App.vue` 内压缩 fixed topbar，并同步回收壳层顶部留白与提示浮层偏移。</what-built>
  <action>按下面步骤做人工验收，确认这次 quick 修复确实把首屏地图可视区域还给用户，而不是只通过了类型检查。</action>
  <how-to-verify>
1. 运行 `pnpm dev:web`，打开本地首页（通常是 `http://localhost:5173`）。
2. 在常见桌面视口下确认顶部栏仍显示 kawaii 风格的品牌信息，但视觉高度显著低于当前版本，地图卡片在第一屏占据主要面积。
3. 刷新页面并触发一次会显示 `interactionNotice` 的交互，确认提示条出现在顶部栏下方，不与顶部栏重叠，也不把地图再次明显向下挤压。
4. 如有窄屏预览，再检查一次：主标题“旅记”仍可见，地图第一屏没有被超高头部压缩到只剩很浅一条。
  </how-to-verify>
  <verify>
    <automated>pnpm --filter @trip-map/web exec vue-tsc --noEmit</automated>
  </verify>
  <done>人工确认桌面与窄屏下的顶部栏高度已收敛，地图第一屏恢复主要可视面积，且提示浮层位置正常。</done>
  <resume-signal>输入“approved”继续，或直接描述仍然过高/遮挡的具体现象。</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @trip-map/web exec vue-tsc --noEmit` 通过。
- 首页在桌面视口下无需滚动即可看到主要地图区域，顶部栏不再占据过多垂直空间。
- `interactionNotice` 与装饰 spark 的位置已跟随新头部高度同步，不出现重叠或残留空白带。
</verification>

<success_criteria>
- 顶部栏保持 kawaii 品牌感，但视觉高度收敛到轻量导航层级。
- 地图恢复首屏主要可视面积，用户一打开页面就能看到足够多的地图内容。
- 修复范围保持在 `App.vue` 壳层，不触碰地图业务逻辑和全局主题体系。
</success_criteria>

<output>
After completion, create `.planning/quick/260408-nw1-kawaii/260408-nw1-SUMMARY.md`
</output>

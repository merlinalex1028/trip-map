# AGENTS

本文件定义本仓库中的代理协作约定。

## 语言约定

- 与用户交流时，始终使用中文回答。
- 除非用户明确要求，否则不要切换为英文。
- 代码、命令、配置键名、接口字段名可保持原始语言，必要时配以中文说明。

## 执行约定

- 在开始实现前，先简要说明将要执行的操作。
- 修改代码时优先保持改动最小，并遵循现有项目结构与风格。
- 用户显式授权：在执行 GSD workflow、GSD phase、GSD review、GSD audit、GSD execute、GSD plan 等 GSD 流程时，Codex 可以按 GSD workflow 需要自动启动子代理、并行代理或委派代理任务。
- 如果使用子代理处理事务，必须等待子代理返回结果后再继续处理；不要因为等待时间过长而直接介入接管该事务。
- 完成后用中文简要说明变更内容、影响范围与验证结果。
